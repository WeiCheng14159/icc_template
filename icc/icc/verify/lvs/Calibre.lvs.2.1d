//  @(#) TSMC 0.13UM CALIBRE ERC/LVS COMMAND FILE V21d  May 12,2006
//  @(#) FOR TSMC 0.13um LOGIC SALICIDE FSG IMD (1P8M,1.2V/2.5V) (CL013G)
//  @(#) SPICE MODEL DOCUMENT:  T-013-LO-SP-004   V2.1                    
//  @(#) COMMAND FILE DOCUMENT: T-013-LO-SP-004-C1
//***************************************************************************
//
// Sheet Resistance table
VARIABLE  RVPSL  7.67		//  P+Poly resistor w/i Silicide (Width>=2um)
VARIABLE  RVPSS  7.67		//  P+Poly resistor w/i Silicide (Width<2um)
VARIABLE  RVNSL  7.31		//  N+Poly resistor w/i Silicide (Width>=2um)
VARIABLE  RVNSS  7.31		//  N+Poly resistor w/i Silicide (Width<2um)
VARIABLE  RVNIL  6.65		//  N+Diff resistor w/i Silicide (Width>=2um)
VARIABLE  RVNIS  6.65		//  N+Diff resistor w/i Silicide (Width<2um)
VARIABLE  RVND  72.75		//  N+Diff resistor w/o Silicide
VARIABLE  RVPIL  7.25		//  P+Diff resistor w/i Silicide (Width>=2um)
VARIABLE  RVPIS  7.25		//  P+Diff resistor w/i Silicide (Width<2um)
VARIABLE  RVPD  145.87		//  P+Diff resistor w/o Silicide
VARIABLE  RVWO  409		//  NWell resistor under OD
VARIABLE  RVWR  1090		//  NWell resistor under STI
VARIABLE  RVM1  0.090		//  Metal-1  resistor
VARIABLE  RVM2  0.057		//  Metal-2  resistor
VARIABLE  RVM3  0.057		//  Metal-3  resistor
VARIABLE  RVM4  0.057		//  Metal-4  resistor
VARIABLE  RVM5  0.057		//  Metal-5  resistor
VARIABLE  RVM6  0.057		//  Metal-6  resistor
VARIABLE  RVM7  0.057		//  Metal-7  resistor
VARIABLE  RVME  0.0225		//  Top Metal resistor
VARIABLE  RVNR  260.09		//  N+Poly resistor w/o Silicide
VARIABLE  RVPR  319.55		//  P+Poly resistor w/o Silicide
VARIABLE  DWPI  -0.070e-6	//  P+Diff resistor w/i Silicide Delta_W
VARIABLE  DWWR  0.451e-6	//  NWell resistor under STI Delta_W
VARIABLE  DWNR  0.07157e-6	//  N+Poly resistor w/o Silicide Delta_W
VARIABLE  DLNR  0.02051e-6	//  N+Poly resistor w/o Silicide Delta_L
VARIABLE  DDNR  3.91E-05            //  N+Poly resistor w/o Silicide Rend0
VARIABLE  DWWO  0.224e-6	//  NWell resistor under OD Delta_W
VARIABLE  DWPR  0.07953e-6	//  P+Poly resistor w/o Silicide Delta_W
VARIABLE  DLPR  0.04698e-6	//  P+Poly resistor w/o Silicide Delta_L
VARIABLE  DDPR  1.01E-04           //  P+Poly resistor w/o Silicide Rend0
VARIABLE  DWNS  -0.055e-6	//  N+Poly resistor w/i Silicide Delta_W
VARIABLE  DWPS  -0.035e-6	//  P+Poly resistor w/i Silicide Delta_W
VARIABLE  DWND  0.011e-6	        //  N+Diff resistor w/o Silicide Delta_W
VARIABLE  DWPD  0.068e-6	//  P+Diff resistor w/o Silicide Delta_W
VARIABLE  DWNI  -0.057e-6	//  N+Diff resistor w/i Silicide Delta_W
// END VARIABLE SETTING
//********************************************************************
// PROCESS VARIABLE SETTING
#DEFINE GENERIC
//*******************************
//*******************************
// RESISTANCE VALUE VARIABLE SETTING
//#DEFINE ACCURACY
//*******************************
//*************************************************************************************************
//
// ***!!! NOTICE !!!***
//
//    This LVS file ,named as "LVS sheet resistance file", only contains the 
// sheet resistance table which is corresponding to the sheet resistance value
// specified in the spice model document. Please be aware of that this lvs file
// can't be ran alone, otherwise you will get a syntax error message! 
//    Another LVS rule file which is called the "LVS main deck" contains all
// the LVS devices extraction, circuit comparsion and connectivity extraction
// coding and also need the "LVS sheet restance file" for sheet resistance
// reference.
//    To obtain a complete LVS deck, you need to finish the following procedures.
//
// Procedures:
//
// 1. Please download the most update "LVS main deck" from TSMC-online system.
//    Currently, the "LVS main deck" is packed together with the "LVS sheet 
//    resistance file" in  TSMC 0.13UM LOGIC SALICIDE FSG IMD (1P8M,1.2V/2.5V) (CL013G) 
//    process (Doc. T-013-LO-SP-004-C1). This LVS main deck is for all 0.13um
//    FSG process.
//
// 2. Starting to unpack the compressed file (from step 1) and choose the 
//    "LVS main deck" base on the metalization option of your chip.
//    (EX: 1P8M , 1P7M, 1P6M ......)
//
// 3. Under unix environment, using the unix command 'cat' to combine this
//    deck (LVS sheet resistance file) with the "LVS main deck" chose in 
//    step 2 to obtain the complete LVS deck.
//
//    EX: (assuming % is the unix prompt)
//
//    For 1P8M:
//    %cat T-013-LO-SP-004-C1_V21a.res LVS_RC_Calibre_0.13um_8M.lvs > CL013G_1P8M.lvs
//
//    For 1P7M:
//    %cat T-013-LO-SP-004-C1_V21a.res LVS_RC_Calibre_0.13um_7M.lvs > CL013G_1P7M.lvs
//
//    **where "T-013-LO-SP-004-C1_V21a.res" is the "LVS sheet 
//      resistance file" for all metalization processes (1P3M~1P8M)
//    **"LVS_RC_Calibre_0.13um_8M.lvs" is the "LVS main deck" for 1P8M process.
//    **"LVS_RC_Calibre_0.13um_7M.lvs" is the "LVS main deck" for 1P7M process.
//
//    As the result, CL013G_1P8M.lvs is the complete LVS command file for this process.
//
//    SPECIAL NOTICE:
//
//      1. Though the "LVS main deck" contains all the devices for TSMC 0.11um 
//         processes, it dosen't mean that you can use all the deivces in your 
//         desgin. For what kind of devices that can be used, please refered to 
//         the Spice Model Card corresponded to your process.
//      2. There is a variable setting for the accuracy of the resistance value.
//         It will look like below in the file:
//         //#DEFINE ACCURACY       ==> this means using simple and easy way to get the 
//					resistance value r=rsh*L/W
//					(This is the default setting)
//	   If you want to get the more accuracy resistance value, you can un-comment the "//".
//         It will look like below in the file:
//         #DEFINE ACCURACY         ==> this means using accuracy way to get the resistance value.
//
//*************************************************************************************************

// xCalibre/calibre  RC/LVS command file based on the follow calibre command file//
/////////////////////////////////////////////////////////////////////////////////
// 
//////////////////////////////////////////////////////////////////////////////// 
// 
// DISCLAIMER   
// 
// The information contained herein is provided by TSMC on an "AS IS" basis 
// without any warranty, and TSMC has no obligation to support or otherwise 
// maintain the information.  TSMC disclaims any representation that the 
// information does not infringe any intellectual property rights or proprietary 
// rights of any third parties.  There are no other warranties given by TSMC, 
// whether express, implied or statutory, including, without limitation, implied 
// warranties of merchantability and fitness for a particular purpose. 
// 
// STATEMENT OF USE 
// 
// This information contains confidential and proprietary information of TSMC. 
// No part of this information may be reproduced, transmitted, transcribed, 
// stored in a retrieval system, or translated into any human or computer 
// language, in any form or by any means, electronic, mechanical, magnetic, 
// optical, chemical, manual, or otherwise, without the prior written permission 
// of TSMC.  This information was prepared for informational purpose and is for 
// use by TSMC's customers only.  TSMC reserves the right to make changes in the 
// information at any time and without notice. 
// 
//////////////////////////////////////////////////////////////////////////////// 
// 
//                           CHANGE HISTORY 
//  
//   Date     version   Who                      Description 
// ---------- ------- --------- ------------------------------------------------- 
//  
// 
//////////////////////////////////////////////////////////////////////////////// 
// 
//                             DEVICE FORMULA 
//	 CORE == 1.0v or 1.2v  
//	 I/O  == 2.5v or 3.3v 
//   --------------- ----------------------------- ---------------------------------- 
//   NAME            DESCRIPTION                   DEVICE LAYER OPERATION 
//   --------------- ----------------------------- ---------------------------------- 
//   M[nch_sram]     CORE SRAM NMOS/DNW            ((NGATE-OD2-NTN)*DMSRM-HVTSRM-VTL_N-VTH_N) 
//   M[nch_sram_hvt] CORE HVT_SRAM NMOS/DNW        ((NGATE-OD2-NTN)*DMSRM*HVTSRM-VTL_N-VTH_N) 
//   M[NL]           CORE native device            ((NGATE-OD2)*NTN) 
//   M[N]            CORE Nominal VT NMOS          ((NGATE-OD2-NTN)-DNW) 
//   M[N1]           CORE Nominal VT NMOS/DNW      ((NGATE-OD2-NTN)*DNW) 
//   M[N1LV]         CORE LOW-VT NMOS/DNW          ((NGATE-OD2-NTN)*DNW*VTL_N) 
//   M[N1HV]         CORE HIGH-VT NMOS/DNW         ((NGATE-OD2-NTN)*DNW*VTH_N) 
//   M[NLV]          CORE LOW-VT NMOS	         (NGATE-OD2-NTN-DNW*VTL_N) 
//   M[NHV]          CORE High-VT NMOS             (NGATE-OD2-NTN-DNW*VTH_N) 
//   M[NN]           I/O native device             ((NGATE*OD2)*NTN) 
//   M[ND]           I/O Nominal VT NMOS           ((NGATE*OD2-NTN)-DNW) 
//   M[N2]           I/O Nominal VT NMOS/DNW       ((NGATE*OD2-NTN)*DNW) 
//   M[N2LV]         I/O LOW-VT NMOS/DNW           ((NGATE*OD2-NTN)*DNW*VTL_N) 
//   M[NHL]          I/O LOW-VT NMOS	         (NGATE*OD2-NTN-DNW*VTL_N) 
//   M[P]            CORE Nominal VT PMOS          (PGATE-OD2) 
//   M[pch_sram]     CORE SRAM PMOS                (PGATE-OD2*DMSRM-HVTSRM-VTL_P-VTH_P) 
//   M[pchpu_hvtsr_g] CORE HVT_SRAM PMOS           (PGATE-OD2*DMSRM*HVTSRM-VTL_P-VTH_P) 
//   M[PLV]          CORE LOW-VT PMOS	         (PGATE-OD2*VTL_P) 
//   M[PHV]          CORE High-VT PMOS             (PGATE-OD2*VTH_P) 
//   M[PD]           I/O Nominal VT PMOS           (PGATE*OD2) 
//   M[PHL]          I/O LOW-VT PMOS	         (PGATE*OD2*VTL_P) 
////   M[C1]           Varactor on thin oxide        (VARGT-OD2) 
////   M[C2]           Varactor on thick oxide       (VARGT*OD2) 
//   R[WO]           N-Well resistor under OD      (NWDMYi*NWELL*(OD*RPO)) 
//   R[WR]           N-Well resister under STI     (NWDMYi*NWELL-(OD*RPO)) 
//   R[PR]           P+Poly resistor w/o Silicide  (RPDMYi*POLYG*PP*RPO*RHDMY) 
//   R[NR]           N+Poly resistor w/o Silicide  (RPDMYi*POLYG*NP*RPO*RHDMY) 
//   R[PS]           P+Poly resistor w/i Silicide  (RPDMYi*POLYG*PP-RPO) 
//   R[NS]           N+Poly resistor w/i Silicide  (RPDMYi*POLYG*NP-RPO) 
//   R[PD]           P+diff resistor w/o Silicide  (RPDMYi*OD*PP*RPO*RHDMY*NWELL) 
//   R[ND]           N+diff resistor w/o Silicide  (RPDMYi*OD*NP*RPO*RHDMY-NWELL) 
//   R[PI]           P+diff resistor w/i Silicide  (RPDMYi*OD*PP-RPO*NWELL) 
//   R[NI]           N+diff resistor w/i Silicide  (RPDMYi*OD*NP-RPO-NWELL) 
//   R[M1]           METAL1 resistor               (RMDMY*METAL1) 
//   R[M2]           METAL2 resistor               (RMDMY*METAL2) 
//   R[M3]           METAL3 resistor               (RMDMY*METAL3) 
//   R[M4]           METAL4 resistor               (RMDMY*METAL4) 
//   R[M5]           METAL5 resistor               (RMDMY*METAL5) 
//   R[M6]           METAL6 resistor               (RMDMY*METAL6) 
//   R[M7]           METAL7 resistor               (RMDMY*METAL7) 
//   R[ME]           METAL8 resistor               (RMDMY*METAL8) 
//   D[DP]           CORE diode P+/NW              (DIODMY*OD*PP-OD2*NWELL) 
//   D[DN]           CORE diode N+/PW              (DIODMY*OD*NP-OD2*PSUB) 
//   D[DW]           CORE diode NW/PW              (DIODMY*NWELL-OD2) 
//   D[DPH]          CORE High-VT diode P+/NW      (DIODMY*OD*PP-OD2*NWELL*VTH_P) 
//   D[DNH]          CORE High-VT diode N+/PW      (DIODMY*OD*NP-OD2*PSUB*VTH_N) 
//   D[DPL]          CORE Low-VT diode P+/NW       (DIODMY*OD*PP-OD2*NWELL*VTL_P) 
//   D[DNL]          CORE Low-VT diode N+/PW       (DIODMY*OD*NP-OD2*PSUB*VTL_N) 
//   D[D1N]          CORE native diode N+/PW       (DIODMY*OD*NP-OD2*PSUB*NTN) 
//   D[D1]           I/O diode P+/NW               (DIODMY*OD*PP*OD2*NWELL) 
//   D[D2]           I/O diode N+/PW               (DIODMY*OD*NP*OD2*PSUB) 
//   D[NDIO_lvt33]   I/O Low_VT diode N+/PW        (DIODMY*OD*NP*OD2*PSUB*VTL_N) 
//   D[PDIO_lvt33]   I/O Low_VT diode P+/NW        (DIODMY*OD*PP*OD2*NWELL*VTL_P) 
//   D[D3]           I/O diode NW/PW               (DIODMY*NWELL*OD2)    
//   D[D2N]          I/O native diode N+/PW        (DIODMY*OD*NP*OD2*PSUB*NTN) 
//   D[DB]           ESD diode                     ((((((((ESD3DMY ADN OD)  AND  
//                                                 NIMP) AND RPO) NOT NWELL) NOT  
//                                                 (POLY1 SIZE 0.2))) SIZE -0.8)  
//                                                 SIZE 0.6) 				        
//   Q[PV]           CORE PNP-BJT                  (BJTDMY*OD*PP-OD2*NWELL) 
//   Q[P1]           I/O PNP-BJT                   (BJTDMY*OD*PP*OD2*NWELL) 
//   Q[NV]           CORE NPN-BJT		         (BJTDMY*OD*NIMP*PWELL*DNW) 
// 
//   NC1             NMOS Varactor core            (VARGT-OD2) 
//   NC2             NMOS Varactor I/O             (VARGT*OD2) 
//   NP1             PMOS Varactor core            (PVARGT-OD2) 
//   NP2             PMOS Varactor I/O             (PVARGT*OD2) 
//   rnwod_m        N-Well resistor under OD      (NWDMY1*NW*(OD*RPO))  
//   rpodwo_m       P+diff resistor w/o Silicide  (RPDMY1*OD*PP*RPO*RH)  
//   rnodwo_m       N+diff resistor w/o Silicide  (RPDMY1*OD*NP*RPO*RH)  
//   rpodl_m        P+diff resistor w/i Silicide  (RPDMY1*OD*PP-RPO)  
//   rnodl_m        N+diff resistor w/i Silicide  (RPDMY1*OD*NP-RPO)  
//   rpods_m        P+diff resistor w/i Silicide  (RPDMY1*OD*PP-RPO)  
//   rnods_m        N+diff resistor w/i Silicide  (RPDMY1*OD*NP-RPO)  
//   rppolyhri      HRI High poly resistor        (RPDMYi*RPO*RHRIDMY*POLY*HRI) 
//   ----- ----------------------------- ---------------------------------- 
//   rnpolywo_dis     N+Poly resistor w/o Silicide(distributed)  (RPDMY1*POLYG*NP*RPO*RHDMY) 
//   rppolywo_dis     P+Poly resistor w/o Silicide(distributed)  (RPDMY1*POLYG*PP*RPO*RHDMY) 
//   rnpolys_dis      N+Poly resistor w/i Silicide(distributed)  (RPDMY1*POLYG*NP*RPO*RHDMY) (w<2um) 
//   rppolys_dis      P+Poly resistor w/i Silicide(distributed)  (RPDMY1*POLYG*PP*RPO*RHDMY) (w<2um) 
//   rppolyhri_dis    HRI High poly resistor(distributed    )    (RPDMY1*RPO*RHRIDMY*POLY*HRI) 
//   rnpolyl_dis      N+Poly resistor w/i Silicide(distributed)  (RPDMY1*POLYG*NP*RPO*RHDMY) (w>=2um) 
//   rppolyl_dis      P+Poly resistor w/i Silicide(distributed)  (RPDMY1*POLYG*PP*RPO*RHDMY) (w>=2um) 
//   ----- ----------------------------- ---------------------------------- 
//   * NGATE = OD * POLYG * NP - NWELL 
//   * PGATE = OD * POLYG * PP * NWELL 
//   * VARGT = OD * POLYG * NP * NWELL * VARDMY 
// 
//////////////////////////////////////////////////////////////////////////////// 
// 
//LVS_GENERATOR_2005.06.8
// LVS/RC switch option
// --------------------
//#define RC_DECK    //uncomment this line when this deck would be used as a RC command file
//#define ZERO_NRS_NRD    //uncomment this line when this deck would set NRS=0 and NRD=0 
// POWER and GROUND name string variable setting
VARIABLE POWER_NAME  "VDDPST"  "TAVDD"  "AVDDG"  "VD33"  "TAVD33"  "DVDD"  "HVDDWELL"  "AVDDBG"  "AHVDDB"  "TAVDDPST"  "DHVDD"  "AHVDDG"  "AVDDR"  "AHVDDWELL"  "VDD"  "VDWELL"  "VDD5V"  "TAVD33PST"  "AHVDDR"  "AVDD"  "VDDG"  "VDDSA"  "AVDWELL"  "AHVDD"  "AVDDB" "VDD1"

VARIABLE GROUND_NAME  "AGND"  "VSS"  "AHVSSR"  "AHVSSUB"  "AVSS"  "VSSPST"  "VSSG"  "AVSSB"  "VSSUB"  "AHVSS"  "AVSSG"  "TAVSS"  "VS33"  "AVSSBG"  "AVSSUB"  "TAVSSPST"  "DVSS"  "AHVSSB"  "AVSSR"  "AHVSSG"  "DHVSS"  "GND"  "HVSSUB" "VSS1"

//#########################################################################
//# ENVIRONMENT SETTING                              
//#########################################################################
// LVS option
// --------------------
//#define extract_as_ad
//

//LVS HEAP DIRECTORY "/tmp"
#define MODEL_NAME
//#DEFINE extract_dnwdio    
#IFDEF RC_DECK
 #DEFINE ACCURACY
#ENDIF 

LAYOUT PRIMARY   "CHIP"
LAYOUT PATH      "./CHIP_pr.gds"
LAYOUT SYSTEM    GDSII
LAYOUT CASE      YES
//LAYOUT PATH    "./layout.spi"
//LAYOUT SYSTEM  SPICE

SOURCE PRIMARY   "traffic_light"
SOURCE PATH      "./source.spi"
SOURCE SYSTEM    SPICE

DRC RESULTS DATABASE "calibre_drc.db" ASCII // ASCII or GDSII
DRC SUMMARY REPORT "calibre_drc.sum"

LVS REPORT "lvs.rep"
LVS REPORT MAXIMUM 1000 // ALL

#IFDEF RC_DECK
//MASK RESULTS DATABASE NONE //"mask.db"
#ELSE
MASK RESULTS DATABASE NONE //"mask.db"
#ENDIF

#IFDEF RC_DECK
//MASK SVDB DIRECTORY "svdb" QUERY
#ELSE
MASK SVDB DIRECTORY "svdb" QUERY
#ENDIF
LVS ISOLATE SHORTS YES CELL PRIMARY && NAME "?"

LVS REPORT OPTION S

PRECISION 1000
RESOLUTION 1
UNIT LENGTH U

#IFDEF RC_DECK
//UNIT CAPACITANCE FF
#ELSE
UNIT CAPACITANCE FF
#ENDIF

TEXT DEPTH PRIMARY
PORT DEPTH PRIMARY

FLAG SKEW YES
FLAG OFFGRID YES

LVS SPICE PREFER PINS	         YES
LVS ABORT ON SUPPLY ERROR        NO
LVS ALL CAPACITOR PINS SWAPPABLE YES
LVS RECOGNIZE GATES              NONE
LVS IGNORE PORTS                 NO
LVS CHECK PORT NAMES             YES
LVS REDUCE PARALLEL BIPOLAR      YES
LVS REDUCE PARALLEL MOS          YES
LVS REDUCE PARALLEL DIODES       YES
LVS REDUCE PARALLEL CAPACITORS   YES
LVS REDUCE PARALLEL RESISTORS    YES
LVS REDUCE SERIES RESISTORS      YES      //Smashes series resistors
LVS REDUCE SERIES CAPACITORS     YES      //Smashes series capacitors
LVS REDUCE SPLIT GATES           NO       //Smashes MOS split-gates.
//LVS FILTER UNUSED OPTION B D E O
LVS FILTER UNUSED OPTION AB RC RE RG
LVS PROPERTY RESOLUTION MAXIMUM 65536 // ALL

// should be modify
LAYOUT TOP LAYER M1 VIA1 M2 VIA2 M3 VIA3 M4 VIA4 M5 VIA5 M6 VIA6 M7 VIA7 M8

VIRTUAL CONNECT COLON NO

LVS POWER NAME POWER_NAME 
LVS GROUND NAME GROUND_NAME 
//#############################################
//# MAPPING                                   #
//#############################################
LAYER CTM        77  //  For MIM device
LAYER CBM        88  //  For MIM device
#IFDEF LP
LAYER LVID       73  //  FOR LOW_POWER Low_VT device
 VTL_N = COPY LVID
 VTL_P = COPY LVID
#ENDIF
LAYER HVTSRM     60  //  HVT SRAM
LAYER DNW        1  //  DNW -- Deep N-Well
LAYER NTN        11  //   Native Device Blocked Implant
LAYER NWELL      3  //  NW -- N-Well
LAYER OD         7  8  6  //  OD -- Thin Oxide
LAYER OD2        15  //  OD2-- Thick Oxide
LAYER PP         25  //  PP -- P+ S/D Implantation
LAYER NP         26  //  NP -- N+ S/D Implantation
LAYER POLYG      17  //  PO -- Poly Si
#IFNDEF LP
LAYER VTL_N      12  //  CORE Low VT NMOS
#ENDIF
#IFNDEF LP
LAYER VTL_P      13  //  CORE Low VT PMOS
#ENDIF
LAYER VTH_N      67  //  CORE High VT NMOS
LAYER VTH_P      68  //  CORE High VT PMOS
LAYER CONT       30  //  CO -- Contact Window
LAYER M1         31  //  M1 -- Metal-1
LAYER VIA1       51  //  Via1 Hole
LAYER M2         32  //  M2 -- Metal-2
LAYER VIA2       52  //  Via2 Hole
LAYER M3         33  //  M3 -- Metal-3
LAYER VIA3       53  //  Via3 Hole
LAYER M4         34  //  M4 -- Metal-4
LAYER VIA4       54  //  Via4 Hole
LAYER M5         35  //  M5 -- Metal-5
LAYER VIA5       55  //  Via5 Hole
LAYER M6         36  //  M6 -- Metal-6
LAYER VIA6       56  //  Via6 Hole
LAYER M7         37  //  M7 -- Metal-7
LAYER VIA7       57  //  Via7 Hole
LAYER M8         38  //  M8 -- Metal-8
LAYER VIAD       167  //  Via hole above MetalTop to connect MetalTop to MD
LAYER MD         168  //  MD above MetalTop for redistribution interconnection
LAYER RPO        29  //  RPO-- Resist Protection Oxide
LAYER ESD3DMY    147  //  ESD3DMY
LAYER PSUB2      111  //  dummy layer for subtrate isolation
LAYER RODMY      49  //  dummy layer to remove OD
LAYER BJTDMY     110  //  dummy layer to form BJT
LAYER RHRIDMY    120  //  HRI high poly resistor dummy layer
LAYER HRI        70  //  HRI high poly resistor implantation
LAYER DIODMY     119  //  dummy layer to form diode
LAYER NWDMYi    300
LAYER MAP 114 DATATYPE 0 300 //  dummy layer to form N-Well resistor
LAYER NWDMY1    301
LAYER MAP 114 DATATYPE 1 301 //  dummy layer to form N-Well three terminals resistor
LAYER RPDMYi    302
LAYER MAP 115 DATATYPE 0 302 //  dummy layer to form OD/POLY resistor
LAYER RPDMY1    303
LAYER MAP 115 DATATYPE 1 303 //  dummy layer to form OD/POLY three terminals resistor
LAYER RMDMY      116  //  dummy layer to form METAL resistor
LAYER RMDMYi1   304
LAYER MAP 116 DATATYPE 1 304 //  dummy layer to form METAL1 resistor
LAYER RMDMYi2   305
LAYER MAP 116 DATATYPE 2 305 //  dummy layer to form METAL2 resistor
LAYER RMDMYi3   306
LAYER MAP 116 DATATYPE 3 306 //  dummy layer to form METAL3 resistor
LAYER RMDMYi4   307
LAYER MAP 116 DATATYPE 4 307 //  dummy layer to form METAL4 resistor
LAYER RMDMYi5   308
LAYER MAP 116 DATATYPE 5 308 //  dummy layer to form METAL5 resistor
LAYER RMDMYi6   309
LAYER MAP 116 DATATYPE 6 309 //  dummy layer to form METAL6 resistor
LAYER RMDMYi7   310
LAYER MAP 116 DATATYPE 7 310 //  dummy layer to form METAL7 resistor
LAYER RMDMYi8   311
LAYER MAP 116 DATATYPE 8 311 //  dummy layer to form METAL8 resistor
LAYER VARDMY     143  //  dummy layer to form Varactor
LAYER RHDMY      117  //  dummy layer to form diffusion and poly resistor
LAYER DMSRM      50  //  dummy layer to form SRAM mos
LAYER DUM1      312
LAYER MAP 31 DATATYPE 1 312 //  dummy metal M1
LAYER M1SLOT    313
LAYER MAP 31 DATATYPE 2 313 //  M1 slot
LAYER DUM2      314
LAYER MAP 32 DATATYPE 1 314 //  dummy metal M2
LAYER M2SLOT    315
LAYER MAP 32 DATATYPE 2 315 //  M2 slot
LAYER DUM3      316
LAYER MAP 33 DATATYPE 1 316 //  dummy metal M3
LAYER M3SLOT    317
LAYER MAP 33 DATATYPE 2 317 //  M3 slot
LAYER DUM4      318
LAYER MAP 34 DATATYPE 1 318 //  dummy metal M4
LAYER M4SLOT    319
LAYER MAP 34 DATATYPE 2 319 //  M4 slot
LAYER DUM5      320
LAYER MAP 35 DATATYPE 1 320 //  dummy metal M5
LAYER M5SLOT    321
LAYER MAP 35 DATATYPE 2 321 //  M5 slot
LAYER DUM6      322
LAYER MAP 36 DATATYPE 1 322 //  dummy metal M6
LAYER M6SLOT    323
LAYER MAP 36 DATATYPE 2 323 //  M6 slot
LAYER DUM7      324
LAYER MAP 37 DATATYPE 1 324 //  dummy metal M7
LAYER M7SLOT    325
LAYER MAP 37 DATATYPE 2 325 //  M7 slot
LAYER DUM8      326
LAYER MAP 38 DATATYPE 1 326 //  dummy metal M8
LAYER M8SLOT    327
LAYER MAP 38 DATATYPE 2 327 //  M8 slot
LAYER DRAIN     328
LAYER MAP 6 DATATYPE 3 328 //  For RF MOS drain side using only
LAYER RFDMY      161  //  For RF Device using only ( some layout using datatype 1 )
LAYER IND_STD   329
LAYER MAP 144 DATATYPE 0 329 //  For RF Inductor device standard
LAYER IND_SYM   330
LAYER MAP 144 DATATYPE 1 330 //  For RF Inductor device symmetric
LAYER IND_CTi   331
LAYER MAP 144 DATATYPE 2 331 //  For RF Inductor device center-tap
LAYER IND_RAD   332
LAYER MAP 144 DATATYPE 30 332 //  For RF Inductor devices radious
LAYER MOMDMY     155  //  dummy layer for RTMOM devices
TEXT LAYER 131 ATTACH 131 metal1
PORT LAYER TEXT 131
TEXT LAYER 132 ATTACH 132 metal2
PORT LAYER TEXT 132
TEXT LAYER 133 ATTACH 133 metal3
PORT LAYER TEXT 133
TEXT LAYER 134 ATTACH 134 metal4
PORT LAYER TEXT 134
TEXT LAYER 135 ATTACH 135 metal5
PORT LAYER TEXT 135
TEXT LAYER 136 ATTACH 136 metal6
PORT LAYER TEXT 136
TEXT LAYER 137 ATTACH 137 metal7
PORT LAYER TEXT 137
TEXT LAYER 138 ATTACH 138 metal8
PORT LAYER TEXT 138
TEXT LAYER 159 ATTACH 159 metal9
PORT LAYER TEXT 159
#IFDEF RC_DECK
TEXT LAYER 149 ATTACH 149 poly
PORT LAYER TEXT 149
#ENDIF

#IFNDEF RC_DECK
LAYER MAP 149 TEXTTYPE 1 333
TEXT LAYER 333 ATTACH 333 poly
PORT LAYER TEXT 333
#ENDIF
//#############################################
//# LOGIC OPERATION POOL                      #
//#############################################

//Form BULK layer
DRC:1 = EXTENT
BULK  = SIZE DRC:1 BY 1.0

//* Define device -- nmos nch_sram
p1hr1 = POLYG AND RPO 
RPDMY = RPDMYi OR RPDMY1 
diff = OD NOT RODMY 
rp1 = RPDMY NOT INTERACT diff 
p1rdum = rp1 INTERACT POLYG  //  poly1 resistor dummy
rpop1 = p1hr1 INTERACT p1rdum 
hripo1 = rpop1 AND HRI 
rpop = rpop1 NOT INTERACT hripo1 
rpolywo1 = rpop AND RHDMY 
rpolywo2 = rpolywo1 AND RPO 
rpolywo = rpolywo2 INTERACT p1rdum 
poly1 = POLYG NOT rpolywo 
rpoly1 = POLYG AND p1rdum 
rpoly = rpoly1 NOT INTERACT RPO 
poly12 = poly1 NOT rpoly 
hripo2 = hripo1 AND RHRIDMY 
hripo3 = hripo2 NOT NP 
hripo41 = hripo3 NOT PP 
polytmp = poly12 NOT hripo41 
poly = polytmp NOT MOMDMY  //  POLY interconnect
dupo1 = poly INSIDE diff 
dupo2 = dupo1 NOT INTERACT CONT 
realpo = poly NOT dupo2 
rodwo1 = diff AND RHDMY 
rodwo2 = rodwo1 AND RPO 
drdum = RPDMY INTERACT diff  //  diff resistor dummy
rodwo = rodwo2 INTERACT drdum 
mdiff1 = diff NOT rodwo 
rod1 = diff AND drdum 
rod = rod1 NOT INTERACT RPO 
mdiff = mdiff1 NOT rod 
gate1 = realpo AND mdiff 
gate_s1 = gate1 AND DMSRM 
NWDMY = NWDMYi OR NWDMY1 
nxwell = NWELL NOT NWDMY  //  define N_well region
gatennw_s = gate_s1 NOT nxwell  //  gate not inside nwell
ngate_s1 = gatennw_s AND NP 
ngate_s2 = ngate_s1 NOT OD2 
ngate_s3 = ngate_s2 NOT VTH_N 
ngate_s4 = ngate_s3 NOT VTL_N 
ngate_s = ngate_s4 NOT HVTSRM  //  SRAM NMOS

//***** Define connectivity/via layer -- tndiff ****
nthin1 = mdiff AND NP  //  define N+ thin oxide
ydio_a = ESD3DMY AND OD 
ydio_c = SIZE ydio_a BY -0.2 
ydio_d = ydio_c AND NP 
ydio_e = ydio_d AND RPO 
ydio_f = ydio_e NOT NWELL 
ydio_g = ESD3DMY AND POLYG 
ydio_h = SIZE ydio_g BY 0.2 
ydio_i = ydio_f NOT ydio_h 
ydio_j = SIZE ydio_i BY -0.7 
ydio = SIZE ydio_j BY 0.5 
nthin = nthin1 NOT ydio 
tndiff = nthin NOT gate1  //  define N+ diffusion region

//***** Define connectivity/via layer -- psub ****
ppsub = BULK NOT NWELL  //  define total p_sub region
psub2s = SIZE PSUB2 BY 0.01 
psubband = psub2s NOT PSUB2  //  psub separator band (doughnut).
psub = ppsub NOT psubband 

//* Define device -- nmos nch_sram_hvt
ngate_s_hvt = ngate_s4 AND HVTSRM  //  HVT SRAM NMOS

//* Define device -- nmos NL
gate_a = gate1 NOT DMSRM 
gate = gate_a NOT RFDMY 
gatennw = gate NOT nxwell  //  gate not inside nwell
tngate1 = gatennw AND NP 
tngate2 = tngate1 NOT OD2 
nlgate1 = tngate2 AND NTN 
nlgate2 = nlgate1 NOT VTL_N 
nlgate3 = nlgate2 NOT VTH_N 
nlgate = nlgate3 NOT DNW  //  CORE Native Devies

//* Define device -- nmos N
tngate3 = tngate2 NOT NTN 
tngate4 = tngate3 NOT DNW 
nlvgate = tngate4 AND VTL_N  //  CORE LOW_VT NMOS
ngate1 = tngate4 NOT nlvgate 
nhvgate = tngate4 AND VTH_N  //  CORE HIGH_VT NMOS
ngate = ngate1 NOT nhvgate  //  CORE NMOS

//* Define device -- nmos N1
tdgate1 = tngate3 AND DNW 
n1lvgate = tdgate1 AND VTL_N  //  CORE LOW_VT NMOS on DNW
nrgate1 = tdgate1 NOT n1lvgate 
n1hvgate = tdgate1 AND VTH_N  //  CORE HIGH_VT NMOS on DNW
nrgate = nrgate1 NOT n1hvgate  //  CORE NMOS on DNW

//* Define device -- nmos NN
bngate1 = tngate1 AND OD2 
bnngate1 = bngate1 AND NTN 
bnngate2 = bnngate1 NOT VTL_N 
bnngate = bnngate2 NOT DNW  //  I/O Native Devices

//* Define device -- nmos ND
bngate2 = bngate1 NOT NTN 
bngate3 = bngate2 NOT DNW 
bngate = bngate3 NOT VTL_N  //  I/O NMOS

//* Define device -- nmos N2
bndgate = bngate2 AND DNW 
bnrgate = bndgate NOT VTL_N  //  I/O NMOS on DNW

//* Define device -- nmos N2LV
bndlvgate = bndgate AND VTL_N  //  I/O LOW_VT NMOS on DNW

//* Define device -- nmos NHL
bnlvgate = bngate3 AND VTL_N  //  I/O LOW_VT NMOS

//* Define device -- pmos pch_sram
gatenw_s = gate_s1 AND nxwell  //  gate inside nwell
pgate_s1 = gatenw_s AND PP 
pgate_s2 = pgate_s1 NOT OD2 
pgate_s3 = pgate_s2 NOT VTH_P 
pgate_s4 = pgate_s3 NOT VTL_P 
pgate_s = pgate_s4 NOT HVTSRM  //  SRAM PMOS

//***** Define connectivity/via layer -- tpdiff ****
pthin = mdiff AND PP  //  define P+ thin oxide
tpdiff = pthin NOT gate1  //  define P+ diffusion region

//* Define device -- pmos pchpu_hvtsr_g
pgate_s_hvt = pgate_s4 AND HVTSRM  //  HVT SRAM PMOS

//* Define device -- pmos P
gatenw = gate AND nxwell  //  gate inside nwell
tpgate1 = gatenw AND PP 
tpgate2 = tpgate1 NOT OD2 
plvgate = tpgate2 AND VTL_P  //  CORE LOW_VT PMOS
pgate1 = tpgate2 NOT plvgate 
phvgate = tpgate2 AND VTH_P  //  CORE HIGH_VT PMOS
pgate = pgate1 NOT phvgate  //  CORE PMOS

//* Define device -- pmos PD
bpgate1 = tpgate1 AND OD2 
bpgate = bpgate1 NOT VTL_P  //  I/O PMOS

//* Define device -- pmos PHL
bplvgate = bpgate1 AND VTL_P  //  I/O LOW_VT PMOS

//* Define device -- diode DP
pdio1 = tpdiff AND DIODMY 
pdio2 = pdio1 AND nxwell 
pdio3 = pdio2 NOT OD2 
cpdio1 = pdio3 NOT VTH_P 
cpdio = cpdio1 NOT VTL_P  //  CORE P+/NW DIODE

//* Define device -- diode DN
ndio1 = tndiff AND DIODMY 
ndio2 = ndio1 AND psub 
ndio3 = ndio2 NOT OD2 
ndio4 = ndio3 NOT NTN 
cndio1 = ndio4 NOT VTH_N 
cndio = cndio1 NOT VTL_N  //  CORE N+/PW DIODE

//* Define device -- diode DW
DIONW = DIODMY OUTSIDE tpdiff 
nwdio1 = nxwell AND DIONW 
cnwdio = nwdio1 NOT OD2  //  CORE NW/PW diode

//* Define device -- diode DPH
pdioh = pdio3 AND VTH_P  //  CORE P+/NW HIGH_VT DIODE

//* Define device -- diode DNH
ndioh = ndio4 AND VTH_N  //  CORE N+/PW HIGH_VT DIODE

//* Define device -- diode DPL
pdiol = pdio3 AND VTL_P  //  CORE P+/NW Low_VT DIODE

//* Define device -- diode DNL
ndiol = ndio4 AND VTL_N  //  CORE N+/PW Low_VT DIODE

//* Define device -- diode D1N
nndio1 = ndio3 AND NTN 
cndion1 = nndio1 NOT VTH_N 
cndion = cndion1 NOT VTL_N  //  CORE N+/PW Native DIODE

//* Define device -- diode D1
ipdioa = pdio2 AND OD2 
ipdio = ipdioa NOT VTL_P  //  I/O  P+/NW DIODE		

//* Define device -- diode D2
indio1 = ndio2 AND OD2 
indioa = indio1 NOT NTN 
indio = indioa NOT VTL_N  //  I/O N+/PW DIODE

//* Define device -- diode D3
inwdio = nwdio1 AND OD2  //  I/O NW/PW diode

//* Define device -- diode D2N
indion = indio1 AND NTN  //  I/O N+/PW Native DIODE

//***** Define connectivity/via layer -- ydio_hole ****
ydio_hole = HOLES ydio 

//* Define device -- diode NDIO_lvt33
indio_lvt = indio1 AND VTL_N  //  I/O N+/PW Low_VT DIODE

//* Define device -- diode PDIO_lvt33
ipdio_lvt = ipdioa AND VTL_P  //  I/O  P+/NW Low_VT DIODE

//* Define device -- bjt PV
ttdiff = tpdiff AND BJTDMY 
nbase1a = nxwell NOT INTERACT DNW 
nbase1 = nbase1a AND BJTDMY 
cemit1 = ttdiff AND nbase1  //  define BJT[PV] emitter

//***** Define connectivity/via layer -- coll1 ****
collsur = psub TOUCH nbase1 
coll1a = collsur OR nbase1  //  define BJT[PV] collector
collsura = collsur TOUCH cemit1 
coll1b = coll1a NOT collsura 
coll1 = coll1b NOT cemit1  //  define pnp collector

//***** Define connectivity/via layer -- cemit1a ****
cemit1a = ttdiff AND nbase1 

//* Define device -- npn NV
npnrg = DNW AND BJTDMY  //  define NPN region
nndiff = tndiff AND npnrg 
pbase1 = psub AND npnrg  //  define BJT[NV] base terminal
cnemit1 = nndiff AND pbase1  //  define BJT[NV] emitter

//***** Define connectivity/via layer -- ncoll1 ****
ncollrg = nxwell AND npnrg 
ncolls = ncollrg TOUCH pbase1 
ncoll1 = ncolls OR pbase1  //  define BJT[NV] collector

//***** Define connectivity/via layer -- cnemit1a ****
cnemit1a = nndiff AND pbase1  //  define BJT[NV] emitter

//* Define device -- r M1
M1X = M1 OR DUM1 
M1Y = M1X NOT M1SLOT 
mt1res_RM = RMDMY OR RMDMYi1 
mt1res = M1Y AND mt1res_RM  //  metal1 resistor

//***** Define connectivity/via layer -- metal1 ****
metal1z = M1Y NOT mt1res 
metal1 = metal1z NOT MOMDMY  //  metal1 interconnect

//* Define device -- r M2
M2X = M2 OR DUM2 
M2Y = M2X NOT M2SLOT 
mt2res_RM = RMDMY OR RMDMYi2 
mt2res = M2Y AND mt2res_RM  //  metal2 resistor

//***** Define connectivity/via layer -- metal2 ****
metal2z = M2Y NOT mt2res 
metal2 = metal2z NOT MOMDMY  //  metal2 interconnect

//* Define device -- r M3
M3X = M3 OR DUM3 
M3Y = M3X NOT M3SLOT 
mt3res_RM = RMDMY OR RMDMYi3 
mt3res = M3Y AND mt3res_RM  //  metal3 resistor

//***** Define connectivity/via layer -- metal3 ****
metal3z = M3Y NOT mt3res 
metal3 = metal3z NOT MOMDMY  //  metal3 interconnect

//* Define device -- r M4
M4X = M4 OR DUM4 
M4Y = M4X NOT M4SLOT 
mt4res_RM = RMDMY OR RMDMYi4 
mt4res = M4Y AND mt4res_RM  //  metal4 resistor

//***** Define connectivity/via layer -- metal4 ****
metal4z = M4Y NOT mt4res 
metal4 = metal4z NOT MOMDMY  //  metal4 interconnect

//* Define device -- r M5
M5X = M5 OR DUM5 
M5Y = M5X NOT M5SLOT 
mt5res_RM = RMDMY OR RMDMYi5 
mt5res = M5Y AND mt5res_RM  //  metal5 resistor

//***** Define connectivity/via layer -- metal5 ****
metal5z = M5Y NOT mt5res 
metal5 = metal5z NOT MOMDMY  //  metal5 interconnect

//* Define device -- r M6
M6X = M6 OR DUM6 
M6Y = M6X NOT M6SLOT 
mt6res_RM = RMDMY OR RMDMYi6 
mt6res = M6Y AND mt6res_RM  //  metal6 resistor

//***** Define connectivity/via layer -- metal6 ****
metal6z = M6Y NOT mt6res 
metal6 = metal6z NOT MOMDMY  //  metal6 interconnect

//* Define device -- r M7
M7X = M7 OR DUM7 
M7Y = M7X NOT M7SLOT 
mt7res_RM = RMDMY OR RMDMYi7 
mt7res = M7Y AND mt7res_RM  //  metal7 resistor

//***** Define connectivity/via layer -- metal7 ****
metal7z = M7Y NOT mt7res 
metal7 = metal7z NOT MOMDMY  //  metal7 interconnect

//* Define device -- r ME
M8X = M8 OR DUM8 
M8Y = M8X NOT M8SLOT 
mt8res_RM = RMDMY OR RMDMYi8 
mt8res = M8Y AND mt8res_RM  //  metal8 resistor

//***** Define connectivity/via layer -- metal8 ****
metal8z = M8Y NOT mt8res 
metal8 = metal8z NOT MOMDMY  //  metal8 interconnect

//* Define device -- xdev R(WR)
trwell = NWDMY AND NWELL  //  N-well resistor layer
drpo = diff AND RPO 
nwsti_r = trwell NOT drpo  //  NWDMY*NWELL-RPO-diff --> nwsti

//* Define device -- xdev R(WO)
nwod_r1 = trwell AND drpo 
nwod_r = nwod_r1 INTERACT NWDMYi  //  NWDMY*NWELL*RPO*diff --> nwod

//* Define device -- xdev R(PR)
rppolywo_r1 = rpolywo AND PP 
rppolywo_r2 = rppolywo_r1 INTERACT RPDMYi 
rppolywo_r = rppolywo_r2 NOT RFDMY  //  P+POLY resistor w/o silicide

//* Define device -- xdev R(NR)
rnpolywo_r1 = rpolywo AND NP 
rnpolywo_r = rnpolywo_r1 INTERACT RPDMYi  //  N+POLY resistor w/o silicide

//* Define device -- xdev R(PD)
rpodwo1 = rodwo AND PP 
rpodwo_r1 = rpodwo1 AND NWELL 
rpodwo_r = rpodwo_r1 INTERACT RPDMYi  //  P+OD resistor w/o silicide

//* Define device -- xdev R(ND)
rnodwo1 = rodwo AND NP 
rnodwo_r1 = rnodwo1 NOT NWELL 
rnodwo_r = rnodwo_r1 INTERACT RPDMYi  //  N+OD resistor w/o silicide

//* Define device -- xdev R(PS1)
rppoly_r1 = rpoly AND PP 
rppoly_r1_eg = rppoly_r1 INSIDE EDGE POLYG 
l_rppoly_r1_eg = LENGTH rppoly_r1_eg  >= 2  < 99999999 
l_rppoly_r1 = rppoly_r1 WITH EDGE l_rppoly_r1_eg 
rf_lrppoly = l_rppoly_r1 AND RFDMY 
lrppoly_rf = rf_lrppoly INTERACT RPDMYi  //  P+POLY resistor w/i silicide (3-terminal rf w >=  2um) 
lrppoly_ra = l_rppoly_r1 NOT lrppoly_rf 
lrppoly_d = l_rppoly_r1 INTERACT RPDMY1  //  P+POLY resistor w/i silicide (3-terminal dis w >=  2um)
lrppoly_rb = lrppoly_ra NOT lrppoly_d 
s_rppoly_r1 = rppoly_r1 NOT l_rppoly_r1 
rf_srppoly = s_rppoly_r1 AND RFDMY 
srppoly_rf = rf_srppoly INTERACT RPDMYi  //  P+POLY resistor w/i silicide (3-terminal rf w < 2um)
srppoly_ra = s_rppoly_r1 NOT srppoly_rf 
srppoly_d = s_rppoly_r1 INTERACT RPDMY1  //  P+POLY resistor w/i silicide (3-terminal dis w < 2um)
srppoly_rb = srppoly_ra NOT srppoly_d 
rppoly_r = lrppoly_rb OR srppoly_rb  //  P+POLY resistor w/i silicide
rppoly_edge = rppoly_r INSIDE EDGE POLYG 
l_rppoly = LENGTH rppoly_edge  >= 2  < 99999999 
rppolyl_r = rppoly_r WITH EDGE l_rppoly  //   P+POLY resistor w/i silicide >=  2

//* Define device -- xdev R(PS2)
rppolys_r = rppoly_r NOT rppolyl_r  //   P+POLY resistor w/i silicide < 2

//* Define device -- xdev R(NS1)
rnpoly_r1 = rpoly AND NP 
rnpoly_r = rnpoly_r1 INTERACT RPDMYi  //  N+POLY resistor w/i silicide
rnpoly_edge = rnpoly_r INSIDE EDGE POLYG 
l_rnpoly = LENGTH rnpoly_edge  >= 2  < 99999999 
rnpolyl_r = rnpoly_r WITH EDGE l_rnpoly  //   N+POLY resistor w/i silicide >=  2

//* Define device -- xdev R(NS2)
rnpolys_r = rnpoly_r NOT rnpolyl_r  //   N+POLY resistor w/i silicide < 2

//* Define device -- xdev R(PI1)
rpod1 = rod AND PP 
rpod_r1 = rpod1 AND NWELL 
rpod_r = rpod_r1 INTERACT RPDMYi  //  P+OD resistor w/i silicide
rpod_edge = rpod_r INSIDE EDGE diff 
l_rpod = LENGTH rpod_edge  >= 2  < 99999999 
rpodl_r = rpod_r WITH EDGE l_rpod  //   P+OD resistor w/i silicide >=  2

//* Define device -- xdev R(PI2)
rpods_r = rpod_r NOT rpodl_r  //   P+OD resistor w/i silicide < 2

//* Define device -- xdev R(NI1)
rnod1 = rod AND NP 
rnod_r1 = rnod1 NOT NWELL 
rnod_r = rnod_r1 INTERACT RPDMYi  //  N+OD resistor w/i silicide
rnod_edge = rnod_r INSIDE EDGE diff 
l_rnod = LENGTH rnod_edge  >= 2  < 99999999 
rnodl_r = rnod_r WITH EDGE l_rnod  //   N+OD resistor w/i silicide >=  2

//* Define device -- xdev R(NI2)
rnods_r = rnod_r NOT rnodl_r  //   N+OD resistor w/i silicide < 2

//* Define device -- xdev NC1
vargt1 = gatenw AND NP 
vargt2 = vargt1 AND VARDMY 
vargt3 = vargt2 NOT OD2 
vargt = vargt3 NOT RFDMY  //  Varactor on thin oxide

//* Define device -- xdev NC2
bvargt1 = vargt2 AND OD2 
bvargt = bvargt1 NOT RFDMY  //  Varactor on thick oxide

//* Define device -- xdev PC1
pvargt1 = gatennw AND PP 
pvargt2 = pvargt1 AND VARDMY 
pvargt = pvargt2 NOT OD2  //  Varactor on thin oxide

//* Define device -- xdev PC2
pbvargt = pvargt2 AND OD2  //  Varactor on thick oxide

//* Define device -- xdev rnwod_m
nwod_mr = nwod_r1 INTERACT NWDMY1  //  NWDMY*NWELL*RPO*diff --> nwod (3-terminals)

//* Define device -- xdev rpodwo_m
rpodwo_mr = rpodwo_r1 INTERACT RPDMY1  //  P+OD resistor w/o silicide (3-terminals)

//* Define device -- xdev rnodwo_m
rnodwo_mr = rnodwo_r1 INTERACT RPDMY1  //  N+OD resistor w/o silicide (3-terminals)

//* Define device -- xdev rpodl_m
rpod_mr1 = rpod_r1 INTERACT RPDMY1 
rpod_mr1_edge = rpod_mr1 INSIDE EDGE diff 
l_rpod_mr1 = LENGTH rpod_mr1_edge  >= 2  < 99999999 
rpodl_mr = rpod_mr1 WITH EDGE l_rpod_mr1  //  P+OD resistor w/i silicide (3-terminals)

//* Define device -- xdev rnodl_m
rnod_mr1 = rnod_r1 INTERACT RPDMY1 
rnod_mr1_edge = rnod_mr1 INSIDE EDGE diff 
l_rnod_mr1 = LENGTH rnod_mr1_edge  >= 2  < 99999999 
rnodl_mr = rnod_mr1 WITH EDGE l_rnod_mr1  //  N+OD resistor w/i silicide (3-terminals)

//* Define device -- xdev rpods_m
rpods_mr = rpod_mr1 NOT rpodl_mr  //  P+OD resistor w/i silicide (3-terminals)

//* Define device -- xdev rnods_m
rnods_mr = rnod_mr1 NOT rnodl_mr  //  N+OD resistor w/i silicide (3-terminals)

//* Define device -- xdev dnwpsub
dnwdio_b = nxwell INTERACT DNW 
dnwdio_a = nxwell INSIDE DNW 
dnwdio_k = dnwdio_b NOT dnwdio_a 
dnwdio = DNW INTERACT dnwdio_k 
rwdio = DNW NOT nxwell 

//***** Define connectivity/via layer -- psub_term ****
psub_term_a = psub2s NOT PSUB2 
psub_term_b = BULK NOT psub_term_a 
psub_term_c = BULK AND DNW 
psub_term_d = BULK NOT psub_term_c 
psub_term = psub_term_b AND psub_term_d 

//* Define device -- xdev rppolyhri
hripo42 = hripo41 INTERACT RPDMYi 
hripo4 = hripo42 NOT RFDMY  //  HRI high poly resistor (2-terminals)

//* Define device -- xdev rppolyhri_dis
hripo_d = hripo41 INTERACT RPDMY1  //  HRI high poly resistor (3-terminals dis)

//***** Define connectivity/via layer -- poly_term ****
poly_term11 = POLYG AND RPDMY1 
rf_hripo = hripo42 AND RFDMY  //  HRI high poly resistor (3-terminals rf)
poly_term1 = poly_term11 OR rf_hripo 
poly_term2 = SIZE poly_term1 BY 0.005 
poly_term = poly_term2 NOT poly_term1 

//* Define device -- xdev rnpolywo_dis
rnpolywo_d = rnpolywo_r1 INTERACT RPDMY1  //  N+POLY resistor w/o silicide (3-terminals dis)

//* Define device -- xdev rppolywo_dis
rppolywo_d = rppolywo_r1 INTERACT RPDMY1  //  P+POLY resistor w/o silicide (3-terminals dis)

//* Define device -- xdev rnpolyl_dis
rnpoly_m = rnpoly_r1 INTERACT RPDMY1 
rnpoly_eg = rnpoly_m INSIDE EDGE POLYG 
l_rnpoly_eg = LENGTH rnpoly_eg  >= 2  < 99999999 
lrnpoly_d = rnpoly_m WITH EDGE l_rnpoly_eg  //  N+POLY resistor w/i silicide (3-terminal dis w >=  2um)

//* Define device -- xdev rnpolys_dis
srnpoly_d = rnpoly_m NOT lrnpoly_d  //   N+POLY resistor w/i silicide (3-terminal dis w < 2um)
cemit10 = AREA cemit1  > 99.999  < 100.001 
cemit5 = AREA cemit1  > 24.999  < 25.001 
cemit2 = AREA cemit1  > 3.999  < 4.001 
nemit10 = AREA cnemit1  > 99.999  < 100.001 
nemit5 = AREA cnemit1  > 24.999  < 25.001 
nemit2 = AREA cnemit1  > 3.999  < 4.001 

//***** Define connectivity/via layer -- odCont ****
odCont = CONT NOT POLYG 

//***** Define connectivity/via layer -- tiod ****
tioda = diff NOT tpdiff 
tiodb = tioda NOT tndiff 
tiod1 = tiodb NOT POLYG 
tiod2 = tiod1 NOT RPO 
tiod3 = tiod2 NOT RPDMY 
tiod = tiod3 NOT NWDMY  //  RPO, RPDMY, NWDMY cut ti-od

//***** Define connectivity/via layer -- metal9 ****
metal9 = COPY MD 

//***** Define connectivity/via layer -- n_psub ****
nw_dnw1 = NWELL INTERACT DNW 
nw_dnw_hole1 = HOLES nw_dnw1 
nw_dnw_hole = nw_dnw_hole1 INSIDE DNW 
nw_dnw = NWELL INTERACT nw_dnw_hole 
n_ppsub = BULK NOT nw_dnw 
n_psub = n_ppsub NOT psubband 

//***** Define connectivity/via layer -- nplug ****
nplug1 = tndiff AND nxwell  //  define N_well contact
nplug3 = nplug1 INTERACT CONT 
tpdiffco = tpdiff INTERACT CONT 
nplug2 = nplug1 INTERACT tpdiffco 
nplug = nplug3 OR nplug2  //   define N_well contact

//***** Define connectivity/via layer -- pplug ****
pplug1 = tpdiff AND psub  //  define p_sub contact
pplug3 = pplug1 INTERACT CONT 
tndiffco = tndiff INTERACT CONT 
pplug2 = pplug1 INTERACT tndiffco 
pplug = pplug3 OR pplug2  //  define p_sub contact

//***** Define connectivity/via layer -- nxwell_npn ****
ncollsa = ncolls TOUCH cnemit1 
nxwell_npn = ncolls NOT ncollsa 

//***** Define connectivity/via layer -- poly_nw_via ****
poly_nw_via = poly_term AND nxwell 

//***** Define connectivity/via layer -- ydio_ass ****
ydio_ass = SIZE ydio_j BY 0.5 

//***** Define connectivity/via layer -- dnwc ****
dnwc = DNW AND nxwell 

//***** Define connectivity/via layer -- n_pplug ****
n_pplug1 = tpdiff NOT nxwell 
n_pplug2 = n_pplug1 INTERACT CONT 
n_pplug = n_pplug2 AND n_psub 

//***** Define connectivity/via layer -- poly_psub_via ****
poly_psub_via = poly_term AND psub 

//***** Define connectivity/via layer -- poly_rf ****
poly_rf = poly INTERACT CONT 

//***** Define connectivity/via layer -- ntap ****
xwell = NWELL OUTSIDE NWDMY 
ntapx = tndiff AND xwell 
ntap1 = ntapx NOT BJTDMY 
ntap2 = ntap1 INTERACT CONT 
ntap3 = ntap1 INTERACT tpdiffco 
ntap = ntap2 OR ntap3 

//***** Define connectivity/via layer -- VIA8 ****
VIA8 = COPY VIAD 

//***** Define connectivity/via layer -- pl1co ****
pl1co = CONT AND POLYG 

//***** Define connectivity/via layer -- ptap ****
ptap1 = tpdiff AND psub 
ptap2 = ptap1 INTERACT CONT 
ptap3 = ptap1 INTERACT tndiffco 
ptap = ptap2 OR ptap3 
//#############################################
//# CONNECTION                                #
//#############################################
CONNECT nbase1 tndiff BY nplug
CONNECT poly_rf poly
CONNECT gate1 poly
CONNECT cemit1a tpdiff
CONNECT tndiff tpdiff
CONNECT tndiff nxwell_npn BY nplug
CONNECT tndiff ydio_hole
CONNECT tndiff ydio_ass
CONNECT ntap tndiff
CONNECT pbase1 tpdiff BY pplug
CONNECT cnemit1a tndiff
CONNECT metal1 tpdiff BY odCont
CONNECT metal1 poly_rf BY pl1co
CONNECT metal1 poly BY pl1co
CONNECT metal1 tndiff BY odCont
CONNECT metal2 metal1 BY VIA1
CONNECT metal3 metal2 BY VIA2
CONNECT metal4 metal3 BY VIA3
CONNECT metal5 metal4 BY VIA4
CONNECT tiod tpdiff
CONNECT tiod tndiff
CONNECT metal6 metal5 BY VIA5
CONNECT ptap tpdiff
CONNECT metal7 metal6 BY VIA6
CONNECT metal8 metal7 BY VIA7
CONNECT metal9 metal8 BY VIA8
CONNECT ncoll1 nxwell_npn

#IFDEF RC_DECK
CONNECT  poly gate
CONNECT  tpdiff psub_term BY pplug
CONNECT  tpdiff n_psub BY n_pplug
CONNECT  tpdiff psub BY pplug
CONNECT  tpdiff coll1 BY pplug
CONNECT  psub poly_term BY poly_psub_via
CONNECT  tndiff nxwell BY nplug
CONNECT  nxwell poly_term BY poly_nw_via
CONNECT  nxwell DNW BY dnwc
#ELSE
SCONNECT tpdiff psub_term BY pplug
SCONNECT tpdiff n_psub BY n_pplug
SCONNECT tpdiff psub BY pplug
SCONNECT tpdiff coll1 BY pplug
SCONNECT psub poly_term BY poly_psub_via
SCONNECT tndiff nxwell BY nplug
SCONNECT nxwell poly_term BY poly_nw_via
SCONNECT nxwell DNW BY dnwc
#ENDIF
//#############################################
//# FORM DEVICE                               #
//#############################################

#IFDEF RC_DECK
#IFDEF MODEL_NAME
DEVICE MN(N1) nrgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nch [
#ELSE
DEVICE MN(N1) nrgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(nrgate, tndiff ) + perimeter_inside(nrgate, tndiff)) / 2
   L=area(nrgate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,nthin)
   PI_D_OD = perimeter_inside(D,nthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]
#IFDEF MODEL_NAME
DEVICE MN(N) ngate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nch [
#ELSE
DEVICE MN(N) ngate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(ngate, tndiff ) + perimeter_inside(ngate, tndiff)) / 2
   L=area(ngate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,nthin)
   PI_D_OD = perimeter_inside(D,nthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]
#IFDEF MODEL_NAME
DEVICE MN(N2) bnrgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nch_33 [
#ELSE
DEVICE MN(N2) bnrgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(bnrgate, tndiff ) + perimeter_inside(bnrgate, tndiff)) / 2
   L=area(bnrgate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,nthin)
   PI_D_OD = perimeter_inside(D,nthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]
#IFDEF MODEL_NAME
DEVICE MN(N1LV) n1lvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nch_lvt [
#ELSE
DEVICE MN(N1LV) n1lvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(n1lvgate, tndiff ) + perimeter_inside(n1lvgate, tndiff)) / 2
   L=area(n1lvgate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,nthin)
   PI_D_OD = perimeter_inside(D,nthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]
#IFDEF MODEL_NAME
DEVICE MN(ND) bngate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nch_33 [
#ELSE
DEVICE MN(ND) bngate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(bngate, tndiff ) + perimeter_inside(bngate, tndiff)) / 2
   L=area(bngate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,nthin)
   PI_D_OD = perimeter_inside(D,nthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]
#IFDEF MODEL_NAME
DEVICE MN(N2LV) bndlvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nch_lvt33 [
#ELSE
DEVICE MN(N2LV) bndlvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(bndlvgate, tndiff ) + perimeter_inside(bndlvgate, tndiff)) / 2
   L=area(bndlvgate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,nthin)
   PI_D_OD = perimeter_inside(D,nthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]
#IFDEF MODEL_NAME
DEVICE MN(NHV) nhvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nch_hvt [
#ELSE
DEVICE MN(NHV) nhvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(nhvgate, tndiff ) + perimeter_inside(nhvgate, tndiff)) / 2
   L=area(nhvgate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,nthin)
   PI_D_OD = perimeter_inside(D,nthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]
#IFDEF MODEL_NAME
//DEVICE MN(nch_sram_hvt) ngate_s_hvt poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nchpd_hvtsr [
DEVICE MN(nch_sram_hvt) ngate_s_hvt poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nch_hvt [
#ELSE
DEVICE MN(nch_sram_hvt) ngate_s_hvt poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(ngate_s_hvt, tndiff ) + perimeter_inside(ngate_s_hvt, tndiff)) / 2
   L=area(ngate_s_hvt) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,nthin)
   PI_D_OD = perimeter_inside(D,nthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]
#IFDEF MODEL_NAME
DEVICE MN(N1HV) n1hvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nch_hvt [
#ELSE
DEVICE MN(N1HV) n1hvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(n1hvgate, tndiff ) + perimeter_inside(n1hvgate, tndiff)) / 2
   L=area(n1hvgate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,nthin)
   PI_D_OD = perimeter_inside(D,nthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]
#IFDEF MODEL_NAME
//DEVICE MN(nch_sram) ngate_s poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nchpd_sr [
DEVICE MN(nch_sram) ngate_s poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nch [
#ELSE
DEVICE MN(nch_sram) ngate_s poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(ngate_s, tndiff ) + perimeter_inside(ngate_s, tndiff)) / 2
   L=area(ngate_s) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,nthin)
   PI_D_OD = perimeter_inside(D,nthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]
#IFDEF MODEL_NAME
DEVICE MN(NL) nlgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nch_na [
#ELSE
DEVICE MN(NL) nlgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(nlgate, tndiff ) + perimeter_inside(nlgate, tndiff)) / 2
   L=area(nlgate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,nthin)
   PI_D_OD = perimeter_inside(D,nthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]
#IFDEF MODEL_NAME
DEVICE MN(NHL) bnlvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nch_lvt33 [
#ELSE
DEVICE MN(NHL) bnlvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(bnlvgate, tndiff ) + perimeter_inside(bnlvgate, tndiff)) / 2
   L=area(bnlvgate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,nthin)
   PI_D_OD = perimeter_inside(D,nthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]
#IFDEF MODEL_NAME
DEVICE MN(NN) bnngate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nch_na33 [
#ELSE
DEVICE MN(NN) bnngate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(bnngate, tndiff ) + perimeter_inside(bnngate, tndiff)) / 2
   L=area(bnngate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,nthin)
   PI_D_OD = perimeter_inside(D,nthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]
#IFDEF MODEL_NAME
DEVICE MN(NLV) nlvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> netlist model nch_lvt [
#ELSE
DEVICE MN(NLV) nlvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(nlvgate, tndiff ) + perimeter_inside(nlvgate, tndiff)) / 2
   L=area(nlvgate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,nthin)
   PI_D_OD = perimeter_inside(D,nthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]
#IFDEF MODEL_NAME
DEVICE MP(PD) bpgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> <OD> netlist model pch_33 [
#ELSE
DEVICE MP(PD) bpgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(bpgate, tpdiff ) + perimeter_inside(bpgate, tpdiff)) / 2 
   L=area(bpgate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,pthin)
   PI_D_OD = perimeter_inside(D,pthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]

#IFDEF MODEL_NAME
DEVICE MP(P) pgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> <OD> netlist model pch [
#ELSE
DEVICE MP(P) pgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(pgate, tpdiff ) + perimeter_inside(pgate, tpdiff)) / 2 
   L=area(pgate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,pthin)
   PI_D_OD = perimeter_inside(D,pthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]

#IFDEF MODEL_NAME
DEVICE MP(PHL) bplvgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> <OD> netlist model pch_lvt33 [
#ELSE
DEVICE MP(PHL) bplvgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(bplvgate, tpdiff ) + perimeter_inside(bplvgate, tpdiff)) / 2 
   L=area(bplvgate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,pthin)
   PI_D_OD = perimeter_inside(D,pthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]

#IFDEF MODEL_NAME
DEVICE MP(pchpu_hvtsr_g) pgate_s_hvt poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> <OD> netlist model pchpu_hvtsr [
#ELSE
DEVICE MP(pchpu_hvtsr_g) pgate_s_hvt poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(pgate_s_hvt, tpdiff ) + perimeter_inside(pgate_s_hvt, tpdiff)) / 2 
   L=area(pgate_s_hvt) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,pthin)
   PI_D_OD = perimeter_inside(D,pthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]

#IFDEF MODEL_NAME
//DEVICE MP(pch_sram) pgate_s poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> <OD> netlist model pchpu_sr [
DEVICE MP(pch_sram) pgate_s poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> <OD> netlist model pch [
#ELSE
DEVICE MP(pch_sram) pgate_s poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(pgate_s, tpdiff ) + perimeter_inside(pgate_s, tpdiff)) / 2 
   L=area(pgate_s) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,pthin)
   PI_D_OD = perimeter_inside(D,pthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]

#IFDEF MODEL_NAME
DEVICE MP(PHV) phvgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> <OD> netlist model pch_hvt [
#ELSE
DEVICE MP(PHV) phvgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(phvgate, tpdiff ) + perimeter_inside(phvgate, tpdiff)) / 2 
   L=area(phvgate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,pthin)
   PI_D_OD = perimeter_inside(D,pthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]

#IFDEF MODEL_NAME
DEVICE MP(PLV) plvgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> <OD> netlist model pch_lvt [
#ELSE
DEVICE MP(PLV) plvgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> <OD> [
#ENDIF
  property sa,sb,W,L,AS,AD,PD,PS,NRD,NRS
   W=(perimeter_coincide(plvgate, tpdiff ) + perimeter_inside(plvgate, tpdiff)) / 2 
   L=area(plvgate) / W
   s = enclosure_vector( OD, 60 )
   sa = W/sum(s::W/(s::a + 0.5 *L)) - 0.5*L
   sb = W/sum(s::W/(s::b + 0.5 *L)) - 0.5*L
   PI_S_OD = perimeter_inside(S,pthin)
   PI_D_OD = perimeter_inside(D,pthin)
   IF(PI_S_OD == 0) {
    AD = area(D) * W / PI_D_OD /2
    AS = AD
    PD = perimeter(D) * W /PI_D_OD /2
    PS = PD } 
   ELSE IF(PI_D_OD == 0) {
    AS = area(S) * W / PI_S_OD /2
    AD = AS
    PS = perimeter(S) * W /PI_S_OD /2
    PD = PS } 
   ELSE {
    AS = area(S) * W / PI_S_OD
    AD = area(D) * W / PI_D_OD
    PS = perimeter(S) * W /PI_S_OD
    PD = perimeter(D) * W /PI_D_OD }
 #IFDEF ZERO_NRS_NRD 
   NRS = 0
   NRD = 0
 #ELSE 
   NRS = AS / W / W
   NRD = AD / W / W
 #ENDIF 
]

#ELSE

#IFDEF extract_as_ad
DEVICE MN(N1) nrgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(nrgate, tndiff ) + perimeter_inside(nrgate, tndiff)) / 2
   L=area(nrgate) / W
  PI_S_OD = perimeter_inside(S,nthin)
  PI_D_OD = perimeter_inside(D,nthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,nthin)
  AD = area(D) * W /perimeter_inside(D,nthin)
  }
  ]
DEVICE MN(N) ngate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(ngate, tndiff ) + perimeter_inside(ngate, tndiff)) / 2
   L=area(ngate) / W
  PI_S_OD = perimeter_inside(S,nthin)
  PI_D_OD = perimeter_inside(D,nthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,nthin)
  AD = area(D) * W /perimeter_inside(D,nthin)
  }
  ]
DEVICE MN(N2) bnrgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(bnrgate, tndiff ) + perimeter_inside(bnrgate, tndiff)) / 2
   L=area(bnrgate) / W
  PI_S_OD = perimeter_inside(S,nthin)
  PI_D_OD = perimeter_inside(D,nthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,nthin)
  AD = area(D) * W /perimeter_inside(D,nthin)
  }
  ]
DEVICE MN(N1LV) n1lvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(n1lvgate, tndiff ) + perimeter_inside(n1lvgate, tndiff)) / 2
   L=area(n1lvgate) / W
  PI_S_OD = perimeter_inside(S,nthin)
  PI_D_OD = perimeter_inside(D,nthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,nthin)
  AD = area(D) * W /perimeter_inside(D,nthin)
  }
  ]
DEVICE MN(ND) bngate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(bngate, tndiff ) + perimeter_inside(bngate, tndiff)) / 2
   L=area(bngate) / W
  PI_S_OD = perimeter_inside(S,nthin)
  PI_D_OD = perimeter_inside(D,nthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,nthin)
  AD = area(D) * W /perimeter_inside(D,nthin)
  }
  ]
DEVICE MN(N2LV) bndlvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(bndlvgate, tndiff ) + perimeter_inside(bndlvgate, tndiff)) / 2
   L=area(bndlvgate) / W
  PI_S_OD = perimeter_inside(S,nthin)
  PI_D_OD = perimeter_inside(D,nthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,nthin)
  AD = area(D) * W /perimeter_inside(D,nthin)
  }
  ]
DEVICE MN(NHV) nhvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(nhvgate, tndiff ) + perimeter_inside(nhvgate, tndiff)) / 2
   L=area(nhvgate) / W
  PI_S_OD = perimeter_inside(S,nthin)
  PI_D_OD = perimeter_inside(D,nthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,nthin)
  AD = area(D) * W /perimeter_inside(D,nthin)
  }
  ]
DEVICE MN(nch_sram_hvt) ngate_s_hvt poly(G) tndiff(S) tndiff(D) psub(B) <nthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(ngate_s_hvt, tndiff ) + perimeter_inside(ngate_s_hvt, tndiff)) / 2
   L=area(ngate_s_hvt) / W
  PI_S_OD = perimeter_inside(S,nthin)
  PI_D_OD = perimeter_inside(D,nthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,nthin)
  AD = area(D) * W /perimeter_inside(D,nthin)
  }
  ]
DEVICE MN(N1HV) n1hvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(n1hvgate, tndiff ) + perimeter_inside(n1hvgate, tndiff)) / 2
   L=area(n1hvgate) / W
  PI_S_OD = perimeter_inside(S,nthin)
  PI_D_OD = perimeter_inside(D,nthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,nthin)
  AD = area(D) * W /perimeter_inside(D,nthin)
  }
  ]
DEVICE MN(nch_sram) ngate_s poly(G) tndiff(S) tndiff(D) psub(B) <nthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(ngate_s, tndiff ) + perimeter_inside(ngate_s, tndiff)) / 2
   L=area(ngate_s) / W
  PI_S_OD = perimeter_inside(S,nthin)
  PI_D_OD = perimeter_inside(D,nthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,nthin)
  AD = area(D) * W /perimeter_inside(D,nthin)
  }
  ]
DEVICE MN(NL) nlgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(nlgate, tndiff ) + perimeter_inside(nlgate, tndiff)) / 2
   L=area(nlgate) / W
  PI_S_OD = perimeter_inside(S,nthin)
  PI_D_OD = perimeter_inside(D,nthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,nthin)
  AD = area(D) * W /perimeter_inside(D,nthin)
  }
  ]
DEVICE MN(NHL) bnlvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(bnlvgate, tndiff ) + perimeter_inside(bnlvgate, tndiff)) / 2
   L=area(bnlvgate) / W
  PI_S_OD = perimeter_inside(S,nthin)
  PI_D_OD = perimeter_inside(D,nthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,nthin)
  AD = area(D) * W /perimeter_inside(D,nthin)
  }
  ]
DEVICE MN(NN) bnngate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(bnngate, tndiff ) + perimeter_inside(bnngate, tndiff)) / 2
   L=area(bnngate) / W
  PI_S_OD = perimeter_inside(S,nthin)
  PI_D_OD = perimeter_inside(D,nthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,nthin)
  AD = area(D) * W /perimeter_inside(D,nthin)
  }
  ]
DEVICE MN(NLV) nlvgate poly(G) tndiff(S) tndiff(D) psub(B) <nthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(nlvgate, tndiff ) + perimeter_inside(nlvgate, tndiff)) / 2
   L=area(nlvgate) / W
  PI_S_OD = perimeter_inside(S,nthin)
  PI_D_OD = perimeter_inside(D,nthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,nthin)
  AD = area(D) * W /perimeter_inside(D,nthin)
  }
  ]
DEVICE MP(PD) bpgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(bpgate, tpdiff ) + perimeter_inside(bpgate, tpdiff)) / 2 
   L=area(bpgate) / W
  PI_S_OD = perimeter_inside(S,pthin)
  PI_D_OD = perimeter_inside(D,pthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,pthin)
  AD = area(D) * W /perimeter_inside(D,pthin)
  }
  ]
DEVICE MP(P) pgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(pgate, tpdiff ) + perimeter_inside(pgate, tpdiff)) / 2 
   L=area(pgate) / W
  PI_S_OD = perimeter_inside(S,pthin)
  PI_D_OD = perimeter_inside(D,pthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,pthin)
  AD = area(D) * W /perimeter_inside(D,pthin)
  }
  ]
DEVICE MP(PHL) bplvgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(bplvgate, tpdiff ) + perimeter_inside(bplvgate, tpdiff)) / 2 
   L=area(bplvgate) / W
  PI_S_OD = perimeter_inside(S,pthin)
  PI_D_OD = perimeter_inside(D,pthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,pthin)
  AD = area(D) * W /perimeter_inside(D,pthin)
  }
  ]
DEVICE MP(pchpu_hvtsr_g) pgate_s_hvt poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(pgate_s_hvt, tpdiff ) + perimeter_inside(pgate_s_hvt, tpdiff)) / 2 
   L=area(pgate_s_hvt) / W
  PI_S_OD = perimeter_inside(S,pthin)
  PI_D_OD = perimeter_inside(D,pthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,pthin)
  AD = area(D) * W /perimeter_inside(D,pthin)
  }
  ]
DEVICE MP(pch_sram) pgate_s poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(pgate_s, tpdiff ) + perimeter_inside(pgate_s, tpdiff)) / 2 
   L=area(pgate_s) / W
  PI_S_OD = perimeter_inside(S,pthin)
  PI_D_OD = perimeter_inside(D,pthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,pthin)
  AD = area(D) * W /perimeter_inside(D,pthin)
  }
  ]
DEVICE MP(PHV) phvgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(phvgate, tpdiff ) + perimeter_inside(phvgate, tpdiff)) / 2 
   L=area(phvgate) / W
  PI_S_OD = perimeter_inside(S,pthin)
  PI_D_OD = perimeter_inside(D,pthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,pthin)
  AD = area(D) * W /perimeter_inside(D,pthin)
  }
  ]
DEVICE MP(PLV) plvgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) <pthin> [
  property W,L,AS,AD
   W=(perimeter_coincide(plvgate, tpdiff ) + perimeter_inside(plvgate, tpdiff)) / 2 
   L=area(plvgate) / W
  PI_S_OD = perimeter_inside(S,pthin)
  PI_D_OD = perimeter_inside(D,pthin)
  IF(PI_S_OD ==0) {
    AD = area(D) * W /PI_D_OD/2
    AS = AD }
  ELSE IF(PI_D_OD==0) {
    AS = area(S) * W /PI_S_OD/2
    AD = AS }
  ELSE {
  AS = area(S) * W /perimeter_inside(S,pthin)
  AD = area(D) * W /perimeter_inside(D,pthin)
  }
  ]


#ELSE
DEVICE MN(N1) nrgate poly(G) tndiff(S) tndiff(D) psub(B) [
   property W,L
   W=(perimeter_coincide(nrgate, tndiff ) + perimeter_inside(nrgate, tndiff)) / 2 
   L=area(nrgate) / W
]
DEVICE MN(N) ngate poly(G) tndiff(S) tndiff(D) psub(B) [
   property W,L
   W=(perimeter_coincide(ngate, tndiff ) + perimeter_inside(ngate, tndiff)) / 2 
   L=area(ngate) / W
]
DEVICE MN(N2) bnrgate poly(G) tndiff(S) tndiff(D) psub(B) [
   property W,L
   W=(perimeter_coincide(bnrgate, tndiff ) + perimeter_inside(bnrgate, tndiff)) / 2 
   L=area(bnrgate) / W
]
DEVICE MN(N1LV) n1lvgate poly(G) tndiff(S) tndiff(D) psub(B) [
   property W,L
   W=(perimeter_coincide(n1lvgate, tndiff ) + perimeter_inside(n1lvgate, tndiff)) / 2 
   L=area(n1lvgate) / W
]
DEVICE MN(ND) bngate poly(G) tndiff(S) tndiff(D) psub(B) [
   property W,L
   W=(perimeter_coincide(bngate, tndiff ) + perimeter_inside(bngate, tndiff)) / 2 
   L=area(bngate) / W
]
DEVICE MN(N2LV) bndlvgate poly(G) tndiff(S) tndiff(D) psub(B) [
   property W,L
   W=(perimeter_coincide(bndlvgate, tndiff ) + perimeter_inside(bndlvgate, tndiff)) / 2 
   L=area(bndlvgate) / W
]
DEVICE MN(NHV) nhvgate poly(G) tndiff(S) tndiff(D) psub(B) [
   property W,L
   W=(perimeter_coincide(nhvgate, tndiff ) + perimeter_inside(nhvgate, tndiff)) / 2 
   L=area(nhvgate) / W
]
DEVICE MN(nch_sram_hvt) ngate_s_hvt poly(G) tndiff(S) tndiff(D) psub(B) [
   property W,L
   W=(perimeter_coincide(ngate_s_hvt, tndiff ) + perimeter_inside(ngate_s_hvt, tndiff)) / 2 
   L=area(ngate_s_hvt) / W
]
DEVICE MN(N1HV) n1hvgate poly(G) tndiff(S) tndiff(D) psub(B) [
   property W,L
   W=(perimeter_coincide(n1hvgate, tndiff ) + perimeter_inside(n1hvgate, tndiff)) / 2 
   L=area(n1hvgate) / W
]
DEVICE MN(nch_sram) ngate_s poly(G) tndiff(S) tndiff(D) psub(B) [
   property W,L
   W=(perimeter_coincide(ngate_s, tndiff ) + perimeter_inside(ngate_s, tndiff)) / 2 
   L=area(ngate_s) / W
]
DEVICE MN(NL) nlgate poly(G) tndiff(S) tndiff(D) psub(B) [
   property W,L
   W=(perimeter_coincide(nlgate, tndiff ) + perimeter_inside(nlgate, tndiff)) / 2 
   L=area(nlgate) / W
]
DEVICE MN(NHL) bnlvgate poly(G) tndiff(S) tndiff(D) psub(B) [
   property W,L
   W=(perimeter_coincide(bnlvgate, tndiff ) + perimeter_inside(bnlvgate, tndiff)) / 2 
   L=area(bnlvgate) / W
]
DEVICE MN(NN) bnngate poly(G) tndiff(S) tndiff(D) psub(B) [
   property W,L
   W=(perimeter_coincide(bnngate, tndiff ) + perimeter_inside(bnngate, tndiff)) / 2 
   L=area(bnngate) / W
]
DEVICE MN(NLV) nlvgate poly(G) tndiff(S) tndiff(D) psub(B) [
   property W,L
   W=(perimeter_coincide(nlvgate, tndiff ) + perimeter_inside(nlvgate, tndiff)) / 2 
   L=area(nlvgate) / W
]
DEVICE MP(PD) bpgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) [
   property W,L
   W=(perimeter_coincide(bpgate, tpdiff ) + perimeter_inside(bpgate, tpdiff)) / 2 
   L=area(bpgate) / W
]
DEVICE MP(P) pgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) [
   property W,L
   W=(perimeter_coincide(pgate, tpdiff ) + perimeter_inside(pgate, tpdiff)) / 2 
   L=area(pgate) / W
]
DEVICE MP(PHL) bplvgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) [
   property W,L
   W=(perimeter_coincide(bplvgate, tpdiff ) + perimeter_inside(bplvgate, tpdiff)) / 2 
   L=area(bplvgate) / W
]
DEVICE MP(pchpu_hvtsr_g) pgate_s_hvt poly(G) tpdiff(S) tpdiff(D) nxwell(B) [
   property W,L
   W=(perimeter_coincide(pgate_s_hvt, tpdiff ) + perimeter_inside(pgate_s_hvt, tpdiff)) / 2 
   L=area(pgate_s_hvt) / W
]
DEVICE MP(pch_sram) pgate_s poly(G) tpdiff(S) tpdiff(D) nxwell(B) [
   property W,L
   W=(perimeter_coincide(pgate_s, tpdiff ) + perimeter_inside(pgate_s, tpdiff)) / 2 
   L=area(pgate_s) / W
]
DEVICE MP(PHV) phvgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) [
   property W,L
   W=(perimeter_coincide(phvgate, tpdiff ) + perimeter_inside(phvgate, tpdiff)) / 2 
   L=area(phvgate) / W
]
DEVICE MP(PLV) plvgate poly(G) tpdiff(S) tpdiff(D) nxwell(B) [
   property W,L
   W=(perimeter_coincide(plvgate, tpdiff ) + perimeter_inside(plvgate, tpdiff)) / 2 
   L=area(plvgate) / W
]


#ENDIF

#ENDIF


#IFNDEF RC_DECK
TRACE PROPERTY MN(N1) L L 0
TRACE PROPERTY MN(N1) W W 0
TRACE PROPERTY MN(N) L L 0
TRACE PROPERTY MN(N) W W 0
TRACE PROPERTY MN(N2) L L 0
TRACE PROPERTY MN(N2) W W 0
TRACE PROPERTY MN(N1LV) L L 0
TRACE PROPERTY MN(N1LV) W W 0
TRACE PROPERTY MN(ND) L L 0
TRACE PROPERTY MN(ND) W W 0
TRACE PROPERTY MN(N2LV) L L 0
TRACE PROPERTY MN(N2LV) W W 0
TRACE PROPERTY MN(NHV) L L 0
TRACE PROPERTY MN(NHV) W W 0
TRACE PROPERTY MN(nch_sram_hvt) L L 0
TRACE PROPERTY MN(nch_sram_hvt) W W 0
TRACE PROPERTY MN(N1HV) L L 0
TRACE PROPERTY MN(N1HV) W W 0
TRACE PROPERTY MN(nch_sram) L L 0
TRACE PROPERTY MN(nch_sram) W W 0
TRACE PROPERTY MN(NL) L L 0
TRACE PROPERTY MN(NL) W W 0
TRACE PROPERTY MN(NHL) L L 0
TRACE PROPERTY MN(NHL) W W 0
TRACE PROPERTY MN(NN) L L 0
TRACE PROPERTY MN(NN) W W 0
TRACE PROPERTY MN(NLV) L L 0
TRACE PROPERTY MN(NLV) W W 0
TRACE PROPERTY MP(PD) L L 0
TRACE PROPERTY MP(PD) W W 0
TRACE PROPERTY MP(P) L L 0
TRACE PROPERTY MP(P) W W 0
TRACE PROPERTY MP(PHL) L L 0
TRACE PROPERTY MP(PHL) W W 0
TRACE PROPERTY MP(pchpu_hvtsr_g) L L 0
TRACE PROPERTY MP(pchpu_hvtsr_g) W W 0
TRACE PROPERTY MP(pch_sram) L L 0
TRACE PROPERTY MP(pch_sram) W W 0
TRACE PROPERTY MP(PHV) L L 0
TRACE PROPERTY MP(PHV) W W 0
TRACE PROPERTY MP(PLV) L L 0
TRACE PROPERTY MP(PLV) W W 0
#ENDIF

#IFDEF RC_DECK
  #IFDEF MODEL_NAME
DEVICE Q(PV) cemit2 coll1 nbase1 cemit1a netlist model pnp2
  #ELSE
DEVICE Q(PV) cemit2 coll1 nbase1 cemit1a 
  #ENDIF
#ELSE
DEVICE Q(PV) cemit2 coll1 nbase1 cemit1a [
		property A
		A = area(cemit2)
]
#ENDIF
#IFDEF RC_DECK
  #IFDEF MODEL_NAME
DEVICE Q(PV) cemit10 coll1 nbase1 cemit1a netlist model pnp10
  #ELSE
DEVICE Q(PV) cemit10 coll1 nbase1 cemit1a 
  #ENDIF
#ELSE
DEVICE Q(PV) cemit10 coll1 nbase1 cemit1a [
		property A
		A = area(cemit10)
]
#ENDIF
#IFDEF RC_DECK
  #IFDEF MODEL_NAME
DEVICE Q(PV) cemit5 coll1 nbase1 cemit1a netlist model pnp5
  #ELSE
DEVICE Q(PV) cemit5 coll1 nbase1 cemit1a 
  #ENDIF
#ELSE
DEVICE Q(PV) cemit5 coll1 nbase1 cemit1a [
		property A
		A = area(cemit5)
]
#ENDIF

#IFNDEF RC_DECK
  TRACE PROPERTY Q(PV) A A 0
#ENDIF
#IFDEF RC_DECK
  #IFDEF MODEL_NAME
DEVICE Q(NV) nemit2 ncoll1 pbase1 cnemit1a netlist model npn2
  #ELSE
DEVICE Q(NV) nemit2 ncoll1 pbase1 cnemit1a 
  #ENDIF
#ELSE
DEVICE Q(NV) nemit2 ncoll1 pbase1 cnemit1a [
		property A
		A = area(nemit2)
]
#ENDIF
#IFDEF RC_DECK
  #IFDEF MODEL_NAME
DEVICE Q(NV) nemit10 ncoll1 pbase1 cnemit1a netlist model npn10
  #ELSE
DEVICE Q(NV) nemit10 ncoll1 pbase1 cnemit1a 
  #ENDIF
#ELSE
DEVICE Q(NV) nemit10 ncoll1 pbase1 cnemit1a [
		property A
		A = area(nemit10)
]
#ENDIF
#IFDEF RC_DECK
  #IFDEF MODEL_NAME
DEVICE Q(NV) nemit5 ncoll1 pbase1 cnemit1a netlist model npn5
  #ELSE
DEVICE Q(NV) nemit5 ncoll1 pbase1 cnemit1a 
  #ENDIF
#ELSE
DEVICE Q(NV) nemit5 ncoll1 pbase1 cnemit1a [
		property A
		A = area(nemit5)
]
#ENDIF

#IFNDEF RC_DECK
  TRACE PROPERTY Q(NV) A A 0
#ENDIF
#IFDEF MODEL_NAME
DEVICE D(PDIO_lvt33) ipdio_lvt tpdiff nxwell netlist model pdio_lvt33 
#ELSE
DEVICE D(PDIO_lvt33) ipdio_lvt tpdiff nxwell 
#ENDIF
  TRACE PROPERTY D(PDIO_lvt33) A A 0
#IFDEF MODEL_NAME
DEVICE D(D1N) cndion psub tndiff netlist model ndio_na 
#ELSE
DEVICE D(D1N) cndion psub tndiff 
#ENDIF
  TRACE PROPERTY D(D1N) A A 0
#IFDEF MODEL_NAME
DEVICE D(DW) cnwdio psub nxwell netlist model nwdio 
#ELSE
DEVICE D(DW) cnwdio psub nxwell 
#ENDIF
  TRACE PROPERTY D(DW) A A 0
#IFDEF MODEL_NAME
DEVICE D(D2N) indion psub tndiff netlist model ndio_na33 
#ELSE
DEVICE D(D2N) indion psub tndiff 
#ENDIF
  TRACE PROPERTY D(D2N) A A 0
#IFDEF MODEL_NAME
DEVICE D(DNH) ndioh psub tndiff netlist model ndio_hvt 
#ELSE
DEVICE D(DNH) ndioh psub tndiff 
#ENDIF
  TRACE PROPERTY D(DNH) A A 0
#IFDEF MODEL_NAME
DEVICE D(DN) cndio psub tndiff netlist model ndio 
#ELSE
DEVICE D(DN) cndio psub tndiff 
#ENDIF
  TRACE PROPERTY D(DN) A A 0
#IFDEF MODEL_NAME
DEVICE D(DPH) pdioh tpdiff nxwell netlist model pdio_hvt 
#ELSE
DEVICE D(DPH) pdioh tpdiff nxwell 
#ENDIF
  TRACE PROPERTY D(DPH) A A 0
#IFDEF MODEL_NAME
DEVICE D(DNL) ndiol psub tndiff netlist model ndio_lvt 
#ELSE
DEVICE D(DNL) ndiol psub tndiff 
#ENDIF
  TRACE PROPERTY D(DNL) A A 0
#IFDEF MODEL_NAME
DEVICE D(DP) cpdio tpdiff nxwell netlist model pdio 
#ELSE
DEVICE D(DP) cpdio tpdiff nxwell 
#ENDIF
  TRACE PROPERTY D(DP) A A 0
#IFDEF MODEL_NAME
DEVICE D(D1) ipdio tpdiff nxwell netlist model pdio_33 
#ELSE
DEVICE D(D1) ipdio tpdiff nxwell 
#ENDIF
  TRACE PROPERTY D(D1) A A 0
#IFDEF MODEL_NAME
DEVICE D(D2) indio psub tndiff netlist model ndio_33 
#ELSE
DEVICE D(D2) indio psub tndiff 
#ENDIF
  TRACE PROPERTY D(D2) A A 0
#IFDEF MODEL_NAME
DEVICE D(NDIO_lvt33) indio_lvt psub tndiff netlist model ndio_lvt33 
#ELSE
DEVICE D(NDIO_lvt33) indio_lvt psub tndiff 
#ENDIF
  TRACE PROPERTY D(NDIO_lvt33) A A 0
#IFDEF MODEL_NAME
DEVICE D(DB) ydio psub tndiff netlist model DIO_esd33 
#ELSE
DEVICE D(DB) ydio psub tndiff 
#ENDIF
  TRACE PROPERTY D(DB) A A 0
#IFDEF MODEL_NAME
DEVICE D(D3) inwdio psub nxwell netlist model nwdio 
#ELSE
DEVICE D(D3) inwdio psub nxwell 
#ENDIF
  TRACE PROPERTY D(D3) A A 0
#IFDEF MODEL_NAME
DEVICE D(DPL) pdiol tpdiff nxwell netlist model pdio_lvt 
#ELSE
DEVICE D(DPL) pdiol tpdiff nxwell 
#ENDIF
  TRACE PROPERTY D(DPL) A A 0
#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(M3) mt3res metal3 metal3 netlist model rm2_7s netlist element "X" [
  property w,l,R
  w = perimeter_coincide(mt3res,metal3)/2
  A = area(mt3res)
  l = A/w
  R = RVM3*l/w
  ]
  TRACE PROPERTY R(M3) w w 0
  TRACE PROPERTY R(M3) l l 0
  TRACE PROPERTY R(M3) R R 2
 #ELSE
  DEVICE R(M3) mt3res metal3 metal3 [
  property R
  w = perimeter_coincide(mt3res,metal3)/2
  A = area(mt3res)
  l = A/w
  R = RVM3*l/w
  ]
  TRACE PROPERTY R(M3) R R 2
 #ENDIF
#ELSE
  DEVICE R(M3) mt3res metal3 metal3 [RVM3]
  TRACE PROPERTY R(M3) R R 0
#ENDIF
#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(M4) mt4res metal4 metal4 netlist model rm2_7s netlist element "X" [
  property w,l,R
  w = perimeter_coincide(mt4res,metal4)/2
  A = area(mt4res)
  l = A/w
  R = RVM4*l/w
  ]
  TRACE PROPERTY R(M4) w w 0
  TRACE PROPERTY R(M4) l l 0
  TRACE PROPERTY R(M4) R R 2
 #ELSE
  DEVICE R(M4) mt4res metal4 metal4 [
  property R
  w = perimeter_coincide(mt4res,metal4)/2
  A = area(mt4res)
  l = A/w
  R = RVM4*l/w
  ]
  TRACE PROPERTY R(M4) R R 2
 #ENDIF
#ELSE
  DEVICE R(M4) mt4res metal4 metal4 [RVM4]
  TRACE PROPERTY R(M4) R R 0
#ENDIF
#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(ME) mt8res metal8 metal8 netlist model rm8s netlist element "X" [
  property w,l,R
  w = perimeter_coincide(mt8res,metal8)/2
  A = area(mt8res)
  l = A/w
  R = RVME*l/w
  ]
  TRACE PROPERTY R(ME) w w 0
  TRACE PROPERTY R(ME) l l 0
  TRACE PROPERTY R(ME) R R 2
 #ELSE
  DEVICE R(ME) mt8res metal8 metal8 [
  property R
  w = perimeter_coincide(mt8res,metal8)/2
  A = area(mt8res)
  l = A/w
  R = RVME*l/w
  ]
  TRACE PROPERTY R(ME) R R 2
 #ENDIF
#ELSE
  DEVICE R(ME) mt8res metal8 metal8 [RVME]
  TRACE PROPERTY R(ME) R R 0
#ENDIF
#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(M5) mt5res metal5 metal5 netlist model rm2_7s netlist element "X" [
  property w,l,R
  w = perimeter_coincide(mt5res,metal5)/2
  A = area(mt5res)
  l = A/w
  R = RVM5*l/w
  ]
  TRACE PROPERTY R(M5) w w 0
  TRACE PROPERTY R(M5) l l 0
  TRACE PROPERTY R(M5) R R 2
 #ELSE
  DEVICE R(M5) mt5res metal5 metal5 [
  property R
  w = perimeter_coincide(mt5res,metal5)/2
  A = area(mt5res)
  l = A/w
  R = RVM5*l/w
  ]
  TRACE PROPERTY R(M5) R R 2
 #ENDIF
#ELSE
  DEVICE R(M5) mt5res metal5 metal5 [RVM5]
  TRACE PROPERTY R(M5) R R 0
#ENDIF
#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(M6) mt6res metal6 metal6 netlist model rm2_7s netlist element "X" [
  property w,l,R
  w = perimeter_coincide(mt6res,metal6)/2
  A = area(mt6res)
  l = A/w
  R = RVM6*l/w
  ]
  TRACE PROPERTY R(M6) w w 0
  TRACE PROPERTY R(M6) l l 0
  TRACE PROPERTY R(M6) R R 2
 #ELSE
  DEVICE R(M6) mt6res metal6 metal6 [
  property R
  w = perimeter_coincide(mt6res,metal6)/2
  A = area(mt6res)
  l = A/w
  R = RVM6*l/w
  ]
  TRACE PROPERTY R(M6) R R 2
 #ENDIF
#ELSE
  DEVICE R(M6) mt6res metal6 metal6 [RVM6]
  TRACE PROPERTY R(M6) R R 0
#ENDIF
#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(M7) mt7res metal7 metal7 netlist model rm2_7s netlist element "X" [
  property w,l,R
  w = perimeter_coincide(mt7res,metal7)/2
  A = area(mt7res)
  l = A/w
  R = RVM7*l/w
  ]
  TRACE PROPERTY R(M7) w w 0
  TRACE PROPERTY R(M7) l l 0
  TRACE PROPERTY R(M7) R R 2
 #ELSE
  DEVICE R(M7) mt7res metal7 metal7 [
  property R
  w = perimeter_coincide(mt7res,metal7)/2
  A = area(mt7res)
  l = A/w
  R = RVM7*l/w
  ]
  TRACE PROPERTY R(M7) R R 2
 #ENDIF
#ELSE
  DEVICE R(M7) mt7res metal7 metal7 [RVM7]
  TRACE PROPERTY R(M7) R R 0
#ENDIF
#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(M1) mt1res metal1 metal1 netlist model rm1s netlist element "X" [
  property w,l,R
  w = perimeter_coincide(mt1res,metal1)/2
  A = area(mt1res)
  l = A/w
  R = RVM1*l/w
  ]
  TRACE PROPERTY R(M1) w w 0
  TRACE PROPERTY R(M1) l l 0
  TRACE PROPERTY R(M1) R R 2
 #ELSE
  DEVICE R(M1) mt1res metal1 metal1 [
  property R
  w = perimeter_coincide(mt1res,metal1)/2
  A = area(mt1res)
  l = A/w
  R = RVM1*l/w
  ]
  TRACE PROPERTY R(M1) R R 2
 #ENDIF
#ELSE
  DEVICE R(M1) mt1res metal1 metal1 [RVM1]
  TRACE PROPERTY R(M1) R R 0
#ENDIF
#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(M2) mt2res metal2 metal2 netlist model rm2_7s netlist element "X" [
  property w,l,R
  w = perimeter_coincide(mt2res,metal2)/2
  A = area(mt2res)
  l = A/w
  R = RVM2*l/w
  ]
  TRACE PROPERTY R(M2) w w 0
  TRACE PROPERTY R(M2) l l 0
  TRACE PROPERTY R(M2) R R 2
 #ELSE
  DEVICE R(M2) mt2res metal2 metal2 [
  property R
  w = perimeter_coincide(mt2res,metal2)/2
  A = area(mt2res)
  l = A/w
  R = RVM2*l/w
  ]
  TRACE PROPERTY R(M2) R R 2
 #ENDIF
#ELSE
  DEVICE R(M2) mt2res metal2 metal2 [RVM2]
  TRACE PROPERTY R(M2) R R 0
#ENDIF

// User define devices 
#IFDEF ACCURACY
 #IFDEF MODEL_NAME
//  Modify by cplin //
  DEVICE R(PR) rppolywo_r poly poly NETLIST MODEL rppolywo NETLIST ELEMENT "R" [
//  DEVICE R(PR) rppolywo_r poly poly NETLIST MODEL rppolywo NETLIST ELEMENT "X" [
//	property l,w,r
	property r
	w = perimeter_coincide(rppolywo_r,poly)/2
	l = area(rppolywo_r)/w
	WD = w-DWPR
	LD = l-DLPR
	Rend = DDPR/WD
	Rpure = RVPR*LD/WD
	r = 2*Rend+Rpure
  ]
//  TRACE PROPERTY R(PR) w w 0
//  TRACE PROPERTY R(PR) l l 0
  TRACE PROPERTY R(PR) r r 2
 #ELSE
  DEVICE R(PR) rppolywo_r poly poly [
	property r
	w = perimeter_coincide(rppolywo_r,poly)/2
	l = area(rppolywo_r)/w
	WD = w-DWPR
	LD = l-DLPR
	Rend = DDPR/WD
	Rpure = RVPR*LD/WD
	r = 2*Rend+Rpure
  ]
  TRACE PROPERTY R(PR) r r 2
 #ENDIF
#ELSE
  DEVICE R(PR) rppolywo_r poly poly [RVPR]
  TRACE PROPERTY R(PR) r r 2
#ENDIF

#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(PS) rppolyl_r poly poly NETLIST MODEL rppolyl NETLIST ELEMENT "X" [
	property l,w,r
	w = perimeter_coincide(rppolyl_r,poly)/2
	l = area(rppolyl_r)/w
	r = RVPSL*l/(w-DWPS)
  ]

  //TRACE PROPERTY R(PS) w w 0
  //TRACE PROPERTY R(PS) l l 0
  //TRACE PROPERTY R(PS) r r 2
 #ELSE
  DEVICE R(PS) rppolyl_r poly poly [
	property r
	w = perimeter_coincide(rppolyl_r,poly)/2
	l = area(rppolyl_r)/w
	r = RVPSL*l/(w-DWPS)
  ]
  //TRACE PROPERTY R(PS) r r 2
 #ENDIF
#ELSE
  DEVICE R(PS) rppolyl_r poly poly [RVPSL]
  //TRACE PROPERTY R(PS) r r 2
#ENDIF

#IFDEF MODEL_NAME
DEVICE nc1 vargt poly(PLUS) tndiff(MINUS) <nthin> NETLIST MODEL nmoscap  [
#ELSE
DEVICE nc1 vargt poly(PLUS) tndiff(MINUS) <nthin> [
#ENDIF
       property lr,wr
       nr = count(vargt)
       wr = perimeter_inside(vargt,nthin)/2/nr
       lr = area(vargt)/wr/nr
]
TRACE PROPERTY nc1 wr wr 0
TRACE PROPERTY nc1 lr lr 0

#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(PI) rpods_r tpdiff tpdiff NETLIST MODEL rpods NETLIST ELEMENT "X" [
	property l,w,r
	w = perimeter_coincide(rpods_r,tpdiff)/2
	l = area(rpods_r)/w
	r = RVPIS*l/(w-DWPI)
  ]

  TRACE PROPERTY R(PI) w w 0
  TRACE PROPERTY R(PI) l l 0
  TRACE PROPERTY R(PI) r r 2
 #ELSE
  DEVICE R(PI) rpods_r tpdiff tpdiff [
	property r
	w = perimeter_coincide(rpods_r,tpdiff)/2
	l = area(rpods_r)/w
	r = RVPIS*l/(w-DWPI)
  ]
  TRACE PROPERTY R(PI) r r 2
 #ENDIF
#ELSE
  DEVICE R(PI) rpods_r tpdiff tpdiff [RVPIS]
  TRACE PROPERTY R(PI) r r 2
#ENDIF

DEVICE rnods_m rnods_mr tndiff(PLUS) tndiff(MINUS) psub(B) (PLUS MINUS) [
   property w,l
   w = perimeter_coincide(rnods_mr,tndiff)/2
   l = area(rnods_mr)/w
]
TRACE PROPERTY rnods_m w w 0
TRACE PROPERTY rnods_m l l 0

#IFDEF MODEL_NAME
DEVICE nc2 bvargt poly(PLUS) tndiff(MINUS) <nthin> NETLIST MODEL nmoscap_33 [
#ELSE
DEVICE nc2 bvargt poly(PLUS) tndiff(MINUS) <nthin> [
#ENDIF
       property lr,wr
       nr = count(bvargt)
       wr = perimeter_inside(bvargt,nthin)/2/nr
       lr = area(bvargt)/wr/nr
]
TRACE PROPERTY nc2 wr wr 0
TRACE PROPERTY nc2 lr lr 0

DEVICE rppolys_dis srppoly_d poly(PLUS) poly(MINUS) poly_term(BULK) (PLUS MINUS) [
  property l,w
  w = perimeter_coincide(srppoly_d,poly)/2
  l = area(srppoly_d)/w
]
TRACE PROPERTY rppolys_dis w w 0
TRACE PROPERTY rppolys_dis l l 0

DEVICE rppolywo_dis rppolywo_d poly(PLUS) poly(MINUS) poly_term(BULK) (PLUS MINUS) [
  property l,w
  w = perimeter_coincide(rppolywo_d,poly)/2
  l = area(rppolywo_d)/w
]
TRACE PROPERTY rppolywo_dis w w 0
TRACE PROPERTY rppolywo_dis l l 0

#IFDEF MODEL_NAME
DEVICE pc1 pvargt poly(PLUS) tpdiff(MINUS) <pthin> NETLIST MODEL pmoscap [
#ELSE
DEVICE pc1 pvargt poly(PLUS) tpdiff(MINUS) <pthin> [
#ENDIF
       
       property lr,wr
       nr = count(pvargt)
       wr = perimeter_inside(pvargt,pthin)/2/nr
       lr = area(pvargt)/wr/nr
]
TRACE PROPERTY pc1 wr wr 0
TRACE PROPERTY pc1 lr lr 0

#IFDEF MODEL_NAME
DEVICE pc2 pbvargt poly(PLUS) tpdiff(MINUS) <pthin> NETLIST MODEL pmoscap_33 [
#ELSE
DEVICE pc2 pbvargt poly(PLUS) tpdiff(MINUS) <pthin> [
#ENDIF
       property lr,wr
       nr = count(pbvargt)
       wr = perimeter_inside(pbvargt,pthin)/2/nr
       lr = area(pbvargt)/wr/nr
]
TRACE PROPERTY pc2 wr wr 0
TRACE PROPERTY pc2 lr lr 0

DEVICE rnwod_m nwod_mr nxwell(PLUS) nxwell(MINUS) psub(B) (PLUS MINUS) [
   property w,l
   w = perimeter_coincide(nwod_mr,nxwell)/2
   l = area(nwod_mr)/w
]
TRACE PROPERTY rnwod_m w w 0
TRACE PROPERTY rnwod_m l l 0

#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(WO) nwod_r nxwell nxwell NETLIST MODEL rnwod NETLIST ELEMENT "X" [
	property l,w,r
	w = perimeter_coincide(nwod_r,nxwell)/2
	l = area(nwod_r)/w
	r = RVWO*l/(w-DWWO)
  ]

  TRACE PROPERTY R(WO) w w 0
  TRACE PROPERTY R(WO) l l 0
  TRACE PROPERTY R(WO) r r 2
 #ELSE
  DEVICE R(WO) nwod_r nxwell nxwell [
	property r
	w = perimeter_coincide(nwod_r,nxwell)/2
	l = area(nwod_r)/w
	r = RVWO*l/(w-DWWO)
  ]
  TRACE PROPERTY R(WO) r r 2
 #ENDIF
#ELSE
  DEVICE R(WO) nwod_r nxwell nxwell [RVWO]
  TRACE PROPERTY R(WO) r r 2
#ENDIF

DEVICE rnpolyl_dis lrnpoly_d poly(PLUS) poly(MINUS) poly_term(BULK) (PLUS MINUS) [
  property l,w
  w = perimeter_coincide(lrnpoly_d,poly)/2
  l = area(lrnpoly_d)/w
]
TRACE PROPERTY rnpolyl_dis w w 0
TRACE PROPERTY rnpolyl_dis l l 0

DEVICE rnodl_m rnodl_mr tndiff(PLUS) tndiff(MINUS) psub(B) (PLUS MINUS) [
   property w,l
   w = perimeter_coincide(rnodl_mr,tndiff)/2
   l = area(rnodl_mr)/w
]
TRACE PROPERTY rnodl_m w w 0
TRACE PROPERTY rnodl_m l l 0

DEVICE rpods_m rpods_mr tpdiff(PLUS) tpdiff(MINUS) nxwell(B) (PLUS MINUS) [
   property w,l
   w = perimeter_coincide(rpods_mr,tpdiff)/2
   l = area(rpods_mr)/w
]
TRACE PROPERTY rpods_m w w 0
TRACE PROPERTY rpods_m l l 0

#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(NS) rnpolys_r poly poly NETLIST MODEL rnpolys NETLIST ELEMENT "X" [
	property l,w,r
	w = perimeter_coincide(rnpolys_r,poly)/2
	l = area(rnpolys_r)/w
	r = RVNSS*l/(w-DWNS)
  ]

  TRACE PROPERTY R(NS) w w 0
  TRACE PROPERTY R(NS) l l 0
  TRACE PROPERTY R(NS) r r 2
 #ELSE
  DEVICE R(NS) rnpolys_r poly poly [
	property r
	w = perimeter_coincide(rnpolys_r,poly)/2
	l = area(rnpolys_r)/w
	r = RVNSS*l/(w-DWNS)
  ]
  TRACE PROPERTY R(NS) r r 2
 #ENDIF
#ELSE
  DEVICE R(NS) rnpolys_r poly poly [RVNSS]
  TRACE PROPERTY R(NS) r r 2
#ENDIF

#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(NI) rnodl_r tndiff tndiff NETLIST MODEL rnodl NETLIST ELEMENT "X" [
	property l,w,r
	w = perimeter_coincide(rnodl_r,tndiff)/2
	l = area(rnodl_r)/w
	r = RVNIL*l/(w-DWNI)
  ]

  //TRACE PROPERTY R(NI) w w 0
  //TRACE PROPERTY R(NI) l l 0
  //TRACE PROPERTY R(NI) r r 2
 #ELSE
  DEVICE R(NI) rnodl_r tndiff tndiff [
	property r
	w = perimeter_coincide(rnodl_r,tndiff)/2
	l = area(rnodl_r)/w
	r = RVNIL*l/(w-DWNI)
  ]
  //TRACE PROPERTY R(NI) r r 2
 #ENDIF
#ELSE
  DEVICE R(NI) rnodl_r tndiff tndiff [RVNIL]
  //TRACE PROPERTY R(NI) r r 2
#ENDIF

#IFDEF ACCURACY
 #IFDEF MODEL_NAME
//  Modify 10 lines by cplin //
  DEVICE R(ND) rnodwo_r tndiff tndiff NETLIST MODEL rnodwo NETLIST ELEMENT "R" [
//  DEVICE R(ND) rnodwo_r tndiff tndiff NETLIST MODEL rnodwo NETLIST ELEMENT "X" [
//	property l,w,r
	property r
	w = perimeter_coincide(rnodwo_r,tndiff)/2
	l = area(rnodwo_r)/w
	r = RVND*l/(w-DWND)
  ]

//  TRACE PROPERTY R(ND) w w 0
//  TRACE PROPERTY R(ND) l l 0
  TRACE PROPERTY R(ND) r r 2
 #ELSE
  DEVICE R(ND) rnodwo_r tndiff tndiff [
	property r
	w = perimeter_coincide(rnodwo_r,tndiff)/2
	l = area(rnodwo_r)/w
	r = RVND*l/(w-DWND)
  ]
  TRACE PROPERTY R(ND) r r 2
 #ENDIF
#ELSE
  DEVICE R(ND) rnodwo_r tndiff tndiff [RVND]
  TRACE PROPERTY R(ND) r r 2
#ENDIF

DEVICE rpodl_m rpodl_mr tpdiff(PLUS) tpdiff(MINUS) nxwell(B) (PLUS MINUS) [
   property w,l
   w = perimeter_coincide(rpodl_mr,tpdiff)/2
   l = area(rpodl_mr)/w
]
TRACE PROPERTY rpodl_m w w 0
TRACE PROPERTY rpodl_m l l 0

#IFDEF extract_dnwdio
DEVICE D(pwdnw) rwdio psub DNW
TRACE PROPERTY D(pwdnw) a a 0
DEVICE D(dnwpsub) dnwdio psub_term DNW [
   property a
   a = area(dnw)
]
TRACE PROPERTY D(dnwpsub) a a 0
#ENDIF

#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(PD) rpodwo_r tpdiff tpdiff NETLIST MODEL rpodwo NETLIST ELEMENT "X" [
	property l,w,r
	w = perimeter_coincide(rpodwo_r,tpdiff)/2
	l = area(rpodwo_r)/w
	r = RVPD*l/(w-DWPD)
  ]

  TRACE PROPERTY R(PD) w w 0
  TRACE PROPERTY R(PD) l l 0
  TRACE PROPERTY R(PD) r r 2
 #ELSE
  DEVICE R(PD) rpodwo_r tpdiff tpdiff [
	property r
	w = perimeter_coincide(rpodwo_r,tpdiff)/2
	l = area(rpodwo_r)/w
	r = RVPD*l/(w-DWPD)
  ]
  TRACE PROPERTY R(PD) r r 2
 #ENDIF
#ELSE
  DEVICE R(PD) rpodwo_r tpdiff tpdiff [RVPD]
  TRACE PROPERTY R(PD) r r 2
#ENDIF

#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(PS) rppolys_r poly poly NETLIST MODEL rppolys NETLIST ELEMENT "X" [
	property l,w,r
	w = perimeter_coincide(rppolys_r,poly)/2
	l = area(rppolys_r)/w
	r = RVPSS*l/(w-DWPS)
  ]

  TRACE PROPERTY R(PS) w w 0
  TRACE PROPERTY R(PS) l l 0
  TRACE PROPERTY R(PS) r r 2
 #ELSE
  DEVICE R(PS) rppolys_r poly poly [
	property r
	w = perimeter_coincide(rppolys_r,poly)/2
	l = area(rppolys_r)/w
	r = RVPSS*l/(w-DWPS)
  ]
  TRACE PROPERTY R(PS) r r 2
 #ENDIF
#ELSE
  DEVICE R(PS) rppolys_r poly poly [RVPSS]
  TRACE PROPERTY R(PS) r r 2
#ENDIF

#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(PI) rpodl_r tpdiff tpdiff NETLIST MODEL rpodl NETLIST ELEMENT "X" [
	property l,w,r
	w = perimeter_coincide(rpodl_r,tpdiff)/2
	l = area(rpodl_r)/w
	r = RVPIL*l/(w-DWPI)
  ]

  //TRACE PROPERTY R(PI) w w 0
  //TRACE PROPERTY R(PI) l l 0
  //TRACE PROPERTY R(PI) r r 2
 #ELSE
  DEVICE R(PI) rpodl_r tpdiff tpdiff [
	property r
	w = perimeter_coincide(rpodl_r,tpdiff)/2
	l = area(rpodl_r)/w
	r = RVPIL*l/(w-DWPI)
  ]
  //TRACE PROPERTY R(PI) r r 2
 #ENDIF
#ELSE
  DEVICE R(PI) rpodl_r tpdiff tpdiff [RVPIL]
  //TRACE PROPERTY R(PI) r r 2
#ENDIF

DEVICE rppolyl_dis lrppoly_d poly(PLUS) poly(MINUS) poly_term(BULK) (PLUS MINUS) [
  property l,w
  w = perimeter_coincide(lrppoly_d,poly)/2
  l = area(lrppoly_d)/w
]
TRACE PROPERTY rppolyl_dis w w 0
TRACE PROPERTY rppolyl_dis l l 0

DEVICE rppolyhri_dis hripo_d poly(PLUS) poly(MINUS) poly_term(BULK) (PLUS MINUS) [
  property l,w
  w = perimeter_coincide(hripo_d,poly)/2
  l = area(hripo_d)/w
]
TRACE PROPERTY rppolyhri_dis w w 0
TRACE PROPERTY rppolyhri_dis l l 0

DEVICE rnodwo_m rnodwo_mr tndiff(PLUS) tndiff(MINUS) psub(B) (PLUS MINUS) [
   property w,l
   w = perimeter_coincide(rnodwo_mr,tndiff)/2
   l = area(rnodwo_mr)/w
]
TRACE PROPERTY rnodwo_m w w 0
TRACE PROPERTY rnodwo_m l l 0

DEVICE rppolyhri hripo4 poly(PLUS) poly(MINUS) (PLUS MINUS) [
   property w,l
   w = perimeter_coincide(hripo4,poly)/2
   l = area(hripo4)/w
]
TRACE PROPERTY rppolyhri w w 0
TRACE PROPERTY rppolyhri l l 0

DEVICE rnpolywo_dis rnpolywo_d poly(PLUS) poly(MINUS) poly_term(BULK) (PLUS MINUS) [
  property l,w
  w = perimeter_coincide(rnpolywo_d,poly)/2
  l = area(rnpolywo_d)/w
]
TRACE PROPERTY rnpolywo_dis w w 0
TRACE PROPERTY rnpolywo_dis l l 0

DEVICE rnpolys_dis srnpoly_d poly(PLUS) poly(MINUS) poly_term(BULK) (PLUS MINUS) [
  property l,w
  w = perimeter_coincide(srnpoly_d,poly)/2
  l = area(srnpoly_d)/w
]
TRACE PROPERTY rnpolys_dis w w 0
TRACE PROPERTY rnpolys_dis l l 0

#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(WR) nwsti_r nxwell nxwell NETLIST MODEL rnwsti NETLIST ELEMENT "X" [
	property l,w,r
	w = perimeter_coincide(nwsti_r,nxwell)/2
	l = area(nwsti_r)/w
	r = RVWR*l/(w-DWWR)
  ]

  TRACE PROPERTY R(WR) w w 0
  TRACE PROPERTY R(WR) l l 0
  TRACE PROPERTY R(WR) r r 2
 #ELSE
  DEVICE R(WR) nwsti_r nxwell nxwell [
	property r
	w = perimeter_coincide(nwsti_r,nxwell)/2
	l = area(nwsti_r)/w
	r = RVWR*l/(w-DWWR)
  ]
  TRACE PROPERTY R(WR) r r 2
 #ENDIF
#ELSE
  DEVICE R(WR) nwsti_r nxwell nxwell [RVWR]
  TRACE PROPERTY R(WR) r r 2
#ENDIF

#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(NS) rnpolyl_r poly poly NETLIST MODEL rnpolyl NETLIST ELEMENT "X" [
	property l,w,r
	w = perimeter_coincide(rnpolyl_r,poly)/2
	l = area(rnpolyl_r)/w
	r = RVNSL*l/(w-DWNS)
  ]

  //TRACE PROPERTY R(NS) w w 0
  //TRACE PROPERTY R(NS) l l 0
  //TRACE PROPERTY R(NS) r r 2
 #ELSE
  DEVICE R(NS) rnpolyl_r poly poly [
	property r
	w = perimeter_coincide(rnpolyl_r,poly)/2
	l = area(rnpolyl_r)/w
	r = RVNSL*l/(w-DWNS)
  ]
  //TRACE PROPERTY R(NS) r r 2
 #ENDIF
#ELSE
  DEVICE R(NS) rnpolyl_r poly poly [RVNSL]
  //TRACE PROPERTY R(NS) r r 2
#ENDIF

#IFDEF ACCURACY
 #IFDEF MODEL_NAME
//  Modify by cplin //
  DEVICE R(NR) rnpolywo_r poly poly NETLIST MODEL rnpolywo NETLIST ELEMENT "R" [
//  DEVICE R(NR) rnpolywo_r poly poly NETLIST MODEL rnpolywo NETLIST ELEMENT "X" [
//	property l,w,r
	property r
	w = perimeter_coincide(rnpolywo_r,poly)/2
	l = area(rnpolywo_r)/w
	WD = w-DWNR
	LD = l-DLNR
	Rend = DDNR/WD
	Rpure = RVNR*LD/WD
	r = 2*Rend+Rpure
  ]
//  TRACE PROPERTY R(NR) w w 0
//  TRACE PROPERTY R(NR) l l 0
  TRACE PROPERTY R(NR) r r 2
 #ELSE
  DEVICE R(NR) rnpolywo_r poly poly [
	property r
	w = perimeter_coincide(rnpolywo_r,poly)/2
	l = area(rnpolywo_r)/w
	WD = w-DWNR
	LD = l-DLNR
	Rend = DDNR/WD
	Rpure = RVNR*LD/WD
	r = 2*Rend+Rpure
  ]
  TRACE PROPERTY R(NR) r r 2
 #ENDIF
#ELSE
  DEVICE R(NR) rnpolywo_r poly poly [RVNR]
  TRACE PROPERTY R(NR) r r 2
#ENDIF

DEVICE rpodwo_m rpodwo_mr tpdiff(PLUS) tpdiff(MINUS) nxwell(B) (PLUS MINUS) [
   property w,l
   w = perimeter_coincide(rpodwo_mr,tpdiff)/2
   l = area(rpodwo_mr)/w
]
TRACE PROPERTY rpodwo_m w w 0
TRACE PROPERTY rpodwo_m l l 0

#IFDEF ACCURACY
 #IFDEF MODEL_NAME
  DEVICE R(NI) rnods_r tndiff tndiff NETLIST MODEL rnods NETLIST ELEMENT "X" [
	property l,w,r
	w = perimeter_coincide(rnods_r,tndiff)/2
	l = area(rnods_r)/w
	r = RVNIS*l/(w-DWNI)
  ]

  TRACE PROPERTY R(NI) w w 0
  TRACE PROPERTY R(NI) l l 0
  TRACE PROPERTY R(NI) r r 2
 #ELSE
  DEVICE R(NI) rnods_r tndiff tndiff [
	property r
	w = perimeter_coincide(rnods_r,tndiff)/2
	l = area(rnods_r)/w
	r = RVNIS*l/(w-DWNI)
  ]
  TRACE PROPERTY R(NI) r r 2
 #ENDIF
#ELSE
  DEVICE R(NI) rnods_r tndiff tndiff [RVNIS]
  TRACE PROPERTY R(NI) r r 2
#ENDIF


#IFDEF RC_DECK
//=============================
//======= Setup for PEX =======
//=============================
MASK SVDB DIRECTORY "svdb" XRC

PEX BACKANNOTATION DISTRIBUTED NONE
PEX BACKANNOTATION LUMPED      NONE
PEX COUPLED DISTRIBUTED        NO MASK DIRECT
PEX DELAY                      none   //PENFIELD 0.7 MASK DIRECT
PEX EXCLUDE DISTRIBUTED        "VDD" "AVDD" "VD33" "VDDSA" "VDDPST" "VDD5V" MASK DIRECT
PEX EXCLUDE LUMPED             "VDD" "AVDD" "VD33" "VDDSA" "VDDPST" "VDD5V" MASK DIRECT
PEX NETLIST DISTRIBUTED        "netlist" SPICE LAYOUT GROUND VSS MASK DIRECT LOCATION LOOP
PEX NETLIST LUMPED             "netlist" SPICE LAYOUT GROUND VSS MASK DIRECT LOCATION
PEX NETLIST SIMPLE             "net.simple" SPICE LAYOUT MASK DIRECT LOCATION
//Back-annotation xcalibre flow
//PEX NETLIST DISTRIBUTED        "net.dist" SPICE SOURCE GROUND VSS MASK DIRECT LOCATION LOOP
//PEX NETLIST LUMPED             "temp.spi" SPICE SOURCE GROUND VSS MASK DIRECT LOCATION
//PEX NETLIST SIMPLE             "net.simple" SPICE SOURCE MASK DIRECT LOCATION

PEX REPORT DISTRIBUTED         none  //"report.dist" ASCII LAYOUT MASK DIRECT
PEX REPORT LUMPED              none  //"report.lump" LAYOUT MASK DIRECT
PEX RESISTANCE LUMPED          YES MASK DIRECT
//PEX REDUCE LUMPED C            < 10000000
PEX FDB allnets GLOBAL 5000  EXCLUDE LAYOUT VCC GND

// xcalibre capacitance and resistance file 
UNIT CAPACITANCE ff
UNIT RESISTANCE OHM

//include ./rules  //Modify by cplin
RESISTANCE SHEET tndiff [6.65 0]
RESISTANCE SHEET tpdiff [7.25 0]
RESISTANCE SHEET poly [7.31e+00  0]
RESISTANCE RHO metal8 [0.01867 0]
RESISTANCE RHO metal7 [0.02109 0]
RESISTANCE RHO metal6 [0.02109 0]
RESISTANCE RHO metal5 [0.02109 0]
RESISTANCE RHO metal4 [0.02109 0]
RESISTANCE RHO metal3 [0.02109 0]
RESISTANCE RHO metal2 [0.02109 0]
RESISTANCE RHO metal1 [0.02340 0]
CAPACITANCE ORDER tndiff tpdiff gate poly metal1 metal2 metal3 metal4 metal5 metal6 metal7 metal8 
RESISTANCE CONNECTION metal1 tpdiff [0.367872 0]
RESISTANCE CONNECTION metal1 tndiff [0.363776 0]
RESISTANCE CONNECTION metal1 poly [0.28416 0]
RESISTANCE CONNECTION metal2 metal1 [0.024548  0]
RESISTANCE CONNECTION metal3 metal2 [0.024548  0]
RESISTANCE CONNECTION metal4 metal3 [0.024548  0]
RESISTANCE CONNECTION metal5 metal4 [0.024548  0]
RESISTANCE CONNECTION metal6 metal5 [0.024548  0]
RESISTANCE CONNECTION metal7 metal6 [0.024548  0]
RESISTANCE CONNECTION metal8 metal7 [0.054432  0]

// To specify density window size 
PEX DENSITY metal8 WINDOW 50 50
PEX DENSITY metal7 WINDOW 50 50
PEX DENSITY metal6 WINDOW 50 50
PEX DENSITY metal5 WINDOW 50 50
PEX DENSITY metal4 WINDOW 50 50
PEX DENSITY metal3 WINDOW 50 50
PEX DENSITY metal2 WINDOW 50 50
PEX DENSITY metal1 WINDOW 50 50

//The -- #DEFINE XCALIBRATE_EXCLUDE_PROFILE_diffcap -- is added to disable diff_cap profile

//
// Generated on Wed Apr 19 22:44:40 2006 by xCalibrate v2006.1_19.19 Tue Mar  7 12:29:27 PST 2006
//
// Comments: 
//
// Technology name: t013_1p08m_typical
// Precision: 1000
// DBU Size: 1e-09
// DB Magnification: 1
// Capacitance Units: ff
// Background Dielectric: 1
//
VARIABLE m34RS 1
VARIABLE m36RS 0
VARIABLE m37RS 1
VARIABLE m33RS 0
#ifndef $XCAL_MAXD_MODE
#define $XCAL_MAXD_MODE smart // set max_distance type unless already set from shell - smart, classic or maximum
#endif // ifndef $XCAL_MAXD_MODE
#ifdef $XCAL_MAXD_MODE smart
    VARIABLE MAXDISTANCE_poly 0.62
    VARIABLE MAXWIDTH_poly 0.62
    VARIABLE MAXENCLOSE_poly 0.62
    VARIABLE MAXRADIUS_poly 500

    VARIABLE MAXDISTANCE_metal1 0.68
    VARIABLE MAXWIDTH_metal1 0.68
    VARIABLE MAXENCLOSE_metal1 0.68
    VARIABLE MAXRADIUS_metal1 816.5

    VARIABLE MAXDISTANCE_metal2 0.82
    VARIABLE MAXWIDTH_metal2 0.82
    VARIABLE MAXENCLOSE_metal2 0.82
    VARIABLE MAXRADIUS_metal2 1000

    VARIABLE MAXDISTANCE_metal3 0.82
    VARIABLE MAXWIDTH_metal3 0.82
    VARIABLE MAXENCLOSE_metal3 0.82
    VARIABLE MAXRADIUS_metal3 1000

    VARIABLE MAXDISTANCE_metal4 0.82
    VARIABLE MAXWIDTH_metal4 0.82
    VARIABLE MAXENCLOSE_metal4 0.82
    VARIABLE MAXRADIUS_metal4 1000

    VARIABLE MAXDISTANCE_metal5 0.82
    VARIABLE MAXWIDTH_metal5 0.82
    VARIABLE MAXENCLOSE_metal5 0.82
    VARIABLE MAXRADIUS_metal5 1000

    VARIABLE MAXDISTANCE_metal6 0.82
    VARIABLE MAXWIDTH_metal6 0.82
    VARIABLE MAXENCLOSE_metal6 0.82
    VARIABLE MAXRADIUS_metal6 1000

    VARIABLE MAXDISTANCE_metal7 0.82
    VARIABLE MAXWIDTH_metal7 0.82
    VARIABLE MAXENCLOSE_metal7 0.82
    VARIABLE MAXRADIUS_metal7 1000

    VARIABLE MAXDISTANCE_metal8 1.64
    VARIABLE MAXWIDTH_metal8 1.64
    VARIABLE MAXENCLOSE_metal8 1.64
    VARIABLE MAXRADIUS_metal8 838

    VARIABLE MAXDISTANCE_diff 0.56
    VARIABLE MAXWIDTH_diff 0.56
    VARIABLE MAXENCLOSE_diff 0.56
    VARIABLE MAXRADIUS_diff 495

    VARIABLE MAXDISTANCE_diff2 0.56
    VARIABLE MAXWIDTH_diff2 0.56
    VARIABLE MAXENCLOSE_diff2 0.56
    VARIABLE MAXRADIUS_diff2 495

#endif // ifdef $XCAL_MAXD_MODE smart
#ifdef $XCAL_MAXD_MODE classic
    VARIABLE MAXDISTANCE_poly 3
    VARIABLE MAXWIDTH_poly 3
    VARIABLE MAXENCLOSE_poly 3
    VARIABLE MAXRADIUS_poly 3

    VARIABLE MAXDISTANCE_metal1 3
    VARIABLE MAXWIDTH_metal1 3
    VARIABLE MAXENCLOSE_metal1 3
    VARIABLE MAXRADIUS_metal1 3

    VARIABLE MAXDISTANCE_metal2 3
    VARIABLE MAXWIDTH_metal2 3
    VARIABLE MAXENCLOSE_metal2 3
    VARIABLE MAXRADIUS_metal2 3

    VARIABLE MAXDISTANCE_metal3 3
    VARIABLE MAXWIDTH_metal3 3
    VARIABLE MAXENCLOSE_metal3 3
    VARIABLE MAXRADIUS_metal3 3

    VARIABLE MAXDISTANCE_metal4 3
    VARIABLE MAXWIDTH_metal4 3
    VARIABLE MAXENCLOSE_metal4 3
    VARIABLE MAXRADIUS_metal4 3

    VARIABLE MAXDISTANCE_metal5 3
    VARIABLE MAXWIDTH_metal5 3
    VARIABLE MAXENCLOSE_metal5 3
    VARIABLE MAXRADIUS_metal5 3

    VARIABLE MAXDISTANCE_metal6 3
    VARIABLE MAXWIDTH_metal6 3
    VARIABLE MAXENCLOSE_metal6 3
    VARIABLE MAXRADIUS_metal6 3

    VARIABLE MAXDISTANCE_metal7 3
    VARIABLE MAXWIDTH_metal7 3
    VARIABLE MAXENCLOSE_metal7 3
    VARIABLE MAXRADIUS_metal7 3

    VARIABLE MAXDISTANCE_metal8 3
    VARIABLE MAXWIDTH_metal8 3
    VARIABLE MAXENCLOSE_metal8 3
    VARIABLE MAXRADIUS_metal8 3

    VARIABLE MAXDISTANCE_diff 3
    VARIABLE MAXWIDTH_diff 3
    VARIABLE MAXENCLOSE_diff 3
    VARIABLE MAXRADIUS_diff 3

    VARIABLE MAXDISTANCE_diff2 3
    VARIABLE MAXWIDTH_diff2 3
    VARIABLE MAXENCLOSE_diff2 3
    VARIABLE MAXRADIUS_diff2 3

#endif // ifdef $XCAL_MAXD_MODE classic
#ifdef $XCAL_MAXD_MODE maximum
    VARIABLE MAXDISTANCE_poly 5
    VARIABLE MAXWIDTH_poly 0.44
    VARIABLE MAXENCLOSE_poly 5
    VARIABLE MAXRADIUS_poly 500

    VARIABLE MAXDISTANCE_metal1 8.165
    VARIABLE MAXWIDTH_metal1 0.96
    VARIABLE MAXENCLOSE_metal1 8.165
    VARIABLE MAXRADIUS_metal1 816.5

    VARIABLE MAXDISTANCE_metal2 10
    VARIABLE MAXWIDTH_metal2 1.2
    VARIABLE MAXENCLOSE_metal2 10
    VARIABLE MAXRADIUS_metal2 1000

    VARIABLE MAXDISTANCE_metal3 10
    VARIABLE MAXWIDTH_metal3 1.2
    VARIABLE MAXENCLOSE_metal3 10
    VARIABLE MAXRADIUS_metal3 1000

    VARIABLE MAXDISTANCE_metal4 10
    VARIABLE MAXWIDTH_metal4 1.2
    VARIABLE MAXENCLOSE_metal4 10
    VARIABLE MAXRADIUS_metal4 1000

    VARIABLE MAXDISTANCE_metal5 10
    VARIABLE MAXWIDTH_metal5 1.2
    VARIABLE MAXENCLOSE_metal5 10
    VARIABLE MAXRADIUS_metal5 1000

    VARIABLE MAXDISTANCE_metal6 10
    VARIABLE MAXWIDTH_metal6 1.2
    VARIABLE MAXENCLOSE_metal6 10
    VARIABLE MAXRADIUS_metal6 1000

    VARIABLE MAXDISTANCE_metal7 10
    VARIABLE MAXWIDTH_metal7 1.2
    VARIABLE MAXENCLOSE_metal7 10
    VARIABLE MAXRADIUS_metal7 1000

    VARIABLE MAXDISTANCE_metal8 8.38
    VARIABLE MAXWIDTH_metal8 2.64
    VARIABLE MAXENCLOSE_metal8 8.38
    VARIABLE MAXRADIUS_metal8 838

    VARIABLE MAXDISTANCE_diff 4.95
    VARIABLE MAXWIDTH_diff 0.6
    VARIABLE MAXENCLOSE_diff 4.95
    VARIABLE MAXRADIUS_diff 495

    VARIABLE MAXDISTANCE_diff2 4.95
    VARIABLE MAXWIDTH_diff2 0.6
    VARIABLE MAXENCLOSE_diff2 4.95
    VARIABLE MAXRADIUS_diff2 495

#endif // ifdef $XCAL_MAXD_MODE maximum

// Following line(s) should be in your rulesfile
// UNIT CAPACITANCE ff
// Above line(s) should be in your rulesfile

// 
// Background dielectric = 1
// 
// Profile = field
// Layers =  (field_base field_base_diel poly polyCont poly_diel poly_diel2 metal1 via1 metal1_diel metal1_diel2 metal1_diel3 metal1_diel4 metal1_diel5 metal2 via2 metal2_diel metal2_diel2 metal2_diel3 metal2_diel4 metal2_diel5 metal3 via3 metal3_diel metal3_diel2 metal3_diel3 metal3_diel4 metal3_diel5 metal4 via4 metal4_diel metal4_diel2 metal4_diel3 metal4_diel4 metal4_diel5 metal5 via5 metal5_diel metal5_diel2 metal5_diel3 metal5_diel4 metal5_diel5 metal6 via6 metal6_diel metal6_diel2 metal6_diel3 metal6_diel4 metal6_diel5 metal7 via7 metal7_diel metal7_diel2 metal7_diel3 metal7_diel4 metal7_diel5 metal8 metal8_diel metal8_diel2 metal8_diel3 metal8_diel4 metal8_diel5 metal8_diel6)
// 
//     Z-COORD              NAME TYPE THICKNESS   DIEL   WIDTH   SPACE      WIDTH   SPACE    BIAS ENC      NONPLANAR DIELECTRIC PARMS
//                                                 ACTUAL  ACTUAL     DRAWN   DRAWN                   TYPE HEIGHT SWTHK   SWSLOPE  GAP  MIN   TYPE
//                                                                                                                                 TYPE GAP  PARMS
//  --- 8.4450  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel6   D    0.4000   8.1000      
//  --- 8.0450  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel5   D    0.4000   4.2000      
//  --- 7.6450  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel4   D    0.0750   8.1000      
//  --- 7.5700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel3   D    0.7000   4.2000      
//  --- 6.8700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel2   D    0.0300   8.1000      
//  --- 6.8400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal8_diel   D   -0.7300   4.2000      
//  --- 7.5700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal8   C    0.8300           0.4400  0.3800 
//  --- 6.7400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel5   D    0.6200   4.2000      
//  --- 6.1200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel4   D    0.0500   8.1000      
//  --- 6.0700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel3   D    0.2500   3.7000      
//  --- 5.8200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel2   D    0.0300   8.1000      
//  --- 5.7900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal7_diel   D   -0.2800   3.7000      
//  --- 5.7000  ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  --- 6.7400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                       via7   V    0.6700           0.3600  0.3500 
//  --- 6.0700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal7   C    0.3700           0.2200  0.1900 
//  --- 5.7000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel5   D    0.4000   3.7000      
//  --- 5.3000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel4   D    0.0500   8.1000      
//  --- 5.2500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel3   D    0.2500   3.7000      
//  --- 5.0000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel2   D    0.0300   8.1000      
//  --- 4.9700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal6_diel   D   -0.2800   3.7000      
//  --- 4.8800  ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  --- 5.7000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                       via6   V    0.4500           0.1900  0.2200 
//  --- 5.2500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal6   C    0.3700           0.2200  0.1900 
//  --- 4.8800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel5   D    0.4000   3.7000      
//  --- 4.4800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel4   D    0.0500   8.1000      
//  --- 4.4300  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel3   D    0.2500   3.7000      
//  --- 4.1800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel2   D    0.0300   8.1000      
//  --- 4.1500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal5_diel   D   -0.2800   3.7000      
//  --- 4.0600  ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  --- 4.8800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                       via5   V    0.4500           0.1900  0.2200 
//  --- 4.4300  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal5   C    0.3700           0.2200  0.1900 
//  --- 4.0600  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel5   D    0.4000   3.7000      
//  --- 3.6600  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel4   D    0.0500   8.1000      
//  --- 3.6100  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel3   D    0.2500   3.7000      
//  --- 3.3600  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel2   D    0.0300   8.1000      
//  --- 3.3300  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal4_diel   D   -0.2800   3.7000      
//  --- 3.2400  ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  --- 4.0600  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                       via4   V    0.4500           0.1900  0.2200 
//  --- 3.6100  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal4   C    0.3700           0.2200  0.1900 
//  --- 3.2400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel5   D    0.4000   3.7000      
//  --- 2.8400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel4   D    0.0500   8.1000      
//  --- 2.7900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel3   D    0.2500   3.7000      
//  --- 2.5400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel2   D    0.0300   8.1000      
//  --- 2.5100  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal3_diel   D   -0.2800   3.7000      
//  --- 2.4200  ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  --- 3.2400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                       via3   V    0.4500           0.1900  0.2200 
//  --- 2.7900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal3   C    0.3700           0.2200  0.1900 
//  --- 2.4200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel5   D    0.4000   3.7000      
//  --- 2.0200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel4   D    0.0500   8.1000      
//  --- 1.9700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel3   D    0.2500   3.7000      
//  --- 1.7200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel2   D    0.0300   8.1000      
//  --- 1.6900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal2_diel   D   -0.2800   3.7000      
//  --- 1.6000  ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  --- 2.4200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                       via2   V    0.4500           0.1900  0.2200 
//  --- 1.9700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal2   C    0.3700           0.2200  0.1900 
//  --- 1.6000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel5   D    0.4000   3.7000      
//  --- 1.2000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel4   D    0.0500   8.1000      
//  --- 1.1500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel3   D    0.1700   3.7000      
//  --- 0.9800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel2   D    0.0300   8.1000      
//  --- 0.9500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal1_diel   D   -0.2000   4.2000      
//  --- 0.8900  ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  --- 1.6000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                       via1   V    0.4500           0.1900  0.2200 
//  --- 1.1500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal1   C    0.2600           0.1750  0.1650 
//  --- 0.8900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                 poly_diel2   D    0.2900   4.2000      
//  --- 0.6000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                  poly_diel   D    0.0000   4.2000      
//  --- 0.4200  ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  --- 0.8900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                   polyCont   V    0.2900           0.1600  0.1800 
//  --- 0.6000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                       poly   C    0.1800           0.1100  0.2000     0.1300  0.1800   -0.0200
//  --- 0.4200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |            field_base_diel   D    0.4200   3.9000      
//  --- 0.0000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                 field_base   B    1.0000
//  --- -1.0000 ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  ===========================
//  Non-planar Z coordinate map
//  ===========================
// 
//  --- 8.4450 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel6 (8.1000)
//  --- 8.0450 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel5 (4.2000)
//  --- 7.6450 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel4 (8.1000)
//  --- 7.5700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel3 (4.2000)                   metal8
//  --- 6.8700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel2 (8.1000)                   metal8
//  --- 6.8400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal8_diel (4.2000)                   metal8
//  --- 6.7400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel5 (4.2000)                     via7
//  --- 6.1200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel4 (8.1000)                     via7
//  --- 6.0700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel3 (3.7000)                   metal7
//  --- 5.8200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel2 (8.1000)                   metal7
//  --- 5.7900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal7_diel (3.7000)                   metal7
//  --- 5.7000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel5 (3.7000)                     via6
//  --- 5.3000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel4 (8.1000)                     via6
//  --- 5.2500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel3 (3.7000)                   metal6
//  --- 5.0000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel2 (8.1000)                   metal6
//  --- 4.9700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal6_diel (3.7000)                   metal6
//  --- 4.8800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel5 (3.7000)                     via5
//  --- 4.4800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel4 (8.1000)                     via5
//  --- 4.4300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel3 (3.7000)                   metal5
//  --- 4.1800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel2 (8.1000)                   metal5
//  --- 4.1500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal5_diel (3.7000)                   metal5
//  --- 4.0600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel5 (3.7000)                     via4
//  --- 3.6600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel4 (8.1000)                     via4
//  --- 3.6100 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel3 (3.7000)                   metal4
//  --- 3.3600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel2 (8.1000)                   metal4
//  --- 3.3300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal4_diel (3.7000)                   metal4
//  --- 3.2400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel5 (3.7000)                     via3
//  --- 2.8400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel4 (8.1000)                     via3
//  --- 2.7900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel3 (3.7000)                   metal3
//  --- 2.5400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel2 (8.1000)                   metal3
//  --- 2.5100 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal3_diel (3.7000)                   metal3
//  --- 2.4200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel5 (3.7000)                     via2
//  --- 2.0200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel4 (8.1000)                     via2
//  --- 1.9700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel3 (3.7000)                   metal2
//  --- 1.7200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel2 (8.1000)                   metal2
//  --- 1.6900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal2_diel (3.7000)                   metal2
//  --- 1.6000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel5 (3.7000)                     via1
//  --- 1.2000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel4 (8.1000)                     via1
//  --- 1.1500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel3 (3.7000)                   metal1
//  --- 0.9800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel2 (8.1000)                   metal1
//  --- 0.9500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal1_diel (4.2000)                   metal1
//  --- 0.8900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                  poly_diel2 (4.2000)                 polyCont
//  --- 0.6000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                   poly_diel (4.2000)                     poly
//  --- 0.4200 ---------------------------------------------------------------------------------------------------------------------------------------------
//             field_base_diel (3.9000)                         
//  --- 0.0000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                           field_base                         
// 
// Background dielectric = 1
// 
// Profile = active
// Layers =  (active_base active_base_diel metal1 metal1_diel metal1_diel2 metal1_diel3 metal1_diel4 metal1_diel5 metal2 metal2_diel metal2_diel2 metal2_diel3 metal2_diel4 metal2_diel5 metal3 metal3_diel metal3_diel2 metal3_diel3 metal3_diel4 metal3_diel5 metal4 metal4_diel metal4_diel2 metal4_diel3 metal4_diel4 metal4_diel5 metal5 metal5_diel metal5_diel2 metal5_diel3 metal5_diel4 metal5_diel5 metal6 metal6_diel metal6_diel2 metal6_diel3 metal6_diel4 metal6_diel5 metal7 metal7_diel metal7_diel2 metal7_diel3 metal7_diel4 metal7_diel5 metal8 metal8_diel metal8_diel2 metal8_diel3 metal8_diel4 metal8_diel5 metal8_diel6)
// 
//     Z-COORD              NAME TYPE THICKNESS   DIEL   WIDTH   SPACE  ENC      NONPLANAR DIELECTRIC PARMS
//                                                                          TYPE HEIGHT SWTHK   SWSLOPE  GAP  MIN   TYPE
//                                                                                                       TYPE GAP  PARMS
//  --- 8.0250  ------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel6   D    0.4000   8.1000      
//  --- 7.6250  ------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel5   D    0.4000   4.2000      
//  --- 7.2250  ------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel4   D    0.0750   8.1000      
//  --- 7.1500  ------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel3   D    0.7000   4.2000      
//  --- 6.4500  ------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel2   D    0.0300   8.1000      
//  --- 6.4200  ------------------------------------------------------------------------------------------------------------------
//  |                metal8_diel   D   -0.7300   4.2000      
//  --- 7.1500  ------------------------------------------------------------------------------------------------------------------
//  |                     metal8   C    0.8300           0.4400  0.3800 
//  --- 6.3200  ------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel5   D    0.6200   4.2000      
//  --- 5.7000  ------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel4   D    0.0500   8.1000      
//  --- 5.6500  ------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel3   D    0.2500   3.7000      
//  --- 5.4000  ------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel2   D    0.0300   8.1000      
//  --- 5.3700  ------------------------------------------------------------------------------------------------------------------
//  |                metal7_diel   D   -0.2800   3.7000      
//  --- 5.6500  ------------------------------------------------------------------------------------------------------------------
//  |                     metal7   C    0.3700           0.2200  0.1900 
//  --- 5.2800  ------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel5   D    0.4000   3.7000      
//  --- 4.8800  ------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel4   D    0.0500   8.1000      
//  --- 4.8300  ------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel3   D    0.2500   3.7000      
//  --- 4.5800  ------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel2   D    0.0300   8.1000      
//  --- 4.5500  ------------------------------------------------------------------------------------------------------------------
//  |                metal6_diel   D   -0.2800   3.7000      
//  --- 4.8300  ------------------------------------------------------------------------------------------------------------------
//  |                     metal6   C    0.3700           0.2200  0.1900 
//  --- 4.4600  ------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel5   D    0.4000   3.7000      
//  --- 4.0600  ------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel4   D    0.0500   8.1000      
//  --- 4.0100  ------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel3   D    0.2500   3.7000      
//  --- 3.7600  ------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel2   D    0.0300   8.1000      
//  --- 3.7300  ------------------------------------------------------------------------------------------------------------------
//  |                metal5_diel   D   -0.2800   3.7000      
//  --- 4.0100  ------------------------------------------------------------------------------------------------------------------
//  |                     metal5   C    0.3700           0.2200  0.1900 
//  --- 3.6400  ------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel5   D    0.4000   3.7000      
//  --- 3.2400  ------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel4   D    0.0500   8.1000      
//  --- 3.1900  ------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel3   D    0.2500   3.7000      
//  --- 2.9400  ------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel2   D    0.0300   8.1000      
//  --- 2.9100  ------------------------------------------------------------------------------------------------------------------
//  |                metal4_diel   D   -0.2800   3.7000      
//  --- 3.1900  ------------------------------------------------------------------------------------------------------------------
//  |                     metal4   C    0.3700           0.2200  0.1900 
//  --- 2.8200  ------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel5   D    0.4000   3.7000      
//  --- 2.4200  ------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel4   D    0.0500   8.1000      
//  --- 2.3700  ------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel3   D    0.2500   3.7000      
//  --- 2.1200  ------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel2   D    0.0300   8.1000      
//  --- 2.0900  ------------------------------------------------------------------------------------------------------------------
//  |                metal3_diel   D   -0.2800   3.7000      
//  --- 2.3700  ------------------------------------------------------------------------------------------------------------------
//  |                     metal3   C    0.3700           0.2200  0.1900 
//  --- 2.0000  ------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel5   D    0.4000   3.7000      
//  --- 1.6000  ------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel4   D    0.0500   8.1000      
//  --- 1.5500  ------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel3   D    0.2500   3.7000      
//  --- 1.3000  ------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel2   D    0.0300   8.1000      
//  --- 1.2700  ------------------------------------------------------------------------------------------------------------------
//  |                metal2_diel   D   -0.2800   3.7000      
//  --- 1.5500  ------------------------------------------------------------------------------------------------------------------
//  |                     metal2   C    0.3700           0.2200  0.1900 
//  --- 1.1800  ------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel5   D    0.4000   3.7000      
//  --- 0.7800  ------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel4   D    0.0500   8.1000      
//  --- 0.7300  ------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel3   D    0.1700   3.7000      
//  --- 0.5600  ------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel2   D    0.0300   8.1000      
//  --- 0.5300  ------------------------------------------------------------------------------------------------------------------
//  |                metal1_diel   D   -0.2000   4.2000      
//  --- 0.7300  ------------------------------------------------------------------------------------------------------------------
//  |                     metal1   C    0.2600           0.1750  0.1650 
//  --- 0.4700  ------------------------------------------------------------------------------------------------------------------
//  |           active_base_diel   D    0.4700   4.2000      
//  --- 0.0000  ------------------------------------------------------------------------------------------------------------------
//  |                active_base   B    1.0000
//  --- -1.0000 ------------------------------------------------------------------------------------------------------------------
// 
//  ===========================
//  Non-planar Z coordinate map
//  ===========================
// 
//  --- 8.0250 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel6 (8.1000)
//  --- 7.6250 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel5 (4.2000)
//  --- 7.2250 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel4 (8.1000)
//  --- 7.1500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel3 (4.2000)                   metal8
//  --- 6.4500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel2 (8.1000)                   metal8
//  --- 6.4200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal8_diel (4.2000)                   metal8
//  --- 6.3200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel5 (4.2000)                         
//  --- 5.7000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel4 (8.1000)                         
//  --- 5.6500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel3 (3.7000)                   metal7
//  --- 5.4000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel2 (8.1000)                   metal7
//  --- 5.3700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal7_diel (3.7000)                   metal7
//  --- 5.2800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel5 (3.7000)                         
//  --- 4.8800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel4 (8.1000)                         
//  --- 4.8300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel3 (3.7000)                   metal6
//  --- 4.5800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel2 (8.1000)                   metal6
//  --- 4.5500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal6_diel (3.7000)                   metal6
//  --- 4.4600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel5 (3.7000)                         
//  --- 4.0600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel4 (8.1000)                         
//  --- 4.0100 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel3 (3.7000)                   metal5
//  --- 3.7600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel2 (8.1000)                   metal5
//  --- 3.7300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal5_diel (3.7000)                   metal5
//  --- 3.6400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel5 (3.7000)                         
//  --- 3.2400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel4 (8.1000)                         
//  --- 3.1900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel3 (3.7000)                   metal4
//  --- 2.9400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel2 (8.1000)                   metal4
//  --- 2.9100 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal4_diel (3.7000)                   metal4
//  --- 2.8200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel5 (3.7000)                         
//  --- 2.4200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel4 (8.1000)                         
//  --- 2.3700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel3 (3.7000)                   metal3
//  --- 2.1200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel2 (8.1000)                   metal3
//  --- 2.0900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal3_diel (3.7000)                   metal3
//  --- 2.0000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel5 (3.7000)                         
//  --- 1.6000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel4 (8.1000)                         
//  --- 1.5500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel3 (3.7000)                   metal2
//  --- 1.3000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel2 (8.1000)                   metal2
//  --- 1.2700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal2_diel (3.7000)                   metal2
//  --- 1.1800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel5 (3.7000)                         
//  --- 0.7800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel4 (8.1000)                         
//  --- 0.7300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel3 (3.7000)                   metal1
//  --- 0.5600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel2 (8.1000)                   metal1
//  --- 0.5300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal1_diel (4.2000)                   metal1
//  --- 0.4700 ---------------------------------------------------------------------------------------------------------------------------------------------
//             active_base_diel (4.2000)                         
//  --- 0.0000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                          active_base                         
// 
// Background dielectric = 1
// 
// Profile = sdcont
// Layers =  (sdcont_base diff odCont diff_diel poly poly_diel poly_diel2 metal1 metal1_diel metal1_diel2 metal1_diel3 metal1_diel4 metal1_diel5 metal2 metal2_diel metal2_diel2 metal2_diel3 metal2_diel4 metal2_diel5 metal3 metal3_diel metal3_diel2 metal3_diel3 metal3_diel4 metal3_diel5 metal4 metal4_diel metal4_diel2 metal4_diel3 metal4_diel4 metal4_diel5 metal5 metal5_diel metal5_diel2 metal5_diel3 metal5_diel4 metal5_diel5 metal6 metal6_diel metal6_diel2 metal6_diel3 metal6_diel4 metal6_diel5 metal7 metal7_diel metal7_diel2 metal7_diel3 metal7_diel4 metal7_diel5 metal8 metal8_diel metal8_diel2 metal8_diel3 metal8_diel4 metal8_diel5 metal8_diel6)
// 
//     Z-COORD              NAME TYPE THICKNESS   DIEL   WIDTH   SPACE      WIDTH   SPACE    BIAS ENC      NONPLANAR DIELECTRIC PARMS
//                                                 ACTUAL  ACTUAL     DRAWN   DRAWN                   TYPE HEIGHT SWTHK   SWSLOPE  GAP  MIN   TYPE
//                                                                                                                                 TYPE GAP  PARMS
//  --- 8.2550  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel6   D    0.4000   8.1000      
//  --- 7.8550  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel5   D    0.4000   4.2000      
//  --- 7.4550  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel4   D    0.0750   8.1000      
//  --- 7.3800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel3   D    0.7000   4.2000      
//  --- 6.6800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel2   D    0.0300   8.1000      
//  --- 6.6500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal8_diel   D   -0.7300   4.2000      
//  --- 7.3800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal8   C    0.8300           0.4400  0.3800 
//  --- 6.5500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel5   D    0.6200   4.2000      
//  --- 5.9300  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel4   D    0.0500   8.1000      
//  --- 5.8800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel3   D    0.2500   3.7000      
//  --- 5.6300  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel2   D    0.0300   8.1000      
//  --- 5.6000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal7_diel   D   -0.2800   3.7000      
//  --- 5.8800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal7   C    0.3700           0.2200  0.1900 
//  --- 5.5100  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel5   D    0.4000   3.7000      
//  --- 5.1100  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel4   D    0.0500   8.1000      
//  --- 5.0600  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel3   D    0.2500   3.7000      
//  --- 4.8100  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel2   D    0.0300   8.1000      
//  --- 4.7800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal6_diel   D   -0.2800   3.7000      
//  --- 5.0600  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal6   C    0.3700           0.2200  0.1900 
//  --- 4.6900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel5   D    0.4000   3.7000      
//  --- 4.2900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel4   D    0.0500   8.1000      
//  --- 4.2400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel3   D    0.2500   3.7000      
//  --- 3.9900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel2   D    0.0300   8.1000      
//  --- 3.9600  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal5_diel   D   -0.2800   3.7000      
//  --- 4.2400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal5   C    0.3700           0.2200  0.1900 
//  --- 3.8700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel5   D    0.4000   3.7000      
//  --- 3.4700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel4   D    0.0500   8.1000      
//  --- 3.4200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel3   D    0.2500   3.7000      
//  --- 3.1700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel2   D    0.0300   8.1000      
//  --- 3.1400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal4_diel   D   -0.2800   3.7000      
//  --- 3.4200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal4   C    0.3700           0.2200  0.1900 
//  --- 3.0500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel5   D    0.4000   3.7000      
//  --- 2.6500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel4   D    0.0500   8.1000      
//  --- 2.6000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel3   D    0.2500   3.7000      
//  --- 2.3500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel2   D    0.0300   8.1000      
//  --- 2.3200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal3_diel   D   -0.2800   3.7000      
//  --- 2.6000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal3   C    0.3700           0.2200  0.1900 
//  --- 2.2300  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel5   D    0.4000   3.7000      
//  --- 1.8300  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel4   D    0.0500   8.1000      
//  --- 1.7800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel3   D    0.2500   3.7000      
//  --- 1.5300  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel2   D    0.0300   8.1000      
//  --- 1.5000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal2_diel   D   -0.2800   3.7000      
//  --- 1.7800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal2   C    0.3700           0.2200  0.1900 
//  --- 1.4100  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel5   D    0.4000   3.7000      
//  --- 1.0100  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel4   D    0.0500   8.1000      
//  --- 0.9600  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel3   D    0.1700   3.7000      
//  --- 0.7900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel2   D    0.0300   8.1000      
//  --- 0.7600  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal1_diel   D   -0.2000   4.2000      
//  --- 0.9600  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal1   C    0.2600           0.1750  0.1650 
//  --- 0.7000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                 poly_diel2   D    0.2900   4.2000      
//  --- 0.4100  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                  poly_diel   D    0.0000   4.2000      
//  --- 0.4100  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                       poly   C    0.1800           0.1100  0.2000     0.1300  0.1800   -0.0200
//  --- 0.2300  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                  diff_diel   D    0.0200   3.9000      
//  --- 0.0000  ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  --- 0.5200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     odCont   V    0.3100           0.1600  0.1800 
//  --- 0.2100  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                       diff   C    0.2100           0.1500  0.1300 
//  --- 0.0000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                sdcont_base   B    1.0000
//  --- -1.0000 ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  ===========================
//  Non-planar Z coordinate map
//  ===========================
// 
//  --- 8.2550 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel6 (8.1000)
//  --- 7.8550 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel5 (4.2000)
//  --- 7.4550 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel4 (8.1000)
//  --- 7.3800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel3 (4.2000)                   metal8
//  --- 6.6800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel2 (8.1000)                   metal8
//  --- 6.6500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal8_diel (4.2000)                   metal8
//  --- 6.5500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel5 (4.2000)                         
//  --- 5.9300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel4 (8.1000)                         
//  --- 5.8800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel3 (3.7000)                   metal7
//  --- 5.6300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel2 (8.1000)                   metal7
//  --- 5.6000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal7_diel (3.7000)                   metal7
//  --- 5.5100 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel5 (3.7000)                         
//  --- 5.1100 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel4 (8.1000)                         
//  --- 5.0600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel3 (3.7000)                   metal6
//  --- 4.8100 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel2 (8.1000)                   metal6
//  --- 4.7800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal6_diel (3.7000)                   metal6
//  --- 4.6900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel5 (3.7000)                         
//  --- 4.2900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel4 (8.1000)                         
//  --- 4.2400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel3 (3.7000)                   metal5
//  --- 3.9900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel2 (8.1000)                   metal5
//  --- 3.9600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal5_diel (3.7000)                   metal5
//  --- 3.8700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel5 (3.7000)                         
//  --- 3.4700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel4 (8.1000)                         
//  --- 3.4200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel3 (3.7000)                   metal4
//  --- 3.1700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel2 (8.1000)                   metal4
//  --- 3.1400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal4_diel (3.7000)                   metal4
//  --- 3.0500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel5 (3.7000)                         
//  --- 2.6500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel4 (8.1000)                         
//  --- 2.6000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel3 (3.7000)                   metal3
//  --- 2.3500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel2 (8.1000)                   metal3
//  --- 2.3200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal3_diel (3.7000)                   metal3
//  --- 2.2300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel5 (3.7000)                         
//  --- 1.8300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel4 (8.1000)                         
//  --- 1.7800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel3 (3.7000)                   metal2
//  --- 1.5300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel2 (8.1000)                   metal2
//  --- 1.5000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal2_diel (3.7000)                   metal2
//  --- 1.4100 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel5 (3.7000)                         
//  --- 1.0100 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel4 (8.1000)                         
//  --- 0.9600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel3 (3.7000)                   metal1
//  --- 0.7900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel2 (8.1000)                   metal1
//  --- 0.7600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal1_diel (4.2000)                   metal1
//  --- 0.7000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                  poly_diel2 (4.2000)                         
//  --- 0.5200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                  poly_diel2 (4.2000)                   odCont
//  --- 0.4100 ---------------------------------------------------------------------------------------------------------------------------------------------
//                   poly_diel (4.2000)                   odCont                     poly
//  --- 0.2300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                   diff_diel (3.9000)                   odCont                         
//  --- 0.2100 ---------------------------------------------------------------------------------------------------------------------------------------------
//                   diff_diel (3.9000)                     diff                         
//  --- 0.0000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                          sdcont_base                                                  
// 
// Background dielectric = 1
// 
// Profile = diffcap
// Layers =  (diffcap_base_diel diff2 diff2_diel diff2_diel2 poly poly_diel poly_diel2 metal1 metal1_diel metal1_diel2 metal1_diel3 metal1_diel4 metal1_diel5 metal2 metal2_diel metal2_diel2 metal2_diel3 metal2_diel4 metal2_diel5 metal3 metal3_diel metal3_diel2 metal3_diel3 metal3_diel4 metal3_diel5 metal4 metal4_diel metal4_diel2 metal4_diel3 metal4_diel4 metal4_diel5 metal5 metal5_diel metal5_diel2 metal5_diel3 metal5_diel4 metal5_diel5 metal6 metal6_diel metal6_diel2 metal6_diel3 metal6_diel4 metal6_diel5 metal7 metal7_diel metal7_diel2 metal7_diel3 metal7_diel4 metal7_diel5 metal8 metal8_diel metal8_diel2 metal8_diel3 metal8_diel4 metal8_diel5 metal8_diel6)
// 
//     Z-COORD              NAME TYPE THICKNESS   DIEL   WIDTH   SPACE      WIDTH   SPACE    BIAS ENC      NONPLANAR DIELECTRIC PARMS
//                                                 ACTUAL  ACTUAL     DRAWN   DRAWN                   TYPE HEIGHT SWTHK   SWSLOPE  GAP  MIN   TYPE
//                                                                                                                                 TYPE GAP  PARMS
//  --- 8.4450  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel6   D    0.4000   8.1000      
//  --- 8.0450  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel5   D    0.4000   4.2000      
//  --- 7.6450  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel4   D    0.0750   8.1000      
//  --- 7.5700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel3   D    0.7000   4.2000      
//  --- 6.8700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal8_diel2   D    0.0300   8.1000      
//  --- 6.8400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal8_diel   D   -0.7300   4.2000      
//  --- 7.5700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal8   C    0.8300           0.4400  0.3800 
//  --- 6.7400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel5   D    0.6200   4.2000      
//  --- 6.1200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel4   D    0.0500   8.1000      
//  --- 6.0700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel3   D    0.2500   3.7000      
//  --- 5.8200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal7_diel2   D    0.0300   8.1000      
//  --- 5.7900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal7_diel   D   -0.2800   3.7000      
//  --- 6.0700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal7   C    0.3700           0.2200  0.1900 
//  --- 5.7000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel5   D    0.4000   3.7000      
//  --- 5.3000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel4   D    0.0500   8.1000      
//  --- 5.2500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel3   D    0.2500   3.7000      
//  --- 5.0000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal6_diel2   D    0.0300   8.1000      
//  --- 4.9700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal6_diel   D   -0.2800   3.7000      
//  --- 5.2500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal6   C    0.3700           0.2200  0.1900 
//  --- 4.8800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel5   D    0.4000   3.7000      
//  --- 4.4800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel4   D    0.0500   8.1000      
//  --- 4.4300  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel3   D    0.2500   3.7000      
//  --- 4.1800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal5_diel2   D    0.0300   8.1000      
//  --- 4.1500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal5_diel   D   -0.2800   3.7000      
//  --- 4.4300  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal5   C    0.3700           0.2200  0.1900 
//  --- 4.0600  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel5   D    0.4000   3.7000      
//  --- 3.6600  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel4   D    0.0500   8.1000      
//  --- 3.6100  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel3   D    0.2500   3.7000      
//  --- 3.3600  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal4_diel2   D    0.0300   8.1000      
//  --- 3.3300  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal4_diel   D   -0.2800   3.7000      
//  --- 3.6100  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal4   C    0.3700           0.2200  0.1900 
//  --- 3.2400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel5   D    0.4000   3.7000      
//  --- 2.8400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel4   D    0.0500   8.1000      
//  --- 2.7900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel3   D    0.2500   3.7000      
//  --- 2.5400  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal3_diel2   D    0.0300   8.1000      
//  --- 2.5100  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal3_diel   D   -0.2800   3.7000      
//  --- 2.7900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal3   C    0.3700           0.2200  0.1900 
//  --- 2.4200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel5   D    0.4000   3.7000      
//  --- 2.0200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel4   D    0.0500   8.1000      
//  --- 1.9700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel3   D    0.2500   3.7000      
//  --- 1.7200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal2_diel2   D    0.0300   8.1000      
//  --- 1.6900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal2_diel   D   -0.2800   3.7000      
//  --- 1.9700  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal2   C    0.3700           0.2200  0.1900 
//  --- 1.6000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel5   D    0.4000   3.7000      
//  --- 1.2000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel4   D    0.0500   8.1000      
//  --- 1.1500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel3   D    0.1700   3.7000      
//  --- 0.9800  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |               metal1_diel2   D    0.0300   8.1000      
//  --- 0.9500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                metal1_diel   D   -0.2000   4.2000      
//  --- 1.1500  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                     metal1   C    0.2600           0.1750  0.1650 
//  --- 0.8900  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                 poly_diel2   D    0.2900   4.2000      
//  --- 0.6000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                  poly_diel   D    0.0000   4.2000      
//  --- 0.6000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                       poly   C    0.1800           0.1100  0.2000     0.1300  0.1800   -0.0200
//  --- 0.4200  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                diff2_diel2   D    0.0200   3.9000      
//  --- 0.4000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |   0.0000        diff2_diel   D    0.0000   12.0000                                                C  -0.4000   0.0000  90.0000                                               
//  --- 0.0000  ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  --- 0.4000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |                      diff2   C    0.2100           0.1500  0.1300 
//  --- 0.1900  ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  --- 0.4000  ---------------------------------------------------------------------------------------------------------------------------------------------
//  |           diffcap_base_diel   D    0.4000   3.9000      
//  --- 0.0000  ---------------------------------------------------------------------------------------------------------------------------------------------
// 
//  ===========================
//  Non-planar Z coordinate map
//  ===========================
// 
//  --- 8.4450 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel6 (8.1000)
//  --- 8.0450 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel5 (4.2000)
//  --- 7.6450 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel4 (8.1000)
//  --- 7.5700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel3 (4.2000)                   metal8
//  --- 6.8700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal8_diel2 (8.1000)                   metal8
//  --- 6.8400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal8_diel (4.2000)                   metal8
//  --- 6.7400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel5 (4.2000)                         
//  --- 6.1200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel4 (8.1000)                         
//  --- 6.0700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel3 (3.7000)                   metal7
//  --- 5.8200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal7_diel2 (8.1000)                   metal7
//  --- 5.7900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal7_diel (3.7000)                   metal7
//  --- 5.7000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel5 (3.7000)                         
//  --- 5.3000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel4 (8.1000)                         
//  --- 5.2500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel3 (3.7000)                   metal6
//  --- 5.0000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal6_diel2 (8.1000)                   metal6
//  --- 4.9700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal6_diel (3.7000)                   metal6
//  --- 4.8800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel5 (3.7000)                         
//  --- 4.4800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel4 (8.1000)                         
//  --- 4.4300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel3 (3.7000)                   metal5
//  --- 4.1800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal5_diel2 (8.1000)                   metal5
//  --- 4.1500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal5_diel (3.7000)                   metal5
//  --- 4.0600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel5 (3.7000)                         
//  --- 3.6600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel4 (8.1000)                         
//  --- 3.6100 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel3 (3.7000)                   metal4
//  --- 3.3600 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal4_diel2 (8.1000)                   metal4
//  --- 3.3300 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal4_diel (3.7000)                   metal4
//  --- 3.2400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel5 (3.7000)                         
//  --- 2.8400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel4 (8.1000)                         
//  --- 2.7900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel3 (3.7000)                   metal3
//  --- 2.5400 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal3_diel2 (8.1000)                   metal3
//  --- 2.5100 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal3_diel (3.7000)                   metal3
//  --- 2.4200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel5 (3.7000)                         
//  --- 2.0200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel4 (8.1000)                         
//  --- 1.9700 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel3 (3.7000)                   metal2
//  --- 1.7200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal2_diel2 (8.1000)                   metal2
//  --- 1.6900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal2_diel (3.7000)                   metal2
//  --- 1.6000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel5 (3.7000)                         
//  --- 1.2000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel4 (8.1000)                         
//  --- 1.1500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel3 (3.7000)                   metal1
//  --- 0.9800 ---------------------------------------------------------------------------------------------------------------------------------------------
//                metal1_diel2 (8.1000)                   metal1
//  --- 0.9500 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 metal1_diel (4.2000)                   metal1
//  --- 0.8900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                  poly_diel2 (4.2000)                         
//  --- 0.6000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                   poly_diel (4.2000)                     poly
//  --- 0.4200 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 diff2_diel2 (3.9000)                         
//  --- 0.4000 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 diff2_diel (12.0000)                    diff2 diffcap_base_diel (3.9000)
//  --- 0.1900 ---------------------------------------------------------------------------------------------------------------------------------------------
//                 diff2_diel (12.0000)                          diffcap_base_diel (3.9000)

PEX PROFILE default // for inductance extraction and radial shielding - need all layers in capacitance order statement
poly 0.42 0.6
metal1 0.89 1.15
metal2 1.6 1.97
metal3 2.42 2.79
metal4 3.24 3.61
metal5 4.06 4.43
metal6 4.88 5.25
metal7 5.7 6.07
metal8 6.74 7.57


PEX MINIMUM PATH WIDTH 0.125 // = 0.13 - .005

//#define XCALIBRATE_EXCLUDE_INDIE // uncomment this line for classic mode or no indie tables

#ifndef XCALIBRATE_EXCLUDE_INDIE

// indie mode 

PARASITIC VARIATION metal1 WIDTH
SPACE             <= 0.162  DRAWN_WIDTH     <= 0.16  VALUE  0.0099
SPACE     > 0.162  <= 0.18  DRAWN_WIDTH     <= 0.16  VALUE  0.015
SPACE     > 0.18  <= 0.28  DRAWN_WIDTH     <= 0.16  VALUE  0.0453
SPACE     > 0.28  <= 0.38  DRAWN_WIDTH     <= 0.16  VALUE  0.0547
SPACE     > 0.38  <= 0.48  DRAWN_WIDTH     <= 0.16  VALUE  0.0551
SPACE     > 0.48  <= 0.58  DRAWN_WIDTH     <= 0.16  VALUE  0.0554
SPACE     > 0.58  <= 0.68  DRAWN_WIDTH     <= 0.16  VALUE  0.0556
SPACE     > 0.68  <= 0.8  DRAWN_WIDTH     <= 0.16  VALUE  0.0558
SPACE     > 0.8  <= 1  DRAWN_WIDTH     <= 0.16  VALUE  0.056
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.16  VALUE  0.0562
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.16  VALUE  0.0563
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.16  VALUE  0.0564
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.16  VALUE  0.0565
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.16  VALUE  0.0565
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.16  VALUE  0.0566
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.16  VALUE  0.0567
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.16  VALUE  0.0568
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.16  VALUE  0.0569
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.16  VALUE  0.0569
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.16  VALUE  0.057
SPACE             > 10  DRAWN_WIDTH     <= 0.16  VALUE  0.057
SPACE             <= 0.162  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.009
SPACE     > 0.162  <= 0.18  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0141
SPACE     > 0.18  <= 0.28  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0444
SPACE     > 0.28  <= 0.38  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0539
SPACE     > 0.38  <= 0.48  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0543
SPACE     > 0.48  <= 0.58  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0546
SPACE     > 0.58  <= 0.68  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0549
SPACE     > 0.68  <= 0.8  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0551
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0554
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0556
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0558
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0559
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.056
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0561
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0563
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0564
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0566
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0567
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0568
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0569
SPACE             > 10  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0569
SPACE             <= 0.162  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0082
SPACE     > 0.162  <= 0.18  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0133
SPACE     > 0.18  <= 0.28  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0436
SPACE     > 0.28  <= 0.38  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.053
SPACE     > 0.38  <= 0.48  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0534
SPACE     > 0.48  <= 0.58  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0537
SPACE     > 0.58  <= 0.68  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0539
SPACE     > 0.68  <= 0.8  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0542
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0546
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0548
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0551
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0552
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0554
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0555
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0558
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.056
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0562
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0564
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0566
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0567
SPACE             > 10  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0567
SPACE             <= 0.162  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.0079
SPACE     > 0.162  <= 0.18  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.0129
SPACE     > 0.18  <= 0.28  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.043
SPACE     > 0.28  <= 0.38  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.053
SPACE     > 0.38  <= 0.48  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.053
SPACE     > 0.48  <= 0.58  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.053
SPACE     > 0.58  <= 0.68  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.053
SPACE     > 0.68  <= 0.8  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.054
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.054
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.054
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.055
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.055
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.055
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.055
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.055
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.056
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.056
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.056
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.056
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.057
SPACE             > 10  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.057
SPACE             <= 0.162  DRAWN_WIDTH     > 0.96  VALUE  0.0079
SPACE     > 0.162  <= 0.18  DRAWN_WIDTH     > 0.96  VALUE  0.0129
SPACE     > 0.18  <= 0.28  DRAWN_WIDTH     > 0.96  VALUE  0.043
SPACE     > 0.28  <= 0.38  DRAWN_WIDTH     > 0.96  VALUE  0.053
SPACE     > 0.38  <= 0.48  DRAWN_WIDTH     > 0.96  VALUE  0.053
SPACE     > 0.48  <= 0.58  DRAWN_WIDTH     > 0.96  VALUE  0.053
SPACE     > 0.58  <= 0.68  DRAWN_WIDTH     > 0.96  VALUE  0.053
SPACE     > 0.68  <= 0.8  DRAWN_WIDTH     > 0.96  VALUE  0.054
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.96  VALUE  0.054
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.96  VALUE  0.054
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.96  VALUE  0.055
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.96  VALUE  0.055
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.96  VALUE  0.055
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.96  VALUE  0.055
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.96  VALUE  0.055
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.96  VALUE  0.056
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.96  VALUE  0.056
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.96  VALUE  0.056
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.96  VALUE  0.056
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.96  VALUE  0.057
SPACE             > 10  DRAWN_WIDTH     > 0.96  VALUE  0.057

PARASITIC VARIATION metal1 RHO
SPACE             <= 0.162  DRAWN_WIDTH     <= 0.16  VALUE  0.02576
SPACE     > 0.162  <= 0.18  DRAWN_WIDTH     <= 0.16  VALUE  0.02568
SPACE     > 0.18  <= 0.28  DRAWN_WIDTH     <= 0.16  VALUE  0.02526
SPACE     > 0.28  <= 0.38  DRAWN_WIDTH     <= 0.16  VALUE  0.02513
SPACE     > 0.38  <= 0.48  DRAWN_WIDTH     <= 0.16  VALUE  0.02511
SPACE     > 0.48  <= 0.58  DRAWN_WIDTH     <= 0.16  VALUE  0.02509
SPACE     > 0.58  <= 0.68  DRAWN_WIDTH     <= 0.16  VALUE  0.02508
SPACE     > 0.68  <= 0.8  DRAWN_WIDTH     <= 0.16  VALUE  0.02507
SPACE     > 0.8  <= 1  DRAWN_WIDTH     <= 0.16  VALUE  0.02506
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.16  VALUE  0.02505
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.16  VALUE  0.02504
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.16  VALUE  0.02504
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.16  VALUE  0.02503
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.16  VALUE  0.02503
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.16  VALUE  0.02502
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.16  VALUE  0.02502
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.16  VALUE  0.02502
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.16  VALUE  0.02501
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.16  VALUE  0.02501
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.16  VALUE  0.02501
SPACE             > 10  DRAWN_WIDTH     <= 0.16  VALUE  0.02501
SPACE             <= 0.162  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0245
SPACE     > 0.162  <= 0.18  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02447
SPACE     > 0.18  <= 0.28  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02434
SPACE     > 0.28  <= 0.38  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02428
SPACE     > 0.38  <= 0.48  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02426
SPACE     > 0.48  <= 0.58  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02424
SPACE     > 0.58  <= 0.68  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02423
SPACE     > 0.68  <= 0.8  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02422
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.0242
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02419
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02418
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02417
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02417
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02417
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02416
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02415
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02415
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02414
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02414
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02413
SPACE             > 10  DRAWN_WIDTH     > 0.16  <= 0.32  VALUE  0.02413
SPACE             <= 0.162  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0239
SPACE     > 0.162  <= 0.18  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02389
SPACE     > 0.18  <= 0.28  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02384
SPACE     > 0.28  <= 0.38  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02381
SPACE     > 0.38  <= 0.48  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02379
SPACE     > 0.48  <= 0.58  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02377
SPACE     > 0.58  <= 0.68  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02375
SPACE     > 0.68  <= 0.8  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02374
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02372
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02371
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.0237
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02369
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02368
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02367
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02366
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02365
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02364
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02364
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02363
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02362
SPACE             > 10  DRAWN_WIDTH     > 0.32  <= 0.64  VALUE  0.02362
SPACE             <= 0.162  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02371
SPACE     > 0.162  <= 0.18  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02371
SPACE     > 0.18  <= 0.28  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02367
SPACE     > 0.28  <= 0.38  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02365
SPACE     > 0.38  <= 0.48  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02363
SPACE     > 0.48  <= 0.58  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02361
SPACE     > 0.58  <= 0.68  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.0236
SPACE     > 0.68  <= 0.8  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02358
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02356
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02355
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02353
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02352
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02352
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02351
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.0235
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02349
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02347
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02346
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02345
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02344
SPACE             > 10  DRAWN_WIDTH     > 0.64  <= 0.96  VALUE  0.02344
SPACE             <= 0.162  DRAWN_WIDTH     > 0.96  VALUE  0.02371
SPACE     > 0.162  <= 0.18  DRAWN_WIDTH     > 0.96  VALUE  0.02371
SPACE     > 0.18  <= 0.28  DRAWN_WIDTH     > 0.96  VALUE  0.02367
SPACE     > 0.28  <= 0.38  DRAWN_WIDTH     > 0.96  VALUE  0.02365
SPACE     > 0.38  <= 0.48  DRAWN_WIDTH     > 0.96  VALUE  0.02363
SPACE     > 0.48  <= 0.58  DRAWN_WIDTH     > 0.96  VALUE  0.02361
SPACE     > 0.58  <= 0.68  DRAWN_WIDTH     > 0.96  VALUE  0.0236
SPACE     > 0.68  <= 0.8  DRAWN_WIDTH     > 0.96  VALUE  0.02358
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.96  VALUE  0.02356
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.96  VALUE  0.02355
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.96  VALUE  0.02353
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.96  VALUE  0.02352
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.96  VALUE  0.02352
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.96  VALUE  0.02351
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.96  VALUE  0.0235
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.96  VALUE  0.02349
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.96  VALUE  0.02347
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.96  VALUE  0.02346
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.96  VALUE  0.02345
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.96  VALUE  0.02344
SPACE             > 10  DRAWN_WIDTH     > 0.96  VALUE  0.02344

PARASITIC VARIATION metal1 THICKNESS -0.105000 0.314044 // density1 and density2 

PARASITIC VARIATION metal2 WIDTH
SPACE             <= 0.18  DRAWN_WIDTH     <= 0.2  VALUE  0.0198
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     <= 0.2  VALUE  0.02
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     <= 0.2  VALUE  0.0453
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     <= 0.2  VALUE  0.0606
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     <= 0.2  VALUE  0.0709
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     <= 0.2  VALUE  0.0712
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0715
SPACE     > 0.8  <= 1  DRAWN_WIDTH     <= 0.2  VALUE  0.0718
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.2  VALUE  0.072
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.2  VALUE  0.0721
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.2  VALUE  0.0722
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0723
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.2  VALUE  0.0723
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.2  VALUE  0.0725
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.2  VALUE  0.0726
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.2  VALUE  0.0727
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.2  VALUE  0.0728
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.2  VALUE  0.0728
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.2  VALUE  0.0729
SPACE             > 10  DRAWN_WIDTH     <= 0.2  VALUE  0.0729
SPACE             <= 0.18  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0189
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0191
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0445
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0598
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0701
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0704
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0708
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0711
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0714
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0716
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0717
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0718
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0719
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0721
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0723
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0724
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0726
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0727
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0728
SPACE             > 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0728
SPACE             <= 0.18  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0183
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0184
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0437
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0693
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0695
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.07
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0703
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0706
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0708
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.071
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0711
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0713
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0715
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0717
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0722
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0724
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0726
SPACE             > 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0726
SPACE             <= 0.18  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.018
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.018
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.043
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE             > 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE             <= 0.18  DRAWN_WIDTH     > 1.2  VALUE  0.018
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 1.2  VALUE  0.018
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 1.2  VALUE  0.043
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 1.2  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 3  <= 4  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 5  <= 7  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 7  <= 10  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE             > 10  DRAWN_WIDTH     > 1.2  VALUE  0.072

PARASITIC VARIATION metal2 RHO
SPACE             <= 0.18  DRAWN_WIDTH     <= 0.2  VALUE  0.02321
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     <= 0.2  VALUE  0.0232
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     <= 0.2  VALUE  0.023
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     <= 0.2  VALUE  0.02289
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     <= 0.2  VALUE  0.02282
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     <= 0.2  VALUE  0.02281
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0228
SPACE     > 0.8  <= 1  DRAWN_WIDTH     <= 0.2  VALUE  0.02279
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.2  VALUE  0.02279
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE             > 10  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE             <= 0.18  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02234
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02233
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02227
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02223
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0222
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02219
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02218
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02218
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02217
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02217
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02215
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02215
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE             > 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE             <= 0.18  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0219
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0219
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02187
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02186
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02185
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02184
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02183
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02182
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02181
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02181
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0218
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0218
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02179
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02179
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02178
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02178
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE             > 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE             <= 0.18  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02176
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02175
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02174
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02173
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02172
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02171
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.0217
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02169
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02169
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02168
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02168
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02167
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02167
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02166
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02166
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02165
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02164
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02164
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02163
SPACE             > 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02163
SPACE             <= 0.18  DRAWN_WIDTH     > 1.2  VALUE  0.02176
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 1.2  VALUE  0.02175
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 1.2  VALUE  0.02174
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 1.2  VALUE  0.02173
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 1.2  VALUE  0.02172
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 1.2  VALUE  0.02171
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 1.2  VALUE  0.0217
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 1.2  VALUE  0.02169
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 1.2  VALUE  0.02169
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 1.2  VALUE  0.02168
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 1.2  VALUE  0.02168
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 1.2  VALUE  0.02167
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 1.2  VALUE  0.02167
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 1.2  VALUE  0.02166
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 1.2  VALUE  0.02166
SPACE     > 3  <= 4  DRAWN_WIDTH     > 1.2  VALUE  0.02165
SPACE     > 4  <= 5  DRAWN_WIDTH     > 1.2  VALUE  0.02164
SPACE     > 5  <= 7  DRAWN_WIDTH     > 1.2  VALUE  0.02164
SPACE     > 7  <= 10  DRAWN_WIDTH     > 1.2  VALUE  0.02163
SPACE             > 10  DRAWN_WIDTH     > 1.2  VALUE  0.02163

PARASITIC VARIATION metal2 THICKNESS -0.105000 0.426341 // density1 and density2 

PARASITIC VARIATION metal3 WIDTH
SPACE             <= 0.18  DRAWN_WIDTH     <= 0.2  VALUE  0.0198
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     <= 0.2  VALUE  0.02
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     <= 0.2  VALUE  0.0453
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     <= 0.2  VALUE  0.0606
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     <= 0.2  VALUE  0.0709
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     <= 0.2  VALUE  0.0712
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0715
SPACE     > 0.8  <= 1  DRAWN_WIDTH     <= 0.2  VALUE  0.0718
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.2  VALUE  0.072
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.2  VALUE  0.0721
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.2  VALUE  0.0722
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0723
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.2  VALUE  0.0723
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.2  VALUE  0.0725
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.2  VALUE  0.0726
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.2  VALUE  0.0727
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.2  VALUE  0.0728
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.2  VALUE  0.0728
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.2  VALUE  0.0729
SPACE             > 10  DRAWN_WIDTH     <= 0.2  VALUE  0.0729
SPACE             <= 0.18  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0189
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0191
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0445
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0598
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0701
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0704
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0708
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0711
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0714
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0716
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0717
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0718
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0719
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0721
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0723
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0724
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0726
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0727
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0728
SPACE             > 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0728
SPACE             <= 0.18  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0183
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0184
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0437
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0693
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0695
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.07
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0703
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0706
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0708
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.071
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0711
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0713
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0715
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0717
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0722
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0724
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0726
SPACE             > 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0726
SPACE             <= 0.18  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.018
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.018
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.043
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE             > 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE             <= 0.18  DRAWN_WIDTH     > 1.2  VALUE  0.018
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 1.2  VALUE  0.018
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 1.2  VALUE  0.043
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 1.2  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 3  <= 4  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 5  <= 7  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 7  <= 10  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE             > 10  DRAWN_WIDTH     > 1.2  VALUE  0.072

PARASITIC VARIATION metal3 RHO
SPACE             <= 0.18  DRAWN_WIDTH     <= 0.2  VALUE  0.02321
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     <= 0.2  VALUE  0.0232
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     <= 0.2  VALUE  0.023
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     <= 0.2  VALUE  0.02289
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     <= 0.2  VALUE  0.02282
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     <= 0.2  VALUE  0.02281
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0228
SPACE     > 0.8  <= 1  DRAWN_WIDTH     <= 0.2  VALUE  0.02279
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.2  VALUE  0.02279
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE             > 10  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE             <= 0.18  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02234
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02233
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02227
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02223
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0222
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02219
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02218
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02218
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02217
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02217
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02215
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02215
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE             > 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE             <= 0.18  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0219
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0219
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02187
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02186
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02185
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02184
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02183
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02182
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02181
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02181
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0218
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0218
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02179
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02179
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02178
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02178
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE             > 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE             <= 0.18  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02176
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02175
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02174
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02173
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02172
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02171
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.0217
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02169
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02169
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02168
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02168
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02167
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02167
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02166
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02166
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02165
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02164
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02164
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02163
SPACE             > 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02163
SPACE             <= 0.18  DRAWN_WIDTH     > 1.2  VALUE  0.02176
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 1.2  VALUE  0.02175
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 1.2  VALUE  0.02174
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 1.2  VALUE  0.02173
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 1.2  VALUE  0.02172
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 1.2  VALUE  0.02171
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 1.2  VALUE  0.0217
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 1.2  VALUE  0.02169
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 1.2  VALUE  0.02169
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 1.2  VALUE  0.02168
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 1.2  VALUE  0.02168
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 1.2  VALUE  0.02167
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 1.2  VALUE  0.02167
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 1.2  VALUE  0.02166
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 1.2  VALUE  0.02166
SPACE     > 3  <= 4  DRAWN_WIDTH     > 1.2  VALUE  0.02165
SPACE     > 4  <= 5  DRAWN_WIDTH     > 1.2  VALUE  0.02164
SPACE     > 5  <= 7  DRAWN_WIDTH     > 1.2  VALUE  0.02164
SPACE     > 7  <= 10  DRAWN_WIDTH     > 1.2  VALUE  0.02163
SPACE             > 10  DRAWN_WIDTH     > 1.2  VALUE  0.02163

PARASITIC VARIATION metal3 THICKNESS -0.105000 0.426341 // density1 and density2 

PARASITIC VARIATION metal4 WIDTH
SPACE             <= 0.18  DRAWN_WIDTH     <= 0.2  VALUE  0.0198
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     <= 0.2  VALUE  0.02
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     <= 0.2  VALUE  0.0453
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     <= 0.2  VALUE  0.0606
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     <= 0.2  VALUE  0.0709
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     <= 0.2  VALUE  0.0712
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0715
SPACE     > 0.8  <= 1  DRAWN_WIDTH     <= 0.2  VALUE  0.0718
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.2  VALUE  0.072
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.2  VALUE  0.0721
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.2  VALUE  0.0722
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0723
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.2  VALUE  0.0723
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.2  VALUE  0.0725
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.2  VALUE  0.0726
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.2  VALUE  0.0727
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.2  VALUE  0.0728
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.2  VALUE  0.0728
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.2  VALUE  0.0729
SPACE             > 10  DRAWN_WIDTH     <= 0.2  VALUE  0.0729
SPACE             <= 0.18  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0189
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0191
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0445
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0598
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0701
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0704
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0708
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0711
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0714
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0716
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0717
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0718
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0719
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0721
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0723
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0724
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0726
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0727
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0728
SPACE             > 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0728
SPACE             <= 0.18  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0183
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0184
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0437
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0693
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0695
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.07
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0703
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0706
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0708
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.071
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0711
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0713
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0715
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0717
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0722
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0724
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0726
SPACE             > 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0726
SPACE             <= 0.18  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.018
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.018
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.043
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE             > 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE             <= 0.18  DRAWN_WIDTH     > 1.2  VALUE  0.018
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 1.2  VALUE  0.018
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 1.2  VALUE  0.043
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 1.2  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 3  <= 4  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 5  <= 7  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 7  <= 10  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE             > 10  DRAWN_WIDTH     > 1.2  VALUE  0.072

PARASITIC VARIATION metal4 RHO
SPACE             <= 0.18  DRAWN_WIDTH     <= 0.2  VALUE  0.02321
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     <= 0.2  VALUE  0.0232
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     <= 0.2  VALUE  0.023
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     <= 0.2  VALUE  0.02289
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     <= 0.2  VALUE  0.02282
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     <= 0.2  VALUE  0.02281
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0228
SPACE     > 0.8  <= 1  DRAWN_WIDTH     <= 0.2  VALUE  0.02279
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.2  VALUE  0.02279
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE             > 10  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE             <= 0.18  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02234
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02233
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02227
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02223
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0222
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02219
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02218
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02218
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02217
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02217
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02215
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02215
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE             > 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE             <= 0.18  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0219
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0219
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02187
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02186
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02185
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02184
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02183
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02182
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02181
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02181
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0218
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0218
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02179
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02179
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02178
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02178
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE             > 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE             <= 0.18  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02176
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02175
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02174
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02173
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02172
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02171
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.0217
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02169
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02169
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02168
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02168
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02167
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02167
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02166
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02166
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02165
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02164
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02164
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02163
SPACE             > 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02163
SPACE             <= 0.18  DRAWN_WIDTH     > 1.2  VALUE  0.02176
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 1.2  VALUE  0.02175
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 1.2  VALUE  0.02174
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 1.2  VALUE  0.02173
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 1.2  VALUE  0.02172
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 1.2  VALUE  0.02171
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 1.2  VALUE  0.0217
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 1.2  VALUE  0.02169
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 1.2  VALUE  0.02169
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 1.2  VALUE  0.02168
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 1.2  VALUE  0.02168
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 1.2  VALUE  0.02167
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 1.2  VALUE  0.02167
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 1.2  VALUE  0.02166
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 1.2  VALUE  0.02166
SPACE     > 3  <= 4  DRAWN_WIDTH     > 1.2  VALUE  0.02165
SPACE     > 4  <= 5  DRAWN_WIDTH     > 1.2  VALUE  0.02164
SPACE     > 5  <= 7  DRAWN_WIDTH     > 1.2  VALUE  0.02164
SPACE     > 7  <= 10  DRAWN_WIDTH     > 1.2  VALUE  0.02163
SPACE             > 10  DRAWN_WIDTH     > 1.2  VALUE  0.02163

PARASITIC VARIATION metal4 THICKNESS -0.105000 0.426341 // density1 and density2 

PARASITIC VARIATION metal5 WIDTH
SPACE             <= 0.18  DRAWN_WIDTH     <= 0.2  VALUE  0.0198
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     <= 0.2  VALUE  0.02
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     <= 0.2  VALUE  0.0453
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     <= 0.2  VALUE  0.0606
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     <= 0.2  VALUE  0.0709
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     <= 0.2  VALUE  0.0712
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0715
SPACE     > 0.8  <= 1  DRAWN_WIDTH     <= 0.2  VALUE  0.0718
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.2  VALUE  0.072
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.2  VALUE  0.0721
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.2  VALUE  0.0722
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0723
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.2  VALUE  0.0723
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.2  VALUE  0.0725
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.2  VALUE  0.0726
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.2  VALUE  0.0727
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.2  VALUE  0.0728
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.2  VALUE  0.0728
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.2  VALUE  0.0729
SPACE             > 10  DRAWN_WIDTH     <= 0.2  VALUE  0.0729
SPACE             <= 0.18  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0189
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0191
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0445
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0598
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0701
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0704
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0708
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0711
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0714
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0716
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0717
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0718
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0719
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0721
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0723
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0724
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0726
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0727
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0728
SPACE             > 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0728
SPACE             <= 0.18  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0183
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0184
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0437
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0693
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0695
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.07
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0703
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0706
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0708
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.071
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0711
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0713
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0715
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0717
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0722
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0724
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0726
SPACE             > 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0726
SPACE             <= 0.18  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.018
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.018
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.043
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE             > 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE             <= 0.18  DRAWN_WIDTH     > 1.2  VALUE  0.018
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 1.2  VALUE  0.018
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 1.2  VALUE  0.043
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 1.2  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 3  <= 4  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 5  <= 7  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 7  <= 10  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE             > 10  DRAWN_WIDTH     > 1.2  VALUE  0.072

PARASITIC VARIATION metal5 RHO
SPACE             <= 0.18  DRAWN_WIDTH     <= 0.2  VALUE  0.02321
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     <= 0.2  VALUE  0.0232
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     <= 0.2  VALUE  0.023
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     <= 0.2  VALUE  0.02289
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     <= 0.2  VALUE  0.02282
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     <= 0.2  VALUE  0.02281
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0228
SPACE     > 0.8  <= 1  DRAWN_WIDTH     <= 0.2  VALUE  0.02279
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.2  VALUE  0.02279
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE             > 10  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE             <= 0.18  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02234
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02233
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02227
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02223
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0222
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02219
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02218
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02218
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02217
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02217
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02215
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02215
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE             > 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE             <= 0.18  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0219
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0219
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02187
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02186
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02185
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02184
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02183
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02182
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02181
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02181
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0218
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0218
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02179
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02179
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02178
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02178
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE             > 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE             <= 0.18  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02176
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02175
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02174
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02173
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02172
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02171
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.0217
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02169
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02169
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02168
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02168
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02167
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02167
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02166
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02166
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02165
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02164
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02164
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02163
SPACE             > 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02163
SPACE             <= 0.18  DRAWN_WIDTH     > 1.2  VALUE  0.02176
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 1.2  VALUE  0.02175
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 1.2  VALUE  0.02174
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 1.2  VALUE  0.02173
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 1.2  VALUE  0.02172
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 1.2  VALUE  0.02171
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 1.2  VALUE  0.0217
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 1.2  VALUE  0.02169
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 1.2  VALUE  0.02169
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 1.2  VALUE  0.02168
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 1.2  VALUE  0.02168
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 1.2  VALUE  0.02167
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 1.2  VALUE  0.02167
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 1.2  VALUE  0.02166
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 1.2  VALUE  0.02166
SPACE     > 3  <= 4  DRAWN_WIDTH     > 1.2  VALUE  0.02165
SPACE     > 4  <= 5  DRAWN_WIDTH     > 1.2  VALUE  0.02164
SPACE     > 5  <= 7  DRAWN_WIDTH     > 1.2  VALUE  0.02164
SPACE     > 7  <= 10  DRAWN_WIDTH     > 1.2  VALUE  0.02163
SPACE             > 10  DRAWN_WIDTH     > 1.2  VALUE  0.02163

PARASITIC VARIATION metal5 THICKNESS -0.105000 0.426341 // density1 and density2 

PARASITIC VARIATION metal6 WIDTH
SPACE             <= 0.18  DRAWN_WIDTH     <= 0.2  VALUE  0.0198
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     <= 0.2  VALUE  0.02
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     <= 0.2  VALUE  0.0453
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     <= 0.2  VALUE  0.0606
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     <= 0.2  VALUE  0.0709
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     <= 0.2  VALUE  0.0712
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0715
SPACE     > 0.8  <= 1  DRAWN_WIDTH     <= 0.2  VALUE  0.0718
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.2  VALUE  0.072
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.2  VALUE  0.0721
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.2  VALUE  0.0722
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0723
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.2  VALUE  0.0723
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.2  VALUE  0.0725
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.2  VALUE  0.0726
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.2  VALUE  0.0727
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.2  VALUE  0.0728
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.2  VALUE  0.0728
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.2  VALUE  0.0729
SPACE             > 10  DRAWN_WIDTH     <= 0.2  VALUE  0.0729
SPACE             <= 0.18  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0189
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0191
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0445
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0598
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0701
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0704
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0708
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0711
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0714
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0716
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0717
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0718
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0719
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0721
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0723
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0724
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0726
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0727
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0728
SPACE             > 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0728
SPACE             <= 0.18  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0183
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0184
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0437
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0693
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0695
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.07
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0703
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0706
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0708
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.071
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0711
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0713
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0715
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0717
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0722
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0724
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0726
SPACE             > 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0726
SPACE             <= 0.18  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.018
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.018
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.043
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE             > 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE             <= 0.18  DRAWN_WIDTH     > 1.2  VALUE  0.018
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 1.2  VALUE  0.018
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 1.2  VALUE  0.043
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 1.2  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 3  <= 4  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 5  <= 7  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 7  <= 10  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE             > 10  DRAWN_WIDTH     > 1.2  VALUE  0.072

PARASITIC VARIATION metal6 RHO
SPACE             <= 0.18  DRAWN_WIDTH     <= 0.2  VALUE  0.02321
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     <= 0.2  VALUE  0.0232
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     <= 0.2  VALUE  0.023
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     <= 0.2  VALUE  0.02289
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     <= 0.2  VALUE  0.02282
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     <= 0.2  VALUE  0.02281
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0228
SPACE     > 0.8  <= 1  DRAWN_WIDTH     <= 0.2  VALUE  0.02279
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.2  VALUE  0.02279
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE             > 10  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE             <= 0.18  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02234
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02233
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02227
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02223
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0222
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02219
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02218
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02218
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02217
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02217
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02215
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02215
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE             > 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE             <= 0.18  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0219
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0219
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02187
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02186
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02185
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02184
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02183
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02182
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02181
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02181
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0218
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0218
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02179
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02179
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02178
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02178
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE             > 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE             <= 0.18  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02176
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02175
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02174
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02173
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02172
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02171
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.0217
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02169
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02169
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02168
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02168
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02167
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02167
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02166
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02166
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02165
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02164
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02164
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02163
SPACE             > 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02163
SPACE             <= 0.18  DRAWN_WIDTH     > 1.2  VALUE  0.02176
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 1.2  VALUE  0.02175
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 1.2  VALUE  0.02174
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 1.2  VALUE  0.02173
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 1.2  VALUE  0.02172
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 1.2  VALUE  0.02171
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 1.2  VALUE  0.0217
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 1.2  VALUE  0.02169
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 1.2  VALUE  0.02169
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 1.2  VALUE  0.02168
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 1.2  VALUE  0.02168
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 1.2  VALUE  0.02167
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 1.2  VALUE  0.02167
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 1.2  VALUE  0.02166
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 1.2  VALUE  0.02166
SPACE     > 3  <= 4  DRAWN_WIDTH     > 1.2  VALUE  0.02165
SPACE     > 4  <= 5  DRAWN_WIDTH     > 1.2  VALUE  0.02164
SPACE     > 5  <= 7  DRAWN_WIDTH     > 1.2  VALUE  0.02164
SPACE     > 7  <= 10  DRAWN_WIDTH     > 1.2  VALUE  0.02163
SPACE             > 10  DRAWN_WIDTH     > 1.2  VALUE  0.02163

PARASITIC VARIATION metal6 THICKNESS -0.105000 0.426341 // density1 and density2 

PARASITIC VARIATION metal7 WIDTH
SPACE             <= 0.18  DRAWN_WIDTH     <= 0.2  VALUE  0.0198
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     <= 0.2  VALUE  0.02
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     <= 0.2  VALUE  0.0453
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     <= 0.2  VALUE  0.0606
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     <= 0.2  VALUE  0.0709
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     <= 0.2  VALUE  0.0712
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0715
SPACE     > 0.8  <= 1  DRAWN_WIDTH     <= 0.2  VALUE  0.0718
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.2  VALUE  0.072
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.2  VALUE  0.0721
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.2  VALUE  0.0722
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0723
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.2  VALUE  0.0723
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.2  VALUE  0.0725
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.2  VALUE  0.0726
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.2  VALUE  0.0727
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.2  VALUE  0.0728
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.2  VALUE  0.0728
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.2  VALUE  0.0729
SPACE             > 10  DRAWN_WIDTH     <= 0.2  VALUE  0.0729
SPACE             <= 0.18  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0189
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0191
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0445
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0598
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0701
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0704
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0708
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0711
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0714
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0716
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0717
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0718
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0719
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0721
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0723
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0724
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0726
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0727
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0728
SPACE             > 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0728
SPACE             <= 0.18  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0183
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0184
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0437
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0693
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0695
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.07
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0703
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0706
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0708
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.071
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0711
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0713
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0715
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0717
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0722
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0724
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0726
SPACE             > 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0726
SPACE             <= 0.18  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.018
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.018
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.043
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.069
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.07
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.071
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE             > 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.072
SPACE             <= 0.18  DRAWN_WIDTH     > 1.2  VALUE  0.018
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 1.2  VALUE  0.018
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 1.2  VALUE  0.043
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 1.2  VALUE  0.059
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 1.2  VALUE  0.069
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 1.2  VALUE  0.07
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 1.2  VALUE  0.071
SPACE     > 3  <= 4  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 4  <= 5  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 5  <= 7  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE     > 7  <= 10  DRAWN_WIDTH     > 1.2  VALUE  0.072
SPACE             > 10  DRAWN_WIDTH     > 1.2  VALUE  0.072

PARASITIC VARIATION metal7 RHO
SPACE             <= 0.18  DRAWN_WIDTH     <= 0.2  VALUE  0.02321
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     <= 0.2  VALUE  0.0232
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     <= 0.2  VALUE  0.023
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     <= 0.2  VALUE  0.02289
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     <= 0.2  VALUE  0.02282
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     <= 0.2  VALUE  0.02281
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     <= 0.2  VALUE  0.0228
SPACE     > 0.8  <= 1  DRAWN_WIDTH     <= 0.2  VALUE  0.02279
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.2  VALUE  0.02279
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.2  VALUE  0.02278
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.2  VALUE  0.02277
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE             > 10  DRAWN_WIDTH     <= 0.2  VALUE  0.02276
SPACE             <= 0.18  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02234
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02233
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02227
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02223
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.0222
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02219
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02218
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02218
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02217
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02217
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02216
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02215
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02215
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE             > 10  DRAWN_WIDTH     > 0.2  <= 0.4  VALUE  0.02214
SPACE             <= 0.18  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0219
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0219
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02187
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02186
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02185
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02184
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02183
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02182
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02181
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02181
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0218
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.0218
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02179
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02179
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02178
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02178
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE             > 10  DRAWN_WIDTH     > 0.4  <= 0.8  VALUE  0.02177
SPACE             <= 0.18  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02176
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02175
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02174
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02173
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02172
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02171
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.0217
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02169
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02169
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02168
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02168
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02167
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02167
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02166
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02166
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02165
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02164
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02164
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02163
SPACE             > 10  DRAWN_WIDTH     > 0.8  <= 1.2  VALUE  0.02163
SPACE             <= 0.18  DRAWN_WIDTH     > 1.2  VALUE  0.02176
SPACE     > 0.18  <= 0.21  DRAWN_WIDTH     > 1.2  VALUE  0.02175
SPACE     > 0.21  <= 0.31  DRAWN_WIDTH     > 1.2  VALUE  0.02174
SPACE     > 0.31  <= 0.41  DRAWN_WIDTH     > 1.2  VALUE  0.02173
SPACE     > 0.41  <= 0.51  DRAWN_WIDTH     > 1.2  VALUE  0.02172
SPACE     > 0.51  <= 0.61  DRAWN_WIDTH     > 1.2  VALUE  0.02171
SPACE     > 0.61  <= 0.8  DRAWN_WIDTH     > 1.2  VALUE  0.0217
SPACE     > 0.8  <= 1  DRAWN_WIDTH     > 1.2  VALUE  0.02169
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 1.2  VALUE  0.02169
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 1.2  VALUE  0.02168
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 1.2  VALUE  0.02168
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 1.2  VALUE  0.02167
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 1.2  VALUE  0.02167
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 1.2  VALUE  0.02166
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 1.2  VALUE  0.02166
SPACE     > 3  <= 4  DRAWN_WIDTH     > 1.2  VALUE  0.02165
SPACE     > 4  <= 5  DRAWN_WIDTH     > 1.2  VALUE  0.02164
SPACE     > 5  <= 7  DRAWN_WIDTH     > 1.2  VALUE  0.02164
SPACE     > 7  <= 10  DRAWN_WIDTH     > 1.2  VALUE  0.02163
SPACE             > 10  DRAWN_WIDTH     > 1.2  VALUE  0.02163

PARASITIC VARIATION metal7 THICKNESS -0.105000 0.426341 // density1 and density2 

PARASITIC VARIATION metal8 WIDTH
SPACE             <= 0.4  DRAWN_WIDTH     <= 0.4  VALUE  0.0399
SPACE     > 0.4  <= 0.42  DRAWN_WIDTH     <= 0.4  VALUE  0.04
SPACE     > 0.42  <= 0.56  DRAWN_WIDTH     <= 0.4  VALUE  0.0405
SPACE     > 0.56  <= 0.66  DRAWN_WIDTH     <= 0.4  VALUE  0.0408
SPACE     > 0.66  <= 0.76  DRAWN_WIDTH     <= 0.4  VALUE  0.0411
SPACE     > 0.76  <= 0.86  DRAWN_WIDTH     <= 0.4  VALUE  0.0413
SPACE     > 0.86  <= 1  DRAWN_WIDTH     <= 0.4  VALUE  0.0416
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.4  VALUE  0.0418
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.4  VALUE  0.042
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.4  VALUE  0.0422
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.4  VALUE  0.0424
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.4  VALUE  0.0425
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.4  VALUE  0.0427
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.4  VALUE  0.0428
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.4  VALUE  0.0431
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.4  VALUE  0.0432
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.4  VALUE  0.0433
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.4  VALUE  0.0435
SPACE             > 10  DRAWN_WIDTH     <= 0.4  VALUE  0.0435
SPACE             <= 0.4  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0387
SPACE     > 0.4  <= 0.42  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0388
SPACE     > 0.42  <= 0.56  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0393
SPACE     > 0.56  <= 0.66  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0396
SPACE     > 0.66  <= 0.76  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0398
SPACE     > 0.76  <= 0.86  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0401
SPACE     > 0.86  <= 1  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0403
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0407
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0409
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0412
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0414
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0415
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0418
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0421
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0424
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0427
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0429
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0432
SPACE             > 10  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0432
SPACE             <= 0.4  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.038
SPACE     > 0.4  <= 0.42  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.038
SPACE     > 0.42  <= 0.56  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.038
SPACE     > 0.56  <= 0.66  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.039
SPACE     > 0.66  <= 0.76  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.039
SPACE     > 0.76  <= 0.86  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.039
SPACE     > 0.86  <= 1  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.039
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.04
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.04
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.04
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.04
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.04
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.041
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.041
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.042
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.042
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.042
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.043
SPACE             > 10  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.043
SPACE             <= 0.4  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.038
SPACE     > 0.4  <= 0.42  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.038
SPACE     > 0.42  <= 0.56  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.038
SPACE     > 0.56  <= 0.66  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.038
SPACE     > 0.66  <= 0.76  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.038
SPACE     > 0.76  <= 0.86  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.038
SPACE     > 0.86  <= 1  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.039
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.039
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.039
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.039
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.04
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.04
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.04
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.04
SPACE     > 3  <= 4  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.041
SPACE     > 4  <= 5  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.041
SPACE     > 5  <= 7  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.042
SPACE     > 7  <= 10  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.042
SPACE             > 10  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.042
SPACE             <= 0.4  DRAWN_WIDTH     > 2.64  VALUE  0.038
SPACE     > 0.4  <= 0.42  DRAWN_WIDTH     > 2.64  VALUE  0.038
SPACE     > 0.42  <= 0.56  DRAWN_WIDTH     > 2.64  VALUE  0.038
SPACE     > 0.56  <= 0.66  DRAWN_WIDTH     > 2.64  VALUE  0.038
SPACE     > 0.66  <= 0.76  DRAWN_WIDTH     > 2.64  VALUE  0.038
SPACE     > 0.76  <= 0.86  DRAWN_WIDTH     > 2.64  VALUE  0.038
SPACE     > 0.86  <= 1  DRAWN_WIDTH     > 2.64  VALUE  0.039
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 2.64  VALUE  0.039
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 2.64  VALUE  0.039
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 2.64  VALUE  0.039
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 2.64  VALUE  0.04
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 2.64  VALUE  0.04
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 2.64  VALUE  0.04
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 2.64  VALUE  0.04
SPACE     > 3  <= 4  DRAWN_WIDTH     > 2.64  VALUE  0.041
SPACE     > 4  <= 5  DRAWN_WIDTH     > 2.64  VALUE  0.041
SPACE     > 5  <= 7  DRAWN_WIDTH     > 2.64  VALUE  0.042
SPACE     > 7  <= 10  DRAWN_WIDTH     > 2.64  VALUE  0.042
SPACE             > 10  DRAWN_WIDTH     > 2.64  VALUE  0.042

PARASITIC VARIATION metal8 RHO
SPACE             <= 0.4  DRAWN_WIDTH     <= 0.4  VALUE  0.02055
SPACE     > 0.4  <= 0.42  DRAWN_WIDTH     <= 0.4  VALUE  0.02055
SPACE     > 0.42  <= 0.56  DRAWN_WIDTH     <= 0.4  VALUE  0.02054
SPACE     > 0.56  <= 0.66  DRAWN_WIDTH     <= 0.4  VALUE  0.02054
SPACE     > 0.66  <= 0.76  DRAWN_WIDTH     <= 0.4  VALUE  0.02053
SPACE     > 0.76  <= 0.86  DRAWN_WIDTH     <= 0.4  VALUE  0.02053
SPACE     > 0.86  <= 1  DRAWN_WIDTH     <= 0.4  VALUE  0.02053
SPACE     > 1  <= 1.2  DRAWN_WIDTH     <= 0.4  VALUE  0.02052
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     <= 0.4  VALUE  0.02052
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     <= 0.4  VALUE  0.02051
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     <= 0.4  VALUE  0.02051
SPACE     > 1.8  <= 2  DRAWN_WIDTH     <= 0.4  VALUE  0.02051
SPACE     > 2  <= 2.5  DRAWN_WIDTH     <= 0.4  VALUE  0.02051
SPACE     > 2.5  <= 3  DRAWN_WIDTH     <= 0.4  VALUE  0.0205
SPACE     > 3  <= 4  DRAWN_WIDTH     <= 0.4  VALUE  0.0205
SPACE     > 4  <= 5  DRAWN_WIDTH     <= 0.4  VALUE  0.0205
SPACE     > 5  <= 7  DRAWN_WIDTH     <= 0.4  VALUE  0.0205
SPACE     > 7  <= 10  DRAWN_WIDTH     <= 0.4  VALUE  0.02049
SPACE             > 10  DRAWN_WIDTH     <= 0.4  VALUE  0.02049
SPACE             <= 0.4  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01951
SPACE     > 0.4  <= 0.42  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01951
SPACE     > 0.42  <= 0.56  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0195
SPACE     > 0.56  <= 0.66  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0195
SPACE     > 0.66  <= 0.76  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.0195
SPACE     > 0.76  <= 0.86  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01949
SPACE     > 0.86  <= 1  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01949
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01949
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01948
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01948
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01948
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01947
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01947
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01947
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01946
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01946
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01946
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01945
SPACE             > 10  DRAWN_WIDTH     > 0.4  <= 0.88  VALUE  0.01945
SPACE             <= 0.4  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01908
SPACE     > 0.4  <= 0.42  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01908
SPACE     > 0.42  <= 0.56  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01908
SPACE     > 0.56  <= 0.66  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01907
SPACE     > 0.66  <= 0.76  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01907
SPACE     > 0.76  <= 0.86  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01907
SPACE     > 0.86  <= 1  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01906
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01906
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01906
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01905
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01905
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01905
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01904
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01904
SPACE     > 3  <= 4  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01904
SPACE     > 4  <= 5  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01903
SPACE     > 5  <= 7  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01903
SPACE     > 7  <= 10  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01902
SPACE             > 10  DRAWN_WIDTH     > 0.88  <= 1.76  VALUE  0.01902
SPACE             <= 0.4  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01894
SPACE     > 0.4  <= 0.42  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01894
SPACE     > 0.42  <= 0.56  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01893
SPACE     > 0.56  <= 0.66  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01893
SPACE     > 0.66  <= 0.76  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01893
SPACE     > 0.76  <= 0.86  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01893
SPACE     > 0.86  <= 1  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01893
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01892
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01892
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01892
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01891
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01891
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01891
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.0189
SPACE     > 3  <= 4  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.0189
SPACE     > 4  <= 5  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01889
SPACE     > 5  <= 7  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01889
SPACE     > 7  <= 10  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01888
SPACE             > 10  DRAWN_WIDTH     > 1.76  <= 2.64  VALUE  0.01888
SPACE             <= 0.4  DRAWN_WIDTH     > 2.64  VALUE  0.01894
SPACE     > 0.4  <= 0.42  DRAWN_WIDTH     > 2.64  VALUE  0.01894
SPACE     > 0.42  <= 0.56  DRAWN_WIDTH     > 2.64  VALUE  0.01893
SPACE     > 0.56  <= 0.66  DRAWN_WIDTH     > 2.64  VALUE  0.01893
SPACE     > 0.66  <= 0.76  DRAWN_WIDTH     > 2.64  VALUE  0.01893
SPACE     > 0.76  <= 0.86  DRAWN_WIDTH     > 2.64  VALUE  0.01893
SPACE     > 0.86  <= 1  DRAWN_WIDTH     > 2.64  VALUE  0.01893
SPACE     > 1  <= 1.2  DRAWN_WIDTH     > 2.64  VALUE  0.01892
SPACE     > 1.2  <= 1.4  DRAWN_WIDTH     > 2.64  VALUE  0.01892
SPACE     > 1.4  <= 1.6  DRAWN_WIDTH     > 2.64  VALUE  0.01892
SPACE     > 1.6  <= 1.8  DRAWN_WIDTH     > 2.64  VALUE  0.01891
SPACE     > 1.8  <= 2  DRAWN_WIDTH     > 2.64  VALUE  0.01891
SPACE     > 2  <= 2.5  DRAWN_WIDTH     > 2.64  VALUE  0.01891
SPACE     > 2.5  <= 3  DRAWN_WIDTH     > 2.64  VALUE  0.0189
SPACE     > 3  <= 4  DRAWN_WIDTH     > 2.64  VALUE  0.0189
SPACE     > 4  <= 5  DRAWN_WIDTH     > 2.64  VALUE  0.01889
SPACE     > 5  <= 7  DRAWN_WIDTH     > 2.64  VALUE  0.01889
SPACE     > 7  <= 10  DRAWN_WIDTH     > 2.64  VALUE  0.01888
SPACE             > 10  DRAWN_WIDTH     > 2.64  VALUE  0.01888

PARASITIC VARIATION metal8 THICKNESS -0.145000 0.907805 // density1 and density2 

#else

// classic mode 

PEX THICKNESS metal1 NOMINAL 0.260000

PEX THICKNESS metal2 NOMINAL 0.370000

PEX THICKNESS metal3 NOMINAL 0.370000

PEX THICKNESS metal4 NOMINAL 0.370000

PEX THICKNESS metal5 NOMINAL 0.370000

PEX THICKNESS metal6 NOMINAL 0.370000

PEX THICKNESS metal7 NOMINAL 0.370000

PEX THICKNESS metal8 NOMINAL 0.830000

#endif

PARASITIC VARIATION poly WIDTH
SPACE           <= 0.1800  DRAWN_WIDTH     <= 0.1300  VALUE  -0.0200
SPACE           <= 0.1800  DRAWN_WIDTH     > 0.1300  VALUE  -0.0200
SPACE           > 0.1800  DRAWN_WIDTH     > 0.1300  VALUE  -0.0200
SPACE           > 0.1800  DRAWN_WIDTH     <= 0.1300  VALUE  -0.0200

PEX THICKNESS poly NOMINAL 0.180000

#ifndef XCALIBRATE_EXCLUDE_PROFILE_field
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,poly
//
CAPACITANCE INTRINSIC PLATE poly MASK
[
    PROPERTY C
    C = 0.0837055 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal1
//
CAPACITANCE INTRINSIC PLATE metal1 MASK
[
    PROPERTY C
    C = 0.0405534 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal2
//
CAPACITANCE INTRINSIC PLATE metal2 MASK
[
    PROPERTY C
    C = 0.0223921 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal3
//
CAPACITANCE INTRINSIC PLATE metal3 MASK
[
    PROPERTY C
    C = 0.0146603 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal4
//
CAPACITANCE INTRINSIC PLATE metal4 MASK
[
    PROPERTY C
    C = 0.0108975 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal5
//
CAPACITANCE INTRINSIC PLATE metal5 MASK
[
    PROPERTY C
    C = 0.00867175 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal6
//
CAPACITANCE INTRINSIC PLATE metal6 MASK
[
    PROPERTY C
    C = 0.007201 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal7
//
CAPACITANCE INTRINSIC PLATE metal7 MASK
[
    PROPERTY C
    C = 0.00615677 * area()
]
//
//  model: 2  target: intrinsicp  profile: field  layers: field_base,metal8
//
CAPACITANCE INTRINSIC PLATE metal8 MASK
[
    PROPERTY C
    C = 0.00525184 * area()
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,poly
//
CAPACITANCE INTRINSIC FRINGE poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.136655 * (1 - exp(-1.29094 * (distance() + 0.108672))) * pow(width() , -0.00125145 * distance() + 0.0506898) * (0.236126 * thickness() + 0.287594) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0416112 * (1 - exp(-1.22965 * (distance() + 0.0607615))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.185066 * pow(width() , 0.0771144) * (0.269326 * thickness() + 0.217122) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 5 + m43ink4 * 0.42)))
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,poly
//
CAPACITANCE NEARBODY poly WITH SHIELD poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = length() * (exp(-1.17547 - 1.49857 * distance()) + 0.0889015 / pow(distance() , 1.0973)) * 2.77187 * pow((width1() + width2()) / 2 , 0.142254) * (0.111452 * (thickness1() + thickness2()) / 2 + 0.0188318) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.42)))
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,metal1
//
CAPACITANCE INTRINSIC FRINGE metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.191023 * (1 - exp(-0.618349 * (distance() + 0.207563))) * pow(width() , 0.00629951 * distance() + 0.0151393) * (0.0636065 * thickness() + 0.207573) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0400092 * (1 - exp(-0.651509 * (distance() + 0.0879185))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.175815 * pow(width() , 0.0783148) * (0.132764 * thickness() + 0.223029) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.89)))
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,metal1
//
CAPACITANCE NEARBODY metal1 WITH SHIELD metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.04176 - 1.01167 * distance()) + 0.17541 / pow(distance() , 0.982947)) * 3.22015 * pow((width1() + width2()) / 2 , 0.111089) * (0.0523377 * (thickness1() + thickness2()) / 2 + 0.0175549) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.89)))
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,metal2
//
CAPACITANCE INTRINSIC FRINGE metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.177932 * (1 - exp(-0.373768 * (distance() + 0.399615))) * pow(width() , 0.00831831 * distance() + -0.00744434) * (0.0315299 * thickness() + 0.19963) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.6)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0352851 * (1 - exp(-0.402468 * (distance() + 0.160407))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.6)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.156841 * pow(width() , 0.0811055) * (0.0745294 * thickness() + 0.225352) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.6)))
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,metal2
//
CAPACITANCE NEARBODY metal2 WITH SHIELD metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.56418 - 0.586642 * distance()) + 0.190325 / pow(distance() , 0.930911)) * 5.37385 * pow((width1() + width2()) / 2 , 0.119159) * (0.0281738 * (thickness1() + thickness2()) / 2 + 0.0138232) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.6)))
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,metal3
//
CAPACITANCE INTRINSIC FRINGE metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.162153 * (1 - exp(-0.271167 * (distance() + 0.618224))) * pow(width() , 0.010228 * distance() + -0.0269383) * (0.0255043 * thickness() + 0.196765) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.42)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0311552 * (1 - exp(-0.298805 * (distance() + 0.23212))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.42)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.149612 * pow(width() , 0.0819529) * (0.0641878 * thickness() + 0.212192) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.42)))
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,metal3
//
CAPACITANCE NEARBODY metal3 WITH SHIELD metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.39397 - 0.409138 * distance()) + 0.308587 / pow(distance() , 0.881369)) * 6.04276 * pow((width1() + width2()) / 2 , 0.128866) * (0.0170562 * (thickness1() + thickness2()) / 2 + 0.0089193) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.42)))
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,metal4
//
CAPACITANCE INTRINSIC FRINGE metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.152557 * (1 - exp(-0.214369 * (distance() + 0.842134))) * pow(width() , 0.011005 * distance() + -0.0395772) * (0.0192817 * thickness() + 0.194208) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.24)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0285875 * (1 - exp(-0.238355 * (distance() + 0.300363))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.24)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.14519 * pow(width() , 0.0810088) * (0.0562744 * thickness() + 0.202297) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.24)))
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,metal4
//
CAPACITANCE NEARBODY metal4 WITH SHIELD metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.772642 - 0.304012 * distance()) + 0.673243 / pow(distance() , 0.856886)) * 5.69946 * pow((width1() + width2()) / 2 , 0.134744) * (0.00867838 * (thickness1() + thickness2()) / 2 + 0.0047019) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.24)))
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,metal5
//
CAPACITANCE INTRINSIC FRINGE metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.146511 * (1 - exp(-0.176562 * (distance() + 1.069))) * pow(width() , 0.0113416 * distance() + -0.0492471) * (0.0144247 * thickness() + 0.190725) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.06)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0268284 * (1 - exp(-0.197565 * (distance() + 0.358441))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.06)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.141863 * pow(width() , 0.0789728) * (0.0498828 * thickness() + 0.194011) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.06)))
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,metal5
//
CAPACITANCE NEARBODY metal5 WITH SHIELD metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.291376 - 0.240092 * distance()) + 1.12745 / pow(distance() , 0.849345)) * 5.94788 * pow((width1() + width2()) / 2 , 0.138489) * (0.00500279 * (thickness1() + thickness2()) / 2 + 0.00277868) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.06)))
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,metal6
//
CAPACITANCE INTRINSIC FRINGE metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.143774 * (1 - exp(-0.145112 * (distance() + 1.31386))) * pow(width() , 0.0118095 * distance() + -0.0597398) * (0.0103108 * thickness() + 0.18761) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.88)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0256857 * (1 - exp(-0.166055 * (distance() + 0.41002))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.88)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.138326 * pow(width() , 0.0761681) * (0.0448918 * thickness() + 0.187625) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.88)))
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,metal6
//
CAPACITANCE NEARBODY metal6 WITH SHIELD metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(1.04635 - 0.202817 * distance()) + 4.04538 / pow(distance() , 0.853679)) * 6.29007 * pow((width1() + width2()) / 2 , 0.141608) * (0.00129218 * (thickness1() + thickness2()) / 2 + 0.00073181) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.88)))
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,metal7
//
CAPACITANCE INTRINSIC FRINGE metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.153454 * (1 - exp(-0.108179 * (distance() + 1.59141))) * pow(width() , 0.0136476 * distance() + -0.0815857) * (0.0062333 * thickness() + 0.183312) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.7)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0253968 * (1 - exp(-0.138772 * (distance() + 0.427821))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.7)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.135603 * pow(width() , 0.0705914) * (0.0394443 * thickness() + 0.182465) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 5.7)))
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,metal7
//
CAPACITANCE NEARBODY metal7 WITH SHIELD metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(1.07416 - 0.191929 * distance()) + 3.8071 / pow(distance() , 0.849841)) * 6.40614 * pow((width1() + width2()) / 2 , 0.14634) * (0.00132588 * (thickness1() + thickness2()) / 2 + 0.00079522) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 5.7)))
]
//
//  model: 34  target: intrinsic  profile: field  layers: field_base,metal8
//
CAPACITANCE INTRINSIC FRINGE metal8 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.187083 * (1 - exp(-0.0665522 * (distance() + 1.25909))) * pow(width() , 0.0293721 * distance() + -0.210261) * (-0.00032061 * thickness() + 0.196273) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 6.74)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0316608 * (1 - exp(-0.0892788 * (distance() + 0.223316))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 6.74)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.11644 * pow(width() , 0.0403827) * (0.0287229 * thickness() + 0.202077) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 6.74)))
    }
]
//
//  model: 34  target: nearbody  profile: field  layers: field_base,metal8
//
CAPACITANCE NEARBODY metal8 WITH SHIELD metal8 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    C = length() * (exp(-1.07085 - 0.238798 * distance()) + 0.689017 / pow(distance() , 0.869067)) * 4.45741 * pow((width1() + width2()) / 2 , 0.109509) * (0.00979027 * (thickness1() + thickness2()) / 2 + 0.0096189) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 6.74)))
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE CROSSOVER PLATE poly metal1 MASK
[
    PROPERTY C
    C = 0.131848 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE CROSSOVER PLATE poly metal2 MASK
[
    PROPERTY C
    C = 0.0361961 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE CROSSOVER PLATE poly metal3 MASK
[
    PROPERTY C
    C = 0.0195831 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE CROSSOVER PLATE poly metal4 MASK
[
    PROPERTY C
    C = 0.0134225 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE CROSSOVER PLATE poly metal5 MASK
[
    PROPERTY C
    C = 0.0102105 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE CROSSOVER PLATE poly metal6 MASK
[
    PROPERTY C
    C = 0.00823887 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal7
//
CAPACITANCE CROSSOVER PLATE poly metal7 MASK
[
    PROPERTY C
    C = 0.00690546 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,poly,metal8
//
CAPACITANCE CROSSOVER PLATE poly metal8 MASK
[
    PROPERTY C
    C = 0.00580747 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE CROSSOVER PLATE metal1 metal2 MASK
[
    PROPERTY C
    C = 0.0788796 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE CROSSOVER PLATE metal1 metal3 MASK
[
    PROPERTY C
    C = 0.0276136 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE CROSSOVER PLATE metal1 metal4 MASK
[
    PROPERTY C
    C = 0.0167641 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE CROSSOVER PLATE metal1 metal5 MASK
[
    PROPERTY C
    C = 0.0120354 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE CROSSOVER PLATE metal1 metal6 MASK
[
    PROPERTY C
    C = 0.00938742 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal7
//
CAPACITANCE CROSSOVER PLATE metal1 metal7 MASK
[
    PROPERTY C
    C = 0.00769454 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal1,metal8
//
CAPACITANCE CROSSOVER PLATE metal1 metal8 MASK
[
    PROPERTY C
    C = 0.00635398 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE CROSSOVER PLATE metal2 metal3 MASK
[
    PROPERTY C
    C = 0.0782663 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE CROSSOVER PLATE metal2 metal4 MASK
[
    PROPERTY C
    C = 0.0274772 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE CROSSOVER PLATE metal2 metal5 MASK
[
    PROPERTY C
    C = 0.0166814 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE CROSSOVER PLATE metal2 metal6 MASK
[
    PROPERTY C
    C = 0.011976 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal2,metal7
//
CAPACITANCE CROSSOVER PLATE metal2 metal7 MASK
[
    PROPERTY C
    C = 0.00934107 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal2,metal8
//
CAPACITANCE CROSSOVER PLATE metal2 metal8 MASK
[
    PROPERTY C
    C = 0.00741949 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE CROSSOVER PLATE metal3 metal4 MASK
[
    PROPERTY C
    C = 0.0782663 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE CROSSOVER PLATE metal3 metal5 MASK
[
    PROPERTY C
    C = 0.0274772 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE CROSSOVER PLATE metal3 metal6 MASK
[
    PROPERTY C
    C = 0.0166814 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal3,metal7
//
CAPACITANCE CROSSOVER PLATE metal3 metal7 MASK
[
    PROPERTY C
    C = 0.011976 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal3,metal8
//
CAPACITANCE CROSSOVER PLATE metal3 metal8 MASK
[
    PROPERTY C
    C = 0.00898694 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE CROSSOVER PLATE metal4 metal5 MASK
[
    PROPERTY C
    C = 0.0782663 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE CROSSOVER PLATE metal4 metal6 MASK
[
    PROPERTY C
    C = 0.0274772 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal4,metal7
//
CAPACITANCE CROSSOVER PLATE metal4 metal7 MASK
[
    PROPERTY C
    C = 0.0166814 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal4,metal8
//
CAPACITANCE CROSSOVER PLATE metal4 metal8 MASK
[
    PROPERTY C
    C = 0.0113941 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE CROSSOVER PLATE metal5 metal6 MASK
[
    PROPERTY C
    C = 0.0782662 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal5,metal7
//
CAPACITANCE CROSSOVER PLATE metal5 metal7 MASK
[
    PROPERTY C
    C = 0.0274772 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal5,metal8
//
CAPACITANCE CROSSOVER PLATE metal5 metal8 MASK
[
    PROPERTY C
    C = 0.0155626 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal6,metal7
//
CAPACITANCE CROSSOVER PLATE metal6 metal7 MASK
[
    PROPERTY C
    C = 0.0782663 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal6,metal8
//
CAPACITANCE CROSSOVER PLATE metal6 metal8 MASK
[
    PROPERTY C
    C = 0.0245405 * area()
]
//
//  model: 3  target: crossoverp  profile: field  layers: field_base,metal7,metal8
//
CAPACITANCE CROSSOVER PLATE metal7 metal8 MASK
[
    PROPERTY C
    C = 0.0583349 * area()
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE NEARBODY poly INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = length() * (exp(-1.23562 - 3.49496 * distance()) + 0.00086179 / pow(distance() , 2.44487)) * 3.40496 * pow((width1() + width2()) / 2 , 0.0434791) * (0.323088 * (thickness1() + thickness2()) / 2 + 0.0295323) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.42)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE CROSSOVER FRINGE poly metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_poly
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.127541 * (1 - exp(-2.81281 * (distance() + -0.0348177))) * pow(width() , 0.00191545 * distance() + -0.00544603) * (0.284967 * thickness() + 0.184081)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0300075 * (1 - exp(-2.94331 * (distance() + -0.0424983)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.143073 * pow(width() , 0) * (0.303743 * thickness() + 0.15362)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00332653 * distance() + 0.386467) * (0.157568 - exp(-(-0.612931 * distance() + 0.251089) * (enclose() + 3.06574 * distance() + 2.67718) - (0.016402 * distance() + 0.32946) * (enclose() + 1.92584 * distance() + 1.75566) * (enclose() + 1.92584 * distance() + 1.75566)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.127541 * (1 - exp(-2.81281 * (distance() + -0.0348177))) * pow(width() , 0.00191545 * distance() + -0.00544603) * (0.284967 * thickness() + 0.184081)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.184917 * (0.157568 - exp(2.81357 * (enclose() + 18.0059) - 0.41147 * (enclose() + 11.3849) * (enclose() + 11.3849)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0930443 * (1 - exp(-2.74902 * (distance() + -0.038254))) * pow(width() , 0.00216662 * distance() + 0.00252331) * (0.33889 * thickness() + 0.167267) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0208938 * (1 - exp(-2.8926 * (distance() + -0.0470852))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.121948 * pow(width() , 0.00847638) * (0.300445 * thickness() + 0.118808) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 5 + m43ink4 * 0.42)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE NEARBODY poly INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = length() * (exp(-1.16 - 2.07981 * distance()) + 0.00745671 / pow(distance() , 1.77782)) * 3.28443 * pow((width1() + width2()) / 2 , 0.101089) * (0.200278 * (thickness1() + thickness2()) / 2 + 0.0292348) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.42)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE CROSSOVER FRINGE poly metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_poly
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0653993 * (1 - exp(-1.51533 * (distance() + 0.0187268))) * pow(width() , 0.0051618 * distance() + 0.0161044) * (0.30566 * thickness() + 0.179243)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0144159 * (1 - exp(-1.6736 * (distance() + -0.0236071)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.106095 * pow(width() , 0.0398439) * (0.224882 * thickness() + 0.104041)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00156522 * distance() + 0.0777854) * (0.388025 - exp(-(-0.867157 * distance() + 0.180102) * (enclose() + 5.71545 * distance() + 2.88527) - (0.00030708 * distance() + 0.0818963) * (enclose() + 7.61603 * distance() + 1.74773) * (enclose() + 7.61603 * distance() + 1.74773)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0653993 * (1 - exp(-1.51533 * (distance() + 0.0187268))) * pow(width() , 0.0051618 * distance() + 0.0161044) * (0.30566 * thickness() + 0.179243)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0349797 * (0.388025 - exp(4.15568 * (enclose() + 31.4625) - 0.0834317 * (enclose() + 39.8279) * (enclose() + 39.8279)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.123314 * (1 - exp(-1.59411 * (distance() + 0.0159237))) * pow(width() , 0.00405069 * distance() + 0.00989779) * (0.227367 * thickness() + 0.210468) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.029717 * (1 - exp(-1.722 * (distance() + -0.0189953))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.148986 * pow(width() , 0.0294251) * (0.236735 * thickness() + 0.165984) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 5 + m43ink4 * 0.42)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE NEARBODY poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = length() * (exp(-1.22764 - 1.68423 * distance()) + 0.019521 / pow(distance() , 1.47617)) * 3.35181 * pow((width1() + width2()) / 2 , 0.122575) * (0.162062 * (thickness1() + thickness2()) / 2 + 0.025514) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.42)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE CROSSOVER FRINGE poly metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_poly
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.042997 * (1 - exp(-1.25262 * (distance() + 0.0674516))) * pow(width() , 0.00505873 * distance() + 0.0240445) * (0.314055 * thickness() + 0.174374)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00920253 * (1 - exp(-1.37724 * (distance() + 0.00283557)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0886468 * pow(width() , 0.0560309) * (0.18833 * thickness() + 0.080136)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00137534 * distance() + 0.0354085) * (0.61889 - exp(-(-0.0126912 * distance() + 0.228788) * (enclose() + 3.71017 * distance() + 1.85826) - (-0.039831 * distance() + -0.0041757) * (enclose() + 0.031772 * distance() + -3.57562) * (enclose() + 0.031772 * distance() + -3.57562)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.042997 * (1 - exp(-1.25262 * (distance() + 0.0674516))) * pow(width() , 0.00505873 * distance() + 0.0240445) * (0.314055 * thickness() + 0.174374)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0142659 * (0.61889 - exp(-0.165332 * (enclose() + 20.4091) - -0.203331 * (enclose() + -3.41676) * (enclose() + -3.41676)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.140185 * (1 - exp(-1.34976 * (distance() + 0.0584566))) * pow(width() , 0.00320126 * distance() + 0.0182246) * (0.199543 * thickness() + 0.21764) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0337407 * (1 - exp(-1.43039 * (distance() + 0.00788605))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.157703 * pow(width() , 0.0432775) * (0.236531 * thickness() + 0.187805) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 5 + m43ink4 * 0.42)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE NEARBODY poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = length() * (exp(-1.24496 - 1.54821 * distance()) + 0.0314322 / pow(distance() , 1.34389)) * 3.3454 * pow((width1() + width2()) / 2 , 0.130419) * (0.14185 * (thickness1() + thickness2()) / 2 + 0.0228347) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.42)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE CROSSOVER FRINGE poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_poly
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0342011 * (1 - exp(-1.19162 * (distance() + 0.0909861))) * pow(width() , 0.00384542 * distance() + 0.0309999) * (0.298858 * thickness() + 0.160724)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00677485 * (1 - exp(-1.27623 * (distance() + 0.0202229)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0788907 * pow(width() , 0.0646236) * (0.164396 * thickness() + 0.066719)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00033593 * distance() + 0.0208879) * (0.653052 - exp(-(0.241484 * distance() + 0.222935) * (enclose() + -0.204593 * distance() + 1.4131) - (0.0258095 * distance() + -0.012352) * (enclose() + -0.344236 * distance() + -0.211708) * (enclose() + -0.344236 * distance() + -0.211708)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0342011 * (1 - exp(-1.19162 * (distance() + 0.0909861))) * pow(width() , 0.00384542 * distance() + 0.0309999) * (0.298858 * thickness() + 0.160724)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00960412 * (0.653052 - exp(-1.43036 * (enclose() + 0.390135) - 0.116695 * (enclose() + -1.93289) * (enclose() + -1.93289)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.149037 * (1 - exp(-1.29423 * (distance() + 0.0779856))) * pow(width() , 0.00182341 * distance() + 0.0249916) * (0.189357 * thickness() + 0.218884) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0357153 * (1 - exp(-1.3292 * (distance() + 0.0245469))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.163352 * pow(width() , 0.0510186) * (0.241249 * thickness() + 0.196594) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 5 + m43ink4 * 0.42)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE NEARBODY poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = length() * (exp(-1.31593 - 1.49559 * distance()) + 0.0388088 / pow(distance() , 1.27132)) * 3.34939 * pow((width1() + width2()) / 2 , 0.134244) * (0.138111 * (thickness1() + thickness2()) / 2 + 0.0224922) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.42)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE CROSSOVER FRINGE poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_poly
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0255035 * (1 - exp(-1.17697 * (distance() + 0.102479))) * pow(width() , 0.00287224 * distance() + 0.0361061) * (0.323063 * thickness() + 0.169919)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00536107 * (1 - exp(-1.23269 * (distance() + 0.0309255)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0687745 * pow(width() , 0.0699669) * (0.154912 * thickness() + 0.0608179)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00041235 * distance() + 0.0178686) * (0.6257 - exp(-(0.138048 * distance() + 0.213404) * (enclose() + -0.152591 * distance() + 1.77711) - (0.00964486 * distance() + -0.0066189) * (enclose() + -0.465794 * distance() + 1.08567) * (enclose() + -0.465794 * distance() + 1.08567)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0255035 * (1 - exp(-1.17697 * (distance() + 0.102479))) * pow(width() , 0.00287224 * distance() + 0.0361061) * (0.323063 * thickness() + 0.169919)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00790342 * (0.6257 - exp(-0.903644 * (enclose() + 1.01415) - 0.0416054 * (enclose() + -1.2433) * (enclose() + -1.2433)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.15379 * (1 - exp(-1.28165 * (distance() + 0.0872002))) * pow(width() , 0.00083583 * distance() + 0.029785) * (0.185448 * thickness() + 0.220028) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0368683 * (1 - exp(-1.2861 * (distance() + 0.0342729))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.160984 * pow(width() , 0.0559089) * (0.254451 * thickness() + 0.209074) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 5 + m43ink4 * 0.42)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE NEARBODY poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = length() * (exp(-1.34396 - 1.47564 * distance()) + 0.0448674 / pow(distance() , 1.22738)) * 3.47209 * pow((width1() + width2()) / 2 , 0.136344) * (0.128857 * (thickness1() + thickness2()) / 2 + 0.021083) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.42)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE CROSSOVER FRINGE poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_poly
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0211764 * (1 - exp(-1.17458 * (distance() + 0.108668))) * pow(width() , 0.00224565 * distance() + 0.0396554) * (0.32624 * thickness() + 0.168933)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0044356 * (1 - exp(-1.21152 * (distance() + 0.0376812)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0626835 * pow(width() , 0.07362) * (0.144317 * thickness() + 0.0553981)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00050298 * distance() + 0.0169108) * (0.573598 - exp(-(0.0826145 * distance() + 0.257813) * (enclose() + -0.105145 * distance() + 2.00696) - (0.00858487 * distance() + -0.0298559) * (enclose() + -0.548676 * distance() + 1.28796) * (enclose() + -0.548676 * distance() + 1.28796)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0211764 * (1 - exp(-1.17458 * (distance() + 0.108668))) * pow(width() , 0.00224565 * distance() + 0.0396554) * (0.32624 * thickness() + 0.168933)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00719795 * (0.573598 - exp(-0.670886 * (enclose() + 1.48123) - 0.0130684 * (enclose() + -1.45542) * (enclose() + -1.45542)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.156872 * (1 - exp(-1.27924 * (distance() + 0.0922573))) * pow(width() , 0.00024871 * distance() + 0.0330451) * (0.183794 * thickness() + 0.220878) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0376299 * (1 - exp(-1.26404 * (distance() + 0.0405593))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.162008 * pow(width() , 0.0592956) * (0.260576 * thickness() + 0.214151) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 5 + m43ink4 * 0.42)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal7
//
CAPACITANCE NEARBODY poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = length() * (exp(-1.36373 - 1.46901 * distance()) + 0.0492333 / pow(distance() , 1.19897)) * 3.50149 * pow((width1() + width2()) / 2 , 0.137635) * (0.12506 * (thickness1() + thickness2()) / 2 + 0.0205274) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.42)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal7
//
CAPACITANCE CROSSOVER FRINGE poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_poly
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0180908 * (1 - exp(-1.17602 * (distance() + 0.112311))) * pow(width() , 0.00182716 * distance() + 0.0423202) * (0.329376 * thickness() + 0.168396)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00378285 * (1 - exp(-1.20068 * (distance() + 0.0420793)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0568464 * pow(width() , 0.0762903) * (0.138416 * thickness() + 0.0522227)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00068182 * distance() + 0.0151046) * (0.638397 - exp(-(0.0343253 * distance() + 0.208896) * (enclose() + 0.164872 * distance() + 1.92487) - (0.00342181 * distance() + -0.032119) * (enclose() + -0.899466 * distance() + 0.891437) * (enclose() + -0.899466 * distance() + 0.891437)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0180908 * (1 - exp(-1.17602 * (distance() + 0.112311))) * pow(width() , 0.00182716 * distance() + 0.0423202) * (0.329376 * thickness() + 0.168396)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00584775 * (0.638397 - exp(-0.380522 * (enclose() + 2.74923) - -0.01501 * (enclose() + -3.60589) * (enclose() + -3.60589)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal7
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.125454 * (1 - exp(-1.28211 * (distance() + 0.0947541))) * pow(width() , -0.00019192 * distance() + 0.035946) * (0.235467 * thickness() + 0.280505) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0381675 * (1 - exp(-1.25329 * (distance() + 0.0443207))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.162467 * pow(width() , 0.0617773) * (0.26588 * thickness() + 0.218163) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 5 + m43ink4 * 0.42)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,poly,metal8
//
CAPACITANCE NEARBODY poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = length() * (exp(-1.37933 - 1.46827 * distance()) + 0.052946 / pow(distance() , 1.17667)) * 3.52371 * pow((width1() + width2()) / 2 , 0.138608) * (0.122308 * (thickness1() + thickness2()) / 2 + 0.0200705) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.42)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,poly,metal8
//
CAPACITANCE CROSSOVER FRINGE poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_calenclose = 5 // For reference
    max_width = MAXWIDTH_poly
    max_distance = MAXDISTANCE_poly
    max_enclose = MAXENCLOSE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0154277 * (1 - exp(-1.17818 * (distance() + 0.114725))) * pow(width() , 0.00153429 * distance() + 0.0439996) * (0.332153 * thickness() + 0.167652)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00321894 * (1 - exp(-1.19356 * (distance() + 0.0452573)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0507227 * pow(width() , 0.0781378) * (0.133959 * thickness() + 0.0499144)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00013559 * distance() + 0.00942781) * (0.677431 - exp(-(0.0433429 * distance() + 0.186251) * (enclose() + 0.0401063 * distance() + 1.86528) - (0.00413086 * distance() + -0.00342167) * (enclose() + -0.712336 * distance() + 3.7552) * (enclose() + -0.712336 * distance() + 3.7552)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0154277 * (1 - exp(-1.17818 * (distance() + 0.114725))) * pow(width() , 0.00153429 * distance() + 0.0439996) * (0.332153 * thickness() + 0.167652)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00437493 * (0.677431 - exp(-0.402965 * (enclose() + 2.06581) - 0.0172326 * (enclose() + 0.19352) * (enclose() + 0.19352)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,poly,metal8
//
CAPACITANCE INTRINSIC FRINGE poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    max_width = MAXWIDTH_poly
    max_radius = MAXRADIUS_poly
    max_distance = MAXDISTANCE_poly
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.127017 * (1 - exp(-1.28068 * (distance() + 0.0977896))) * pow(width() , -0.00035236 * distance() + 0.0374177) * (0.232006 * thickness() + 0.281592) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0386395 * (1 - exp(-1.24603 * (distance() + 0.0474237))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.42)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.162675 * pow(width() , 0.0639814) * (0.271329 * thickness() + 0.221911) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 5 + m43ink4 * 0.42)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-0.936871 - 2.1583 * distance()) + 0.0152518 / pow(distance() , 1.55407)) * 3.96937 * pow((width1() + width2()) / 2 , 0.0455311) * (0.109593 * (thickness1() + thickness2()) / 2 + 0.026016) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.89)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE CROSSOVER FRINGE metal1 metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.149045 * (1 - exp(-1.40378 * (distance() + -0.0161667))) * pow(width() , 0.00403335 * distance() + -0.0206789) * (0.113912 * thickness() + 0.191188)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0328783 * (1 - exp(-1.49482 * (distance() + -0.0349011)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.145991 * pow(width() , 0.00430783) * (0.168794 * thickness() + 0.18063)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-5.57305e-06 * distance() + 0.0905652) * (0.71125 - exp(-(0.0208075 * distance() + 2.55887) * (enclose() + -0.0514705 * distance() + 0.646621) - (0.120645 * distance() + -0.518584) * (enclose() + -0.334644 * distance() + 1.67779) * (enclose() + -0.334644 * distance() + 1.67779)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.149045 * (1 - exp(-1.40378 * (distance() + -0.0161667))) * pow(width() , 0.00403335 * distance() + -0.0206789) * (0.113912 * thickness() + 0.191188)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0452598 * (0.71125 - exp(-2.72876 * (enclose() + 0.226364) - 0.466482 * (enclose() + -1.05458) * (enclose() + -1.05458)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0909284 * (1 - exp(-1.31637 * (distance() + -0.0130823))) * pow(width() , 0.00484005 * distance() + -0.011749) * (0.139017 * thickness() + 0.176726) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.019022 * (1 - exp(-1.42741 * (distance() + -0.0381829))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.116716 * pow(width() , 0.0176895) * (0.164103 * thickness() + 0.122015) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.89)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-0.98761 - 1.53742 * distance()) + 0.0296667 / pow(distance() , 1.36871)) * 3.94648 * pow((width1() + width2()) / 2 , 0.0749241) * (0.0888047 * (thickness1() + thickness2()) / 2 + 0.0265383) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.89)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE CROSSOVER FRINGE metal1 metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0920536 * (1 - exp(-0.820549 * (distance() + 0.0338293))) * pow(width() , 0.0100083 * distance() + -0.0356853) * (0.123125 * thickness() + 0.173506)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0183675 * (1 - exp(-0.951568 * (distance() + -0.02754)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.115721 * pow(width() , 0.025694) * (0.152867 * thickness() + 0.121408)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00077621 * distance() + 0.070497) * (0.534792 - exp(-(-0.12886 * distance() + 0.0678471) * (enclose() + 4.21871 * distance() + 3.79695) - (0.00050309 * distance() + 0.0511919) * (enclose() + 2.98865 * distance() + 2.11576) * (enclose() + 2.98865 * distance() + 2.11576)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0920536 * (1 - exp(-0.820549 * (distance() + 0.0338293))) * pow(width() , 0.0100083 * distance() + -0.0356853) * (0.123125 * thickness() + 0.173506)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0320796 * (0.534792 - exp(0.984295 * (enclose() + 38.2427) - 0.0552996 * (enclose() + 26.5181) * (enclose() + 26.5181)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.125003 * (1 - exp(-0.838353 * (distance() + 0.0357959))) * pow(width() , 0.0092822 * distance() + -0.0308043) * (0.0896733 * thickness() + 0.190357) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0259219 * (1 - exp(-0.963114 * (distance() + -0.0229575))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.137053 * pow(width() , 0.0264185) * (0.118444 * thickness() + 0.16172) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.89)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.04147 - 1.31221 * distance()) + 0.0442464 / pow(distance() , 1.26178)) * 3.96293 * pow((width1() + width2()) / 2 , 0.0904971) * (0.0791755 * (thickness1() + thickness2()) / 2 + 0.0249895) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.89)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE CROSSOVER FRINGE metal1 metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0680116 * (1 - exp(-0.667895 * (distance() + 0.0843871))) * pow(width() , 0.0127218 * distance() + -0.042758) * (0.128256 * thickness() + 0.169497)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0131671 * (1 - exp(-0.801588 * (distance() + -0.0108159)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.1026 * pow(width() , 0.0388366) * (0.1111 * thickness() + 0.103473)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (2.5111e-05 * distance() + 0.0407415) * (0.602406 - exp(-(0.0159177 * distance() + 0.377277) * (enclose() + 0.067714 * distance() + 1.56432) - (0.0439187 * distance() + -0.0423104) * (enclose() + -0.2128 * distance() + 2.18953) * (enclose() + -0.2128 * distance() + 2.18953)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0680116 * (1 - exp(-0.667895 * (distance() + 0.0843871))) * pow(width() , 0.0127218 * distance() + -0.042758) * (0.128256 * thickness() + 0.169497)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0204733 * (0.602406 - exp(-0.507245 * (enclose() + 2.1172) - 0.316286 * (enclose() + 0.452018) * (enclose() + 0.452018)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.143889 * (1 - exp(-0.699408 * (distance() + 0.0808752))) * pow(width() , 0.0112806 * distance() + -0.0368756) * (0.0752001 * thickness() + 0.194076) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0294966 * (1 - exp(-0.821192 * (distance() + -0.00425476))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.145758 * pow(width() , 0.0360814) * (0.112382 * thickness() + 0.17921) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.89)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.08766 - 1.19971 * distance()) + 0.0568131 / pow(distance() , 1.19656)) * 4.00327 * pow((width1() + width2()) / 2 , 0.0977312) * (0.0730781 * (thickness1() + thickness2()) / 2 + 0.0235475) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.89)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE CROSSOVER FRINGE metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0540447 * (1 - exp(-0.608474 * (distance() + 0.121329))) * pow(width() , 0.0132888 * distance() + -0.0408274) * (0.131294 * thickness() + 0.167892)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0103231 * (1 - exp(-0.732952 * (distance() + 0.00639775)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0935221 * pow(width() , 0.0475036) * (0.0983728 * thickness() + 0.0897018)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00013432 * distance() + 0.0299622) * (0.657421 - exp(-(0.0395345 * distance() + 0.242794) * (enclose() + -0.0236729 * distance() + 1.78462) - (0.0189369 * distance() + -0.0163608) * (enclose() + -0.210883 * distance() + 2.65378) * (enclose() + -0.210883 * distance() + 2.65378)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0540447 * (1 - exp(-0.608474 * (distance() + 0.121329))) * pow(width() , 0.0132888 * distance() + -0.0408274) * (0.131294 * thickness() + 0.167892)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0144327 * (0.657421 - exp(-0.565593 * (enclose() + 1.59133) - 0.138259 * (enclose() + 0.93192) * (enclose() + 0.93192)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.155184 * (1 - exp(-0.643018 * (distance() + 0.113444))) * pow(width() , 0.0116794 * distance() + -0.0354464) * (0.0690319 * thickness() + 0.196141) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0316626 * (1 - exp(-0.756935 * (distance() + 0.0109117))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.150451 * pow(width() , 0.0432091) * (0.112043 * thickness() + 0.190077) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.89)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.12692 - 1.13314 * distance()) + 0.0675677 / pow(distance() , 1.15259)) * 4.04897 * pow((width1() + width2()) / 2 , 0.101626) * (0.0688264 * (thickness1() + thickness2()) / 2 + 0.022327) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.89)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE CROSSOVER FRINGE metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0448076 * (1 - exp(-0.582334 * (distance() + 0.146674))) * pow(width() , 0.0130631 * distance() + -0.0361494) * (0.133376 * thickness() + 0.166925)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00849877 * (1 - exp(-0.697359 * (distance() + 0.019461)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0866155 * pow(width() , 0.0536454) * (0.0904425 * thickness() + 0.0800611)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00032047 * distance() + 0.0247792) * (0.696509 - exp(-(0.0698101 * distance() + 0.184968) * (enclose() + -0.0754687 * distance() + 1.84219) - (0.0053455 * distance() + -0.0096552) * (enclose() + -0.255727 * distance() + 2.61282) * (enclose() + -0.255727 * distance() + 2.61282)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0448076 * (1 - exp(-0.582334 * (distance() + 0.146674))) * pow(width() , 0.0130631 * distance() + -0.0361494) * (0.133376 * thickness() + 0.166925)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0110813 * (0.696509 - exp(-0.754967 * (enclose() + 1.22599) - 0.0339908 * (enclose() + 0.524809) * (enclose() + 0.524809)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.162614 * (1 - exp(-0.614716 * (distance() + 0.137204))) * pow(width() , 0.0116415 * distance() + -0.0323997) * (0.0656125 * thickness() + 0.197488) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0330981 * (1 - exp(-0.721472 * (distance() + 0.0237143))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.153495 * pow(width() , 0.0484745) * (0.113296 * thickness() + 0.197369) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.89)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal7
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.15476 - 1.09427 * distance()) + 0.0761133 / pow(distance() , 1.12257)) * 4.0813 * pow((width1() + width2()) / 2 , 0.103875) * (0.0656114 * (thickness1() + thickness2()) / 2 + 0.0215419) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.89)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal7
//
CAPACITANCE CROSSOVER FRINGE metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.038238 * (1 - exp(-0.570574 * (distance() + 0.16399))) * pow(width() , 0.0125971 * distance() + -0.0310951) * (0.135247 * thickness() + 0.166141)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00722373 * (1 - exp(-0.676173 * (distance() + 0.0303413)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0812211 * pow(width() , 0.0582223) * (0.0843036 * thickness() + 0.0728239)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00044887 * distance() + 0.0220794) * (0.712053 - exp(-(0.064803 * distance() + 0.154892) * (enclose() + -0.0805687 * distance() + 1.95909) - (0.00080259 * distance() + -0.00772809) * (enclose() + -0.32814 * distance() + 2.25635) * (enclose() + -0.32814 * distance() + 2.25635)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.038238 * (1 - exp(-0.570574 * (distance() + 0.16399))) * pow(width() , 0.0125971 * distance() + -0.0310951) * (0.135247 * thickness() + 0.166141)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00920719 * (0.712053 - exp(-0.684008 * (enclose() + 1.30125) - -0.00117494 * (enclose() + -0.422913) * (enclose() + -0.422913)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal7
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.16734 * (1 - exp(-0.605417 * (distance() + 0.150912))) * pow(width() , 0.0110933 * distance() + -0.027643) * (0.0636145 * thickness() + 0.198567) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0341084 * (1 - exp(-0.700676 * (distance() + 0.0336465))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.155583 * pow(width() , 0.0524807) * (0.114974 * thickness() + 0.20269) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.89)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal1,metal8
//
CAPACITANCE NEARBODY metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.1742 - 1.07071 * distance()) + 0.0847308 / pow(distance() , 1.09573)) * 4.09702 * pow((width1() + width2()) / 2 , 0.105429) * (0.063152 * (thickness1() + thickness2()) / 2 + 0.0207529) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.89)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal1,metal8
//
CAPACITANCE CROSSOVER FRINGE metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0325527 * (1 - exp(-0.563985 * (distance() + 0.177463))) * pow(width() , 0.0121273 * distance() + -0.0269592) * (0.136959 * thickness() + 0.165211)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00612721 * (1 - exp(-0.661288 * (distance() + 0.0393875)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.075939 * pow(width() , 0.0617588) * (0.0783435 * thickness() + 0.0663161)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0002256 * distance() + 0.0162551) * (0.824012 - exp(-(0.0835169 * distance() + 0.111091) * (enclose() + 0.166617 * distance() + 1.32254) - (-0.00214073 * distance() + -0.00208922) * (enclose() + 0.617505 * distance() + 3.43346) * (enclose() + 0.617505 * distance() + 3.43346)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0325527 * (1 - exp(-0.563985 * (distance() + 0.177463))) * pow(width() , 0.0121273 * distance() + -0.0269592) * (0.136959 * thickness() + 0.165211)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00720654 * (0.824012 - exp(-0.793006 * (enclose() + 2.68297) - -0.0195683 * (enclose() + 8.47539) * (enclose() + 8.47539)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal1,metal8
//
CAPACITANCE INTRINSIC FRINGE metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.171408 * (1 - exp(-0.59976 * (distance() + 0.162885))) * pow(width() , 0.0105844 * distance() + -0.0230256) * (0.0621897 * thickness() + 0.199467) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.034974 * (1 - exp(-0.68604 * (distance() + 0.0425302))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.89)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.157336 * pow(width() , 0.0561374) * (0.117001 * thickness() + 0.207428) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.89)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.04942 - 1.70113 * distance()) + 0.0268632 / pow(distance() , 1.45715)) * 4.00194 * pow((width1() + width2()) / 2 , 0.05638) * (0.0772811 * (thickness1() + thickness2()) / 2 + 0.0255471) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.6)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal2 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.18208 * (1 - exp(-1.14416 * (distance() + 0.00274512))) * pow(width() , 0.00340794 * distance() + -0.0146407) * (0.0680897 * thickness() + 0.189833)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0388874 * (1 - exp(-1.2004 * (distance() + -0.0229643)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.151817 * pow(width() , 0.0135432) * (0.11265 * thickness() + 0.216314)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (3.75593e-06 * distance() + 0.116485) * (0.652385 - exp(-(-0.0224386 * distance() + 2.40463) * (enclose() + -0.0389932 * distance() + 0.728665) - (0.0688643 * distance() + -0.469822) * (enclose() + -0.322066 * distance() + 1.75289) * (enclose() + -0.322066 * distance() + 1.75289)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.18208 * (1 - exp(-1.14416 * (distance() + 0.00274512))) * pow(width() , 0.00340794 * distance() + -0.0146407) * (0.0680897 * thickness() + 0.189833)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0582613 * (0.652385 - exp(-2.18024 * (enclose() + 0.338733) - 0.218821 * (enclose() + -1.46777) * (enclose() + -1.46777)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE INTRINSIC FRINGE metal2 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0713711 * (1 - exp(-0.995386 * (distance() + 0.00892837))) * pow(width() , 0.00559399 * distance() + -0.013635) * (0.11448 * thickness() + 0.155219) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.6)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0137746 * (1 - exp(-1.0889 * (distance() + -0.0360776))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.6)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.100854 * pow(width() , 0.0299069) * (0.103303 * thickness() + 0.101152) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.6)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.10979 - 1.1865 * distance()) + 0.042124 / pow(distance() , 1.32304)) * 3.9827 * pow((width1() + width2()) / 2 , 0.0779007) * (0.0646843 * (thickness1() + thickness2()) / 2 + 0.0274469) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.6)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal2 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.117915 * (1 - exp(-0.64024 * (distance() + 0.0522979))) * pow(width() , 0.00902244 * distance() + -0.0374847) * (0.0751887 * thickness() + 0.176479)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0234101 * (1 - exp(-0.732928 * (distance() + -0.0276594)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.126778 * pow(width() , 0.0286092) * (0.0866233 * thickness() + 0.155028)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00097987 * distance() + 0.155244) * (0.302908 - exp(-(-0.139294 * distance() + 0.127463) * (enclose() + 5.2218 * distance() + 6.93469) - (0.00010823 * distance() + 0.0251647) * (enclose() + 5.14002 * distance() + 2.65242) * (enclose() + 5.14002 * distance() + 2.65242)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.117915 * (1 - exp(-0.64024 * (distance() + 0.0522979))) * pow(width() , 0.00902244 * distance() + -0.0374847) * (0.0751887 * thickness() + 0.176479)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0727226 * (0.302908 - exp(1.26548 * (enclose() + 59.1527) - 0.026247 * (enclose() + 54.0526) * (enclose() + 54.0526)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE INTRINSIC FRINGE metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0970161 * (1 - exp(-0.626265 * (distance() + 0.0608583))) * pow(width() , 0.00959141 * distance() + -0.0381438) * (0.0702004 * thickness() + 0.18) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.6)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0193443 * (1 - exp(-0.723941 * (distance() + -0.0262679))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.6)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.118884 * pow(width() , 0.0321591) * (0.0731558 * thickness() + 0.138199) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.6)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.14576 - 0.977081 * distance()) + 0.0582523 / pow(distance() , 1.24023)) * 4.01341 * pow((width1() + width2()) / 2 , 0.0935412) * (0.0568382 * (thickness1() + thickness2()) / 2 + 0.0258093) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.6)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal2 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0910992 * (1 - exp(-0.496201 * (distance() + 0.116352))) * pow(width() , 0.0122252 * distance() + -0.0528121) * (0.0777479 * thickness() + 0.172576)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0175438 * (1 - exp(-0.594276 * (distance() + -0.0129669)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.113867 * pow(width() , 0.0391471) * (0.0761053 * thickness() + 0.128918)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (5.93228e-05 * distance() + 0.0554927) * (0.585822 - exp(-(0.0172204 * distance() + 0.465608) * (enclose() + 0.0104074 * distance() + 1.70535) - (0.0240727 * distance() + -0.0574624) * (enclose() + -0.227412 * distance() + 2.54016) * (enclose() + -0.227412 * distance() + 2.54016)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0910992 * (1 - exp(-0.496201 * (distance() + 0.116352))) * pow(width() , 0.0122252 * distance() + -0.0528121) * (0.0777479 * thickness() + 0.172576)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.028043 * (0.585822 - exp(-0.637812 * (enclose() + 1.80942) - 0.183265 * (enclose() + 0.26604) * (enclose() + 0.26604)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE INTRINSIC FRINGE metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.114267 * (1 - exp(-0.500741 * (distance() + 0.119636))) * pow(width() , 0.0120317 * distance() + -0.0515963) * (0.0546015 * thickness() + 0.187293) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.6)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0226899 * (1 - exp(-0.598939 * (distance() + -0.00859262))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.6)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.126971 * pow(width() , 0.0388926) * (0.0656144 * thickness() + 0.157975) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.6)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.18069 - 0.863774 * distance()) + 0.072873 / pow(distance() , 1.18389)) * 4.04452 * pow((width1() + width2()) / 2 , 0.101701) * (0.051993 * (thickness1() + thickness2()) / 2 + 0.024273) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.6)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0746834 * (1 - exp(-0.432146 * (distance() + 0.172949))) * pow(width() , 0.013474 * distance() + -0.0572524) * (0.0796292 * thickness() + 0.171132)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0141762 * (1 - exp(-0.526798 * (distance() + 0.00671504)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.104763 * pow(width() , 0.0468577) * (0.0699551 * thickness() + 0.113019)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0002103 * distance() + 0.0403867) * (0.677893 - exp(-(0.0456103 * distance() + 0.250831) * (enclose() + -0.0622826 * distance() + 1.95181) - (0.00736146 * distance() + -0.0183623) * (enclose() + -0.222195 * distance() + 3.2207) * (enclose() + -0.222195 * distance() + 3.2207)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0746834 * (1 - exp(-0.432146 * (distance() + 0.172949))) * pow(width() , 0.013474 * distance() + -0.0572524) * (0.0796292 * thickness() + 0.171132)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0191418 * (0.677893 - exp(-0.706934 * (enclose() + 1.32898) - 0.0552523 * (enclose() + 0.99875) * (enclose() + 0.99875)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE INTRINSIC FRINGE metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.126237 * (1 - exp(-0.441355 * (distance() + 0.171302))) * pow(width() , 0.0130659 * distance() + -0.0561084) * (0.0470502 * thickness() + 0.190781) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.6)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0249375 * (1 - exp(-0.535404 * (distance() + 0.0111124))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.6)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.132524 * pow(width() , 0.0447868) * (0.0621436 * thickness() + 0.170114) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.6)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal2,metal7
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.20373 - 0.794421 * distance()) + 0.0865037 / pow(distance() , 1.14291)) * 4.08405 * pow((width1() + width2()) / 2 , 0.106289) * (0.0481205 * (thickness1() + thickness2()) / 2 + 0.0227911) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.6)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal2,metal7
//
CAPACITANCE CROSSOVER FRINGE metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0631985 * (1 - exp(-0.39874 * (distance() + 0.218471))) * pow(width() , 0.0138015 * distance() + -0.0566838) * (0.081299 * thickness() + 0.170689)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.011929 * (1 - exp(-0.487988 * (distance() + 0.025746)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0981289 * pow(width() , 0.0526665) * (0.0650294 * thickness() + 0.101632)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00032688 * distance() + 0.0311879) * (0.77516 - exp(-(0.0593202 * distance() + 0.116424) * (enclose() + 0.161698 * distance() + 1.57209) - (-1.42316e-05 * distance() + -0.00021278) * (enclose() + -6.69365 * distance() + 2.72254) * (enclose() + -6.69365 * distance() + 2.72254)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0631985 * (1 - exp(-0.39874 * (distance() + 0.218471))) * pow(width() , 0.0138015 * distance() + -0.0566838) * (0.081299 * thickness() + 0.170689)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0139596 * (0.77516 - exp(-0.709626 * (enclose() + 3.18907) - -0.000355096 * (enclose() + -64.214) * (enclose() + -64.214)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal2,metal7
//
CAPACITANCE INTRINSIC FRINGE metal2 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.134974 * (1 - exp(-0.409479 * (distance() + 0.212744))) * pow(width() , 0.0133686 * distance() + -0.0562273) * (0.0425564 * thickness() + 0.192571) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.6)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0265395 * (1 - exp(-0.498077 * (distance() + 0.029663))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.6)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.136561 * pow(width() , 0.049559) * (0.0616971 * thickness() + 0.177939) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.6)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal2,metal8
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.22894 - 0.742228 * distance()) + 0.100629 / pow(distance() , 1.10665)) * 4.12628 * pow((width1() + width2()) / 2 , 0.109499) * (0.0448252 * (thickness1() + thickness2()) / 2 + 0.0214653) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.6)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal2,metal8
//
CAPACITANCE CROSSOVER FRINGE metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0535307 * (1 - exp(-0.377262 * (distance() + 0.258826))) * pow(width() , 0.0137537 * distance() + -0.0544424) * (0.0825716 * thickness() + 0.169812)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0100454 * (1 - exp(-0.460109 * (distance() + 0.0448556)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0923112 * pow(width() , 0.0575348) * (0.0602678 * thickness() + 0.0911414)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00011858 * distance() + 0.0236017) * (0.852354 - exp(-(0.0826748 * distance() + 0.104367) * (enclose() + 0.215373 * distance() + 1.16995) - (-0.00176411 * distance() + -0.00257357) * (enclose() + 0.736714 * distance() + 3.39902) * (enclose() + 0.736714 * distance() + 3.39902)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0535307 * (1 - exp(-0.377262 * (distance() + 0.258826))) * pow(width() , 0.0137537 * distance() + -0.0544424) * (0.0825716 * thickness() + 0.169812)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0112079 * (0.852354 - exp(-0.931115 * (enclose() + 3.32368) - -0.0202147 * (enclose() + 10.7662) * (enclose() + 10.7662)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal2,metal8
//
CAPACITANCE INTRINSIC FRINGE metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.142237 * (1 - exp(-0.389027 * (distance() + 0.249864))) * pow(width() , 0.0132876 * distance() + -0.0539125) * (0.0394399 * thickness() + 0.194025) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.6)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0279133 * (1 - exp(-0.4711 * (distance() + 0.0483842))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.6)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.13962 * pow(width() , 0.0540508) * (0.0613342 * thickness() + 0.18554) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.6)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.10979 - 1.52715 * distance()) + 0.0380053 / pow(distance() , 1.35689)) * 4.04133 * pow((width1() + width2()) / 2 , 0.0705769) * (0.0698966 * (thickness1() + thickness2()) / 2 + 0.0242367) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.42)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.195644 * (1 - exp(-1.03515 * (distance() + 0.0285746))) * pow(width() , 0.00374808 * distance() + -0.0122913) * (0.0616569 * thickness() + 0.193052)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0417574 * (1 - exp(-1.08213 * (distance() + -0.00703055)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.15855 * pow(width() , 0.0229515) * (0.106611 * thickness() + 0.228868)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (5.29872e-05 * distance() + 0.119434) * (0.678549 - exp(-(-0.0353119 * distance() + 2.17885) * (enclose() + -0.0184614 * distance() + 0.67019) - (0.0525984 * distance() + -0.451202) * (enclose() + -0.352621 * distance() + 1.61874) * (enclose() + -0.352621 * distance() + 1.61874)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.195644 * (1 - exp(-1.03515 * (distance() + 0.0285746))) * pow(width() , 0.00374808 * distance() + -0.0122913) * (0.0616569 * thickness() + 0.193052)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0599819 * (0.678549 - exp(-1.82573 * (enclose() + 0.485576) - 0.074782 * (enclose() + -1.90747) * (enclose() + -1.90747)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE INTRINSIC FRINGE metal3 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0534861 * (1 - exp(-0.874349 * (distance() + 0.0437439))) * pow(width() , 0.00668501 * distance() + -0.0126999) * (0.127182 * thickness() + 0.148068) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.42)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0101057 * (1 - exp(-0.966155 * (distance() + -0.0204412))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.42)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0898279 * pow(width() , 0.0428286) * (0.0949248 * thickness() + 0.0812385) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.42)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.14992 - 1.028 * distance()) + 0.0564354 / pow(distance() , 1.24579)) * 4.01322 * pow((width1() + width2()) / 2 , 0.0895139) * (0.0580564 * (thickness1() + thickness2()) / 2 + 0.0258394) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.42)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.132088 * (1 - exp(-0.546662 * (distance() + 0.0992509))) * pow(width() , 0.0104991 * distance() + -0.0437791) * (0.0635573 * thickness() + 0.182869)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0262789 * (1 - exp(-0.636738 * (distance() + -0.00982052)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.13403 * pow(width() , 0.0358437) * (0.0805253 * thickness() + 0.170153)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00035903 * distance() + 0.0966547) * (0.530394 - exp(-(-0.0154652 * distance() + 0.016146) * (enclose() + 7.11201 * distance() + 4.76804) - (0.00142966 * distance() + 0.0331408) * (enclose() + 1.21042 * distance() + 3.62532) * (enclose() + 1.21042 * distance() + 3.62532)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.132088 * (1 - exp(-0.546662 * (distance() + 0.0992509))) * pow(width() , 0.0104991 * distance() + -0.0437791) * (0.0635573 * thickness() + 0.182869)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0465322 * (0.530394 - exp(0.138506 * (enclose() + 75.8881) - 0.0474374 * (enclose() + 15.7295) * (enclose() + 15.7295)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE INTRINSIC FRINGE metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0759523 * (1 - exp(-0.523501 * (distance() + 0.113149))) * pow(width() , 0.0115763 * distance() + -0.0457603) * (0.0772221 * thickness() + 0.175199) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.42)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0148073 * (1 - exp(-0.62133 * (distance() + -0.0107934))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.42)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.106696 * pow(width() , 0.0418112) * (0.0687669 * thickness() + 0.116622) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.42)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.16812 - 0.825219 * distance()) + 0.0754946 / pow(distance() , 1.17834)) * 4.04205 * pow((width1() + width2()) / 2 , 0.104028) * (0.0505388 * (thickness1() + thickness2()) / 2 + 0.0241127) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.42)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.105733 * (1 - exp(-0.407868 * (distance() + 0.181014))) * pow(width() , 0.0142044 * distance() + -0.0647437) * (0.0629014 * thickness() + 0.179416)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0202379 * (1 - exp(-0.503529 * (distance() + 0.00642017)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.121791 * pow(width() , 0.0444409) * (0.0686198 * thickness() + 0.144455)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (1.97281e-05 * distance() + 0.0633972) * (0.588253 - exp(-(0.0288972 * distance() + 0.470182) * (enclose() + -0.0300792 * distance() + 1.81337) - (0.0146766 * distance() + -0.0555201) * (enclose() + -0.242866 * distance() + 2.74162) * (enclose() + -0.242866 * distance() + 2.74162)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.105733 * (1 - exp(-0.407868 * (distance() + 0.181014))) * pow(width() , 0.0142044 * distance() + -0.0647437) * (0.0629014 * thickness() + 0.179416)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0317972 * (0.588253 - exp(-0.759154 * (enclose() + 1.51258) - 0.0912459 * (enclose() + 0.31296) * (enclose() + 0.31296)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE INTRINSIC FRINGE metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0929593 * (1 - exp(-0.403385 * (distance() + 0.18909))) * pow(width() , 0.0144714 * distance() + -0.0647156) * (0.0580227 * thickness() + 0.182439) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.42)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0178478 * (1 - exp(-0.501173 * (distance() + 0.00836214))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.42)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.116154 * pow(width() , 0.046502) * (0.0598839 * thickness() + 0.135129) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.42)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal3,metal7
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.17133 - 0.716186 * distance()) + 0.0945061 / pow(distance() , 1.13059)) * 4.07502 * pow((width1() + width2()) / 2 , 0.111677) * (0.045007 * (thickness1() + thickness2()) / 2 + 0.0221313) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.42)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal3,metal7
//
CAPACITANCE CROSSOVER FRINGE metal3 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0890009 * (1 - exp(-0.345117 * (distance() + 0.255089))) * pow(width() , 0.0156765 * distance() + -0.0725493) * (0.06275 * thickness() + 0.178099)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0166752 * (1 - exp(-0.437528 * (distance() + 0.0289452)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.112726 * pow(width() , 0.050813) * (0.06248 * thickness() + 0.128651)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00031336 * distance() + 0.0419235) * (0.802758 - exp(-(0.123947 * distance() + 0.228591) * (enclose() + 0.0953829 * distance() + 1.53311) - (-0.00274976 * distance() + -0.0157727) * (enclose() + 0.463016 * distance() + 3.54287) * (enclose() + 0.463016 * distance() + 3.54287)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0890009 * (1 - exp(-0.345117 * (distance() + 0.255089))) * pow(width() , 0.0156765 * distance() + -0.0725493) * (0.06275 * thickness() + 0.178099)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.019395 * (0.802758 - exp(-1.46806 * (enclose() + 2.48694) - -0.0432703 * (enclose() + 8.17303) * (enclose() + 8.17303)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal3,metal7
//
CAPACITANCE INTRINSIC FRINGE metal3 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.105398 * (1 - exp(-0.345421 * (distance() + 0.258314))) * pow(width() , 0.0157296 * distance() + -0.0725584) * (0.0485471 * thickness() + 0.186119) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.42)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0200059 * (1 - exp(-0.438882 * (distance() + 0.0313022))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.42)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.122346 * pow(width() , 0.0510886) * (0.0563716 * thickness() + 0.147012) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.42)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal3,metal8
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.15988 - 0.639343 * distance()) + 0.117526 / pow(distance() , 1.08833)) * 4.11435 * pow((width1() + width2()) / 2 , 0.116611) * (0.0397395 * (thickness1() + thickness2()) / 2 + 0.0199182) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.42)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal3,metal8
//
CAPACITANCE CROSSOVER FRINGE metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0750823 * (1 - exp(-0.306778 * (distance() + 0.32668))) * pow(width() , 0.0162118 * distance() + -0.0749352) * (0.0626734 * thickness() + 0.17715)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0138576 * (1 - exp(-0.393357 * (distance() + 0.0550178)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.104893 * pow(width() , 0.0561444) * (0.0576423 * thickness() + 0.115004)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-6.77173e-05 * distance() + 0.0308034) * (0.863786 - exp(-(0.0955961 * distance() + 0.145784) * (enclose() + 0.241583 * distance() + 1.2157) - (-0.00164632 * distance() + -0.00780992) * (enclose() + 0.812849 * distance() + 3.42652) * (enclose() + 0.812849 * distance() + 3.42652)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0750823 * (1 - exp(-0.306778 * (distance() + 0.32668))) * pow(width() , 0.0162118 * distance() + -0.0749352) * (0.0626734 * thickness() + 0.17715)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0150631 * (0.863786 - exp(-1.10174 * (enclose() + 3.63153) - -0.0242731 * (enclose() + 11.555) * (enclose() + 11.555)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal3,metal8
//
CAPACITANCE INTRINSIC FRINGE metal3 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.116275 * (1 - exp(-0.309315 * (distance() + 0.325857))) * pow(width() , 0.0161638 * distance() + -0.0747514) * (0.0419669 * thickness() + 0.188433) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.42)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0218597 * (1 - exp(-0.396594 * (distance() + 0.0575398))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.42)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.126485 * pow(width() , 0.055632) * (0.0553471 * thickness() + 0.157961) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.42)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.1665 - 1.43464 * distance()) + 0.0472275 / pow(distance() , 1.29365)) * 4.01491 * pow((width1() + width2()) / 2 , 0.0785106) * (0.0667896 * (thickness1() + thickness2()) / 2 + 0.0236387) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.24)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.204046 * (1 - exp(-0.985819 * (distance() + 0.0464287))) * pow(width() , 0.0036802 * distance() + -0.0081997) * (0.058827 * thickness() + 0.194914)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0435617 * (1 - exp(-1.02206 * (distance() + 0.00529352)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.162577 * pow(width() , 0.0301924) * (0.10499 * thickness() + 0.236797)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00017624 * distance() + 0.121458) * (0.688354 - exp(-(-0.0405122 * distance() + 2.04081) * (enclose() + -0.00313966 * distance() + 0.636783) - (0.0471127 * distance() + -0.437205) * (enclose() + -0.363948 * distance() + 1.53586) * (enclose() + -0.363948 * distance() + 1.53586)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.204046 * (1 - exp(-0.985819 * (distance() + 0.0464287))) * pow(width() , 0.0036802 * distance() + -0.0081997) * (0.058827 * thickness() + 0.194914)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0616102 * (0.688354 - exp(-1.63569 * (enclose() + 0.605386) - 0.033922 * (enclose() + -2.10362) * (enclose() + -2.10362)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE INTRINSIC FRINGE metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.042847 * (1 - exp(-0.819859 * (distance() + 0.0698164))) * pow(width() , 0.00695279 * distance() + -0.00852564) * (0.135034 * thickness() + 0.144365) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.24)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00801349 * (1 - exp(-0.904718 * (distance() + -0.00622693))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.24)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0820549 * pow(width() , 0.0522916) * (0.088224 * thickness() + 0.0694966) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.24)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.19098 - 0.933899 * distance()) + 0.0690327 / pow(distance() , 1.19281)) * 4.05297 * pow((width1() + width2()) / 2 , 0.0962579) * (0.0538144 * (thickness1() + thickness2()) / 2 + 0.0244733) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.24)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.142031 * (1 - exp(-0.498887 * (distance() + 0.139326))) * pow(width() , 0.0110804 * distance() + -0.044708) * (0.0573962 * thickness() + 0.186192)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0282339 * (1 - exp(-0.584177 * (distance() + 0.00789694)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.13863 * pow(width() , 0.0419416) * (0.0769992 * thickness() + 0.18081)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00018012 * distance() + 0.177957) * (0.299988 - exp(-(-0.0316822 * distance() + 0.00712496) * (enclose() + 10.3866 * distance() + 23.8394) - (0.00010655 * distance() + 0.0146191) * (enclose() + 4.34225 * distance() + 7.93561) * (enclose() + 4.34225 * distance() + 7.93561)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.142031 * (1 - exp(-0.498887 * (distance() + 0.139326))) * pow(width() , 0.0110804 * distance() + -0.044708) * (0.0573962 * thickness() + 0.186192)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0880779 * (0.299988 - exp(0.309697 * (enclose() + 127.705) - 0.0156846 * (enclose() + 51.3581) * (enclose() + 51.3581)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE INTRINSIC FRINGE metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0628141 * (1 - exp(-0.473169 * (distance() + 0.158606))) * pow(width() , 0.0123084 * distance() + -0.0465541) * (0.0811536 * thickness() + 0.172531) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.24)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0120776 * (1 - exp(-0.566507 * (distance() + 0.00729271))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.24)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0985856 * pow(width() , 0.0492736) * (0.0646811 * thickness() + 0.102499) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.24)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal4,metal7
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.18074 - 0.734943 * distance()) + 0.0921454 / pow(distance() , 1.13371)) * 4.07656 * pow((width1() + width2()) / 2 , 0.110229) * (0.0458354 * (thickness1() + thickness2()) / 2 + 0.0223937) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.24)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal4,metal7
//
CAPACITANCE CROSSOVER FRINGE metal4 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.116702 * (1 - exp(-0.3616 * (distance() + 0.238975))) * pow(width() , 0.0150586 * distance() + -0.0691844) * (0.0547407 * thickness() + 0.182847)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0221751 * (1 - exp(-0.452777 * (distance() + 0.0277762)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.126126 * pow(width() , 0.0492738) * (0.0661771 * thickness() + 0.156068)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00010674 * distance() + 0.0641431) * (0.64316 - exp(-(0.0409675 * distance() + 0.445271) * (enclose() + -0.0528333 * distance() + 1.78058) - (0.0075608 * distance() + -0.0467445) * (enclose() + -0.278885 * distance() + 3.05287) * (enclose() + -0.278885 * distance() + 3.05287)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.116702 * (1 - exp(-0.3616 * (distance() + 0.238975))) * pow(width() , 0.0150586 * distance() + -0.0691844) * (0.0547407 * thickness() + 0.182847)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0315378 * (0.64316 - exp(-0.854946 * (enclose() + 1.25225) - 0.0288635 * (enclose() + 0.26402) * (enclose() + 0.26402)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal4,metal7
//
CAPACITANCE INTRINSIC FRINGE metal4 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0786342 * (1 - exp(-0.354538 * (distance() + 0.251877))) * pow(width() , 0.0154211 * distance() + -0.06884) * (0.060029 * thickness() + 0.18052) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.24)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0148442 * (1 - exp(-0.446513 * (distance() + 0.0327105))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.24)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.108047 * pow(width() , 0.0525606) * (0.056153 * thickness() + 0.120575) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.24)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal4,metal8
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.13764 - 0.614788 * distance()) + 0.122618 / pow(distance() , 1.084)) * 4.11346 * pow((width1() + width2()) / 2 , 0.118585) * (0.0383703 * (thickness1() + thickness2()) / 2 + 0.0194542) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.24)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal4,metal8
//
CAPACITANCE CROSSOVER FRINGE metal4 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0976607 * (1 - exp(-0.290706 * (distance() + 0.34532))) * pow(width() , 0.0168755 * distance() + -0.0810652) * (0.0530498 * thickness() + 0.181462)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0179983 * (1 - exp(-0.378347 * (distance() + 0.0579381)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.116915 * pow(width() , 0.0552325) * (0.0579529 * thickness() + 0.136893)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-9.56274e-05 * distance() + 0.040351) * (0.849322 - exp(-(0.119181 * distance() + 0.231257) * (enclose() + 0.224685 * distance() + 1.46963) - (-0.00176968 * distance() + -0.0164624) * (enclose() + 0.766727 * distance() + 3.80513) * (enclose() + 0.766727 * distance() + 3.80513)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0976607 * (1 - exp(-0.290706 * (distance() + 0.34532))) * pow(width() , 0.0168755 * distance() + -0.0810652) * (0.0530498 * thickness() + 0.181462)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0196974 * (0.849322 - exp(-1.42307 * (enclose() + 3.71648) - -0.0341592 * (enclose() + 11.4724) * (enclose() + 11.4724)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal4,metal8
//
CAPACITANCE INTRINSIC FRINGE metal4 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0930462 * (1 - exp(-0.289203 * (distance() + 0.353143))) * pow(width() , 0.0169242 * distance() + -0.079883) * (0.0475575 * thickness() + 0.184676) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.24)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.017211 * (1 - exp(-0.377626 * (distance() + 0.0608493))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.24)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.115249 * pow(width() , 0.0567999) * (0.0520148 * thickness() + 0.134528) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.24)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.17994 - 1.38516 * distance()) + 0.0565977 / pow(distance() , 1.24953)) * 4.1129 * pow((width1() + width2()) / 2 , 0.0829866) * (0.0610185 * (thickness1() + thickness2()) / 2 + 0.0217931) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.06)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.21347 * (1 - exp(-0.961486 * (distance() + 0.0584176))) * pow(width() , 0.00347286 * distance() + -0.00401963) * (0.0562391 * thickness() + 0.19266)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0447806 * (1 - exp(-0.988121 * (distance() + 0.0145516)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.165535 * pow(width() , 0.0356299) * (0.105479 * thickness() + 0.241464)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00030098 * distance() + 0.124821) * (0.681466 - exp(-(-0.0428519 * distance() + 1.94788) * (enclose() + 0.00946244 * distance() + 0.621455) - (0.043971 * distance() + -0.427262) * (enclose() + -0.371711 * distance() + 1.475) * (enclose() + -0.371711 * distance() + 1.475)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.21347 * (1 - exp(-0.961486 * (distance() + 0.0584176))) * pow(width() , 0.00347286 * distance() + -0.00401963) * (0.0562391 * thickness() + 0.19266)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0639154 * (0.681466 - exp(-1.51936 * (enclose() + 0.716079) - 0.012448 * (enclose() + -2.24211) * (enclose() + -2.24211)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE INTRINSIC FRINGE metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0357461 * (1 - exp(-0.793564 * (distance() + 0.087861))) * pow(width() , 0.0068429 * distance() + -0.00353171) * (0.14034 * thickness() + 0.14201) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.06)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00664482 * (1 - exp(-0.870392 * (distance() + 0.00526854))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.06)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0765637 * pow(width() , 0.0591842) * (0.0824913 * thickness() + 0.0610585) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.06)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal5,metal7
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.22221 - 0.874786 * distance()) + 0.0803748 / pow(distance() , 1.15407)) * 4.08594 * pow((width1() + width2()) / 2 , 0.100207) * (0.0506319 * (thickness1() + thickness2()) / 2 + 0.0232874) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.06)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal5,metal7
//
CAPACITANCE CROSSOVER FRINGE metal5 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.149389 * (1 - exp(-0.472791 * (distance() + 0.170507))) * pow(width() , 0.0111715 * distance() + -0.0430171) * (0.0536606 * thickness() + 0.187862)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0296316 * (1 - exp(-0.552486 * (distance() + 0.0237814)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.141645 * pow(width() , 0.0468084) * (0.0766435 * thickness() + 0.188224)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00024844 * distance() + 0.122558) * (0.442028 - exp(-(-0.0191225 * distance() + 0.00752524) * (enclose() + 8.87738 * distance() + 14.7906) - (0.00032758 * distance() + 0.022109) * (enclose() + 2.31992 * distance() + 5.18718) * (enclose() + 2.31992 * distance() + 5.18718)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.149389 * (1 - exp(-0.472791 * (distance() + 0.170507))) * pow(width() , 0.0111715 * distance() + -0.0430171) * (0.0536606 * thickness() + 0.187862)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0625212 * (0.442028 - exp(0.1837 * (enclose() + 103.564) - 0.0253848 * (enclose() + 28.3864) * (enclose() + 28.3864)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal5,metal7
//
CAPACITANCE INTRINSIC FRINGE metal5 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0535 * (1 - exp(-0.446347 * (distance() + 0.194181))) * pow(width() , 0.0124029 * distance() + -0.0442022) * (0.0839435 * thickness() + 0.171046) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.06)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0102167 * (1 - exp(-0.533431 * (distance() + 0.0244899))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.06)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0928035 * pow(width() , 0.0549726) * (0.0606429 * thickness() + 0.0920655) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.06)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal5,metal8
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.16994 - 0.652508 * distance()) + 0.114804 / pow(distance() , 1.09088)) * 4.11444 * pow((width1() + width2()) / 2 , 0.11567) * (0.0404353 * (thickness1() + thickness2()) / 2 + 0.0201996) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.06)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal5,metal8
//
CAPACITANCE CROSSOVER FRINGE metal5 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.121377 * (1 - exp(-0.319791 * (distance() + 0.307414))) * pow(width() , 0.0158041 * distance() + -0.073525) * (0.0491969 * thickness() + 0.184639)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0228042 * (1 - exp(-0.405892 * (distance() + 0.0525721)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.127707 * pow(width() , 0.054012) * (0.0629369 * thickness() + 0.161109)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-1.50774e-05 * distance() + 0.0534217) * (0.773768 - exp(-(0.0565273 * distance() + 0.144592) * (enclose() + 0.495191 * distance() + 1.26693) - (-2.77835e-05 * distance() + -0.00116969) * (enclose() + -5.05574 * distance() + 1.61985) * (enclose() + -5.05574 * distance() + 1.61985)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.121377 * (1 - exp(-0.319791 * (distance() + 0.307414))) * pow(width() , 0.0158041 * distance() + -0.073525) * (0.0491969 * thickness() + 0.184639)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0266355 * (0.773768 - exp(-0.709865 * (enclose() + 6.21884) - -0.00144752 * (enclose() + -48.9376) * (enclose() + -48.9376)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal5,metal8
//
CAPACITANCE INTRINSIC FRINGE metal5 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0704499 * (1 - exp(-0.313602 * (distance() + 0.323921))) * pow(width() , 0.0159869 * distance() + -0.0713699) * (0.0586272 * thickness() + 0.180719) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.06)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0131415 * (1 - exp(-0.400833 * (distance() + 0.0559586))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.06)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.103264 * pow(width() , 0.057892) * (0.0520884 * thickness() + 0.112403) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.06)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal6,metal7
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.22098 - 1.35647 * distance()) + 0.0624158 / pow(distance() , 1.21897)) * 4.1491 * pow((width1() + width2()) / 2 , 0.0857424) * (0.0593614 * (thickness1() + thickness2()) / 2 + 0.0213281) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.88)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal6,metal7
//
CAPACITANCE CROSSOVER FRINGE metal6 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.21493 * (1 - exp(-0.949671 * (distance() + 0.0663076))) * pow(width() , 0.00321954 * distance() + -0.00017789) * (0.0563133 * thickness() + 0.195741)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0456517 * (1 - exp(-0.967857 * (distance() + 0.0213102)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.166893 * pow(width() , 0.0398159) * (0.106702 * thickness() + 0.245985)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00815685 * distance() + 1.55687) * (0.0538062 - exp(-(-0.142066 * distance() + 0.161411) * (enclose() + 3.54321 * distance() + 9.56748) - (0.00074613 * distance() + 0.0441944) * (enclose() + 2.88095 * distance() + 5.36321) * (enclose() + 2.88095 * distance() + 5.36321)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.21493 * (1 - exp(-0.949671 * (distance() + 0.0663076))) * pow(width() , 0.00321954 * distance() + -0.00017789) * (0.0563133 * thickness() + 0.195741)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.819219 * (0.0538062 - exp(1.25925 * (enclose() + 44.9996) - 0.0516557 * (enclose() + 34.1727) * (enclose() + 34.1727)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal6,metal7
//
CAPACITANCE INTRINSIC FRINGE metal6 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0306875 * (1 - exp(-0.780451 * (distance() + 0.100309))) * pow(width() , 0.00660427 * distance() + 0.00122437) * (0.144078 * thickness() + 0.14023) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.88)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00567638 * (1 - exp(-0.85019 * (distance() + 0.0139275))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.88)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0720816 * pow(width() , 0.0643749) * (0.0777244 * thickness() + 0.0549489) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.88)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal6,metal8
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.22774 - 0.78274 * distance()) + 0.0976777 / pow(distance() , 1.11284)) * 4.12571 * pow((width1() + width2()) / 2 , 0.106373) * (0.045534 * (thickness1() + thickness2()) / 2 + 0.0215689) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.88)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal6,metal8
//
CAPACITANCE CROSSOVER FRINGE metal6 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.14893 * (1 - exp(-0.419668 * (distance() + 0.220526))) * pow(width() , 0.0122451 * distance() + -0.0484007) * (0.0502799 * thickness() + 0.188692)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0292557 * (1 - exp(-0.497518 * (distance() + 0.0416733)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.142226 * pow(width() , 0.051864) * (0.0728375 * thickness() + 0.187381)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00059837 * distance() + 0.0870296) * (0.586033 - exp(-(-0.0703336 * distance() + 0.122435) * (enclose() + 2.62266 * distance() + 3.08876) - (0.00010727 * distance() + 0.0326067) * (enclose() + 2.20424 * distance() + 1.25052) * (enclose() + 2.20424 * distance() + 1.25052)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.14893 * (1 - exp(-0.419668 * (distance() + 0.220526))) * pow(width() , 0.0122451 * distance() + -0.0484007) * (0.0502799 * thickness() + 0.188692)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0465066 * (0.586033 - exp(0.580901 * (enclose() + 29.3154) - 0.0336794 * (enclose() + 23.2929) * (enclose() + 23.2929)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal6,metal8
//
CAPACITANCE INTRINSIC FRINGE metal6 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0492935 * (1 - exp(-0.399886 * (distance() + 0.246237))) * pow(width() , 0.0130634 * distance() + -0.0470146) * (0.0800323 * thickness() + 0.172525) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.88)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00934911 * (1 - exp(-0.4824 * (distance() + 0.0444137))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.88)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.090281 * pow(width() , 0.0597872) * (0.0554558 * thickness() + 0.0878592) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.88)))
    }
]
//
//  model: 36  target: nearbody  profile: field  layers: field_base,metal7,metal8
//
CAPACITANCE NEARBODY metal7 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(-1.25885 - 1.16839 * distance()) + 0.0709176 / pow(distance() , 1.1685)) * 4.18491 * pow((width1() + width2()) / 2 , 0.0910545) * (0.0559296 * (thickness1() + thickness2()) / 2 + 0.0230491) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 5.7)))
]
//
//  model: 36  target: crossovere  profile: field  layers: field_base,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal7 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.201594 * (1 - exp(-0.753062 * (distance() + 0.0976707))) * pow(width() , 0.00508762 * distance() + -0.00712923) * (0.0546038 * thickness() + 0.195569)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0423696 * (1 - exp(-0.791318 * (distance() + 0.0261773)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.163405 * pow(width() , 0.0447998) * (0.0969905 * thickness() + 0.235883)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00317807 * distance() + 0.481605) * (0.158521 - exp(-(-0.125439 * distance() + 0.164978) * (enclose() + 3.22108 * distance() + 6.73104) - (0.00058271 * distance() + 0.037756) * (enclose() + 2.81851 * distance() + 3.97526) * (enclose() + 2.81851 * distance() + 3.97526)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.201594 * (1 - exp(-0.753062 * (distance() + 0.0976707))) * pow(width() , 0.00508762 * distance() + -0.00712923) * (0.0546038 * thickness() + 0.195569)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.256693 * (0.158521 - exp(1.08941 * (enclose() + 38.9418) - 0.0435831 * (enclose() + 32.1604) * (enclose() + 32.1604)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: field  layers: field_base,metal7,metal8
//
CAPACITANCE INTRINSIC FRINGE metal7 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0303936 * (1 - exp(-0.65057 * (distance() + 0.137))) * pow(width() , 0.0077737 * distance() + -0.00500156) * (0.126192 * thickness() + 0.148494) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.7)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00564799 * (1 - exp(-0.720094 * (distance() + 0.0267582))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.7)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0729376 * pow(width() , 0.0656397) * (0.0663669 * thickness() + 0.0578463) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 5.7)))
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.07918 - 1.84219 * distance()) + 0.0878218 / pow(distance() , 1.11974)) * 3.64801 * pow((width1() + width2()) / 2 , 0.0968867) * (0.072667 * (thickness1() + thickness2()) / 2 + 0.0168809) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.29)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE CROSSOVER FRINGE metal1 poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.14304 * (1 - exp(-1.35247 * (distance() + 0.060547))) * pow(width() , 0.00114938 * distance() + 0.0184054) * (0.0955203 * thickness() + 0.374754) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.14304 * (1 - exp(-1.35247 * (distance() + 0.060547))) * pow(width() , 0.00114938 * distance() + 0.0184054) * (0.0955203 * thickness() + 0.374754) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0553755 * (1 - exp(-1.3245 * (distance() + 0.0332079))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.194058 * pow(width() , 0.055405) * (0.172013 * thickness() + 0.266252) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0467373 * distance() + 17.8029) * (0.00569548 - exp(-(0.117728 * distance() + 0.461288) * (enclose() + -3.24807 * distance() + 5.02691) - (0.00083373 * distance() + 0.0115291) * (enclose() + 3.53151 * distance() + 15.5628) * (enclose() + 3.53151 * distance() + 15.5628))) + 0.5 * 0.53136 * exp(-1.1123 * enclose()) * (0.103154 - 0.109676 * exp(-0.56964 * distance()) * pow(distance() , 0.074328))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0467373 * distance() + 17.8029) * (0.00569548 - exp(-(0.117728 * distance() + 0.461288) * (enclose() + -3.24807 * distance() + 5.02691) - (0.00083373 * distance() + 0.0115291) * (enclose() + 3.53151 * distance() + 15.5628) * (enclose() + 3.53151 * distance() + 15.5628))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0560797 * (1 - exp(-1.62415 * (enclose() + 0.236294))) * pow(width() , -0.00493324 * enclose() + 0.0574671) + 0.5 * (0.0214845 * exp(-0.237134 * enclose()) + 0.0428492 * exp(-1.70017 * enclose())) * pow(width() , 0.0556102 * enclose() + 0.0934641)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0560797 * (1 - exp(-1.62415 * (enclose() + 0.236294))) * pow(width() , -0.00493324 * enclose() + 0.0574671) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.13688 - 0.842248 * distance()) + 0.209359 / pow(distance() , 0.98911)) * 3.19992 * pow((width1() + width2()) / 2 , 0.104979) * (0.0384731 * (thickness1() + thickness2()) / 2 + 0.0170995) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE CROSSOVER FRINGE metal2 poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.193295 * (1 - exp(-0.536516 * (distance() + 0.236359))) * pow(width() , 0.00517937 * distance() + 0.0129234) * (0.0373698 * thickness() + 0.205378) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.193295 * (1 - exp(-0.536516 * (distance() + 0.236359))) * pow(width() , 0.00517937 * distance() + 0.0129234) * (0.0373698 * thickness() + 0.205378) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0402656 * (1 - exp(-0.552763 * (distance() + 0.105478))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.170035 * pow(width() , 0.0756747) * (0.0824332 * thickness() + 0.232496) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00013551 * distance() + 0.412744) * (0.171426 - exp(-(-0.134397 * distance() + 0.24277) * (enclose() + 3.61914 * distance() + 6.62068) - (0.00013212 * distance() + 0.0186506) * (enclose() + 4.79201 * distance() + 1.66341) * (enclose() + 4.79201 * distance() + 1.66341))) + 0.5 * 0.500722 * exp(-0.560449 * enclose()) * (0.112207 - 0.111614 * exp(-0.357172 * distance()) * pow(distance() , 0.0740777))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00013551 * distance() + 0.412744) * (0.171426 - exp(-(-0.134397 * distance() + 0.24277) * (enclose() + 3.61914 * distance() + 6.62068) - (0.00013212 * distance() + 0.0186506) * (enclose() + 4.79201 * distance() + 1.66341) * (enclose() + 4.79201 * distance() + 1.66341))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0414669 * (1 - exp(-0.644747 * (enclose() + 0.344393))) * pow(width() , -0.00177061 * enclose() + 0.0696698) + 0.5 * (0.0375593 * exp(-0.832543 * enclose()) + 0.0227475 * exp(-0.181039 * enclose())) * pow(width() , 0.0219867 * enclose() + 0.0762239)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0414669 * (1 - exp(-0.644747 * (enclose() + 0.344393))) * pow(width() , -0.00177061 * enclose() + 0.0696698) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE NEARBODY metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.51651 - 0.526972 * distance()) + 0.221427 / pow(distance() , 0.914336)) * 5.42513 * pow((width1() + width2()) / 2 , 0.121736) * (0.0247983 * (thickness1() + thickness2()) / 2 + 0.0124354) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.82)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE CROSSOVER FRINGE metal3 poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.171191 * (1 - exp(-0.335757 * (distance() + 0.457034))) * pow(width() , 0.00923374 * distance() + -0.0171092) * (0.0298583 * thickness() + 0.19936) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.171191 * (1 - exp(-0.335757 * (distance() + 0.457034))) * pow(width() , 0.00923374 * distance() + -0.0171092) * (0.0298583 * thickness() + 0.19936) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0337689 * (1 - exp(-0.36525 * (distance() + 0.177517))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.154636 * pow(width() , 0.0801523) * (0.0709066 * thickness() + 0.219621) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0002299 * distance() + 0.109591) * (0.523123 - exp(-(-0.0880031 * distance() + 0.179562) * (enclose() + 2.98659 * distance() + 3.12453) - (8.73849e-05 * distance() + 0.0165161) * (enclose() + 3.78615 * distance() + -0.816945) * (enclose() + 3.78615 * distance() + -0.816945))) + 0.5 * 0.452878 * exp(-0.394135 * enclose()) * (0.122316 - 0.109104 * exp(-0.222194 * distance()) * pow(distance() , 0.0350309))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0002299 * distance() + 0.109591) * (0.523123 - exp(-(-0.0880031 * distance() + 0.179562) * (enclose() + 2.98659 * distance() + 3.12453) - (8.73849e-05 * distance() + 0.0165161) * (enclose() + 3.78615 * distance() + -0.816945) * (enclose() + 3.78615 * distance() + -0.816945))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0355681 * (1 - exp(-0.409165 * (enclose() + 0.378066))) * pow(width() , 0.00130634 * enclose() + 0.0580313) + 0.5 * (0.0328458 * exp(-0.531186 * enclose()) + 0.0210585 * exp(-0.149606 * enclose())) * pow(width() , 0.0122796 * enclose() + 0.0731644)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0355681 * (1 - exp(-0.409165 * (enclose() + 0.378066))) * pow(width() , 0.00130634 * enclose() + 0.0580313) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE NEARBODY metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.14268 - 0.371486 * distance()) + 0.414116 / pow(distance() , 0.874618)) * 5.42899 * pow((width1() + width2()) / 2 , 0.130939) * (0.0143208 * (thickness1() + thickness2()) / 2 + 0.00757883) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.64)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE CROSSOVER FRINGE metal4 poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.160074 * (1 - exp(-0.247899 * (distance() + 0.68709))) * pow(width() , 0.0109587 * distance() + -0.0356883) * (0.0232074 * thickness() + 0.194859) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.160074 * (1 - exp(-0.247899 * (distance() + 0.68709))) * pow(width() , 0.0109587 * distance() + -0.0356883) * (0.0232074 * thickness() + 0.194859) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0303176 * (1 - exp(-0.276197 * (distance() + 0.250301))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.148043 * pow(width() , 0.080666) * (0.0617638 * thickness() + 0.208604) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00053216 * distance() + 0.0651872) * (0.696799 - exp(-(0.0224138 * distance() + 0.234644) * (enclose() + -0.115537 * distance() + 1.986) - (0.0046008 * distance() + -0.0238771) * (enclose() + -0.491059 * distance() + 2.65724) * (enclose() + -0.491059 * distance() + 2.65724))) + 0.5 * 0.456226 * exp(-0.315923 * enclose()) * (0.126555 - 0.106391 * exp(-0.134784 * distance()) * pow(distance() , 0.00184135))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00053216 * distance() + 0.0651872) * (0.696799 - exp(-(0.0224138 * distance() + 0.234644) * (enclose() + -0.115537 * distance() + 1.986) - (0.0046008 * distance() + -0.0238771) * (enclose() + -0.491059 * distance() + 2.65724) * (enclose() + -0.491059 * distance() + 2.65724))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0325897 * (1 - exp(-0.30847 * (enclose() + 0.393459))) * pow(width() , 0.0028211 * enclose() + 0.0496501) + 0.5 * (0.0169326 * exp(-0.123455 * enclose()) + 0.0313324 * exp(-0.36542 * enclose())) * pow(width() , 0.0084032 * enclose() + 0.0708776)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.64)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0325897 * (1 - exp(-0.30847 * (enclose() + 0.393459))) * pow(width() , 0.0028211 * enclose() + 0.0496501) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.64)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE NEARBODY metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.625634 - 0.280363 * distance()) + 0.759974 / pow(distance() , 0.859455)) * 5.71852 * pow((width1() + width2()) / 2 , 0.136136) * (0.00758608 * (thickness1() + thickness2()) / 2 + 0.00414858) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.46)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE CROSSOVER FRINGE metal5 poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.152076 * (1 - exp(-0.194261 * (distance() + 0.928484))) * pow(width() , 0.011855 * distance() + -0.049211) * (0.0191251 * thickness() + 0.192499) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.152076 * (1 - exp(-0.194261 * (distance() + 0.928484))) * pow(width() , 0.011855 * distance() + -0.049211) * (0.0191251 * thickness() + 0.192499) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0281087 * (1 - exp(-0.221476 * (distance() + 0.316753))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.144009 * pow(width() , 0.0793057) * (0.0538631 * thickness() + 0.199399) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00029455 * distance() + 0.0616372) * (0.758864 - exp(-(0.0228306 * distance() + 0.129303) * (enclose() + -0.129073 * distance() + 2.16441) - (0.00206907 * distance() + -0.0155883) * (enclose() + -0.531993 * distance() + 1.78924) * (enclose() + -0.531993 * distance() + 1.78924))) + 0.5 * 0.536952 * exp(-0.270259 * enclose()) * (0.12442 - 0.104267 * exp(-0.073825 * distance()) * pow(distance() , -0.0152309))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00029455 * distance() + 0.0616372) * (0.758864 - exp(-(0.0228306 * distance() + 0.129303) * (enclose() + -0.129073 * distance() + 2.16441) - (0.00206907 * distance() + -0.0155883) * (enclose() + -0.531993 * distance() + 1.78924) * (enclose() + -0.531993 * distance() + 1.78924))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0306494 * (1 - exp(-0.251626 * (enclose() + 0.412886))) * pow(width() , 0.00350722 * enclose() + 0.0441267) + 0.5 * (0.033846 * exp(-0.255494 * enclose()) + 0.00952045 * exp(-0.0932485 * enclose())) * pow(width() , 0.00652629 * enclose() + 0.0678929)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.46)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0306494 * (1 - exp(-0.251626 * (enclose() + 0.412886))) * pow(width() , 0.00350722 * enclose() + 0.0441267) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.46)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(0.0524143 - 0.227977 * distance()) + 1.45746 / pow(distance() , 0.859152)) * 6.07366 * pow((width1() + width2()) / 2 , 0.140188) * (0.00368805 * (thickness1() + thickness2()) / 2 + 0.00206304) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.28)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.14962 * (1 - exp(-0.156623 * (distance() + 1.17696))) * pow(width() , 0.0123801 * distance() + -0.060556) * (0.0128482 * thickness() + 0.188203) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.28)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.14962 * (1 - exp(-0.156623 * (distance() + 1.17696))) * pow(width() , 0.0123801 * distance() + -0.060556) * (0.0128482 * thickness() + 0.188203) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.28)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0266913 * (1 - exp(-0.182427 * (distance() + 0.372934))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.28)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.140813 * pow(width() , 0.0768118) * (0.0480213 * thickness() + 0.191274) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.28)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00054467 * distance() + 0.0559963) * (0.825318 - exp(-(0.0193226 * distance() + 0.0841732) * (enclose() + -0.110942 * distance() + 2.06519) - (0.00107427 * distance() + -0.0103487) * (enclose() + -0.58668 * distance() + 1.33568) * (enclose() + -0.58668 * distance() + 1.33568))) + 0.5 * 0.918121 * exp(-0.240696 * enclose()) * (0.128746 - 0.116104 * exp(-0.0246511 * distance()) * pow(distance() , -0.0132765))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.28)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00054467 * distance() + 0.0559963) * (0.825318 - exp(-(0.0193226 * distance() + 0.0841732) * (enclose() + -0.110942 * distance() + 2.06519) - (0.00107427 * distance() + -0.0103487) * (enclose() + -0.58668 * distance() + 1.33568) * (enclose() + -0.58668 * distance() + 1.33568))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.28)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0291716 * (1 - exp(-0.21543 * (enclose() + 0.438017))) * pow(width() , 0.00377879 * enclose() + 0.0401857) + 0.5 * (0.0195559 * exp(-0.144333 * enclose()) + 0.0196597 * exp(-0.218139 * enclose())) * pow(width() , 0.0054783 * enclose() + 0.0643807)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.28)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0291716 * (1 - exp(-0.21543 * (enclose() + 0.438017))) * pow(width() , 0.00377879 * enclose() + 0.0401857) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.28)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal7
//
CAPACITANCE NEARBODY metal7 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(0.862654 - 0.209589 * distance()) + 2.99128 / pow(distance() , 0.855425)) * 6.11312 * pow((width1() + width2()) / 2 , 0.145506) * (0.0017525 * (thickness1() + thickness2()) / 2 + 0.0010419) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 5.1)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal7
//
CAPACITANCE CROSSOVER FRINGE metal7 poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.158664 * (1 - exp(-0.115061 * (distance() + 1.46008))) * pow(width() , 0.0142961 * distance() + -0.0833583) * (0.00808494 * thickness() + 0.184244) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.1)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.158664 * (1 - exp(-0.115061 * (distance() + 1.46008))) * pow(width() , 0.0142961 * distance() + -0.0833583) * (0.00808494 * thickness() + 0.184244) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.1)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0262399 * (1 - exp(-0.150069 * (distance() + 0.397726))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.1)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.137638 * pow(width() , 0.0713357) * (0.0417783 * thickness() + 0.186113) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 5.1)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00085131 * distance() + 0.0555577) * (0.87539 - exp(-(0.0144367 * distance() + 0.0582506) * (enclose() + -0.0596109 * distance() + 1.9134) - (0.00058075 * distance() + -0.00791961) * (enclose() + -0.64094 * distance() + 0.926793) * (enclose() + -0.64094 * distance() + 0.926793))) + 0.5 * 3.01098 * exp(-0.221547 * enclose()) * (0.122896 - 0.119163 * exp(-0.005786 * distance()) * pow(distance() , -0.00374936))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.1)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00085131 * distance() + 0.0555577) * (0.87539 - exp(-(0.0144367 * distance() + 0.0582506) * (enclose() + -0.0596109 * distance() + 1.9134) - (0.00058075 * distance() + -0.00791961) * (enclose() + -0.64094 * distance() + 0.926793) * (enclose() + -0.64094 * distance() + 0.926793))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.1)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0280474 * (1 - exp(-0.19104 * (enclose() + 0.46688))) * pow(width() , 0.00376353 * enclose() + 0.0354995) + 0.5 * (0.0179751 * exp(-0.157988 * enclose()) + 0.017975 * exp(-0.158129 * enclose())) * pow(width() , 0.00463279 * enclose() + 0.0587564)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 5.1)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0280474 * (1 - exp(-0.19104 * (enclose() + 0.46688))) * pow(width() , 0.00376353 * enclose() + 0.0354995) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 5.1)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,poly,metal8
//
CAPACITANCE NEARBODY metal8 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    C = length() * (exp(-1.1098 - 0.252866 * distance()) + 0.614087 / pow(distance() , 0.880551)) * 4.31434 * pow((width1() + width2()) / 2 , 0.108296) * (0.0111453 * (thickness1() + thickness2()) / 2 + 0.0108503) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 6.14)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,poly,metal8
//
CAPACITANCE CROSSOVER FRINGE metal8 poly MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_calenclose = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    max_enclose = MAXENCLOSE_metal8
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.191199 * (1 - exp(-0.0701049 * (distance() + 1.15246))) * pow(width() , 0.0300462 * distance() + -0.213254) * (0.00066757 * thickness() + 0.198319) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 6.14)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.191199 * (1 - exp(-0.0701049 * (distance() + 1.15246))) * pow(width() , 0.0300462 * distance() + -0.213254) * (0.00066757 * thickness() + 0.198319) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 6.14)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0324681 * (1 - exp(-0.0955067 * (distance() + 0.19711))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 6.14)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.117853 * pow(width() , 0.0392901) * (0.0306111 * thickness() + 0.205979) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 6.14)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00104452 * distance() + 0.0560853) * (0.905248 - exp(-(0.0135977 * distance() + 0.0383182) * (enclose() + 0.00478402 * distance() + 1.50693) - (0.00021093 * distance() + -0.00534516) * (enclose() + -0.661111 * distance() + 0.703755) * (enclose() + -0.661111 * distance() + 0.703755))) + 0.5 * 2.40513 * exp(-0.222945 * enclose()) * (0.112639 - 0.109705 * exp(-0.0113528 * distance()) * pow(distance() , 0.00235923))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 6.14)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00104452 * distance() + 0.0560853) * (0.905248 - exp(-(0.0135977 * distance() + 0.0383182) * (enclose() + 0.00478402 * distance() + 1.50693) - (0.00021093 * distance() + -0.00534516) * (enclose() + -0.661111 * distance() + 0.703755) * (enclose() + -0.661111 * distance() + 0.703755))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 6.14)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.028379 * (1 - exp(-0.168403 * (enclose() + 0.487075))) * pow(width() , 0.0053755 * enclose() + -0.00378258) + 0.5 * (0.0168584 * exp(-0.137837 * enclose()) + 0.0168584 * exp(-0.137847 * enclose())) * pow(width() , 0.00268507 * enclose() + 0.0373768)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 6.14)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.028379 * (1 - exp(-0.168403 * (enclose() + 0.487075))) * pow(width() , 0.0053755 * enclose() + -0.00378258) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 6.14)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.20847 - 1.30249 * distance()) + 0.129555 / pow(distance() , 1.07284)) * 3.56023 * pow((width1() + width2()) / 2 , 0.0927641) * (0.0491434 * (thickness1() + thickness2()) / 2 + 0.0173669) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.221164 * (1 - exp(-0.912746 * (distance() + 0.107146))) * pow(width() , 0.00139517 * distance() + 0.025782) * (0.0304126 * thickness() + 0.222514) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.221164 * (1 - exp(-0.912746 * (distance() + 0.107146))) * pow(width() , 0.00139517 * distance() + 0.025782) * (0.0304126 * thickness() + 0.222514) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0499108 * (1 - exp(-0.889295 * (distance() + 0.0614613))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.180277 * pow(width() , 0.0635926) * (0.0906746 * thickness() + 0.268334) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0187236 * distance() + 2.72777) * (0.0316814 - exp(-(-0.193168 * distance() + 0.430928) * (enclose() + 2.64271 * distance() + 6.99216) - (0.00050399 * distance() + 0.0342549) * (enclose() + 3.39391 * distance() + 3.15214) * (enclose() + 3.39391 * distance() + 3.15214))) + 0.5 * 0.419472 * exp(-0.72266 * enclose()) * (0.111816 - 0.105823 * exp(-0.291069 * distance()) * pow(distance() , 0.035338))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0187236 * distance() + 2.72777) * (0.0316814 - exp(-(-0.193168 * distance() + 0.430928) * (enclose() + 2.64271 * distance() + 6.99216) - (0.00050399 * distance() + 0.0342549) * (enclose() + 3.39391 * distance() + 3.15214) * (enclose() + 3.39391 * distance() + 3.15214))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0511768 * (1 - exp(-1.07442 * (enclose() + 0.430194))) * pow(width() , -0.00300561 * enclose() + 0.0620966) + 0.5 * (0.0308381 * exp(-1.2296 * enclose()) + 0.0191469 * exp(-0.213812 * enclose())) * pow(width() , 0.0389905 * enclose() + 0.0838005)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0511768 * (1 - exp(-1.07442 * (enclose() + 0.430194))) * pow(width() , -0.00300561 * enclose() + 0.0620966) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.57884 - 0.691285 * distance()) + 0.162806 / pow(distance() , 0.956152)) * 5.10506 * pow((width1() + width2()) / 2 , 0.112135) * (0.0329993 * (thickness1() + thickness2()) / 2 + 0.0155878) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE CROSSOVER FRINGE metal3 metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.182465 * (1 - exp(-0.43723 * (distance() + 0.315889))) * pow(width() , 0.00704693 * distance() + -0.00020937) * (0.0351485 * thickness() + 0.202679) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.182465 * (1 - exp(-0.43723 * (distance() + 0.315889))) * pow(width() , 0.00704693 * distance() + -0.00020937) * (0.0351485 * thickness() + 0.202679) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0372357 * (1 - exp(-0.461681 * (distance() + 0.129919))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.161978 * pow(width() , 0.0776572) * (0.078347 * thickness() + 0.227736) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0004971 * distance() + 0.286306) * (0.222841 - exp(-(-0.111188 * distance() + 0.188466) * (enclose() + 4.80336 * distance() + 7.35249) - (5.98524e-05 * distance() + 0.0126509) * (enclose() + 6.20356 * distance() + 1.16412) * (enclose() + 6.20356 * distance() + 1.16412))) + 0.5 * 0.43308 * exp(-0.452527 * enclose()) * (0.115395 - 0.104915 * exp(-0.192428 * distance()) * pow(distance() , 0.0372742))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0004971 * distance() + 0.286306) * (0.222841 - exp(-(-0.111188 * distance() + 0.188466) * (enclose() + 4.80336 * distance() + 7.35249) - (5.98524e-05 * distance() + 0.0126509) * (enclose() + 6.20356 * distance() + 1.16412) * (enclose() + 6.20356 * distance() + 1.16412))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0390298 * (1 - exp(-0.540206 * (enclose() + 0.576236))) * pow(width() , -0.00010128 * enclose() + 0.0639822) + 0.5 * (0.0273103 * exp(-0.685341 * enclose()) + 0.0192068 * exp(-0.172546 * enclose())) * pow(width() , 0.0185523 * enclose() + 0.070454)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0390298 * (1 - exp(-0.540206 * (enclose() + 0.576236))) * pow(width() , -0.00010128 * enclose() + 0.0639822) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.44787 - 0.455257 * distance()) + 0.263226 / pow(distance() , 0.897912)) * 5.67194 * pow((width1() + width2()) / 2 , 0.12592) * (0.0206041 * (thickness1() + thickness2()) / 2 + 0.0105864) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.09)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE CROSSOVER FRINGE metal4 metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.166358 * (1 - exp(-0.293667 * (distance() + 0.546116))) * pow(width() , 0.0102648 * distance() + -0.027174) * (0.0272771 * thickness() + 0.197638) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.166358 * (1 - exp(-0.293667 * (distance() + 0.546116))) * pow(width() , 0.0102648 * distance() + -0.027174) * (0.0272771 * thickness() + 0.197638) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0323087 * (1 - exp(-0.323904 * (distance() + 0.20531))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.152303 * pow(width() , 0.0801761) * (0.0666468 * thickness() + 0.214313) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00066535 * distance() + 0.0749313) * (0.669585 - exp(-(0.0137284 * distance() + 0.368549) * (enclose() + -0.0690104 * distance() + 1.83197) - (0.00615033 * distance() + -0.0397915) * (enclose() + -0.476556 * distance() + 2.96476) * (enclose() + -0.476556 * distance() + 2.96476))) + 0.5 * 0.491581 * exp(-0.350326 * enclose()) * (0.119663 - 0.104362 * exp(-0.101901 * distance()) * pow(distance() , 0.00940062))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00066535 * distance() + 0.0749313) * (0.669585 - exp(-(0.0137284 * distance() + 0.368549) * (enclose() + -0.0690104 * distance() + 1.83197) - (0.00615033 * distance() + -0.0397915) * (enclose() + -0.476556 * distance() + 2.96476) * (enclose() + -0.476556 * distance() + 2.96476))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0345141 * (1 - exp(-0.376597 * (enclose() + 0.646132))) * pow(width() , 0.00188592 * enclose() + 0.0560041) + 0.5 * (0.0182725 * exp(-0.1507 * enclose()) + 0.0241915 * exp(-0.469292 * enclose())) * pow(width() , 0.0118459 * enclose() + 0.066611)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0345141 * (1 - exp(-0.376597 * (enclose() + 0.646132))) * pow(width() , 0.00188592 * enclose() + 0.0560041) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.953261 - 0.329289 * distance()) + 0.512105 / pow(distance() , 0.870389)) * 5.6456 * pow((width1() + width2()) / 2 , 0.133431) * (0.0111948 * (thickness1() + thickness2()) / 2 + 0.00600679) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.91)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE CROSSOVER FRINGE metal5 metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.157002 * (1 - exp(-0.220913 * (distance() + 0.786702))) * pow(width() , 0.0116883 * distance() + -0.0443773) * (0.0207824 * thickness() + 0.194401) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.157002 * (1 - exp(-0.220913 * (distance() + 0.786702))) * pow(width() , 0.0116883 * distance() + -0.0443773) * (0.0207824 * thickness() + 0.194401) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0294719 * (1 - exp(-0.249919 * (distance() + 0.276467))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.146382 * pow(width() , 0.0796566) * (0.0581041 * thickness() + 0.204725) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00017867 * distance() + 0.0698028) * (0.707105 - exp(-(0.0197889 * distance() + 0.200963) * (enclose() + -0.0909004 * distance() + 2.2143) - (0.00248737 * distance() + -0.0210279) * (enclose() + -0.488183 * distance() + 2.68484) * (enclose() + -0.488183 * distance() + 2.68484))) + 0.5 * 0.865264 * exp(-0.295321 * enclose()) * (0.118193 - 0.107364 * exp(-0.0338101 * distance()) * pow(distance() , -0.00480581))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00017867 * distance() + 0.0698028) * (0.707105 - exp(-(0.0197889 * distance() + 0.200963) * (enclose() + -0.0909004 * distance() + 2.2143) - (0.00248737 * distance() + -0.0210279) * (enclose() + -0.488183 * distance() + 2.68484) * (enclose() + -0.488183 * distance() + 2.68484))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0318864 * (1 - exp(-0.297182 * (enclose() + 0.692579))) * pow(width() , 0.00296043 * enclose() + 0.0493556) + 0.5 * (0.0194773 * exp(-0.360787 * enclose()) + 0.0190591 * exp(-0.148053 * enclose())) * pow(width() , 0.00877453 * enclose() + 0.0636563)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0318864 * (1 - exp(-0.297182 * (enclose() + 0.692579))) * pow(width() , 0.00296043 * enclose() + 0.0493556) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-0.065449 - 0.256799 * distance()) + 1.25235 / pow(distance() , 0.865603)) * 5.86104 * pow((width1() + width2()) / 2 , 0.138521) * (0.00440461 * (thickness1() + thickness2()) / 2 + 0.0024334) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.73)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.153733 * (1 - exp(-0.171461 * (distance() + 1.04619))) * pow(width() , 0.0126597 * distance() + -0.0589045) * (0.0155429 * thickness() + 0.189941) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.153733 * (1 - exp(-0.171461 * (distance() + 1.04619))) * pow(width() , 0.0126597 * distance() + -0.0589045) * (0.0155429 * thickness() + 0.189941) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0277164 * (1 - exp(-0.200866 * (distance() + 0.338544))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.142676 * pow(width() , 0.0775595) * (0.0514308 * thickness() + 0.195735) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00102442 * distance() + 0.0663781) * (0.806674 - exp(-(0.0172053 * distance() + 0.108831) * (enclose() + -0.0511475 * distance() + 2.05885) - (0.00078788 * distance() + -0.0127893) * (enclose() + -0.551298 * distance() + 1.89831) * (enclose() + -0.551298 * distance() + 1.89831))) + 0.5 * 3.80903 * exp(-0.261459 * enclose()) * (0.118185 - 0.11559 * exp(-0.00507101 * distance()) * pow(distance() , -0.00178102))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00102442 * distance() + 0.0663781) * (0.806674 - exp(-(0.0172053 * distance() + 0.108831) * (enclose() + -0.0511475 * distance() + 2.05885) - (0.00078788 * distance() + -0.0127893) * (enclose() + -0.551298 * distance() + 1.89831) * (enclose() + -0.551298 * distance() + 1.89831))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0299755 * (1 - exp(-0.250234 * (enclose() + 0.745322))) * pow(width() , 0.0031553 * enclose() + 0.0464301) + 0.5 * (0.0175192 * exp(-0.266553 * enclose()) + 0.0174726 * exp(-0.144318 * enclose())) * pow(width() , 0.0071704 * enclose() + 0.0603218)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.73)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0299755 * (1 - exp(-0.250234 * (enclose() + 0.745322))) * pow(width() , 0.0031553 * enclose() + 0.0464301) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.73)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal7
//
CAPACITANCE NEARBODY metal7 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(0.337918 - 0.228758 * distance()) + 1.71509 / pow(distance() , 0.861273)) * 5.92417 * pow((width1() + width2()) / 2 , 0.144539) * (0.00312369 * (thickness1() + thickness2()) / 2 + 0.00184028) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.55)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal7
//
CAPACITANCE CROSSOVER FRINGE metal7 metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.162803 * (1 - exp(-0.123896 * (distance() + 1.33425))) * pow(width() , 0.0147194 * distance() + -0.0830702) * (0.00994887 * thickness() + 0.185114) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.162803 * (1 - exp(-0.123896 * (distance() + 1.33425))) * pow(width() , 0.0147194 * distance() + -0.0830702) * (0.00994887 * thickness() + 0.185114) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.55)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0270663 * (1 - exp(-0.162466 * (distance() + 0.37019))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.55)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.139894 * pow(width() , 0.0722056) * (0.0445818 * thickness() + 0.189072) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00124634 * distance() + 0.063792) * (0.855941 - exp(-(0.0142237 * distance() + 0.0709136) * (enclose() + -0.0228269 * distance() + 2.02524) - (0.00035266 * distance() + -0.00883021) * (enclose() + -0.557039 * distance() + 1.36871) * (enclose() + -0.557039 * distance() + 1.36871))) + 0.5 * 18.8581 * exp(-0.239959 * enclose()) * (0.115231 - 0.114727 * exp(-0.00090849 * distance()) * pow(distance() , -0.00029247))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.55)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00124634 * distance() + 0.063792) * (0.855941 - exp(-(0.0142237 * distance() + 0.0709136) * (enclose() + -0.0228269 * distance() + 2.02524) - (0.00035266 * distance() + -0.00883021) * (enclose() + -0.557039 * distance() + 1.36871) * (enclose() + -0.557039 * distance() + 1.36871))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.55)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0285894 * (1 - exp(-0.219769 * (enclose() + 0.792499))) * pow(width() , 0.00315055 * enclose() + 0.0419278) + 0.5 * (0.016029 * exp(-0.172093 * enclose()) + 0.016029 * exp(-0.172494 * enclose())) * pow(width() , 0.00601945 * enclose() + 0.054802)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.55)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0285894 * (1 - exp(-0.219769 * (enclose() + 0.792499))) * pow(width() , 0.00315055 * enclose() + 0.0419278) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.55)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal1,metal8
//
CAPACITANCE NEARBODY metal8 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    C = length() * (exp(-1.16899 - 0.266788 * distance()) + 0.536974 / pow(distance() , 0.892396)) * 4.28569 * pow((width1() + width2()) / 2 , 0.106978) * (0.0125973 * (thickness1() + thickness2()) / 2 + 0.0121426) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 5.59)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal1,metal8
//
CAPACITANCE CROSSOVER FRINGE metal8 metal1 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_calenclose = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    max_enclose = MAXENCLOSE_metal8
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.193898 * (1 - exp(-0.0750374 * (distance() + 1.05702))) * pow(width() , 0.0299424 * distance() + -0.209452) * (0.00150608 * thickness() + 0.199441) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.59)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.193898 * (1 - exp(-0.0750374 * (distance() + 1.05702))) * pow(width() , 0.0299424 * distance() + -0.209452) * (0.00150608 * thickness() + 0.199441) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.59)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0332208 * (1 - exp(-0.102237 * (distance() + 0.179198))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.59)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.119308 * pow(width() , 0.0389842) * (0.032399 * thickness() + 0.209685) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 5.59)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00381134 * distance() + 0.0194839) * (0.673678 - exp(-(0.00035531 * distance() + 0.147912) * (enclose() + 0.218113 * distance() + 1.7984) - (0.00476357 * distance() + -0.00222886) * (enclose() + -0.488095 * distance() + 4.82215) * (enclose() + -0.488095 * distance() + 4.82215))) + 0.5 * 7.86459 * exp(-0.240259 * enclose()) * (0.110505 - 0.109766 * exp(-0.0032269 * distance()) * pow(distance() , 0.00157925))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.59)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00381134 * distance() + 0.0194839) * (0.673678 - exp(-(0.00035531 * distance() + 0.147912) * (enclose() + 0.218113 * distance() + 1.7984) - (0.00476357 * distance() + -0.00222886) * (enclose() + -0.488095 * distance() + 4.82215) * (enclose() + -0.488095 * distance() + 4.82215))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.59)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0287243 * (1 - exp(-0.192022 * (enclose() + 0.835577))) * pow(width() , 0.00413447 * enclose() + 0.00588676) + 0.5 * (0.015077 * exp(-0.150024 * enclose()) + 0.0150769 * exp(-0.150075 * enclose())) * pow(width() , 0.0036383 * enclose() + 0.0321604)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 5.59)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0287243 * (1 - exp(-0.192022 * (enclose() + 0.835577))) * pow(width() , 0.00413447 * enclose() + 0.00588676) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 5.59)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.21027 - 1.29935 * distance()) + 0.130624 / pow(distance() , 1.07152)) * 3.56061 * pow((width1() + width2()) / 2 , 0.0929885) * (0.0489305 * (thickness1() + thickness2()) / 2 + 0.017298) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal3 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.22124 * (1 - exp(-0.914656 * (distance() + 0.109835))) * pow(width() , 0.00116174 * distance() + 0.0303438) * (0.0301494 * thickness() + 0.223649) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.22124 * (1 - exp(-0.914656 * (distance() + 0.109835))) * pow(width() , 0.00116174 * distance() + 0.0303438) * (0.0301494 * thickness() + 0.223649) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0500251 * (1 - exp(-0.887238 * (distance() + 0.0650418))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.181338 * pow(width() , 0.0665143) * (0.0901674 * thickness() + 0.268284) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0219117 * distance() + 2.56815) * (0.0334339 - exp(-(-0.183407 * distance() + 0.376923) * (enclose() + 2.81989 * distance() + 7.49126) - (0.00050293 * distance() + 0.033645) * (enclose() + 3.42843 * distance() + 3.74843) * (enclose() + 3.42843 * distance() + 3.74843))) + 0.5 * 0.367553 * exp(-0.666553 * enclose()) * (0.115118 - 0.100319 * exp(-0.15558 * distance()) * pow(distance() , -0.00383249))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0219117 * distance() + 2.56815) * (0.0334339 - exp(-(-0.183407 * distance() + 0.376923) * (enclose() + 2.81989 * distance() + 7.49126) - (0.00050293 * distance() + 0.033645) * (enclose() + 3.42843 * distance() + 3.74843) * (enclose() + 3.42843 * distance() + 3.74843))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0519541 * (1 - exp(-1.1312 * (enclose() + 0.499181))) * pow(width() , -0.00257202 * enclose() + 0.064647) + 0.5 * (0.015538 * exp(-0.22032 * enclose()) + 0.023123 * exp(-1.21225 * enclose())) * pow(width() , 0.0395963 * enclose() + 0.0778937)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0519541 * (1 - exp(-1.1312 * (enclose() + 0.499181))) * pow(width() , -0.00257202 * enclose() + 0.064647) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.57551 - 0.682761 * distance()) + 0.164103 / pow(distance() , 0.956522)) * 5.10815 * pow((width1() + width2()) / 2 , 0.112422) * (0.0327221 * (thickness1() + thickness2()) / 2 + 0.0154697) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal4 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.182563 * (1 - exp(-0.434009 * (distance() + 0.321611))) * pow(width() , 0.0070659 * distance() + -0.0001119) * (0.0347558 * thickness() + 0.202673) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.182563 * (1 - exp(-0.434009 * (distance() + 0.321611))) * pow(width() , 0.0070659 * distance() + -0.0001119) * (0.0347558 * thickness() + 0.202673) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0372537 * (1 - exp(-0.457785 * (distance() + 0.133261))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.161964 * pow(width() , 0.0780231) * (0.0781255 * thickness() + 0.227827) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0010853 * distance() + 0.173869) * (0.355272 - exp(-(-0.101705 * distance() + 0.17753) * (enclose() + 3.51543 * distance() + 5.10976) - (0.00011325 * distance() + 0.0173052) * (enclose() + 4.24455 * distance() + 1.24157) * (enclose() + 4.24455 * distance() + 1.24157))) + 0.5 * 0.455564 * exp(-0.436244 * enclose()) * (0.115467 - 0.102136 * exp(-0.0928477 * distance()) * pow(distance() , 0.00804043))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0010853 * distance() + 0.173869) * (0.355272 - exp(-(-0.101705 * distance() + 0.17753) * (enclose() + 3.51543 * distance() + 5.10976) - (0.00011325 * distance() + 0.0173052) * (enclose() + 4.24455 * distance() + 1.24157) * (enclose() + 4.24455 * distance() + 1.24157))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0394502 * (1 - exp(-0.567561 * (enclose() + 0.738355))) * pow(width() , -3.98506e-05 * enclose() + 0.0655265) + 0.5 * (0.0199808 * exp(-0.692692 * enclose()) + 0.0163631 * exp(-0.184714 * enclose())) * pow(width() , 0.0208716 * enclose() + 0.0625349)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0394502 * (1 - exp(-0.567561 * (enclose() + 0.738355))) * pow(width() , -3.98506e-05 * enclose() + 0.0655265) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.42001 - 0.445728 * distance()) + 0.264588 / pow(distance() , 0.902028)) * 5.67559 * pow((width1() + width2()) / 2 , 0.126351) * (0.0202921 * (thickness1() + thickness2()) / 2 + 0.0104388) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.09)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal5 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.167258 * (1 - exp(-0.283237 * (distance() + 0.565533))) * pow(width() , 0.011036 * distance() + -0.0324108) * (0.028491 * thickness() + 0.197382) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.167258 * (1 - exp(-0.283237 * (distance() + 0.565533))) * pow(width() , 0.011036 * distance() + -0.0324108) * (0.028491 * thickness() + 0.197382) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0323816 * (1 - exp(-0.318267 * (distance() + 0.209235))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.152248 * pow(width() , 0.0798876) * (0.0664507 * thickness() + 0.213965) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00071602 * distance() + 0.0907479) * (0.675779 - exp(-(0.0128159 * distance() + 0.306712) * (enclose() + -0.00603781 * distance() + 1.83799) - (0.00296676 * distance() + -0.0388304) * (enclose() + -0.520625 * distance() + 2.44943) * (enclose() + -0.520625 * distance() + 2.44943))) + 0.5 * 1.4922 * exp(-0.348111 * enclose()) * (0.115415 - 0.110199 * exp(-0.0156218 * distance()) * pow(distance() , -0.00110498))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00071602 * distance() + 0.0907479) * (0.675779 - exp(-(0.0128159 * distance() + 0.306712) * (enclose() + -0.00603781 * distance() + 1.83799) - (0.00296676 * distance() + -0.0388304) * (enclose() + -0.520625 * distance() + 2.44943) * (enclose() + -0.520625 * distance() + 2.44943))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0347501 * (1 - exp(-0.398697 * (enclose() + 0.875953))) * pow(width() , 0.00159049 * enclose() + 0.0592305) + 0.5 * (0.0166701 * exp(-0.168721 * enclose()) + 0.0167684 * exp(-0.488539 * enclose())) * pow(width() , 0.0142178 * enclose() + 0.0580222)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0347501 * (1 - exp(-0.398697 * (enclose() + 0.875953))) * pow(width() , 0.00159049 * enclose() + 0.0592305) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-0.876883 - 0.321874 * distance()) + 0.509006 / pow(distance() , 0.881696)) * 5.68054 * pow((width1() + width2()) / 2 , 0.134597) * (0.0108639 * (thickness1() + thickness2()) / 2 + 0.00586321) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.91)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.160308 * (1 - exp(-0.20787 * (distance() + 0.824759))) * pow(width() , 0.0125514 * distance() + -0.0512035) * (0.0201477 * thickness() + 0.193439) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.160308 * (1 - exp(-0.20787 * (distance() + 0.824759))) * pow(width() , 0.0125514 * distance() + -0.0512035) * (0.0201477 * thickness() + 0.193439) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0297166 * (1 - exp(-0.24197 * (distance() + 0.279519))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.146257 * pow(width() , 0.0787975) * (0.0576664 * thickness() + 0.204187) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00155703 * distance() + 0.0824004) * (0.765734 - exp(-(0.0160493 * distance() + 0.15727) * (enclose() + -0.00358649 * distance() + 2.00632) - (0.00069423 * distance() + -0.0188944) * (enclose() + -0.516627 * distance() + 2.16887) * (enclose() + -0.516627 * distance() + 2.16887))) + 0.5 * 7.86438 * exp(-0.301071 * enclose()) * (0.113053 - 0.11202 * exp(-0.00231669 * distance()) * pow(distance() , -0.00029853))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00155703 * distance() + 0.0824004) * (0.765734 - exp(-(0.0160493 * distance() + 0.15727) * (enclose() + -0.00358649 * distance() + 2.00632) - (0.00069423 * distance() + -0.0188944) * (enclose() + -0.516627 * distance() + 2.16887) * (enclose() + -0.516627 * distance() + 2.16887))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0319246 * (1 - exp(-0.31802 * (enclose() + 0.975646))) * pow(width() , 0.00229509 * enclose() + 0.0545876) + 0.5 * (0.0124967 * exp(-0.380529 * enclose()) + 0.0180629 * exp(-0.167987 * enclose())) * pow(width() , 0.0104301 * enclose() + 0.0549622)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0319246 * (1 - exp(-0.31802 * (enclose() + 0.975646))) * pow(width() , 0.00229509 * enclose() + 0.0545876) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal2,metal7
//
CAPACITANCE NEARBODY metal7 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(-0.341684 - 0.268813 * distance()) + 0.810083 / pow(distance() , 0.874157)) * 5.64021 * pow((width1() + width2()) / 2 , 0.142348) * (0.00679259 * (thickness1() + thickness2()) / 2 + 0.00392921) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.73)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal2,metal7
//
CAPACITANCE CROSSOVER FRINGE metal7 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.167761 * (1 - exp(-0.145261 * (distance() + 1.11705))) * pow(width() , 0.0150453 * distance() + -0.0789177) * (0.0134613 * thickness() + 0.187593) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.167761 * (1 - exp(-0.145261 * (distance() + 1.11705))) * pow(width() , 0.0150453 * distance() + -0.0789177) * (0.0134613 * thickness() + 0.187593) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.028593 * (1 - exp(-0.188677 * (distance() + 0.322498))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.143018 * pow(width() , 0.0738128) * (0.0489817 * thickness() + 0.19617) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0006186 * distance() + 0.0665018) * (0.845793 - exp(-(0.0409986 * distance() + 0.128962) * (enclose() + 0.0593969 * distance() + 2.21324) - (-0.00045995 * distance() + -0.00789107) * (enclose() + 0.525415 * distance() + 4.36448) * (enclose() + 0.525415 * distance() + 4.36448))) + 0.5 * 22.5565 * exp(-0.272904 * enclose()) * (0.112291 - 0.111941 * exp(-0.00072394 * distance()) * pow(distance() , -4.13296e-05))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0006186 * distance() + 0.0665018) * (0.845793 - exp(-(0.0409986 * distance() + 0.128962) * (enclose() + 0.0593969 * distance() + 2.21324) - (-0.00045995 * distance() + -0.00789107) * (enclose() + 0.525415 * distance() + 4.36448) * (enclose() + 0.525415 * distance() + 4.36448))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0300289 * (1 - exp(-0.271099 * (enclose() + 1.0584))) * pow(width() , 0.00242965 * enclose() + 0.0497152) + 0.5 * (0.014023 * exp(-0.157057 * enclose()) + 0.0140218 * exp(-0.256218 * enclose())) * pow(width() , 0.008544 * enclose() + 0.0495231)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.73)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0300289 * (1 - exp(-0.271099 * (enclose() + 1.0584))) * pow(width() , 0.00242965 * enclose() + 0.0497152) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.73)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal2,metal8
//
CAPACITANCE NEARBODY metal8 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    C = length() * (exp(-1.47641 - 0.292693 * distance()) + 0.344852 / pow(distance() , 0.915145)) * 5.27652 * pow((width1() + width2()) / 2 , 0.10432) * (0.0153797 * (thickness1() + thickness2()) / 2 + 0.0145445) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.77)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal2,metal8
//
CAPACITANCE CROSSOVER FRINGE metal8 metal2 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_calenclose = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    max_enclose = MAXENCLOSE_metal8
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.191907 * (1 - exp(-0.0870271 * (distance() + 0.898071))) * pow(width() , 0.0287431 * distance() + -0.194675) * (0.00310049 * thickness() + 0.205018) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.77)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.191907 * (1 - exp(-0.0870271 * (distance() + 0.898071))) * pow(width() , 0.0287431 * distance() + -0.194675) * (0.00310049 * thickness() + 0.205018) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.77)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0345045 * (1 - exp(-0.116079 * (distance() + 0.15382))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.77)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.121876 * pow(width() , 0.0390481) * (0.035541 * thickness() + 0.216403) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 4.77)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00376162 * distance() + 0.0270438) * (0.668885 - exp(-(-0.00220161 * distance() + 0.16664) * (enclose() + 0.193019 * distance() + 2.09263) - (0.00527554 * distance() + -0.0050486) * (enclose() + -0.341324 * distance() + 4.54005) * (enclose() + -0.341324 * distance() + 4.54005))) + 0.5 * 5.68798 * exp(-0.27009 * enclose()) * (0.108731 - 0.107921 * exp(-0.00420907 * distance()) * pow(distance() , 0.0030949))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.77)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00376162 * distance() + 0.0270438) * (0.668885 - exp(-(-0.00220161 * distance() + 0.16664) * (enclose() + 0.193019 * distance() + 2.09263) - (0.00527554 * distance() + -0.0050486) * (enclose() + -0.341324 * distance() + 4.54005) * (enclose() + -0.341324 * distance() + 4.54005))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.77)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0299095 * (1 - exp(-0.23079 * (enclose() + 1.14256))) * pow(width() , 0.00289712 * enclose() + 0.0159333) + 0.5 * (0.0131858 * exp(-0.171709 * enclose()) + 0.0131857 * exp(-0.171995 * enclose())) * pow(width() , 0.00514311 * enclose() + 0.0254447)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 4.77)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0299095 * (1 - exp(-0.23079 * (enclose() + 1.14256))) * pow(width() , 0.00289712 * enclose() + 0.0159333) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 4.77)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.2126 - 1.29366 * distance()) + 0.132015 / pow(distance() , 1.07008)) * 3.56099 * pow((width1() + width2()) / 2 , 0.0933193) * (0.0486395 * (thickness1() + thickness2()) / 2 + 0.017205) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal4 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.221072 * (1 - exp(-0.912892 * (distance() + 0.110506))) * pow(width() , 0.00115075 * distance() + 0.0296802) * (0.029848 * thickness() + 0.223567) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.221072 * (1 - exp(-0.912892 * (distance() + 0.110506))) * pow(width() , 0.00115075 * distance() + 0.0296802) * (0.029848 * thickness() + 0.223567) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0499969 * (1 - exp(-0.883944 * (distance() + 0.0657418))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.181207 * pow(width() , 0.0661591) * (0.0899181 * thickness() + 0.268342) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0199021 * distance() + 2.07638) * (0.0411444 - exp(-(-0.173063 * distance() + 0.331) * (enclose() + 2.76966 * distance() + 7.46725) - (0.00058274 * distance() + 0.0352112) * (enclose() + 3.17481 * distance() + 4.1831) * (enclose() + 3.17481 * distance() + 4.1831))) + 0.5 * 0.39643 * exp(-0.658717 * enclose()) * (0.114782 - 0.0988191 * exp(-0.0817617 * distance()) * pow(distance() , -0.0200558))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0199021 * distance() + 2.07638) * (0.0411444 - exp(-(-0.173063 * distance() + 0.331) * (enclose() + 2.76966 * distance() + 7.46725) - (0.00058274 * distance() + 0.0352112) * (enclose() + 3.17481 * distance() + 4.1831) * (enclose() + 3.17481 * distance() + 4.1831))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0522834 * (1 - exp(-1.19022 * (enclose() + 0.523594))) * pow(width() , -0.00231776 * enclose() + 0.0644704) + 0.5 * (0.0135679 * exp(-0.233559 * enclose()) + 0.0183335 * exp(-1.2484 * enclose())) * pow(width() , 0.0456806 * enclose() + 0.067474)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0522834 * (1 - exp(-1.19022 * (enclose() + 0.523594))) * pow(width() , -0.00231776 * enclose() + 0.0644704) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.56463 - 0.669427 * distance()) + 0.165612 / pow(distance() , 0.958432)) * 5.10831 * pow((width1() + width2()) / 2 , 0.112839) * (0.0323652 * (thickness1() + thickness2()) / 2 + 0.0152787) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal5 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.183029 * (1 - exp(-0.426569 * (distance() + 0.329298))) * pow(width() , 0.00740158 * distance() + -0.00332268) * (0.0352483 * thickness() + 0.20177) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.183029 * (1 - exp(-0.426569 * (distance() + 0.329298))) * pow(width() , 0.00740158 * distance() + -0.00332268) * (0.0352483 * thickness() + 0.20177) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0372623 * (1 - exp(-0.451808 * (distance() + 0.13526))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.161858 * pow(width() , 0.077256) * (0.0776975 * thickness() + 0.227558) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00245524 * distance() + 0.302542) * (0.202789 - exp(-(-0.102508 * distance() + 0.161417) * (enclose() + 5.03059 * distance() + 8.79537) - (4.98547e-05 * distance() + 0.0124315) * (enclose() + 6.15848 * distance() + 2.46717) * (enclose() + 6.15848 * distance() + 2.46717))) + 0.5 * 0.760863 * exp(-0.439437 * enclose()) * (0.112893 - 0.104359 * exp(-0.0315389 * distance()) * pow(distance() , -0.00241283))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00245524 * distance() + 0.302542) * (0.202789 - exp(-(-0.102508 * distance() + 0.161417) * (enclose() + 5.03059 * distance() + 8.79537) - (4.98547e-05 * distance() + 0.0124315) * (enclose() + 6.15848 * distance() + 2.46717) * (enclose() + 6.15848 * distance() + 2.46717))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0396644 * (1 - exp(-0.599411 * (enclose() + 0.817402))) * pow(width() , -0.0001131 * enclose() + 0.0665121) + 0.5 * (0.0150576 * exp(-0.726582 * enclose()) + 0.0149127 * exp(-0.203228 * enclose())) * pow(width() , 0.0236156 * enclose() + 0.0547243)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0396644 * (1 - exp(-0.599411 * (enclose() + 0.817402))) * pow(width() , -0.0001131 * enclose() + 0.0665121) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.34696 - 0.430086 * distance()) + 0.269372 / pow(distance() , 0.911242)) * 5.66617 * pow((width1() + width2()) / 2 , 0.127598) * (0.0195264 * (thickness1() + thickness2()) / 2 + 0.0100777) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.09)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.169308 * (1 - exp(-0.272921 * (distance() + 0.586736))) * pow(width() , 0.0115072 * distance() + -0.0373564) * (0.0267026 * thickness() + 0.196351) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.169308 * (1 - exp(-0.272921 * (distance() + 0.586736))) * pow(width() , 0.0115072 * distance() + -0.0373564) * (0.0267026 * thickness() + 0.196351) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0325424 * (1 - exp(-0.309547 * (distance() + 0.211402))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.15232 * pow(width() , 0.0786552) * (0.0657172 * thickness() + 0.213201) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00095949 * distance() + 0.0931333) * (0.688147 - exp(-(0.0126987 * distance() + 0.292537) * (enclose() + 0.0217611 * distance() + 1.78798) - (0.00229267 * distance() + -0.0377194) * (enclose() + -0.507836 * distance() + 2.33879) * (enclose() + -0.507836 * distance() + 2.33879))) + 0.5 * 12.588 * exp(-0.357987 * enclose()) * (0.111059 - 0.110504 * exp(-0.0013641 * distance()) * pow(distance() , -8.49884e-05))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00095949 * distance() + 0.0931333) * (0.688147 - exp(-(0.0126987 * distance() + 0.292537) * (enclose() + 0.0217611 * distance() + 1.78798) - (0.00229267 * distance() + -0.0377194) * (enclose() + -0.507836 * distance() + 2.33879) * (enclose() + -0.507836 * distance() + 2.33879))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0348494 * (1 - exp(-0.422607 * (enclose() + 1.00515))) * pow(width() , 0.00130033 * enclose() + 0.0611585) + 0.5 * (0.0102651 * exp(-0.562853 * enclose()) + 0.0173615 * exp(-0.200371 * enclose())) * pow(width() , 0.015869 * enclose() + 0.0502166)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0348494 * (1 - exp(-0.422607 * (enclose() + 1.00515))) * pow(width() , 0.00130033 * enclose() + 0.0611585) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal3,metal7
//
CAPACITANCE NEARBODY metal7 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(-0.875693 - 0.328034 * distance()) + 0.427923 / pow(distance() , 0.893402)) * 5.46042 * pow((width1() + width2()) / 2 , 0.138751) * (0.0128271 * (thickness1() + thickness2()) / 2 + 0.00723197) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.91)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal3,metal7
//
CAPACITANCE CROSSOVER FRINGE metal7 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.171794 * (1 - exp(-0.180382 * (distance() + 0.877417))) * pow(width() , 0.014982 * distance() + -0.0718525) * (0.0178315 * thickness() + 0.191349) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.171794 * (1 - exp(-0.180382 * (distance() + 0.877417))) * pow(width() , 0.014982 * distance() + -0.0718525) * (0.0178315 * thickness() + 0.191349) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0305313 * (1 - exp(-0.228206 * (distance() + 0.265109))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.147422 * pow(width() , 0.0746633) * (0.05467 * thickness() + 0.203764) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00073306 * distance() + 0.0781654) * (0.769158 - exp(-(0.0556661 * distance() + 0.207521) * (enclose() + 0.059822 * distance() + 2.5589) - (-0.00051641 * distance() + -0.0131271) * (enclose() + 0.53079 * distance() + 4.84257) * (enclose() + 0.53079 * distance() + 4.84257))) + 0.5 * 17.57 * exp(-0.315875 * enclose()) * (0.110752 - 0.110364 * exp(-0.00089367 * distance()) * pow(distance() , 6.84112e-05))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00073306 * distance() + 0.0781654) * (0.769158 - exp(-(0.0556661 * distance() + 0.207521) * (enclose() + 0.059822 * distance() + 2.5589) - (-0.00051641 * distance() + -0.0131271) * (enclose() + 0.53079 * distance() + 4.84257) * (enclose() + 0.53079 * distance() + 4.84257))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.032118 * (1 - exp(-0.339762 * (enclose() + 1.13838))) * pow(width() , 0.00180139 * enclose() + 0.0554078) + 0.5 * (0.00312504 * exp(-0.649665 * enclose()) + 0.0223464 * exp(-0.212377 * enclose())) * pow(width() , 0.0120429 * enclose() + 0.0448544)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.032118 * (1 - exp(-0.339762 * (enclose() + 1.13838))) * pow(width() , 0.00180139 * enclose() + 0.0554078) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal3,metal8
//
CAPACITANCE NEARBODY metal8 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    C = length() * (exp(-1.50108 - 0.324899 * distance()) + 0.286789 / pow(distance() , 0.944558)) * 4.99571 * pow((width1() + width2()) / 2 , 0.100653) * (0.0186788 * (thickness1() + thickness2()) / 2 + 0.017213) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.95)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal3,metal8
//
CAPACITANCE CROSSOVER FRINGE metal8 metal3 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_calenclose = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    max_enclose = MAXENCLOSE_metal8
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.186017 * (1 - exp(-0.106023 * (distance() + 0.720032))) * pow(width() , 0.0270048 * distance() + -0.177572) * (0.00483394 * thickness() + 0.213159) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.95)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.186017 * (1 - exp(-0.106023 * (distance() + 0.720032))) * pow(width() , 0.0270048 * distance() + -0.177572) * (0.00483394 * thickness() + 0.213159) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.95)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0359244 * (1 - exp(-0.135889 * (distance() + 0.11957))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.95)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.124846 * pow(width() , 0.0379686) * (0.0392311 * thickness() + 0.224254) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 3.95)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00369968 * distance() + 0.0958761) * (0.957885 - exp(-(0.0176271 * distance() + 0.0967045) * (enclose() + -0.0416189 * distance() + 1.42086) - (-0.00080688 * distance() + -0.00654125) * (enclose() + -0.520886 * distance() + 4.51327) * (enclose() + -0.520886 * distance() + 4.51327))) + 0.5 * 23.5365 * exp(-0.307004 * enclose()) * (0.107237 - 0.107078 * exp(-0.00096879 * distance()) * pow(distance() , 0.00083541))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.95)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00369968 * distance() + 0.0958761) * (0.957885 - exp(-(0.0176271 * distance() + 0.0967045) * (enclose() + -0.0416189 * distance() + 1.42086) - (-0.00080688 * distance() + -0.00654125) * (enclose() + -0.520886 * distance() + 4.51327) * (enclose() + -0.520886 * distance() + 4.51327))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.95)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0316416 * (1 - exp(-0.277473 * (enclose() + 1.26741))) * pow(width() , 0.00223442 * enclose() + 0.0202688) + 0.5 * (0.0119527 * exp(-0.199057 * enclose()) + 0.0119526 * exp(-0.199454 * enclose())) * pow(width() , 0.00694833 * enclose() + 0.0192385)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 3.95)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0316416 * (1 - exp(-0.277473 * (enclose() + 1.26741))) * pow(width() , 0.00223442 * enclose() + 0.0202688) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 3.95)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.21588 - 1.28247 * distance()) + 0.134005 / pow(distance() , 1.06862)) * 3.56169 * pow((width1() + width2()) / 2 , 0.0938701) * (0.0481961 * (thickness1() + thickness2()) / 2 + 0.0170582) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal5 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.221001 * (1 - exp(-0.90962 * (distance() + 0.111857))) * pow(width() , 0.00113262 * distance() + 0.0288895) * (0.0294149 * thickness() + 0.223395) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.221001 * (1 - exp(-0.90962 * (distance() + 0.111857))) * pow(width() , 0.00113262 * distance() + 0.0288895) * (0.0294149 * thickness() + 0.223395) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0499763 * (1 - exp(-0.878456 * (distance() + 0.0670279))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.181048 * pow(width() , 0.0657239) * (0.0896338 * thickness() + 0.268412) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0211501 * distance() + 2.01797) * (0.0422357 - exp(-(-0.170064 * distance() + 0.315525) * (enclose() + 2.80526 * distance() + 7.59129) - (0.00059477 * distance() + 0.0357952) * (enclose() + 3.13649 * distance() + 4.3154) * (enclose() + 3.13649 * distance() + 4.3154))) + 0.5 * 0.663817 * exp(-0.668241 * enclose()) * (0.110613 - 0.100806 * exp(-0.0287438 * distance()) * pow(distance() , -0.0157343))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0211501 * distance() + 2.01797) * (0.0422357 - exp(-(-0.170064 * distance() + 0.315525) * (enclose() + 2.80526 * distance() + 7.59129) - (0.00059477 * distance() + 0.0357952) * (enclose() + 3.13649 * distance() + 4.3154) * (enclose() + 3.13649 * distance() + 4.3154))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0525393 * (1 - exp(-1.24341 * (enclose() + 0.535914))) * pow(width() , -0.00210909 * enclose() + 0.0641821) + 0.5 * (0.0148924 * exp(-1.30206 * enclose()) + 0.0122542 * exp(-0.253327 * enclose())) * pow(width() , 0.0500668 * enclose() + 0.0587921)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0525393 * (1 - exp(-1.24341 * (enclose() + 0.535914))) * pow(width() , -0.00210909 * enclose() + 0.0641821) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.53911 - 0.642014 * distance()) + 0.167764 / pow(distance() , 0.963848)) * 5.10467 * pow((width1() + width2()) / 2 , 0.114199) * (0.0316563 * (thickness1() + thickness2()) / 2 + 0.0149767) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.184772 * (1 - exp(-0.412668 * (distance() + 0.345091))) * pow(width() , 0.0080404 * distance() + -0.0086314) * (0.0334834 * thickness() + 0.20094) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.184772 * (1 - exp(-0.412668 * (distance() + 0.345091))) * pow(width() , 0.0080404 * distance() + -0.0086314) * (0.0334834 * thickness() + 0.20094) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0373593 * (1 - exp(-0.44184 * (distance() + 0.138748))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.161406 * pow(width() , 0.0763402) * (0.0774806 * thickness() + 0.227763) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0029046 * distance() + 0.278733) * (0.217762 - exp(-(-0.100237 * distance() + 0.157746) * (enclose() + 4.75478 * distance() + 8.47449) - (5.51152e-05 * distance() + 0.0133165) * (enclose() + 5.705 * distance() + 2.5644) * (enclose() + 5.705 * distance() + 2.5644))) + 0.5 * 4.0543 * exp(-0.453899 * enclose()) * (0.109542 - 0.108025 * exp(-0.0040298 * distance()) * pow(distance() , -0.00062165))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0029046 * distance() + 0.278733) * (0.217762 - exp(-(-0.100237 * distance() + 0.157746) * (enclose() + 4.75478 * distance() + 8.47449) - (5.51152e-05 * distance() + 0.0133165) * (enclose() + 5.705 * distance() + 2.5644) * (enclose() + 5.705 * distance() + 2.5644))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0398424 * (1 - exp(-0.630874 * (enclose() + 0.86415))) * pow(width() , -0.00023186 * enclose() + 0.067568) + 0.5 * (0.0112136 * exp(-0.784924 * enclose()) + 0.0142443 * exp(-0.230017 * enclose())) * pow(width() , 0.0255828 * enclose() + 0.0473355)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0398424 * (1 - exp(-0.630874 * (enclose() + 0.86415))) * pow(width() , -0.00023186 * enclose() + 0.067568) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal4,metal7
//
CAPACITANCE NEARBODY metal7 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(-1.32958 - 0.423306 * distance()) + 0.232194 / pow(distance() , 0.923868)) * 5.70589 * pow((width1() + width2()) / 2 , 0.132292) * (0.0213988 * (thickness1() + thickness2()) / 2 + 0.011574) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.09)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal4,metal7
//
CAPACITANCE CROSSOVER FRINGE metal7 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.17764 * (1 - exp(-0.242619 * (distance() + 0.627536))) * pow(width() , 0.0138275 * distance() + -0.0565746) * (0.0234395 * thickness() + 0.195713) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.17764 * (1 - exp(-0.242619 * (distance() + 0.627536))) * pow(width() , 0.0138275 * distance() + -0.0565746) * (0.0234395 * thickness() + 0.195713) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0333235 * (1 - exp(-0.293018 * (distance() + 0.202579))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.152403 * pow(width() , 0.0747775) * (0.0627209 * thickness() + 0.215115) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00072765 * distance() + 0.069209) * (0.748737 - exp(-(0.0254085 * distance() + 0.28995) * (enclose() + -0.10081 * distance() + 2.25287) - (0.00168397 * distance() + -0.0134721) * (enclose() + -0.508174 * distance() + 5.65825) * (enclose() + -0.508174 * distance() + 5.65825))) + 0.5 * 14.7905 * exp(-0.377242 * enclose()) * (0.109799 - 0.109399 * exp(-0.00103762 * distance()) * pow(distance() , 0.00013385))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00072765 * distance() + 0.069209) * (0.748737 - exp(-(0.0254085 * distance() + 0.28995) * (enclose() + -0.10081 * distance() + 2.25287) - (0.00168397 * distance() + -0.0134721) * (enclose() + -0.508174 * distance() + 5.65825) * (enclose() + -0.508174 * distance() + 5.65825))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0352206 * (1 - exp(-0.447296 * (enclose() + 1.08028))) * pow(width() , 0.00096991 * enclose() + 0.061093) + 0.5 * (0.00600519 * exp(-0.662406 * enclose()) + 0.017629 * exp(-0.232818 * enclose())) * pow(width() , 0.0173087 * enclose() + 0.0409388)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0352206 * (1 - exp(-0.447296 * (enclose() + 1.08028))) * pow(width() , 0.00096991 * enclose() + 0.061093) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal4,metal8
//
CAPACITANCE NEARBODY metal8 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    C = length() * (exp(-1.55584 - 0.367065 * distance()) + 0.224846 / pow(distance() , 0.983088)) * 4.89028 * pow((width1() + width2()) / 2 , 0.0955582) * (0.0229902 * (thickness1() + thickness2()) / 2 + 0.020421) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.13)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal4,metal8
//
CAPACITANCE CROSSOVER FRINGE metal8 metal4 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_calenclose = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    max_enclose = MAXENCLOSE_metal8
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.180733 * (1 - exp(-0.136363 * (distance() + 0.537627))) * pow(width() , 0.0240903 * distance() + -0.152714) * (0.00715946 * thickness() + 0.220909) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.13)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.180733 * (1 - exp(-0.136363 * (distance() + 0.537627))) * pow(width() , 0.0240903 * distance() + -0.152714) * (0.00715946 * thickness() + 0.220909) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.13)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.037702 * (1 - exp(-0.165598 * (distance() + 0.0836845))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.13)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.130223 * pow(width() , 0.0363796) * (0.0432912 * thickness() + 0.230958) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 3.13)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00266195 * distance() + 0.0780334) * (1.21291 - exp(-(0.0225596 * distance() + 0.221753) * (enclose() + -0.121251 * distance() + 1.6536) - (-0.0007459 * distance() + -0.0117539) * (enclose() + -0.485151 * distance() + 7.19534) * (enclose() + -0.485151 * distance() + 7.19534))) + 0.5 * 23.4885 * exp(-0.354421 * enclose()) * (0.106507 - 0.106385 * exp(-0.00096824 * distance()) * pow(distance() , 0.0008905))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.13)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00266195 * distance() + 0.0780334) * (1.21291 - exp(-(0.0225596 * distance() + 0.221753) * (enclose() + -0.121251 * distance() + 1.6536) - (-0.0007459 * distance() + -0.0117539) * (enclose() + -0.485151 * distance() + 7.19534) * (enclose() + -0.485151 * distance() + 7.19534))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.13)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0340846 * (1 - exp(-0.339957 * (enclose() + 1.26736))) * pow(width() , 0.00168424 * enclose() + 0.023153) + 0.5 * (0.011122 * exp(-0.235303 * enclose()) + 0.0111219 * exp(-0.236097 * enclose())) * pow(width() , 0.00944815 * enclose() + 0.0127469)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 3.13)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0340846 * (1 - exp(-0.339957 * (enclose() + 1.26736))) * pow(width() , 0.00168424 * enclose() + 0.023153) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 3.13)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.22273 - 1.25209 * distance()) + 0.137561 / pow(distance() , 1.06762)) * 3.56601 * pow((width1() + width2()) / 2 , 0.0953977) * (0.0472559 * (thickness1() + thickness2()) / 2 + 0.0167763) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal6 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.221069 * (1 - exp(-0.900298 * (distance() + 0.116356))) * pow(width() , 0.00118659 * distance() + 0.0277089) * (0.0304495 * thickness() + 0.222769) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.221069 * (1 - exp(-0.900298 * (distance() + 0.116356))) * pow(width() , 0.00118659 * distance() + 0.0277089) * (0.0304495 * thickness() + 0.222769) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0500143 * (1 - exp(-0.8676 * (distance() + 0.0700832))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.180997 * pow(width() , 0.0654374) * (0.0890016 * thickness() + 0.268672) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00946229 * distance() + 0.865204) * (0.0985423 - exp(-(-0.152753 * distance() + 0.297053) * (enclose() + 2.24185 * distance() + 5.4171) - (0.00118597 * distance() + 0.0438845) * (enclose() + 2.21142 * distance() + 3.71393) * (enclose() + 2.21142 * distance() + 3.71393))) + 0.5 * 0.774206 * exp(-0.691624 * enclose()) * (0.11069 - 0.102989 * exp(-0.0192255 * distance()) * pow(distance() , -0.0115724))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00946229 * distance() + 0.865204) * (0.0985423 - exp(-(-0.152753 * distance() + 0.297053) * (enclose() + 2.24185 * distance() + 5.4171) - (0.00118597 * distance() + 0.0438845) * (enclose() + 2.21142 * distance() + 3.71393) * (enclose() + 2.21142 * distance() + 3.71393))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0528226 * (1 - exp(-1.28543 * (enclose() + 0.543572))) * pow(width() , -0.00190156 * enclose() + 0.0641601) + 0.5 * (0.012146 * exp(-1.37122 * enclose()) + 0.0115021 * exp(-0.282845 * enclose())) * pow(width() , 0.0537628 * enclose() + 0.0514716)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0528226 * (1 - exp(-1.28543 * (enclose() + 0.543572))) * pow(width() , -0.00190156 * enclose() + 0.0641601) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal5,metal7
//
CAPACITANCE NEARBODY metal7 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(-1.48969 - 0.605269 * distance()) + 0.155549 / pow(distance() , 0.974587)) * 5.16252 * pow((width1() + width2()) / 2 , 0.119907) * (0.0323014 * (thickness1() + thickness2()) / 2 + 0.016087) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal5,metal7
//
CAPACITANCE CROSSOVER FRINGE metal7 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.191252 * (1 - exp(-0.373419 * (distance() + 0.379341))) * pow(width() , 0.0102182 * distance() + -0.0254865) * (0.0296936 * thickness() + 0.200813) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.191252 * (1 - exp(-0.373419 * (distance() + 0.379341))) * pow(width() , 0.0102182 * distance() + -0.0254865) * (0.0296936 * thickness() + 0.200813) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.038152 * (1 - exp(-0.418917 * (distance() + 0.139281))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.166667 * pow(width() , 0.0731586) * (0.0709367 * thickness() + 0.224207) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00292686 * distance() + 0.227683) * (0.267835 - exp(-(-0.0945618 * distance() + 0.153047) * (enclose() + 4.16585 * distance() + 7.4049) - (7.06846e-05 * distance() + 0.0151223) * (enclose() + 4.82941 * distance() + 2.38713) * (enclose() + 4.82941 * distance() + 2.38713))) + 0.5 * 1.23139 * exp(-0.479314 * enclose()) * (0.109697 - 0.105532 * exp(-0.0130506 * distance()) * pow(distance() , 0.00084358))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00292686 * distance() + 0.227683) * (0.267835 - exp(-(-0.0945618 * distance() + 0.153047) * (enclose() + 4.16585 * distance() + 7.4049) - (7.06846e-05 * distance() + 0.0151223) * (enclose() + 4.82941 * distance() + 2.38713) * (enclose() + 4.82941 * distance() + 2.38713))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0404874 * (1 - exp(-0.657345 * (enclose() + 0.888813))) * pow(width() , -0.00026784 * enclose() + 0.0664996) + 0.5 * (0.0138381 * exp(-0.263159 * enclose()) + 0.00847267 * exp(-0.825834 * enclose())) * pow(width() , 0.0271465 * enclose() + 0.0385083)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0404874 * (1 - exp(-0.657345 * (enclose() + 0.888813))) * pow(width() , -0.00026784 * enclose() + 0.0664996) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal5,metal8
//
CAPACITANCE NEARBODY metal8 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    C = length() * (exp(-1.56269 - 0.425869 * distance()) + 0.178221 / pow(distance() , 1.03534)) * 4.61691 * pow((width1() + width2()) / 2 , 0.0885791) * (0.0285621 * (thickness1() + thickness2()) / 2 + 0.023933) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.31)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal5,metal8
//
CAPACITANCE CROSSOVER FRINGE metal8 metal5 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_calenclose = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    max_enclose = MAXENCLOSE_metal8
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.174788 * (1 - exp(-0.186442 * (distance() + 0.362142))) * pow(width() , 0.0197859 * distance() + -0.118741) * (0.0103079 * thickness() + 0.234145) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.31)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.174788 * (1 - exp(-0.186442 * (distance() + 0.362142))) * pow(width() , 0.0197859 * distance() + -0.118741) * (0.0103079 * thickness() + 0.234145) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.31)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0403031 * (1 - exp(-0.213418 * (distance() + 0.0494183))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.31)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.135523 * pow(width() , 0.0341644) * (0.0483832 * thickness() + 0.244221) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 2.31)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00262432 * distance() + 0.107711) * (0.857 - exp(-(0.0207146 * distance() + 0.507341) * (enclose() + -0.153297 * distance() + 3.26246) - (0.00051267 * distance() + -0.028162) * (enclose() + -0.181175 * distance() + 7.46661) * (enclose() + -0.181175 * distance() + 7.46661))) + 0.5 * 12.1237 * exp(-0.418437 * enclose()) * (0.104915 - 0.104748 * exp(-0.00193637 * distance()) * pow(distance() , 0.00172649))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.31)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00262432 * distance() + 0.107711) * (0.857 - exp(-(0.0207146 * distance() + 0.507341) * (enclose() + -0.153297 * distance() + 3.26246) - (0.00051267 * distance() + -0.028162) * (enclose() + -0.181175 * distance() + 7.46661) * (enclose() + -0.181175 * distance() + 7.46661))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.31)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0377133 * (1 - exp(-0.431932 * (enclose() + 1.16349))) * pow(width() , 0.00117256 * enclose() + 0.0249306) + 0.5 * (0.0106126 * exp(-0.33108 * enclose()) + 0.0106063 * exp(-0.25096 * enclose())) * pow(width() , 0.0131643 * enclose() + 0.00604291)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 2.31)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0377133 * (1 - exp(-0.431932 * (enclose() + 1.16349))) * pow(width() , 0.00117256 * enclose() + 0.0249306) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 2.31)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal6,metal7
//
CAPACITANCE NEARBODY metal7 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(-1.18681 - 1.1258 * distance()) + 0.140035 / pow(distance() , 1.07072)) * 3.51093 * pow((width1() + width2()) / 2 , 0.103199) * (0.0464528 * (thickness1() + thickness2()) / 2 + 0.01749) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal6,metal7
//
CAPACITANCE CROSSOVER FRINGE metal7 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.222791 * (1 - exp(-0.84162 * (distance() + 0.136672))) * pow(width() , 0.00204166 * distance() + 0.0205413) * (0.0248216 * thickness() + 0.227107) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.222791 * (1 - exp(-0.84162 * (distance() + 0.136672))) * pow(width() , 0.00204166 * distance() + 0.0205413) * (0.0248216 * thickness() + 0.227107) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.050983 * (1 - exp(-0.822149 * (distance() + 0.0782935))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.18204 * pow(width() , 0.0640616) * (0.082748 * thickness() + 0.273767) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00059837 * distance() + 0.183599) * (0.499619 - exp(-(-0.130344 * distance() + 0.667488) * (enclose() + 0.967184 * distance() + 0.908082) - (0.0258506 * distance() + -0.00960419) * (enclose() + 0.456532 * distance() + 1.12591) * (enclose() + 0.456532 * distance() + 1.12591))) + 0.5 * 0.758202 * exp(-0.72733 * enclose()) * (0.108817 - 0.102075 * exp(-0.0188315 * distance()) * pow(distance() , -0.00709542))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00059837 * distance() + 0.183599) * (0.499619 - exp(-(-0.130344 * distance() + 0.667488) * (enclose() + 0.967184 * distance() + 0.908082) - (0.0258506 * distance() + -0.00960419) * (enclose() + 0.456532 * distance() + 1.12591) * (enclose() + 0.456532 * distance() + 1.12591))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0539556 * (1 - exp(-1.30306 * (enclose() + 0.548633))) * pow(width() , -0.00163901 * enclose() + 0.0633969) + 0.5 * (0.0111409 * exp(-0.325581 * enclose()) + 0.0100871 * exp(-1.39438 * enclose())) * pow(width() , 0.0560204 * enclose() + 0.0423552)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0539556 * (1 - exp(-1.30306 * (enclose() + 0.548633))) * pow(width() , -0.00163901 * enclose() + 0.0633969) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal6,metal8
//
CAPACITANCE NEARBODY metal8 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    C = length() * (exp(-1.58263 - 0.515971 * distance()) + 0.132961 / pow(distance() , 1.11029)) * 4.56239 * pow((width1() + width2()) / 2 , 0.0801447) * (0.0353069 * (thickness1() + thickness2()) / 2 + 0.0265672) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.49)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal6,metal8
//
CAPACITANCE CROSSOVER FRINGE metal8 metal6 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_calenclose = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    max_enclose = MAXENCLOSE_metal8
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.176056 * (1 - exp(-0.276718 * (distance() + 0.213209))) * pow(width() , 0.0148761 * distance() + -0.0822179) * (0.0138407 * thickness() + 0.251047) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.49)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.176056 * (1 - exp(-0.276718 * (distance() + 0.213209))) * pow(width() , 0.0148761 * distance() + -0.0822179) * (0.0138407 * thickness() + 0.251047) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.49)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0450176 * (1 - exp(-0.300099 * (distance() + 0.0259947))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.49)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.143351 * pow(width() , 0.0310359) * (0.0553294 * thickness() + 0.265904) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 1.49)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.016824 * distance() + 0.455828) * (0.131743 - exp(-(-0.0460638 * distance() + 0.0612545) * (enclose() + 6.71172 * distance() + 20.4367) - (4.63381e-05 * distance() + 0.00933628) * (enclose() + 5.35656 * distance() + 8.16251) * (enclose() + 5.35656 * distance() + 8.16251))) + 0.5 * 14.7857 * exp(-0.513726 * enclose()) * (0.104415 - 0.104324 * exp(-0.00160034 * distance()) * pow(distance() , 0.00119168))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.49)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.016824 * distance() + 0.455828) * (0.131743 - exp(-(-0.0460638 * distance() + 0.0612545) * (enclose() + 6.71172 * distance() + 20.4367) - (4.63381e-05 * distance() + 0.00933628) * (enclose() + 5.35656 * distance() + 8.16251) * (enclose() + 5.35656 * distance() + 8.16251))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.49)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0437883 * (1 - exp(-0.591535 * (enclose() + 0.961765))) * pow(width() , 0.00064213 * enclose() + 0.0255042) + 0.5 * (0.0104278 * exp(-0.495227 * enclose()) + 0.0102937 * exp(-0.283032 * enclose())) * pow(width() , 0.0193357 * enclose() + -0.00034543)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 1.49)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0437883 * (1 - exp(-0.591535 * (enclose() + 0.961765))) * pow(width() , 0.00064213 * enclose() + 0.0255042) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 1.49)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 37  target: nearbody  profile: field  layers: field_base,metal7,metal8
//
CAPACITANCE NEARBODY metal8 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    C = length() * (exp(-1.23659 - 0.678657 * distance()) + 0.136879 / pow(distance() , 1.22779)) * 3.15296 * pow((width1() + width2()) / 2 , 0.0769182) * (0.0447937 * (thickness1() + thickness2()) / 2 + 0.0254643) * (1 - m37RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.67)))
]
//
//  model: 37  target: crossovere  profile: field  layers: field_base,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal8 metal7 MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_calenclose = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    max_enclose = MAXENCLOSE_metal8
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.197504 * (1 - exp(-0.480189 * (distance() + 0.140086))) * pow(width() , 0.010745 * distance() + -0.0571101) * (0.0142514 * thickness() + 0.281031) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.67)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.197504 * (1 - exp(-0.480189 * (distance() + 0.140086))) * pow(width() , 0.010745 * distance() + -0.0571101) * (0.0142514 * thickness() + 0.281031) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.67)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0570801 * (1 - exp(-0.505166 * (distance() + 0.0454752))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.67)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.158831 * pow(width() , 0.0226479) * (0.0643059 * thickness() + 0.314201) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 0.67)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0427075 * distance() + 1.78252) * (0.0508408 - exp(-(-0.0685155 * distance() + 0.0720147) * (enclose() + 6.16016 * distance() + 19.8504) - (0.00023654 * distance() + 0.0161825) * (enclose() + 4.43618 * distance() + 9.39379) * (enclose() + 4.43618 * distance() + 9.39379))) + 0.5 * 6.52691 * exp(-0.696829 * enclose()) * (0.103553 - 0.103351 * exp(-0.00346191 * distance()) * pow(distance() , 0.00159523))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.67)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0427075 * distance() + 1.78252) * (0.0508408 - exp(-(-0.0685155 * distance() + 0.0720147) * (enclose() + 6.16016 * distance() + 19.8504) - (0.00023654 * distance() + 0.0161825) * (enclose() + 4.43618 * distance() + 9.39379) * (enclose() + 4.43618 * distance() + 9.39379))) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.67)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0573644 * (1 - exp(-0.994594 * (enclose() + 0.647948))) * pow(width() , -0.00014182 * enclose() + 0.020924) + 0.5 * (0.0105518 * exp(-0.837047 * enclose()) + 0.00989337 * exp(-0.355213 * enclose())) * pow(width() , 0.0337628 * enclose() + -0.00518372)) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 0.67)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0573644 * (1 - exp(-0.994594 * (enclose() + 0.647948))) * pow(width() , -0.00014182 * enclose() + 0.020924) * (1 - m37RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 0.67)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal2
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-0.911167 - 3.12821 * distance()) + 0.00621236 / pow(distance() , 1.78831)) * 3.78002 * pow((width1() + width2()) / 2 , 0.0132165) * (0.158588 * (thickness1() + thickness2()) / 2 + 0.0251715) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.29)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal2
//
CAPACITANCE CROSSOVER FRINGE metal1 metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.121003 * (1 - exp(-2.15687 * (distance() + -0.0476652))) * pow(width() , 0.00226792 * distance() + -0.0174518) * (0.167516 * thickness() + 0.161154)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0250329 * (1 - exp(-2.25704 * (distance() + -0.0512541)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.127051 * pow(width() , 0) * (0.227489 * thickness() + 0.133795)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00063245 * distance() + 0.0866461) * (0.595425 - exp(-(0.0209918 * distance() + 0.78201) * (enclose() + 1.6951 * distance() + 0.549888) - (0.0186173 * distance() + -0.320948) * (enclose() + -0.978623 * distance() + -0.554255) * (enclose() + -0.978623 * distance() + -0.554255)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.121003 * (1 - exp(-2.15687 * (distance() + -0.0476652))) * pow(width() , 0.00226792 * distance() + -0.0174518) * (0.167516 * thickness() + 0.161154)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0407411 * (0.595425 - exp(-0.953408 * (enclose() + 14.3904) - -0.168938 * (enclose() + -8.54471) * (enclose() + -8.54471)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal2
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.142499 * (1 - exp(-2.25798 * (distance() + -0.0371805))) * pow(width() , 0.001783 * distance() + -0.0167264) * (0.0893807 * thickness() + 0.227352) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.142499 * (1 - exp(-2.25798 * (distance() + -0.0371805))) * pow(width() , 0.001783 * distance() + -0.0167264) * (0.0893807 * thickness() + 0.227352) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0360162 * (1 - exp(-2.34595 * (distance() + -0.041656))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.158518 * pow(width() , -0.00910148) * (0.163983 * thickness() + 0.180445) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0644933 * distance() + 8.19884) * (0.00908293 - exp(-(0.44163 * distance() + 1.41995) * (enclose() + -0.486834 * distance() + 3.24922) - (0.00045536 * distance() + 7.42141e-05) * (enclose() + 5.59338 * distance() + 2.79143) * (enclose() + 5.59338 * distance() + 2.79143))) + 0.5 * 0.413539 * exp(-3.38741 * enclose()) * (0.0511222 - 0.191057 * exp(-2.04351 * distance()) * pow(distance() , 0.560122))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0644933 * distance() + 8.19884) * (0.00908293 - exp(-(0.44163 * distance() + 1.41995) * (enclose() + -0.486834 * distance() + 3.24922) - (0.00045536 * distance() + 7.42141e-05) * (enclose() + 5.59338 * distance() + 2.79143) * (enclose() + 5.59338 * distance() + 2.79143))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.035543 * (1 - exp(-3.10049 * (enclose() + 0.162544))) * pow(width() , 0.00016431 * enclose() + -0.00785188) + 0.5 * (0.0111666 * exp(-3.09899 * enclose()) + 0.00968999 * exp(-2.76967 * enclose())) * pow(width() , 0.0067867 * enclose() + 0.00196048)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.035543 * (1 - exp(-3.10049 * (enclose() + 0.162544))) * pow(width() , 0.00016431 * enclose() + -0.00785188) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal3
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-0.818265 - 2.2448 * distance()) + 0.0205051 / pow(distance() , 1.51718)) * 3.40849 * pow((width1() + width2()) / 2 , 0.0544215) * (0.112175 * (thickness1() + thickness2()) / 2 + 0.0233616) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.29)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal3
//
CAPACITANCE CROSSOVER FRINGE metal1 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0652119 * (1 - exp(-1.40355 * (distance() + -0.00803843))) * pow(width() , 0.00496713 * distance() + -0.0127199) * (0.19671 * thickness() + 0.143837)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0124887 * (1 - exp(-1.52343 * (distance() + -0.0366377)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0954516 * pow(width() , 0.0226953) * (0.181406 * thickness() + 0.0862622)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00082234 * distance() + 0.0432948) * (0.627908 - exp(-(-0.0164198 * distance() + 0.385215) * (enclose() + 2.97183 * distance() + 1.14694) - (-0.0328373 * distance() + -0.00723104) * (enclose() + 0.0136499 * distance() + -4.54113) * (enclose() + 0.0136499 * distance() + -4.54113)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0652119 * (1 - exp(-1.40355 * (distance() + -0.00803843))) * pow(width() , 0.00496713 * distance() + -0.0127199) * (0.19671 * thickness() + 0.143837)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0182902 * (0.627908 - exp(-0.251147 * (enclose() + 25.4119) - -0.275348 * (enclose() + -4.42968) * (enclose() + -4.42968)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal3
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.169076 * (1 - exp(-1.60238 * (distance() + 0.00124822))) * pow(width() , 0.00297968 * distance() + -0.0168917) * (0.0603554 * thickness() + 0.244058) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.169076 * (1 - exp(-1.60238 * (distance() + 0.00124822))) * pow(width() , 0.00297968 * distance() + -0.0168917) * (0.0603554 * thickness() + 0.244058) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0439638 * (1 - exp(-1.68022 * (distance() + -0.0167004))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.164083 * pow(width() , 0.00701997) * (0.142997 * thickness() + 0.231698) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00243332 * distance() + 0.0887972) * (0.693123 - exp(-(-0.600288 * distance() + 0.243045) * (enclose() + 1.14886 * distance() + 0.00694561) - (-0.0209197 * distance() + 0.219258) * (enclose() + 4.27929 * distance() + 0.653191) * (enclose() + 4.27929 * distance() + 0.653191))) + 0.5 * 0.464301 * exp(-2.50341 * enclose()) * (0.0694178 - 0.146099 * exp(-1.21565 * distance()) * pow(distance() , 0.338024))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00243332 * distance() + 0.0887972) * (0.693123 - exp(-(-0.600288 * distance() + 0.243045) * (enclose() + 1.14886 * distance() + 0.00694561) - (-0.0209197 * distance() + 0.219258) * (enclose() + 4.27929 * distance() + 0.653191) * (enclose() + 4.27929 * distance() + 0.653191))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0441314 * (1 - exp(-2.33571 * (enclose() + 0.190745))) * pow(width() , -0.00038969 * enclose() + 0.00930782) + 0.5 * (0.0162621 * exp(-1.51816 * enclose()) + 0.0161517 * exp(-2.74611 * enclose())) * pow(width() , 0.0339253 * enclose() + 0.0204393)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0441314 * (1 - exp(-2.33571 * (enclose() + 0.190745))) * pow(width() , -0.00038969 * enclose() + 0.00930782) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal4
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-0.867305 - 1.98846 * distance()) + 0.0336028 / pow(distance() , 1.38515)) * 3.43547 * pow((width1() + width2()) / 2 , 0.0739161) * (0.098028 * (thickness1() + thickness2()) / 2 + 0.0216039) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.29)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal4
//
CAPACITANCE CROSSOVER FRINGE metal1 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0454299 * (1 - exp(-1.22732 * (distance() + 0.0219424))) * pow(width() , 0.00595331 * distance() + -0.00880141) * (0.210534 * thickness() + 0.138037)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00850227 * (1 - exp(-1.3381 * (distance() + -0.0207337)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0829058 * pow(width() , 0.0393881) * (0.154882 * thickness() + 0.0666212)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (6.86577e-05 * distance() + 0.0274737) * (0.579838 - exp(-(0.032024 * distance() + 0.0406748) * (enclose() + 0.197576 * distance() + 2.17705) - (0.0591805 * distance() + 0.0393006) * (enclose() + -0.289182 * distance() + 2.64024) * (enclose() + -0.289182 * distance() + 2.64024)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0454299 * (1 - exp(-1.22732 * (distance() + 0.0219424))) * pow(width() , 0.00595331 * distance() + -0.00880141) * (0.210534 * thickness() + 0.138037)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0140171 * (0.579838 - exp(-0.302151 * (enclose() + 3.79026) - 0.522509 * (enclose() + 0.279069) * (enclose() + 0.279069)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal4
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.183362 * (1 - exp(-1.44266 * (distance() + 0.0238808))) * pow(width() , 0.00320397 * distance() + -0.0128927) * (0.0549347 * thickness() + 0.245602) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.183362 * (1 - exp(-1.44266 * (distance() + 0.0238808))) * pow(width() , 0.00320397 * distance() + -0.0128927) * (0.0549347 * thickness() + 0.245602) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0474043 * (1 - exp(-1.49964 * (distance() + -0.00116005))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.169083 * pow(width() , 0.0194547) * (0.146061 * thickness() + 0.248398) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0420038 * distance() + 6.29942) * (0.0150254 - exp(-(0.154379 * distance() + 0.503497) * (enclose() + -2.59488 * distance() + 3.37523) - (0.00146016 * distance() + 0.0191596) * (enclose() + 2.79183 * distance() + 11.2765) * (enclose() + 2.79183 * distance() + 11.2765))) + 0.5 * 0.476775 * exp(-2.10547 * enclose()) * (0.0801609 - 0.132324 * exp(-0.927029 * distance()) * pow(distance() , 0.24312))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0420038 * distance() + 6.29942) * (0.0150254 - exp(-(0.154379 * distance() + 0.503497) * (enclose() + -2.59488 * distance() + 3.37523) - (0.00146016 * distance() + 0.0191596) * (enclose() + 2.79183 * distance() + 11.2765) * (enclose() + 2.79183 * distance() + 11.2765))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.048119 * (1 - exp(-2.05559 * (enclose() + 0.205504))) * pow(width() , -0.00107483 * enclose() + 0.0222897) + 0.5 * (0.0211377 * exp(-1.12746 * enclose()) + 0.0182625 * exp(-2.69553 * enclose())) * pow(width() , 0.0520648 * enclose() + 0.0367275)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.048119 * (1 - exp(-2.05559 * (enclose() + 0.205504))) * pow(width() , -0.00107483 * enclose() + 0.0222897) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal5
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-0.917606 - 1.88662 * distance()) + 0.0438003 / pow(distance() , 1.31125)) * 3.47997 * pow((width1() + width2()) / 2 , 0.0825351) * (0.0911435 * (thickness1() + thickness2()) / 2 + 0.020483) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.29)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal5
//
CAPACITANCE CROSSOVER FRINGE metal1 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0349513 * (1 - exp(-1.16833 * (distance() + 0.0395073))) * pow(width() , 0.0058621 * distance() + -0.00219146) * (0.218671 * thickness() + 0.135035)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00646345 * (1 - exp(-1.26284 * (distance() + -0.00927795)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0735062 * pow(width() , 0.0498615) * (0.139803 * thickness() + 0.0566967)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (3.6677e-05 * distance() + 0.0205453) * (0.585029 - exp(-(0.0228188 * distance() + 0.200141) * (enclose() + 0.141901 * distance() + 1.71544) - (0.0378207 * distance() + 0.00376468) * (enclose() + -0.290528 * distance() + 2.64796) * (enclose() + -0.290528 * distance() + 2.64796)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0349513 * (1 - exp(-1.16833 * (distance() + 0.0395073))) * pow(width() , 0.0058621 * distance() + -0.00219146) * (0.218671 * thickness() + 0.135035)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0104224 * (0.585029 - exp(-0.386457 * (enclose() + 2.87406) - 0.312571 * (enclose() + 0.275799) * (enclose() + 0.275799)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal5
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.19106 * (1 - exp(-1.38889 * (distance() + 0.0359797))) * pow(width() , 0.00291853 * distance() + -0.0074774) * (0.0539243 * thickness() + 0.246534) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.19106 * (1 - exp(-1.38889 * (distance() + 0.0359797))) * pow(width() , 0.00291853 * distance() + -0.0074774) * (0.0539243 * thickness() + 0.246534) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0492932 * (1 - exp(-1.42558 * (distance() + 0.00844613))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.171607 * pow(width() , 0.0276648) * (0.151079 * thickness() + 0.257895) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0396534 * distance() + 6.64601) * (0.0146258 - exp(-(0.145608 * distance() + 0.48702) * (enclose() + -2.64709 * distance() + 3.75476) - (0.00133204 * distance() + 0.0171103) * (enclose() + 2.87487 * distance() + 11.688) * (enclose() + 2.87487 * distance() + 11.688))) + 0.5 * 0.476964 * exp(-1.88135 * enclose()) * (0.087753 - 0.127896 * exp(-0.795708 * distance()) * pow(distance() , 0.194168))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0396534 * distance() + 6.64601) * (0.0146258 - exp(-(0.145608 * distance() + 0.48702) * (enclose() + -2.64709 * distance() + 3.75476) - (0.00133204 * distance() + 0.0171103) * (enclose() + 2.87487 * distance() + 11.688) * (enclose() + 2.87487 * distance() + 11.688))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0503471 * (1 - exp(-1.91914 * (enclose() + 0.214293))) * pow(width() , -0.00175545 * enclose() + 0.0313001) + 0.5 * (0.0216237 * exp(-0.877651 * enclose()) + 0.0224544 * exp(-2.48246 * enclose())) * pow(width() , 0.0620209 * enclose() + 0.0480155)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0503471 * (1 - exp(-1.91914 * (enclose() + 0.214293))) * pow(width() , -0.00175545 * enclose() + 0.0313001) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal6
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-0.96201 - 1.84165 * distance()) + 0.0512408 / pow(distance() , 1.2653)) * 3.52003 * pow((width1() + width2()) / 2 , 0.0869298) * (0.0874461 * (thickness1() + thickness2()) / 2 + 0.0198247) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.29)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal6
//
CAPACITANCE CROSSOVER FRINGE metal1 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0283864 * (1 - exp(-1.14286 * (distance() + 0.0507045))) * pow(width() , 0.00561463 * distance() + 0.0044841) * (0.223757 * thickness() + 0.133739)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00521583 * (1 - exp(-1.22602 * (distance() + -0.00127621)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0633614 * pow(width() , 0.0569095) * (0.135523 * thickness() + 0.0528365)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (1.24478e-05 * distance() + 0.0159491) * (0.607295 - exp(-(0.0274646 * distance() + 0.159918) * (enclose() + 0.118374 * distance() + 1.69219) - (0.0198504 * distance() + 0.00646148) * (enclose() + -0.345949 * distance() + 3.3248) * (enclose() + -0.345949 * distance() + 3.3248)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0283864 * (1 - exp(-1.14286 * (distance() + 0.0507045))) * pow(width() , 0.00561463 * distance() + 0.0044841) * (0.223757 * thickness() + 0.133739)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00802537 * (0.607295 - exp(-0.384166 * (enclose() + 2.65871) - 0.16854 * (enclose() + 0.500126) * (enclose() + 0.500126)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal6
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.195751 * (1 - exp(-1.36816 * (distance() + 0.0428283))) * pow(width() , 0.00257168 * distance() + -0.00269447) * (0.0539139 * thickness() + 0.247295) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.195751 * (1 - exp(-1.36816 * (distance() + 0.0428283))) * pow(width() , 0.00257168 * distance() + -0.00269447) * (0.0539139 * thickness() + 0.247295) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0504742 * (1 - exp(-1.38911 * (distance() + 0.0146388))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.173036 * pow(width() , 0.033326) * (0.155684 * thickness() + 0.264153) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0601007 * distance() + 11.6929) * (0.00842787 - exp(-(0.125885 * distance() + 0.45828) * (enclose() + -3.15522 * distance() + 4.16596) - (0.00111012 * distance() + 0.0154604) * (enclose() + 3.12592 * distance() + 13.4785) * (enclose() + 3.12592 * distance() + 13.4785))) + 0.5 * 0.496754 * exp(-1.73926 * enclose()) * (0.0890472 - 0.120616 * exp(-0.725767 * distance()) * pow(distance() , 0.165402))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0601007 * distance() + 11.6929) * (0.00842787 - exp(-(0.125885 * distance() + 0.45828) * (enclose() + -3.15522 * distance() + 4.16596) - (0.00111012 * distance() + 0.0154604) * (enclose() + 3.12592 * distance() + 13.4785) * (enclose() + 3.12592 * distance() + 13.4785))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0517217 * (1 - exp(-1.84183 * (enclose() + 0.219911))) * pow(width() , -0.00234318 * enclose() + 0.0376859) + 0.5 * (0.0217251 * exp(-0.726889 * enclose()) + 0.0257213 * exp(-2.32827 * enclose())) * pow(width() , 0.0647567 * enclose() + 0.0564453)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0517217 * (1 - exp(-1.84183 * (enclose() + 0.219911))) * pow(width() , -0.00234318 * enclose() + 0.0376859) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal7
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-0.992117 - 1.82148 * distance()) + 0.0569298 / pow(distance() , 1.23485)) * 3.54956 * pow((width1() + width2()) / 2 , 0.08944) * (0.0848533 * (thickness1() + thickness2()) / 2 + 0.019327) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.29)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal7
//
CAPACITANCE CROSSOVER FRINGE metal1 metal7 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0238922 * (1 - exp(-1.13488 * (distance() + 0.0570479))) * pow(width() , 0.00524466 * distance() + 0.00964044) * (0.228095 * thickness() + 0.132529)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00437218 * (1 - exp(-1.20632 * (distance() + 0.00431944)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0605929 * pow(width() , 0.0619469) * (0.121625 * thickness() + 0.0462294)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00036929 * distance() + 0.0158031) * (0.633575 - exp(-(0.03793 * distance() + 0.206536) * (enclose() + 0.34522 * distance() + 1.70851) - (0.00045118 * distance() + -0.0108807) * (enclose() + -1.85901 * distance() + 1.36896) * (enclose() + -1.85901 * distance() + 1.36896)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0238922 * (1 - exp(-1.13488 * (distance() + 0.0570479))) * pow(width() , 0.00524466 * distance() + 0.00964044) * (0.228095 * thickness() + 0.132529)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00639392 * (0.633575 - exp(-0.516234 * (enclose() + 4.52723) - -0.00719682 * (enclose() + -13.8099) * (enclose() + -13.8099)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal7
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.198818 * (1 - exp(-1.35966 * (distance() + 0.0470166))) * pow(width() , 0.00228004 * distance() + 0.00115886) * (0.0542722 * thickness() + 0.247999) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.198818 * (1 - exp(-1.35966 * (distance() + 0.0470166))) * pow(width() , 0.00228004 * distance() + 0.00115886) * (0.0542722 * thickness() + 0.247999) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0512789 * (1 - exp(-1.36923 * (distance() + 0.0188015))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.173916 * pow(width() , 0.0374309) * (0.159774 * thickness() + 0.268581) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.047006 * distance() + 9.812) * (0.0101228 - exp(-(0.12579 * distance() + 0.450018) * (enclose() + -3.08483 * distance() + 4.19476) - (0.00114032 * distance() + 0.0155016) * (enclose() + 3.06611 * distance() + 13.0708) * (enclose() + 3.06611 * distance() + 13.0708))) + 0.5 * 0.494225 * exp(-1.64208 * enclose()) * (0.0928378 - 0.119969 * exp(-0.684983 * distance()) * pow(distance() , 0.147024))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.047006 * distance() + 9.812) * (0.0101228 - exp(-(0.12579 * distance() + 0.450018) * (enclose() + -3.08483 * distance() + 4.19476) - (0.00114032 * distance() + 0.0155016) * (enclose() + 3.06611 * distance() + 13.0708) * (enclose() + 3.06611 * distance() + 13.0708))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0526272 * (1 - exp(-1.79396 * (enclose() + 0.223728))) * pow(width() , -0.00283069 * enclose() + 0.0423652) + 0.5 * (0.0280926 * exp(-2.22828 * enclose()) + 0.0218725 * exp(-0.624484 * enclose())) * pow(width() , 0.0684617 * enclose() + 0.062339)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0526272 * (1 - exp(-1.79396 * (enclose() + 0.223728))) * pow(width() , -0.00283069 * enclose() + 0.0423652) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal1,metal8
//
CAPACITANCE NEARBODY metal1 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.01284 - 1.81236 * distance()) + 0.0621989 / pow(distance() , 1.21041)) * 3.57224 * pow((width1() + width2()) / 2 , 0.0912194) * (0.0824856 * (thickness1() + thickness2()) / 2 + 0.0188425) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.29)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal1,metal8
//
CAPACITANCE CROSSOVER FRINGE metal1 metal8 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.020098 * (1 - exp(-1.13403 * (distance() + 0.0607827))) * pow(width() , 0.00485767 * distance() + 0.0123579) * (0.231943 * thickness() + 0.130897)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00366451 * (1 - exp(-1.1935 * (distance() + 0.00831723)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.055422 * pow(width() , 0.065366) * (0.114202 * thickness() + 0.042107)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-5.28713e-05 * distance() + 0.0110779) * (0.632054 - exp(-(0.0237786 * distance() + 0.162876) * (enclose() + 0.12195 * distance() + 1.92681) - (0.00680237 * distance() + 0.00010284) * (enclose() + -0.430518 * distance() + 4.23213) * (enclose() + -0.430518 * distance() + 4.23213)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.020098 * (1 - exp(-1.13403 * (distance() + 0.0607827))) * pow(width() , 0.00485767 * distance() + 0.0123579) * (0.231943 * thickness() + 0.130897)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0053231 * (0.632054 - exp(-0.357028 * (enclose() + 2.92253) - 0.0556442 * (enclose() + 0.716951) * (enclose() + 0.716951)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal1,metal8
//
CAPACITANCE CROSSOVER FRINGE metal1 poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.201288 * (1 - exp(-1.35584 * (distance() + 0.0501779))) * pow(width() , 0.00201884 * distance() + 0.00473106) * (0.0548612 * thickness() + 0.24875) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.201288 * (1 - exp(-1.35584 * (distance() + 0.0501779))) * pow(width() , 0.00201884 * distance() + 0.00473106) * (0.0548612 * thickness() + 0.24875) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0519544 * (1 - exp(-1.35598 * (distance() + 0.0221576))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.174767 * pow(width() , 0.0410455) * (0.162941 * thickness() + 0.272348) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0345638 * distance() + 7.63991) * (0.0130493 - exp(-(0.123902 * distance() + 0.429642) * (enclose() + -3.07156 * distance() + 4.12661) - (0.0012476 * distance() + 0.0166187) * (enclose() + 2.91782 * distance() + 12.2886) * (enclose() + 2.91782 * distance() + 12.2886))) + 0.5 * 0.496102 * exp(-1.55846 * enclose()) * (0.095284 - 0.11862 * exp(-0.655561 * distance()) * pow(distance() , 0.132595))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0345638 * distance() + 7.63991) * (0.0130493 - exp(-(0.123902 * distance() + 0.429642) * (enclose() + -3.07156 * distance() + 4.12661) - (0.0012476 * distance() + 0.0166187) * (enclose() + 2.91782 * distance() + 12.2886) * (enclose() + 2.91782 * distance() + 12.2886))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0533427 * (1 - exp(-1.75777 * (enclose() + 0.226782))) * pow(width() , -0.00328155 * enclose() + 0.0462922) + 0.5 * (0.021711 * exp(-0.538003 * enclose()) + 0.0305614 * exp(-2.12478 * enclose())) * pow(width() , 0.0690271 * enclose() + 0.0681197)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0533427 * (1 - exp(-1.75777 * (enclose() + 0.226782))) * pow(width() , -0.00328155 * enclose() + 0.0462922) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.29)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal2,metal3
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-0.877046 - 1.98295 * distance()) + 0.0191653 / pow(distance() , 1.58942)) * 3.50369 * pow((width1() + width2()) / 2 , 0.0344336) * (0.088867 * (thickness1() + thickness2()) / 2 + 0.0267103) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal2 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.168486 * (1 - exp(-1.33298 * (distance() + -0.0252173))) * pow(width() , 0.00261183 * distance() + -0.0140849) * (0.0788925 * thickness() + 0.180927)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0353193 * (1 - exp(-1.39268 * (distance() + -0.0392522)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.144978 * pow(width() , 0.00365743) * (0.122385 * thickness() + 0.197499)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-5.93602e-06 * distance() + 0.0943988) * (0.734669 - exp(-(0.00776288 * distance() + 2.47897) * (enclose() + -0.0464665 * distance() + 0.684901) - (0.0993908 * distance() + -0.459694) * (enclose() + -0.283323 * distance() + 1.81802) * (enclose() + -0.283323 * distance() + 1.81802)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.168486 * (1 - exp(-1.33298 * (distance() + -0.0252173))) * pow(width() , 0.00261183 * distance() + -0.0140849) * (0.0788925 * thickness() + 0.180927)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0471697 * (0.734669 - exp(-2.5566 * (enclose() + 0.220236) - 0.534214 * (enclose() + -1.01521) * (enclose() + -1.01521)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal2 poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0930574 * (1 - exp(-1.21638 * (distance() + -0.0272134))) * pow(width() , 0.00377526 * distance() + -0.0133338) * (0.098291 * thickness() + 0.167349) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0930574 * (1 - exp(-1.21638 * (distance() + -0.0272134))) * pow(width() , 0.00377526 * distance() + -0.0133338) * (0.098291 * thickness() + 0.167349) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0187445 * (1 - exp(-1.30134 * (distance() + -0.050172))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.114183 * pow(width() , 0.0125904) * (0.107814 * thickness() + 0.124856) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00053154 * distance() + 0.0544149) * (0.72353 - exp(-(-0.777123 * distance() + 0.432496) * (enclose() + 1.35859 * distance() + 0.313005) - (0.00125914 * distance() + 0.199008) * (enclose() + 2.18651 * distance() + 0.163093) * (enclose() + 2.18651 * distance() + 0.163093))) + 0.5 * 0.27454 * exp(-1.5527 * enclose()) * (0.0708805 - 0.157773 * exp(-1.18733 * distance()) * pow(distance() , 0.427395))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00053154 * distance() + 0.0544149) * (0.72353 - exp(-(-0.777123 * distance() + 0.432496) * (enclose() + 1.35859 * distance() + 0.313005) - (0.00125914 * distance() + 0.199008) * (enclose() + 2.18651 * distance() + 0.163093) * (enclose() + 2.18651 * distance() + 0.163093))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0189001 * (1 - exp(-1.42548 * (enclose() + 0.203012))) * pow(width() , 0.00117383 * enclose() + 0.00641895) + 0.5 * (0.00963572 * exp(-1.46823 * enclose()) + 0.0106807 * exp(-1.46492 * enclose())) * pow(width() , -0.00797523 * enclose() + 0.0410681)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0189001 * (1 - exp(-1.42548 * (enclose() + 0.203012))) * pow(width() , 0.00117383 * enclose() + 0.00641895) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal2,metal4
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-0.887019 - 1.41472 * distance()) + 0.0366201 / pow(distance() , 1.41332)) * 3.3118 * pow((width1() + width2()) / 2 , 0.0604404) * (0.0723093 * (thickness1() + thickness2()) / 2 + 0.0280855) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal2 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.102834 * (1 - exp(-0.787128 * (distance() + 0.0122507))) * pow(width() , 0.00687624 * distance() + -0.0269358) * (0.093359 * thickness() + 0.16638)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0202417 * (1 - exp(-0.875486 * (distance() + -0.0397703)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.11794 * pow(width() , 0.0217318) * (0.10123 * thickness() + 0.135525)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0007609 * distance() + 0.0935527) * (0.444132 - exp(-(-0.213674 * distance() + 0.182452) * (enclose() + 3.77065 * distance() + 3.2326) - (0.00021827 * distance() + 0.0420726) * (enclose() + 4.16334 * distance() + 1.27223) * (enclose() + 4.16334 * distance() + 1.27223)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.102834 * (1 - exp(-0.787128 * (distance() + 0.0122507))) * pow(width() , 0.00687624 * distance() + -0.0269358) * (0.093359 * thickness() + 0.16638)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0429718 * (0.444132 - exp(1.95429 * (enclose() + 40.9391) - 0.0442553 * (enclose() + 42.9056) * (enclose() + 42.9056)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal2 poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.121882 * (1 - exp(-0.801435 * (distance() + 0.0149644))) * pow(width() , 0.00658923 * distance() + -0.0272439) * (0.0602646 * thickness() + 0.187544) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.121882 * (1 - exp(-0.801435 * (distance() + 0.0149644))) * pow(width() , 0.00658923 * distance() + -0.0272439) * (0.0602646 * thickness() + 0.187544) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0251151 * (1 - exp(-0.88816 * (distance() + -0.0353254))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.131065 * pow(width() , 0.0195062) * (0.0766734 * thickness() + 0.164532) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00137781 * distance() + 0.121041) * (0.439576 - exp(-(-0.22715 * distance() + 0.234499) * (enclose() + 2.23278 * distance() + 2.46286) - (0.00061048 * distance() + 0.056981) * (enclose() + 2.72485 * distance() + 1.35112) * (enclose() + 2.72485 * distance() + 1.35112))) + 0.5 * 0.348169 * exp(-1.27887 * enclose()) * (0.0828467 - 0.139228 * exp(-0.794233 * distance()) * pow(distance() , 0.298134))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00137781 * distance() + 0.121041) * (0.439576 - exp(-(-0.22715 * distance() + 0.234499) * (enclose() + 2.23278 * distance() + 2.46286) - (0.00061048 * distance() + 0.056981) * (enclose() + 2.72485 * distance() + 1.35112) * (enclose() + 2.72485 * distance() + 1.35112))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0253675 * (1 - exp(-1.1228 * (enclose() + 0.240066))) * pow(width() , 0.00084551 * enclose() + 0.0151058) + 0.5 * (0.0163531 * exp(-1.11337 * enclose()) + 0.0128026 * exp(-1.11392 * enclose())) * pow(width() , 0.00117024 * enclose() + 0.0345258)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0253675 * (1 - exp(-1.1228 * (enclose() + 0.240066))) * pow(width() , 0.00084551 * enclose() + 0.0151058) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal2,metal5
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-0.920925 - 1.1937 * distance()) + 0.0547239 / pow(distance() , 1.30673)) * 3.2333 * pow((width1() + width2()) / 2 , 0.0778734) * (0.0646617 * (thickness1() + thickness2()) / 2 + 0.0268517) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal2 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0767006 * (1 - exp(-0.632196 * (distance() + 0.0635346))) * pow(width() , 0.00938608 * distance() + -0.0356041) * (0.0998776 * thickness() + 0.161814)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0147502 * (1 - exp(-0.726345 * (distance() + -0.0247551)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.104767 * pow(width() , 0.0345486) * (0.0890512 * thickness() + 0.110512)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00041446 * distance() + 0.0443798) * (0.680411 - exp(-(-0.215918 * distance() + 0.170299) * (enclose() + 3.31438 * distance() + 1.53557) - (6.26575e-05 * distance() + 0.0378519) * (enclose() + 4.25006 * distance() + 0.133893) * (enclose() + 4.25006 * distance() + 0.133893)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0767006 * (1 - exp(-0.632196 * (distance() + 0.0635346))) * pow(width() , 0.00938608 * distance() + -0.0356041) * (0.0998776 * thickness() + 0.161814)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0201176 * (0.680411 - exp(1.98888 * (enclose() + 34.6794) - 0.0384785 * (enclose() + 42.6345) * (enclose() + 42.6345)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal2 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.139075 * (1 - exp(-0.664331 * (distance() + 0.0605514))) * pow(width() , 0.00830597 * distance() + -0.0337806) * (0.0484351 * thickness() + 0.193572) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.139075 * (1 - exp(-0.664331 * (distance() + 0.0605514))) * pow(width() , 0.00830597 * distance() + -0.0337806) * (0.0484351 * thickness() + 0.193572) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.028652 * (1 - exp(-0.75108 * (distance() + -0.0174103))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.139369 * pow(width() , 0.0287121) * (0.0690922 * thickness() + 0.183228) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00217566 * distance() + 0.213364) * (0.279647 - exp(-(-0.19664 * distance() + 0.227467) * (enclose() + 3.26869 * distance() + 4.57122) - (0.00023015 * distance() + 0.0331845) * (enclose() + 4.14141 * distance() + 1.81557) * (enclose() + 4.14141 * distance() + 1.81557))) + 0.5 * 0.381381 * exp(-1.11716 * enclose()) * (0.091197 - 0.131247 * exp(-0.620437 * distance()) * pow(distance() , 0.229752))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00217566 * distance() + 0.213364) * (0.279647 - exp(-(-0.19664 * distance() + 0.227467) * (enclose() + 3.26869 * distance() + 4.57122) - (0.00023015 * distance() + 0.0331845) * (enclose() + 4.14141 * distance() + 1.81557) * (enclose() + 4.14141 * distance() + 1.81557))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0290884 * (1 - exp(-0.978911 * (enclose() + 0.259859))) * pow(width() , 0.00044553 * enclose() + 0.0253904) + 0.5 * (0.017583 * exp(-1.01984 * enclose()) + 0.0175417 * exp(-0.863912 * enclose())) * pow(width() , 0.00854317 * enclose() + 0.0405329)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0290884 * (1 - exp(-0.978911 * (enclose() + 0.259859))) * pow(width() , 0.00044553 * enclose() + 0.0253904) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal2,metal6
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-0.937174 - 1.07708 * distance()) + 0.0723312 / pow(distance() , 1.2396)) * 3.20248 * pow((width1() + width2()) / 2 , 0.0868036) * (0.0587122 * (thickness1() + thickness2()) / 2 + 0.0250422) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal2 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.061388 * (1 - exp(-0.565213 * (distance() + 0.106551))) * pow(width() , 0.0102997 * distance() + -0.0362127) * (0.104244 * thickness() + 0.159994)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0116981 * (1 - exp(-0.654541 * (distance() + -0.00536881)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0959187 * pow(width() , 0.0436929) * (0.0829508 * thickness() + 0.0947519)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-3.06933e-05 * distance() + 0.0330133) * (0.666554 - exp(-(0.0164321 * distance() + 0.242889) * (enclose() + 0.0453382 * distance() + 1.7828) - (0.0179877 * distance() + -0.016066) * (enclose() + -0.215277 * distance() + 2.96973) * (enclose() + -0.215277 * distance() + 2.96973)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.061388 * (1 - exp(-0.565213 * (distance() + 0.106551))) * pow(width() , 0.0102997 * distance() + -0.0362127) * (0.104244 * thickness() + 0.159994)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0163532 * (0.666554 - exp(-0.40721 * (enclose() + 2.23618) - 0.163811 * (enclose() + 0.81696) * (enclose() + 0.81696)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal2 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.150442 * (1 - exp(-0.601795 * (distance() + 0.0978648))) * pow(width() , 0.0089355 * distance() + -0.0341069) * (0.0430539 * thickness() + 0.196179) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.150442 * (1 - exp(-0.601795 * (distance() + 0.0978648))) * pow(width() , 0.0089355 * distance() + -0.0341069) * (0.0430539 * thickness() + 0.196179) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0309054 * (1 - exp(-0.683545 * (distance() + 0.00014128))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.144276 * pow(width() , 0.036062) * (0.0672895 * thickness() + 0.194589) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00299579 * distance() + 0.330821) * (0.191433 - exp(-(-0.168732 * distance() + 0.211877) * (enclose() + 3.97426 * distance() + 6.56711) - (0.00014601 * distance() + 0.0240678) * (enclose() + 5.00007 * distance() + 2.44848) * (enclose() + 5.00007 * distance() + 2.44848))) + 0.5 * 0.419441 * exp(-1.01854 * enclose()) * (0.0928576 - 0.121931 * exp(-0.529242 * distance()) * pow(distance() , 0.189231))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00299579 * distance() + 0.330821) * (0.191433 - exp(-(-0.168732 * distance() + 0.211877) * (enclose() + 3.97426 * distance() + 6.56711) - (0.00014601 * distance() + 0.0240678) * (enclose() + 5.00007 * distance() + 2.44848) * (enclose() + 5.00007 * distance() + 2.44848))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0315081 * (1 - exp(-0.894749 * (enclose() + 0.273705))) * pow(width() , 4.93756e-05 * enclose() + 0.0336049) + 0.5 * (0.00888601 * exp(-0.548679 * enclose()) + 0.0305996 * exp(-0.943715 * enclose())) * pow(width() , 0.0133425 * enclose() + 0.0469939)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0315081 * (1 - exp(-0.894749 * (enclose() + 0.273705))) * pow(width() , 4.93756e-05 * enclose() + 0.0336049) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal2,metal7
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-0.964964 - 1.00788 * distance()) + 0.0870473 / pow(distance() , 1.1932)) * 3.18874 * pow((width1() + width2()) / 2 , 0.0917577) * (0.0552448 * (thickness1() + thickness2()) / 2 + 0.0238753) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal2,metal7
//
CAPACITANCE CROSSOVER FRINGE metal2 metal7 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0512698 * (1 - exp(-0.531502 * (distance() + 0.139383))) * pow(width() , 0.0104593 * distance() + -0.0333969) * (0.107201 * thickness() + 0.15873)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0097111 * (1 - exp(-0.616232 * (distance() + 0.00879258)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.089873 * pow(width() , 0.0504291) * (0.0750599 * thickness() + 0.0843978)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0001387 * distance() + 0.0275615) * (0.681167 - exp(-(0.0291608 * distance() + 0.209839) * (enclose() + -0.0235556 * distance() + 1.99127) - (0.00899796 * distance() + -0.0120461) * (enclose() + -0.214266 * distance() + 3.33663) * (enclose() + -0.214266 * distance() + 3.33663)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0512698 * (1 - exp(-0.531502 * (distance() + 0.139383))) * pow(width() , 0.0104593 * distance() + -0.0333969) * (0.107201 * thickness() + 0.15873)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0130872 * (0.681167 - exp(-0.501447 * (enclose() + 1.75571) - 0.0779335 * (enclose() + 1.19397) * (enclose() + 1.19397)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal2,metal7
//
CAPACITANCE CROSSOVER FRINGE metal2 poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.158218 * (1 - exp(-0.569488 * (distance() + 0.125998))) * pow(width() , 0.00902287 * distance() + -0.0317617) * (0.040206 * thickness() + 0.197672) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.158218 * (1 - exp(-0.569488 * (distance() + 0.125998))) * pow(width() , 0.00902287 * distance() + -0.0317617) * (0.040206 * thickness() + 0.197672) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0324504 * (1 - exp(-0.644957 * (distance() + 0.0153251))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.147577 * pow(width() , 0.0416904) * (0.0675066 * thickness() + 0.202165) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00148583 * distance() + 0.198381) * (0.327836 - exp(-(-0.138464 * distance() + 0.208566) * (enclose() + 2.67566 * distance() + 4.30959) - (0.00035879 * distance() + 0.0316217) * (enclose() + 3.09995 * distance() + 1.79708) * (enclose() + 3.09995 * distance() + 1.79708))) + 0.5 * 0.43903 * exp(-0.94677 * enclose()) * (0.0953738 - 0.118147 * exp(-0.477191 * distance()) * pow(distance() , 0.163593))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00148583 * distance() + 0.198381) * (0.327836 - exp(-(-0.138464 * distance() + 0.208566) * (enclose() + 2.67566 * distance() + 4.30959) - (0.00035879 * distance() + 0.0316217) * (enclose() + 3.09995 * distance() + 1.79708) * (enclose() + 3.09995 * distance() + 1.79708))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0332035 * (1 - exp(-0.841068 * (enclose() + 0.28366))) * pow(width() , -0.00042996 * enclose() + 0.0412687) + 0.5 * (0.0261444 * exp(-0.982218 * enclose()) + 0.0166706 * exp(-0.521173 * enclose())) * pow(width() , 0.0163324 * enclose() + 0.0525644)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0332035 * (1 - exp(-0.841068 * (enclose() + 0.28366))) * pow(width() , -0.00042996 * enclose() + 0.0412687) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal2,metal8
//
CAPACITANCE NEARBODY metal2 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-0.981193 - 0.95725 * distance()) + 0.103051 / pow(distance() , 1.15432)) * 3.17218 * pow((width1() + width2()) / 2 , 0.0951718) * (0.0518761 * (thickness1() + thickness2()) / 2 + 0.0226019) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal2,metal8
//
CAPACITANCE CROSSOVER FRINGE metal2 metal8 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0428794 * (1 - exp(-0.510628 * (distance() + 0.166963))) * pow(width() , 0.0103271 * distance() + -0.0297427) * (0.109702 * thickness() + 0.157478)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00808368 * (1 - exp(-0.588245 * (distance() + 0.0241842)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0840127 * pow(width() , 0.05592) * (0.0694595 * thickness() + 0.0750665)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00041939 * distance() + 0.0238117) * (0.743913 - exp(-(0.0564735 * distance() + 0.115117) * (enclose() + -0.0617234 * distance() + 2.08577) - (0.00016727 * distance() + -0.00276851) * (enclose() + -0.25337 * distance() + 3.17981) * (enclose() + -0.25337 * distance() + 3.17981)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0428794 * (1 - exp(-0.510628 * (distance() + 0.166963))) * pow(width() , 0.0103271 * distance() + -0.0297427) * (0.109702 * thickness() + 0.157478)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0098089 * (0.743913 - exp(-0.679852 * (enclose() + 1.46854) - -0.00109581 * (enclose() + 0.64611) * (enclose() + 0.64611)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal2,metal8
//
CAPACITANCE CROSSOVER FRINGE metal2 poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.164178 * (1 - exp(-0.549593 * (distance() + 0.149854))) * pow(width() , 0.00883589 * distance() + -0.0279008) * (0.0385195 * thickness() + 0.199287) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.164178 * (1 - exp(-0.549593 * (distance() + 0.149854))) * pow(width() , 0.00883589 * distance() + -0.0279008) * (0.0385195 * thickness() + 0.199287) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0337392 * (1 - exp(-0.617718 * (distance() + 0.0298373))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.150337 * pow(width() , 0.0468436) * (0.0679875 * thickness() + 0.208715) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00295705 * distance() + 0.440923) * (0.151805 - exp(-(-0.149168 * distance() + 0.209519) * (enclose() + 4.27469 * distance() + 7.78995) - (0.00011419 * distance() + 0.0194724) * (enclose() + 5.42691 * distance() + 2.7022) * (enclose() + 5.42691 * distance() + 2.7022))) + 0.5 * 0.448902 * exp(-0.885464 * enclose()) * (0.098739 - 0.116926 * exp(-0.441224 * distance()) * pow(distance() , 0.144022))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00295705 * distance() + 0.440923) * (0.151805 - exp(-(-0.149168 * distance() + 0.209519) * (enclose() + 4.27469 * distance() + 7.78995) - (0.00011419 * distance() + 0.0194724) * (enclose() + 5.42691 * distance() + 2.7022) * (enclose() + 5.42691 * distance() + 2.7022))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0346169 * (1 - exp(-0.797038 * (enclose() + 0.293099))) * pow(width() , -0.00079743 * enclose() + 0.0471458) + 0.5 * (0.025535 * exp(-0.996718 * enclose()) + 0.0203411 * exp(-0.464348 * enclose())) * pow(width() , 0.0185889 * enclose() + 0.0579378)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0346169 * (1 - exp(-0.797038 * (enclose() + 0.293099))) * pow(width() , -0.00079743 * enclose() + 0.0471458) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal3,metal4
//
CAPACITANCE NEARBODY metal3 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.910053 - 1.64436 * distance()) + 0.0344283 / pow(distance() , 1.43001)) * 3.42336 * pow((width1() + width2()) / 2 , 0.0603287) * (0.0754623 * (thickness1() + thickness2()) / 2 + 0.0253929) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.82)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.186164 * (1 - exp(-1.10438 * (distance() + 0.00990195))) * pow(width() , 0.00357461 * distance() + -0.0146595) * (0.0659619 * thickness() + 0.191098)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0398033 * (1 - exp(-1.15952 * (distance() + -0.0189031)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.154133 * pow(width() , 0.015771) * (0.109252 * thickness() + 0.22036)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00026195 * distance() + 0.0908108) * (0.836654 - exp(-(0.0979171 * distance() + 1.33094) * (enclose() + -0.0734838 * distance() + 0.908289) - (0.0248485 * distance() + -0.0932552) * (enclose() + -0.535995 * distance() + 3.50564) * (enclose() + -0.535995 * distance() + 3.50564)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.186164 * (1 - exp(-1.10438 * (distance() + 0.00990195))) * pow(width() , 0.00357461 * distance() + -0.0146595) * (0.0659619 * thickness() + 0.191098)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0467151 * (0.836654 - exp(-2.31011 * (enclose() + 0.173451) - 0.15523 * (enclose() + -1.85431) * (enclose() + -1.85431)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0650803 * (1 - exp(-0.952468 * (distance() + 0.0179314))) * pow(width() , 0.00596423 * distance() + -0.0135233) * (0.11879 * thickness() + 0.152616) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0650803 * (1 - exp(-0.952468 * (distance() + 0.0179314))) * pow(width() , 0.00596423 * distance() + -0.0135233) * (0.11879 * thickness() + 0.152616) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0124616 * (1 - exp(-1.04694 * (distance() + -0.0326247))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0970363 * pow(width() , 0.0335174) * (0.100459 * thickness() + 0.0943363) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00107323 * distance() + 0.0505999) * (0.574619 - exp(-(0.138363 * distance() + 0.181008) * (enclose() + -0.11001 * distance() + 1.71393) - (-0.0106592 * distance() + 0.0348584) * (enclose() + 0.0413558 * distance() + 1.79096) * (enclose() + 0.0413558 * distance() + 1.79096))) + 0.5 * 0.182011 * exp(-0.984137 * enclose()) * (0.0889548 - 0.125994 * exp(-0.743335 * distance()) * pow(distance() , 0.257875))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00107323 * distance() + 0.0505999) * (0.574619 - exp(-(0.138363 * distance() + 0.181008) * (enclose() + -0.11001 * distance() + 1.71393) - (-0.0106592 * distance() + 0.0348584) * (enclose() + 0.0413558 * distance() + 1.79096) * (enclose() + 0.0413558 * distance() + 1.79096))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0128451 * (1 - exp(-0.893023 * (enclose() + 0.18146))) * pow(width() , 0.00330356 * enclose() + 0.0104113) + 0.5 * (0.00911573 * exp(-0.960946 * enclose()) + 0.00911837 * exp(-0.961772 * enclose())) * pow(width() , -0.0107121 * enclose() + 0.0969696)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0128451 * (1 - exp(-0.893023 * (enclose() + 0.18146))) * pow(width() , 0.00330356 * enclose() + 0.0104113) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal3,metal5
//
CAPACITANCE NEARBODY metal3 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.909698 - 1.13765 * distance()) + 0.0562958 / pow(distance() , 1.30137)) * 3.19196 * pow((width1() + width2()) / 2 , 0.0809825) * (0.0636095 * (thickness1() + thickness2()) / 2 + 0.0274598) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.82)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.122215 * (1 - exp(-0.608024 * (distance() + 0.0636367))) * pow(width() , 0.00952949 * distance() + -0.040174) * (0.0713178 * thickness() + 0.178691)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0243046 * (1 - exp(-0.700758 * (distance() + -0.0241962)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.129026 * pow(width() , 0.0301213) * (0.0842203 * thickness() + 0.159873)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00042519 * distance() + 0.090565) * (0.529826 - exp(-(-0.100684 * distance() + 0.126117) * (enclose() + 3.07821 * distance() + 3.23429) - (0.00042623 * distance() + 0.0416461) * (enclose() + 2.45412 * distance() + 1.53025) * (enclose() + 2.45412 * distance() + 1.53025)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.122215 * (1 - exp(-0.608024 * (distance() + 0.0636367))) * pow(width() , 0.00952949 * distance() + -0.040174) * (0.0713178 * thickness() + 0.178691)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0431566 * (0.529826 - exp(0.880723 * (enclose() + 34.0164) - 0.0459084 * (enclose() + 26.0714) * (enclose() + 26.0714)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0896121 * (1 - exp(-0.590942 * (distance() + 0.0731308))) * pow(width() , 0.0102269 * distance() + -0.0407077) * (0.0728248 * thickness() + 0.17828) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0896121 * (1 - exp(-0.590942 * (distance() + 0.0731308))) * pow(width() , 0.0102269 * distance() + -0.0407077) * (0.0728248 * thickness() + 0.17828) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0177416 * (1 - exp(-0.689288 * (distance() + -0.0237073))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.114679 * pow(width() , 0.034684) * (0.0718534 * thickness() + 0.130799) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0007317 * distance() + 0.0502236) * (0.760723 - exp(-(-0.317153 * distance() + 0.2011) * (enclose() + 2.4791 * distance() + 0.785346) - (0.00016768 * distance() + 0.0499988) * (enclose() + 3.84731 * distance() + -0.0182719) * (enclose() + 3.84731 * distance() + -0.0182719))) + 0.5 * 0.261201 * exp(-0.845361 * enclose()) * (0.0934724 - 0.119938 * exp(-0.534239 * distance()) * pow(distance() , 0.200312))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0007317 * distance() + 0.0502236) * (0.760723 - exp(-(-0.317153 * distance() + 0.2011) * (enclose() + 2.4791 * distance() + 0.785346) - (0.00016768 * distance() + 0.0499988) * (enclose() + 3.84731 * distance() + -0.0182719) * (enclose() + 3.84731 * distance() + -0.0182719))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0181912 * (1 - exp(-0.737462 * (enclose() + 0.234264))) * pow(width() , 0.00266778 * enclose() + 0.0169228) + 0.5 * (0.0128019 * exp(-0.775343 * enclose()) + 0.0128022 * exp(-0.776899 * enclose())) * pow(width() , -0.00472115 * enclose() + 0.0682655)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0181912 * (1 - exp(-0.737462 * (enclose() + 0.234264))) * pow(width() , 0.00266778 * enclose() + 0.0169228) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal3,metal6
//
CAPACITANCE NEARBODY metal3 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.889545 - 0.93002 * distance()) + 0.0813299 / pow(distance() , 1.22259)) * 3.08085 * pow((width1() + width2()) / 2 , 0.096243) * (0.0552238 * (thickness1() + thickness2()) / 2 + 0.0255329) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.82)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0954628 * (1 - exp(-0.466081 * (distance() + 0.13186))) * pow(width() , 0.0128826 * distance() + -0.0571884) * (0.0727835 * thickness() + 0.174972)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0183734 * (1 - exp(-0.563734 * (distance() + -0.00932782)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.116351 * pow(width() , 0.0400807) * (0.0734193 * thickness() + 0.133791)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (4.4657e-05 * distance() + 0.0574497) * (0.591981 - exp(-(0.0198019 * distance() + 0.475191) * (enclose() + -0.00048763 * distance() + 1.70566) - (0.0213189 * distance() + -0.0589504) * (enclose() + -0.226727 * distance() + 2.57913) * (enclose() + -0.226727 * distance() + 2.57913)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0954628 * (1 - exp(-0.466081 * (distance() + 0.13186))) * pow(width() , 0.0128826 * distance() + -0.0571884) * (0.0727835 * thickness() + 0.174972)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0289481 * (0.591981 - exp(-0.67321 * (enclose() + 1.70078) - 0.154239 * (enclose() + 0.31186) * (enclose() + 0.31186)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.106967 * (1 - exp(-0.467384 * (distance() + 0.136028))) * pow(width() , 0.0128148 * distance() + -0.055899) * (0.0558586 * thickness() + 0.185345) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.106967 * (1 - exp(-0.467384 * (distance() + 0.136028))) * pow(width() , 0.0128148 * distance() + -0.055899) * (0.0558586 * thickness() + 0.185345) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0209917 * (1 - exp(-0.565814 * (distance() + -0.00590738))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.123317 * pow(width() , 0.040801) * (0.0638267 * thickness() + 0.150057) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00100581 * distance() + 0.0691783) * (0.647953 - exp(-(-0.23523 * distance() + 0.187429) * (enclose() + 3.67769 * distance() + 1.78544) - (6.5153e-05 * distance() + 0.0270792) * (enclose() + 5.52692 * distance() + -0.207928) * (enclose() + 5.52692 * distance() + -0.207928))) + 0.5 * 0.310553 * exp(-0.762106 * enclose()) * (0.0975415 - 0.114656 * exp(-0.419175 * distance()) * pow(distance() , 0.157544))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00100581 * distance() + 0.0691783) * (0.647953 - exp(-(-0.23523 * distance() + 0.187429) * (enclose() + 3.67769 * distance() + 1.78544) - (6.5153e-05 * distance() + 0.0270792) * (enclose() + 5.52692 * distance() + -0.207928) * (enclose() + 5.52692 * distance() + -0.207928))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.021544 * (1 - exp(-0.65135 * (enclose() + 0.258896))) * pow(width() , 0.00221517 * enclose() + 0.0254472) + 0.5 * (0.0154285 * exp(-0.670203 * enclose()) + 0.0154286 * exp(-0.671413 * enclose())) * pow(width() , -0.00071159 * enclose() + 0.0646856)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.021544 * (1 - exp(-0.65135 * (enclose() + 0.258896))) * pow(width() , 0.00221517 * enclose() + 0.0254472) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal3,metal7
//
CAPACITANCE NEARBODY metal3 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.23209 - 0.817667 * distance()) + 0.0742562 / pow(distance() , 1.16841)) * 4.29874 * pow((width1() + width2()) / 2 , 0.104236) * (0.0496624 * (thickness1() + thickness2()) / 2 + 0.023624) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.82)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal3,metal7
//
CAPACITANCE CROSSOVER FRINGE metal3 metal7 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.078905 * (1 - exp(-0.402519 * (distance() + 0.19298))) * pow(width() , 0.0141987 * distance() + -0.06262) * (0.0739451 * thickness() + 0.173572)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.014937 * (1 - exp(-0.496825 * (distance() + 0.0109427)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.107245 * pow(width() , 0.0474237) * (0.0675501 * thickness() + 0.117823)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0003683 * distance() + 0.0430386) * (0.690303 - exp(-(0.0611566 * distance() + 0.238504) * (enclose() + -0.0802168 * distance() + 1.95416) - (0.00334781 * distance() + -0.0174369) * (enclose() + -0.234909 * distance() + 3.18432) * (enclose() + -0.234909 * distance() + 3.18432)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.078905 * (1 - exp(-0.402519 * (distance() + 0.19298))) * pow(width() , 0.0141987 * distance() + -0.06262) * (0.0739451 * thickness() + 0.173572)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0196778 * (0.690303 - exp(-0.85007 * (enclose() + 1.15199) - 0.0160412 * (enclose() + 0.83523) * (enclose() + 0.83523)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal3,metal7
//
CAPACITANCE CROSSOVER FRINGE metal3 poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.119382 * (1 - exp(-0.408466 * (distance() + 0.192312))) * pow(width() , 0.0139192 * distance() + -0.0614029) * (0.0475634 * thickness() + 0.188472) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.119382 * (1 - exp(-0.408466 * (distance() + 0.192312))) * pow(width() , 0.0139192 * distance() + -0.0614029) * (0.0475634 * thickness() + 0.188472) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0232149 * (1 - exp(-0.502579 * (distance() + 0.0147399))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.128676 * pow(width() , 0.0463413) * (0.0609491 * thickness() + 0.162479) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0012754 * distance() + 0.0919926) * (0.53081 - exp(-(-0.151129 * distance() + 0.183982) * (enclose() + 3.97209 * distance() + 2.93154) - (5.96002e-05 * distance() + 0.0194344) * (enclose() + 5.3996 * distance() + -0.468055) * (enclose() + 5.3996 * distance() + -0.468055))) + 0.5 * 0.345052 * exp(-0.704572 * enclose()) * (0.100522 - 0.111392 * exp(-0.35424 * distance()) * pow(distance() , 0.12895))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0012754 * distance() + 0.0919926) * (0.53081 - exp(-(-0.151129 * distance() + 0.183982) * (enclose() + 3.97209 * distance() + 2.93154) - (5.96002e-05 * distance() + 0.0194344) * (enclose() + 5.3996 * distance() + -0.468055) * (enclose() + 5.3996 * distance() + -0.468055))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0238697 * (1 - exp(-0.596443 * (enclose() + 0.275316))) * pow(width() , 0.0018516 * enclose() + 0.0323783) + 0.5 * (0.0172256 * exp(-0.603043 * enclose()) + 0.0175853 * exp(-0.600132 * enclose())) * pow(width() , 0.00208969 * enclose() + 0.0658227)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0238697 * (1 - exp(-0.596443 * (enclose() + 0.275316))) * pow(width() , 0.0018516 * enclose() + 0.0323783) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal3,metal8
//
CAPACITANCE NEARBODY metal3 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.33984 - 0.738732 * distance()) + 0.0833263 / pow(distance() , 1.12184)) * 4.71538 * pow((width1() + width2()) / 2 , 0.109363) * (0.045325 * (thickness1() + thickness2()) / 2 + 0.0219496) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.82)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal3,metal8
//
CAPACITANCE CROSSOVER FRINGE metal3 metal8 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.065351 * (1 - exp(-0.364296 * (distance() + 0.250471))) * pow(width() , 0.014645 * distance() + -0.0633104) * (0.0752501 * thickness() + 0.172979)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0122675 * (1 - exp(-0.452405 * (distance() + 0.0338722)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0995256 * pow(width() , 0.0534874) * (0.0617914 * thickness() + 0.104504)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00031853 * distance() + 0.0349421) * (0.686339 - exp(-(0.0371609 * distance() + 0.227543) * (enclose() + -0.0221528 * distance() + 2.00692) - (0.00611356 * distance() + -0.0248746) * (enclose() + -0.113591 * distance() + 2.49142) * (enclose() + -0.113591 * distance() + 2.49142)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.065351 * (1 - exp(-0.364296 * (distance() + 0.250471))) * pow(width() , 0.014645 * distance() + -0.0633104) * (0.0752501 * thickness() + 0.172979)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0158784 * (0.686339 - exp(-0.599152 * (enclose() + 1.78539) - 0.036261 * (enclose() + 1.35551) * (enclose() + 1.35551)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal3,metal8
//
CAPACITANCE CROSSOVER FRINGE metal3 poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.129137 * (1 - exp(-0.372383 * (distance() + 0.24568))) * pow(width() , 0.0142658 * distance() + -0.0619104) * (0.0423345 * thickness() + 0.191446) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.129137 * (1 - exp(-0.372383 * (distance() + 0.24568))) * pow(width() , 0.0142658 * distance() + -0.0619104) * (0.0423345 * thickness() + 0.191446) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0250694 * (1 - exp(-0.459982 * (distance() + 0.0377198))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.133264 * pow(width() , 0.0516223) * (0.0593577 * thickness() + 0.172434) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00143141 * distance() + 0.112668) * (0.458645 - exp(-(-0.122711 * distance() + 0.166266) * (enclose() + 4.3904 * distance() + 4.17502) - (4.85155e-05 * distance() + 0.0154996) * (enclose() + 5.72135 * distance() + -0.219642) * (enclose() + 5.72135 * distance() + -0.219642))) + 0.5 * 0.372864 * exp(-0.65528 * enclose()) * (0.10332 - 0.109337 * exp(-0.309913 * distance()) * pow(distance() , 0.106807))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00143141 * distance() + 0.112668) * (0.458645 - exp(-(-0.122711 * distance() + 0.166266) * (enclose() + 4.3904 * distance() + 4.17502) - (4.85155e-05 * distance() + 0.0154996) * (enclose() + 5.72135 * distance() + -0.219642) * (enclose() + 5.72135 * distance() + -0.219642))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.025843 * (1 - exp(-0.552552 * (enclose() + 0.289947))) * pow(width() , 0.00150233 * enclose() + 0.0386965) + 0.5 * (0.0193715 * exp(-0.595263 * enclose()) + 0.0190514 * exp(-0.498169 * enclose())) * pow(width() , 0.00442768 * enclose() + 0.0684244)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.025843 * (1 - exp(-0.552552 * (enclose() + 0.289947))) * pow(width() , 0.00150233 * enclose() + 0.0386965) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.82)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal4,metal5
//
CAPACITANCE NEARBODY metal4 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.961168 - 1.49527 * distance()) + 0.0478861 / pow(distance() , 1.3376)) * 3.42959 * pow((width1() + width2()) / 2 , 0.073267) * (0.068729 * (thickness1() + thickness2()) / 2 + 0.0240167) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.64)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.198479 * (1 - exp(-1.01675 * (distance() + 0.0342625))) * pow(width() , 0.00377704 * distance() + -0.0114363) * (0.0606313 * thickness() + 0.19354)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0423388 * (1 - exp(-1.06122 * (distance() + -0.00339713)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.159878 * pow(width() , 0.0250498) * (0.105229 * thickness() + 0.231609)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (8.39844e-05 * distance() + 0.127388) * (0.643433 - exp(-(-0.0368003 * distance() + 2.12939) * (enclose() + -0.0125109 * distance() + 0.684578) - (0.0504822 * distance() + -0.447211) * (enclose() + -0.35681 * distance() + 1.58607) * (enclose() + -0.35681 * distance() + 1.58607)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.198479 * (1 - exp(-1.01675 * (distance() + 0.0342625))) * pow(width() , 0.00377704 * distance() + -0.0114363) * (0.0606313 * thickness() + 0.19354)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0641139 * (0.643433 - exp(-1.76139 * (enclose() + 0.559469) - 0.057611 * (enclose() + -1.98203) * (enclose() + -1.98203)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0499858 * (1 - exp(-0.854555 * (distance() + 0.0521714))) * pow(width() , 0.00680775 * distance() + -0.0111969) * (0.129722 * thickness() + 0.1469) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0499858 * (1 - exp(-0.854555 * (distance() + 0.0521714))) * pow(width() , 0.00680775 * distance() + -0.0111969) * (0.129722 * thickness() + 0.1469) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0094114 * (1 - exp(-0.944694 * (distance() + -0.0159554))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0872553 * pow(width() , 0.046114) * (0.0934366 * thickness() + 0.0773728) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (2.08101e-05 * distance() + 0.0215204) * (0.818083 - exp(-(0.214787 * distance() + -0.0947441) * (enclose() + -0.321111 * distance() + 1.69868) - (0.00910233 * distance() + 0.0922096) * (enclose() + -0.583222 * distance() + 1.47133) * (enclose() + -0.583222 * distance() + 1.47133))) + 0.5 * 0.139106 * exp(-0.720951 * enclose()) * (0.0995975 - 0.106791 * exp(-0.524864 * distance()) * pow(distance() , 0.139953))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (2.08101e-05 * distance() + 0.0215204) * (0.818083 - exp(-(0.214787 * distance() + -0.0947441) * (enclose() + -0.321111 * distance() + 1.69868) - (0.00910233 * distance() + 0.0922096) * (enclose() + -0.583222 * distance() + 1.47133) * (enclose() + -0.583222 * distance() + 1.47133))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.00988953 * (1 - exp(-0.635382 * (enclose() + 0.129865))) * pow(width() , 0.00626692 * enclose() + -0.00115956) + 0.5 * (0.00822298 * exp(-0.714805 * enclose()) + 0.0082225 * exp(-0.71523 * enclose())) * pow(width() , -0.0091138 * enclose() + 0.137713)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.64)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.00988953 * (1 - exp(-0.635382 * (enclose() + 0.129865))) * pow(width() , 0.00626692 * enclose() + -0.00115956) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.64)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal4,metal6
//
CAPACITANCE NEARBODY metal4 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.914849 - 0.997044 * distance()) + 0.0765812 / pow(distance() , 1.23038)) * 3.13278 * pow((width1() + width2()) / 2 , 0.0917534) * (0.056992 * (thickness1() + thickness2()) / 2 + 0.025575) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.64)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.135246 * (1 - exp(-0.52988 * (distance() + 0.110938))) * pow(width() , 0.0107582 * distance() + -0.0447044) * (0.0614849 * thickness() + 0.183941)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0268969 * (1 - exp(-0.619086 * (distance() + -0.0051277)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.135571 * pow(width() , 0.0375391) * (0.0794579 * thickness() + 0.173296)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00045036 * distance() + 0.157555) * (0.329393 - exp(-(-0.0366259 * distance() + 0.0107902) * (enclose() + 9.10454 * distance() + 18.9972) - (0.00013246 * distance() + 0.0170328) * (enclose() + 4.03689 * distance() + 6.80018) * (enclose() + 4.03689 * distance() + 6.80018)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.135246 * (1 - exp(-0.52988 * (distance() + 0.110938))) * pow(width() , 0.0107582 * distance() + -0.0447044) * (0.0614849 * thickness() + 0.183941)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0765257 * (0.329393 - exp(0.355469 * (enclose() + 110.043) - 0.0183574 * (enclose() + 47.1691) * (enclose() + 47.1691)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0716672 * (1 - exp(-0.506014 * (distance() + 0.126688))) * pow(width() , 0.0118521 * distance() + -0.0460445) * (0.0785532 * thickness() + 0.174402) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0716672 * (1 - exp(-0.506014 * (distance() + 0.126688))) * pow(width() , 0.0118521 * distance() + -0.0460445) * (0.0785532 * thickness() + 0.174402) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0139118 * (1 - exp(-0.60286 * (distance() + -0.005752))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.104065 * pow(width() , 0.0442855) * (0.06751 * thickness() + 0.11219) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (2.36754e-05 * distance() + 0.0361942) * (0.71057 - exp(-(0.0909012 * distance() + 0.0903108) * (enclose() + -0.217032 * distance() + 2.02367) - (0.0111226 * distance() + 0.0113657) * (enclose() + -0.434214 * distance() + 2.00975) * (enclose() + -0.434214 * distance() + 2.00975))) + 0.5 * 0.215587 * exp(-0.635709 * enclose()) * (0.0984601 - 0.106398 * exp(-0.401427 * distance()) * pow(distance() , 0.127968))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (2.36754e-05 * distance() + 0.0361942) * (0.71057 - exp(-(0.0909012 * distance() + 0.0903108) * (enclose() + -0.217032 * distance() + 2.02367) - (0.0111226 * distance() + 0.0113657) * (enclose() + -0.434214 * distance() + 2.00975) * (enclose() + -0.434214 * distance() + 2.00975))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0144954 * (1 - exp(-0.545109 * (enclose() + 0.203768))) * pow(width() , 0.00488056 * enclose() + 0.00882587) + 0.5 * (0.0114526 * exp(-0.599765 * enclose()) + 0.0114527 * exp(-0.600822 * enclose())) * pow(width() , -0.00527813 * enclose() + 0.0950074)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.64)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0144954 * (1 - exp(-0.545109 * (enclose() + 0.203768))) * pow(width() , 0.00488056 * enclose() + 0.00882587) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.64)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal4,metal7
//
CAPACITANCE NEARBODY metal4 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.22911 - 0.795943 * distance()) + 0.075439 / pow(distance() , 1.16556)) * 4.31353 * pow((width1() + width2()) / 2 , 0.10607) * (0.0488884 * (thickness1() + thickness2()) / 2 + 0.0235322) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.64)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal4,metal7
//
CAPACITANCE CROSSOVER FRINGE metal4 metal7 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.109213 * (1 - exp(-0.391871 * (distance() + 0.197545))) * pow(width() , 0.014555 * distance() + -0.0668171) * (0.0601345 * thickness() + 0.180438)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0208412 * (1 - exp(-0.486649 * (distance() + 0.0118936)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.122713 * pow(width() , 0.0457255) * (0.0680455 * thickness() + 0.148569)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-4.47093e-05 * distance() + 0.0664439) * (0.581386 - exp(-(0.0347579 * distance() + 0.461397) * (enclose() + -0.0435303 * distance() + 1.8879) - (0.0118434 * distance() + -0.0527204) * (enclose() + -0.247834 * distance() + 2.8215) * (enclose() + -0.247834 * distance() + 2.8215)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.109213 * (1 - exp(-0.391871 * (distance() + 0.197545))) * pow(width() , 0.014555 * distance() + -0.0668171) * (0.0601345 * thickness() + 0.180438)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0329984 * (0.581386 - exp(-0.808976 * (enclose() + 1.4526) - 0.0657136 * (enclose() + 0.34316) * (enclose() + 0.34316)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal4,metal7
//
CAPACITANCE CROSSOVER FRINGE metal4 poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0883849 * (1 - exp(-0.386622 * (distance() + 0.207336))) * pow(width() , 0.0148185 * distance() + -0.0661194) * (0.0586928 * thickness() + 0.181764) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0883849 * (1 - exp(-0.386622 * (distance() + 0.207336))) * pow(width() , 0.0148185 * distance() + -0.0661194) * (0.0586928 * thickness() + 0.181764) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0168708 * (1 - exp(-0.48326 * (distance() + 0.0141541))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.113549 * pow(width() , 0.0484725) * (0.0587759 * thickness() + 0.130532) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (7.48003e-05 * distance() + 0.0441524) * (0.688554 - exp(-(0.0542887 * distance() + 0.167722) * (enclose() + -0.154278 * distance() + 1.97213) - (0.0102051 * distance() + -0.00848208) * (enclose() + -0.409483 * distance() + 2.49936) * (enclose() + -0.409483 * distance() + 2.49936))) + 0.5 * 0.261856 * exp(-0.582946 * enclose()) * (0.103036 - 0.106169 * exp(-0.312336 * distance()) * pow(distance() , 0.10104))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (7.48003e-05 * distance() + 0.0441524) * (0.688554 - exp(-(0.0542887 * distance() + 0.167722) * (enclose() + -0.154278 * distance() + 1.97213) - (0.0102051 * distance() + -0.00848208) * (enclose() + -0.409483 * distance() + 2.49936) * (enclose() + -0.409483 * distance() + 2.49936))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0175474 * (1 - exp(-0.488695 * (enclose() + 0.23544))) * pow(width() , 0.00409331 * enclose() + 0.0183971) + 0.5 * (0.0138276 * exp(-0.529346 * enclose()) + 0.0138273 * exp(-0.52851 * enclose())) * pow(width() , -0.00284972 * enclose() + 0.084675)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.64)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0175474 * (1 - exp(-0.488695 * (enclose() + 0.23544))) * pow(width() , 0.00409331 * enclose() + 0.0183971) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.64)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal4,metal8
//
CAPACITANCE NEARBODY metal4 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.31641 - 0.673293 * distance()) + 0.0896683 / pow(distance() , 1.11127)) * 4.77867 * pow((width1() + width2()) / 2 , 0.114614) * (0.0423083 * (thickness1() + thickness2()) / 2 + 0.0211096) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.64)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal4,metal8
//
CAPACITANCE CROSSOVER FRINGE metal4 metal8 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0898569 * (1 - exp(-0.321014 * (distance() + 0.289357))) * pow(width() , 0.0162849 * distance() + -0.0769867) * (0.0595531 * thickness() + 0.179111)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0167153 * (1 - exp(-0.411286 * (distance() + 0.0412044)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.113041 * pow(width() , 0.0524371) * (0.0602853 * thickness() + 0.129511)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00018207 * distance() + 0.0388091) * (0.843336 - exp(-(0.122755 * distance() + 0.220203) * (enclose() + 0.202453 * distance() + 1.42636) - (-0.00204181 * distance() + -0.0154824) * (enclose() + 0.711942 * distance() + 3.67844) * (enclose() + 0.711942 * distance() + 3.67844)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0898569 * (1 - exp(-0.321014 * (distance() + 0.289357))) * pow(width() , 0.0162849 * distance() + -0.0769867) * (0.0595531 * thickness() + 0.179111)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0184942 * (0.843336 - exp(-1.44775 * (enclose() + 3.45089) - -0.0359005 * (enclose() + 10.7979) * (enclose() + 10.7979)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal4,metal8
//
CAPACITANCE CROSSOVER FRINGE metal4 poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.102771 * (1 - exp(-0.321337 * (distance() + 0.294111))) * pow(width() , 0.0162396 * distance() + -0.07547) * (0.0474183 * thickness() + 0.186054) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.102771 * (1 - exp(-0.321337 * (distance() + 0.294111))) * pow(width() , 0.0162396 * distance() + -0.07547) * (0.0474183 * thickness() + 0.186054) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0193305 * (1 - exp(-0.412831 * (distance() + 0.0424886))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.12076 * pow(width() , 0.0534791) * (0.0546925 * thickness() + 0.144294) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00017691 * distance() + 0.0491665) * (0.684863 - exp(-(0.0432309 * distance() + 0.197673) * (enclose() + -0.140605 * distance() + 1.97816) - (0.00900421 * distance() + -0.0152396) * (enclose() + -0.409266 * distance() + 2.54096) * (enclose() + -0.409266 * distance() + 2.54096))) + 0.5 * 0.306313 * exp(-0.539463 * enclose()) * (0.105887 - 0.10435 * exp(-0.249956 * distance()) * pow(distance() , 0.0764084))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00017691 * distance() + 0.0491665) * (0.684863 - exp(-(0.0432309 * distance() + 0.197673) * (enclose() + -0.140605 * distance() + 1.97816) - (0.00900421 * distance() + -0.0152396) * (enclose() + -0.409266 * distance() + 2.54096) * (enclose() + -0.409266 * distance() + 2.54096))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.64)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0201151 * (1 - exp(-0.442383 * (enclose() + 0.262046))) * pow(width() , 0.00354273 * enclose() + 0.0263011) + 0.5 * (0.0159704 * exp(-0.471238 * enclose()) + 0.0159702 * exp(-0.472142 * enclose())) * pow(width() , -0.00082995 * enclose() + 0.0816811)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.64)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0201151 * (1 - exp(-0.442383 * (enclose() + 0.262046))) * pow(width() , 0.00354273 * enclose() + 0.0263011) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.64)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.990921 - 1.41839 * distance()) + 0.059829 / pow(distance() , 1.2793)) * 3.41296 * pow((width1() + width2()) / 2 , 0.0800357) * (0.0643239 * (thickness1() + thickness2()) / 2 + 0.0228525) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.46)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.20596 * (1 - exp(-0.976997 * (distance() + 0.0502847))) * pow(width() , 0.0036348 * distance() + -0.00706216) * (0.0583001 * thickness() + 0.195179)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0439501 * (1 - exp(-1.01049 * (distance() + 0.00810883)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.163418 * pow(width() , 0.0318337) * (0.105029 * thickness() + 0.238447)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00020409 * distance() + 0.117369) * (0.716474 - exp(-(-0.0411072 * distance() + 2.01572) * (enclose() + -0.00020917 * distance() + 0.610836) - (0.046318 * distance() + -0.435521) * (enclose() + -0.365202 * distance() + 1.51689) * (enclose() + -0.365202 * distance() + 1.51689)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.20596 * (1 - exp(-0.976997 * (distance() + 0.0502847))) * pow(width() , 0.0036348 * distance() + -0.00706216) * (0.0583001 * thickness() + 0.195179)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0597049 * (0.716474 - exp(-1.60465 * (enclose() + 0.608744) - 0.027659 * (enclose() + -2.13513) * (enclose() + -2.13513)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.040579 * (1 - exp(-0.810594 * (distance() + 0.0759688))) * pow(width() , 0.00691646 * distance() + -0.00645046) * (0.136705 * thickness() + 0.143727) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.040579 * (1 - exp(-0.810594 * (distance() + 0.0759688))) * pow(width() , 0.00691646 * distance() + -0.00645046) * (0.136705 * thickness() + 0.143727) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00757559 * (1 - exp(-0.892944 * (distance() + -0.00226172))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0802412 * pow(width() , 0.0548689) * (0.0870646 * thickness() + 0.0668147) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (1.23894e-05 * distance() + 0.0171714) * (0.817344 - exp(-(0.147398 * distance() + -0.124083) * (enclose() + -0.319433 * distance() + 2.02182) - (0.00646573 * distance() + 0.0611349) * (enclose() + -0.611325 * distance() + 2.20956) * (enclose() + -0.611325 * distance() + 2.20956))) + 0.5 * 0.113869 * exp(-0.568279 * enclose()) * (0.106284 - 0.0945255 * exp(-0.397828 * distance()) * pow(distance() , 0.0548641))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (1.23894e-05 * distance() + 0.0171714) * (0.817344 - exp(-(0.147398 * distance() + -0.124083) * (enclose() + -0.319433 * distance() + 2.02182) - (0.00646573 * distance() + 0.0611349) * (enclose() + -0.611325 * distance() + 2.20956) * (enclose() + -0.611325 * distance() + 2.20956))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.00813722 * (1 - exp(-0.481476 * (enclose() + 0.0647355))) * pow(width() , 0.00957979 * enclose() + -0.0203544) + 0.5 * (0.00744104 * exp(-0.569038 * enclose()) + 0.00744092 * exp(-0.569181 * enclose())) * pow(width() , -0.00709873 * enclose() + 0.166065)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.46)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.00813722 * (1 - exp(-0.481476 * (enclose() + 0.0647355))) * pow(width() , 0.00957979 * enclose() + -0.0203544) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.46)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal5,metal7
//
CAPACITANCE NEARBODY metal5 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.930958 - 0.914753 * distance()) + 0.0946025 / pow(distance() , 1.18103)) * 3.10801 * pow((width1() + width2()) / 2 , 0.0975826) * (0.0527579 * (thickness1() + thickness2()) / 2 + 0.0241015) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.46)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal5,metal7
//
CAPACITANCE CROSSOVER FRINGE metal5 metal7 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.144516 * (1 - exp(-0.489766 * (distance() + 0.148873))) * pow(width() , 0.0111555 * distance() + -0.0445364) * (0.0560909 * thickness() + 0.186528)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0286727 * (1 - exp(-0.573644 * (distance() + 0.0124557)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.139424 * pow(width() , 0.0433591) * (0.0767884 * thickness() + 0.183351)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (1.21416e-05 * distance() + 0.144315) * (0.371152 - exp(-(-0.0198602 * distance() + 0.00412966) * (enclose() + 11.5328 * distance() + 23.6455) - (0.00018567 * distance() + 0.0175374) * (enclose() + 3.1754 * distance() + 6.67443) * (enclose() + 3.1754 * distance() + 6.67443)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.144516 * (1 - exp(-0.489766 * (distance() + 0.148873))) * pow(width() , 0.0111555 * distance() + -0.0445364) * (0.0560909 * thickness() + 0.186528)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0722182 * (0.371152 - exp(0.194472 * (enclose() + 138.974) - 0.0193941 * (enclose() + 38.4284) * (enclose() + 38.4284)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal5,metal7
//
CAPACITANCE CROSSOVER FRINGE metal5 poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0598877 * (1 - exp(-0.464054 * (distance() + 0.169881))) * pow(width() , 0.012341 * distance() + -0.0455103) * (0.0820059 * thickness() + 0.172099) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0598877 * (1 - exp(-0.464054 * (distance() + 0.169881))) * pow(width() , 0.012341 * distance() + -0.0455103) * (0.0820059 * thickness() + 0.172099) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0114867 * (1 - exp(-0.554951 * (distance() + 0.0138777))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0967878 * pow(width() , 0.0512836) * (0.0633669 * thickness() + 0.0993402) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-8.64744e-09 * distance() + 0.027785) * (0.757974 - exp(-(0.0795319 * distance() + 0.0723771) * (enclose() + -0.234833 * distance() + 2.00346) - (0.00793196 * distance() + 0.0115138) * (enclose() + -0.437518 * distance() + 1.56652) * (enclose() + -0.437518 * distance() + 1.56652))) + 0.5 * 0.179672 * exp(-0.510289 * enclose()) * (0.104368 - 0.100504 * exp(-0.324251 * distance()) * pow(distance() , 0.0756611))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-8.64744e-09 * distance() + 0.027785) * (0.757974 - exp(-(0.0795319 * distance() + 0.0723771) * (enclose() + -0.234833 * distance() + 2.00346) - (0.00793196 * distance() + 0.0115138) * (enclose() + -0.437518 * distance() + 1.56652) * (enclose() + -0.437518 * distance() + 1.56652))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0122073 * (1 - exp(-0.426674 * (enclose() + 0.163268))) * pow(width() , 0.00712538 * enclose() + -0.00339952) + 0.5 * (0.0103484 * exp(-0.490859 * enclose()) + 0.0103481 * exp(-0.491124 * enclose())) * pow(width() , -0.00476856 * enclose() + 0.114775)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.46)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0122073 * (1 - exp(-0.426674 * (enclose() + 0.163268))) * pow(width() , 0.00712538 * enclose() + -0.00339952) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.46)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal5,metal8
//
CAPACITANCE NEARBODY metal5 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.32414 - 0.691487 * distance()) + 0.0877354 / pow(distance() , 1.11419)) * 4.76023 * pow((width1() + width2()) / 2 , 0.113237) * (0.0431628 * (thickness1() + thickness2()) / 2 + 0.0213903) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.46)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal5,metal8
//
CAPACITANCE CROSSOVER FRINGE metal5 metal8 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.115617 * (1 - exp(-0.336925 * (distance() + 0.272023))) * pow(width() , 0.0156938 * distance() + -0.0734506) * (0.0527957 * thickness() + 0.183312)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0218113 * (1 - exp(-0.42683 * (distance() + 0.0377593)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.125499 * pow(width() , 0.0512176) * (0.0641367 * thickness() + 0.154961)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-9.90973e-05 * distance() + 0.0531098) * (0.759455 - exp(-(0.0622763 * distance() + 0.138488) * (enclose() + 0.697322 * distance() + 1.43433) - (-9.57965e-06 * distance() + -0.00058013) * (enclose() + -8.7669 * distance() + -1.98149) * (enclose() + -8.7669 * distance() + -1.98149)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.115617 * (1 - exp(-0.336925 * (distance() + 0.272023))) * pow(width() , 0.0156938 * distance() + -0.0734506) * (0.0527957 * thickness() + 0.183312)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0260594 * (0.759455 - exp(-0.761251 * (enclose() + 8.40755) - -0.000675927 * (enclose() + -89.6505) * (enclose() + -89.6505)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal5,metal8
//
CAPACITANCE CROSSOVER FRINGE metal5 poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0779593 * (1 - exp(-0.331504 * (distance() + 0.286158))) * pow(width() , 0.0158517 * distance() + -0.07117) * (0.0577505 * thickness() + 0.181021) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0779593 * (1 - exp(-0.331504 * (distance() + 0.286158))) * pow(width() , 0.0158517 * distance() + -0.07117) * (0.0577505 * thickness() + 0.181021) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0146064 * (1 - exp(-0.422503 * (distance() + 0.0411841))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.107537 * pow(width() , 0.0549046) * (0.0542754 * thickness() + 0.119877) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (1.31734e-05 * distance() + 0.0353792) * (0.737674 - exp(-(0.0449734 * distance() + 0.127398) * (enclose() + -0.156454 * distance() + 1.99655) - (0.00722419 * distance() + -0.00635783) * (enclose() + -0.399116 * distance() + 2.29278) * (enclose() + -0.399116 * distance() + 2.29278))) + 0.5 * 0.237762 * exp(-0.46767 * enclose()) * (0.106418 - 0.100033 * exp(-0.238446 * distance()) * pow(distance() , 0.0571611))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (1.31734e-05 * distance() + 0.0353792) * (0.737674 - exp(-(0.0449734 * distance() + 0.127398) * (enclose() + -0.156454 * distance() + 1.99655) - (0.00722419 * distance() + -0.00635783) * (enclose() + -0.399116 * distance() + 2.29278) * (enclose() + -0.399116 * distance() + 2.29278))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.46)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0154644 * (1 - exp(-0.381457 * (enclose() + 0.209637))) * pow(width() , 0.00568812 * enclose() + 0.0107981) + 0.5 * (0.0128881 * exp(-0.430986 * enclose()) + 0.0128882 * exp(-0.431053 * enclose())) * pow(width() , -0.00295403 * enclose() + 0.0983776)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.46)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0154644 * (1 - exp(-0.381457 * (enclose() + 0.209637))) * pow(width() , 0.00568812 * enclose() + 0.0107981) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.46)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal6,metal7
//
CAPACITANCE NEARBODY metal6 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.02191 - 1.37489 * distance()) + 0.0691639 / pow(distance() , 1.24027)) * 3.44784 * pow((width1() + width2()) / 2 , 0.0839056) * (0.0610167 * (thickness1() + thickness2()) / 2 + 0.0218389) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.28)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal6,metal7
//
CAPACITANCE CROSSOVER FRINGE metal6 metal7 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.214617 * (1 - exp(-0.957016 * (distance() + 0.0610226))) * pow(width() , 0.00340821 * distance() + -0.0029341) * (0.055991 * thickness() + 0.193039)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0450543 * (1 - exp(-0.981264 * (distance() + 0.0166788)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.166273 * pow(width() , 0.0369001) * (0.105667 * thickness() + 0.242395)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00036733 * distance() + 0.123958) * (0.68664 - exp(-(-0.043671 * distance() + 1.98219) * (enclose() + 0.00510523 * distance() + 0.62713) - (0.0456427 * distance() + -0.432694) * (enclose() + -0.363304 * distance() + 1.49669) * (enclose() + -0.363304 * distance() + 1.49669)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.214617 * (1 - exp(-0.957016 * (distance() + 0.0610226))) * pow(width() , 0.00340821 * distance() + -0.0029341) * (0.055991 * thickness() + 0.193039)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0638157 * (0.68664 - exp(-1.54548 * (enclose() + 0.678182) - 0.023733 * (enclose() + -2.13635) * (enclose() + -2.13635)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal6,metal7
//
CAPACITANCE CROSSOVER FRINGE metal6 poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0341566 * (1 - exp(-0.789167 * (distance() + 0.0922339))) * pow(width() , 0.00674542 * distance() + -0.00140588) * (0.141532 * thickness() + 0.141598) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.28)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0341566 * (1 - exp(-0.789167 * (distance() + 0.0922339))) * pow(width() , 0.00674542 * distance() + -0.00140588) * (0.141532 * thickness() + 0.141598) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.28)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0063419 * (1 - exp(-0.863484 * (distance() + 0.00847273))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.28)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0752144 * pow(width() , 0.0612341) * (0.0812069 * thickness() + 0.0591543) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.28)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-3.07494e-07 * distance() + 0.0146311) * (0.798948 - exp(-(0.111015 * distance() + -0.0774739) * (enclose() + -0.327873 * distance() + 2.21165) - (0.00487714 * distance() + 0.0421491) * (enclose() + -0.638236 * distance() + 2.42143) * (enclose() + -0.638236 * distance() + 2.42143))) + 0.5 * 0.0970062 * exp(-0.46827 * enclose()) * (0.110719 - 0.0860585 * exp(-0.315355 * distance()) * pow(distance() , -0.00921371))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.28)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-3.07494e-07 * distance() + 0.0146311) * (0.798948 - exp(-(0.111015 * distance() + -0.0774739) * (enclose() + -0.327873 * distance() + 2.21165) - (0.00487714 * distance() + 0.0421491) * (enclose() + -0.638236 * distance() + 2.42143) * (enclose() + -0.638236 * distance() + 2.42143))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.28)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.00699294 * (1 - exp(-0.378892 * (enclose() + -0.00793371))) * pow(width() , 0.0128105 * enclose() + -0.0429115) + 0.5 * (0.0067707 * exp(-0.470162 * enclose()) + 0.00676816 * exp(-0.475103 * enclose())) * pow(width() , -0.0054459 * enclose() + 0.186391)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.28)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.00699294 * (1 - exp(-0.378892 * (enclose() + -0.00793371))) * pow(width() , 0.0128105 * enclose() + -0.0429115) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.28)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal6,metal8
//
CAPACITANCE NEARBODY metal6 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-0.930444 - 0.809614 * distance()) + 0.119213 / pow(distance() , 1.1328)) * 3.07553 * pow((width1() + width2()) / 2 , 0.104734) * (0.0476114 * (thickness1() + thickness2()) / 2 + 0.0224182) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.28)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal6,metal8
//
CAPACITANCE CROSSOVER FRINGE metal6 metal8 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.145204 * (1 - exp(-0.429353 * (distance() + 0.202352))) * pow(width() , 0.0123597 * distance() + -0.0503241) * (0.0520247 * thickness() + 0.187779)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0285283 * (1 - exp(-0.511136 * (distance() + 0.0317144)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.140396 * pow(width() , 0.0491956) * (0.0731325 * thickness() + 0.183603)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00044765 * distance() + 0.119321) * (0.429239 - exp(-(-0.0751073 * distance() + 0.0852019) * (enclose() + 4.17936 * distance() + 6.75622) - (4.57605e-05 * distance() + 0.0212854) * (enclose() + 3.67021 * distance() + 2.78163) * (enclose() + 3.67021 * distance() + 2.78163)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.145204 * (1 - exp(-0.429353 * (distance() + 0.202352))) * pow(width() , 0.0123597 * distance() + -0.0503241) * (0.0520247 * thickness() + 0.187779)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0618987 * (0.429239 - exp(0.665871 * (enclose() + 48.5498) - 0.021743 * (enclose() + 39.4837) * (enclose() + 39.4837)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal6,metal8
//
CAPACITANCE CROSSOVER FRINGE metal6 poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0542757 * (1 - exp(-0.409939 * (distance() + 0.226188))) * pow(width() , 0.0131663 * distance() + -0.0487927) * (0.078767 * thickness() + 0.173245) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.28)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0542757 * (1 - exp(-0.409939 * (distance() + 0.226188))) * pow(width() , 0.0131663 * distance() + -0.0487927) * (0.078767 * thickness() + 0.173245) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.28)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.010323 * (1 - exp(-0.496419 * (distance() + 0.034115))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.28)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0934377 * pow(width() , 0.0570627) * (0.0577706 * thickness() + 0.093682) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.28)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-4.71745e-05 * distance() + 0.0242873) * (0.780252 - exp(-(0.0601583 * distance() + 0.0686294) * (enclose() + -0.223435 * distance() + 2.03418) - (0.00563936 * distance() + 0.00640455) * (enclose() + -0.439688 * distance() + 1.59009) * (enclose() + -0.439688 * distance() + 1.59009))) + 0.5 * 0.166197 * exp(-0.418847 * enclose()) * (0.106899 - 0.0951033 * exp(-0.262685 * distance()) * pow(distance() , 0.0371791))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.28)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-4.71745e-05 * distance() + 0.0242873) * (0.780252 - exp(-(0.0601583 * distance() + 0.0686294) * (enclose() + -0.223435 * distance() + 2.03418) - (0.00563936 * distance() + 0.00640455) * (enclose() + -0.439688 * distance() + 1.59009) * (enclose() + -0.439688 * distance() + 1.59009))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.28)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0112294 * (1 - exp(-0.338984 * (enclose() + 0.130342))) * pow(width() , 0.00878323 * enclose() + -0.013277) + 0.5 * (0.00985748 * exp(-0.406829 * enclose()) + 0.00985724 * exp(-0.407732 * enclose())) * pow(width() , -0.00392602 * enclose() + 0.124894)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.28)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0112294 * (1 - exp(-0.338984 * (enclose() + 0.130342))) * pow(width() , 0.00878323 * enclose() + -0.013277) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.28)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,poly,metal7,metal8
//
CAPACITANCE NEARBODY metal7 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(-1.01443 - 1.18377 * distance()) + 0.0841298 / pow(distance() , 1.18348)) * 3.32344 * pow((width1() + width2()) / 2 , 0.0898238) * (0.0570734 * (thickness1() + thickness2()) / 2 + 0.0234423) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 5.1)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,poly,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal7 metal8 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.20036 * (1 - exp(-0.757014 * (distance() + 0.092474))) * pow(width() , 0.00527266 * distance() + -0.00957243) * (0.0545544 * thickness() + 0.19424)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0418881 * (1 - exp(-0.799731 * (distance() + 0.0218417)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.162286 * pow(width() , 0.0425006) * (0.0962645 * thickness() + 0.233855)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00499697 * distance() + 0.867056) * (0.0878666 - exp(-(-0.124947 * distance() + 0.142949) * (enclose() + 4.135 * distance() + 10.0133) - (0.00034182 * distance() + 0.0301032) * (enclose() + 3.69754 * distance() + 5.362) * (enclose() + 3.69754 * distance() + 5.362)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.20036 * (1 - exp(-0.757014 * (distance() + 0.092474))) * pow(width() , 0.00527266 * distance() + -0.00957243) * (0.0545544 * thickness() + 0.19424)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.458513 * (0.0878666 - exp(1.10652 * (enclose() + 51.3633) - 0.0335214 * (enclose() + 42.3374) * (enclose() + 42.3374)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,poly,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal7 poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.033333 * (1 - exp(-0.655383 * (distance() + 0.129882))) * pow(width() , 0.00793053 * distance() + -0.00722031) * (0.124342 * thickness() + 0.149401) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.1)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.033333 * (1 - exp(-0.655383 * (distance() + 0.129882))) * pow(width() , 0.00793053 * distance() + -0.00722031) * (0.124342 * thickness() + 0.149401) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.1)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00620769 * (1 - exp(-0.728697 * (distance() + 0.0216177))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.1)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0756072 * pow(width() , 0.06332) * (0.0689004 * thickness() + 0.0615281) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 5.1)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-2.6906e-05 * distance() + 0.0144688) * (0.791751 - exp(-(0.0773506 * distance() + 0.00051182) * (enclose() + -0.303508 * distance() + 2.21594) - (0.00417554 * distance() + 0.023649) * (enclose() + -0.591827 * distance() + 2.10563) * (enclose() + -0.591827 * distance() + 2.10563))) + 0.5 * 0.0968699 * exp(-0.389686 * enclose()) * (0.111836 - 0.0844021 * exp(-0.274544 * distance()) * pow(distance() , -0.0297634))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.1)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-2.6906e-05 * distance() + 0.0144688) * (0.791751 - exp(-(0.0773506 * distance() + 0.00051182) * (enclose() + -0.303508 * distance() + 2.21594) - (0.00417554 * distance() + 0.023649) * (enclose() + -0.591827 * distance() + 2.10563) * (enclose() + -0.591827 * distance() + 2.10563))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.1)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.00702528 * (1 - exp(-0.301341 * (enclose() + -0.0355064))) * pow(width() , 0.0145838 * enclose() + -0.0587708) + 0.5 * (0.00677738 * exp(-0.395039 * enclose()) + 0.00677689 * exp(-0.393588 * enclose())) * pow(width() , -0.00446895 * enclose() + 0.181561)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 5.1)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.00702528 * (1 - exp(-0.301341 * (enclose() + -0.0355064))) * pow(width() , 0.0145838 * enclose() + -0.0587708) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 5.1)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal2,metal3
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-0.894785 - 2.53585 * distance()) + 0.00979286 / pow(distance() , 1.75995)) * 3.61615 * pow((width1() + width2()) / 2 , 0.00979828) * (0.112642 * (thickness1() + thickness2()) / 2 + 0.0262928) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal2 metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.148442 * (1 - exp(-1.73565 * (distance() + -0.0512697))) * pow(width() , 0.00174933 * distance() + -0.0127756) * (0.105749 * thickness() + 0.163147)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0301003 * (1 - exp(-1.79856 * (distance() + -0.0551516)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.135378 * pow(width() , 0) * (0.165366 * thickness() + 0.158898)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00166046 * distance() + 0.328829) * (0.187086 - exp(-(0.358562 * distance() + 0.939835) * (enclose() + -0.00716934 * distance() + 1.69152) - (0.00016268 * distance() + -0.00255913) * (enclose() + 7.69632 * distance() + -5.15498) * (enclose() + 7.69632 * distance() + -5.15498)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.148442 * (1 - exp(-1.73565 * (distance() + -0.0512697))) * pow(width() , 0.00174933 * distance() + -0.0127756) * (0.105749 * thickness() + 0.163147)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.156112 * (0.187086 - exp(-4.52546 * (enclose() + 1.61983) - -0.00093233 * (enclose() + 71.8082) * (enclose() + 71.8082)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal2,metal3
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.133374 * (1 - exp(-1.74201 * (distance() + -0.0475697))) * pow(width() , 0.00167655 * distance() + -0.0118058) * (0.060704 * thickness() + 0.194901) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.133374 * (1 - exp(-1.74201 * (distance() + -0.0475697))) * pow(width() , 0.00167655 * distance() + -0.0118058) * (0.060704 * thickness() + 0.194901) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0290058 * (1 - exp(-1.80734 * (distance() + -0.0523719))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.13721 * pow(width() , -0.00376214) * (0.0989114 * thickness() + 0.172548) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00033209 * distance() + 0.100203) * (0.538204 - exp(-(2.38748 * distance() + 0.0242398) * (enclose() + -0.0134269 * distance() + 0.359941) - (-0.115244 * distance() + 0.137294) * (enclose() + 0.00428494 * distance() + 2.00121) * (enclose() + 0.00428494 * distance() + 2.00121))) + 0.5 * 0.217928 * exp(-2.64909 * enclose()) * (0.060515 - 0.156766 * exp(-1.25727 * distance()) * pow(distance() , 0.446392))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00033209 * distance() + 0.100203) * (0.538204 - exp(-(2.38748 * distance() + 0.0242398) * (enclose() + -0.0134269 * distance() + 0.359941) - (-0.115244 * distance() + 0.137294) * (enclose() + 0.00428494 * distance() + 2.00121) * (enclose() + 0.00428494 * distance() + 2.00121))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0288001 * (1 - exp(-2.37062 * (enclose() + 0.265416))) * pow(width() , 0.00021083 * enclose() + -0.00336328) + 0.5 * (0.00632307 * exp(-2.29894 * enclose()) + 0.00653879 * exp(-2.30974 * enclose())) * pow(width() , 0.00990388 * enclose() + -0.00305534)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0288001 * (1 - exp(-2.37062 * (enclose() + 0.265416))) * pow(width() , 0.00021083 * enclose() + -0.00336328) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal2,metal4
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-0.882226 - 1.81086 * distance()) + 0.0251309 / pow(distance() , 1.51878)) * 3.4249 * pow((width1() + width2()) / 2 , 0.0430141) * (0.0835058 * (thickness1() + thickness2()) / 2 + 0.025928) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal2 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0850494 * (1 - exp(-1.07712 * (distance() + -0.0155171))) * pow(width() , 0.00457101 * distance() + -0.0144835) * (0.128916 * thickness() + 0.144553)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0160931 * (1 - exp(-1.16454 * (distance() + -0.0468509)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.10546 * pow(width() , 0.0184983) * (0.129674 * thickness() + 0.105974)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00053209 * distance() + 0.056744) * (0.587135 - exp(-(-0.115444 * distance() + 0.353843) * (enclose() + 1.84521 * distance() + 0.964135) - (0.00559845 * distance() + 0.121539) * (enclose() + 0.970472 * distance() + 0.00577033) * (enclose() + 0.970472 * distance() + 0.00577033)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0850494 * (1 - exp(-1.07712 * (distance() + -0.0155171))) * pow(width() , 0.00457101 * distance() + -0.0144835) * (0.128916 * thickness() + 0.144553)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0257115 * (0.587135 - exp(0.800597 * (enclose() + 19.4162) - 0.177524 * (enclose() + 9.71049) * (enclose() + 9.71049)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal2,metal4
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.160405 * (1 - exp(-1.20447 * (distance() + -0.00688701))) * pow(width() , 0.0029894 * distance() + -0.0147603) * (0.0343924 * thickness() + 0.214628) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.160405 * (1 - exp(-1.20447 * (distance() + -0.00688701))) * pow(width() , 0.0029894 * distance() + -0.0147603) * (0.0343924 * thickness() + 0.214628) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0363018 * (1 - exp(-1.26542 * (distance() + -0.0270401))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.154768 * pow(width() , 0.00756787) * (0.0682735 * thickness() + 0.209494) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00140663 * distance() + 0.738472) * (0.09788 - exp(-(-0.0102222 * distance() + 2.12368) * (enclose() + -0.0419014 * distance() + 1.65347) - (0.042965 * distance() + -0.335549) * (enclose() + -0.418021 * distance() + 1.97106) * (enclose() + -0.418021 * distance() + 1.97106))) + 0.5 * 0.274807 * exp(-1.9644 * enclose()) * (0.0777263 - 0.138455 * exp(-0.781265 * distance()) * pow(distance() , 0.2922))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00140663 * distance() + 0.738472) * (0.09788 - exp(-(-0.0102222 * distance() + 2.12368) * (enclose() + -0.0419014 * distance() + 1.65347) - (0.042965 * distance() + -0.335549) * (enclose() + -0.418021 * distance() + 1.97106) * (enclose() + -0.418021 * distance() + 1.97106))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0363666 * (1 - exp(-1.78212 * (enclose() + 0.313918))) * pow(width() , -0.00010861 * enclose() + 0.0089965) + 0.5 * (0.0108007 * exp(-1.98477 * enclose()) + 0.00999649 * exp(-1.28301 * enclose())) * pow(width() , 0.0216058 * enclose() + 0.0108387)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0363666 * (1 - exp(-1.78212 * (enclose() + 0.313918))) * pow(width() , -0.00010861 * enclose() + 0.0089965) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal2,metal5
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-0.932392 - 1.56125 * distance()) + 0.0401257 / pow(distance() , 1.38899)) * 3.41873 * pow((width1() + width2()) / 2 , 0.0633982) * (0.0730553 * (thickness1() + thickness2()) / 2 + 0.0243162) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal2 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0610724 * (1 - exp(-0.899392 * (distance() + 0.0250001))) * pow(width() , 0.00622488 * distance() + -0.0158077) * (0.140645 * thickness() + 0.138286)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0113227 * (1 - exp(-0.98927 * (distance() + -0.0297276)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0923951 * pow(width() , 0.0341083) * (0.115489 * thickness() + 0.0828249)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00018809 * distance() + 0.0259916) * (0.881708 - exp(-(-0.619104 * distance() + 0.321876) * (enclose() + 1.69742 * distance() + -0.185924) - (0.00031083 * distance() + 0.128648) * (enclose() + 2.7861 * distance() + -0.415656) * (enclose() + 2.7861 * distance() + -0.415656)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0610724 * (1 - exp(-0.899392 * (distance() + 0.0250001))) * pow(width() , 0.00622488 * distance() + -0.0158077) * (0.140645 * thickness() + 0.138286)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0120553 * (0.881708 - exp(5.86916 * (enclose() + 16.7883) - 0.131756 * (enclose() + 27.4453) * (enclose() + 27.4453)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal2,metal5
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.17298 * (1 - exp(-1.04335 * (distance() + 0.0257159))) * pow(width() , 0.00366491 * distance() + -0.0138467) * (0.028522 * thickness() + 0.222557) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.17298 * (1 - exp(-1.04335 * (distance() + 0.0257159))) * pow(width() , 0.00366491 * distance() + -0.0138467) * (0.028522 * thickness() + 0.222557) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0399233 * (1 - exp(-1.09716 * (distance() + -0.00785762))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.162376 * pow(width() , 0.0187788) * (0.0640668 * thickness() + 0.225551) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00012827 * distance() + 0.120913) * (0.649909 - exp(-(-0.0337856 * distance() + 1.82446) * (enclose() + -0.0048671 * distance() + 0.689451) - (0.0327411 * distance() + -0.320408) * (enclose() + -0.456035 * distance() + 1.70602) * (enclose() + -0.456035 * distance() + 1.70602))) + 0.5 * 0.31261 * exp(-1.61958 * enclose()) * (0.0857693 - 0.125682 * exp(-0.578302 * distance()) * pow(distance() , 0.21193))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00012827 * distance() + 0.120913) * (0.649909 - exp(-(-0.0337856 * distance() + 1.82446) * (enclose() + -0.0048671 * distance() + 0.689451) - (0.0327411 * distance() + -0.320408) * (enclose() + -0.456035 * distance() + 1.70602) * (enclose() + -0.456035 * distance() + 1.70602))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.040342 * (1 - exp(-1.53745 * (enclose() + 0.342217))) * pow(width() , -0.00048644 * enclose() + 0.0205565) + 0.5 * (0.0163647 * exp(-1.00367 * enclose()) + 0.00996946 * exp(-2.07337 * enclose())) * pow(width() , 0.0328853 * enclose() + 0.0262033)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.040342 * (1 - exp(-1.53745 * (enclose() + 0.342217))) * pow(width() , -0.00048644 * enclose() + 0.0205565) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal2,metal6
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-0.993627 - 1.44233 * distance()) + 0.0524039 / pow(distance() , 1.31179)) * 3.38004 * pow((width1() + width2()) / 2 , 0.0735172) * (0.0691973 * (thickness1() + thickness2()) / 2 + 0.0236492) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal2 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0478781 * (1 - exp(-0.826677 * (distance() + 0.0550194))) * pow(width() , 0.00676529 * distance() + -0.0124946) * (0.147511 * thickness() + 0.135081)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00878038 * (1 - exp(-0.911185 * (distance() + -0.0142851)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.084014 * pow(width() , 0.044854) * (0.104034 * thickness() + 0.0699199)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (4.19533e-05 * distance() + 0.0262215) * (0.624661 - exp(-(0.0145316 * distance() + 0.0814823) * (enclose() + 0.236839 * distance() + 2.00443) - (0.024212 * distance() + 0.0141884) * (enclose() + -0.268166 * distance() + 3.28258) * (enclose() + -0.268166 * distance() + 3.28258)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0478781 * (1 - exp(-0.826677 * (distance() + 0.0550194))) * pow(width() , 0.00676529 * distance() + -0.0124946) * (0.147511 * thickness() + 0.135081)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0133205 * (0.624661 - exp(-0.226798 * (enclose() + 4.37282) - 0.256308 * (enclose() + 0.60092) * (enclose() + 0.60092)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal2,metal6
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.18167 * (1 - exp(-0.97702 * (distance() + 0.0476262))) * pow(width() , 0.00371083 * distance() + -0.01001) * (0.0270282 * thickness() + 0.224844) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.18167 * (1 - exp(-0.97702 * (distance() + 0.0476262))) * pow(width() , 0.00371083 * distance() + -0.01001) * (0.0270282 * thickness() + 0.224844) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0420739 * (1 - exp(-1.01988 * (distance() + 0.00662467))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.171545 * pow(width() , 0.0270911) * (0.062386 * thickness() + 0.228203) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-8.64349e-05 * distance() + 0.143744) * (0.572218 - exp(-(-0.0443648 * distance() + 1.64357) * (enclose() + 0.0422547 * distance() + 0.687683) - (0.0310754 * distance() + -0.332095) * (enclose() + -0.454727 * distance() + 1.42197) * (enclose() + -0.454727 * distance() + 1.42197))) + 0.5 * 0.33472 * exp(-1.41896 * enclose()) * (0.0914579 - 0.120255 * exp(-0.479106 * distance()) * pow(distance() , 0.167048))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-8.64349e-05 * distance() + 0.143744) * (0.572218 - exp(-(-0.0443648 * distance() + 1.64357) * (enclose() + 0.0422547 * distance() + 0.687683) - (0.0310754 * distance() + -0.332095) * (enclose() + -0.454727 * distance() + 1.42197) * (enclose() + -0.454727 * distance() + 1.42197))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0427676 * (1 - exp(-1.40576 * (enclose() + 0.361067))) * pow(width() , -0.00088302 * enclose() + 0.0291995) + 0.5 * (0.0128375 * exp(-1.91921 * enclose()) + 0.017566 * exp(-0.796156 * enclose())) * pow(width() , 0.0397602 * enclose() + 0.0380043)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0427676 * (1 - exp(-1.40576 * (enclose() + 0.361067))) * pow(width() , -0.00088302 * enclose() + 0.0291995) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal2,metal7
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-0.993343 - 1.37899 * distance()) + 0.0653848 / pow(distance() , 1.26169)) * 3.42134 * pow((width1() + width2()) / 2 , 0.0789937) * (0.0624741 * (thickness1() + thickness2()) / 2 + 0.0215827) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal2,metal7
//
CAPACITANCE CROSSOVER FRINGE metal2 metal7 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0393571 * (1 - exp(-0.794428 * (distance() + 0.075175))) * pow(width() , 0.00663622 * distance() + -0.00720278) * (0.152828 * thickness() + 0.13291)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00717999 * (1 - exp(-0.869493 * (distance() + -0.00172153)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0776048 * pow(width() , 0.0524718) * (0.096641 * thickness() + 0.0611442)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (1.86156e-06 * distance() + 0.0210709) * (0.63767 - exp(-(0.0153806 * distance() + 0.151962) * (enclose() + 0.127471 * distance() + 1.92402) - (0.0158711 * distance() + 0.00137796) * (enclose() + -0.270719 * distance() + 3.39716) * (enclose() + -0.270719 * distance() + 3.39716)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0393571 * (1 - exp(-0.794428 * (distance() + 0.075175))) * pow(width() , 0.00663622 * distance() + -0.00720278) * (0.152828 * thickness() + 0.13291)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0105448 * (0.63767 - exp(-0.305768 * (enclose() + 3.19873) - 0.160089 * (enclose() + 0.68997) * (enclose() + 0.68997)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal2,metal7
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.188628 * (1 - exp(-0.945796 * (distance() + 0.0620434))) * pow(width() , 0.00351526 * distance() + -0.00558807) * (0.0260699 * thickness() + 0.224796) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.188628 * (1 - exp(-0.945796 * (distance() + 0.0620434))) * pow(width() , 0.00351526 * distance() + -0.00558807) * (0.0260699 * thickness() + 0.224796) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0434798 * (1 - exp(-0.978305 * (distance() + 0.0172868))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.17305 * pow(width() , 0.0331893) * (0.0644845 * thickness() + 0.235812) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00106731 * distance() + 0.131546) * (0.596664 - exp(-(0.0782562 * distance() + 0.558691) * (enclose() + 0.0482774 * distance() + 0.272121) - (0.0143683 * distance() + 0.00651567) * (enclose() + -0.682156 * distance() + 6.33457) * (enclose() + -0.682156 * distance() + 6.33457))) + 0.5 * 0.350111 * exp(-1.29131 * enclose()) * (0.0953093 - 0.11715 * exp(-0.423304 * distance()) * pow(distance() , 0.139056))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00106731 * distance() + 0.131546) * (0.596664 - exp(-(0.0782562 * distance() + 0.558691) * (enclose() + 0.0482774 * distance() + 0.272121) - (0.0143683 * distance() + 0.00651567) * (enclose() + -0.682156 * distance() + 6.33457) * (enclose() + -0.682156 * distance() + 6.33457))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0443719 * (1 - exp(-1.32476 * (enclose() + 0.374587))) * pow(width() , -0.00125442 * enclose() + 0.0356696) + 0.5 * (0.0181955 * exp(-0.663904 * enclose()) + 0.0153233 * exp(-1.79608 * enclose())) * pow(width() , 0.0429266 * enclose() + 0.0469557)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0443719 * (1 - exp(-1.32476 * (enclose() + 0.374587))) * pow(width() , -0.00125442 * enclose() + 0.0356696) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal2,metal8
//
CAPACITANCE NEARBODY metal2 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.04613 - 1.33794 * distance()) + 0.074466 / pow(distance() , 1.22157)) * 3.44991 * pow((width1() + width2()) / 2 , 0.0827182) * (0.0603919 * (thickness1() + thickness2()) / 2 + 0.0210216) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal2,metal8
//
CAPACITANCE CROSSOVER FRINGE metal2 metal8 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0324669 * (1 - exp(-0.775311 * (distance() + 0.0908726))) * pow(width() , 0.00641429 * distance() + -0.00247007) * (0.157128 * thickness() + 0.131171)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00590121 * (1 - exp(-0.841726 * (distance() + 0.00862339)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0722587 * pow(width() , 0.0583993) * (0.0888129 * thickness() + 0.0534079)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-5.14175e-05 * distance() + 0.0176641) * (0.635005 - exp(-(0.00588322 * distance() + 0.0156387) * (enclose() + 0.637885 * distance() + 2.48162) - (0.00816477 * distance() + 0.0123986) * (enclose() + -0.249067 * distance() + 4.76455) * (enclose() + -0.249067 * distance() + 4.76455)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0324669 * (1 - exp(-0.775311 * (distance() + 0.0908726))) * pow(width() , 0.00641429 * distance() + -0.00247007) * (0.157128 * thickness() + 0.131171)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00857496 * (0.635005 - exp(-0.0744709 * (enclose() + 8.86047) - 0.0940463 * (enclose() + 2.27388) * (enclose() + 2.27388)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal2,metal8
//
CAPACITANCE CROSSOVER FRINGE metal2 metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.193588 * (1 - exp(-0.927995 * (distance() + 0.0731594))) * pow(width() , 0.00322374 * distance() + -0.00083953) * (0.02599 * thickness() + 0.225419) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.193588 * (1 - exp(-0.927995 * (distance() + 0.0731594))) * pow(width() , 0.00322374 * distance() + -0.00083953) * (0.02599 * thickness() + 0.225419) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.044618 * (1 - exp(-0.950396 * (distance() + 0.026394))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.173029 * pow(width() , 0.0385344) * (0.0676002 * thickness() + 0.243697) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00095141 * distance() + 0.112509) * (0.7113 - exp(-(0.0734673 * distance() + 0.739502) * (enclose() + -0.0299701 * distance() + 0.759453) - (0.0106042 * distance() + -0.0075875) * (enclose() + -0.785938 * distance() + 6.49944) * (enclose() + -0.785938 * distance() + 6.49944))) + 0.5 * 0.362941 * exp(-1.18829 * enclose()) * (0.0983368 - 0.114738 * exp(-0.384953 * distance()) * pow(distance() , 0.117536))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00095141 * distance() + 0.112509) * (0.7113 - exp(-(0.0734673 * distance() + 0.739502) * (enclose() + -0.0299701 * distance() + 0.759453) - (0.0106042 * distance() + -0.0075875) * (enclose() + -0.785938 * distance() + 6.49944) * (enclose() + -0.785938 * distance() + 6.49944))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0456663 * (1 - exp(-1.26343 * (enclose() + 0.386054))) * pow(width() , -0.0016688 * enclose() + 0.0416354) + 0.5 * (0.018699 * exp(-0.55983 * enclose()) + 0.017694 * exp(-1.68983 * enclose())) * pow(width() , 0.045066 * enclose() + 0.0551886)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0456663 * (1 - exp(-1.26343 * (enclose() + 0.386054))) * pow(width() , -0.0016688 * enclose() + 0.0416354) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal3,metal4
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal1 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.880123 - 1.8227 * distance()) + 0.0247963 / pow(distance() , 1.52105)) * 3.43325 * pow((width1() + width2()) / 2 , 0.0456434) * (0.0829353 * (thickness1() + thickness2()) / 2 + 0.0264309) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.174995 * (1 - exp(-1.22025 * (distance() + -0.0114135))) * pow(width() , 0.00306648 * distance() + -0.0149314) * (0.0729288 * thickness() + 0.187073)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0372957 * (1 - exp(-1.27958 * (distance() + -0.0313635)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.149296 * pow(width() , 0.00818374) * (0.114936 * thickness() + 0.207613)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-7.96988e-06 * distance() + 0.119979) * (0.609191 - exp(-(-0.0099479 * distance() + 2.47731) * (enclose() + -0.044162 * distance() + 0.76218) - (0.0826003 * distance() + -0.47596) * (enclose() + -0.301326 * distance() + 1.78476) * (enclose() + -0.301326 * distance() + 1.78476)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.174995 * (1 - exp(-1.22025 * (distance() + -0.0114135))) * pow(width() , 0.00306648 * distance() + -0.0149314) * (0.0729288 * thickness() + 0.187073)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0599497 * (0.609191 - exp(-2.37783 * (enclose() + 0.32056) - 0.350043 * (enclose() + -1.2285) * (enclose() + -1.2285)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0804772 * (1 - exp(-1.08299 * (distance() + -0.0115262))) * pow(width() , 0.00485154 * distance() + -0.0161427) * (0.107471 * thickness() + 0.158964) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0804772 * (1 - exp(-1.08299 * (distance() + -0.0115262))) * pow(width() , 0.00485154 * distance() + -0.0161427) * (0.107471 * thickness() + 0.158964) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0157423 * (1 - exp(-1.1754 * (distance() + -0.0454614))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.1061 * pow(width() , 0.0195516) * (0.106975 * thickness() + 0.110249) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00042968 * distance() + 0.0417045) * (0.793769 - exp(-(-0.685582 * distance() + 0.464135) * (enclose() + 1.74431 * distance() + 0.052787) - (0.00074986 * distance() + 0.140337) * (enclose() + 2.79206 * distance() + -0.332677) * (enclose() + 2.79206 * distance() + -0.332677))) + 0.5 * 0.154525 * exp(-1.33076 * enclose()) * (0.081977 - 0.129245 * exp(-0.691262 * distance()) * pow(distance() , 0.290079))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00042968 * distance() + 0.0417045) * (0.793769 - exp(-(-0.685582 * distance() + 0.464135) * (enclose() + 1.74431 * distance() + 0.052787) - (0.00074986 * distance() + 0.140337) * (enclose() + 2.79206 * distance() + -0.332677) * (enclose() + 2.79206 * distance() + -0.332677))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0159669 * (1 - exp(-1.21548 * (enclose() + 0.331565))) * pow(width() , 0.00150355 * enclose() + 0.0103896) + 0.5 * (0.00659977 * exp(-1.2626 * enclose()) + 0.00660079 * exp(-1.26434 * enclose())) * pow(width() , -0.00380753 * enclose() + 0.0536626)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0159669 * (1 - exp(-1.21548 * (enclose() + 0.331565))) * pow(width() , 0.00150355 * enclose() + 0.0103896) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal3,metal5
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.890465 - 1.28815 * distance()) + 0.0437649 / pow(distance() , 1.36705)) * 3.2511 * pow((width1() + width2()) / 2 , 0.0691399) * (0.06849 * (thickness1() + thickness2()) / 2 + 0.028064) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.110937 * (1 - exp(-0.701508 * (distance() + 0.0302837))) * pow(width() , 0.00802056 * distance() + -0.0329472) * (0.0828331 * thickness() + 0.172214)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0219679 * (1 - exp(-0.792083 * (distance() + -0.0349149)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.122808 * pow(width() , 0.0246047) * (0.0922898 * thickness() + 0.146437)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00090803 * distance() + 0.124588) * (0.358258 - exp(-(-0.161744 * distance() + 0.14507) * (enclose() + 4.58343 * distance() + 5.21412) - (0.00014968 * distance() + 0.0312333) * (enclose() + 4.64234 * distance() + 2.02665) * (enclose() + 4.64234 * distance() + 2.02665)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.110937 * (1 - exp(-0.701508 * (distance() + 0.0302837))) * pow(width() , 0.00802056 * distance() + -0.0329472) * (0.0828331 * thickness() + 0.172214)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0577539 * (0.358258 - exp(1.47237 * (enclose() + 51.0484) - 0.0327301 * (enclose() + 48.4501) * (enclose() + 48.4501)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.107152 * (1 - exp(-0.696708 * (distance() + 0.0339023))) * pow(width() , 0.00832152 * distance() + -0.0353494) * (0.0665057 * thickness() + 0.182504) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.107152 * (1 - exp(-0.696708 * (distance() + 0.0339023))) * pow(width() , 0.00832152 * distance() + -0.0353494) * (0.0665057 * thickness() + 0.182504) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.02166 * (1 - exp(-0.791352 * (distance() + -0.0342135))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.123545 * pow(width() , 0.024477) * (0.0754753 * thickness() + 0.149053) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00103518 * distance() + 0.091359) * (0.500588 - exp(-(-0.257527 * distance() + 0.224583) * (enclose() + 3.31877 * distance() + 2.36359) - (0.00020102 * distance() + 0.0391905) * (enclose() + 4.46098 * distance() + 0.717599) * (enclose() + 4.46098 * distance() + 0.717599))) + 0.5 * 0.228711 * exp(-1.10117 * enclose()) * (0.0888484 - 0.120124 * exp(-0.47227 * distance()) * pow(distance() , 0.210635))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00103518 * distance() + 0.091359) * (0.500588 - exp(-(-0.257527 * distance() + 0.224583) * (enclose() + 3.31877 * distance() + 2.36359) - (0.00020102 * distance() + 0.0391905) * (enclose() + 4.46098 * distance() + 0.717599) * (enclose() + 4.46098 * distance() + 0.717599))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0219459 * (1 - exp(-0.972908 * (enclose() + 0.389726))) * pow(width() , 0.00112385 * enclose() + 0.017935) + 0.5 * (0.00732654 * exp(-0.972192 * enclose()) + 0.0124841 * exp(-0.973124 * enclose())) * pow(width() , 0.00271563 * enclose() + 0.0403408)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0219459 * (1 - exp(-0.972908 * (enclose() + 0.389726))) * pow(width() , 0.00112385 * enclose() + 0.017935) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal3,metal6
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.907443 - 1.07367 * distance()) + 0.0641625 / pow(distance() , 1.272)) * 3.16938 * pow((width1() + width2()) / 2 , 0.0855666) * (0.060618 * (thickness1() + thickness2()) / 2 + 0.0265627) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0840651 * (1 - exp(-0.553157 * (distance() + 0.0874083))) * pow(width() , 0.0108927 * distance() + -0.045053) * (0.0873525 * thickness() + 0.168588)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0162547 * (1 - exp(-0.649646 * (distance() + -0.0204733)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.109745 * pow(width() , 0.0362245) * (0.0815141 * thickness() + 0.120654)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00041286 * distance() + 0.0548869) * (0.593801 - exp(-(-0.0881863 * distance() + 0.142999) * (enclose() + 3.00687 * distance() + 2.82855) - (9.22375e-05 * distance() + 0.0357986) * (enclose() + 2.59213 * distance() + 0.376705) * (enclose() + 2.59213 * distance() + 0.376705)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0840651 * (1 - exp(-0.553157 * (distance() + 0.0874083))) * pow(width() , 0.0108927 * distance() + -0.045053) * (0.0873525 * thickness() + 0.168588)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0253792 * (0.593801 - exp(0.738864 * (enclose() + 32.8972) - 0.036721 * (enclose() + 26.298) * (enclose() + 26.298)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.124272 * (1 - exp(-0.566437 * (distance() + 0.0857555))) * pow(width() , 0.0104554 * distance() + -0.0454788) * (0.0525789 * thickness() + 0.189524) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.124272 * (1 - exp(-0.566437 * (distance() + 0.0857555))) * pow(width() , 0.0104554 * distance() + -0.0454788) * (0.0525789 * thickness() + 0.189524) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0250914 * (1 - exp(-0.661993 * (distance() + -0.016904))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.132388 * pow(width() , 0.0326135) * (0.0671695 * thickness() + 0.167786) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00154144 * distance() + 0.150607) * (0.345763 - exp(-(-0.161337 * distance() + 0.183586) * (enclose() + 4.24145 * distance() + 4.8562) - (0.00012376 * distance() + 0.0234411) * (enclose() + 5.14526 * distance() + 1.42367) * (enclose() + 5.14526 * distance() + 1.42367))) + 0.5 * 0.278412 * exp(-0.963779 * enclose()) * (0.0937061 - 0.114612 * exp(-0.363336 * distance()) * pow(distance() , 0.163835))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00154144 * distance() + 0.150607) * (0.345763 - exp(-(-0.161337 * distance() + 0.183586) * (enclose() + 4.24145 * distance() + 4.8562) - (0.00012376 * distance() + 0.0234411) * (enclose() + 5.14526 * distance() + 1.42367) * (enclose() + 5.14526 * distance() + 1.42367))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0255227 * (1 - exp(-0.849297 * (enclose() + 0.42188))) * pow(width() , 0.00080269 * enclose() + 0.0270542) + 0.5 * (0.0127444 * exp(-0.815845 * enclose()) + 0.011919 * exp(-0.824738 * enclose())) * pow(width() , 0.00775346 * enclose() + 0.0439227)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0255227 * (1 - exp(-0.849297 * (enclose() + 0.42188))) * pow(width() , 0.00080269 * enclose() + 0.0270542) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal3,metal7
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.933663 - 0.958233 * distance()) + 0.0826256 / pow(distance() , 1.20991)) * 3.15769 * pow((width1() + width2()) / 2 , 0.0940715) * (0.0553547 * (thickness1() + thickness2()) / 2 + 0.0249393) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal3,metal7
//
CAPACITANCE CROSSOVER FRINGE metal3 metal7 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0683025 * (1 - exp(-0.487833 * (distance() + 0.137106))) * pow(width() , 0.011988 * distance() + -0.0476709) * (0.0900844 * thickness() + 0.166626)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0130187 * (1 - exp(-0.580975 * (distance() + -0.00117896)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.100759 * pow(width() , 0.0446419) * (0.074653 * thickness() + 0.105081)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00012338 * distance() + 0.03698) * (0.672365 - exp(-(0.0333259 * distance() + 0.235726) * (enclose() + -0.0353735 * distance() + 1.93605) - (0.011022 * distance() + -0.0149721) * (enclose() + -0.211668 * distance() + 3.25695) * (enclose() + -0.211668 * distance() + 3.25695)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0683025 * (1 - exp(-0.487833 * (distance() + 0.137106))) * pow(width() , 0.011988 * distance() + -0.0476709) * (0.0900844 * thickness() + 0.166626)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0178731 * (0.672365 - exp(-0.568985 * (enclose() + 1.58231) - 0.0952479 * (enclose() + 1.14027) * (enclose() + 1.14027)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal3,metal7
//
CAPACITANCE CROSSOVER FRINGE metal3 metal1 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.135906 * (1 - exp(-0.505624 * (distance() + 0.130308))) * pow(width() , 0.01132 * distance() + -0.0479725) * (0.0459828 * thickness() + 0.192743) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.135906 * (1 - exp(-0.505624 * (distance() + 0.130308))) * pow(width() , 0.01132 * distance() + -0.0479725) * (0.0459828 * thickness() + 0.192743) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0273482 * (1 - exp(-0.596308 * (distance() + 0.00189124))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.13735 * pow(width() , 0.0393404) * (0.0646246 * thickness() + 0.180009) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00137765 * distance() + 0.161626) * (0.343105 - exp(-(-0.134709 * distance() + 0.177009) * (enclose() + 4.08014 * distance() + 5.15541) - (0.0001273 * distance() + 0.0213398) * (enclose() + 4.7985 * distance() + 1.40802) * (enclose() + 4.7985 * distance() + 1.40802))) + 0.5 * 0.311102 * exp(-0.872003 * enclose()) * (0.0978619 - 0.112506 * exp(-0.305618 * distance()) * pow(distance() , 0.135006))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00137765 * distance() + 0.161626) * (0.343105 - exp(-(-0.134709 * distance() + 0.177009) * (enclose() + 4.08014 * distance() + 5.15541) - (0.0001273 * distance() + 0.0213398) * (enclose() + 4.7985 * distance() + 1.40802) * (enclose() + 4.7985 * distance() + 1.40802))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0279219 * (1 - exp(-0.773793 * (enclose() + 0.444739))) * pow(width() , 0.00051744 * enclose() + 0.034305) + 0.5 * (0.0142062 * exp(-0.845868 * enclose()) + 0.0141902 * exp(-0.624587 * enclose())) * pow(width() , 0.0112161 * enclose() + 0.0491821)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0279219 * (1 - exp(-0.773793 * (enclose() + 0.444739))) * pow(width() , 0.00051744 * enclose() + 0.034305) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal3,metal8
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.934863 - 0.878289 * distance()) + 0.104719 / pow(distance() , 1.15911)) * 3.09677 * pow((width1() + width2()) / 2 , 0.0994791) * (0.0507352 * (thickness1() + thickness2()) / 2 + 0.0232116) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal3,metal8
//
CAPACITANCE CROSSOVER FRINGE metal3 metal8 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0558589 * (1 - exp(-0.449431 * (distance() + 0.182026))) * pow(width() , 0.0123131 * distance() + -0.0465023) * (0.0924874 * thickness() + 0.165314)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0105629 * (1 - exp(-0.536083 * (distance() + 0.0181647)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0932603 * pow(width() , 0.0514777) * (0.0690612 * thickness() + 0.0917984)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00034593 * distance() + 0.0280663) * (0.777824 - exp(-(0.0638429 * distance() + 0.10914) * (enclose() + 0.114001 * distance() + 1.57085) - (-9.60019e-06 * distance() + -2.72671e-05) * (enclose() + -10.1082 * distance() + 5.29815) * (enclose() + -10.1082 * distance() + 5.29815)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0558589 * (1 - exp(-0.449431 * (distance() + 0.182026))) * pow(width() , 0.0123131 * distance() + -0.0465023) * (0.0924874 * thickness() + 0.165314)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0123035 * (0.777824 - exp(-0.747569 * (enclose() + 2.71086) - -0.000123269 * (enclose() + -95.7839) * (enclose() + -95.7839)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal3,metal8
//
CAPACITANCE CROSSOVER FRINGE metal3 metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.145587 * (1 - exp(-0.469279 * (distance() + 0.170813))) * pow(width() , 0.0115302 * distance() + -0.0464397) * (0.0416906 * thickness() + 0.194505) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.145587 * (1 - exp(-0.469279 * (distance() + 0.170813))) * pow(width() , 0.0115302 * distance() + -0.0464397) * (0.0416906 * thickness() + 0.194505) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0291732 * (1 - exp(-0.552983 * (distance() + 0.0218063))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.141212 * pow(width() , 0.0455167) * (0.0646585 * thickness() + 0.189549) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00141077 * distance() + 0.199391) * (0.290811 - exp(-(-0.118714 * distance() + 0.147117) * (enclose() + 4.64984 * distance() + 6.97457) - (9.3006e-05 * distance() + 0.0169429) * (enclose() + 5.4128 * distance() + 2.42401) * (enclose() + 5.4128 * distance() + 2.42401))) + 0.5 * 0.336812 * exp(-0.796888 * enclose()) * (0.101697 - 0.111461 * exp(-0.266881 * distance()) * pow(distance() , 0.112894))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00141077 * distance() + 0.199391) * (0.290811 - exp(-(-0.118714 * distance() + 0.147117) * (enclose() + 4.64984 * distance() + 6.97457) - (9.3006e-05 * distance() + 0.0169429) * (enclose() + 5.4128 * distance() + 2.42401) * (enclose() + 5.4128 * distance() + 2.42401))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0298986 * (1 - exp(-0.715324 * (enclose() + 0.465456))) * pow(width() , 0.00022072 * enclose() + 0.0408141) + 0.5 * (0.0199031 * exp(-0.796261 * enclose()) + 0.011969 * exp(-0.471005 * enclose())) * pow(width() , 0.0133536 * enclose() + 0.0551313)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0298986 * (1 - exp(-0.715324 * (enclose() + 0.465456))) * pow(width() , 0.00022072 * enclose() + 0.0408141) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal4,metal5
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.930085 - 1.57707 * distance()) + 0.0395337 / pow(distance() , 1.39104)) * 3.42845 * pow((width1() + width2()) / 2 , 0.066121) * (0.0725272 * (thickness1() + thickness2()) / 2 + 0.0248498) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.09)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.191419 * (1 - exp(-1.06337 * (distance() + 0.0199785))) * pow(width() , 0.00371664 * distance() + -0.0137951) * (0.0634381 * thickness() + 0.192177)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0408805 * (1 - exp(-1.11496 * (distance() + -0.0127399)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.156539 * pow(width() , 0.0194879) * (0.10675 * thickness() + 0.22543)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (2.57085e-05 * distance() + 0.108474) * (0.733614 - exp(-(-0.0314958 * distance() + 2.26474) * (enclose() + -0.0274212 * distance() + 0.652102) - (0.0575702 * distance() + -0.460854) * (enclose() + -0.341266 * distance() + 1.66619) * (enclose() + -0.341266 * distance() + 1.66619)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.191419 * (1 - exp(-1.06337 * (distance() + 0.0199785))) * pow(width() , 0.00371664 * distance() + -0.0137951) * (0.0634381 * thickness() + 0.192177)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0543655 * (0.733614 - exp(-1.94978 * (enclose() + 0.37789) - 0.114848 * (enclose() + -1.74647) * (enclose() + -1.74647)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0584728 * (1 - exp(-0.904447 * (distance() + 0.0301136))) * pow(width() , 0.00658325 * distance() + -0.0169845) * (0.123539 * thickness() + 0.149538) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0584728 * (1 - exp(-0.904447 * (distance() + 0.0301136))) * pow(width() , 0.00658325 * distance() + -0.0169845) * (0.123539 * thickness() + 0.149538) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0111094 * (1 - exp(-1.0001 * (distance() + -0.0282791))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.092854 * pow(width() , 0.0362762) * (0.0979402 * thickness() + 0.0866467) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00212598 * distance() + 0.103599) * (0.248995 - exp(-(0.0890148 * distance() + 0.220785) * (enclose() + -1.87642 * distance() + 3.78544) - (4.14409e-06 * distance() + 0.00011005) * (enclose() + 28.7602 * distance() + 60.3275) * (enclose() + 28.7602 * distance() + 60.3275))) + 0.5 * 0.124471 * exp(-0.896711 * enclose()) * (0.0945884 - 0.109367 * exp(-0.43584 * distance()) * pow(distance() , 0.170841))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00212598 * distance() + 0.103599) * (0.248995 - exp(-(0.0890148 * distance() + 0.220785) * (enclose() + -1.87642 * distance() + 3.78544) - (4.14409e-06 * distance() + 0.00011005) * (enclose() + 28.7602 * distance() + 60.3275) * (enclose() + 28.7602 * distance() + 60.3275))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0114797 * (1 - exp(-0.805055 * (enclose() + 0.334256))) * pow(width() , 0.00376327 * enclose() + 0.00873498) + 0.5 * (0.00642928 * exp(-0.875177 * enclose()) + 0.00642862 * exp(-0.872772 * enclose())) * pow(width() , -0.00569604 * enclose() + 0.105313)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0114797 * (1 - exp(-0.805055 * (enclose() + 0.334256))) * pow(width() , 0.00376327 * enclose() + 0.00873498) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal4,metal6
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.907615 - 1.07633 * distance()) + 0.0639662 / pow(distance() , 1.27249)) * 3.17097 * pow((width1() + width2()) / 2 , 0.0857507) * (0.0605884 * (thickness1() + thickness2()) / 2 + 0.026633) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.09)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.127553 * (1 - exp(-0.572693 * (distance() + 0.0818343))) * pow(width() , 0.0101185 * distance() + -0.0426699) * (0.0669248 * thickness() + 0.181061)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0253775 * (1 - exp(-0.664627 * (distance() + -0.0173218)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.131763 * pow(width() , 0.0330102) * (0.0818006 * thickness() + 0.165566)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00070319 * distance() + 0.150169) * (0.332842 - exp(-(-0.0484666 * distance() + 0.0175731) * (enclose() + 7.98065 * distance() + 15.4175) - (0.00013854 * distance() + 0.0189058) * (enclose() + 4.1608 * distance() + 6.10852) * (enclose() + 4.1608 * distance() + 6.10852)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.127553 * (1 - exp(-0.572693 * (distance() + 0.0818343))) * pow(width() , 0.0101185 * distance() + -0.0426699) * (0.0669248 * thickness() + 0.181061)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0715685 * (0.332842 - exp(0.467093 * (enclose() + 95.224) - 0.0202912 * (enclose() + 47.7165) * (enclose() + 47.7165)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0819597 * (1 - exp(-0.550346 * (distance() + 0.0918887))) * pow(width() , 0.0112541 * distance() + -0.0471704) * (0.0753514 * thickness() + 0.176098) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0819597 * (1 - exp(-0.550346 * (distance() + 0.0918887))) * pow(width() , 0.0112541 * distance() + -0.0471704) * (0.0753514 * thickness() + 0.176098) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0160743 * (1 - exp(-0.650612 * (distance() + -0.0197591))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.110141 * pow(width() , 0.0368673) * (0.0702882 * thickness() + 0.122698) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00085758 * distance() + 0.054657) * (0.638715 - exp(-(-0.103943 * distance() + 0.190109) * (enclose() + 2.54136 * distance() + 1.74314) - (0.00026259 * distance() + 0.0324205) * (enclose() + 2.64055 * distance() + -0.365315) * (enclose() + 2.64055 * distance() + -0.365315))) + 0.5 * 0.197218 * exp(-0.773602 * enclose()) * (0.0964812 - 0.108387 * exp(-0.323332 * distance()) * pow(distance() , 0.139703))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00085758 * distance() + 0.054657) * (0.638715 - exp(-(-0.103943 * distance() + 0.190109) * (enclose() + 2.54136 * distance() + 1.74314) - (0.00026259 * distance() + 0.0324205) * (enclose() + 2.64055 * distance() + -0.365315) * (enclose() + 2.64055 * distance() + -0.365315))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0165048 * (1 - exp(-0.675232 * (enclose() + 0.407843))) * pow(width() , 0.00287883 * enclose() + 0.0168803) + 0.5 * (0.00932855 * exp(-0.713261 * enclose()) + 0.00932954 * exp(-0.711229 * enclose())) * pow(width() , -0.00130691 * enclose() + 0.0719492)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0165048 * (1 - exp(-0.675232 * (enclose() + 0.407843))) * pow(width() , 0.00287883 * enclose() + 0.0168803) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal4,metal7
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.90436 - 0.865316 * distance()) + 0.0907489 / pow(distance() , 1.19769)) * 3.07328 * pow((width1() + width2()) / 2 , 0.100649) * (0.0526885 * (thickness1() + thickness2()) / 2 + 0.0250214) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.09)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal4,metal7
//
CAPACITANCE CROSSOVER FRINGE metal4 metal7 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.100907 * (1 - exp(-0.432789 * (distance() + 0.156697))) * pow(width() , 0.0136673 * distance() + -0.0618522) * (0.0672528 * thickness() + 0.177579)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0193779 * (1 - exp(-0.529857 * (distance() + -0.00192647)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.119314 * pow(width() , 0.0422325) * (0.0706168 * thickness() + 0.139589)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-4.49037e-08 * distance() + 0.0616402) * (0.583045 - exp(-(0.0266556 * distance() + 0.471786) * (enclose() + -0.0216193 * distance() + 1.80241) - (0.0167619 * distance() + -0.0562887) * (enclose() + -0.231235 * distance() + 2.69833) * (enclose() + -0.231235 * distance() + 2.69833)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.100907 * (1 - exp(-0.432789 * (distance() + 0.156697))) * pow(width() , 0.0136673 * distance() + -0.0618522) * (0.0672528 * thickness() + 0.177579)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0308199 * (0.583045 - exp(-0.738342 * (enclose() + 1.58622) - 0.11133 * (enclose() + 0.38598) * (enclose() + 0.38598)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal4,metal7
//
CAPACITANCE CROSSOVER FRINGE metal4 metal1 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0992732 * (1 - exp(-0.429213 * (distance() + 0.161117))) * pow(width() , 0.014031 * distance() + -0.064319) * (0.0571139 * thickness() + 0.183182) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0992732 * (1 - exp(-0.429213 * (distance() + 0.161117))) * pow(width() , 0.014031 * distance() + -0.064319) * (0.0571139 * thickness() + 0.183182) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.019214 * (1 - exp(-0.529198 * (distance() + -0.00161571))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.119623 * pow(width() , 0.0424731) * (0.0614127 * thickness() + 0.1412) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00089058 * distance() + 0.0649874) * (0.624256 - exp(-(-0.142292 * distance() + 0.156183) * (enclose() + 3.81339 * distance() + 2.39583) - (5.60955e-05 * distance() + 0.0207183) * (enclose() + 4.97009 * distance() + -0.241295) * (enclose() + 4.97009 * distance() + -0.241295))) + 0.5 * 0.252253 * exp(-0.701106 * enclose()) * (0.0996996 - 0.106541 * exp(-0.249367 * distance()) * pow(distance() , 0.110522))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00089058 * distance() + 0.0649874) * (0.624256 - exp(-(-0.142292 * distance() + 0.156183) * (enclose() + 3.81339 * distance() + 2.39583) - (5.60955e-05 * distance() + 0.0207183) * (enclose() + 4.97009 * distance() + -0.241295) * (enclose() + 4.97009 * distance() + -0.241295))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0197304 * (1 - exp(-0.598603 * (enclose() + 0.444999))) * pow(width() , 0.00241612 * enclose() + 0.0252171) + 0.5 * (0.0115242 * exp(-0.617351 * enclose()) + 0.0115241 * exp(-0.616699 * enclose())) * pow(width() , 0.00133281 * enclose() + 0.0665627)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0197304 * (1 - exp(-0.598603 * (enclose() + 0.444999))) * pow(width() , 0.00241612 * enclose() + 0.0252171) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal4,metal8
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.31665 - 0.746058 * distance()) + 0.077968 / pow(distance() , 1.1402)) * 4.68879 * pow((width1() + width2()) / 2 , 0.109389) * (0.0463331 * (thickness1() + thickness2()) / 2 + 0.0225818) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.09)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal4,metal8
//
CAPACITANCE CROSSOVER FRINGE metal4 metal8 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0817304 * (1 - exp(-0.361496 * (distance() + 0.235279))) * pow(width() , 0.0152751 * distance() + -0.0699926) * (0.0677252 * thickness() + 0.175919)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0153504 * (1 - exp(-0.453491 * (distance() + 0.0253511)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.108795 * pow(width() , 0.0497566) * (0.0637648 * thickness() + 0.121166)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00020671 * distance() + 0.036498) * (0.836309 - exp(-(0.129286 * distance() + 0.197466) * (enclose() + 0.176126 * distance() + 1.34143) - (-0.00270712 * distance() + -0.0127951) * (enclose() + 0.625278 * distance() + 3.51355) * (enclose() + 0.625278 * distance() + 3.51355)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0817304 * (1 - exp(-0.361496 * (distance() + 0.235279))) * pow(width() , 0.0152751 * distance() + -0.0699926) * (0.0677252 * thickness() + 0.175919)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0172155 * (0.836309 - exp(-1.49033 * (enclose() + 3.10269) - -0.0398663 * (enclose() + 9.76633) * (enclose() + 9.76633)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal4,metal8
//
CAPACITANCE CROSSOVER FRINGE metal4 metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.113477 * (1 - exp(-0.363666 * (distance() + 0.23458))) * pow(width() , 0.015336 * distance() + -0.0714954) * (0.0470959 * thickness() + 0.18747) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.113477 * (1 - exp(-0.363666 * (distance() + 0.23458))) * pow(width() , 0.015336 * distance() + -0.0714954) * (0.0470959 * thickness() + 0.18747) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0217451 * (1 - exp(-0.458174 * (distance() + 0.0242022))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.125727 * pow(width() , 0.0485107) * (0.0584455 * thickness() + 0.155794) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00108538 * distance() + 0.08977) * (0.498007 - exp(-(-0.106883 * distance() + 0.141251) * (enclose() + 4.75741 * distance() + 4.32253) - (3.30454e-05 * distance() + 0.0140841) * (enclose() + 5.84985 * distance() + -0.152747) * (enclose() + 5.84985 * distance() + -0.152747))) + 0.5 * 0.301579 * exp(-0.636062 * enclose()) * (0.103641 - 0.105922 * exp(-0.196523 * distance()) * pow(distance() , 0.0848885))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00108538 * distance() + 0.08977) * (0.498007 - exp(-(-0.106883 * distance() + 0.141251) * (enclose() + 4.75741 * distance() + 4.32253) - (3.30454e-05 * distance() + 0.0140841) * (enclose() + 5.84985 * distance() + -0.152747) * (enclose() + 5.84985 * distance() + -0.152747))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0223717 * (1 - exp(-0.540443 * (enclose() + 0.474297))) * pow(width() , 0.00201386 * enclose() + 0.0330083) + 0.5 * (0.0135348 * exp(-0.544655 * enclose()) + 0.013535 * exp(-0.541393 * enclose())) * pow(width() , 0.00364656 * enclose() + 0.0670109)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0223717 * (1 - exp(-0.540443 * (enclose() + 0.474297))) * pow(width() , 0.00201386 * enclose() + 0.0330083) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.99167 - 1.4607 * distance()) + 0.0515348 / pow(distance() , 1.31369)) * 3.39067 * pow((width1() + width2()) / 2 , 0.0762155) * (0.0687663 * (thickness1() + thickness2()) / 2 + 0.024217) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.91)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.201602 * (1 - exp(-0.995533 * (distance() + 0.0416648))) * pow(width() , 0.00386953 * distance() + -0.0100288) * (0.0582485 * thickness() + 0.194842)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0430069 * (1 - exp(-1.03906 * (distance() + 0.00122142)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.161339 * pow(width() , 0.0277761) * (0.104935 * thickness() + 0.234485)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00173237 * distance() + 0.94789) * (0.0861757 - exp(-(-0.143048 * distance() + 0.155951) * (enclose() + 3.2378 * distance() + 7.76077) - (0.00111893 * distance() + 0.0545071) * (enclose() + 2.42081 * distance() + 4.55164) * (enclose() + 2.42081 * distance() + 4.55164)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.201602 * (1 - exp(-0.995533 * (distance() + 0.0416648))) * pow(width() , 0.00386953 * distance() + -0.0100288) * (0.0582485 * thickness() + 0.194842)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.482607 * (0.0861757 - exp(1.27453 * (enclose() + 40.1388) - 0.0656964 * (enclose() + 28.7597) * (enclose() + 28.7597)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0460034 * (1 - exp(-0.832551 * (distance() + 0.06033))) * pow(width() , 0.00708421 * distance() + -0.0131789) * (0.132652 * thickness() + 0.145061) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0460034 * (1 - exp(-0.832551 * (distance() + 0.06033))) * pow(width() , 0.00708421 * distance() + -0.0131789) * (0.132652 * thickness() + 0.145061) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0086263 * (1 - exp(-0.921978 * (distance() + -0.0126644))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0843905 * pow(width() , 0.0474954) * (0.0906078 * thickness() + 0.0727992) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (2.75401e-05 * distance() + 0.0194711) * (0.824705 - exp(-(0.15507 * distance() + -0.167708) * (enclose() + -0.275184 * distance() + 2.15082) - (0.0108221 * distance() + 0.0676472) * (enclose() + -0.512214 * distance() + 2.3962) * (enclose() + -0.512214 * distance() + 2.3962))) + 0.5 * 0.10727 * exp(-0.677174 * enclose()) * (0.101872 - 0.0971438 * exp(-0.301049 * distance()) * pow(distance() , 0.0861051))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (2.75401e-05 * distance() + 0.0194711) * (0.824705 - exp(-(0.15507 * distance() + -0.167708) * (enclose() + -0.275184 * distance() + 2.15082) - (0.0108221 * distance() + 0.0676472) * (enclose() + -0.512214 * distance() + 2.3962) * (enclose() + -0.512214 * distance() + 2.3962))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.00908189 * (1 - exp(-0.588034 * (enclose() + 0.307189))) * pow(width() , 0.00677465 * enclose() + -0.00470723) + 0.5 * (0.00610327 * exp(-0.667609 * enclose()) + 0.00610304 * exp(-0.669595 * enclose())) * pow(width() , -0.0049498 * enclose() + 0.141788)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.00908189 * (1 - exp(-0.588034 * (enclose() + 0.307189))) * pow(width() , 0.00677465 * enclose() + -0.00470723) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal5,metal7
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.945108 - 0.956254 * distance()) + 0.0828238 / pow(distance() , 1.20862)) * 3.1637 * pow((width1() + width2()) / 2 , 0.094348) * (0.0552355 * (thickness1() + thickness2()) / 2 + 0.0250921) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.91)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal5,metal7
//
CAPACITANCE CROSSOVER FRINGE metal5 metal7 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.138921 * (1 - exp(-0.512216 * (distance() + 0.125936))) * pow(width() , 0.0109735 * distance() + -0.0450234) * (0.0591941 * thickness() + 0.185189)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0276196 * (1 - exp(-0.59962 * (distance() + 0.00147672)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.137086 * pow(width() , 0.0398255) * (0.0777164 * thickness() + 0.177694)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00027406 * distance() + 0.162524) * (0.323796 - exp(-(-0.0258115 * distance() + 0.00390305) * (enclose() + 11.8559 * distance() + 26.151) - (0.00012191 * distance() + 0.0153777) * (enclose() + 4.04989 * distance() + 7.69502) * (enclose() + 4.04989 * distance() + 7.69502)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.138921 * (1 - exp(-0.512216 * (distance() + 0.125936))) * pow(width() , 0.0109735 * distance() + -0.0450234) * (0.0591941 * thickness() + 0.185189)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0798917 * (0.323796 - exp(0.254212 * (enclose() + 144.71) - 0.0165968 * (enclose() + 48.1939) * (enclose() + 48.1939)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal5,metal7
//
CAPACITANCE CROSSOVER FRINGE metal5 metal1 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0668452 * (1 - exp(-0.485875 * (distance() + 0.14198))) * pow(width() , 0.0123611 * distance() + -0.0495864) * (0.0799731 * thickness() + 0.172965) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0668452 * (1 - exp(-0.485875 * (distance() + 0.14198))) * pow(width() , 0.0123611 * distance() + -0.0495864) * (0.0799731 * thickness() + 0.172965) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.012889 * (1 - exp(-0.582729 * (distance() + -0.00049403))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.101046 * pow(width() , 0.0455917) * (0.0661062 * thickness() + 0.106675) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (2.61506e-05 * distance() + 0.0339035) * (0.699558 - exp(-(0.0425453 * distance() + 0.11508) * (enclose() + -0.0922529 * distance() + 2.03074) - (0.0123382 * distance() + 0.0010489) * (enclose() + -0.368055 * distance() + 3.17641) * (enclose() + -0.368055 * distance() + 3.17641))) + 0.5 * 0.174174 * exp(-0.599107 * enclose()) * (0.102199 - 0.101726 * exp(-0.239581 * distance()) * pow(distance() , 0.0853244))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (2.61506e-05 * distance() + 0.0339035) * (0.699558 - exp(-(0.0425453 * distance() + 0.11508) * (enclose() + -0.0922529 * distance() + 2.03074) - (0.0123382 * distance() + 0.0010489) * (enclose() + -0.368055 * distance() + 3.17641) * (enclose() + -0.368055 * distance() + 3.17641))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0134477 * (1 - exp(-0.512578 * (enclose() + 0.400197))) * pow(width() , 0.00493285 * enclose() + 0.00890924) + 0.5 * (0.00872547 * exp(-0.565231 * enclose()) + 0.00872607 * exp(-0.565111 * enclose())) * pow(width() , -0.00233443 * enclose() + 0.0963618)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0134477 * (1 - exp(-0.512578 * (enclose() + 0.400197))) * pow(width() , 0.00493285 * enclose() + 0.00890924) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal5,metal8
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.31417 - 0.737214 * distance()) + 0.0786834 / pow(distance() , 1.13879)) * 4.69597 * pow((width1() + width2()) / 2 , 0.11021) * (0.0459303 * (thickness1() + thickness2()) / 2 + 0.0225005) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.91)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal5,metal8
//
CAPACITANCE CROSSOVER FRINGE metal5 metal8 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.109488 * (1 - exp(-0.359022 * (distance() + 0.235944))) * pow(width() , 0.0153698 * distance() + -0.0719297) * (0.0570005 * thickness() + 0.181624)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0207487 * (1 - exp(-0.452139 * (distance() + 0.0237724)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.123504 * pow(width() , 0.0484375) * (0.0647861 * thickness() + 0.14821)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00020267 * distance() + 0.0527195) * (0.744995 - exp(-(0.0677033 * distance() + 0.140787) * (enclose() + 0.507266 * distance() + 1.51062) - (-1.12705e-05 * distance() + -0.00058508) * (enclose() + -7.90006 * distance() + -0.332081) * (enclose() + -7.90006 * distance() + -0.332081)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.109488 * (1 - exp(-0.359022 * (distance() + 0.235944))) * pow(width() , 0.0153698 * distance() + -0.0719297) * (0.0570005 * thickness() + 0.181624)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0253464 * (0.744995 - exp(-0.81782 * (enclose() + 6.58328) - -0.000697785 * (enclose() + -79.3327) * (enclose() + -79.3327)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal5,metal8
//
CAPACITANCE CROSSOVER FRINGE metal5 metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0856866 * (1 - exp(-0.353403 * (distance() + 0.245226))) * pow(width() , 0.0157665 * distance() + -0.0731055) * (0.0570165 * thickness() + 0.181775) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0856866 * (1 - exp(-0.353403 * (distance() + 0.245226))) * pow(width() , 0.0157665 * distance() + -0.0731055) * (0.0570165 * thickness() + 0.181775) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0161942 * (1 - exp(-0.448889 * (distance() + 0.0245671))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.111828 * pow(width() , 0.0503786) * (0.0564524 * thickness() + 0.12768) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (8.02923e-05 * distance() + 0.0415932) * (0.694159 - exp(-(0.0314857 * distance() + 0.189626) * (enclose() + -0.0862843 * distance() + 2.04956) - (0.0100458 * distance() + -0.0130716) * (enclose() + -0.348026 * distance() + 2.97694) * (enclose() + -0.348026 * distance() + 2.97694))) + 0.5 * 0.239608 * exp(-0.542401 * enclose()) * (0.106775 - 0.10385 * exp(-0.170584 * distance()) * pow(distance() , 0.0634202))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (8.02923e-05 * distance() + 0.0415932) * (0.694159 - exp(-(0.0314857 * distance() + 0.189626) * (enclose() + -0.0862843 * distance() + 2.04956) - (0.0100458 * distance() + -0.0130716) * (enclose() + -0.348026 * distance() + 2.97694) * (enclose() + -0.348026 * distance() + 2.97694))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0168496 * (1 - exp(-0.45374 * (enclose() + 0.449985))) * pow(width() , 0.00392773 * enclose() + 0.0207804) + 0.5 * (0.01108 * exp(-0.489832 * enclose()) + 0.0110801 * exp(-0.487911 * enclose())) * pow(width() , -0.00038512 * enclose() + 0.0838338)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0168496 * (1 - exp(-0.45374 * (enclose() + 0.449985))) * pow(width() , 0.00392773 * enclose() + 0.0207804) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal6,metal7
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.00141 - 1.39983 * distance()) + 0.0635973 / pow(distance() , 1.2632)) * 3.43003 * pow((width1() + width2()) / 2 , 0.0816956) * (0.0627906 * (thickness1() + thickness2()) / 2 + 0.0223896) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.73)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal6,metal7
//
CAPACITANCE CROSSOVER FRINGE metal6 metal7 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.21228 * (1 - exp(-0.967693 * (distance() + 0.0548417))) * pow(width() , 0.0035682 * distance() + -0.00556713) * (0.0564561 * thickness() + 0.191775)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0443993 * (1 - exp(-0.998083 * (distance() + 0.0115152)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.164546 * pow(width() , 0.0338479) * (0.105201 * thickness() + 0.240113)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00025223 * distance() + 0.115034) * (0.735048 - exp(-(-0.0421124 * distance() + 2.00265) * (enclose() + 0.00238534 * distance() + 0.593676) - (0.0461427 * distance() + -0.436638) * (enclose() + -0.363359 * distance() + 1.50152) * (enclose() + -0.363359 * distance() + 1.50152)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.21228 * (1 - exp(-0.967693 * (distance() + 0.0548417))) * pow(width() , 0.0035682 * distance() + -0.00556713) * (0.0564561 * thickness() + 0.191775)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0587782 * (0.735048 - exp(-1.58153 * (enclose() + 0.617529) - 0.024789 * (enclose() + -2.13207) * (enclose() + -2.13207)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal6,metal7
//
CAPACITANCE CROSSOVER FRINGE metal6 metal1 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0379086 * (1 - exp(-0.799033 * (distance() + 0.0810758))) * pow(width() , 0.0070534 * distance() + -0.00803844) * (0.138732 * thickness() + 0.142387) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0379086 * (1 - exp(-0.799033 * (distance() + 0.0810758))) * pow(width() , 0.0070534 * distance() + -0.00803844) * (0.138732 * thickness() + 0.142387) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0070586 * (1 - exp(-0.880312 * (distance() + -9.13139e-05))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0780366 * pow(width() , 0.0553756) * (0.084824 * thickness() + 0.0636341) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (1.90251e-05 * distance() + 0.0159256) * (0.816576 - exp(-(0.115299 * distance() + -0.126892) * (enclose() + -0.280498 * distance() + 2.34789) - (0.00768969 * distance() + 0.0491722) * (enclose() + -0.53174 * distance() + 2.64446) * (enclose() + -0.53174 * distance() + 2.64446))) + 0.5 * 0.0969135 * exp(-0.543328 * enclose()) * (0.106383 - 0.088725 * exp(-0.208771 * distance()) * pow(distance() , 0.0200203))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (1.90251e-05 * distance() + 0.0159256) * (0.816576 - exp(-(0.115299 * distance() + -0.126892) * (enclose() + -0.280498 * distance() + 2.34789) - (0.00768969 * distance() + 0.0491722) * (enclose() + -0.53174 * distance() + 2.64446) * (enclose() + -0.53174 * distance() + 2.64446))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.00759596 * (1 - exp(-0.451368 * (enclose() + 0.263408))) * pow(width() , 0.0102695 * enclose() + -0.0262367) + 0.5 * (0.0057346 * exp(-0.54131 * enclose()) + 0.00573465 * exp(-0.54168 * enclose())) * pow(width() , -0.00409523 * enclose() + 0.167722)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.73)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.00759596 * (1 - exp(-0.451368 * (enclose() + 0.263408))) * pow(width() , 0.0102695 * enclose() + -0.0262367) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.73)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal6,metal8
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-0.919593 - 0.840326 * distance()) + 0.10888 / pow(distance() , 1.15385)) * 3.07858 * pow((width1() + width2()) / 2 , 0.102785) * (0.0493696 * (thickness1() + thickness2()) / 2 + 0.0231115) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.73)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal6,metal8
//
CAPACITANCE CROSSOVER FRINGE metal6 metal8 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.141185 * (1 - exp(-0.441876 * (distance() + 0.183004))) * pow(width() , 0.0123737 * distance() + -0.0516135) * (0.0540373 * thickness() + 0.18673)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.027753 * (1 - exp(-0.527182 * (distance() + 0.0217891)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.13843 * pow(width() , 0.0464822) * (0.0738214 * thickness() + 0.17957)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00025374 * distance() + 0.173299) * (0.294167 - exp(-(-0.082257 * distance() + 0.0835225) * (enclose() + 5.9508 * distance() + 10.568) - (2.30942e-05 * distance() + 0.0154773) * (enclose() + 5.46002 * distance() + 3.89145) * (enclose() + 5.46002 * distance() + 3.89145)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.141185 * (1 - exp(-0.441876 * (distance() + 0.183004))) * pow(width() , 0.0123737 * distance() + -0.0516135) * (0.0540373 * thickness() + 0.18673)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0879182 * (0.294167 - exp(0.739048 * (enclose() + 70.076) - 0.0157082 * (enclose() + 58.4916) * (enclose() + 58.4916)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal6,metal8
//
CAPACITANCE CROSSOVER FRINGE metal6 metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0595046 * (1 - exp(-0.42137 * (distance() + 0.202636))) * pow(width() , 0.0134441 * distance() + -0.0536726) * (0.0775097 * thickness() + 0.173742) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0595046 * (1 - exp(-0.42137 * (distance() + 0.202636))) * pow(width() , 0.0134441 * distance() + -0.0536726) * (0.0775097 * thickness() + 0.173742) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0113534 * (1 - exp(-0.51233 * (distance() + 0.0224826))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0966089 * pow(width() , 0.0526616) * (0.0599474 * thickness() + 0.0994951) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-1.63288e-05 * distance() + 0.0281554) * (0.737285 - exp(-(0.038662 * distance() + 0.0735457) * (enclose() + -0.119178 * distance() + 2.29196) - (0.00804753 * distance() + 0.00392338) * (enclose() + -0.362551 * distance() + 3.09034) * (enclose() + -0.362551 * distance() + 3.09034))) + 0.5 * 0.170505 * exp(-0.481138 * enclose()) * (0.106245 - 0.0974418 * exp(-0.17191 * distance()) * pow(distance() , 0.0419281))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-1.63288e-05 * distance() + 0.0281554) * (0.737285 - exp(-(0.038662 * distance() + 0.0735457) * (enclose() + -0.119178 * distance() + 2.29196) - (0.00804753 * distance() + 0.00392338) * (enclose() + -0.362551 * distance() + 3.09034) * (enclose() + -0.362551 * distance() + 3.09034))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0120647 * (1 - exp(-0.39963 * (enclose() + 0.391671))) * pow(width() , 0.00670828 * enclose() + 0.00044532) + 0.5 * (0.00854224 * exp(-0.458533 * enclose()) + 0.00854253 * exp(-0.458319 * enclose())) * pow(width() , -0.00202169 * enclose() + 0.110383)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.73)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0120647 * (1 - exp(-0.39963 * (enclose() + 0.391671))) * pow(width() , 0.00670828 * enclose() + 0.00044532) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.73)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal1,metal7,metal8
//
CAPACITANCE NEARBODY metal7 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(-1.00329 - 1.19882 * distance()) + 0.078658 / pow(distance() , 1.20082)) * 3.34503 * pow((width1() + width2()) / 2 , 0.0884611) * (0.058001 * (thickness1() + thickness2()) / 2 + 0.0237627) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.55)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal1,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal7 metal8 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.197788 * (1 - exp(-0.762955 * (distance() + 0.0864749))) * pow(width() , 0.00542462 * distance() + -0.0118377) * (0.0551233 * thickness() + 0.193938)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0413804 * (1 - exp(-0.809761 * (distance() + 0.0172145)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.160294 * pow(width() , 0.0401397) * (0.0973068 * thickness() + 0.232422)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00438066 * distance() + 0.878561) * (0.0863546 - exp(-(-0.124513 * distance() + 0.138984) * (enclose() + 4.22726 * distance() + 10.2375) - (0.00033876 * distance() + 0.0301687) * (enclose() + 3.73265 * distance() + 5.43303) * (enclose() + 3.73265 * distance() + 5.43303)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.197788 * (1 - exp(-0.762955 * (distance() + 0.0864749))) * pow(width() , 0.00542462 * distance() + -0.0118377) * (0.0551233 * thickness() + 0.193938)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.461184 * (0.0863546 - exp(1.10615 * (enclose() + 52.5101) - 0.0335563 * (enclose() + 42.7595) * (enclose() + 42.7595)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal1,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal7 metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0364208 * (1 - exp(-0.659778 * (distance() + 0.120031))) * pow(width() , 0.00829992 * distance() + -0.0132819) * (0.122362 * thickness() + 0.149751) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0364208 * (1 - exp(-0.659778 * (distance() + 0.120031))) * pow(width() , 0.00829992 * distance() + -0.0132819) * (0.122362 * thickness() + 0.149751) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.55)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00678972 * (1 - exp(-0.738876 * (distance() + 0.0137679))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.55)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0780076 * pow(width() , 0.0587775) * (0.0716467 * thickness() + 0.0652003) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.55)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-3.70025e-06 * distance() + 0.0156327) * (0.797247 - exp(-(0.074789 * distance() + -0.0422242) * (enclose() + -0.252246 * distance() + 2.49164) - (0.00608411 * distance() + 0.0276714) * (enclose() + -0.491217 * distance() + 2.67721) * (enclose() + -0.491217 * distance() + 2.67721))) + 0.5 * 0.10215 * exp(-0.442655 * enclose()) * (0.108902 - 0.0867787 * exp(-0.153692 * distance()) * pow(distance() , -0.013197))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.55)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-3.70025e-06 * distance() + 0.0156327) * (0.797247 - exp(-(0.074789 * distance() + -0.0422242) * (enclose() + -0.252246 * distance() + 2.49164) - (0.00608411 * distance() + 0.0276714) * (enclose() + -0.491217 * distance() + 2.67721) * (enclose() + -0.491217 * distance() + 2.67721))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.55)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.00746636 * (1 - exp(-0.353136 * (enclose() + 0.254012))) * pow(width() , 0.0123419 * enclose() + -0.041475) + 0.5 * (0.0058954 * exp(-0.443891 * enclose()) + 0.00589595 * exp(-0.440838 * enclose())) * pow(width() , -0.00297474 * enclose() + 0.166899)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.55)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.00746636 * (1 - exp(-0.353136 * (enclose() + 0.254012))) * pow(width() , 0.0123419 * enclose() + -0.041475) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.55)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal3,metal4
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal2 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.894787 - 2.53585 * distance()) + 0.00979283 / pow(distance() , 1.75995)) * 3.61616 * pow((width1() + width2()) / 2 , 0.00979838) * (0.112642 * (thickness1() + thickness2()) / 2 + 0.0262926) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal4 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.148443 * (1 - exp(-1.73565 * (distance() + -0.0512697))) * pow(width() , 0.00174932 * distance() + -0.0127754) * (0.105752 * thickness() + 0.163145)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0301002 * (1 - exp(-1.79856 * (distance() + -0.0551526)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.135378 * pow(width() , 0) * (0.165368 * thickness() + 0.158898)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00170802 * distance() + 0.338379) * (0.181805 - exp(-(0.357193 * distance() + 0.938888) * (enclose() + -0.0244965 * distance() + 1.73064) - (0.00017824 * distance() + -0.00267139) * (enclose() + 7.35918 * distance() + -5.2896) * (enclose() + 7.35918 * distance() + -5.2896)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.148443 * (1 - exp(-1.73565 * (distance() + -0.0512697))) * pow(width() , 0.00174932 * distance() + -0.0127754) * (0.105752 * thickness() + 0.163145)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.160649 * (0.181805 - exp(-4.51082 * (enclose() + 1.48567) - -0.00088899 * (enclose() + 68.3022) * (enclose() + 68.3022)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal3,metal4
//
CAPACITANCE CROSSOVER FRINGE metal3 metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.133374 * (1 - exp(-1.74201 * (distance() + -0.0475697))) * pow(width() , 0.00167656 * distance() + -0.0118058) * (0.0607046 * thickness() + 0.1949) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.133374 * (1 - exp(-1.74201 * (distance() + -0.0475697))) * pow(width() , 0.00167656 * distance() + -0.0118058) * (0.0607046 * thickness() + 0.1949) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0290058 * (1 - exp(-1.80734 * (distance() + -0.052371))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.13721 * pow(width() , -0.00376195) * (0.0989134 * thickness() + 0.172547) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-5.54118e-06 * distance() + 0.30655) * (0.186364 - exp(-(0.096791 * distance() + 1.7977) * (enclose() + -0.0620337 * distance() + 1.34057) - (0.137533 * distance() + -0.221958) * (enclose() + -0.263742 * distance() + 1.98866) * (enclose() + -0.263742 * distance() + 1.98866))) + 0.5 * 0.12649 * exp(-2.68131 * enclose()) * (0.0661193 - 0.141469 * exp(-0.968614 * distance()) * pow(distance() , 0.36318))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-5.54118e-06 * distance() + 0.30655) * (0.186364 - exp(-(0.096791 * distance() + 1.7977) * (enclose() + -0.0620337 * distance() + 1.34057) - (0.137533 * distance() + -0.221958) * (enclose() + -0.263742 * distance() + 1.98866) * (enclose() + -0.263742 * distance() + 1.98866))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0288023 * (1 - exp(-2.39764 * (enclose() + 0.297665))) * pow(width() , 0.00017456 * enclose() + -0.00303968) + 0.5 * (0.00384032 * exp(-2.32239 * enclose()) + 0.00420528 * exp(-2.3019 * enclose())) * pow(width() , 0.0214628 * enclose() + -0.0127855)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0288023 * (1 - exp(-2.39764 * (enclose() + 0.297665))) * pow(width() , 0.00017456 * enclose() + -0.00303968) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal3,metal5
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal2 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.882227 - 1.81086 * distance()) + 0.025131 / pow(distance() , 1.51878)) * 3.4249 * pow((width1() + width2()) / 2 , 0.043014) * (0.083507 * (thickness1() + thickness2()) / 2 + 0.0259276) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.085049 * (1 - exp(-1.07712 * (distance() + -0.0155171))) * pow(width() , 0.00457102 * distance() + -0.0144836) * (0.128914 * thickness() + 0.144555)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0160931 * (1 - exp(-1.16454 * (distance() + -0.0468507)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.10546 * pow(width() , 0.0184983) * (0.129674 * thickness() + 0.105974)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00037787 * distance() + 0.0450736) * (0.737592 - exp(-(-0.467592 * distance() + 0.402869) * (enclose() + 2.01313 * distance() + 0.26407) - (0.00072769 * distance() + 0.112682) * (enclose() + 2.73393 * distance() + -0.229205) * (enclose() + 2.73393 * distance() + -0.229205)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.085049 * (1 - exp(-1.07712 * (distance() + -0.0155171))) * pow(width() , 0.00457102 * distance() + -0.0144836) * (0.128914 * thickness() + 0.144555)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0206474 * (0.737592 - exp(4.27305 * (enclose() + 20.3954) - 0.119959 * (enclose() + 27.1101) * (enclose() + 27.1101)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal3,metal5
//
CAPACITANCE CROSSOVER FRINGE metal3 metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.160405 * (1 - exp(-1.20447 * (distance() + -0.00688718))) * pow(width() , 0.00298939 * distance() + -0.0147603) * (0.0343938 * thickness() + 0.214626) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.160405 * (1 - exp(-1.20447 * (distance() + -0.00688718))) * pow(width() , 0.00298939 * distance() + -0.0147603) * (0.0343938 * thickness() + 0.214626) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0363018 * (1 - exp(-1.26542 * (distance() + -0.0270404))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.154768 * pow(width() , 0.00756787) * (0.0682735 * thickness() + 0.209494) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00014063 * distance() + 0.113381) * (0.634165 - exp(-(-0.0214543 * distance() + 2.31122) * (enclose() + -0.0443179 * distance() + 0.771124) - (0.0531649 * distance() + -0.399275) * (enclose() + -0.382085 * distance() + 1.90577) * (enclose() + -0.382085 * distance() + 1.90577))) + 0.5 * 0.177716 * exp(-1.95311 * enclose()) * (0.0822686 - 0.128018 * exp(-0.581867 * distance()) * pow(distance() , 0.232793))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00014063 * distance() + 0.113381) * (0.634165 - exp(-(-0.0214543 * distance() + 2.31122) * (enclose() + -0.0443179 * distance() + 0.771124) - (0.0531649 * distance() + -0.399275) * (enclose() + -0.382085 * distance() + 1.90577) * (enclose() + -0.382085 * distance() + 1.90577))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0363729 * (1 - exp(-1.82136 * (enclose() + 0.355878))) * pow(width() , -0.0001442 * enclose() + 0.00937282) + 0.5 * (0.00779598 * exp(-1.31498 * enclose()) + 0.00609134 * exp(-2.07658 * enclose())) * pow(width() , 0.0304917 * enclose() + 0.00278423)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0363729 * (1 - exp(-1.82136 * (enclose() + 0.355878))) * pow(width() , -0.0001442 * enclose() + 0.00937282) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal3,metal6
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.932387 - 1.56125 * distance()) + 0.0401258 / pow(distance() , 1.38899)) * 3.41873 * pow((width1() + width2()) / 2 , 0.0633978) * (0.0730556 * (thickness1() + thickness2()) / 2 + 0.0243156) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0610727 * (1 - exp(-0.899392 * (distance() + 0.0250002))) * pow(width() , 0.00622484 * distance() + -0.0158075) * (0.140646 * thickness() + 0.138285)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0113227 * (1 - exp(-0.98927 * (distance() + -0.0297276)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0923951 * pow(width() , 0.0341083) * (0.115489 * thickness() + 0.0828249)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00017894 * distance() + 0.0260837) * (0.875646 - exp(-(-0.656754 * distance() + 0.30382) * (enclose() + 1.70687 * distance() + -0.151628) - (0.00029882 * distance() + 0.133216) * (enclose() + 2.83243 * distance() + -0.336428) * (enclose() + 2.83243 * distance() + -0.336428)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0610727 * (1 - exp(-0.899392 * (distance() + 0.0250002))) * pow(width() , 0.00622484 * distance() + -0.0158075) * (0.140646 * thickness() + 0.138285)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0121472 * (0.875646 - exp(6.26372 * (enclose() + 16.9171) - 0.136204 * (enclose() + 27.9879) * (enclose() + 27.9879)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal3,metal6
//
CAPACITANCE CROSSOVER FRINGE metal3 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.172981 * (1 - exp(-1.04335 * (distance() + 0.0257158))) * pow(width() , 0.00366488 * distance() + -0.0138471) * (0.0285222 * thickness() + 0.222556) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.172981 * (1 - exp(-1.04335 * (distance() + 0.0257158))) * pow(width() , 0.00366488 * distance() + -0.0138471) * (0.0285222 * thickness() + 0.222556) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0399233 * (1 - exp(-1.09716 * (distance() + -0.00785821))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.162376 * pow(width() , 0.0187791) * (0.0640685 * thickness() + 0.225551) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0006896 * distance() + 0.121859) * (0.609608 - exp(-(0.0973939 * distance() + 0.582591) * (enclose() + 0.0369777 * distance() + 0.339647) - (0.0232361 * distance() + 0.00674358) * (enclose() + -0.560691 * distance() + 5.18172) * (enclose() + -0.560691 * distance() + 5.18172))) + 0.5 * 0.216157 * exp(-1.5811 * enclose()) * (0.0898954 - 0.117704 * exp(-0.410476 * distance()) * pow(distance() , 0.162144))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0006896 * distance() + 0.121859) * (0.609608 - exp(-(0.0973939 * distance() + 0.582591) * (enclose() + 0.0369777 * distance() + 0.339647) - (0.0232361 * distance() + 0.00674358) * (enclose() + -0.560691 * distance() + 5.18172) * (enclose() + -0.560691 * distance() + 5.18172))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0403582 * (1 - exp(-1.57976 * (enclose() + 0.389701))) * pow(width() , -0.0004993 * enclose() + 0.0208905) + 0.5 * (0.0114186 * exp(-0.994883 * enclose()) + 0.00688565 * exp(-2.05975 * enclose())) * pow(width() , 0.0400469 * enclose() + 0.0194585)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0403582 * (1 - exp(-1.57976 * (enclose() + 0.389701))) * pow(width() , -0.0004993 * enclose() + 0.0208905) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal3,metal7
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-0.993626 - 1.44233 * distance()) + 0.052404 / pow(distance() , 1.31179)) * 3.38005 * pow((width1() + width2()) / 2 , 0.0735176) * (0.0691978 * (thickness1() + thickness2()) / 2 + 0.0236487) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal3,metal7
//
CAPACITANCE CROSSOVER FRINGE metal3 metal7 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0478781 * (1 - exp(-0.826678 * (distance() + 0.0550193))) * pow(width() , 0.00676528 * distance() + -0.0124944) * (0.147509 * thickness() + 0.135081)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00878038 * (1 - exp(-0.911184 * (distance() + -0.0142844)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.084014 * pow(width() , 0.044854) * (0.104034 * thickness() + 0.0699199)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (4.03667e-05 * distance() + 0.0265396) * (0.617431 - exp(-(0.0134751 * distance() + 0.101808) * (enclose() + 0.2264 * distance() + 2.07095) - (0.024881 * distance() + 0.011654) * (enclose() + -0.259851 * distance() + 3.17994) * (enclose() + -0.259851 * distance() + 3.17994)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0478781 * (1 - exp(-0.826678 * (distance() + 0.0550193))) * pow(width() , 0.00676528 * distance() + -0.0124944) * (0.147509 * thickness() + 0.135081)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0134716 * (0.617431 - exp(-0.236559 * (enclose() + 4.33495) - 0.260464 * (enclose() + 0.58143) * (enclose() + 0.58143)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal3,metal7
//
CAPACITANCE CROSSOVER FRINGE metal3 metal2 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.181671 * (1 - exp(-0.97702 * (distance() + 0.0476261))) * pow(width() , 0.00371082 * distance() + -0.0100101) * (0.027029 * thickness() + 0.224842) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.181671 * (1 - exp(-0.97702 * (distance() + 0.0476261))) * pow(width() , 0.00371082 * distance() + -0.0100101) * (0.027029 * thickness() + 0.224842) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0420739 * (1 - exp(-1.01988 * (distance() + 0.00662434))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.171545 * pow(width() , 0.0270911) * (0.062386 * thickness() + 0.228203) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00052965 * distance() + 0.0932333) * (0.835975 - exp(-(0.0822914 * distance() + 0.98551) * (enclose() + -0.0754288 * distance() + 0.974787) - (0.0133555 * distance() + -0.0330983) * (enclose() + -0.719979 * distance() + 5.18564) * (enclose() + -0.719979 * distance() + 5.18564))) + 0.5 * 0.245234 * exp(-1.37297 * enclose()) * (0.0945675 - 0.1125 * exp(-0.326148 * distance()) * pow(distance() , 0.12233))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00052965 * distance() + 0.0932333) * (0.835975 - exp(-(0.0822914 * distance() + 0.98551) * (enclose() + -0.0754288 * distance() + 0.974787) - (0.0133555 * distance() + -0.0330983) * (enclose() + -0.719979 * distance() + 5.18564) * (enclose() + -0.719979 * distance() + 5.18564))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0428017 * (1 - exp(-1.44612 * (enclose() + 0.412849))) * pow(width() , -0.0008395 * enclose() + 0.0293139) + 0.5 * (0.0129134 * exp(-0.793843 * enclose()) + 0.00881574 * exp(-1.92523 * enclose())) * pow(width() , 0.0454202 * enclose() + 0.0322168)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0428017 * (1 - exp(-1.44612 * (enclose() + 0.412849))) * pow(width() , -0.0008395 * enclose() + 0.0293139) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal3,metal8
//
CAPACITANCE NEARBODY metal3 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.00091 - 1.37074 * distance()) + 0.0671354 / pow(distance() , 1.25411)) * 3.42611 * pow((width1() + width2()) / 2 , 0.0797313) * (0.0619732 * (thickness1() + thickness2()) / 2 + 0.0214406) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal3,metal8
//
CAPACITANCE CROSSOVER FRINGE metal3 metal8 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0380838 * (1 - exp(-0.788671 * (distance() + 0.0777068))) * pow(width() , 0.00676184 * distance() + -0.00764001) * (0.153459 * thickness() + 0.13255)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0069383 * (1 - exp(-0.863876 * (distance() + -0.00034691)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0765884 * pow(width() , 0.0530137) * (0.0952307 * thickness() + 0.0597471)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-1.8119e-05 * distance() + 0.0197159) * (0.661873 - exp(-(0.00243425 * distance() + 0.0431847) * (enclose() + 1.00968 * distance() + 2.05452) - (0.014814 * distance() + 0.014326) * (enclose() + -0.216884 * distance() + 3.56087) * (enclose() + -0.216884 * distance() + 3.56087)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0380838 * (1 - exp(-0.788671 * (distance() + 0.0777068))) * pow(width() , 0.00676184 * distance() + -0.00764001) * (0.153459 * thickness() + 0.13255)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00976736 * (0.661873 - exp(-0.0675272 * (enclose() + 12.1513) - 0.162466 * (enclose() + 1.39203) * (enclose() + 1.39203)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal3,metal8
//
CAPACITANCE CROSSOVER FRINGE metal3 metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.18951 * (1 - exp(-0.94203 * (distance() + 0.0641573))) * pow(width() , 0.00346821 * distance() + -0.00477165) * (0.0260394 * thickness() + 0.224962) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.18951 * (1 - exp(-0.94203 * (distance() + 0.0641573))) * pow(width() , 0.00346821 * distance() + -0.00477165) * (0.0260394 * thickness() + 0.224962) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0436917 * (1 - exp(-0.972721 * (distance() + 0.018976))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.167911 * pow(width() , 0.03415) * (0.0674303 * thickness() + 0.244358) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00055349 * distance() + 0.112407) * (0.722734 - exp(-(-0.0235586 * distance() + 1.66987) * (enclose() + -0.035551 * distance() + 0.839187) - (0.0253887 * distance() + -0.22072) * (enclose() + -0.507807 * distance() + 2.31294) * (enclose() + -0.507807 * distance() + 2.31294))) + 0.5 * 0.273934 * exp(-1.22112 * enclose()) * (0.0970971 - 0.107839 * exp(-0.271795 * distance()) * pow(distance() , 0.0934905))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00055349 * distance() + 0.112407) * (0.722734 - exp(-(-0.0235586 * distance() + 1.66987) * (enclose() + -0.035551 * distance() + 0.839187) - (0.0253887 * distance() + -0.22072) * (enclose() + -0.507807 * distance() + 2.31294) * (enclose() + -0.507807 * distance() + 2.31294))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0446675 * (1 - exp(-1.35233 * (enclose() + 0.43165))) * pow(width() , -0.00123266 * enclose() + 0.0366517) + 0.5 * (0.0138786 * exp(-0.64251 * enclose()) + 0.011037 * exp(-1.7781 * enclose())) * pow(width() , 0.0477634 * enclose() + 0.0436877)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0446675 * (1 - exp(-1.35233 * (enclose() + 0.43165))) * pow(width() , -0.00123266 * enclose() + 0.0366517) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal4,metal5
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal2 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.880122 - 1.82269 * distance()) + 0.0247963 / pow(distance() , 1.52106)) * 3.43325 * pow((width1() + width2()) / 2 , 0.0456431) * (0.082934 * (thickness1() + thickness2()) / 2 + 0.0264313) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.174992 * (1 - exp(-1.22025 * (distance() + -0.0114133))) * pow(width() , 0.00306642 * distance() + -0.0149313) * (0.0729217 * thickness() + 0.18708)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0372957 * (1 - exp(-1.27958 * (distance() + -0.0313637)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.149296 * pow(width() , 0.00818359) * (0.114934 * thickness() + 0.207614)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-1.45363e-06 * distance() + 0.119231) * (0.61285 - exp(-(-0.0108636 * distance() + 2.48175) * (enclose() + -0.0439135 * distance() + 0.758917) - (0.083382 * distance() + -0.477746) * (enclose() + -0.299902 * distance() + 1.78224) * (enclose() + -0.299902 * distance() + 1.78224)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.174992 * (1 - exp(-1.22025 * (distance() + -0.0114133))) * pow(width() , 0.00306642 * distance() + -0.0149313) * (0.0729217 * thickness() + 0.18708)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0596082 * (0.61285 - exp(-2.37311 * (enclose() + 0.319782) - 0.356074 * (enclose() + -1.21678) * (enclose() + -1.21678)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0804772 * (1 - exp(-1.08299 * (distance() + -0.0115263))) * pow(width() , 0.00485155 * distance() + -0.0161427) * (0.10747 * thickness() + 0.158964) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0804772 * (1 - exp(-1.08299 * (distance() + -0.0115263))) * pow(width() , 0.00485155 * distance() + -0.0161427) * (0.10747 * thickness() + 0.158964) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0157423 * (1 - exp(-1.1754 * (distance() + -0.0454614))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.1061 * pow(width() , 0.0195516) * (0.106975 * thickness() + 0.110249) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.0004592 * distance() + 0.0445506) * (0.743008 - exp(-(-0.482821 * distance() + 0.423762) * (enclose() + 1.89286 * distance() + 0.223425) - (0.00079491 * distance() + 0.109893) * (enclose() + 2.71477 * distance() + -0.257672) * (enclose() + 2.71477 * distance() + -0.257672))) + 0.5 * 0.104246 * exp(-1.35682 * enclose()) * (0.0861512 - 0.119807 * exp(-0.506781 * distance()) * pow(distance() , 0.230151))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.0004592 * distance() + 0.0445506) * (0.743008 - exp(-(-0.482821 * distance() + 0.423762) * (enclose() + 1.89286 * distance() + 0.223425) - (0.00079491 * distance() + 0.109893) * (enclose() + 2.71477 * distance() + -0.257672) * (enclose() + 2.71477 * distance() + -0.257672))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0159597 * (1 - exp(-1.2352 * (enclose() + 0.411636))) * pow(width() , 0.00130645 * enclose() + 0.0116761) + 0.5 * (0.00449441 * exp(-1.27503 * enclose()) + 0.00451054 * exp(-1.27447 * enclose())) * pow(width() , 0.0056417 * enclose() + 0.0438214)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0159597 * (1 - exp(-1.2352 * (enclose() + 0.411636))) * pow(width() , 0.00130645 * enclose() + 0.0116761) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal4,metal6
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.890466 - 1.28815 * distance()) + 0.0437649 / pow(distance() , 1.36705)) * 3.2511 * pow((width1() + width2()) / 2 , 0.0691399) * (0.0684894 * (thickness1() + thickness2()) / 2 + 0.0280643) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.110938 * (1 - exp(-0.701509 * (distance() + 0.0302834))) * pow(width() , 0.00802055 * distance() + -0.032947) * (0.0828356 * thickness() + 0.172212)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0219679 * (1 - exp(-0.792083 * (distance() + -0.0349182)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.122808 * pow(width() , 0.0246042) * (0.092292 * thickness() + 0.146437)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0007835 * distance() + 0.110826) * (0.401834 - exp(-(-0.15158 * distance() + 0.14567) * (enclose() + 4.08691 * distance() + 4.51176) - (0.00020394 * distance() + 0.0351306) * (enclose() + 3.96185 * distance() + 1.78987) * (enclose() + 3.96185 * distance() + 1.78987)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.110938 * (1 - exp(-0.701509 * (distance() + 0.0302834))) * pow(width() , 0.00802055 * distance() + -0.032947) * (0.0828356 * thickness() + 0.172212)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0514955 * (0.401834 - exp(1.37013 * (enclose() + 45.3809) - 0.03717 * (enclose() + 41.4084) * (enclose() + 41.4084)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.107152 * (1 - exp(-0.696708 * (distance() + 0.0339023))) * pow(width() , 0.00832153 * distance() + -0.0353493) * (0.0665045 * thickness() + 0.182505) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.107152 * (1 - exp(-0.696708 * (distance() + 0.0339023))) * pow(width() , 0.00832153 * distance() + -0.0353493) * (0.0665045 * thickness() + 0.182505) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.02166 * (1 - exp(-0.791352 * (distance() + -0.0342135))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.123545 * pow(width() , 0.024477) * (0.0754753 * thickness() + 0.149053) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00110868 * distance() + 0.109209) * (0.414398 - exp(-(-0.233187 * distance() + 0.218344) * (enclose() + 4.00151 * distance() + 3.26072) - (0.00014537 * distance() + 0.0327807) * (enclose() + 5.11993 * distance() + 0.873586) * (enclose() + 5.11993 * distance() + 0.873586))) + 0.5 * 0.169291 * exp(-1.11629 * enclose()) * (0.0917249 - 0.113035 * exp(-0.33509 * distance()) * pow(distance() , 0.163556))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00110868 * distance() + 0.109209) * (0.414398 - exp(-(-0.233187 * distance() + 0.218344) * (enclose() + 4.00151 * distance() + 3.26072) - (0.00014537 * distance() + 0.0327807) * (enclose() + 5.11993 * distance() + 0.873586) * (enclose() + 5.11993 * distance() + 0.873586))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.021933 * (1 - exp(-0.994714 * (enclose() + 0.485281))) * pow(width() , 0.00087869 * enclose() + 0.0195729) + 0.5 * (0.00674903 * exp(-0.97716 * enclose()) + 0.00736924 * exp(-0.978366 * enclose())) * pow(width() , 0.0101778 * enclose() + 0.0318593)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.021933 * (1 - exp(-0.994714 * (enclose() + 0.485281))) * pow(width() , 0.00087869 * enclose() + 0.0195729) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal4,metal7
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.907444 - 1.07367 * distance()) + 0.0641624 / pow(distance() , 1.272)) * 3.16938 * pow((width1() + width2()) / 2 , 0.0855665) * (0.0606174 * (thickness1() + thickness2()) / 2 + 0.026563) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal4,metal7
//
CAPACITANCE CROSSOVER FRINGE metal4 metal7 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0840649 * (1 - exp(-0.553157 * (distance() + 0.0874086))) * pow(width() , 0.0108927 * distance() + -0.0450531) * (0.0873495 * thickness() + 0.168589)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0162547 * (1 - exp(-0.649646 * (distance() + -0.0204728)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.109745 * pow(width() , 0.0362246) * (0.0815115 * thickness() + 0.120655)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00056869 * distance() + 0.0667928) * (0.492392 - exp(-(-0.112723 * distance() + 0.115139) * (enclose() + 4.68179 * distance() + 4.90062) - (2.89376e-05 * distance() + 0.0231517) * (enclose() + 4.66296 * distance() + 1.2103) * (enclose() + 4.66296 * distance() + 1.2103)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0840649 * (1 - exp(-0.553157 * (distance() + 0.0874086))) * pow(width() , 0.0108927 * distance() + -0.0450531) * (0.0873495 * thickness() + 0.168589)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0305529 * (0.492392 - exp(1.01209 * (enclose() + 51.7185) - 0.0234411 * (enclose() + 47.8399) * (enclose() + 47.8399)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal4,metal7
//
CAPACITANCE CROSSOVER FRINGE metal4 metal2 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.124271 * (1 - exp(-0.566437 * (distance() + 0.0857555))) * pow(width() , 0.0104554 * distance() + -0.0454787) * (0.0525795 * thickness() + 0.189524) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.124271 * (1 - exp(-0.566437 * (distance() + 0.0857555))) * pow(width() , 0.0104554 * distance() + -0.0454787) * (0.0525795 * thickness() + 0.189524) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0250914 * (1 - exp(-0.661993 * (distance() + -0.0169047))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.132388 * pow(width() , 0.0326139) * (0.0671674 * thickness() + 0.167787) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00126038 * distance() + 0.149497) * (0.342638 - exp(-(-0.142487 * distance() + 0.16178) * (enclose() + 4.34933 * distance() + 5.36392) - (0.00012628 * distance() + 0.023298) * (enclose() + 4.8913 * distance() + 1.82488) * (enclose() + 4.8913 * distance() + 1.82488))) + 0.5 * 0.221662 * exp(-0.96891 * enclose()) * (0.0958917 - 0.108949 * exp(-0.24684 * distance()) * pow(distance() , 0.123431))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00126038 * distance() + 0.149497) * (0.342638 - exp(-(-0.142487 * distance() + 0.16178) * (enclose() + 4.34933 * distance() + 5.36392) - (0.00012628 * distance() + 0.023298) * (enclose() + 4.8913 * distance() + 1.82488) * (enclose() + 4.8913 * distance() + 1.82488))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0255083 * (1 - exp(-0.870929 * (enclose() + 0.52735))) * pow(width() , 0.00056869 * enclose() + 0.0286485) + 0.5 * (0.0090492 * exp(-0.821119 * enclose()) + 0.00905032 * exp(-0.820429 * enclose())) * pow(width() , 0.0140175 * enclose() + 0.0364595)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0255083 * (1 - exp(-0.870929 * (enclose() + 0.52735))) * pow(width() , 0.00056869 * enclose() + 0.0286485) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal4,metal8
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.932652 - 0.942659 * distance()) + 0.0864786 / pow(distance() , 1.20013)) * 3.14885 * pow((width1() + width2()) / 2 , 0.0951791) * (0.0543856 * (thickness1() + thickness2()) / 2 + 0.0245746) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal4,metal8
//
CAPACITANCE CROSSOVER FRINGE metal4 metal8 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0659628 * (1 - exp(-0.479338 * (distance() + 0.144799))) * pow(width() , 0.0121529 * distance() + -0.0485831) * (0.090542 * thickness() + 0.16629)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.012553 * (1 - exp(-0.571831 * (distance() + 0.00081662)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0993855 * pow(width() , 0.0455152) * (0.0735285 * thickness() + 0.10264)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00032951 * distance() + 0.0358117) * (0.703389 - exp(-(0.0598193 * distance() + 0.180668) * (enclose() + -0.0648285 * distance() + 1.97299) - (0.00484496 * distance() + -0.00891902) * (enclose() + -0.182177 * distance() + 3.34042) * (enclose() + -0.182177 * distance() + 3.34042)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0659628 * (1 - exp(-0.479338 * (distance() + 0.144799))) * pow(width() , 0.0121529 * distance() + -0.0485831) * (0.090542 * thickness() + 0.16629)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0162583 * (0.703389 - exp(-0.778861 * (enclose() + 1.32471) - 0.0395306 * (enclose() + 1.51865) * (enclose() + 1.51865)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal4,metal8
//
CAPACITANCE CROSSOVER FRINGE metal4 metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.137656 * (1 - exp(-0.498043 * (distance() + 0.137672))) * pow(width() , 0.0113905 * distance() + -0.0479007) * (0.0451394 * thickness() + 0.193178) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.137656 * (1 - exp(-0.498043 * (distance() + 0.137672))) * pow(width() , 0.0113905 * distance() + -0.0479007) * (0.0451394 * thickness() + 0.193178) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0276883 * (1 - exp(-0.587604 * (distance() + 0.00532108))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.138093 * pow(width() , 0.0404615) * (0.0644449 * thickness() + 0.181809) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00125442 * distance() + 0.198743) * (0.276842 - exp(-(-0.12342 * distance() + 0.151613) * (enclose() + 4.94398 * distance() + 7.09345) - (8.45831e-05 * distance() + 0.0178532) * (enclose() + 5.57216 * distance() + 2.28026) * (enclose() + 5.57216 * distance() + 2.28026))) + 0.5 * 0.271102 * exp(-0.855405 * enclose()) * (0.0992794 - 0.106404 * exp(-0.192313 * distance()) * pow(distance() , 0.0940413))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00125442 * distance() + 0.198743) * (0.276842 - exp(-(-0.12342 * distance() + 0.151613) * (enclose() + 4.94398 * distance() + 7.09345) - (8.45831e-05 * distance() + 0.0178532) * (enclose() + 5.57216 * distance() + 2.28026) * (enclose() + 5.57216 * distance() + 2.28026))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0282792 * (1 - exp(-0.782993 * (enclose() + 0.562453))) * pow(width() , 0.00026693 * enclose() + 0.0369218) + 0.5 * (0.015681 * exp(-0.79959 * enclose()) + 0.00615333 * exp(-0.525989 * enclose())) * pow(width() , 0.0167062 * enclose() + 0.0437593)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0282792 * (1 - exp(-0.782993 * (enclose() + 0.562453))) * pow(width() , 0.00026693 * enclose() + 0.0369218) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.930084 - 1.57706 * distance()) + 0.0395337 / pow(distance() , 1.39104)) * 3.42844 * pow((width1() + width2()) / 2 , 0.0661205) * (0.0725287 * (thickness1() + thickness2()) / 2 + 0.0248492) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.09)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.191423 * (1 - exp(-1.06337 * (distance() + 0.0199792))) * pow(width() , 0.00371664 * distance() + -0.0137945) * (0.0634486 * thickness() + 0.192169)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0408805 * (1 - exp(-1.11496 * (distance() + -0.0127402)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.156539 * pow(width() , 0.0194886) * (0.106754 * thickness() + 0.225429)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00054751 * distance() + 0.103562) * (0.737609 - exp(-(0.121104 * distance() + 0.77273) * (enclose() + -0.0169902 * distance() + 0.630909) - (0.0224772 * distance() + -0.0130869) * (enclose() + -0.557579 * distance() + 4.7627) * (enclose() + -0.557579 * distance() + 4.7627)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.191423 * (1 - exp(-1.06337 * (distance() + 0.0199792))) * pow(width() , 0.00371664 * distance() + -0.0137945) * (0.0634486 * thickness() + 0.192169)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0545185 * (0.737609 - exp(-1.98377 * (enclose() + 0.461007) - 0.211685 * (enclose() + -0.81309) * (enclose() + -0.81309)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0584725 * (1 - exp(-0.904446 * (distance() + 0.0301142))) * pow(width() , 0.00658324 * distance() + -0.0169843) * (0.123539 * thickness() + 0.149539) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0584725 * (1 - exp(-0.904446 * (distance() + 0.0301142))) * pow(width() , 0.00658324 * distance() + -0.0169843) * (0.123539 * thickness() + 0.149539) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0111094 * (1 - exp(-1.0001 * (distance() + -0.0282789))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.092854 * pow(width() , 0.0362762) * (0.0979402 * thickness() + 0.0866467) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00025287 * distance() + 0.0268544) * (0.853753 - exp(-(-0.734214 * distance() + 0.253714) * (enclose() + 1.71521 * distance() + -0.0549773) - (0.00035531 * distance() + 0.134975) * (enclose() + 2.97829 * distance() + -0.137248) * (enclose() + 2.97829 * distance() + -0.137248))) + 0.5 * 0.0959782 * exp(-0.918448 * enclose()) * (0.0954359 - 0.102758 * exp(-0.301823 * distance()) * pow(distance() , 0.131867))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00025287 * distance() + 0.0268544) * (0.853753 - exp(-(-0.734214 * distance() + 0.253714) * (enclose() + 1.71521 * distance() + -0.0549773) - (0.00035531 * distance() + 0.134975) * (enclose() + 2.97829 * distance() + -0.137248) * (enclose() + 2.97829 * distance() + -0.137248))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0114663 * (1 - exp(-0.822638 * (enclose() + 0.456547))) * pow(width() , 0.00324116 * enclose() + 0.0124944) + 0.5 * (0.00463715 * exp(-0.883578 * enclose()) + 0.0046373 * exp(-0.885388 * enclose())) * pow(width() , 0.00161367 * enclose() + 0.0957562)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0114663 * (1 - exp(-0.822638 * (enclose() + 0.456547))) * pow(width() , 0.00324116 * enclose() + 0.0124944) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal5,metal7
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.907614 - 1.07633 * distance()) + 0.0639666 / pow(distance() , 1.27249)) * 3.17097 * pow((width1() + width2()) / 2 , 0.0857509) * (0.0605904 * (thickness1() + thickness2()) / 2 + 0.026632) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.09)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal5,metal7
//
CAPACITANCE CROSSOVER FRINGE metal5 metal7 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.127553 * (1 - exp(-0.572693 * (distance() + 0.0818342))) * pow(width() , 0.0101185 * distance() + -0.0426697) * (0.0669226 * thickness() + 0.181063)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0253775 * (1 - exp(-0.664628 * (distance() + -0.0173227)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.131763 * pow(width() , 0.0330101) * (0.0817986 * thickness() + 0.165567)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0005477 * distance() + 0.122003) * (0.408639 - exp(-(-0.025289 * distance() + 0.00067125) * (enclose() + 11.1038 * distance() + 18.7958) - (0.00021183 * distance() + 0.0201861) * (enclose() + 3.30744 * distance() + 6.13211) * (enclose() + 3.30744 * distance() + 6.13211)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.127553 * (1 - exp(-0.572693 * (distance() + 0.0818342))) * pow(width() , 0.0101185 * distance() + -0.0426697) * (0.0669226 * thickness() + 0.181063)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.058263 * (0.408639 - exp(0.252219 * (enclose() + 129.834) - 0.0223044 * (enclose() + 39.2065) * (enclose() + 39.2065)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal5,metal7
//
CAPACITANCE CROSSOVER FRINGE metal5 metal2 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.08196 * (1 - exp(-0.550346 * (distance() + 0.0918886))) * pow(width() , 0.0112541 * distance() + -0.0471705) * (0.0753536 * thickness() + 0.176097) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.08196 * (1 - exp(-0.550346 * (distance() + 0.0918886))) * pow(width() , 0.0112541 * distance() + -0.0471705) * (0.0753536 * thickness() + 0.176097) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0160743 * (1 - exp(-0.650612 * (distance() + -0.0197591))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.110141 * pow(width() , 0.0368673) * (0.0702882 * thickness() + 0.122698) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00057059 * distance() + 0.0452638) * (0.748393 - exp(-(-0.239448 * distance() + 0.192373) * (enclose() + 3.06088 * distance() + 0.878627) - (7.87111e-05 * distance() + 0.0359012) * (enclose() + 4.40778 * distance() + -0.398932) * (enclose() + 4.40778 * distance() + -0.398932))) + 0.5 * 0.160202 * exp(-0.791026 * enclose()) * (0.0999531 - 0.106125 * exp(-0.216141 * distance()) * pow(distance() , 0.104216))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00057059 * distance() + 0.0452638) * (0.748393 - exp(-(-0.239448 * distance() + 0.192373) * (enclose() + 3.06088 * distance() + 0.878627) - (7.87111e-05 * distance() + 0.0359012) * (enclose() + 4.40778 * distance() + -0.398932) * (enclose() + 4.40778 * distance() + -0.398932))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0164768 * (1 - exp(-0.692952 * (enclose() + 0.545998))) * pow(width() , 0.00234604 * enclose() + 0.0206113) + 0.5 * (0.00695077 * exp(-0.718308 * enclose()) + 0.00695167 * exp(-0.719985 * enclose())) * pow(width() , 0.00438632 * enclose() + 0.0635351)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0164768 * (1 - exp(-0.692952 * (enclose() + 0.545998))) * pow(width() , 0.00234604 * enclose() + 0.0206113) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal5,metal8
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.903729 - 0.83918 * distance()) + 0.0955416 / pow(distance() , 1.18693)) * 3.06508 * pow((width1() + width2()) / 2 , 0.102548) * (0.051544 * (thickness1() + thickness2()) / 2 + 0.0246502) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.09)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal5,metal8
//
CAPACITANCE CROSSOVER FRINGE metal5 metal8 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0972528 * (1 - exp(-0.41684 * (distance() + 0.170037))) * pow(width() , 0.0141241 * distance() + -0.0647701) * (0.0673247 * thickness() + 0.177133)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0185837 * (1 - exp(-0.512944 * (distance() + 0.00378231)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.117317 * pow(width() , 0.0433195) * (0.0692602 * thickness() + 0.136122)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00032866 * distance() + 0.052764) * (0.688255 - exp(-(0.068288 * distance() + 0.243888) * (enclose() + -0.0897232 * distance() + 2.05129) - (0.00312422 * distance() + -0.0121115) * (enclose() + -0.235603 * distance() + 4.25197) * (enclose() + -0.235603 * distance() + 4.25197)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0972528 * (1 - exp(-0.41684 * (distance() + 0.170037))) * pow(width() , 0.0141241 * distance() + -0.0647701) * (0.0673247 * thickness() + 0.177133)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0247387 * (0.688255 - exp(-0.926768 * (enclose() + 1.15406) - 0.0191307 * (enclose() + 1.89594) * (enclose() + 1.89594)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal5,metal8
//
CAPACITANCE CROSSOVER FRINGE metal5 metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.101929 * (1 - exp(-0.415124 * (distance() + 0.174019))) * pow(width() , 0.0143417 * distance() + -0.0660924) * (0.0549703 * thickness() + 0.18406) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.101929 * (1 - exp(-0.415124 * (distance() + 0.174019))) * pow(width() , 0.0143417 * distance() + -0.0660924) * (0.0549703 * thickness() + 0.18406) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0196875 * (1 - exp(-0.514378 * (distance() + 0.00259447))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.120979 * pow(width() , 0.0435819) * (0.0605212 * thickness() + 0.143813) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00012775 * distance() + 0.0589431) * (0.606658 - exp(-(0.02238 * distance() + 0.394321) * (enclose() + -0.0424213 * distance() + 1.8639) - (0.0167651 * distance() + -0.0427034) * (enclose() + -0.316426 * distance() + 2.82781) * (enclose() + -0.316426 * distance() + 2.82781))) + 0.5 * 0.239837 * exp(-0.697063 * enclose()) * (0.101817 - 0.103675 * exp(-0.146378 * distance()) * pow(distance() , 0.0733024))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00012775 * distance() + 0.0589431) * (0.606658 - exp(-(0.02238 * distance() + 0.394321) * (enclose() + -0.0424213 * distance() + 1.8639) - (0.0167651 * distance() + -0.0427034) * (enclose() + -0.316426 * distance() + 2.82781) * (enclose() + -0.316426 * distance() + 2.82781))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0201842 * (1 - exp(-0.60418 * (enclose() + 0.60147))) * pow(width() , 0.00182086 * enclose() + 0.0303569) + 0.5 * (0.0090932 * exp(-0.605428 * enclose()) + 0.00909306 * exp(-0.607126 * enclose())) * pow(width() , 0.0065542 * enclose() + 0.0589401)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0201842 * (1 - exp(-0.60418 * (enclose() + 0.60147))) * pow(width() , 0.00182086 * enclose() + 0.0303569) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal6,metal7
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-0.991671 - 1.4607 * distance()) + 0.051535 / pow(distance() , 1.31369)) * 3.39067 * pow((width1() + width2()) / 2 , 0.0762157) * (0.0687645 * (thickness1() + thickness2()) / 2 + 0.0242176) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.91)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal6,metal7
//
CAPACITANCE CROSSOVER FRINGE metal6 metal7 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.201597 * (1 - exp(-0.99553 * (distance() + 0.0416648))) * pow(width() , 0.00386974 * distance() + -0.0100301) * (0.0582269 * thickness() + 0.194856)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0430069 * (1 - exp(-1.03906 * (distance() + 0.00122157)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.16134 * pow(width() , 0.0277739) * (0.104917 * thickness() + 0.23449)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00014606 * distance() + 0.121671) * (0.680446 - exp(-(-0.0385382 * distance() + 2.11089) * (enclose() + -0.00949872 * distance() + 0.652303) - (0.0503718 * distance() + -0.450109) * (enclose() + -0.353295 * distance() + 1.56219) * (enclose() + -0.353295 * distance() + 1.56219)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.201597 * (1 - exp(-0.99553 * (distance() + 0.0416648))) * pow(width() , 0.00386974 * distance() + -0.0100301) * (0.0582269 * thickness() + 0.194856)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0615658 * (0.680446 - exp(-1.72551 * (enclose() + 0.557316) - 0.053609 * (enclose() + -1.97076) * (enclose() + -1.97076)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal6,metal7
//
CAPACITANCE CROSSOVER FRINGE metal6 metal2 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0460034 * (1 - exp(-0.832551 * (distance() + 0.0603297))) * pow(width() , 0.0070842 * distance() + -0.0131793) * (0.132651 * thickness() + 0.145061) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0460034 * (1 - exp(-0.832551 * (distance() + 0.0603297))) * pow(width() , 0.0070842 * distance() + -0.0131793) * (0.132651 * thickness() + 0.145061) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0086263 * (1 - exp(-0.921979 * (distance() + -0.0126656))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0843905 * pow(width() , 0.0474954) * (0.0906078 * thickness() + 0.0727992) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (2.56888e-05 * distance() + 0.0208472) * (0.772053 - exp(-(0.118277 * distance() + -0.0871369) * (enclose() + -0.227526 * distance() + 2.35456) - (0.0147709 * distance() + 0.0502724) * (enclose() + -0.411311 * distance() + 2.44098) * (enclose() + -0.411311 * distance() + 2.44098))) + 0.5 * 0.0920113 * exp(-0.695198 * enclose()) * (0.102444 - 0.0945829 * exp(-0.184508 * distance()) * pow(distance() , 0.0584201))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (2.56888e-05 * distance() + 0.0208472) * (0.772053 - exp(-(0.118277 * distance() + -0.0871369) * (enclose() + -0.227526 * distance() + 2.35456) - (0.0147709 * distance() + 0.0502724) * (enclose() + -0.411311 * distance() + 2.44098) * (enclose() + -0.411311 * distance() + 2.44098))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.00905702 * (1 - exp(-0.603934 * (enclose() + 0.465855))) * pow(width() , 0.00591688 * enclose() + 0.00166884) + 0.5 * (0.00458493 * exp(-0.679286 * enclose()) + 0.00458473 * exp(-0.676651 * enclose())) * pow(width() , 0.00056125 * enclose() + 0.132915)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.00905702 * (1 - exp(-0.603934 * (enclose() + 0.465855))) * pow(width() , 0.00591688 * enclose() + 0.00166884) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal6,metal8
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-0.908666 - 0.909307 * distance()) + 0.0901151 / pow(distance() , 1.19644)) * 3.0958 * pow((width1() + width2()) / 2 , 0.0981115) * (0.0532448 * (thickness1() + thickness2()) / 2 + 0.0245604) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.91)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal6,metal8
//
CAPACITANCE CROSSOVER FRINGE metal6 metal8 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.132947 * (1 - exp(-0.47381 * (distance() + 0.145019))) * pow(width() , 0.0121044 * distance() + -0.0521367) * (0.0587484 * thickness() + 0.184287)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0261712 * (1 - exp(-0.564566 * (distance() + 0.00396364)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.13398 * pow(width() , 0.0413237) * (0.0754388 * thickness() + 0.172198)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (5.6579e-05 * distance() + 0.0849155) * (0.573346 - exp(-(-0.0747383 * distance() + 0.111505) * (enclose() + 2.96514 * distance() + 3.40127) - (0.00012393 * distance() + 0.0339926) * (enclose() + 2.37782 * distance() + 1.37967) * (enclose() + 2.37782 * distance() + 1.37967)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.132947 * (1 - exp(-0.47381 * (distance() + 0.145019))) * pow(width() , 0.0121044 * distance() + -0.0521367) * (0.0587484 * thickness() + 0.184287)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0427406 * (0.573346 - exp(0.635878 * (enclose() + 33.0527) - 0.0352319 * (enclose() + 25.1579) * (enclose() + 25.1579)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal6,metal8
//
CAPACITANCE CROSSOVER FRINGE metal6 metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0702119 * (1 - exp(-0.454404 * (distance() + 0.159963))) * pow(width() , 0.0131545 * distance() + -0.0548899) * (0.0747857 * thickness() + 0.175373) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0702119 * (1 - exp(-0.454404 * (distance() + 0.159963))) * pow(width() , 0.0131545 * distance() + -0.0548899) * (0.0747857 * thickness() + 0.175373) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0135211 * (1 - exp(-0.550853 * (distance() + 0.00452804))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.103157 * pow(width() , 0.0464785) * (0.0636978 * thickness() + 0.111093) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-3.42397e-07 * distance() + 0.0358913) * (0.694624 - exp(-(0.0216791 * distance() + 0.178738) * (enclose() + -0.0132785 * distance() + 1.94561) - (0.0136219 * distance() + -0.00888573) * (enclose() + -0.291718 * distance() + 3.18254) * (enclose() + -0.291718 * distance() + 3.18254))) + 0.5 * 0.177813 * exp(-0.601995 * enclose()) * (0.103154 - 0.0996283 * exp(-0.131079 * distance()) * pow(distance() , 0.052579))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-3.42397e-07 * distance() + 0.0358913) * (0.694624 - exp(-(0.0216791 * distance() + 0.178738) * (enclose() + -0.0132785 * distance() + 1.94561) - (0.0136219 * distance() + -0.00888573) * (enclose() + -0.291718 * distance() + 3.18254) * (enclose() + -0.291718 * distance() + 3.18254))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0140492 * (1 - exp(-0.516653 * (enclose() + 0.585778))) * pow(width() , 0.00389866 * enclose() + 0.0174301) + 0.5 * (0.00708235 * exp(-0.556847 * enclose()) + 0.007082 * exp(-0.556198 * enclose())) * pow(width() , 0.00235134 * enclose() + 0.0853004)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0140492 * (1 - exp(-0.516653 * (enclose() + 0.585778))) * pow(width() , 0.00389866 * enclose() + 0.0174301) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal2,metal7,metal8
//
CAPACITANCE NEARBODY metal7 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(-0.967339 - 1.23658 * distance()) + 0.0698411 / pow(distance() , 1.23479)) * 3.32524 * pow((width1() + width2()) / 2 , 0.0854114) * (0.0601498 * (thickness1() + thickness2()) / 2 + 0.024464) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.73)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal2,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal7 metal8 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.192255 * (1 - exp(-0.778807 * (distance() + 0.0738112))) * pow(width() , 0.00564363 * distance() + -0.0157066) * (0.0566087 * thickness() + 0.193578)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0403467 * (1 - exp(-0.832977 * (distance() + 0.00804752)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.157541 * pow(width() , 0.0355943) * (0.0966996 * thickness() + 0.228636)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0032814 * distance() + 0.932902) * (0.0805241 - exp(-(-0.127203 * distance() + 0.136804) * (enclose() + 4.43717 * distance() + 10.6836) - (0.00032028 * distance() + 0.0302115) * (enclose() + 3.88738 * distance() + 5.51789) * (enclose() + 3.88738 * distance() + 5.51789)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.192255 * (1 - exp(-0.778807 * (distance() + 0.0738112))) * pow(width() , 0.00564363 * distance() + -0.0157066) * (0.0566087 * thickness() + 0.193578)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.482858 * (0.0805241 - exp(1.13523 * (enclose() + 55.0553) - 0.0334143 * (enclose() + 44.3917) * (enclose() + 44.3917)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal2,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal7 metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0467306 * (1 - exp(-0.676795 * (distance() + 0.102376))) * pow(width() , 0.00850271 * distance() + -0.0180123) * (0.108541 * thickness() + 0.138218) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0467306 * (1 - exp(-0.676795 * (distance() + 0.102376))) * pow(width() , 0.00850271 * distance() + -0.0180123) * (0.108541 * thickness() + 0.138218) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00799391 * (1 - exp(-0.76275 * (distance() + 0.00268741))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0829249 * pow(width() , 0.0532341) * (0.0759254 * thickness() + 0.0726858) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-7.24153e-06 * distance() + 0.0201452) * (0.738601 - exp(-(0.0553156 * distance() + -0.0261378) * (enclose() + -0.161267 * distance() + 2.72698) - (0.0106635 * distance() + 0.0237002) * (enclose() + -0.372017 * distance() + 3.16453) * (enclose() + -0.372017 * distance() + 3.16453))) + 0.5 * 0.110912 * exp(-0.543937 * enclose()) * (0.10829 - 0.0946196 * exp(-0.101462 * distance()) * pow(distance() , 0.0116036))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-7.24153e-06 * distance() + 0.0201452) * (0.738601 - exp(-(0.0553156 * distance() + -0.0261378) * (enclose() + -0.161267 * distance() + 2.72698) - (0.0106635 * distance() + 0.0237002) * (enclose() + -0.372017 * distance() + 3.16453) * (enclose() + -0.372017 * distance() + 3.16453))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.73)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0085215 * (1 - exp(-0.455595 * (enclose() + 0.493924))) * pow(width() , 0.00823379 * enclose() + -0.0118729) + 0.5 * (0.00490807 * exp(-0.53202 * enclose()) + 0.00490809 * exp(-0.531759 * enclose())) * pow(width() , 0.00030526 * enclose() + 0.141409)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.73)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0085215 * (1 - exp(-0.455595 * (enclose() + 0.493924))) * pow(width() , 0.00823379 * enclose() + -0.0118729) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.73)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal4,metal5
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal3 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.894786 - 2.53585 * distance()) + 0.00979285 / pow(distance() , 1.75994)) * 3.61616 * pow((width1() + width2()) / 2 , 0.00979829) * (0.112642 * (thickness1() + thickness2()) / 2 + 0.0262928) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.148441 * (1 - exp(-1.73565 * (distance() + -0.0512697))) * pow(width() , 0.0017494 * distance() + -0.0127753) * (0.105744 * thickness() + 0.163151)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0301003 * (1 - exp(-1.79856 * (distance() + -0.0551512)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.135378 * pow(width() , 0) * (0.165366 * thickness() + 0.158898)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00151531 * distance() + 0.303736) * (0.202466 - exp(-(0.357748 * distance() + 0.944667) * (enclose() + 0.0417288 * distance() + 1.58749) - (0.00011831 * distance() + -0.00209749) * (enclose() + 9.03045 * distance() + -5.2199) * (enclose() + 9.03045 * distance() + -5.2199)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.148441 * (1 - exp(-1.73565 * (distance() + -0.0512697))) * pow(width() , 0.0017494 * distance() + -0.0127753) * (0.105744 * thickness() + 0.163151)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.144291 * (0.202466 - exp(-4.52215 * (enclose() + 2.00478) - -0.00091439 * (enclose() + 85.0846) * (enclose() + 85.0846)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal4,metal5
//
CAPACITANCE CROSSOVER FRINGE metal4 metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.133374 * (1 - exp(-1.74201 * (distance() + -0.0475697))) * pow(width() , 0.00167654 * distance() + -0.0118058) * (0.0607038 * thickness() + 0.194902) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.133374 * (1 - exp(-1.74201 * (distance() + -0.0475697))) * pow(width() , 0.00167654 * distance() + -0.0118058) * (0.0607038 * thickness() + 0.194902) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0290058 * (1 - exp(-1.80734 * (distance() + -0.0523713))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.13721 * pow(width() , -0.00376214) * (0.0989114 * thickness() + 0.172548) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00137397 * distance() + 0.263682) * (0.22552 - exp(-(0.328809 * distance() + 0.961237) * (enclose() + 0.0457588 * distance() + 1.50496) - (0.00017815 * distance() + -0.00305931) * (enclose() + 7.47726 * distance() + -6.14002) * (enclose() + 7.47726 * distance() + -6.14002))) + 0.5 * 0.0898406 * exp(-2.7079 * enclose()) * (0.0700716 - 0.138494 * exp(-0.873476 * distance()) * pow(distance() , 0.325888))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00137397 * distance() + 0.263682) * (0.22552 - exp(-(0.328809 * distance() + 0.961237) * (enclose() + 0.0457588 * distance() + 1.50496) - (0.00017815 * distance() + -0.00305931) * (enclose() + 7.47726 * distance() + -6.14002) * (enclose() + 7.47726 * distance() + -6.14002))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0288057 * (1 - exp(-2.41549 * (enclose() + 0.307883))) * pow(width() , 0.00014369 * enclose() + -0.00272425) + 0.5 * (0.00298263 * exp(-2.32884 * enclose()) + 0.00303175 * exp(-2.31413 * enclose())) * pow(width() , 0.0297716 * enclose() + -0.0209423)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0288057 * (1 - exp(-2.41549 * (enclose() + 0.307883))) * pow(width() , 0.00014369 * enclose() + -0.00272425) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal4,metal6
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.882227 - 1.81086 * distance()) + 0.0251309 / pow(distance() , 1.51878)) * 3.4249 * pow((width1() + width2()) / 2 , 0.0430141) * (0.083506 * (thickness1() + thickness2()) / 2 + 0.0259281) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0850493 * (1 - exp(-1.07712 * (distance() + -0.015517))) * pow(width() , 0.00457102 * distance() + -0.0144835) * (0.128918 * thickness() + 0.144553)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0160932 * (1 - exp(-1.16454 * (distance() + -0.0468496)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.105459 * pow(width() , 0.0184976) * (0.129679 * thickness() + 0.105973)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00051144 * distance() + 0.056326) * (0.592634 - exp(-(-0.218762 * distance() + 0.359064) * (enclose() + 2.08662 * distance() + 0.924111) - (0.00141047 * distance() + 0.0942729) * (enclose() + 1.94739 * distance() + -0.0111372) * (enclose() + 1.94739 * distance() + -0.0111372)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0850493 * (1 - exp(-1.07712 * (distance() + -0.015517))) * pow(width() , 0.00457102 * distance() + -0.0144835) * (0.128918 * thickness() + 0.144553)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0256058 * (0.592634 - exp(1.82856 * (enclose() + 21.7903) - 0.108378 * (enclose() + 19.4628) * (enclose() + 19.4628)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal4,metal6
//
CAPACITANCE CROSSOVER FRINGE metal4 metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.160404 * (1 - exp(-1.20447 * (distance() + -0.00688723))) * pow(width() , 0.00298941 * distance() + -0.0147602) * (0.034392 * thickness() + 0.214628) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.160404 * (1 - exp(-1.20447 * (distance() + -0.00688723))) * pow(width() , 0.00298941 * distance() + -0.0147602) * (0.034392 * thickness() + 0.214628) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0363018 * (1 - exp(-1.26542 * (distance() + -0.0270404))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.154768 * pow(width() , 0.00756787) * (0.0682735 * thickness() + 0.209494) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00010038 * distance() + 0.111061) * (0.645607 - exp(-(-0.0235321 * distance() + 2.39673) * (enclose() + -0.0443038 * distance() + 0.75909) - (0.0599558 * distance() + -0.429733) * (enclose() + -0.359767 * distance() + 1.87244) * (enclose() + -0.359767 * distance() + 1.87244))) + 0.5 * 0.135986 * exp(-1.96798 * enclose()) * (0.0838562 - 0.123262 * exp(-0.513027 * distance()) * pow(distance() , 0.205865))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00010038 * distance() + 0.111061) * (0.645607 - exp(-(-0.0235321 * distance() + 2.39673) * (enclose() + -0.0443038 * distance() + 0.75909) - (0.0599558 * distance() + -0.429733) * (enclose() + -0.359767 * distance() + 1.87244) * (enclose() + -0.359767 * distance() + 1.87244))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0363784 * (1 - exp(-1.84764 * (enclose() + 0.370347))) * pow(width() , -0.00018312 * enclose() + 0.00977119) + 0.5 * (0.00698501 * exp(-1.35443 * enclose()) + 0.0037135 * exp(-2.25779 * enclose())) * pow(width() , 0.0377016 * enclose() + -0.00484232)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0363784 * (1 - exp(-1.84764 * (enclose() + 0.370347))) * pow(width() , -0.00018312 * enclose() + 0.00977119) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal4,metal7
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal3 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.932388 - 1.56125 * distance()) + 0.0401258 / pow(distance() , 1.38899)) * 3.41873 * pow((width1() + width2()) / 2 , 0.0633977) * (0.0730553 * (thickness1() + thickness2()) / 2 + 0.0243159) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal4,metal7
//
CAPACITANCE CROSSOVER FRINGE metal4 metal7 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0610726 * (1 - exp(-0.899392 * (distance() + 0.0250001))) * pow(width() , 0.00622484 * distance() + -0.0158073) * (0.140644 * thickness() + 0.138286)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0113227 * (1 - exp(-0.989267 * (distance() + -0.0297268)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0923951 * pow(width() , 0.0341083) * (0.115489 * thickness() + 0.0828249)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00020107 * distance() + 0.0264002) * (0.870481 - exp(-(-0.542011 * distance() + 0.293202) * (enclose() + 1.71456 * distance() + -0.122605) - (0.00031325 * distance() + 0.11825) * (enclose() + 2.72533 * distance() + -0.323055) * (enclose() + 2.72533 * distance() + -0.323055)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0610726 * (1 - exp(-0.899392 * (distance() + 0.0250001))) * pow(width() , 0.00622484 * distance() + -0.0158073) * (0.140644 * thickness() + 0.138286)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0121947 * (0.870481 - exp(5.12691 * (enclose() + 17.023) - 0.121382 * (enclose() + 26.9302) * (enclose() + 26.9302)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal4,metal7
//
CAPACITANCE CROSSOVER FRINGE metal4 metal3 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.172981 * (1 - exp(-1.04335 * (distance() + 0.0257157))) * pow(width() , 0.00366487 * distance() + -0.013847) * (0.0285221 * thickness() + 0.222557) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.172981 * (1 - exp(-1.04335 * (distance() + 0.0257157))) * pow(width() , 0.00366487 * distance() + -0.013847) * (0.0285221 * thickness() + 0.222557) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0399233 * (1 - exp(-1.09716 * (distance() + -0.00785732))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.162376 * pow(width() , 0.0187788) * (0.0640668 * thickness() + 0.225551) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-8.9597e-05 * distance() + 0.120913) * (0.648731 - exp(-(-0.0453159 * distance() + 2.04028) * (enclose() + -0.00205363 * distance() + 0.672018) - (0.0431217 * distance() + -0.412371) * (enclose() + -0.400499 * distance() + 1.59643) * (enclose() + -0.400499 * distance() + 1.59643))) + 0.5 * 0.171636 * exp(-1.58691 * enclose()) * (0.0912815 - 0.11383 * exp(-0.3498 * distance()) * pow(distance() , 0.138722))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-8.9597e-05 * distance() + 0.120913) * (0.648731 - exp(-(-0.0453159 * distance() + 2.04028) * (enclose() + -0.00205363 * distance() + 0.672018) - (0.0431217 * distance() + -0.412371) * (enclose() + -0.400499 * distance() + 1.59643) * (enclose() + -0.400499 * distance() + 1.59643))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0403689 * (1 - exp(-1.60988 * (enclose() + 0.406536))) * pow(width() , -0.00053153 * enclose() + 0.0212991) + 0.5 * (0.00728365 * exp(-0.926001 * enclose()) + 0.0071202 * exp(-1.85426 * enclose())) * pow(width() , 0.0463793 * enclose() + 0.012427)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0403689 * (1 - exp(-1.60988 * (enclose() + 0.406536))) * pow(width() , -0.00053153 * enclose() + 0.0212991) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal4,metal8
//
CAPACITANCE NEARBODY metal4 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-0.996332 - 1.4271 * distance()) + 0.0548558 / pow(distance() , 1.30081)) * 3.38851 * pow((width1() + width2()) / 2 , 0.0748077) * (0.067874 * (thickness1() + thickness2()) / 2 + 0.0232635) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal4,metal8
//
CAPACITANCE CROSSOVER FRINGE metal4 metal8 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.045952 * (1 - exp(-0.818997 * (distance() + 0.0586587))) * pow(width() , 0.006758 * distance() + -0.0124289) * (0.149021 * thickness() + 0.134389)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00842147 * (1 - exp(-0.901359 * (distance() + -0.0123298)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0827018 * pow(width() , 0.0458191) * (0.102205 * thickness() + 0.0679667)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (2.26602e-05 * distance() + 0.0238061) * (0.661175 - exp(-(0.00064346 * distance() + 0.0441632) * (enclose() + 1.72788 * distance() + 2.03681) - (0.0253254 * distance() + 0.0202018) * (enclose() + -0.205852 * distance() + 2.89959) * (enclose() + -0.205852 * distance() + 2.89959)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.045952 * (1 - exp(-0.818997 * (distance() + 0.0586587))) * pow(width() , 0.006758 * distance() + -0.0124289) * (0.149021 * thickness() + 0.134389)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0120164 * (0.661175 - exp(-0.0505978 * (enclose() + 19.3156) - 0.273456 * (enclose() + 0.84107) * (enclose() + 0.84107)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal4,metal8
//
CAPACITANCE CROSSOVER FRINGE metal4 metal3 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.182932 * (1 - exp(-0.969429 * (distance() + 0.0508241))) * pow(width() , 0.00368047 * distance() + -0.00914067) * (0.026924 * thickness() + 0.225126) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.182932 * (1 - exp(-0.969429 * (distance() + 0.0508241))) * pow(width() , 0.00368047 * distance() + -0.00914067) * (0.026924 * thickness() + 0.225126) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0423827 * (1 - exp(-1.01032 * (distance() + 0.00879961))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.171827 * pow(width() , 0.0284017) * (0.0628091 * thickness() + 0.229943) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (3.76099e-05 * distance() + 0.129332) * (0.636127 - exp(-(-0.0509402 * distance() + 1.7965) * (enclose() + 0.0359796 * distance() + 0.630026) - (0.0374165 * distance() + -0.392152) * (enclose() + -0.415544 * distance() + 1.41481) * (enclose() + -0.415544 * distance() + 1.41481))) + 0.5 * 0.205892 * exp(-1.3428 * enclose()) * (0.096619 - 0.108493 * exp(-0.257011 * distance()) * pow(distance() , 0.0946892))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (3.76099e-05 * distance() + 0.129332) * (0.636127 - exp(-(-0.0509402 * distance() + 1.7965) * (enclose() + 0.0359796 * distance() + 0.630026) - (0.0374165 * distance() + -0.392152) * (enclose() + -0.415544 * distance() + 1.41481) * (enclose() + -0.415544 * distance() + 1.41481))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0431892 * (1 - exp(-1.46338 * (enclose() + 0.432495))) * pow(width() , -0.00092481 * enclose() + 0.031274) + 0.5 * (0.0106104 * exp(-0.76352 * enclose()) + 0.0072644 * exp(-1.9323 * enclose())) * pow(width() , 0.0515307 * enclose() + 0.0278022)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0431892 * (1 - exp(-1.46338 * (enclose() + 0.432495))) * pow(width() , -0.00092481 * enclose() + 0.031274) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.880125 - 1.8227 * distance()) + 0.0247963 / pow(distance() , 1.52105)) * 3.43325 * pow((width1() + width2()) / 2 , 0.0456434) * (0.0829361 * (thickness1() + thickness2()) / 2 + 0.0264308) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.174995 * (1 - exp(-1.22025 * (distance() + -0.0114132))) * pow(width() , 0.00306648 * distance() + -0.0149298) * (0.0729346 * thickness() + 0.187071)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0372957 * (1 - exp(-1.27958 * (distance() + -0.0313644)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.149296 * pow(width() , 0.00818422) * (0.114938 * thickness() + 0.207613)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-2.5513e-06 * distance() + 0.112858) * (0.647395 - exp(-(-0.00999893 * distance() + 2.48432) * (enclose() + -0.043858 * distance() + 0.735112) - (0.0839199 * distance() + -0.480126) * (enclose() + -0.298737 * distance() + 1.77637) * (enclose() + -0.298737 * distance() + 1.77637)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.174995 * (1 - exp(-1.22025 * (distance() + -0.0114132))) * pow(width() , 0.00306648 * distance() + -0.0149298) * (0.0729346 * thickness() + 0.187071)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0564162 * (0.647395 - exp(-2.38433 * (enclose() + 0.296532) - 0.359073 * (enclose() + -1.211) * (enclose() + -1.211)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0804771 * (1 - exp(-1.08299 * (distance() + -0.0115262))) * pow(width() , 0.00485155 * distance() + -0.0161426) * (0.10747 * thickness() + 0.158965) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0804771 * (1 - exp(-1.08299 * (distance() + -0.0115262))) * pow(width() , 0.00485155 * distance() + -0.0161426) * (0.10747 * thickness() + 0.158965) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0157423 * (1 - exp(-1.1754 * (distance() + -0.045461))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.1061 * pow(width() , 0.0195516) * (0.106975 * thickness() + 0.110249) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00041037 * distance() + 0.0435825) * (0.753467 - exp(-(-0.560434 * distance() + 0.395776) * (enclose() + 1.93016 * distance() + 0.213403) - (0.00070175 * distance() + 0.118861) * (enclose() + 2.86819 * distance() + -0.139869) * (enclose() + 2.86819 * distance() + -0.139869))) + 0.5 * 0.0806738 * exp(-1.37566 * enclose()) * (0.0877184 - 0.116129 * exp(-0.445309 * distance()) * pow(distance() , 0.20544))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00041037 * distance() + 0.0435825) * (0.753467 - exp(-(-0.560434 * distance() + 0.395776) * (enclose() + 1.93016 * distance() + 0.213403) - (0.00070175 * distance() + 0.118861) * (enclose() + 2.86819 * distance() + -0.139869) * (enclose() + 2.86819 * distance() + -0.139869))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0159614 * (1 - exp(-1.24954 * (enclose() + 0.445232))) * pow(width() , 0.00113119 * enclose() + 0.0131275) + 0.5 * (0.00348765 * exp(-1.28289 * enclose()) + 0.00348231 * exp(-1.28375 * enclose())) * pow(width() , 0.0125198 * enclose() + 0.0360324)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0159614 * (1 - exp(-1.24954 * (enclose() + 0.445232))) * pow(width() , 0.00113119 * enclose() + 0.0131275) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal5,metal7
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal3 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.890459 - 1.28815 * distance()) + 0.0437649 / pow(distance() , 1.36705)) * 3.2511 * pow((width1() + width2()) / 2 , 0.0691404) * (0.0684912 * (thickness1() + thickness2()) / 2 + 0.0280633) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal5,metal7
//
CAPACITANCE CROSSOVER FRINGE metal5 metal7 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.110937 * (1 - exp(-0.701508 * (distance() + 0.0302838))) * pow(width() , 0.00802059 * distance() + -0.0329475) * (0.0828337 * thickness() + 0.172214)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0219679 * (1 - exp(-0.792082 * (distance() + -0.034915)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.122808 * pow(width() , 0.024604) * (0.0922898 * thickness() + 0.146437)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00094783 * distance() + 0.133844) * (0.33282 - exp(-(-0.158737 * distance() + 0.136085) * (enclose() + 4.94863 * distance() + 5.89421) - (0.0001288 * distance() + 0.0293489) * (enclose() + 4.9469 * distance() + 2.31028) * (enclose() + 4.9469 * distance() + 2.31028)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.110937 * (1 - exp(-0.701508 * (distance() + 0.0302838))) * pow(width() , 0.00802059 * distance() + -0.0329475) * (0.0828337 * thickness() + 0.172214)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0621828 * (0.33282 - exp(1.45128 * (enclose() + 55.3805) - 0.0306369 * (enclose() + 51.7793) * (enclose() + 51.7793)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal5,metal7
//
CAPACITANCE CROSSOVER FRINGE metal5 metal3 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.107152 * (1 - exp(-0.696708 * (distance() + 0.0339024))) * pow(width() , 0.00832151 * distance() + -0.035349) * (0.0665064 * thickness() + 0.182503) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.107152 * (1 - exp(-0.696708 * (distance() + 0.0339024))) * pow(width() , 0.00832151 * distance() + -0.035349) * (0.0665064 * thickness() + 0.182503) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.02166 * (1 - exp(-0.791353 * (distance() + -0.0342083))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.123544 * pow(width() , 0.0244767) * (0.0754776 * thickness() + 0.149052) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.000962 * distance() + 0.102622) * (0.437471 - exp(-(-0.180645 * distance() + 0.191421) * (enclose() + 3.54096 * distance() + 3.31275) - (0.0002288 * distance() + 0.0358672) * (enclose() + 3.97589 * distance() + 1.15998) * (enclose() + 3.97589 * distance() + 1.15998))) + 0.5 * 0.137294 * exp(-1.13078 * enclose()) * (0.0929168 - 0.110283 * exp(-0.286467 * distance()) * pow(distance() , 0.143317))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.000962 * distance() + 0.102622) * (0.437471 - exp(-(-0.180645 * distance() + 0.191421) * (enclose() + 3.54096 * distance() + 3.31275) - (0.0002288 * distance() + 0.0358672) * (enclose() + 3.97589 * distance() + 1.15998) * (enclose() + 3.97589 * distance() + 1.15998))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0219266 * (1 - exp(-1.01139 * (enclose() + 0.527176))) * pow(width() , 0.00071011 * enclose() + 0.0207561) + 0.5 * (0.00505908 * exp(-0.984632 * enclose()) + 0.0061213 * exp(-0.982594 * enclose())) * pow(width() , 0.0160089 * enclose() + 0.024739)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0219266 * (1 - exp(-1.01139 * (enclose() + 0.527176))) * pow(width() , 0.00071011 * enclose() + 0.0207561) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal5,metal8
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.909447 - 1.04676 * distance()) + 0.0682164 / pow(distance() , 1.25743)) * 3.16067 * pow((width1() + width2()) / 2 , 0.0876452) * (0.0593187 * (thickness1() + thickness2()) / 2 + 0.0261707) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal5,metal8
//
CAPACITANCE CROSSOVER FRINGE metal5 metal8 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0805016 * (1 - exp(-0.536432 * (distance() + 0.0972747))) * pow(width() , 0.0112675 * distance() + -0.047065) * (0.0880039 * thickness() + 0.168064)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0155151 * (1 - exp(-0.631803 * (distance() + -0.0155034)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.107757 * pow(width() , 0.037595) * (0.0800428 * thickness() + 0.117194)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-7.89698e-05 * distance() + 0.0446827) * (0.66045 - exp(-(0.017085 * distance() + 0.200183) * (enclose() + 0.0268272 * distance() + 1.90148) - (0.0158847 * distance() + -0.00646142) * (enclose() + -0.196487 * distance() + 3.55783) * (enclose() + -0.196487 * distance() + 3.55783)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0805016 * (1 - exp(-0.536432 * (distance() + 0.0972747))) * pow(width() , 0.0112675 * distance() + -0.047065) * (0.0880039 * thickness() + 0.168064)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0219465 * (0.66045 - exp(-0.371033 * (enclose() + 2.16975) - 0.152386 * (enclose() + 1.59296) * (enclose() + 1.59296)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal5,metal8
//
CAPACITANCE CROSSOVER FRINGE metal5 metal3 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.12684 * (1 - exp(-0.551589 * (distance() + 0.0951097))) * pow(width() , 0.0106848 * distance() + -0.0462835) * (0.0509668 * thickness() + 0.190286) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.12684 * (1 - exp(-0.551589 * (distance() + 0.0951097))) * pow(width() , 0.0106848 * distance() + -0.0462835) * (0.0509668 * thickness() + 0.190286) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.025589 * (1 - exp(-0.646365 * (distance() + -0.0131957))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.133576 * pow(width() , 0.0340926) * (0.0663092 * thickness() + 0.170482) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00126057 * distance() + 0.176029) * (0.292925 - exp(-(-0.12703 * distance() + 0.134076) * (enclose() + 4.99792 * distance() + 7.122) - (9.60959e-05 * distance() + 0.0201796) * (enclose() + 5.34057 * distance() + 2.71965) * (enclose() + 5.34057 * distance() + 2.71965))) + 0.5 * 0.197652 * exp(-0.955932 * enclose()) * (0.0973445 - 0.106017 * exp(-0.19165 * distance()) * pow(distance() , 0.0988201))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00126057 * distance() + 0.176029) * (0.292925 - exp(-(-0.12703 * distance() + 0.134076) * (enclose() + 4.99792 * distance() + 7.122) - (9.60959e-05 * distance() + 0.0201796) * (enclose() + 5.34057 * distance() + 2.71965) * (enclose() + 5.34057 * distance() + 2.71965))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0260275 * (1 - exp(-0.870847 * (enclose() + 0.582562))) * pow(width() , 0.00033735 * enclose() + 0.0314562) + 0.5 * (0.00659499 * exp(-0.716866 * enclose()) + 0.008562 * exp(-0.875647 * enclose())) * pow(width() , 0.0195194 * enclose() + 0.0312852)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0260275 * (1 - exp(-0.870847 * (enclose() + 0.582562))) * pow(width() , 0.00033735 * enclose() + 0.0314562) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal6,metal7
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal3 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-0.930086 - 1.57707 * distance()) + 0.0395336 / pow(distance() , 1.39104)) * 3.42845 * pow((width1() + width2()) / 2 , 0.0661209) * (0.0725269 * (thickness1() + thickness2()) / 2 + 0.0248499) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.09)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal6,metal7
//
CAPACITANCE CROSSOVER FRINGE metal6 metal7 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.191418 * (1 - exp(-1.06337 * (distance() + 0.0199786))) * pow(width() , 0.00371659 * distance() + -0.0137946) * (0.0634338 * thickness() + 0.19218)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0408805 * (1 - exp(-1.11496 * (distance() + -0.0127402)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.15654 * pow(width() , 0.0194896) * (0.106732 * thickness() + 0.225436)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-6.08012e-05 * distance() + 0.570886) * (0.138684 - exp(-(-0.128976 * distance() + 0.131993) * (enclose() + 2.97636 * distance() + 5.97141) - (0.00209433 * distance() + 0.0691179) * (enclose() + 1.81936 * distance() + 3.93458) * (enclose() + 1.81936 * distance() + 3.93458)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.191418 * (1 - exp(-1.06337 * (distance() + 0.0199786))) * pow(width() , 0.00371659 * distance() + -0.0137946) * (0.0634338 * thickness() + 0.19218)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.285139 * (0.138684 - exp(1.15777 * (enclose() + 35.735) - 0.0900612 * (enclose() + 22.1282) * (enclose() + 22.1282)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal6,metal7
//
CAPACITANCE CROSSOVER FRINGE metal6 metal3 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0584726 * (1 - exp(-0.904447 * (distance() + 0.0301137))) * pow(width() , 0.00658322 * distance() + -0.0169843) * (0.123538 * thickness() + 0.149539) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0584726 * (1 - exp(-0.904447 * (distance() + 0.0301137))) * pow(width() , 0.00658322 * distance() + -0.0169843) * (0.123538 * thickness() + 0.149539) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0111094 * (1 - exp(-1.0001 * (distance() + -0.028279))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.092854 * pow(width() , 0.0362762) * (0.0979402 * thickness() + 0.0866467) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00025944 * distance() + 0.0270055) * (0.8503 - exp(-(-0.637869 * distance() + 0.241132) * (enclose() + 1.71069 * distance() + -0.0292947) - (0.00035197 * distance() + 0.123909) * (enclose() + 2.8856 * distance() + -0.0922606) * (enclose() + 2.8856 * distance() + -0.0922606))) + 0.5 * 0.0781743 * exp(-0.933174 * enclose()) * (0.0976503 - 0.101812 * exp(-0.250048 * distance()) * pow(distance() , 0.111971))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00025944 * distance() + 0.0270055) * (0.8503 - exp(-(-0.637869 * distance() + 0.241132) * (enclose() + 1.71069 * distance() + -0.0292947) - (0.00035197 * distance() + 0.123909) * (enclose() + 2.8856 * distance() + -0.0922606) * (enclose() + 2.8856 * distance() + -0.0922606))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0114588 * (1 - exp(-0.835909 * (enclose() + 0.514628))) * pow(width() , 0.00290947 * enclose() + 0.014889) + 0.5 * (0.00368668 * exp(-0.89379 * enclose()) + 0.00368633 * exp(-0.889686 * enclose())) * pow(width() , 0.00729222 * enclose() + 0.0883136)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0114588 * (1 - exp(-0.835909 * (enclose() + 0.514628))) * pow(width() , 0.00290947 * enclose() + 0.014889) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal6,metal8
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-0.900008 - 1.02285 * distance()) + 0.0696372 / pow(distance() , 1.25494)) * 3.1371 * pow((width1() + width2()) / 2 , 0.0897661) * (0.0586297 * (thickness1() + thickness2()) / 2 + 0.0262773) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.09)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal6,metal8
//
CAPACITANCE CROSSOVER FRINGE metal6 metal8 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.12121 * (1 - exp(-0.533542 * (distance() + 0.0971302))) * pow(width() , 0.0111439 * distance() + -0.0487557) * (0.0670685 * thickness() + 0.180237)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.023935 * (1 - exp(-0.628155 * (distance() + -0.0152478)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.128638 * pow(width() , 0.0347953) * (0.0793949 * thickness() + 0.159867)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00030484 * distance() + 0.092677) * (0.500542 - exp(-(-0.0832667 * distance() + 0.0968912) * (enclose() + 3.79126 * distance() + 4.78458) - (0.00011038 * distance() + 0.0302395) * (enclose() + 3.04773 * distance() + 1.89557) * (enclose() + 3.04773 * distance() + 1.89557)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.12121 * (1 - exp(-0.533542 * (distance() + 0.0971302))) * pow(width() , 0.0111439 * distance() + -0.0487557) * (0.0670685 * thickness() + 0.180237)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0448143 * (0.500542 - exp(0.735776 * (enclose() + 42.6972) - 0.0313433 * (enclose() + 32.3729) * (enclose() + 32.3729)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal6,metal8
//
CAPACITANCE CROSSOVER FRINGE metal6 metal3 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.08562 * (1 - exp(-0.518107 * (distance() + 0.106331))) * pow(width() , 0.0119691 * distance() + -0.051507) * (0.070695 * thickness() + 0.178324) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.08562 * (1 - exp(-0.518107 * (distance() + 0.106331))) * pow(width() , 0.0119691 * distance() + -0.051507) * (0.070695 * thickness() + 0.178324) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0167752 * (1 - exp(-0.616959 * (distance() + -0.0126813))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.112363 * pow(width() , 0.0381368) * (0.0678063 * thickness() + 0.127119) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (6.12932e-05 * distance() + 0.0508654) * (0.612159 - exp(-(0.0224585 * distance() + 0.368789) * (enclose() + -0.0149628 * distance() + 1.77993) - (0.0222742 * distance() + -0.036417) * (enclose() + -0.289061 * distance() + 2.80731) * (enclose() + -0.289061 * distance() + 2.80731))) + 0.5 * 0.154885 * exp(-0.783341 * enclose()) * (0.0994825 - 0.102367 * exp(-0.159867 * distance()) * pow(distance() , 0.0807086))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (6.12932e-05 * distance() + 0.0508654) * (0.612159 - exp(-(0.0224585 * distance() + 0.368789) * (enclose() + -0.0149628 * distance() + 1.77993) - (0.0222742 * distance() + -0.036417) * (enclose() + -0.289061 * distance() + 2.80731) * (enclose() + -0.289061 * distance() + 2.80731))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0171828 * (1 - exp(-0.688283 * (enclose() + 0.627406))) * pow(width() , 0.00189301 * enclose() + 0.0250021) + 0.5 * (0.00595928 * exp(-0.702805 * enclose()) + 0.00595972 * exp(-0.699299 * enclose())) * pow(width() , 0.00940785 * enclose() + 0.0553299)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0171828 * (1 - exp(-0.688283 * (enclose() + 0.627406))) * pow(width() , 0.00189301 * enclose() + 0.0250021) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal3,metal7,metal8
//
CAPACITANCE NEARBODY metal7 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(-0.950381 - 1.29869 * distance()) + 0.0572964 / pow(distance() , 1.28381)) * 3.32867 * pow((width1() + width2()) / 2 , 0.0802635) * (0.0644877 * (thickness1() + thickness2()) / 2 + 0.0259003) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.91)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal3,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal7 metal8 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.187608 * (1 - exp(-0.809513 * (distance() + 0.0554104))) * pow(width() , 0.00574404 * distance() + -0.0195895) * (0.0581801 * thickness() + 0.189686)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0388873 * (1 - exp(-0.871505 * (distance() + -0.0037227)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.154085 * pow(width() , 0.0296546) * (0.0978507 * thickness() + 0.222137)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00080687 * distance() + 0.110307) * (0.642747 - exp(-(0.0936878 * distance() + 0.541546) * (enclose() + -0.0167629 * distance() + 1.00945) - (0.010075 * distance() + -0.0057062) * (enclose() + -0.720055 * distance() + 6.58612) * (enclose() + -0.720055 * distance() + 6.58612)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.187608 * (1 - exp(-0.809513 * (distance() + 0.0554104))) * pow(width() , 0.00574404 * distance() + -0.0195895) * (0.0581801 * thickness() + 0.189686)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0591879 * (0.642747 - exp(-1.47842 * (enclose() + 0.841821) - 0.0950438 * (enclose() + -0.61443) * (enclose() + -0.61443)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal3,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal7 metal3 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0508117 * (1 - exp(-0.710311 * (distance() + 0.0774877))) * pow(width() , 0.00842635 * distance() + -0.0222533) * (0.114939 * thickness() + 0.155986) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0508117 * (1 - exp(-0.710311 * (distance() + 0.0774877))) * pow(width() , 0.00842635 * distance() + -0.0222533) * (0.114939 * thickness() + 0.155986) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00972163 * (1 - exp(-0.802973 * (distance() + -0.0111516))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0894017 * pow(width() , 0.04579) * (0.0809533 * thickness() + 0.0826633) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (1.06263e-05 * distance() + 0.0279272) * (0.651532 - exp(-(0.024456 * distance() + 0.0430158) * (enclose() + 0.0726545 * distance() + 2.16433) - (0.0186098 * distance() + 0.0149811) * (enclose() + -0.316382 * distance() + 3.56604) * (enclose() + -0.316382 * distance() + 3.56604))) + 0.5 * 0.0980999 * exp(-0.685905 * enclose()) * (0.102534 - 0.0954149 * exp(-0.129832 * distance()) * pow(distance() , 0.0434776))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (1.06263e-05 * distance() + 0.0279272) * (0.651532 - exp(-(0.024456 * distance() + 0.0430158) * (enclose() + 0.0726545 * distance() + 2.16433) - (0.0186098 * distance() + 0.0149811) * (enclose() + -0.316382 * distance() + 3.56604) * (enclose() + -0.316382 * distance() + 3.56604))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0101557 * (1 - exp(-0.597503 * (enclose() + 0.583017))) * pow(width() , 0.00478638 * enclose() + 0.00921026) + 0.5 * (0.00414218 * exp(-0.65801 * enclose()) + 0.00414227 * exp(-0.656158 * enclose())) * pow(width() , 0.00521617 * enclose() + 0.110063)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0101557 * (1 - exp(-0.597503 * (enclose() + 0.583017))) * pow(width() , 0.00478638 * enclose() + 0.00921026) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.91)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal4,metal5,metal6
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal4 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.894788 - 2.53586 * distance()) + 0.00979292 / pow(distance() , 1.75994)) * 3.61615 * pow((width1() + width2()) / 2 , 0.00979837) * (0.112643 * (thickness1() + thickness2()) / 2 + 0.0262923) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal4,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.148444 * (1 - exp(-1.73565 * (distance() + -0.0512693))) * pow(width() , 0.0017493 * distance() + -0.0127743) * (0.105757 * thickness() + 0.163142)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0301004 * (1 - exp(-1.79856 * (distance() + -0.0551487)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.135376 * pow(width() , 0) * (0.165392 * thickness() + 0.15889)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00159591 * distance() + 0.319823) * (0.192272 - exp(-(0.359319 * distance() + 0.945219) * (enclose() + 0.0364884 * distance() + 1.63341) - (0.00011861 * distance() + -0.00208975) * (enclose() + 9.01374 * distance() + -4.87326) * (enclose() + 9.01374 * distance() + -4.87326)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.148444 * (1 - exp(-1.73565 * (distance() + -0.0512693))) * pow(width() , 0.0017493 * distance() + -0.0127743) * (0.105757 * thickness() + 0.163142)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.151932 * (0.192272 - exp(-4.53841 * (enclose() + 1.99829) - -0.00090365 * (enclose() + 85.2641) * (enclose() + 85.2641)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal4,metal5,metal6
//
CAPACITANCE CROSSOVER FRINGE metal5 metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.133374 * (1 - exp(-1.74201 * (distance() + -0.0475697))) * pow(width() , 0.00167653 * distance() + -0.0118059) * (0.0607052 * thickness() + 0.1949) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.133374 * (1 - exp(-1.74201 * (distance() + -0.0475697))) * pow(width() , 0.00167653 * distance() + -0.0118059) * (0.0607052 * thickness() + 0.1949) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0290058 * (1 - exp(-1.80735 * (distance() + -0.0523723))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.137209 * pow(width() , -0.00375976) * (0.0989333 * thickness() + 0.172542) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00094054 * distance() + 0.184606) * (0.321733 - exp(-(0.353466 * distance() + 0.94322) * (enclose() + 0.09941 * distance() + 1.1728) - (0.00027645 * distance() + -0.00530688) * (enclose() + 6.00669 * distance() + -4.97145) * (enclose() + 6.00669 * distance() + -4.97145))) + 0.5 * 0.0706989 * exp(-2.72627 * enclose()) * (0.0715716 - 0.136243 * exp(-0.829658 * distance()) * pow(distance() , 0.308103))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00094054 * distance() + 0.184606) * (0.321733 - exp(-(0.353466 * distance() + 0.94322) * (enclose() + 0.09941 * distance() + 1.1728) - (0.00027645 * distance() + -0.00530688) * (enclose() + 6.00669 * distance() + -4.97145) * (enclose() + 6.00669 * distance() + -4.97145))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0288077 * (1 - exp(-2.42681 * (enclose() + 0.312903))) * pow(width() , 0.00012734 * enclose() + -0.00255462) + 0.5 * (0.00255324 * exp(-2.33485 * enclose()) + 0.00227266 * exp(-2.32123 * enclose())) * pow(width() , 0.0344261 * enclose() + -0.0264405)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0288077 * (1 - exp(-2.42681 * (enclose() + 0.312903))) * pow(width() , 0.00012734 * enclose() + -0.00255462) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal4,metal5,metal7
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal4 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.882223 - 1.81085 * distance()) + 0.0251309 / pow(distance() , 1.51878)) * 3.4249 * pow((width1() + width2()) / 2 , 0.0430143) * (0.0835079 * (thickness1() + thickness2()) / 2 + 0.0259271) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal4,metal5,metal7
//
CAPACITANCE CROSSOVER FRINGE metal5 metal7 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0850491 * (1 - exp(-1.07712 * (distance() + -0.0155171))) * pow(width() , 0.004571 * distance() + -0.0144832) * (0.128914 * thickness() + 0.144555)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0160931 * (1 - exp(-1.16454 * (distance() + -0.0468504)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.10546 * pow(width() , 0.0184979) * (0.129674 * thickness() + 0.105974)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00034074 * distance() + 0.0438865) * (0.753646 - exp(-(-0.588593 * distance() + 0.355214) * (enclose() + 2.02584 * distance() + 0.254749) - (0.00061631 * distance() + 0.125046) * (enclose() + 2.95632 * distance() + -0.0291307) * (enclose() + 2.95632 * distance() + -0.0291307)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0850491 * (1 - exp(-1.07712 * (distance() + -0.0155171))) * pow(width() , 0.004571 * distance() + -0.0144832) * (0.128914 * thickness() + 0.144555)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0202396 * (0.753646 - exp(5.53072 * (enclose() + 20.5131) - 0.131209 * (enclose() + 29.5341) * (enclose() + 29.5341)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal4,metal5,metal7
//
CAPACITANCE CROSSOVER FRINGE metal5 metal4 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.160406 * (1 - exp(-1.20447 * (distance() + -0.00688679))) * pow(width() , 0.00298934 * distance() + -0.0147598) * (0.034396 * thickness() + 0.214624) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.160406 * (1 - exp(-1.20447 * (distance() + -0.00688679))) * pow(width() , 0.00298934 * distance() + -0.0147598) * (0.034396 * thickness() + 0.214624) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0363018 * (1 - exp(-1.26542 * (distance() + -0.0270396))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.154768 * pow(width() , 0.00756772) * (0.0682754 * thickness() + 0.209493) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.0004697 * distance() + 0.130949) * (0.528094 - exp(-(0.119392 * distance() + 0.596047) * (enclose() + 0.0251331 * distance() + 0.492245) - (0.0371173 * distance() + 0.0119231) * (enclose() + -0.463752 * distance() + 4.26576) * (enclose() + -0.463752 * distance() + 4.26576))) + 0.5 * 0.11054 * exp(-1.98072 * enclose()) * (0.0847049 - 0.121171 * exp(-0.481038 * distance()) * pow(distance() , 0.192977))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.0004697 * distance() + 0.130949) * (0.528094 - exp(-(0.119392 * distance() + 0.596047) * (enclose() + 0.0251331 * distance() + 0.492245) - (0.0371173 * distance() + 0.0119231) * (enclose() + -0.463752 * distance() + 4.26576) * (enclose() + -0.463752 * distance() + 4.26576))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0363825 * (1 - exp(-1.8658 * (enclose() + 0.377505))) * pow(width() , -0.00020979 * enclose() + 0.0100486) + 0.5 * (0.00356251 * exp(-2.18616 * enclose()) + 0.0051907 * exp(-1.32437 * enclose())) * pow(width() , 0.0421598 * enclose() + -0.0103644)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0363825 * (1 - exp(-1.8658 * (enclose() + 0.377505))) * pow(width() , -0.00020979 * enclose() + 0.0100486) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal4,metal5,metal8
//
CAPACITANCE NEARBODY metal5 INSIDE OF metal4 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.942647 - 1.53248 * distance()) + 0.042837 / pow(distance() , 1.37066)) * 3.41862 * pow((width1() + width2()) / 2 , 0.0658869) * (0.0717621 * (thickness1() + thickness2()) / 2 + 0.0240454) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal4,metal5,metal8
//
CAPACITANCE CROSSOVER FRINGE metal5 metal8 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0581117 * (1 - exp(-0.879718 * (distance() + 0.0314028))) * pow(width() , 0.00647938 * distance() + -0.0167072) * (0.141909 * thickness() + 0.137222)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0107292 * (1 - exp(-0.969967 * (distance() + -0.0271272)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0906188 * pow(width() , 0.0357908) * (0.113113 * thickness() + 0.0796938)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (6.16089e-05 * distance() + 0.0332642) * (0.605674 - exp(-(0.00168292 * distance() + 0.132662) * (enclose() + 0.63632 * distance() + 1.91369) - (0.0425164 * distance() + 0.0117857) * (enclose() + -0.218127 * distance() + 2.55477) * (enclose() + -0.218127 * distance() + 2.55477)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0581117 * (1 - exp(-0.879718 * (distance() + 0.0314028))) * pow(width() , 0.00647938 * distance() + -0.0167072) * (0.141909 * thickness() + 0.137222)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0169401 * (0.605674 - exp(-0.149491 * (enclose() + 8.27689) - 0.43695 * (enclose() + 0.3735) * (enclose() + 0.3735)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal4,metal5,metal8
//
CAPACITANCE CROSSOVER FRINGE metal5 metal4 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.175189 * (1 - exp(-1.02665 * (distance() + 0.0306289))) * pow(width() , 0.00369883 * distance() + -0.01316) * (0.0279307 * thickness() + 0.222869) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.175189 * (1 - exp(-1.02665 * (distance() + 0.0306289))) * pow(width() , 0.00369883 * distance() + -0.01316) * (0.0279307 * thickness() + 0.222869) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0404129 * (1 - exp(-1.07827 * (distance() + -0.00472297))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.16332 * pow(width() , 0.0206259) * (0.0640626 * thickness() + 0.227725) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00053709 * distance() + 1.12106) * (0.0700375 - exp(-(-0.153975 * distance() + 0.185832) * (enclose() + 3.05329 * distance() + 7.51441) - (0.00114998 * distance() + 0.052186) * (enclose() + 2.46647 * distance() + 4.68465) * (enclose() + 2.46647 * distance() + 4.68465))) + 0.5 * 0.148676 * exp(-1.54346 * enclose()) * (0.0929797 - 0.110755 * exp(-0.30061 * distance()) * pow(distance() , 0.117996))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00053709 * distance() + 1.12106) * (0.0700375 - exp(-(-0.153975 * distance() + 0.185832) * (enclose() + 3.05329 * distance() + 7.51441) - (0.00114998 * distance() + 0.052186) * (enclose() + 2.46647 * distance() + 4.68465) * (enclose() + 2.46647 * distance() + 4.68465))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0409324 * (1 - exp(-1.60125 * (enclose() + 0.420508))) * pow(width() , -0.00062436 * enclose() + 0.0234925) + 0.5 * (0.0078698 * exp(-0.949889 * enclose()) + 0.00462963 * exp(-2.12267 * enclose())) * pow(width() , 0.0517581 * enclose() + 0.00999115)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0409324 * (1 - exp(-1.60125 * (enclose() + 0.420508))) * pow(width() , -0.00062436 * enclose() + 0.0234925) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal4,metal6,metal7
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal4 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-0.880122 - 1.8227 * distance()) + 0.0247963 / pow(distance() , 1.52106)) * 3.43325 * pow((width1() + width2()) / 2 , 0.0456431) * (0.0829335 * (thickness1() + thickness2()) / 2 + 0.0264315) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal4,metal6,metal7
//
CAPACITANCE CROSSOVER FRINGE metal6 metal7 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.174992 * (1 - exp(-1.22025 * (distance() + -0.0114134))) * pow(width() , 0.0030665 * distance() + -0.0149307) * (0.0729165 * thickness() + 0.187082)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0372957 * (1 - exp(-1.27958 * (distance() + -0.0313634)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.149297 * pow(width() , 0.00818624) * (0.114913 * thickness() + 0.207621)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (8.41294e-06 * distance() + 0.105419) * (0.69252 - exp(-(-0.0112738 * distance() + 2.50143) * (enclose() + -0.0429355 * distance() + 0.704092) - (0.0867964 * distance() + -0.488925) * (enclose() + -0.293342 * distance() + 1.76176) * (enclose() + -0.293342 * distance() + 1.76176)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.174992 * (1 - exp(-1.22025 * (distance() + -0.0114134))) * pow(width() , 0.0030665 * distance() + -0.0149307) * (0.0729165 * thickness() + 0.187082)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0527516 * (0.69252 - exp(-2.38869 * (enclose() + 0.274737) - 0.379039 * (enclose() + -1.17166) * (enclose() + -1.17166)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal4,metal6,metal7
//
CAPACITANCE CROSSOVER FRINGE metal6 metal4 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0804772 * (1 - exp(-1.08299 * (distance() + -0.0115264))) * pow(width() , 0.00485155 * distance() + -0.0161427) * (0.107471 * thickness() + 0.158964) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0804772 * (1 - exp(-1.08299 * (distance() + -0.0115264))) * pow(width() , 0.00485155 * distance() + -0.0161427) * (0.107471 * thickness() + 0.158964) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0157423 * (1 - exp(-1.1754 * (distance() + -0.0454614))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.1061 * pow(width() , 0.0195516) * (0.106975 * thickness() + 0.110249) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00041946 * distance() + 0.0450875) * (0.727461 - exp(-(-0.522025 * distance() + 0.352351) * (enclose() + 1.99553 * distance() + 0.34536) - (0.00068527 * distance() + 0.112549) * (enclose() + 2.88636 * distance() + 0.0430142) * (enclose() + 2.88636 * distance() + 0.0430142))) + 0.5 * 0.0656495 * exp(-1.38895 * enclose()) * (0.0890347 - 0.115083 * exp(-0.416089 * distance()) * pow(distance() , 0.193452))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00041946 * distance() + 0.0450875) * (0.727461 - exp(-(-0.522025 * distance() + 0.352351) * (enclose() + 1.99553 * distance() + 0.34536) - (0.00068527 * distance() + 0.112549) * (enclose() + 2.88636 * distance() + 0.0430142) * (enclose() + 2.88636 * distance() + 0.0430142))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0159585 * (1 - exp(-1.26088 * (enclose() + 0.462779))) * pow(width() , 0.00105677 * enclose() + 0.0136006) + 0.5 * (0.00285697 * exp(-1.29026 * enclose()) + 0.00285696 * exp(-1.2921 * enclose())) * pow(width() , 0.0168748 * enclose() + 0.0304152)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0159585 * (1 - exp(-1.26088 * (enclose() + 0.462779))) * pow(width() , 0.00105677 * enclose() + 0.0136006) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal4,metal6,metal8
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal4 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-0.89447 - 1.23204 * distance()) + 0.0480031 / pow(distance() , 1.34343)) * 3.22607 * pow((width1() + width2()) / 2 , 0.073586) * (0.0666651 * (thickness1() + thickness2()) / 2 + 0.0278351) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal4,metal6,metal8
//
CAPACITANCE CROSSOVER FRINGE metal6 metal8 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.104559 * (1 - exp(-0.659847 * (distance() + 0.0419662))) * pow(width() , 0.00885295 * distance() + -0.0373363) * (0.083849 * thickness() + 0.171082)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0205714 * (1 - exp(-0.753505 * (distance() + -0.0329394)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.119828 * pow(width() , 0.0268654) * (0.0900005 * thickness() + 0.140259)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00047197 * distance() + 0.0727569) * (0.567825 - exp(-(-0.0957875 * distance() + 0.108488) * (enclose() + 3.33098 * distance() + 3.15147) - (0.00027348 * distance() + 0.0423392) * (enclose() + 2.53388 * distance() + 1.4274) * (enclose() + 2.53388 * distance() + 1.4274)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.104559 * (1 - exp(-0.659847 * (distance() + 0.0419662))) * pow(width() , 0.00885295 * distance() + -0.0373363) * (0.083849 * thickness() + 0.171082)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0340186 * (0.567825 - exp(0.849387 * (enclose() + 36.4613) - 0.045074 * (enclose() + 26.7662) * (enclose() + 26.7662)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal4,metal6,metal8
//
CAPACITANCE CROSSOVER FRINGE metal6 metal4 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.111177 * (1 - exp(-0.661838 * (distance() + 0.0449291))) * pow(width() , 0.00886933 * distance() + -0.0380026) * (0.0626752 * thickness() + 0.184212) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.111177 * (1 - exp(-0.661838 * (distance() + 0.0449291))) * pow(width() , 0.00886933 * distance() + -0.0380026) * (0.0626752 * thickness() + 0.184212) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0224516 * (1 - exp(-0.757883 * (distance() + -0.0308352))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.125512 * pow(width() , 0.0263581) * (0.0733517 * thickness() + 0.153714) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00096524 * distance() + 0.114025) * (0.40409 - exp(-(-0.159826 * distance() + 0.170793) * (enclose() + 3.82511 * distance() + 4.06846) - (0.00019512 * distance() + 0.0321776) * (enclose() + 4.10799 * distance() + 1.4929) * (enclose() + 4.10799 * distance() + 1.4929))) + 0.5 * 0.12525 * exp(-1.10622 * enclose()) * (0.0939849 - 0.107628 * exp(-0.243109 * distance()) * pow(distance() , 0.124485))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00096524 * distance() + 0.114025) * (0.40409 - exp(-(-0.159826 * distance() + 0.170793) * (enclose() + 3.82511 * distance() + 4.06846) - (0.00019512 * distance() + 0.0321776) * (enclose() + 4.10799 * distance() + 1.4929) * (enclose() + 4.10799 * distance() + 1.4929))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0227478 * (1 - exp(-0.99434 * (enclose() + 0.562301))) * pow(width() , 0.00051757 * enclose() + 0.0237691) + 0.5 * (0.0052529 * exp(-0.952533 * enclose()) + 0.00467468 * exp(-0.947968 * enclose())) * pow(width() , 0.0207233 * enclose() + 0.0203607)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0227478 * (1 - exp(-0.99434 * (enclose() + 0.562301))) * pow(width() , 0.00051757 * enclose() + 0.0237691) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal4,metal7,metal8
//
CAPACITANCE NEARBODY metal7 INSIDE OF metal4 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(-0.915355 - 1.40981 * distance()) + 0.0434834 / pow(distance() , 1.35788)) * 3.34979 * pow((width1() + width2()) / 2 , 0.0708425) * (0.0697432 * (thickness1() + thickness2()) / 2 + 0.0272851) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.09)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal4,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal7 metal8 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.176991 * (1 - exp(-0.871089 * (distance() + 0.0294452))) * pow(width() , 0.00551585 * distance() + -0.0222536) * (0.0627072 * thickness() + 0.187109)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0366881 * (1 - exp(-0.940969 * (distance() + -0.0187411)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.149752 * pow(width() , 0.021679) * (0.0989415 * thickness() + 0.211696)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00015787 * distance() + 0.115343) * (0.611562 - exp(-(-0.00163775 * distance() + 1.65271) * (enclose() + -0.0316105 * distance() + 0.977132) - (0.0343529 * distance() + -0.254348) * (enclose() + -0.402208 * distance() + 2.23424) * (enclose() + -0.402208 * distance() + 2.23424)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.176991 * (1 - exp(-0.871089 * (distance() + 0.0294452))) * pow(width() , 0.00551585 * distance() + -0.0222536) * (0.0627072 * thickness() + 0.187109)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0584609 * (0.611562 - exp(-1.63633 * (enclose() + 0.661027) - 0.089181 * (enclose() + -1.78784) * (enclose() + -1.78784)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal4,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal7 metal4 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0640867 * (1 - exp(-0.778608 * (distance() + 0.0429769))) * pow(width() , 0.00776698 * distance() + -0.0245286) * (0.106769 * thickness() + 0.160213) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0640867 * (1 - exp(-0.778608 * (distance() + 0.0429769))) * pow(width() , 0.00776698 * distance() + -0.0245286) * (0.106769 * thickness() + 0.160213) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.012433 * (1 - exp(-0.875483 * (distance() + -0.0269004))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0976743 * pow(width() , 0.0353235) * (0.0893063 * thickness() + 0.0971955) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00033968 * distance() + 0.0307602) * (0.844238 - exp(-(-0.372471 * distance() + 0.249321) * (enclose() + 1.95759 * distance() + 0.0534882) - (0.00025479 * distance() + 0.0776843) * (enclose() + 2.96066 * distance() + -0.307972) * (enclose() + 2.96066 * distance() + -0.307972))) + 0.5 * 0.0783806 * exp(-0.90891 * enclose()) * (0.0982463 - 0.101447 * exp(-0.209159 * distance()) * pow(distance() , 0.0972638))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00033968 * distance() + 0.0307602) * (0.844238 - exp(-(-0.372471 * distance() + 0.249321) * (enclose() + 1.95759 * distance() + 0.0534882) - (0.00025479 * distance() + 0.0776843) * (enclose() + 2.96066 * distance() + -0.307972) * (enclose() + 2.96066 * distance() + -0.307972))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0127822 * (1 - exp(-0.811077 * (enclose() + 0.578988))) * pow(width() , 0.0024143 * enclose() + 0.0181393) + 0.5 * (0.00345881 * exp(-0.853034 * enclose()) + 0.00345807 * exp(-0.853204 * enclose())) * pow(width() , 0.0114202 * enclose() + 0.070553)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0127822 * (1 - exp(-0.811077 * (enclose() + 0.578988))) * pow(width() , 0.0024143 * enclose() + 0.0181393) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.09)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal5,metal6,metal7
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal5 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-0.894771 - 2.53585 * distance()) + 0.00979296 / pow(distance() , 1.75995)) * 3.61615 * pow((width1() + width2()) / 2 , 0.00979892) * (0.112639 * (thickness1() + thickness2()) / 2 + 0.026293) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal5,metal6,metal7
//
CAPACITANCE CROSSOVER FRINGE metal6 metal7 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.14844 * (1 - exp(-1.73565 * (distance() + -0.0512696))) * pow(width() , 0.00174936 * distance() + -0.0127749) * (0.105743 * thickness() + 0.163152)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0301003 * (1 - exp(-1.79856 * (distance() + -0.0551513)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.135378 * pow(width() , 0) * (0.165364 * thickness() + 0.158899)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00123212 * distance() + 0.251537) * (0.24424 - exp(-(0.369952 * distance() + 0.929356) * (enclose() + 0.0124862 * distance() + 1.4598) - (0.0001907 * distance() + -0.00309259) * (enclose() + 7.1117 * distance() + -5.74613) * (enclose() + 7.1117 * distance() + -5.74613)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.14844 * (1 - exp(-1.73565 * (distance() + -0.0512696))) * pow(width() , 0.00174936 * distance() + -0.0127749) * (0.105743 * thickness() + 0.163152)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.119608 * (0.24424 - exp(-4.62888 * (enclose() + 1.58466) - -0.00118559 * (enclose() + 65.3709) * (enclose() + 65.3709)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal5,metal6,metal7
//
CAPACITANCE CROSSOVER FRINGE metal6 metal5 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.133374 * (1 - exp(-1.74201 * (distance() + -0.0475699))) * pow(width() , 0.00167655 * distance() + -0.011806) * (0.0607042 * thickness() + 0.194901) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.133374 * (1 - exp(-1.74201 * (distance() + -0.0475699))) * pow(width() , 0.00167655 * distance() + -0.011806) * (0.0607042 * thickness() + 0.194901) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0290058 * (1 - exp(-1.80734 * (distance() + -0.0523711))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.13721 * pow(width() , -0.00376195) * (0.0989134 * thickness() + 0.172547) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00084526 * distance() + 0.170839) * (0.347264 - exp(-(0.360789 * distance() + 0.971807) * (enclose() + 0.323271 * distance() + 0.952392) - (0.00012211 * distance() + -0.00346871) * (enclose() + 9.04081 * distance() + -2.8075) * (enclose() + 9.04081 * distance() + -2.8075))) + 0.5 * 0.0590187 * exp(-2.73672 * enclose()) * (0.0717258 - 0.133897 * exp(-0.807365 * distance()) * pow(distance() , 0.298832))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00084526 * distance() + 0.170839) * (0.347264 - exp(-(0.360789 * distance() + 0.971807) * (enclose() + 0.323271 * distance() + 0.952392) - (0.00012211 * distance() + -0.00346871) * (enclose() + 9.04081 * distance() + -2.8075) * (enclose() + 9.04081 * distance() + -2.8075))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.028809 * (1 - exp(-2.4353 * (enclose() + 0.315612))) * pow(width() , 0.00011139 * enclose() + -0.0023972) + 0.5 * (0.00153795 * exp(-2.31261 * enclose()) + 0.00250427 * exp(-2.35269 * enclose())) * pow(width() , 0.0369189 * enclose() + -0.0300199)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.028809 * (1 - exp(-2.4353 * (enclose() + 0.315612))) * pow(width() , 0.00011139 * enclose() + -0.0023972) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal5,metal6,metal8
//
CAPACITANCE NEARBODY metal6 INSIDE OF metal5 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-0.890106 - 1.743 * distance()) + 0.0284621 / pow(distance() , 1.48475)) * 3.41472 * pow((width1() + width2()) / 2 , 0.0485586) * (0.0806509 * (thickness1() + thickness2()) / 2 + 0.0255469) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal5,metal6,metal8
//
CAPACITANCE CROSSOVER FRINGE metal6 metal8 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0785334 * (1 - exp(-1.02621 * (distance() + -0.00682772))) * pow(width() , 0.00507926 * distance() + -0.0164073) * (0.132735 * thickness() + 0.143871)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0148919 * (1 - exp(-1.11565 * (distance() + -0.0437709)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.102361 * pow(width() , 0.0215085) * (0.125954 * thickness() + 0.100621)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00044869 * distance() + 0.0518724) * (0.590546 - exp(-(-0.143714 * distance() + 0.302845) * (enclose() + 2.19639 * distance() + 1.11527) - (0.00155543 * distance() + 0.0916999) * (enclose() + 1.60375 * distance() + -0.022338) * (enclose() + 1.60375 * distance() + -0.022338)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0785334 * (1 - exp(-1.02621 * (distance() + -0.00682772))) * pow(width() , 0.00507926 * distance() + -0.0164073) * (0.132735 * thickness() + 0.143871)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0236927 * (0.590546 - exp(1.1343 * (enclose() + 23.0792) - 0.107254 * (enclose() + 16.0152) * (enclose() + 16.0152)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal5,metal6,metal8
//
CAPACITANCE CROSSOVER FRINGE metal6 metal5 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.159992 * (1 - exp(-1.15996 * (distance() + 0.00068722))) * pow(width() , 0.00318218 * distance() + -0.0147695) * (0.0331016 * thickness() + 0.221524) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.159992 * (1 - exp(-1.15996 * (distance() + 0.00068722))) * pow(width() , 0.00318218 * distance() + -0.0147695) * (0.0331016 * thickness() + 0.221524) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.037183 * (1 - exp(-1.21968 * (distance() + -0.0226311))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.156705 * pow(width() , 0.0102437) * (0.0666486 * thickness() + 0.213528) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00031068 * distance() + 0.0895503) * (0.790746 - exp(-(-0.0664632 * distance() + 0.743122) * (enclose() + -1.50948 * distance() + 0.438655) - (0.0274726 * distance() + -0.00327601) * (enclose() + -0.532433 * distance() + 8.12654) * (enclose() + -0.532433 * distance() + 8.12654))) + 0.5 * 0.0990454 * exp(-1.88797 * enclose()) * (0.0869963 - 0.117714 * exp(-0.422431 * distance()) * pow(distance() , 0.169604))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00031068 * distance() + 0.0895503) * (0.790746 - exp(-(-0.0664632 * distance() + 0.743122) * (enclose() + -1.50948 * distance() + 0.438655) - (0.0274726 * distance() + -0.00327601) * (enclose() + -0.532433 * distance() + 8.12654) * (enclose() + -0.532433 * distance() + 8.12654))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0373512 * (1 - exp(-1.81726 * (enclose() + 0.390699))) * pow(width() , -0.00030185 * enclose() + 0.0130112) + 0.5 * (0.00403696 * exp(-1.17689 * enclose()) + 0.00399123 * exp(-2.04706 * enclose())) * pow(width() , 0.0472388 * enclose() + -0.01006)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0373512 * (1 - exp(-1.81726 * (enclose() + 0.390699))) * pow(width() , -0.00030185 * enclose() + 0.0130112) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal5,metal7,metal8
//
CAPACITANCE NEARBODY metal7 INSIDE OF metal5 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(-0.872551 - 1.63379 * distance()) + 0.0282233 / pow(distance() , 1.47843)) * 3.39108 * pow((width1() + width2()) / 2 , 0.0520084) * (0.0783754 * (thickness1() + thickness2()) / 2 + 0.0286381) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.27)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal5,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal7 metal8 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.159578 * (1 - exp(-1.01305 * (distance() + -0.0061878))) * pow(width() , 0.00449893 * distance() + -0.0210674) * (0.074176 * thickness() + 0.181533)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0330718 * (1 - exp(-1.0869 * (distance() + -0.0367973)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.142449 * pow(width() , 0.0110673) * (0.107559 * thickness() + 0.192813)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00029132 * distance() + 0.0999125) * (0.632007 - exp(-(0.10866 * distance() + 0.644209) * (enclose() + -0.0197068 * distance() + 0.912994) - (0.023744 * distance() + -0.0138377) * (enclose() + -0.524755 * distance() + 4.60515) * (enclose() + -0.524755 * distance() + 4.60515)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.159578 * (1 - exp(-1.01305 * (distance() + -0.0061878))) * pow(width() , 0.00449893 * distance() + -0.0210674) * (0.074176 * thickness() + 0.181533)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0514129 * (0.632007 - exp(-1.73081 * (enclose() + 0.715926) - 0.223602 * (enclose() + -0.6424) * (enclose() + -0.6424)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal5,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal7 metal5 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0872008 * (1 - exp(-0.942784 * (distance() + -0.00191262))) * pow(width() , 0.00572303 * distance() + -0.020906) * (0.09216 * thickness() + 0.169072) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0872008 * (1 - exp(-0.942784 * (distance() + -0.00191262))) * pow(width() , 0.00572303 * distance() + -0.020906) * (0.09216 * thickness() + 0.169072) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0174122 * (1 - exp(-1.03455 * (distance() + -0.0430435))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.111771 * pow(width() , 0.0200226) * (0.0934722 * thickness() + 0.122552) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (-0.00063698 * distance() + 0.0620673) * (0.586594 - exp(-(-0.123206 * distance() + 0.252316) * (enclose() + 2.15551 * distance() + 1.32229) - (0.00221241 * distance() + 0.0783437) * (enclose() + 1.47829 * distance() + 0.527655) * (enclose() + 1.47829 * distance() + 0.527655))) + 0.5 * 0.06526 * exp(-1.32699 * enclose()) * (0.0902295 - 0.112736 * exp(-0.362398 * distance()) * pow(distance() , 0.173037))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (-0.00063698 * distance() + 0.0620673) * (0.586594 - exp(-(-0.123206 * distance() + 0.252316) * (enclose() + 2.15551 * distance() + 1.32229) - (0.00221241 * distance() + 0.0783437) * (enclose() + 1.47829 * distance() + 0.527655) * (enclose() + 1.47829 * distance() + 0.527655))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.0176321 * (1 - exp(-1.1966 * (enclose() + 0.499305))) * pow(width() , 0.00086832 * enclose() + 0.0156576) + 0.5 * (0.00279038 * exp(-1.20812 * enclose()) + 0.00279003 * exp(-1.2082 * enclose())) * pow(width() , 0.0201204 * enclose() + 0.0206889)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.0176321 * (1 - exp(-1.1966 * (enclose() + 0.499305))) * pow(width() , 0.00086832 * enclose() + 0.0156576) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.27)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: nearbody  profile: field  layers: field_base,metal6,metal7,metal8
//
CAPACITANCE NEARBODY metal7 INSIDE OF metal6 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(-0.880102 - 2.24431 * distance()) + 0.0132157 / pow(distance() , 1.67973)) * 3.58176 * pow((width1() + width2()) / 2 , 0.020149) * (0.100407 * (thickness1() + thickness2()) / 2 + 0.0276216) * (1 - m33RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.45)))
]
//
//  model: 33  target: crossovere  profile: field  layers: field_base,metal6,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal7 metal8 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.12871 * (1 - exp(-1.45374 * (distance() + -0.0451151))) * pow(width() , 0.00259989 * distance() + -0.0149682) * (0.112398 * thickness() + 0.161359)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0260681 * (1 - exp(-1.52899 * (distance() + -0.0563528)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.127149 * pow(width() , 0.00023142) * (0.148666 * thickness() + 0.148445)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00010668 * distance() + 0.0900073) * (0.564166 - exp(-(0.110199 * distance() + 0.65417) * (enclose() + 0.0189543 * distance() + 0.614234) - (0.0724958 * distance() + -0.00332257) * (enclose() + -0.347221 * distance() + 3.12324) * (enclose() + -0.347221 * distance() + 3.12324)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.12871 * (1 - exp(-1.45374 * (distance() + -0.0451151))) * pow(width() , 0.00259989 * distance() + -0.0149682) * (0.112398 * thickness() + 0.161359)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0455371 * (0.564166 - exp(-1.75616 * (enclose() + 0.803777) - 0.721635 * (enclose() + -0.34897) * (enclose() + -0.34897)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 33  target: intrinsice  profile: field  layers: field_base,metal6,metal7,metal8
//
CAPACITANCE CROSSOVER FRINGE metal7 metal6 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.138472 * (1 - exp(-1.53109 * (distance() + -0.0367009))) * pow(width() , 0.00198963 * distance() + -0.0122947) * (0.0491619 * thickness() + 0.208231) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.138472 * (1 - exp(-1.53109 * (distance() + -0.0367009))) * pow(width() , 0.00198963 * distance() + -0.0122947) * (0.0491619 * thickness() + 0.208231) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0313307 * (1 - exp(-1.59319 * (distance() + -0.0448484))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.14349 * pow(width() , -0.00107954) * (0.0821448 * thickness() + 0.186235) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance()) && (enclose() > 0)) {
        C = length() * (0.5 * (0.00040869 * distance() + 0.259221) * (0.23481 - exp(-(0.329049 * distance() + 0.510442) * (enclose() + -0.181634 * distance() + 1.98131) - (0.0721737 * distance() + 0.0644828) * (enclose() + -0.357533 * distance() + 2.13243) * (enclose() + -0.357533 * distance() + 2.13243))) + 0.5 * 0.0583229 * exp(-2.49706 * enclose()) * (0.0752791 - 0.129066 * exp(-0.694508 * distance()) * pow(distance() , 0.264883))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() > 0.0) && (enclose() <= 0)) {
        C = length() * 0.5 * (0.00040869 * distance() + 0.259221) * (0.23481 - exp(-(0.329049 * distance() + 0.510442) * (enclose() + -0.181634 * distance() + 1.98131) - (0.0721737 * distance() + 0.0644828) * (enclose() + -0.357533 * distance() + 2.13243) * (enclose() + -0.357533 * distance() + 2.13243))) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose) && (enclose() > 0)) {
        C = length() * (0.031193 * (1 - exp(-2.23469 * (enclose() + 0.337803))) * pow(width() , 2.29602e-05 * enclose() + 0.00089194) + 0.5 * (0.00315158 * exp(-2.24309 * enclose()) + 0.00103684 * exp(-1.69684 * enclose())) * pow(width() , 0.0397373 * enclose() + -0.0290412)) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if ((distance() <= 0.0) && (enclose() <= 0)) {
        C = length() * 0.031193 * (1 - exp(-2.23469 * (enclose() + 0.337803))) * pow(width() , 2.29602e-05 * enclose() + 0.00089194) * (1 - m33RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 0.45)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE NEARBODY poly metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0698213 * exp(-1.36209 * distance()) + 0.0102412 * exp(-0.250311 * distance())) * pow((width1() + width2()) / 2 , 0.243367) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0698213 * exp(-1.36209 * distance()) + 0.0102412 * exp(-0.250311 * distance())) * pow((width1() + width2()) / 2 , 0.243367)
    }
]
CAPACITANCE NEARBODY metal1 poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0698213 * exp(-1.36209 * distance()) + 0.0102412 * exp(-0.250311 * distance())) * pow((width1() + width2()) / 2 , 0.243367) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0698213 * exp(-1.36209 * distance()) + 0.0102412 * exp(-0.250311 * distance())) * pow((width1() + width2()) / 2 , 0.243367)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE NEARBODY poly metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0177133 * exp(-0.455152 * distance()) + 0.0177134 * exp(-0.45453 * distance())) * pow((width1() + width2()) / 2 , 0.29948) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0177133 * exp(-0.455152 * distance()) + 0.0177134 * exp(-0.45453 * distance())) * pow((width1() + width2()) / 2 , 0.29948)
    }
]
CAPACITANCE NEARBODY metal2 poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0177133 * exp(-0.455152 * distance()) + 0.0177134 * exp(-0.45453 * distance())) * pow((width1() + width2()) / 2 , 0.29948) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0177133 * exp(-0.455152 * distance()) + 0.0177134 * exp(-0.45453 * distance())) * pow((width1() + width2()) / 2 , 0.29948)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE NEARBODY poly metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0105644 * exp(-0.278204 * distance()) + 0.0105641 * exp(-0.277955 * distance())) * pow((width1() + width2()) / 2 , 0.309081) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0105644 * exp(-0.278204 * distance()) + 0.0105641 * exp(-0.277955 * distance())) * pow((width1() + width2()) / 2 , 0.309081)
    }
]
CAPACITANCE NEARBODY metal3 poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0105644 * exp(-0.278204 * distance()) + 0.0105641 * exp(-0.277955 * distance())) * pow((width1() + width2()) / 2 , 0.309081) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0105644 * exp(-0.278204 * distance()) + 0.0105641 * exp(-0.277955 * distance())) * pow((width1() + width2()) / 2 , 0.309081)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE NEARBODY poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00739588 * exp(-0.188066 * distance()) + 0.00739584 * exp(-0.188254 * distance())) * pow((width1() + width2()) / 2 , 0.309689) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00739588 * exp(-0.188066 * distance()) + 0.00739584 * exp(-0.188254 * distance())) * pow((width1() + width2()) / 2 , 0.309689)
    }
]
CAPACITANCE NEARBODY metal4 poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00739588 * exp(-0.188066 * distance()) + 0.00739584 * exp(-0.188254 * distance())) * pow((width1() + width2()) / 2 , 0.309689) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00739588 * exp(-0.188066 * distance()) + 0.00739584 * exp(-0.188254 * distance())) * pow((width1() + width2()) / 2 , 0.309689)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE NEARBODY poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00564805 * exp(-0.13301 * distance()) + 0.00564803 * exp(-0.13314 * distance())) * pow((width1() + width2()) / 2 , 0.308033) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00564805 * exp(-0.13301 * distance()) + 0.00564803 * exp(-0.13314 * distance())) * pow((width1() + width2()) / 2 , 0.308033)
    }
]
CAPACITANCE NEARBODY metal5 poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00564805 * exp(-0.13301 * distance()) + 0.00564803 * exp(-0.13314 * distance())) * pow((width1() + width2()) / 2 , 0.308033) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00564805 * exp(-0.13301 * distance()) + 0.00564803 * exp(-0.13314 * distance())) * pow((width1() + width2()) / 2 , 0.308033)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE NEARBODY poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00456425 * exp(-0.0965117 * distance()) + 0.00456425 * exp(-0.0970308 * distance())) * pow((width1() + width2()) / 2 , 0.305686) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00456425 * exp(-0.0965117 * distance()) + 0.00456425 * exp(-0.0970308 * distance())) * pow((width1() + width2()) / 2 , 0.305686)
    }
]
CAPACITANCE NEARBODY metal6 poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00456425 * exp(-0.0965117 * distance()) + 0.00456425 * exp(-0.0970308 * distance())) * pow((width1() + width2()) / 2 , 0.305686) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00456425 * exp(-0.0965117 * distance()) + 0.00456425 * exp(-0.0970308 * distance())) * pow((width1() + width2()) / 2 , 0.305686)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,poly,metal7
//
CAPACITANCE NEARBODY poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00385953 * exp(-0.0717727 * distance()) + 0.00385925 * exp(-0.0719836 * distance())) * pow((width1() + width2()) / 2 , 0.302061) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00385953 * exp(-0.0717727 * distance()) + 0.00385925 * exp(-0.0719836 * distance())) * pow((width1() + width2()) / 2 , 0.302061)
    }
]
CAPACITANCE NEARBODY metal7 poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00385953 * exp(-0.0717727 * distance()) + 0.00385925 * exp(-0.0719836 * distance())) * pow((width1() + width2()) / 2 , 0.302061) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00385953 * exp(-0.0717727 * distance()) + 0.00385925 * exp(-0.0719836 * distance())) * pow((width1() + width2()) / 2 , 0.302061)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,poly,metal8
//
CAPACITANCE NEARBODY poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00341841 * exp(-0.0511923 * distance()) + 0.00341854 * exp(-0.0498904 * distance())) * pow((width1() + width2()) / 2 , 0.275323) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00341841 * exp(-0.0511923 * distance()) + 0.00341854 * exp(-0.0498904 * distance())) * pow((width1() + width2()) / 2 , 0.275323)
    }
]
CAPACITANCE NEARBODY metal8 poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00341841 * exp(-0.0511923 * distance()) + 0.00341854 * exp(-0.0498904 * distance())) * pow((width1() + width2()) / 2 , 0.275323) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00341841 * exp(-0.0511923 * distance()) + 0.00341854 * exp(-0.0498904 * distance())) * pow((width1() + width2()) / 2 , 0.275323)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE NEARBODY metal1 metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0115713 * exp(-0.129985 * distance()) + 0.0595183 * exp(-0.892401 * distance())) * pow((width1() + width2()) / 2 , 0.235237) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0115713 * exp(-0.129985 * distance()) + 0.0595183 * exp(-0.892401 * distance())) * pow((width1() + width2()) / 2 , 0.235237)
    }
]
CAPACITANCE NEARBODY metal2 metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0115713 * exp(-0.129985 * distance()) + 0.0595183 * exp(-0.892401 * distance())) * pow((width1() + width2()) / 2 , 0.235237) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0115713 * exp(-0.129985 * distance()) + 0.0595183 * exp(-0.892401 * distance())) * pow((width1() + width2()) / 2 , 0.235237)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE NEARBODY metal1 metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0366553 * exp(-0.348049 * distance()) + 0.00111669) * pow((width1() + width2()) / 2 , 0.297769) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0366553 * exp(-0.348049 * distance()) + 0.00111669) * pow((width1() + width2()) / 2 , 0.297769)
    }
]
CAPACITANCE NEARBODY metal3 metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0366553 * exp(-0.348049 * distance()) + 0.00111669) * pow((width1() + width2()) / 2 , 0.297769) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0366553 * exp(-0.348049 * distance()) + 0.00111669) * pow((width1() + width2()) / 2 , 0.297769)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE NEARBODY metal1 metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0128906 * exp(-0.215844 * distance()) + 0.0128906 * exp(-0.215514 * distance())) * pow((width1() + width2()) / 2 , 0.314643) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0128906 * exp(-0.215844 * distance()) + 0.0128906 * exp(-0.215514 * distance())) * pow((width1() + width2()) / 2 , 0.314643)
    }
]
CAPACITANCE NEARBODY metal4 metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0128906 * exp(-0.215844 * distance()) + 0.0128906 * exp(-0.215514 * distance())) * pow((width1() + width2()) / 2 , 0.314643) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0128906 * exp(-0.215844 * distance()) + 0.0128906 * exp(-0.215514 * distance())) * pow((width1() + width2()) / 2 , 0.314643)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE NEARBODY metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00973661 * exp(-0.151506 * distance()) + 0.00973669 * exp(-0.151254 * distance())) * pow((width1() + width2()) / 2 , 0.319343) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00973661 * exp(-0.151506 * distance()) + 0.00973669 * exp(-0.151254 * distance())) * pow((width1() + width2()) / 2 , 0.319343)
    }
]
CAPACITANCE NEARBODY metal5 metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00973661 * exp(-0.151506 * distance()) + 0.00973669 * exp(-0.151254 * distance())) * pow((width1() + width2()) / 2 , 0.319343) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00973661 * exp(-0.151506 * distance()) + 0.00973669 * exp(-0.151254 * distance())) * pow((width1() + width2()) / 2 , 0.319343)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE NEARBODY metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00781263 * exp(-0.11007 * distance()) + 0.00781267 * exp(-0.110136 * distance())) * pow((width1() + width2()) / 2 , 0.319606) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00781263 * exp(-0.11007 * distance()) + 0.00781267 * exp(-0.110136 * distance())) * pow((width1() + width2()) / 2 , 0.319606)
    }
]
CAPACITANCE NEARBODY metal6 metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00781263 * exp(-0.11007 * distance()) + 0.00781267 * exp(-0.110136 * distance())) * pow((width1() + width2()) / 2 , 0.319606) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00781263 * exp(-0.11007 * distance()) + 0.00781267 * exp(-0.110136 * distance())) * pow((width1() + width2()) / 2 , 0.319606)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal1,metal7
//
CAPACITANCE NEARBODY metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00656415 * exp(-0.082088 * distance()) + 0.00656416 * exp(-0.0821401 * distance())) * pow((width1() + width2()) / 2 , 0.315748) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00656415 * exp(-0.082088 * distance()) + 0.00656416 * exp(-0.0821401 * distance())) * pow((width1() + width2()) / 2 , 0.315748)
    }
]
CAPACITANCE NEARBODY metal7 metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00656415 * exp(-0.082088 * distance()) + 0.00656416 * exp(-0.0821401 * distance())) * pow((width1() + width2()) / 2 , 0.315748) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00656415 * exp(-0.082088 * distance()) + 0.00656416 * exp(-0.0821401 * distance())) * pow((width1() + width2()) / 2 , 0.315748)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal1,metal8
//
CAPACITANCE NEARBODY metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00575458 * exp(-0.0585769 * distance()) + 0.00575456 * exp(-0.058274 * distance())) * pow((width1() + width2()) / 2 , 0.284627) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00575458 * exp(-0.0585769 * distance()) + 0.00575456 * exp(-0.058274 * distance())) * pow((width1() + width2()) / 2 , 0.284627)
    }
]
CAPACITANCE NEARBODY metal8 metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00575458 * exp(-0.0585769 * distance()) + 0.00575456 * exp(-0.058274 * distance())) * pow((width1() + width2()) / 2 , 0.284627) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00575458 * exp(-0.0585769 * distance()) + 0.00575456 * exp(-0.058274 * distance())) * pow((width1() + width2()) / 2 , 0.284627)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE NEARBODY metal2 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0232176 * exp(-0.162742 * distance()) + 0.0543463 * exp(-0.946673 * distance())) * pow((width1() + width2()) / 2 , 0.22253) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0232176 * exp(-0.162742 * distance()) + 0.0543463 * exp(-0.946673 * distance())) * pow((width1() + width2()) / 2 , 0.22253)
    }
]
CAPACITANCE NEARBODY metal3 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0232176 * exp(-0.162742 * distance()) + 0.0543463 * exp(-0.946673 * distance())) * pow((width1() + width2()) / 2 , 0.22253) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0232176 * exp(-0.162742 * distance()) + 0.0543463 * exp(-0.946673 * distance())) * pow((width1() + width2()) / 2 , 0.22253)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE NEARBODY metal2 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0397306 * exp(-0.325944 * distance()) + 0.003772) * pow((width1() + width2()) / 2 , 0.282381) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0397306 * exp(-0.325944 * distance()) + 0.003772) * pow((width1() + width2()) / 2 , 0.282381)
    }
]
CAPACITANCE NEARBODY metal4 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0397306 * exp(-0.325944 * distance()) + 0.003772) * pow((width1() + width2()) / 2 , 0.282381) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0397306 * exp(-0.325944 * distance()) + 0.003772) * pow((width1() + width2()) / 2 , 0.282381)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE NEARBODY metal2 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.015427 * exp(-0.183532 * distance()) + 0.0154271 * exp(-0.18356 * distance())) * pow((width1() + width2()) / 2 , 0.297544) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.015427 * exp(-0.183532 * distance()) + 0.0154271 * exp(-0.18356 * distance())) * pow((width1() + width2()) / 2 , 0.297544)
    }
]
CAPACITANCE NEARBODY metal5 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.015427 * exp(-0.183532 * distance()) + 0.0154271 * exp(-0.18356 * distance())) * pow((width1() + width2()) / 2 , 0.297544) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.015427 * exp(-0.183532 * distance()) + 0.0154271 * exp(-0.18356 * distance())) * pow((width1() + width2()) / 2 , 0.297544)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE NEARBODY metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0120248 * exp(-0.129934 * distance()) + 0.0120248 * exp(-0.130339 * distance())) * pow((width1() + width2()) / 2 , 0.301564) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0120248 * exp(-0.129934 * distance()) + 0.0120248 * exp(-0.130339 * distance())) * pow((width1() + width2()) / 2 , 0.301564)
    }
]
CAPACITANCE NEARBODY metal6 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0120248 * exp(-0.129934 * distance()) + 0.0120248 * exp(-0.130339 * distance())) * pow((width1() + width2()) / 2 , 0.301564) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0120248 * exp(-0.129934 * distance()) + 0.0120248 * exp(-0.130339 * distance())) * pow((width1() + width2()) / 2 , 0.301564)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal2,metal7
//
CAPACITANCE NEARBODY metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00992793 * exp(-0.0956552 * distance()) + 0.00992782 * exp(-0.0956217 * distance())) * pow((width1() + width2()) / 2 , 0.298734) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00992793 * exp(-0.0956552 * distance()) + 0.00992782 * exp(-0.0956217 * distance())) * pow((width1() + width2()) / 2 , 0.298734)
    }
]
CAPACITANCE NEARBODY metal7 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00992793 * exp(-0.0956552 * distance()) + 0.00992782 * exp(-0.0956217 * distance())) * pow((width1() + width2()) / 2 , 0.298734) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00992793 * exp(-0.0956552 * distance()) + 0.00992782 * exp(-0.0956217 * distance())) * pow((width1() + width2()) / 2 , 0.298734)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal2,metal8
//
CAPACITANCE NEARBODY metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00859635 * exp(-0.068371 * distance()) + 0.00859634 * exp(-0.0668794 * distance())) * pow((width1() + width2()) / 2 , 0.266551) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00859635 * exp(-0.068371 * distance()) + 0.00859634 * exp(-0.0668794 * distance())) * pow((width1() + width2()) / 2 , 0.266551)
    }
]
CAPACITANCE NEARBODY metal8 metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00859635 * exp(-0.068371 * distance()) + 0.00859634 * exp(-0.0668794 * distance())) * pow((width1() + width2()) / 2 , 0.266551) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00859635 * exp(-0.068371 * distance()) + 0.00859634 * exp(-0.0668794 * distance())) * pow((width1() + width2()) / 2 , 0.266551)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE NEARBODY metal3 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0282406 * exp(-0.14498 * distance()) + 0.0527716 * exp(-0.958045 * distance())) * pow((width1() + width2()) / 2 , 0.220843) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0282406 * exp(-0.14498 * distance()) + 0.0527716 * exp(-0.958045 * distance())) * pow((width1() + width2()) / 2 , 0.220843)
    }
]
CAPACITANCE NEARBODY metal4 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0282406 * exp(-0.14498 * distance()) + 0.0527716 * exp(-0.958045 * distance())) * pow((width1() + width2()) / 2 , 0.220843) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0282406 * exp(-0.14498 * distance()) + 0.0527716 * exp(-0.958045 * distance())) * pow((width1() + width2()) / 2 , 0.220843)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE NEARBODY metal3 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00648824 + 0.0401389 * exp(-0.315945 * distance())) * pow((width1() + width2()) / 2 , 0.270406) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00648824 + 0.0401389 * exp(-0.315945 * distance())) * pow((width1() + width2()) / 2 , 0.270406)
    }
]
CAPACITANCE NEARBODY metal5 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00648824 + 0.0401389 * exp(-0.315945 * distance())) * pow((width1() + width2()) / 2 , 0.270406) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00648824 + 0.0401389 * exp(-0.315945 * distance())) * pow((width1() + width2()) / 2 , 0.270406)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE NEARBODY metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0168784 * exp(-0.16057 * distance()) + 0.0168783 * exp(-0.161626 * distance())) * pow((width1() + width2()) / 2 , 0.279945) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0168784 * exp(-0.16057 * distance()) + 0.0168783 * exp(-0.161626 * distance())) * pow((width1() + width2()) / 2 , 0.279945)
    }
]
CAPACITANCE NEARBODY metal6 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0168784 * exp(-0.16057 * distance()) + 0.0168783 * exp(-0.161626 * distance())) * pow((width1() + width2()) / 2 , 0.279945) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0168784 * exp(-0.16057 * distance()) + 0.0168783 * exp(-0.161626 * distance())) * pow((width1() + width2()) / 2 , 0.279945)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal3,metal7
//
CAPACITANCE NEARBODY metal3 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.013467 * exp(-0.114293 * distance()) + 0.013467 * exp(-0.114293 * distance())) * pow((width1() + width2()) / 2 , 0.278136) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.013467 * exp(-0.114293 * distance()) + 0.013467 * exp(-0.114293 * distance())) * pow((width1() + width2()) / 2 , 0.278136)
    }
]
CAPACITANCE NEARBODY metal7 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.013467 * exp(-0.114293 * distance()) + 0.013467 * exp(-0.114293 * distance())) * pow((width1() + width2()) / 2 , 0.278136) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.013467 * exp(-0.114293 * distance()) + 0.013467 * exp(-0.114293 * distance())) * pow((width1() + width2()) / 2 , 0.278136)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal3,metal8
//
CAPACITANCE NEARBODY metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114022 * exp(-0.0784434 * distance()) + 0.0114022 * exp(-0.0789513 * distance())) * pow((width1() + width2()) / 2 , 0.24542) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114022 * exp(-0.0784434 * distance()) + 0.0114022 * exp(-0.0789513 * distance())) * pow((width1() + width2()) / 2 , 0.24542)
    }
]
CAPACITANCE NEARBODY metal8 metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114022 * exp(-0.0784434 * distance()) + 0.0114022 * exp(-0.0789513 * distance())) * pow((width1() + width2()) / 2 , 0.24542) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114022 * exp(-0.0784434 * distance()) + 0.0114022 * exp(-0.0789513 * distance())) * pow((width1() + width2()) / 2 , 0.24542)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE NEARBODY metal4 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0521982 * exp(-0.964059 * distance()) + 0.0312279 * exp(-0.128019 * distance())) * pow((width1() + width2()) / 2 , 0.219998) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0521982 * exp(-0.964059 * distance()) + 0.0312279 * exp(-0.128019 * distance())) * pow((width1() + width2()) / 2 , 0.219998)
    }
]
CAPACITANCE NEARBODY metal5 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0521982 * exp(-0.964059 * distance()) + 0.0312279 * exp(-0.128019 * distance())) * pow((width1() + width2()) / 2 , 0.219998) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0521982 * exp(-0.964059 * distance()) + 0.0312279 * exp(-0.128019 * distance())) * pow((width1() + width2()) / 2 , 0.219998)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE NEARBODY metal4 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0242464 * exp(-0.220937 * distance()) + 0.0242464 * exp(-0.220945 * distance())) * pow((width1() + width2()) / 2 , 0.26258) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0242464 * exp(-0.220937 * distance()) + 0.0242464 * exp(-0.220945 * distance())) * pow((width1() + width2()) / 2 , 0.26258)
    }
]
CAPACITANCE NEARBODY metal6 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0242464 * exp(-0.220937 * distance()) + 0.0242464 * exp(-0.220945 * distance())) * pow((width1() + width2()) / 2 , 0.26258) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0242464 * exp(-0.220937 * distance()) + 0.0242464 * exp(-0.220945 * distance())) * pow((width1() + width2()) / 2 , 0.26258)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal4,metal7
//
CAPACITANCE NEARBODY metal4 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0180837 * exp(-0.142649 * distance()) + 0.0180837 * exp(-0.14915 * distance())) * pow((width1() + width2()) / 2 , 0.26682) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0180837 * exp(-0.142649 * distance()) + 0.0180837 * exp(-0.14915 * distance())) * pow((width1() + width2()) / 2 , 0.26682)
    }
]
CAPACITANCE NEARBODY metal7 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0180837 * exp(-0.142649 * distance()) + 0.0180837 * exp(-0.14915 * distance())) * pow((width1() + width2()) / 2 , 0.26682) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0180837 * exp(-0.142649 * distance()) + 0.0180837 * exp(-0.14915 * distance())) * pow((width1() + width2()) / 2 , 0.26682)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal4,metal8
//
CAPACITANCE NEARBODY metal4 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0147252 * exp(-0.0963946 * distance()) + 0.0147251 * exp(-0.0958196 * distance())) * pow((width1() + width2()) / 2 , 0.234551) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0147252 * exp(-0.0963946 * distance()) + 0.0147251 * exp(-0.0958196 * distance())) * pow((width1() + width2()) / 2 , 0.234551)
    }
]
CAPACITANCE NEARBODY metal8 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0147252 * exp(-0.0963946 * distance()) + 0.0147251 * exp(-0.0958196 * distance())) * pow((width1() + width2()) / 2 , 0.234551) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0147252 * exp(-0.0963946 * distance()) + 0.0147251 * exp(-0.0958196 * distance())) * pow((width1() + width2()) / 2 , 0.234551)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE NEARBODY metal5 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0334856 * exp(-0.11607 * distance()) + 0.0518155 * exp(-0.968763 * distance())) * pow((width1() + width2()) / 2 , 0.219309) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0334856 * exp(-0.11607 * distance()) + 0.0518155 * exp(-0.968763 * distance())) * pow((width1() + width2()) / 2 , 0.219309)
    }
]
CAPACITANCE NEARBODY metal6 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0334856 * exp(-0.11607 * distance()) + 0.0518155 * exp(-0.968763 * distance())) * pow((width1() + width2()) / 2 , 0.219309) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0334856 * exp(-0.11607 * distance()) + 0.0518155 * exp(-0.968763 * distance())) * pow((width1() + width2()) / 2 , 0.219309)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal5,metal7
//
CAPACITANCE NEARBODY metal5 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0252813 * exp(-0.204914 * distance()) + 0.0252813 * exp(-0.204895 * distance())) * pow((width1() + width2()) / 2 , 0.255332) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0252813 * exp(-0.204914 * distance()) + 0.0252813 * exp(-0.204895 * distance())) * pow((width1() + width2()) / 2 , 0.255332)
    }
]
CAPACITANCE NEARBODY metal7 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0252813 * exp(-0.204914 * distance()) + 0.0252813 * exp(-0.204895 * distance())) * pow((width1() + width2()) / 2 , 0.255332) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0252813 * exp(-0.204914 * distance()) + 0.0252813 * exp(-0.204895 * distance())) * pow((width1() + width2()) / 2 , 0.255332)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal5,metal8
//
CAPACITANCE NEARBODY metal5 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0191059 * exp(-0.124308 * distance()) + 0.0191057 * exp(-0.124063 * distance())) * pow((width1() + width2()) / 2 , 0.228709) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0191059 * exp(-0.124308 * distance()) + 0.0191057 * exp(-0.124063 * distance())) * pow((width1() + width2()) / 2 , 0.228709)
    }
]
CAPACITANCE NEARBODY metal8 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0191059 * exp(-0.124308 * distance()) + 0.0191057 * exp(-0.124063 * distance())) * pow((width1() + width2()) / 2 , 0.228709) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0191059 * exp(-0.124308 * distance()) + 0.0191057 * exp(-0.124063 * distance())) * pow((width1() + width2()) / 2 , 0.228709)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal6,metal7
//
CAPACITANCE NEARBODY metal6 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0513372 * exp(-0.966507 * distance()) + 0.0360381 * exp(-0.110555 * distance())) * pow((width1() + width2()) / 2 , 0.218176) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0513372 * exp(-0.966507 * distance()) + 0.0360381 * exp(-0.110555 * distance())) * pow((width1() + width2()) / 2 , 0.218176)
    }
]
CAPACITANCE NEARBODY metal7 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0513372 * exp(-0.966507 * distance()) + 0.0360381 * exp(-0.110555 * distance())) * pow((width1() + width2()) / 2 , 0.218176) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0513372 * exp(-0.966507 * distance()) + 0.0360381 * exp(-0.110555 * distance())) * pow((width1() + width2()) / 2 , 0.218176)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal6,metal8
//
CAPACITANCE NEARBODY metal6 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0258763 * exp(-0.17352 * distance()) + 0.0258763 * exp(-0.17352 * distance())) * pow((width1() + width2()) / 2 , 0.22464) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0258763 * exp(-0.17352 * distance()) + 0.0258763 * exp(-0.17352 * distance())) * pow((width1() + width2()) / 2 , 0.22464)
    }
]
CAPACITANCE NEARBODY metal8 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0258763 * exp(-0.17352 * distance()) + 0.0258763 * exp(-0.17352 * distance())) * pow((width1() + width2()) / 2 , 0.22464) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0258763 * exp(-0.17352 * distance()) + 0.0258763 * exp(-0.17352 * distance())) * pow((width1() + width2()) / 2 , 0.22464)
    }
]
//
//  model: 614  target: nearbody2  profile: field  layers: field_base,metal7,metal8
//
CAPACITANCE NEARBODY metal7 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0610318 * exp(-0.53999 * distance()) + 0.0220083 * exp(-0.0123183 * distance())) * pow((width1() + width2()) / 2 , 0.207975) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0610318 * exp(-0.53999 * distance()) + 0.0220083 * exp(-0.0123183 * distance())) * pow((width1() + width2()) / 2 , 0.207975)
    }
]
CAPACITANCE NEARBODY metal8 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0610318 * exp(-0.53999 * distance()) + 0.0220083 * exp(-0.0123183 * distance())) * pow((width1() + width2()) / 2 , 0.207975) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0610318 * exp(-0.53999 * distance()) + 0.0220083 * exp(-0.0123183 * distance())) * pow((width1() + width2()) / 2 , 0.207975)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,poly,metal1
//
CAPACITANCE NEARBODY poly metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00323395 * exp(-0.542613 * distance())) / pow(distance() , 0.641816) * pow((width1() + width2()) / 2 , -0.0266363) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00323395 * exp(-0.542613 * distance())) / pow(distance() , 0.641816) * pow((width1() + width2()) / 2 , -0.0266363)
    }
]
CAPACITANCE NEARBODY metal1 poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00323395 * exp(-0.542613 * distance())) / pow(distance() , 0.641816) * pow((width1() + width2()) / 2 , -0.0266363) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00323395 * exp(-0.542613 * distance())) / pow(distance() , 0.641816) * pow((width1() + width2()) / 2 , -0.0266363)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,poly,metal2
//
CAPACITANCE NEARBODY poly metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0088287 * exp(-0.608236 * distance())) / pow(distance() , 0.0331929) * pow((width1() + width2()) / 2 , 0.16761) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0088287 * exp(-0.608236 * distance())) / pow(distance() , 0.0331929) * pow((width1() + width2()) / 2 , 0.16761)
    }
]
CAPACITANCE NEARBODY metal2 poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0088287 * exp(-0.608236 * distance())) / pow(distance() , 0.0331929) * pow((width1() + width2()) / 2 , 0.16761) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0088287 * exp(-0.608236 * distance())) / pow(distance() , 0.0331929) * pow((width1() + width2()) / 2 , 0.16761)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,poly,metal3
//
CAPACITANCE NEARBODY poly metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00720084 * exp(-0.35593 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.264713) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00720084 * exp(-0.35593 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.264713)
    }
]
CAPACITANCE NEARBODY metal3 poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00720084 * exp(-0.35593 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.264713) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00720084 * exp(-0.35593 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.264713)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,poly,metal4
//
CAPACITANCE NEARBODY poly metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00557035 * exp(-0.222183 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.303553) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00557035 * exp(-0.222183 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.303553)
    }
]
CAPACITANCE NEARBODY metal4 poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00557035 * exp(-0.222183 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.303553) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00557035 * exp(-0.222183 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.303553)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,poly,metal5
//
CAPACITANCE NEARBODY poly metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00442653 * exp(-0.143628 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.318827) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00442653 * exp(-0.143628 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.318827)
    }
]
CAPACITANCE NEARBODY metal5 poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00442653 * exp(-0.143628 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.318827) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00442653 * exp(-0.143628 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.318827)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,poly,metal6
//
CAPACITANCE NEARBODY poly metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00362803 * exp(-0.0939824 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.323636) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00362803 * exp(-0.0939824 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.323636)
    }
]
CAPACITANCE NEARBODY metal6 poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00362803 * exp(-0.0939824 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.323636) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00362803 * exp(-0.0939824 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.323636)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,poly,metal7
//
CAPACITANCE NEARBODY poly metal7 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00307659 * exp(-0.062975 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.324577) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00307659 * exp(-0.062975 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.324577)
    }
]
CAPACITANCE NEARBODY metal7 poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00307659 * exp(-0.062975 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.324577) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00307659 * exp(-0.062975 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.324577)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,poly,metal8
//
CAPACITANCE NEARBODY poly metal8 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00263402 * exp(-0.0507933 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.301502) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00263402 * exp(-0.0507933 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.301502)
    }
]
CAPACITANCE NEARBODY metal8 poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00263402 * exp(-0.0507933 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.301502) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00263402 * exp(-0.0507933 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.301502)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal1,metal2
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00317736 * exp(-0.264968 * distance())) / pow(distance() , 0.544878) * pow((width1() + width2()) / 2 , -0.0801135) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00317736 * exp(-0.264968 * distance())) / pow(distance() , 0.544878) * pow((width1() + width2()) / 2 , -0.0801135)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00317736 * exp(-0.264968 * distance())) / pow(distance() , 0.544878) * pow((width1() + width2()) / 2 , -0.0801135) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00317736 * exp(-0.264968 * distance())) / pow(distance() , 0.544878) * pow((width1() + width2()) / 2 , -0.0801135)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal1,metal3
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00759135 * exp(-0.436937 * distance())) / pow(distance() , 0.0672954) * pow((width1() + width2()) / 2 , 0.114583) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00759135 * exp(-0.436937 * distance())) / pow(distance() , 0.0672954) * pow((width1() + width2()) / 2 , 0.114583)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00759135 * exp(-0.436937 * distance())) / pow(distance() , 0.0672954) * pow((width1() + width2()) / 2 , 0.114583) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00759135 * exp(-0.436937 * distance())) / pow(distance() , 0.0672954) * pow((width1() + width2()) / 2 , 0.114583)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal1,metal4
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00752995 * exp(-0.33334 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.221923) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00752995 * exp(-0.33334 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.221923)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00752995 * exp(-0.33334 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.221923) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00752995 * exp(-0.33334 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.221923)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal1,metal5
//
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00617577 * exp(-0.208401 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.276335) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00617577 * exp(-0.208401 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.276335)
    }
]
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00617577 * exp(-0.208401 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.276335) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00617577 * exp(-0.208401 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.276335)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal1,metal6
//
CAPACITANCE NEARBODY metal1 metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00525052 * exp(-0.144846 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.305042) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00525052 * exp(-0.144846 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.305042)
    }
]
CAPACITANCE NEARBODY metal6 metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00525052 * exp(-0.144846 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.305042) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00525052 * exp(-0.144846 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.305042)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal1,metal7
//
CAPACITANCE NEARBODY metal1 metal7 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00451701 * exp(-0.103965 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.316998) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00451701 * exp(-0.103965 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.316998)
    }
]
CAPACITANCE NEARBODY metal7 metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00451701 * exp(-0.103965 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.316998) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00451701 * exp(-0.103965 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.316998)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal1,metal8
//
CAPACITANCE NEARBODY metal1 metal8 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0038394 * exp(-0.0801929 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.296237) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0038394 * exp(-0.0801929 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.296237)
    }
]
CAPACITANCE NEARBODY metal8 metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0038394 * exp(-0.0801929 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.296237) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0038394 * exp(-0.0801929 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.296237)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal2,metal3
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00347119 * exp(-0.104228 * distance())) / pow(distance() , 0.550375) * pow((width1() + width2()) / 2 , -0.0984801) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00347119 * exp(-0.104228 * distance())) / pow(distance() , 0.550375) * pow((width1() + width2()) / 2 , -0.0984801)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00347119 * exp(-0.104228 * distance())) / pow(distance() , 0.550375) * pow((width1() + width2()) / 2 , -0.0984801) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00347119 * exp(-0.104228 * distance())) / pow(distance() , 0.550375) * pow((width1() + width2()) / 2 , -0.0984801)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal2,metal4
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00764348 * exp(-0.347897 * distance())) / pow(distance() , 0.100882) * pow((width1() + width2()) / 2 , 0.0524344) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00764348 * exp(-0.347897 * distance())) / pow(distance() , 0.100882) * pow((width1() + width2()) / 2 , 0.0524344)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00764348 * exp(-0.347897 * distance())) / pow(distance() , 0.100882) * pow((width1() + width2()) / 2 , 0.0524344) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00764348 * exp(-0.347897 * distance())) / pow(distance() , 0.100882) * pow((width1() + width2()) / 2 , 0.0524344)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal2,metal5
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00789849 * exp(-0.289005 * distance())) / pow(distance() , 0.00230579) * pow((width1() + width2()) / 2 , 0.153528) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00789849 * exp(-0.289005 * distance())) / pow(distance() , 0.00230579) * pow((width1() + width2()) / 2 , 0.153528)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00789849 * exp(-0.289005 * distance())) / pow(distance() , 0.00230579) * pow((width1() + width2()) / 2 , 0.153528) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00789849 * exp(-0.289005 * distance())) / pow(distance() , 0.00230579) * pow((width1() + width2()) / 2 , 0.153528)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal2,metal6
//
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00692517 * exp(-0.202222 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.21101) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00692517 * exp(-0.202222 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.21101)
    }
]
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00692517 * exp(-0.202222 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.21101) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00692517 * exp(-0.202222 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.21101)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal2,metal7
//
CAPACITANCE NEARBODY metal2 metal7 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0060188 * exp(-0.146346 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.238676) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0060188 * exp(-0.146346 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.238676)
    }
]
CAPACITANCE NEARBODY metal7 metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0060188 * exp(-0.146346 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.238676) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0060188 * exp(-0.146346 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.238676)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal2,metal8
//
CAPACITANCE NEARBODY metal2 metal8 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00508901 * exp(-0.110603 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.225913) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00508901 * exp(-0.110603 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.225913)
    }
]
CAPACITANCE NEARBODY metal8 metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00508901 * exp(-0.110603 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.225913) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00508901 * exp(-0.110603 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.225913)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal3,metal4
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00385253 * exp(-0.0269404 * distance())) / pow(distance() , 0.507127) * pow((width1() + width2()) / 2 , -0.107945) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00385253 * exp(-0.0269404 * distance())) / pow(distance() , 0.507127) * pow((width1() + width2()) / 2 , -0.107945)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00385253 * exp(-0.0269404 * distance())) / pow(distance() , 0.507127) * pow((width1() + width2()) / 2 , -0.107945) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00385253 * exp(-0.0269404 * distance())) / pow(distance() , 0.507127) * pow((width1() + width2()) / 2 , -0.107945)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal3,metal5
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00781162 * exp(-0.280107 * distance())) / pow(distance() , 0.109626) * pow((width1() + width2()) / 2 , 0.0282236) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00781162 * exp(-0.280107 * distance())) / pow(distance() , 0.109626) * pow((width1() + width2()) / 2 , 0.0282236)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00781162 * exp(-0.280107 * distance())) / pow(distance() , 0.109626) * pow((width1() + width2()) / 2 , 0.0282236) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00781162 * exp(-0.280107 * distance())) / pow(distance() , 0.109626) * pow((width1() + width2()) / 2 , 0.0282236)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal3,metal6
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00819756 * exp(-0.25304 * distance())) / pow(distance() , 0.0098271) * pow((width1() + width2()) / 2 , 0.118642) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00819756 * exp(-0.25304 * distance())) / pow(distance() , 0.0098271) * pow((width1() + width2()) / 2 , 0.118642)
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00819756 * exp(-0.25304 * distance())) / pow(distance() , 0.0098271) * pow((width1() + width2()) / 2 , 0.118642) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00819756 * exp(-0.25304 * distance())) / pow(distance() , 0.0098271) * pow((width1() + width2()) / 2 , 0.118642)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal3,metal7
//
CAPACITANCE NEARBODY metal3 metal7 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00749852 * exp(-0.203066 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.165562) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00749852 * exp(-0.203066 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.165562)
    }
]
CAPACITANCE NEARBODY metal7 metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00749852 * exp(-0.203066 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.165562) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00749852 * exp(-0.203066 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.165562)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal3,metal8
//
CAPACITANCE NEARBODY metal3 metal8 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00613208 * exp(-0.138784 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.158707) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00613208 * exp(-0.138784 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.158707)
    }
]
CAPACITANCE NEARBODY metal8 metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00613208 * exp(-0.138784 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.158707) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00613208 * exp(-0.138784 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.158707)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal4,metal5
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00426016 * exp(0.0103889 * distance())) / pow(distance() , 0.468416) * pow((width1() + width2()) / 2 , -0.105703) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00426016 * exp(0.0103889 * distance())) / pow(distance() , 0.468416) * pow((width1() + width2()) / 2 , -0.105703)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00426016 * exp(0.0103889 * distance())) / pow(distance() , 0.468416) * pow((width1() + width2()) / 2 , -0.105703) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00426016 * exp(0.0103889 * distance())) / pow(distance() , 0.468416) * pow((width1() + width2()) / 2 , -0.105703)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal4,metal6
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00804688 * exp(-0.236211 * distance())) / pow(distance() , 0.1126) * pow((width1() + width2()) / 2 , 0.0160293) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00804688 * exp(-0.236211 * distance())) / pow(distance() , 0.1126) * pow((width1() + width2()) / 2 , 0.0160293)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00804688 * exp(-0.236211 * distance())) / pow(distance() , 0.1126) * pow((width1() + width2()) / 2 , 0.0160293) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00804688 * exp(-0.236211 * distance())) / pow(distance() , 0.1126) * pow((width1() + width2()) / 2 , 0.0160293)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal4,metal7
//
CAPACITANCE NEARBODY metal4 metal7 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00844703 * exp(-0.22792 * distance())) / pow(distance() , 0.0169089) * pow((width1() + width2()) / 2 , 0.0968587) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00844703 * exp(-0.22792 * distance())) / pow(distance() , 0.0169089) * pow((width1() + width2()) / 2 , 0.0968587)
    }
]
CAPACITANCE NEARBODY metal7 metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00844703 * exp(-0.22792 * distance())) / pow(distance() , 0.0169089) * pow((width1() + width2()) / 2 , 0.0968587) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00844703 * exp(-0.22792 * distance())) / pow(distance() , 0.0169089) * pow((width1() + width2()) / 2 , 0.0968587)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal4,metal8
//
CAPACITANCE NEARBODY metal4 metal8 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00711602 * exp(-0.167827 * distance())) / pow(distance() , 0.00921784) * pow((width1() + width2()) / 2 , 0.0990656) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00711602 * exp(-0.167827 * distance())) / pow(distance() , 0.00921784) * pow((width1() + width2()) / 2 , 0.0990656)
    }
]
CAPACITANCE NEARBODY metal8 metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00711602 * exp(-0.167827 * distance())) / pow(distance() , 0.00921784) * pow((width1() + width2()) / 2 , 0.0990656) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00711602 * exp(-0.167827 * distance())) / pow(distance() , 0.00921784) * pow((width1() + width2()) / 2 , 0.0990656)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal5,metal6
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00464636 * exp(0.027094 * distance())) / pow(distance() , 0.436755) * pow((width1() + width2()) / 2 , -0.101454) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00464636 * exp(0.027094 * distance())) / pow(distance() , 0.436755) * pow((width1() + width2()) / 2 , -0.101454)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00464636 * exp(0.027094 * distance())) / pow(distance() , 0.436755) * pow((width1() + width2()) / 2 , -0.101454) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00464636 * exp(0.027094 * distance())) / pow(distance() , 0.436755) * pow((width1() + width2()) / 2 , -0.101454)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal5,metal7
//
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00823318 * exp(-0.209637 * distance())) / pow(distance() , 0.114876) * pow((width1() + width2()) / 2 , 0.00185469) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00823318 * exp(-0.209637 * distance())) / pow(distance() , 0.114876) * pow((width1() + width2()) / 2 , 0.00185469)
    }
]
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00823318 * exp(-0.209637 * distance())) / pow(distance() , 0.114876) * pow((width1() + width2()) / 2 , 0.00185469) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00823318 * exp(-0.209637 * distance())) / pow(distance() , 0.114876) * pow((width1() + width2()) / 2 , 0.00185469)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal5,metal8
//
CAPACITANCE NEARBODY metal5 metal8 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00775766 * exp(-0.180714 * distance())) / pow(distance() , 0.058378) * pow((width1() + width2()) / 2 , 0.0355237) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00775766 * exp(-0.180714 * distance())) / pow(distance() , 0.058378) * pow((width1() + width2()) / 2 , 0.0355237)
    }
]
CAPACITANCE NEARBODY metal8 metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00775766 * exp(-0.180714 * distance())) / pow(distance() , 0.058378) * pow((width1() + width2()) / 2 , 0.0355237) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00775766 * exp(-0.180714 * distance())) / pow(distance() , 0.058378) * pow((width1() + width2()) / 2 , 0.0355237)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal6,metal7
//
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0049491 * exp(0.0320615 * distance())) / pow(distance() , 0.414875) * pow((width1() + width2()) / 2 , -0.107199) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0049491 * exp(0.0320615 * distance())) / pow(distance() , 0.414875) * pow((width1() + width2()) / 2 , -0.107199)
    }
]
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0049491 * exp(0.0320615 * distance())) / pow(distance() , 0.414875) * pow((width1() + width2()) / 2 , -0.107199) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0049491 * exp(0.0320615 * distance())) / pow(distance() , 0.414875) * pow((width1() + width2()) / 2 , -0.107199)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal6,metal8
//
CAPACITANCE NEARBODY metal6 metal8 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00754679 * exp(-0.153908 * distance())) / pow(distance() , 0.174525) * pow((width1() + width2()) / 2 , -0.0436603) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00754679 * exp(-0.153908 * distance())) / pow(distance() , 0.174525) * pow((width1() + width2()) / 2 , -0.0436603)
    }
]
CAPACITANCE NEARBODY metal8 metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00754679 * exp(-0.153908 * distance())) / pow(distance() , 0.174525) * pow((width1() + width2()) / 2 , -0.0436603) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00754679 * exp(-0.153908 * distance())) / pow(distance() , 0.174525) * pow((width1() + width2()) / 2 , -0.0436603)
    }
]
//
//  model: 114  target: nearbody2  profile: field  layers: field_base,metal7,metal8
//
CAPACITANCE NEARBODY metal7 metal8 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00521664 * exp(0.0175708 * distance())) / pow(distance() , 0.443964) * pow((width1() + width2()) / 2 , -0.157458) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00521664 * exp(0.0175708 * distance())) / pow(distance() , 0.443964) * pow((width1() + width2()) / 2 , -0.157458)
    }
]
CAPACITANCE NEARBODY metal8 metal7 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00521664 * exp(0.0175708 * distance())) / pow(distance() , 0.443964) * pow((width1() + width2()) / 2 , -0.157458) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00521664 * exp(0.0175708 * distance())) / pow(distance() , 0.443964) * pow((width1() + width2()) / 2 , -0.157458)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal2
//
CAPACITANCE NEARBODY poly metal1 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0357065 * exp(-1.58409 * distance()) + 0.0356927 * exp(-1.58419 * distance())) * pow((width1() + width2()) / 2 , 0.230419) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0357065 * exp(-1.58409 * distance()) + 0.0356927 * exp(-1.58419 * distance())) * pow((width1() + width2()) / 2 , 0.230419)
    }
]
CAPACITANCE NEARBODY metal1 poly INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0357065 * exp(-1.58409 * distance()) + 0.0356927 * exp(-1.58419 * distance())) * pow((width1() + width2()) / 2 , 0.230419) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0357065 * exp(-1.58409 * distance()) + 0.0356927 * exp(-1.58419 * distance())) * pow((width1() + width2()) / 2 , 0.230419)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal3
//
CAPACITANCE NEARBODY poly metal1 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0380267 * exp(-1.33166 * distance()) + 0.0380267 * exp(-1.331 * distance())) * pow((width1() + width2()) / 2 , 0.234638) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0380267 * exp(-1.33166 * distance()) + 0.0380267 * exp(-1.331 * distance())) * pow((width1() + width2()) / 2 , 0.234638)
    }
]
CAPACITANCE NEARBODY metal1 poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0380267 * exp(-1.33166 * distance()) + 0.0380267 * exp(-1.331 * distance())) * pow((width1() + width2()) / 2 , 0.234638) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0380267 * exp(-1.33166 * distance()) + 0.0380267 * exp(-1.331 * distance())) * pow((width1() + width2()) / 2 , 0.234638)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal4
//
CAPACITANCE NEARBODY poly metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0389712 * exp(-1.60818 * distance()) + 0.0387732 * exp(-0.969224 * distance())) * pow((width1() + width2()) / 2 , 0.237729) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0389712 * exp(-1.60818 * distance()) + 0.0387732 * exp(-0.969224 * distance())) * pow((width1() + width2()) / 2 , 0.237729)
    }
]
CAPACITANCE NEARBODY metal1 poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0389712 * exp(-1.60818 * distance()) + 0.0387732 * exp(-0.969224 * distance())) * pow((width1() + width2()) / 2 , 0.237729) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0389712 * exp(-1.60818 * distance()) + 0.0387732 * exp(-0.969224 * distance())) * pow((width1() + width2()) / 2 , 0.237729)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal5
//
CAPACITANCE NEARBODY poly metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0705204 * exp(-1.34828 * distance()) + 0.00796064 * exp(-0.426261 * distance())) * pow((width1() + width2()) / 2 , 0.239545) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0705204 * exp(-1.34828 * distance()) + 0.00796064 * exp(-0.426261 * distance())) * pow((width1() + width2()) / 2 , 0.239545)
    }
]
CAPACITANCE NEARBODY metal1 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0705204 * exp(-1.34828 * distance()) + 0.00796064 * exp(-0.426261 * distance())) * pow((width1() + width2()) / 2 , 0.239545) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0705204 * exp(-1.34828 * distance()) + 0.00796064 * exp(-0.426261 * distance())) * pow((width1() + width2()) / 2 , 0.239545)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal6
//
CAPACITANCE NEARBODY poly metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0693358 * exp(-1.36287 * distance()) + 0.00957488 * exp(-0.403 * distance())) * pow((width1() + width2()) / 2 , 0.240535) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0693358 * exp(-1.36287 * distance()) + 0.00957488 * exp(-0.403 * distance())) * pow((width1() + width2()) / 2 , 0.240535)
    }
]
CAPACITANCE NEARBODY metal1 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0693358 * exp(-1.36287 * distance()) + 0.00957488 * exp(-0.403 * distance())) * pow((width1() + width2()) / 2 , 0.240535) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0693358 * exp(-1.36287 * distance()) + 0.00957488 * exp(-0.403 * distance())) * pow((width1() + width2()) / 2 , 0.240535)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal7
//
CAPACITANCE NEARBODY poly metal1 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0101185 * exp(-0.377374 * distance()) + 0.0690508 * exp(-1.36757 * distance())) * pow((width1() + width2()) / 2 , 0.241182) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0101185 * exp(-0.377374 * distance()) + 0.0690508 * exp(-1.36757 * distance())) * pow((width1() + width2()) / 2 , 0.241182)
    }
]
CAPACITANCE NEARBODY metal1 poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0101185 * exp(-0.377374 * distance()) + 0.0690508 * exp(-1.36757 * distance())) * pow((width1() + width2()) / 2 , 0.241182) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0101185 * exp(-0.377374 * distance()) + 0.0690508 * exp(-1.36757 * distance())) * pow((width1() + width2()) / 2 , 0.241182)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal8
//
CAPACITANCE NEARBODY poly metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0688935 * exp(-1.37172 * distance()) + 0.0104654 * exp(-0.356058 * distance())) * pow((width1() + width2()) / 2 , 0.241623) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0688935 * exp(-1.37172 * distance()) + 0.0104654 * exp(-0.356058 * distance())) * pow((width1() + width2()) / 2 , 0.241623)
    }
]
CAPACITANCE NEARBODY metal1 poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0688935 * exp(-1.37172 * distance()) + 0.0104654 * exp(-0.356058 * distance())) * pow((width1() + width2()) / 2 , 0.241623) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0688935 * exp(-1.37172 * distance()) + 0.0104654 * exp(-0.356058 * distance())) * pow((width1() + width2()) / 2 , 0.241623)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal3
//
CAPACITANCE NEARBODY poly metal2 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0148185 * exp(-0.759797 * distance()) + 0.0148165 * exp(-0.768392 * distance())) * pow((width1() + width2()) / 2 , 0.320389) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0148185 * exp(-0.759797 * distance()) + 0.0148165 * exp(-0.768392 * distance())) * pow((width1() + width2()) / 2 , 0.320389)
    }
]
CAPACITANCE NEARBODY metal2 poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0148185 * exp(-0.759797 * distance()) + 0.0148165 * exp(-0.768392 * distance())) * pow((width1() + width2()) / 2 , 0.320389) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0148185 * exp(-0.759797 * distance()) + 0.0148165 * exp(-0.768392 * distance())) * pow((width1() + width2()) / 2 , 0.320389)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal4
//
CAPACITANCE NEARBODY poly metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0163371 * exp(-0.64867 * distance()) + 0.0163373 * exp(-0.648075 * distance())) * pow((width1() + width2()) / 2 , 0.305671) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0163371 * exp(-0.64867 * distance()) + 0.0163373 * exp(-0.648075 * distance())) * pow((width1() + width2()) / 2 , 0.305671)
    }
]
CAPACITANCE NEARBODY metal2 poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0163371 * exp(-0.64867 * distance()) + 0.0163373 * exp(-0.648075 * distance())) * pow((width1() + width2()) / 2 , 0.305671) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0163371 * exp(-0.64867 * distance()) + 0.0163373 * exp(-0.648075 * distance())) * pow((width1() + width2()) / 2 , 0.305671)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal5
//
CAPACITANCE NEARBODY poly metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0169 * exp(-0.590817 * distance()) + 0.0169 * exp(-0.590319 * distance())) * pow((width1() + width2()) / 2 , 0.302464) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0169 * exp(-0.590817 * distance()) + 0.0169 * exp(-0.590319 * distance())) * pow((width1() + width2()) / 2 , 0.302464)
    }
]
CAPACITANCE NEARBODY metal2 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0169 * exp(-0.590817 * distance()) + 0.0169 * exp(-0.590319 * distance())) * pow((width1() + width2()) / 2 , 0.302464) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0169 * exp(-0.590817 * distance()) + 0.0169 * exp(-0.590319 * distance())) * pow((width1() + width2()) / 2 , 0.302464)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal6
//
CAPACITANCE NEARBODY poly metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0171643 * exp(-0.556964 * distance()) + 0.0171644 * exp(-0.55627 * distance())) * pow((width1() + width2()) / 2 , 0.301315) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0171643 * exp(-0.556964 * distance()) + 0.0171644 * exp(-0.55627 * distance())) * pow((width1() + width2()) / 2 , 0.301315)
    }
]
CAPACITANCE NEARBODY metal2 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0171643 * exp(-0.556964 * distance()) + 0.0171644 * exp(-0.55627 * distance())) * pow((width1() + width2()) / 2 , 0.301315) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0171643 * exp(-0.556964 * distance()) + 0.0171644 * exp(-0.55627 * distance())) * pow((width1() + width2()) / 2 , 0.301315)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal7
//
CAPACITANCE NEARBODY poly metal2 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0172654 * exp(-0.534889 * distance()) + 0.0172655 * exp(-0.535498 * distance())) * pow((width1() + width2()) / 2 , 0.299354) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0172654 * exp(-0.534889 * distance()) + 0.0172655 * exp(-0.535498 * distance())) * pow((width1() + width2()) / 2 , 0.299354)
    }
]
CAPACITANCE NEARBODY metal2 poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0172654 * exp(-0.534889 * distance()) + 0.0172655 * exp(-0.535498 * distance())) * pow((width1() + width2()) / 2 , 0.299354) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0172654 * exp(-0.534889 * distance()) + 0.0172655 * exp(-0.535498 * distance())) * pow((width1() + width2()) / 2 , 0.299354)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal8
//
CAPACITANCE NEARBODY poly metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0174062 * exp(-0.517315 * distance()) + 0.0174072 * exp(-0.518677 * distance())) * pow((width1() + width2()) / 2 , 0.300507) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0174062 * exp(-0.517315 * distance()) + 0.0174072 * exp(-0.518677 * distance())) * pow((width1() + width2()) / 2 , 0.300507)
    }
]
CAPACITANCE NEARBODY metal2 poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0174062 * exp(-0.517315 * distance()) + 0.0174072 * exp(-0.518677 * distance())) * pow((width1() + width2()) / 2 , 0.300507) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0174062 * exp(-0.517315 * distance()) + 0.0174072 * exp(-0.518677 * distance())) * pow((width1() + width2()) / 2 , 0.300507)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal4
//
CAPACITANCE NEARBODY poly metal3 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00793569 * exp(-0.522696 * distance()) + 0.00793431 * exp(-0.522666 * distance())) * pow((width1() + width2()) / 2 , 0.356049) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00793569 * exp(-0.522696 * distance()) + 0.00793431 * exp(-0.522666 * distance())) * pow((width1() + width2()) / 2 , 0.356049)
    }
]
CAPACITANCE NEARBODY metal3 poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00793569 * exp(-0.522696 * distance()) + 0.00793431 * exp(-0.522666 * distance())) * pow((width1() + width2()) / 2 , 0.356049) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00793569 * exp(-0.522696 * distance()) + 0.00793431 * exp(-0.522666 * distance())) * pow((width1() + width2()) / 2 , 0.356049)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal5
//
CAPACITANCE NEARBODY poly metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00907784 * exp(-0.44467 * distance()) + 0.00907447 * exp(-0.444071 * distance())) * pow((width1() + width2()) / 2 , 0.32758) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00907784 * exp(-0.44467 * distance()) + 0.00907447 * exp(-0.444071 * distance())) * pow((width1() + width2()) / 2 , 0.32758)
    }
]
CAPACITANCE NEARBODY metal3 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00907784 * exp(-0.44467 * distance()) + 0.00907447 * exp(-0.444071 * distance())) * pow((width1() + width2()) / 2 , 0.32758) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00907784 * exp(-0.44467 * distance()) + 0.00907447 * exp(-0.444071 * distance())) * pow((width1() + width2()) / 2 , 0.32758)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal6
//
CAPACITANCE NEARBODY poly metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00958521 * exp(-0.400242 * distance()) + 0.00958045 * exp(-0.400185 * distance())) * pow((width1() + width2()) / 2 , 0.320187) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00958521 * exp(-0.400242 * distance()) + 0.00958045 * exp(-0.400185 * distance())) * pow((width1() + width2()) / 2 , 0.320187)
    }
]
CAPACITANCE NEARBODY metal3 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00958521 * exp(-0.400242 * distance()) + 0.00958045 * exp(-0.400185 * distance())) * pow((width1() + width2()) / 2 , 0.320187) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00958521 * exp(-0.400242 * distance()) + 0.00958045 * exp(-0.400185 * distance())) * pow((width1() + width2()) / 2 , 0.320187)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal7
//
CAPACITANCE NEARBODY poly metal3 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00985072 * exp(-0.372842 * distance()) + 0.00985101 * exp(-0.372477 * distance())) * pow((width1() + width2()) / 2 , 0.317115) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00985072 * exp(-0.372842 * distance()) + 0.00985101 * exp(-0.372477 * distance())) * pow((width1() + width2()) / 2 , 0.317115)
    }
]
CAPACITANCE NEARBODY metal3 poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00985072 * exp(-0.372842 * distance()) + 0.00985101 * exp(-0.372477 * distance())) * pow((width1() + width2()) / 2 , 0.317115) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00985072 * exp(-0.372842 * distance()) + 0.00985101 * exp(-0.372477 * distance())) * pow((width1() + width2()) / 2 , 0.317115)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal8
//
CAPACITANCE NEARBODY poly metal3 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0100269 * exp(-0.35123 * distance()) + 0.0100271 * exp(-0.351677 * distance())) * pow((width1() + width2()) / 2 , 0.314971) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0100269 * exp(-0.35123 * distance()) + 0.0100271 * exp(-0.351677 * distance())) * pow((width1() + width2()) / 2 , 0.314971)
    }
]
CAPACITANCE NEARBODY metal3 poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0100269 * exp(-0.35123 * distance()) + 0.0100271 * exp(-0.351677 * distance())) * pow((width1() + width2()) / 2 , 0.314971) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0100269 * exp(-0.35123 * distance()) + 0.0100271 * exp(-0.351677 * distance())) * pow((width1() + width2()) / 2 , 0.314971)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal5
//
CAPACITANCE NEARBODY poly metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00496398 * exp(-0.390334 * distance()) + 0.00496401 * exp(-0.38789 * distance())) * pow((width1() + width2()) / 2 , 0.372001) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00496398 * exp(-0.390334 * distance()) + 0.00496401 * exp(-0.38789 * distance())) * pow((width1() + width2()) / 2 , 0.372001)
    }
]
CAPACITANCE NEARBODY metal4 poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00496398 * exp(-0.390334 * distance()) + 0.00496401 * exp(-0.38789 * distance())) * pow((width1() + width2()) / 2 , 0.372001) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00496398 * exp(-0.390334 * distance()) + 0.00496401 * exp(-0.38789 * distance())) * pow((width1() + width2()) / 2 , 0.372001)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal6
//
CAPACITANCE NEARBODY poly metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00587564 * exp(-0.331302 * distance()) + 0.00587521 * exp(-0.330944 * distance())) * pow((width1() + width2()) / 2 , 0.337876) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00587564 * exp(-0.331302 * distance()) + 0.00587521 * exp(-0.330944 * distance())) * pow((width1() + width2()) / 2 , 0.337876)
    }
]
CAPACITANCE NEARBODY metal4 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00587564 * exp(-0.331302 * distance()) + 0.00587521 * exp(-0.330944 * distance())) * pow((width1() + width2()) / 2 , 0.337876) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00587564 * exp(-0.331302 * distance()) + 0.00587521 * exp(-0.330944 * distance())) * pow((width1() + width2()) / 2 , 0.337876)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal7
//
CAPACITANCE NEARBODY poly metal4 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00631314 * exp(-0.295671 * distance()) + 0.00631264 * exp(-0.296863 * distance())) * pow((width1() + width2()) / 2 , 0.327239) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00631314 * exp(-0.295671 * distance()) + 0.00631264 * exp(-0.296863 * distance())) * pow((width1() + width2()) / 2 , 0.327239)
    }
]
CAPACITANCE NEARBODY metal4 poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00631314 * exp(-0.295671 * distance()) + 0.00631264 * exp(-0.296863 * distance())) * pow((width1() + width2()) / 2 , 0.327239) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00631314 * exp(-0.295671 * distance()) + 0.00631264 * exp(-0.296863 * distance())) * pow((width1() + width2()) / 2 , 0.327239)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal8
//
CAPACITANCE NEARBODY poly metal4 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00659749 * exp(-0.27153 * distance()) + 0.00659736 * exp(-0.269045 * distance())) * pow((width1() + width2()) / 2 , 0.321891) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00659749 * exp(-0.27153 * distance()) + 0.00659736 * exp(-0.269045 * distance())) * pow((width1() + width2()) / 2 , 0.321891)
    }
]
CAPACITANCE NEARBODY metal4 poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00659749 * exp(-0.27153 * distance()) + 0.00659736 * exp(-0.269045 * distance())) * pow((width1() + width2()) / 2 , 0.321891) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00659749 * exp(-0.27153 * distance()) + 0.00659736 * exp(-0.269045 * distance())) * pow((width1() + width2()) / 2 , 0.321891)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal5,metal6
//
CAPACITANCE NEARBODY poly metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00338947 * exp(-0.304192 * distance()) + 0.00338956 * exp(-0.294938 * distance())) * pow((width1() + width2()) / 2 , 0.380299) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00338947 * exp(-0.304192 * distance()) + 0.00338956 * exp(-0.294938 * distance())) * pow((width1() + width2()) / 2 , 0.380299)
    }
]
CAPACITANCE NEARBODY metal5 poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00338947 * exp(-0.304192 * distance()) + 0.00338956 * exp(-0.294938 * distance())) * pow((width1() + width2()) / 2 , 0.380299) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00338947 * exp(-0.304192 * distance()) + 0.00338956 * exp(-0.294938 * distance())) * pow((width1() + width2()) / 2 , 0.380299)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal5,metal7
//
CAPACITANCE NEARBODY poly metal5 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00413076 * exp(-0.254849 * distance()) + 0.00413097 * exp(-0.255593 * distance())) * pow((width1() + width2()) / 2 , 0.34378) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00413076 * exp(-0.254849 * distance()) + 0.00413097 * exp(-0.255593 * distance())) * pow((width1() + width2()) / 2 , 0.34378)
    }
]
CAPACITANCE NEARBODY metal5 poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00413076 * exp(-0.254849 * distance()) + 0.00413097 * exp(-0.255593 * distance())) * pow((width1() + width2()) / 2 , 0.34378) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00413076 * exp(-0.254849 * distance()) + 0.00413097 * exp(-0.255593 * distance())) * pow((width1() + width2()) / 2 , 0.34378)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal5,metal8
//
CAPACITANCE NEARBODY poly metal5 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00456608 * exp(-0.223318 * distance()) + 0.00456613 * exp(-0.223435 * distance())) * pow((width1() + width2()) / 2 , 0.330275) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00456608 * exp(-0.223318 * distance()) + 0.00456613 * exp(-0.223435 * distance())) * pow((width1() + width2()) / 2 , 0.330275)
    }
]
CAPACITANCE NEARBODY metal5 poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00456608 * exp(-0.223318 * distance()) + 0.00456613 * exp(-0.223435 * distance())) * pow((width1() + width2()) / 2 , 0.330275) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00456608 * exp(-0.223318 * distance()) + 0.00456613 * exp(-0.223435 * distance())) * pow((width1() + width2()) / 2 , 0.330275)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal6,metal7
//
CAPACITANCE NEARBODY poly metal6 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00246193 * exp(-0.235086 * distance()) + 0.00246148 * exp(-0.234912 * distance())) * pow((width1() + width2()) / 2 , 0.387117) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00246193 * exp(-0.235086 * distance()) + 0.00246148 * exp(-0.234912 * distance())) * pow((width1() + width2()) / 2 , 0.387117)
    }
]
CAPACITANCE NEARBODY metal6 poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00246193 * exp(-0.235086 * distance()) + 0.00246148 * exp(-0.234912 * distance())) * pow((width1() + width2()) / 2 , 0.387117) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00246193 * exp(-0.235086 * distance()) + 0.00246148 * exp(-0.234912 * distance())) * pow((width1() + width2()) / 2 , 0.387117)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal6,metal8
//
CAPACITANCE NEARBODY poly metal6 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0031455 * exp(-0.195334 * distance()) + 0.00314545 * exp(-0.195816 * distance())) * pow((width1() + width2()) / 2 , 0.344473) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0031455 * exp(-0.195334 * distance()) + 0.00314545 * exp(-0.195816 * distance())) * pow((width1() + width2()) / 2 , 0.344473)
    }
]
CAPACITANCE NEARBODY metal6 poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0031455 * exp(-0.195334 * distance()) + 0.00314545 * exp(-0.195816 * distance())) * pow((width1() + width2()) / 2 , 0.344473) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0031455 * exp(-0.195334 * distance()) + 0.00314545 * exp(-0.195816 * distance())) * pow((width1() + width2()) / 2 , 0.344473)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,poly,metal7,metal8
//
CAPACITANCE NEARBODY poly metal7 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00200166 * exp(-0.180623 * distance()) + 0.00199929 * exp(-0.179893 * distance())) * pow((width1() + width2()) / 2 , 0.378083) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00200166 * exp(-0.180623 * distance()) + 0.00199929 * exp(-0.179893 * distance())) * pow((width1() + width2()) / 2 , 0.378083)
    }
]
CAPACITANCE NEARBODY metal7 poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00200166 * exp(-0.180623 * distance()) + 0.00199929 * exp(-0.179893 * distance())) * pow((width1() + width2()) / 2 , 0.378083) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00200166 * exp(-0.180623 * distance()) + 0.00199929 * exp(-0.179893 * distance())) * pow((width1() + width2()) / 2 , 0.378083)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal3
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0298457 * exp(-1.08131 * distance()) + 0.0298457 * exp(-1.07958 * distance())) * pow((width1() + width2()) / 2 , 0.218205) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0298457 * exp(-1.08131 * distance()) + 0.0298457 * exp(-1.07958 * distance())) * pow((width1() + width2()) / 2 , 0.218205)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0298457 * exp(-1.08131 * distance()) + 0.0298457 * exp(-1.07958 * distance())) * pow((width1() + width2()) / 2 , 0.218205) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0298457 * exp(-1.08131 * distance()) + 0.0298457 * exp(-1.07958 * distance())) * pow((width1() + width2()) / 2 , 0.218205)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal4
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0324055 * exp(-0.90565 * distance()) + 0.0324056 * exp(-0.90414 * distance())) * pow((width1() + width2()) / 2 , 0.218439) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0324055 * exp(-0.90565 * distance()) + 0.0324056 * exp(-0.90414 * distance())) * pow((width1() + width2()) / 2 , 0.218439)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0324055 * exp(-0.90565 * distance()) + 0.0324056 * exp(-0.90414 * distance())) * pow((width1() + width2()) / 2 , 0.218439) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0324055 * exp(-0.90565 * distance()) + 0.0324056 * exp(-0.90414 * distance())) * pow((width1() + width2()) / 2 , 0.218439)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal5
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0335902 * exp(-1.01326 * distance()) + 0.0333763 * exp(-0.678665 * distance())) * pow((width1() + width2()) / 2 , 0.222586) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0335902 * exp(-1.01326 * distance()) + 0.0333763 * exp(-0.678665 * distance())) * pow((width1() + width2()) / 2 , 0.222586)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0335902 * exp(-1.01326 * distance()) + 0.0333763 * exp(-0.678665 * distance())) * pow((width1() + width2()) / 2 , 0.222586) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0335902 * exp(-1.01326 * distance()) + 0.0333763 * exp(-0.678665 * distance())) * pow((width1() + width2()) / 2 , 0.222586)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal6
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0632252 * exp(-0.860994 * distance()) + 0.00487751 * exp(-0.226195 * distance())) * pow((width1() + width2()) / 2 , 0.225629) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0632252 * exp(-0.860994 * distance()) + 0.00487751 * exp(-0.226195 * distance())) * pow((width1() + width2()) / 2 , 0.225629)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0632252 * exp(-0.860994 * distance()) + 0.00487751 * exp(-0.226195 * distance())) * pow((width1() + width2()) / 2 , 0.225629) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0632252 * exp(-0.860994 * distance()) + 0.00487751 * exp(-0.226195 * distance())) * pow((width1() + width2()) / 2 , 0.225629)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal7
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00767373 * exp(-0.239761 * distance()) + 0.0611262 * exp(-0.874504 * distance())) * pow((width1() + width2()) / 2 , 0.227659) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00767373 * exp(-0.239761 * distance()) + 0.0611262 * exp(-0.874504 * distance())) * pow((width1() + width2()) / 2 , 0.227659)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00767373 * exp(-0.239761 * distance()) + 0.0611262 * exp(-0.874504 * distance())) * pow((width1() + width2()) / 2 , 0.227659) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00767373 * exp(-0.239761 * distance()) + 0.0611262 * exp(-0.874504 * distance())) * pow((width1() + width2()) / 2 , 0.227659)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal8
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00937992 * exp(-0.230127 * distance()) + 0.0599359 * exp(-0.884339 * distance())) * pow((width1() + width2()) / 2 , 0.229272) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00937992 * exp(-0.230127 * distance()) + 0.0599359 * exp(-0.884339 * distance())) * pow((width1() + width2()) / 2 , 0.229272)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00937992 * exp(-0.230127 * distance()) + 0.0599359 * exp(-0.884339 * distance())) * pow((width1() + width2()) / 2 , 0.229272) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00937992 * exp(-0.230127 * distance()) + 0.0599359 * exp(-0.884339 * distance())) * pow((width1() + width2()) / 2 , 0.229272)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal4
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0144989 * exp(-0.618905 * distance()) + 0.0144988 * exp(-0.617774 * distance())) * pow((width1() + width2()) / 2 , 0.329366) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0144989 * exp(-0.618905 * distance()) + 0.0144988 * exp(-0.617774 * distance())) * pow((width1() + width2()) / 2 , 0.329366)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0144989 * exp(-0.618905 * distance()) + 0.0144988 * exp(-0.617774 * distance())) * pow((width1() + width2()) / 2 , 0.329366) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0144989 * exp(-0.618905 * distance()) + 0.0144988 * exp(-0.617774 * distance())) * pow((width1() + width2()) / 2 , 0.329366)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal5
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0163679 * exp(-0.522947 * distance()) + 0.0163679 * exp(-0.522708 * distance())) * pow((width1() + width2()) / 2 , 0.304166) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0163679 * exp(-0.522947 * distance()) + 0.0163679 * exp(-0.522708 * distance())) * pow((width1() + width2()) / 2 , 0.304166)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0163679 * exp(-0.522947 * distance()) + 0.0163679 * exp(-0.522708 * distance())) * pow((width1() + width2()) / 2 , 0.304166) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0163679 * exp(-0.522947 * distance()) + 0.0163679 * exp(-0.522708 * distance())) * pow((width1() + width2()) / 2 , 0.304166)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal6
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0172008 * exp(-0.47038 * distance()) + 0.0172008 * exp(-0.471035 * distance())) * pow((width1() + width2()) / 2 , 0.29926) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0172008 * exp(-0.47038 * distance()) + 0.0172008 * exp(-0.471035 * distance())) * pow((width1() + width2()) / 2 , 0.29926)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0172008 * exp(-0.47038 * distance()) + 0.0172008 * exp(-0.471035 * distance())) * pow((width1() + width2()) / 2 , 0.29926) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0172008 * exp(-0.47038 * distance()) + 0.0172008 * exp(-0.471035 * distance())) * pow((width1() + width2()) / 2 , 0.29926)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal7
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0176442 * exp(-0.437933 * distance()) + 0.0176442 * exp(-0.439007 * distance())) * pow((width1() + width2()) / 2 , 0.297946) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0176442 * exp(-0.437933 * distance()) + 0.0176442 * exp(-0.439007 * distance())) * pow((width1() + width2()) / 2 , 0.297946)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0176442 * exp(-0.437933 * distance()) + 0.0176442 * exp(-0.439007 * distance())) * pow((width1() + width2()) / 2 , 0.297946) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0176442 * exp(-0.437933 * distance()) + 0.0176442 * exp(-0.439007 * distance())) * pow((width1() + width2()) / 2 , 0.297946)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal8
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0179458 * exp(-0.413957 * distance()) + 0.0179458 * exp(-0.413707 * distance())) * pow((width1() + width2()) / 2 , 0.297553) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0179458 * exp(-0.413957 * distance()) + 0.0179458 * exp(-0.413707 * distance())) * pow((width1() + width2()) / 2 , 0.297553)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0179458 * exp(-0.413957 * distance()) + 0.0179458 * exp(-0.413707 * distance())) * pow((width1() + width2()) / 2 , 0.297553) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0179458 * exp(-0.413957 * distance()) + 0.0179458 * exp(-0.413707 * distance())) * pow((width1() + width2()) / 2 , 0.297553)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal5
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0089691 * exp(-0.443445 * distance()) + 0.00896879 * exp(-0.443637 * distance())) * pow((width1() + width2()) / 2 , 0.380258) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0089691 * exp(-0.443445 * distance()) + 0.00896879 * exp(-0.443637 * distance())) * pow((width1() + width2()) / 2 , 0.380258)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0089691 * exp(-0.443445 * distance()) + 0.00896879 * exp(-0.443637 * distance())) * pow((width1() + width2()) / 2 , 0.380258) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0089691 * exp(-0.443445 * distance()) + 0.00896879 * exp(-0.443637 * distance())) * pow((width1() + width2()) / 2 , 0.380258)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal6
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0104251 * exp(-0.376609 * distance()) + 0.0104251 * exp(-0.37714 * distance())) * pow((width1() + width2()) / 2 , 0.34031) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0104251 * exp(-0.376609 * distance()) + 0.0104251 * exp(-0.37714 * distance())) * pow((width1() + width2()) / 2 , 0.34031)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0104251 * exp(-0.376609 * distance()) + 0.0104251 * exp(-0.37714 * distance())) * pow((width1() + width2()) / 2 , 0.34031) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0104251 * exp(-0.376609 * distance()) + 0.0104251 * exp(-0.37714 * distance())) * pow((width1() + width2()) / 2 , 0.34031)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal7
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0111373 * exp(-0.337477 * distance()) + 0.0111373 * exp(-0.337429 * distance())) * pow((width1() + width2()) / 2 , 0.329231) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0111373 * exp(-0.337477 * distance()) + 0.0111373 * exp(-0.337429 * distance())) * pow((width1() + width2()) / 2 , 0.329231)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0111373 * exp(-0.337477 * distance()) + 0.0111373 * exp(-0.337429 * distance())) * pow((width1() + width2()) / 2 , 0.329231) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0111373 * exp(-0.337477 * distance()) + 0.0111373 * exp(-0.337429 * distance())) * pow((width1() + width2()) / 2 , 0.329231)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal8
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0116007 * exp(-0.308156 * distance()) + 0.0116007 * exp(-0.308195 * distance())) * pow((width1() + width2()) / 2 , 0.32411) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0116007 * exp(-0.308156 * distance()) + 0.0116007 * exp(-0.308195 * distance())) * pow((width1() + width2()) / 2 , 0.32411)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0116007 * exp(-0.308156 * distance()) + 0.0116007 * exp(-0.308195 * distance())) * pow((width1() + width2()) / 2 , 0.32411) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0116007 * exp(-0.308156 * distance()) + 0.0116007 * exp(-0.308195 * distance())) * pow((width1() + width2()) / 2 , 0.32411)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal5,metal6
//
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00612563 * exp(-0.337542 * distance()) + 0.00612568 * exp(-0.336946 * distance())) * pow((width1() + width2()) / 2 , 0.408362) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00612563 * exp(-0.337542 * distance()) + 0.00612568 * exp(-0.336946 * distance())) * pow((width1() + width2()) / 2 , 0.408362)
    }
]
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00612563 * exp(-0.337542 * distance()) + 0.00612568 * exp(-0.336946 * distance())) * pow((width1() + width2()) / 2 , 0.408362) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00612563 * exp(-0.337542 * distance()) + 0.00612568 * exp(-0.336946 * distance())) * pow((width1() + width2()) / 2 , 0.408362)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal5,metal7
//
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0072994 * exp(-0.286786 * distance()) + 0.00729937 * exp(-0.288255 * distance())) * pow((width1() + width2()) / 2 , 0.360117) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0072994 * exp(-0.286786 * distance()) + 0.00729937 * exp(-0.288255 * distance())) * pow((width1() + width2()) / 2 , 0.360117)
    }
]
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0072994 * exp(-0.286786 * distance()) + 0.00729937 * exp(-0.288255 * distance())) * pow((width1() + width2()) / 2 , 0.360117) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0072994 * exp(-0.286786 * distance()) + 0.00729937 * exp(-0.288255 * distance())) * pow((width1() + width2()) / 2 , 0.360117)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal5,metal8
//
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.008 * exp(-0.252068 * distance()) + 0.00800003 * exp(-0.252065 * distance())) * pow((width1() + width2()) / 2 , 0.343416) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.008 * exp(-0.252068 * distance()) + 0.00800003 * exp(-0.252065 * distance())) * pow((width1() + width2()) / 2 , 0.343416)
    }
]
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.008 * exp(-0.252068 * distance()) + 0.00800003 * exp(-0.252065 * distance())) * pow((width1() + width2()) / 2 , 0.343416) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.008 * exp(-0.252068 * distance()) + 0.00800003 * exp(-0.252065 * distance())) * pow((width1() + width2()) / 2 , 0.343416)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal6,metal7
//
CAPACITANCE NEARBODY metal1 metal6 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00444753 * exp(-0.263637 * distance()) + 0.00444749 * exp(-0.26359 * distance())) * pow((width1() + width2()) / 2 , 0.426081) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00444753 * exp(-0.263637 * distance()) + 0.00444749 * exp(-0.26359 * distance())) * pow((width1() + width2()) / 2 , 0.426081)
    }
]
CAPACITANCE NEARBODY metal6 metal1 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00444753 * exp(-0.263637 * distance()) + 0.00444749 * exp(-0.26359 * distance())) * pow((width1() + width2()) / 2 , 0.426081) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00444753 * exp(-0.263637 * distance()) + 0.00444749 * exp(-0.26359 * distance())) * pow((width1() + width2()) / 2 , 0.426081)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal6,metal8
//
CAPACITANCE NEARBODY metal1 metal6 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00553975 * exp(-0.219478 * distance()) + 0.00553974 * exp(-0.220159 * distance())) * pow((width1() + width2()) / 2 , 0.368248) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00553975 * exp(-0.219478 * distance()) + 0.00553974 * exp(-0.220159 * distance())) * pow((width1() + width2()) / 2 , 0.368248)
    }
]
CAPACITANCE NEARBODY metal6 metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00553975 * exp(-0.219478 * distance()) + 0.00553974 * exp(-0.220159 * distance())) * pow((width1() + width2()) / 2 , 0.368248) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00553975 * exp(-0.219478 * distance()) + 0.00553974 * exp(-0.220159 * distance())) * pow((width1() + width2()) / 2 , 0.368248)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal1,metal7,metal8
//
CAPACITANCE NEARBODY metal1 metal7 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00359364 * exp(-0.202315 * distance()) + 0.00358411 * exp(-0.202286 * distance())) * pow((width1() + width2()) / 2 , 0.419266) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00359364 * exp(-0.202315 * distance()) + 0.00358411 * exp(-0.202286 * distance())) * pow((width1() + width2()) / 2 , 0.419266)
    }
]
CAPACITANCE NEARBODY metal7 metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00359364 * exp(-0.202315 * distance()) + 0.00358411 * exp(-0.202286 * distance())) * pow((width1() + width2()) / 2 , 0.419266) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00359364 * exp(-0.202315 * distance()) + 0.00358411 * exp(-0.202286 * distance())) * pow((width1() + width2()) / 2 , 0.419266)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal4
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0314441 * exp(-0.966438 * distance()) + 0.0314438 * exp(-0.969105 * distance())) * pow((width1() + width2()) / 2 , 0.208884) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0314441 * exp(-0.966438 * distance()) + 0.0314438 * exp(-0.969105 * distance())) * pow((width1() + width2()) / 2 , 0.208884)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0314441 * exp(-0.966438 * distance()) + 0.0314438 * exp(-0.969105 * distance())) * pow((width1() + width2()) / 2 , 0.208884) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0314441 * exp(-0.966438 * distance()) + 0.0314438 * exp(-0.969105 * distance())) * pow((width1() + width2()) / 2 , 0.208884)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal5
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0344049 * exp(-0.629732 * distance()) + 0.0345177 * exp(-1.05046 * distance())) * pow((width1() + width2()) / 2 , 0.204724) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0344049 * exp(-0.629732 * distance()) + 0.0345177 * exp(-1.05046 * distance())) * pow((width1() + width2()) / 2 , 0.204724)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0344049 * exp(-0.629732 * distance()) + 0.0345177 * exp(-1.05046 * distance())) * pow((width1() + width2()) / 2 , 0.204724) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0344049 * exp(-0.629732 * distance()) + 0.0345177 * exp(-1.05046 * distance())) * pow((width1() + width2()) / 2 , 0.204724)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal6
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0497195 * exp(-0.953395 * distance()) + 0.0219098 * exp(-0.418602 * distance())) * pow((width1() + width2()) / 2 , 0.207984) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0497195 * exp(-0.953395 * distance()) + 0.0219098 * exp(-0.418602 * distance())) * pow((width1() + width2()) / 2 , 0.207984)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0497195 * exp(-0.953395 * distance()) + 0.0219098 * exp(-0.418602 * distance())) * pow((width1() + width2()) / 2 , 0.207984) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0497195 * exp(-0.953395 * distance()) + 0.0219098 * exp(-0.418602 * distance())) * pow((width1() + width2()) / 2 , 0.207984)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal7
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0256988 * exp(-0.376966 * distance()) + 0.0474454 * exp(-0.982983 * distance())) * pow((width1() + width2()) / 2 , 0.210859) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0256988 * exp(-0.376966 * distance()) + 0.0474454 * exp(-0.982983 * distance())) * pow((width1() + width2()) / 2 , 0.210859)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0256988 * exp(-0.376966 * distance()) + 0.0474454 * exp(-0.982983 * distance())) * pow((width1() + width2()) / 2 , 0.210859) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0256988 * exp(-0.376966 * distance()) + 0.0474454 * exp(-0.982983 * distance())) * pow((width1() + width2()) / 2 , 0.210859)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal8
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0261617 * exp(-0.331291 * distance()) + 0.0480469 * exp(-0.988052 * distance())) * pow((width1() + width2()) / 2 , 0.213322) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0261617 * exp(-0.331291 * distance()) + 0.0480469 * exp(-0.988052 * distance())) * pow((width1() + width2()) / 2 , 0.213322)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0261617 * exp(-0.331291 * distance()) + 0.0480469 * exp(-0.988052 * distance())) * pow((width1() + width2()) / 2 , 0.213322) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0261617 * exp(-0.331291 * distance()) + 0.0480469 * exp(-0.988052 * distance())) * pow((width1() + width2()) / 2 , 0.213322)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal5
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0159139 * exp(-0.553217 * distance()) + 0.0159139 * exp(-0.552187 * distance())) * pow((width1() + width2()) / 2 , 0.322943) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0159139 * exp(-0.553217 * distance()) + 0.0159139 * exp(-0.552187 * distance())) * pow((width1() + width2()) / 2 , 0.322943)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0159139 * exp(-0.553217 * distance()) + 0.0159139 * exp(-0.552187 * distance())) * pow((width1() + width2()) / 2 , 0.322943) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0159139 * exp(-0.553217 * distance()) + 0.0159139 * exp(-0.552187 * distance())) * pow((width1() + width2()) / 2 , 0.322943)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal6
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0181329 * exp(-0.466534 * distance()) + 0.0181329 * exp(-0.465903 * distance())) * pow((width1() + width2()) / 2 , 0.292425) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0181329 * exp(-0.466534 * distance()) + 0.0181329 * exp(-0.465903 * distance())) * pow((width1() + width2()) / 2 , 0.292425)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0181329 * exp(-0.466534 * distance()) + 0.0181329 * exp(-0.465903 * distance())) * pow((width1() + width2()) / 2 , 0.292425) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0181329 * exp(-0.466534 * distance()) + 0.0181329 * exp(-0.465903 * distance())) * pow((width1() + width2()) / 2 , 0.292425)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal7
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0191797 * exp(-0.415493 * distance()) + 0.0191798 * exp(-0.420562 * distance())) * pow((width1() + width2()) / 2 , 0.285653) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0191797 * exp(-0.415493 * distance()) + 0.0191798 * exp(-0.420562 * distance())) * pow((width1() + width2()) / 2 , 0.285653)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0191797 * exp(-0.415493 * distance()) + 0.0191798 * exp(-0.420562 * distance())) * pow((width1() + width2()) / 2 , 0.285653) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0191797 * exp(-0.415493 * distance()) + 0.0191798 * exp(-0.420562 * distance())) * pow((width1() + width2()) / 2 , 0.285653)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal8
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0198475 * exp(-0.383453 * distance()) + 0.0198475 * exp(-0.383122 * distance())) * pow((width1() + width2()) / 2 , 0.283407) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0198475 * exp(-0.383453 * distance()) + 0.0198475 * exp(-0.383122 * distance())) * pow((width1() + width2()) / 2 , 0.283407)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0198475 * exp(-0.383453 * distance()) + 0.0198475 * exp(-0.383122 * distance())) * pow((width1() + width2()) / 2 , 0.283407) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0198475 * exp(-0.383453 * distance()) + 0.0198475 * exp(-0.383122 * distance())) * pow((width1() + width2()) / 2 , 0.283407)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal5,metal6
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0101883 * exp(-0.397082 * distance()) + 0.0101883 * exp(-0.396115 * distance())) * pow((width1() + width2()) / 2 , 0.376684) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0101883 * exp(-0.397082 * distance()) + 0.0101883 * exp(-0.396115 * distance())) * pow((width1() + width2()) / 2 , 0.376684)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0101883 * exp(-0.397082 * distance()) + 0.0101883 * exp(-0.396115 * distance())) * pow((width1() + width2()) / 2 , 0.376684) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0101883 * exp(-0.397082 * distance()) + 0.0101883 * exp(-0.396115 * distance())) * pow((width1() + width2()) / 2 , 0.376684)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal5,metal7
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0119543 * exp(-0.337169 * distance()) + 0.0119489 * exp(-0.336717 * distance())) * pow((width1() + width2()) / 2 , 0.330863) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0119543 * exp(-0.337169 * distance()) + 0.0119489 * exp(-0.336717 * distance())) * pow((width1() + width2()) / 2 , 0.330863)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0119543 * exp(-0.337169 * distance()) + 0.0119489 * exp(-0.336717 * distance())) * pow((width1() + width2()) / 2 , 0.330863) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0119543 * exp(-0.337169 * distance()) + 0.0119489 * exp(-0.336717 * distance())) * pow((width1() + width2()) / 2 , 0.330863)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal5,metal8
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0129773 * exp(-0.29585 * distance()) + 0.0129773 * exp(-0.296067 * distance())) * pow((width1() + width2()) / 2 , 0.315975) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0129773 * exp(-0.29585 * distance()) + 0.0129773 * exp(-0.296067 * distance())) * pow((width1() + width2()) / 2 , 0.315975)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0129773 * exp(-0.29585 * distance()) + 0.0129773 * exp(-0.296067 * distance())) * pow((width1() + width2()) / 2 , 0.315975) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0129773 * exp(-0.29585 * distance()) + 0.0129773 * exp(-0.296067 * distance())) * pow((width1() + width2()) / 2 , 0.315975)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal6,metal7
//
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00715794 * exp(-0.302222 * distance()) + 0.007158 * exp(-0.302156 * distance())) * pow((width1() + width2()) / 2 , 0.407264) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00715794 * exp(-0.302222 * distance()) + 0.007158 * exp(-0.302156 * distance())) * pow((width1() + width2()) / 2 , 0.407264)
    }
]
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00715794 * exp(-0.302222 * distance()) + 0.007158 * exp(-0.302156 * distance())) * pow((width1() + width2()) / 2 , 0.407264) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00715794 * exp(-0.302222 * distance()) + 0.007158 * exp(-0.302156 * distance())) * pow((width1() + width2()) / 2 , 0.407264)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal6,metal8
//
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0087825 * exp(-0.251664 * distance()) + 0.00878264 * exp(-0.251451 * distance())) * pow((width1() + width2()) / 2 , 0.347906) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0087825 * exp(-0.251664 * distance()) + 0.00878264 * exp(-0.251451 * distance())) * pow((width1() + width2()) / 2 , 0.347906)
    }
]
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0087825 * exp(-0.251664 * distance()) + 0.00878264 * exp(-0.251451 * distance())) * pow((width1() + width2()) / 2 , 0.347906) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0087825 * exp(-0.251664 * distance()) + 0.00878264 * exp(-0.251451 * distance())) * pow((width1() + width2()) / 2 , 0.347906)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal2,metal7,metal8
//
CAPACITANCE NEARBODY metal2 metal7 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00564446 * exp(-0.228406 * distance()) + 0.00564453 * exp(-0.228744 * distance())) * pow((width1() + width2()) / 2 , 0.406534) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00564446 * exp(-0.228406 * distance()) + 0.00564453 * exp(-0.228744 * distance())) * pow((width1() + width2()) / 2 , 0.406534)
    }
]
CAPACITANCE NEARBODY metal7 metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00564446 * exp(-0.228406 * distance()) + 0.00564453 * exp(-0.228744 * distance())) * pow((width1() + width2()) / 2 , 0.406534) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00564446 * exp(-0.228406 * distance()) + 0.00564453 * exp(-0.228744 * distance())) * pow((width1() + width2()) / 2 , 0.406534)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal5
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0631347 * exp(-0.936642 * distance()) + 0.00136366 * exp(-0.120743 * distance())) * pow((width1() + width2()) / 2 , 0.216393) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0631347 * exp(-0.936642 * distance()) + 0.00136366 * exp(-0.120743 * distance())) * pow((width1() + width2()) / 2 , 0.216393)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0631347 * exp(-0.936642 * distance()) + 0.00136366 * exp(-0.120743 * distance())) * pow((width1() + width2()) / 2 , 0.216393) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0631347 * exp(-0.936642 * distance()) + 0.00136366 * exp(-0.120743 * distance())) * pow((width1() + width2()) / 2 , 0.216393)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal6
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0530259 * exp(-0.936023 * distance()) + 0.0178857 * exp(-0.399429 * distance())) * pow((width1() + width2()) / 2 , 0.208572) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0530259 * exp(-0.936023 * distance()) + 0.0178857 * exp(-0.399429 * distance())) * pow((width1() + width2()) / 2 , 0.208572)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0530259 * exp(-0.936023 * distance()) + 0.0178857 * exp(-0.399429 * distance())) * pow((width1() + width2()) / 2 , 0.208572) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0530259 * exp(-0.936023 * distance()) + 0.0178857 * exp(-0.399429 * distance())) * pow((width1() + width2()) / 2 , 0.208572)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal7
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0453104 * exp(-0.99888 * distance()) + 0.0285826 * exp(-0.382113 * distance())) * pow((width1() + width2()) / 2 , 0.209929) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0453104 * exp(-0.99888 * distance()) + 0.0285826 * exp(-0.382113 * distance())) * pow((width1() + width2()) / 2 , 0.209929)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0453104 * exp(-0.99888 * distance()) + 0.0285826 * exp(-0.382113 * distance())) * pow((width1() + width2()) / 2 , 0.209929) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0453104 * exp(-0.99888 * distance()) + 0.0285826 * exp(-0.382113 * distance())) * pow((width1() + width2()) / 2 , 0.209929)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal8
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0307301 * exp(-0.330543 * distance()) + 0.0450765 * exp(-1.01411 * distance())) * pow((width1() + width2()) / 2 , 0.212133) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0307301 * exp(-0.330543 * distance()) + 0.0450765 * exp(-1.01411 * distance())) * pow((width1() + width2()) / 2 , 0.212133)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0307301 * exp(-0.330543 * distance()) + 0.0450765 * exp(-1.01411 * distance())) * pow((width1() + width2()) / 2 , 0.212133) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0307301 * exp(-0.330543 * distance()) + 0.0450765 * exp(-1.01411 * distance())) * pow((width1() + width2()) / 2 , 0.212133)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,metal5,metal6
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0165115 * exp(-0.505989 * distance()) + 0.0165115 * exp(-0.505982 * distance())) * pow((width1() + width2()) / 2 , 0.323242) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0165115 * exp(-0.505989 * distance()) + 0.0165115 * exp(-0.505982 * distance())) * pow((width1() + width2()) / 2 , 0.323242)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0165115 * exp(-0.505989 * distance()) + 0.0165115 * exp(-0.505982 * distance())) * pow((width1() + width2()) / 2 , 0.323242) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0165115 * exp(-0.505989 * distance()) + 0.0165115 * exp(-0.505982 * distance())) * pow((width1() + width2()) / 2 , 0.323242)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,metal5,metal7
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189055 * exp(-0.425278 * distance()) + 0.0189055 * exp(-0.425432 * distance())) * pow((width1() + width2()) / 2 , 0.288942) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189055 * exp(-0.425278 * distance()) + 0.0189055 * exp(-0.425432 * distance())) * pow((width1() + width2()) / 2 , 0.288942)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189055 * exp(-0.425278 * distance()) + 0.0189055 * exp(-0.425432 * distance())) * pow((width1() + width2()) / 2 , 0.288942) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189055 * exp(-0.425278 * distance()) + 0.0189055 * exp(-0.425432 * distance())) * pow((width1() + width2()) / 2 , 0.288942)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,metal5,metal8
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0202234 * exp(-0.374116 * distance()) + 0.0202234 * exp(-0.373818 * distance())) * pow((width1() + width2()) / 2 , 0.279303) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0202234 * exp(-0.374116 * distance()) + 0.0202234 * exp(-0.373818 * distance())) * pow((width1() + width2()) / 2 , 0.279303)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0202234 * exp(-0.374116 * distance()) + 0.0202234 * exp(-0.373818 * distance())) * pow((width1() + width2()) / 2 , 0.279303) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0202234 * exp(-0.374116 * distance()) + 0.0202234 * exp(-0.373818 * distance())) * pow((width1() + width2()) / 2 , 0.279303)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,metal6,metal7
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0107024 * exp(-0.360031 * distance()) + 0.0107024 * exp(-0.361026 * distance())) * pow((width1() + width2()) / 2 , 0.370988) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0107024 * exp(-0.360031 * distance()) + 0.0107024 * exp(-0.361026 * distance())) * pow((width1() + width2()) / 2 , 0.370988)
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0107024 * exp(-0.360031 * distance()) + 0.0107024 * exp(-0.361026 * distance())) * pow((width1() + width2()) / 2 , 0.370988) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0107024 * exp(-0.360031 * distance()) + 0.0107024 * exp(-0.361026 * distance())) * pow((width1() + width2()) / 2 , 0.370988)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,metal6,metal8
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0128734 * exp(-0.292682 * distance()) + 0.0128735 * exp(-0.303366 * distance())) * pow((width1() + width2()) / 2 , 0.318137) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0128734 * exp(-0.292682 * distance()) + 0.0128735 * exp(-0.303366 * distance())) * pow((width1() + width2()) / 2 , 0.318137)
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0128734 * exp(-0.292682 * distance()) + 0.0128735 * exp(-0.303366 * distance())) * pow((width1() + width2()) / 2 , 0.318137) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0128734 * exp(-0.292682 * distance()) + 0.0128735 * exp(-0.303366 * distance())) * pow((width1() + width2()) / 2 , 0.318137)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal3,metal7,metal8
//
CAPACITANCE NEARBODY metal3 metal7 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0080469 * exp(-0.26367 * distance()) + 0.00804679 * exp(-0.263208 * distance())) * pow((width1() + width2()) / 2 , 0.377914) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0080469 * exp(-0.26367 * distance()) + 0.00804679 * exp(-0.263208 * distance())) * pow((width1() + width2()) / 2 , 0.377914)
    }
]
CAPACITANCE NEARBODY metal7 metal3 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0080469 * exp(-0.26367 * distance()) + 0.00804679 * exp(-0.263208 * distance())) * pow((width1() + width2()) / 2 , 0.377914) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0080469 * exp(-0.26367 * distance()) + 0.00804679 * exp(-0.263208 * distance())) * pow((width1() + width2()) / 2 , 0.377914)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal4,metal5,metal6
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0043548 * exp(-0.230099 * distance()) + 0.0610702 * exp(-0.947313 * distance())) * pow((width1() + width2()) / 2 , 0.221312) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0043548 * exp(-0.230099 * distance()) + 0.0610702 * exp(-0.947313 * distance())) * pow((width1() + width2()) / 2 , 0.221312)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0043548 * exp(-0.230099 * distance()) + 0.0610702 * exp(-0.947313 * distance())) * pow((width1() + width2()) / 2 , 0.221312) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0043548 * exp(-0.230099 * distance()) + 0.0610702 * exp(-0.947313 * distance())) * pow((width1() + width2()) / 2 , 0.221312)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal4,metal5,metal7
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0211704 * exp(-0.363586 * distance()) + 0.0509214 * exp(-0.959245 * distance())) * pow((width1() + width2()) / 2 , 0.211623) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0211704 * exp(-0.363586 * distance()) + 0.0509214 * exp(-0.959245 * distance())) * pow((width1() + width2()) / 2 , 0.211623)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0211704 * exp(-0.363586 * distance()) + 0.0509214 * exp(-0.959245 * distance())) * pow((width1() + width2()) / 2 , 0.211623) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0211704 * exp(-0.363586 * distance()) + 0.0509214 * exp(-0.959245 * distance())) * pow((width1() + width2()) / 2 , 0.211623)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal4,metal5,metal8
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0303169 * exp(-0.331271 * distance()) + 0.0453388 * exp(-1.01118 * distance())) * pow((width1() + width2()) / 2 , 0.212227) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0303169 * exp(-0.331271 * distance()) + 0.0453388 * exp(-1.01118 * distance())) * pow((width1() + width2()) / 2 , 0.212227)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0303169 * exp(-0.331271 * distance()) + 0.0453388 * exp(-1.01118 * distance())) * pow((width1() + width2()) / 2 , 0.212227) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0303169 * exp(-0.331271 * distance()) + 0.0453388 * exp(-1.01118 * distance())) * pow((width1() + width2()) / 2 , 0.212227)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal4,metal6,metal7
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0168463 * exp(-0.476221 * distance()) + 0.0168463 * exp(-0.475513 * distance())) * pow((width1() + width2()) / 2 , 0.324639) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0168463 * exp(-0.476221 * distance()) + 0.0168463 * exp(-0.475513 * distance())) * pow((width1() + width2()) / 2 , 0.324639)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0168463 * exp(-0.476221 * distance()) + 0.0168463 * exp(-0.475513 * distance())) * pow((width1() + width2()) / 2 , 0.324639) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0168463 * exp(-0.476221 * distance()) + 0.0168463 * exp(-0.475513 * distance())) * pow((width1() + width2()) / 2 , 0.324639)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal4,metal6,metal8
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0196599 * exp(-0.387701 * distance()) + 0.0196598 * exp(-0.38912 * distance())) * pow((width1() + width2()) / 2 , 0.285503) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0196599 * exp(-0.387701 * distance()) + 0.0196598 * exp(-0.38912 * distance())) * pow((width1() + width2()) / 2 , 0.285503)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0196599 * exp(-0.387701 * distance()) + 0.0196598 * exp(-0.38912 * distance())) * pow((width1() + width2()) / 2 , 0.285503) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0196599 * exp(-0.387701 * distance()) + 0.0196598 * exp(-0.38912 * distance())) * pow((width1() + width2()) / 2 , 0.285503)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal4,metal7,metal8
//
CAPACITANCE NEARBODY metal4 metal7 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0115867 * exp(-0.323115 * distance()) + 0.0115867 * exp(-0.323075 * distance())) * pow((width1() + width2()) / 2 , 0.351589) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0115867 * exp(-0.323115 * distance()) + 0.0115867 * exp(-0.323075 * distance())) * pow((width1() + width2()) / 2 , 0.351589)
    }
]
CAPACITANCE NEARBODY metal7 metal4 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0115867 * exp(-0.323115 * distance()) + 0.0115867 * exp(-0.323075 * distance())) * pow((width1() + width2()) / 2 , 0.351589) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0115867 * exp(-0.323115 * distance()) + 0.0115867 * exp(-0.323075 * distance())) * pow((width1() + width2()) / 2 , 0.351589)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal5,metal6,metal7
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0596938 * exp(-0.957294 * distance()) + 0.00629721 * exp(-0.238445 * distance())) * pow((width1() + width2()) / 2 , 0.224549) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0596938 * exp(-0.957294 * distance()) + 0.00629721 * exp(-0.238445 * distance())) * pow((width1() + width2()) / 2 , 0.224549)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0596938 * exp(-0.957294 * distance()) + 0.00629721 * exp(-0.238445 * distance())) * pow((width1() + width2()) / 2 , 0.224549) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0596938 * exp(-0.957294 * distance()) + 0.00629721 * exp(-0.238445 * distance())) * pow((width1() + width2()) / 2 , 0.224549)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal5,metal6,metal8
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.024147 * exp(-0.328044 * distance()) + 0.0494897 * exp(-0.976209 * distance())) * pow((width1() + width2()) / 2 , 0.213738) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.024147 * exp(-0.328044 * distance()) + 0.0494897 * exp(-0.976209 * distance())) * pow((width1() + width2()) / 2 , 0.213738)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.024147 * exp(-0.328044 * distance()) + 0.0494897 * exp(-0.976209 * distance())) * pow((width1() + width2()) / 2 , 0.213738) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.024147 * exp(-0.328044 * distance()) + 0.0494897 * exp(-0.976209 * distance())) * pow((width1() + width2()) / 2 , 0.213738)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal5,metal7,metal8
//
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0178181 * exp(-0.43491 * distance()) + 0.0178181 * exp(-0.434913 * distance())) * pow((width1() + width2()) / 2 , 0.312645) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0178181 * exp(-0.43491 * distance()) + 0.0178181 * exp(-0.434913 * distance())) * pow((width1() + width2()) / 2 , 0.312645)
    }
]
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0178181 * exp(-0.43491 * distance()) + 0.0178181 * exp(-0.434913 * distance())) * pow((width1() + width2()) / 2 , 0.312645) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0178181 * exp(-0.43491 * distance()) + 0.0178181 * exp(-0.434913 * distance())) * pow((width1() + width2()) / 2 , 0.312645)
    }
]
//
//  model: 214  target: nearbody2  profile: field  layers: field_base,metal6,metal7,metal8
//
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0339352 * exp(-0.765372 * distance()) + 0.0339352 * exp(-0.76403 * distance())) * pow((width1() + width2()) / 2 , 0.222336) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0339352 * exp(-0.765372 * distance()) + 0.0339352 * exp(-0.76403 * distance())) * pow((width1() + width2()) / 2 , 0.222336)
    }
]
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0339352 * exp(-0.765372 * distance()) + 0.0339352 * exp(-0.76403 * distance())) * pow((width1() + width2()) / 2 , 0.222336) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0339352 * exp(-0.765372 * distance()) + 0.0339352 * exp(-0.76403 * distance())) * pow((width1() + width2()) / 2 , 0.222336)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal2
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0576821 * exp(-1.00187 * distance()) + 0.00502835 * exp(-0.0732654 * distance())) * pow((width1() + width2()) / 2 , 0.258199) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0576821 * exp(-1.00187 * distance()) + 0.00502835 * exp(-0.0732654 * distance())) * pow((width1() + width2()) / 2 , 0.258199)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0576821 * exp(-1.00187 * distance()) + 0.00502835 * exp(-0.0732654 * distance())) * pow((width1() + width2()) / 2 , 0.258199) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0576821 * exp(-1.00187 * distance()) + 0.00502835 * exp(-0.0732654 * distance())) * pow((width1() + width2()) / 2 , 0.258199)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal3
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0157656 * exp(-0.397927 * distance()) + 0.0157656 * exp(-0.398135 * distance())) * pow((width1() + width2()) / 2 , 0.355453) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0157656 * exp(-0.397927 * distance()) + 0.0157656 * exp(-0.398135 * distance())) * pow((width1() + width2()) / 2 , 0.355453)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0157656 * exp(-0.397927 * distance()) + 0.0157656 * exp(-0.398135 * distance())) * pow((width1() + width2()) / 2 , 0.355453) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0157656 * exp(-0.397927 * distance()) + 0.0157656 * exp(-0.398135 * distance())) * pow((width1() + width2()) / 2 , 0.355453)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal4
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0103938 * exp(-0.254429 * distance()) + 0.0103937 * exp(-0.252863 * distance())) * pow((width1() + width2()) / 2 , 0.386095) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0103938 * exp(-0.254429 * distance()) + 0.0103937 * exp(-0.252863 * distance())) * pow((width1() + width2()) / 2 , 0.386095)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0103938 * exp(-0.254429 * distance()) + 0.0103937 * exp(-0.252863 * distance())) * pow((width1() + width2()) / 2 , 0.386095) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0103938 * exp(-0.254429 * distance()) + 0.0103937 * exp(-0.252863 * distance())) * pow((width1() + width2()) / 2 , 0.386095)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal5
//
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00765167 * exp(-0.174076 * distance()) + 0.00765193 * exp(-0.174016 * distance())) * pow((width1() + width2()) / 2 , 0.396478) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00765167 * exp(-0.174076 * distance()) + 0.00765193 * exp(-0.174016 * distance())) * pow((width1() + width2()) / 2 , 0.396478)
    }
]
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00765167 * exp(-0.174076 * distance()) + 0.00765193 * exp(-0.174016 * distance())) * pow((width1() + width2()) / 2 , 0.396478) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00765167 * exp(-0.174076 * distance()) + 0.00765193 * exp(-0.174016 * distance())) * pow((width1() + width2()) / 2 , 0.396478)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal6
//
CAPACITANCE NEARBODY metal1 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00603183 * exp(-0.12392 * distance()) + 0.00603182 * exp(-0.123764 * distance())) * pow((width1() + width2()) / 2 , 0.399157) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00603183 * exp(-0.12392 * distance()) + 0.00603182 * exp(-0.123764 * distance())) * pow((width1() + width2()) / 2 , 0.399157)
    }
]
CAPACITANCE NEARBODY metal6 metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00603183 * exp(-0.12392 * distance()) + 0.00603182 * exp(-0.123764 * distance())) * pow((width1() + width2()) / 2 , 0.399157) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00603183 * exp(-0.12392 * distance()) + 0.00603182 * exp(-0.123764 * distance())) * pow((width1() + width2()) / 2 , 0.399157)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal7
//
CAPACITANCE NEARBODY metal1 metal7 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00500744 * exp(-0.0883836 * distance()) + 0.00500732 * exp(-0.0920384 * distance())) * pow((width1() + width2()) / 2 , 0.396256) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00500744 * exp(-0.0883836 * distance()) + 0.00500732 * exp(-0.0920384 * distance())) * pow((width1() + width2()) / 2 , 0.396256)
    }
]
CAPACITANCE NEARBODY metal7 metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00500744 * exp(-0.0883836 * distance()) + 0.00500732 * exp(-0.0920384 * distance())) * pow((width1() + width2()) / 2 , 0.396256) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00500744 * exp(-0.0883836 * distance()) + 0.00500732 * exp(-0.0920384 * distance())) * pow((width1() + width2()) / 2 , 0.396256)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal8
//
CAPACITANCE NEARBODY metal1 metal8 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00434801 * exp(-0.061523 * distance()) + 0.00434811 * exp(-0.0620535 * distance())) * pow((width1() + width2()) / 2 , 0.36556) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00434801 * exp(-0.061523 * distance()) + 0.00434811 * exp(-0.0620535 * distance())) * pow((width1() + width2()) / 2 , 0.36556)
    }
]
CAPACITANCE NEARBODY metal8 metal1 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00434801 * exp(-0.061523 * distance()) + 0.00434811 * exp(-0.0620535 * distance())) * pow((width1() + width2()) / 2 , 0.36556) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00434801 * exp(-0.061523 * distance()) + 0.00434811 * exp(-0.0620535 * distance())) * pow((width1() + width2()) / 2 , 0.36556)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal3
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0571579 * exp(-0.936521 * distance()) + 0.0166585 * exp(-0.152898 * distance())) * pow((width1() + width2()) / 2 , 0.225185) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0571579 * exp(-0.936521 * distance()) + 0.0166585 * exp(-0.152898 * distance())) * pow((width1() + width2()) / 2 , 0.225185)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0571579 * exp(-0.936521 * distance()) + 0.0166585 * exp(-0.152898 * distance())) * pow((width1() + width2()) / 2 , 0.225185) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0571579 * exp(-0.936521 * distance()) + 0.0166585 * exp(-0.152898 * distance())) * pow((width1() + width2()) / 2 , 0.225185)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal4
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0201869 * exp(-0.216945 * distance()) + 0.0202056 * exp(-0.439836 * distance())) * pow((width1() + width2()) / 2 , 0.297351) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0201869 * exp(-0.216945 * distance()) + 0.0202056 * exp(-0.439836 * distance())) * pow((width1() + width2()) / 2 , 0.297351)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0201869 * exp(-0.216945 * distance()) + 0.0202056 * exp(-0.439836 * distance())) * pow((width1() + width2()) / 2 , 0.297351) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0201869 * exp(-0.216945 * distance()) + 0.0202056 * exp(-0.439836 * distance())) * pow((width1() + width2()) / 2 , 0.297351)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal5
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0140793 * exp(-0.201683 * distance()) + 0.0140793 * exp(-0.201684 * distance())) * pow((width1() + width2()) / 2 , 0.318611) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0140793 * exp(-0.201683 * distance()) + 0.0140793 * exp(-0.201684 * distance())) * pow((width1() + width2()) / 2 , 0.318611)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0140793 * exp(-0.201683 * distance()) + 0.0140793 * exp(-0.201684 * distance())) * pow((width1() + width2()) / 2 , 0.318611) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0140793 * exp(-0.201683 * distance()) + 0.0140793 * exp(-0.201684 * distance())) * pow((width1() + width2()) / 2 , 0.318611)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal6
//
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0108378 * exp(-0.141115 * distance()) + 0.0108377 * exp(-0.140713 * distance())) * pow((width1() + width2()) / 2 , 0.325559) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0108378 * exp(-0.141115 * distance()) + 0.0108377 * exp(-0.140713 * distance())) * pow((width1() + width2()) / 2 , 0.325559)
    }
]
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0108378 * exp(-0.141115 * distance()) + 0.0108377 * exp(-0.140713 * distance())) * pow((width1() + width2()) / 2 , 0.325559) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0108378 * exp(-0.141115 * distance()) + 0.0108377 * exp(-0.140713 * distance())) * pow((width1() + width2()) / 2 , 0.325559)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal7
//
CAPACITANCE NEARBODY metal2 metal7 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00887188 * exp(-0.103332 * distance()) + 0.00887183 * exp(-0.100436 * distance())) * pow((width1() + width2()) / 2 , 0.323866) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00887188 * exp(-0.103332 * distance()) + 0.00887183 * exp(-0.100436 * distance())) * pow((width1() + width2()) / 2 , 0.323866)
    }
]
CAPACITANCE NEARBODY metal7 metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00887188 * exp(-0.103332 * distance()) + 0.00887183 * exp(-0.100436 * distance())) * pow((width1() + width2()) / 2 , 0.323866) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00887188 * exp(-0.103332 * distance()) + 0.00887183 * exp(-0.100436 * distance())) * pow((width1() + width2()) / 2 , 0.323866)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal8
//
CAPACITANCE NEARBODY metal2 metal8 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00762866 * exp(-0.070251 * distance()) + 0.00762839 * exp(-0.0694601 * distance())) * pow((width1() + width2()) / 2 , 0.292261) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00762866 * exp(-0.070251 * distance()) + 0.00762839 * exp(-0.0694601 * distance())) * pow((width1() + width2()) / 2 , 0.292261)
    }
]
CAPACITANCE NEARBODY metal8 metal2 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00762866 * exp(-0.070251 * distance()) + 0.00762839 * exp(-0.0694601 * distance())) * pow((width1() + width2()) / 2 , 0.292261) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00762866 * exp(-0.070251 * distance()) + 0.00762839 * exp(-0.0694601 * distance())) * pow((width1() + width2()) / 2 , 0.292261)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal4
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0247907 * exp(-0.149092 * distance()) + 0.0541496 * exp(-0.946619 * distance())) * pow((width1() + width2()) / 2 , 0.221477) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0247907 * exp(-0.149092 * distance()) + 0.0541496 * exp(-0.946619 * distance())) * pow((width1() + width2()) / 2 , 0.221477)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0247907 * exp(-0.149092 * distance()) + 0.0541496 * exp(-0.946619 * distance())) * pow((width1() + width2()) / 2 , 0.221477) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0247907 * exp(-0.149092 * distance()) + 0.0541496 * exp(-0.946619 * distance())) * pow((width1() + width2()) / 2 , 0.221477)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal5
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0222547 * exp(-0.259912 * distance()) + 0.0222547 * exp(-0.259119 * distance())) * pow((width1() + width2()) / 2 , 0.276221) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0222547 * exp(-0.259912 * distance()) + 0.0222547 * exp(-0.259119 * distance())) * pow((width1() + width2()) / 2 , 0.276221)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0222547 * exp(-0.259912 * distance()) + 0.0222547 * exp(-0.259119 * distance())) * pow((width1() + width2()) / 2 , 0.276221) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0222547 * exp(-0.259912 * distance()) + 0.0222547 * exp(-0.259119 * distance())) * pow((width1() + width2()) / 2 , 0.276221)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal6
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0160696 * exp(-0.170431 * distance()) + 0.0160696 * exp(-0.170465 * distance())) * pow((width1() + width2()) / 2 , 0.288727) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0160696 * exp(-0.170431 * distance()) + 0.0160696 * exp(-0.170465 * distance())) * pow((width1() + width2()) / 2 , 0.288727)
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0160696 * exp(-0.170431 * distance()) + 0.0160696 * exp(-0.170465 * distance())) * pow((width1() + width2()) / 2 , 0.288727) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0160696 * exp(-0.170431 * distance()) + 0.0160696 * exp(-0.170465 * distance())) * pow((width1() + width2()) / 2 , 0.288727)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal7
//
CAPACITANCE NEARBODY metal3 metal7 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0127364 * exp(-0.11987 * distance()) + 0.0127364 * exp(-0.119283 * distance())) * pow((width1() + width2()) / 2 , 0.288061) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0127364 * exp(-0.11987 * distance()) + 0.0127364 * exp(-0.119283 * distance())) * pow((width1() + width2()) / 2 , 0.288061)
    }
]
CAPACITANCE NEARBODY metal7 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0127364 * exp(-0.11987 * distance()) + 0.0127364 * exp(-0.119283 * distance())) * pow((width1() + width2()) / 2 , 0.288061) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0127364 * exp(-0.11987 * distance()) + 0.0127364 * exp(-0.119283 * distance())) * pow((width1() + width2()) / 2 , 0.288061)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal8
//
CAPACITANCE NEARBODY metal3 metal8 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0107265 * exp(-0.0801483 * distance()) + 0.0107265 * exp(-0.0808496 * distance())) * pow((width1() + width2()) / 2 , 0.255933) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0107265 * exp(-0.0801483 * distance()) + 0.0107265 * exp(-0.0808496 * distance())) * pow((width1() + width2()) / 2 , 0.255933)
    }
]
CAPACITANCE NEARBODY metal8 metal3 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0107265 * exp(-0.0801483 * distance()) + 0.0107265 * exp(-0.0808496 * distance())) * pow((width1() + width2()) / 2 , 0.255933) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0107265 * exp(-0.0801483 * distance()) + 0.0107265 * exp(-0.0808496 * distance())) * pow((width1() + width2()) / 2 , 0.255933)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal5
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0528139 * exp(-0.957649 * distance()) + 0.0293476 * exp(-0.132826 * distance())) * pow((width1() + width2()) / 2 , 0.220111) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0528139 * exp(-0.957649 * distance()) + 0.0293476 * exp(-0.132826 * distance())) * pow((width1() + width2()) / 2 , 0.220111)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0528139 * exp(-0.957649 * distance()) + 0.0293476 * exp(-0.132826 * distance())) * pow((width1() + width2()) / 2 , 0.220111) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0528139 * exp(-0.957649 * distance()) + 0.0293476 * exp(-0.132826 * distance())) * pow((width1() + width2()) / 2 , 0.220111)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal6
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0236994 * exp(-0.229959 * distance()) + 0.0236994 * exp(-0.230244 * distance())) * pow((width1() + width2()) / 2 , 0.265554) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0236994 * exp(-0.229959 * distance()) + 0.0236994 * exp(-0.230244 * distance())) * pow((width1() + width2()) / 2 , 0.265554)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0236994 * exp(-0.229959 * distance()) + 0.0236994 * exp(-0.230244 * distance())) * pow((width1() + width2()) / 2 , 0.265554) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0236994 * exp(-0.229959 * distance()) + 0.0236994 * exp(-0.230244 * distance())) * pow((width1() + width2()) / 2 , 0.265554)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal7
//
CAPACITANCE NEARBODY metal4 metal7 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0175782 * exp(-0.150949 * distance()) + 0.0175717 * exp(-0.150946 * distance())) * pow((width1() + width2()) / 2 , 0.270844) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0175782 * exp(-0.150949 * distance()) + 0.0175717 * exp(-0.150946 * distance())) * pow((width1() + width2()) / 2 , 0.270844)
    }
]
CAPACITANCE NEARBODY metal7 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0175782 * exp(-0.150949 * distance()) + 0.0175717 * exp(-0.150946 * distance())) * pow((width1() + width2()) / 2 , 0.270844) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0175782 * exp(-0.150949 * distance()) + 0.0175717 * exp(-0.150946 * distance())) * pow((width1() + width2()) / 2 , 0.270844)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal8
//
CAPACITANCE NEARBODY metal4 metal8 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0142501 * exp(-0.0981696 * distance()) + 0.0142501 * exp(-0.0977777 * distance())) * pow((width1() + width2()) / 2 , 0.239132) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0142501 * exp(-0.0981696 * distance()) + 0.0142501 * exp(-0.0977777 * distance())) * pow((width1() + width2()) / 2 , 0.239132)
    }
]
CAPACITANCE NEARBODY metal8 metal4 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0142501 * exp(-0.0981696 * distance()) + 0.0142501 * exp(-0.0977777 * distance())) * pow((width1() + width2()) / 2 , 0.239132) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0142501 * exp(-0.0981696 * distance()) + 0.0142501 * exp(-0.0977777 * distance())) * pow((width1() + width2()) / 2 , 0.239132)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal5,metal6
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0521063 * exp(-0.965456 * distance()) + 0.0323982 * exp(-0.119859 * distance())) * pow((width1() + width2()) / 2 , 0.219075) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0521063 * exp(-0.965456 * distance()) + 0.0323982 * exp(-0.119859 * distance())) * pow((width1() + width2()) / 2 , 0.219075)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0521063 * exp(-0.965456 * distance()) + 0.0323982 * exp(-0.119859 * distance())) * pow((width1() + width2()) / 2 , 0.219075) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0521063 * exp(-0.965456 * distance()) + 0.0323982 * exp(-0.119859 * distance())) * pow((width1() + width2()) / 2 , 0.219075)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal5,metal7
//
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0249374 * exp(-0.210313 * distance()) + 0.0249374 * exp(-0.210324 * distance())) * pow((width1() + width2()) / 2 , 0.256531) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0249374 * exp(-0.210313 * distance()) + 0.0249374 * exp(-0.210324 * distance())) * pow((width1() + width2()) / 2 , 0.256531)
    }
]
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0249374 * exp(-0.210313 * distance()) + 0.0249374 * exp(-0.210324 * distance())) * pow((width1() + width2()) / 2 , 0.256531) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0249374 * exp(-0.210313 * distance()) + 0.0249374 * exp(-0.210324 * distance())) * pow((width1() + width2()) / 2 , 0.256531)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal5,metal8
//
CAPACITANCE NEARBODY metal5 metal8 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0187765 * exp(-0.126376 * distance()) + 0.0187764 * exp(-0.126309 * distance())) * pow((width1() + width2()) / 2 , 0.230469) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0187765 * exp(-0.126376 * distance()) + 0.0187764 * exp(-0.126309 * distance())) * pow((width1() + width2()) / 2 , 0.230469)
    }
]
CAPACITANCE NEARBODY metal8 metal5 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0187765 * exp(-0.126376 * distance()) + 0.0187764 * exp(-0.126309 * distance())) * pow((width1() + width2()) / 2 , 0.230469) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0187765 * exp(-0.126376 * distance()) + 0.0187764 * exp(-0.126309 * distance())) * pow((width1() + width2()) / 2 , 0.230469)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal6,metal7
//
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0514613 * exp(-0.965138 * distance()) + 0.035411 * exp(-0.113621 * distance())) * pow((width1() + width2()) / 2 , 0.217656) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0514613 * exp(-0.965138 * distance()) + 0.035411 * exp(-0.113621 * distance())) * pow((width1() + width2()) / 2 , 0.217656)
    }
]
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0514613 * exp(-0.965138 * distance()) + 0.035411 * exp(-0.113621 * distance())) * pow((width1() + width2()) / 2 , 0.217656) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0514613 * exp(-0.965138 * distance()) + 0.035411 * exp(-0.113621 * distance())) * pow((width1() + width2()) / 2 , 0.217656)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal6,metal8
//
CAPACITANCE NEARBODY metal6 metal8 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0256535 * exp(-0.176133 * distance()) + 0.0256535 * exp(-0.176134 * distance())) * pow((width1() + width2()) / 2 , 0.224898) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0256535 * exp(-0.176133 * distance()) + 0.0256535 * exp(-0.176134 * distance())) * pow((width1() + width2()) / 2 , 0.224898)
    }
]
CAPACITANCE NEARBODY metal8 metal6 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0256535 * exp(-0.176133 * distance()) + 0.0256535 * exp(-0.176134 * distance())) * pow((width1() + width2()) / 2 , 0.224898) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0256535 * exp(-0.176133 * distance()) + 0.0256535 * exp(-0.176134 * distance())) * pow((width1() + width2()) / 2 , 0.224898)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,poly,metal7,metal8
//
CAPACITANCE NEARBODY metal7 metal8 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0608744 * exp(-0.54212 * distance()) + 0.0218573 * exp(-0.0149668 * distance())) * pow((width1() + width2()) / 2 , 0.207453) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0608744 * exp(-0.54212 * distance()) + 0.0218573 * exp(-0.0149668 * distance())) * pow((width1() + width2()) / 2 , 0.207453)
    }
]
CAPACITANCE NEARBODY metal8 metal7 INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0608744 * exp(-0.54212 * distance()) + 0.0218573 * exp(-0.0149668 * distance())) * pow((width1() + width2()) / 2 , 0.207453) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0608744 * exp(-0.54212 * distance()) + 0.0218573 * exp(-0.0149668 * distance())) * pow((width1() + width2()) / 2 , 0.207453)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal3
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0334424 * exp(-0.71772 * distance()) + 0.0334424 * exp(-0.717721 * distance())) * pow((width1() + width2()) / 2 , 0.234098) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0334424 * exp(-0.71772 * distance()) + 0.0334424 * exp(-0.717721 * distance())) * pow((width1() + width2()) / 2 , 0.234098)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0334424 * exp(-0.71772 * distance()) + 0.0334424 * exp(-0.717721 * distance())) * pow((width1() + width2()) / 2 , 0.234098) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0334424 * exp(-0.71772 * distance()) + 0.0334424 * exp(-0.717721 * distance())) * pow((width1() + width2()) / 2 , 0.234098)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal4
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (1.43695e-06 * exp(1.36862 * distance()) + 0.0359418 * exp(-0.369661 * distance())) * pow((width1() + width2()) / 2 , 0.32944) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (1.43695e-06 * exp(1.36862 * distance()) + 0.0359418 * exp(-0.369661 * distance())) * pow((width1() + width2()) / 2 , 0.32944)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (1.43695e-06 * exp(1.36862 * distance()) + 0.0359418 * exp(-0.369661 * distance())) * pow((width1() + width2()) / 2 , 0.32944) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (1.43695e-06 * exp(1.36862 * distance()) + 0.0359418 * exp(-0.369661 * distance())) * pow((width1() + width2()) / 2 , 0.32944)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal5
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0122047 * exp(-0.230707 * distance()) + 0.0122047 * exp(-0.234933 * distance())) * pow((width1() + width2()) / 2 , 0.361996) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0122047 * exp(-0.230707 * distance()) + 0.0122047 * exp(-0.234933 * distance())) * pow((width1() + width2()) / 2 , 0.361996)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0122047 * exp(-0.230707 * distance()) + 0.0122047 * exp(-0.234933 * distance())) * pow((width1() + width2()) / 2 , 0.361996) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0122047 * exp(-0.230707 * distance()) + 0.0122047 * exp(-0.234933 * distance())) * pow((width1() + width2()) / 2 , 0.361996)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal6
//
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00919479 * exp(-0.161883 * distance()) + 0.00919479 * exp(-0.161553 * distance())) * pow((width1() + width2()) / 2 , 0.374123) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00919479 * exp(-0.161883 * distance()) + 0.00919479 * exp(-0.161553 * distance())) * pow((width1() + width2()) / 2 , 0.374123)
    }
]
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00919479 * exp(-0.161883 * distance()) + 0.00919479 * exp(-0.161553 * distance())) * pow((width1() + width2()) / 2 , 0.374123) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00919479 * exp(-0.161883 * distance()) + 0.00919479 * exp(-0.161553 * distance())) * pow((width1() + width2()) / 2 , 0.374123)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal7
//
CAPACITANCE NEARBODY metal2 metal7 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00740242 * exp(-0.11641 * distance()) + 0.00740243 * exp(-0.1161 * distance())) * pow((width1() + width2()) / 2 , 0.375318) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00740242 * exp(-0.11641 * distance()) + 0.00740243 * exp(-0.1161 * distance())) * pow((width1() + width2()) / 2 , 0.375318)
    }
]
CAPACITANCE NEARBODY metal7 metal2 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00740242 * exp(-0.11641 * distance()) + 0.00740243 * exp(-0.1161 * distance())) * pow((width1() + width2()) / 2 , 0.375318) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00740242 * exp(-0.11641 * distance()) + 0.00740243 * exp(-0.1161 * distance())) * pow((width1() + width2()) / 2 , 0.375318)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal8
//
CAPACITANCE NEARBODY metal2 metal8 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00627173 * exp(-0.0790953 * distance()) + 0.00627175 * exp(-0.0787723 * distance())) * pow((width1() + width2()) / 2 , 0.345329) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00627173 * exp(-0.0790953 * distance()) + 0.00627175 * exp(-0.0787723 * distance())) * pow((width1() + width2()) / 2 , 0.345329)
    }
]
CAPACITANCE NEARBODY metal8 metal2 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00627173 * exp(-0.0790953 * distance()) + 0.00627175 * exp(-0.0787723 * distance())) * pow((width1() + width2()) / 2 , 0.345329) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00627173 * exp(-0.0790953 * distance()) + 0.00627175 * exp(-0.0787723 * distance())) * pow((width1() + width2()) / 2 , 0.345329)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal4
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0202543 * exp(-0.155956 * distance()) + 0.0557844 * exp(-0.938679 * distance())) * pow((width1() + width2()) / 2 , 0.22314) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0202543 * exp(-0.155956 * distance()) + 0.0557844 * exp(-0.938679 * distance())) * pow((width1() + width2()) / 2 , 0.22314)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0202543 * exp(-0.155956 * distance()) + 0.0557844 * exp(-0.938679 * distance())) * pow((width1() + width2()) / 2 , 0.22314) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0202543 * exp(-0.155956 * distance()) + 0.0557844 * exp(-0.938679 * distance())) * pow((width1() + width2()) / 2 , 0.22314)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal5
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0422166 * exp(-0.299042 * distance()) + 5.63253e-06 * exp(1.15653 * distance())) * pow((width1() + width2()) / 2 , 0.287655) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0422166 * exp(-0.299042 * distance()) + 5.63253e-06 * exp(1.15653 * distance())) * pow((width1() + width2()) / 2 , 0.287655)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0422166 * exp(-0.299042 * distance()) + 5.63253e-06 * exp(1.15653 * distance())) * pow((width1() + width2()) / 2 , 0.287655) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0422166 * exp(-0.299042 * distance()) + 5.63253e-06 * exp(1.15653 * distance())) * pow((width1() + width2()) / 2 , 0.287655)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal6
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0149232 * exp(-0.188262 * distance()) + 0.0149232 * exp(-0.188274 * distance())) * pow((width1() + width2()) / 2 , 0.30501) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0149232 * exp(-0.188262 * distance()) + 0.0149232 * exp(-0.188274 * distance())) * pow((width1() + width2()) / 2 , 0.30501)
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0149232 * exp(-0.188262 * distance()) + 0.0149232 * exp(-0.188274 * distance())) * pow((width1() + width2()) / 2 , 0.30501) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0149232 * exp(-0.188262 * distance()) + 0.0149232 * exp(-0.188274 * distance())) * pow((width1() + width2()) / 2 , 0.30501)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal7
//
CAPACITANCE NEARBODY metal3 metal7 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0116649 * exp(-0.131799 * distance()) + 0.0116649 * exp(-0.131735 * distance())) * pow((width1() + width2()) / 2 , 0.306945) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0116649 * exp(-0.131799 * distance()) + 0.0116649 * exp(-0.131735 * distance())) * pow((width1() + width2()) / 2 , 0.306945)
    }
]
CAPACITANCE NEARBODY metal7 metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0116649 * exp(-0.131799 * distance()) + 0.0116649 * exp(-0.131735 * distance())) * pow((width1() + width2()) / 2 , 0.306945) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0116649 * exp(-0.131799 * distance()) + 0.0116649 * exp(-0.131735 * distance())) * pow((width1() + width2()) / 2 , 0.306945)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal8
//
CAPACITANCE NEARBODY metal3 metal8 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00969946 * exp(-0.0880709 * distance()) + 0.00969946 * exp(-0.0880709 * distance())) * pow((width1() + width2()) / 2 , 0.276036) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00969946 * exp(-0.0880709 * distance()) + 0.00969946 * exp(-0.0880709 * distance())) * pow((width1() + width2()) / 2 , 0.276036)
    }
]
CAPACITANCE NEARBODY metal8 metal3 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00969946 * exp(-0.0880709 * distance()) + 0.00969946 * exp(-0.0880709 * distance())) * pow((width1() + width2()) / 2 , 0.276036) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00969946 * exp(-0.0880709 * distance()) + 0.00969946 * exp(-0.0880709 * distance())) * pow((width1() + width2()) / 2 , 0.276036)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal5
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.053286 * exp(-0.954059 * distance()) + 0.0270114 * exp(-0.145086 * distance())) * pow((width1() + width2()) / 2 , 0.220983) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.053286 * exp(-0.954059 * distance()) + 0.0270114 * exp(-0.145086 * distance())) * pow((width1() + width2()) / 2 , 0.220983)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.053286 * exp(-0.954059 * distance()) + 0.0270114 * exp(-0.145086 * distance())) * pow((width1() + width2()) / 2 , 0.220983) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.053286 * exp(-0.954059 * distance()) + 0.0270114 * exp(-0.145086 * distance())) * pow((width1() + width2()) / 2 , 0.220983)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal6
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0228645 * exp(-0.247029 * distance()) + 0.0228645 * exp(-0.246986 * distance())) * pow((width1() + width2()) / 2 , 0.271685) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0228645 * exp(-0.247029 * distance()) + 0.0228645 * exp(-0.246986 * distance())) * pow((width1() + width2()) / 2 , 0.271685)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0228645 * exp(-0.247029 * distance()) + 0.0228645 * exp(-0.246986 * distance())) * pow((width1() + width2()) / 2 , 0.271685) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0228645 * exp(-0.247029 * distance()) + 0.0228645 * exp(-0.246986 * distance())) * pow((width1() + width2()) / 2 , 0.271685)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal7
//
CAPACITANCE NEARBODY metal4 metal7 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0167524 * exp(-0.162439 * distance()) + 0.0167524 * exp(-0.16179 * distance())) * pow((width1() + width2()) / 2 , 0.279779) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0167524 * exp(-0.162439 * distance()) + 0.0167524 * exp(-0.16179 * distance())) * pow((width1() + width2()) / 2 , 0.279779)
    }
]
CAPACITANCE NEARBODY metal7 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0167524 * exp(-0.162439 * distance()) + 0.0167524 * exp(-0.16179 * distance())) * pow((width1() + width2()) / 2 , 0.279779) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0167524 * exp(-0.162439 * distance()) + 0.0167524 * exp(-0.16179 * distance())) * pow((width1() + width2()) / 2 , 0.279779)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal8
//
CAPACITANCE NEARBODY metal4 metal8 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0134344 * exp(-0.105111 * distance()) + 0.0134344 * exp(-0.104511 * distance())) * pow((width1() + width2()) / 2 , 0.249751) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0134344 * exp(-0.105111 * distance()) + 0.0134344 * exp(-0.104511 * distance())) * pow((width1() + width2()) / 2 , 0.249751)
    }
]
CAPACITANCE NEARBODY metal8 metal4 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0134344 * exp(-0.105111 * distance()) + 0.0134344 * exp(-0.104511 * distance())) * pow((width1() + width2()) / 2 , 0.249751) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0134344 * exp(-0.105111 * distance()) + 0.0134344 * exp(-0.104511 * distance())) * pow((width1() + width2()) / 2 , 0.249751)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal5,metal6
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0310483 * exp(-0.130586 * distance()) + 0.0521133 * exp(-0.965477 * distance())) * pow((width1() + width2()) / 2 , 0.2199) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0310483 * exp(-0.130586 * distance()) + 0.0521133 * exp(-0.965477 * distance())) * pow((width1() + width2()) / 2 , 0.2199)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0310483 * exp(-0.130586 * distance()) + 0.0521133 * exp(-0.965477 * distance())) * pow((width1() + width2()) / 2 , 0.2199) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0310483 * exp(-0.130586 * distance()) + 0.0521133 * exp(-0.965477 * distance())) * pow((width1() + width2()) / 2 , 0.2199)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal5,metal7
//
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0243107 * exp(-0.221485 * distance()) + 0.0243106 * exp(-0.221357 * distance())) * pow((width1() + width2()) / 2 , 0.260928) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0243107 * exp(-0.221485 * distance()) + 0.0243106 * exp(-0.221357 * distance())) * pow((width1() + width2()) / 2 , 0.260928)
    }
]
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0243107 * exp(-0.221485 * distance()) + 0.0243106 * exp(-0.221357 * distance())) * pow((width1() + width2()) / 2 , 0.260928) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0243107 * exp(-0.221485 * distance()) + 0.0243106 * exp(-0.221357 * distance())) * pow((width1() + width2()) / 2 , 0.260928)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal5,metal8
//
CAPACITANCE NEARBODY metal5 metal8 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0181191 * exp(-0.134267 * distance()) + 0.0181196 * exp(-0.131204 * distance())) * pow((width1() + width2()) / 2 , 0.236842) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0181191 * exp(-0.134267 * distance()) + 0.0181196 * exp(-0.131204 * distance())) * pow((width1() + width2()) / 2 , 0.236842)
    }
]
CAPACITANCE NEARBODY metal8 metal5 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0181191 * exp(-0.134267 * distance()) + 0.0181196 * exp(-0.131204 * distance())) * pow((width1() + width2()) / 2 , 0.236842) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0181191 * exp(-0.134267 * distance()) + 0.0181196 * exp(-0.131204 * distance())) * pow((width1() + width2()) / 2 , 0.236842)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal6,metal7
//
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.034583 * exp(-0.122448 * distance()) + 0.0512539 * exp(-0.967422 * distance())) * pow((width1() + width2()) / 2 , 0.218641) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.034583 * exp(-0.122448 * distance()) + 0.0512539 * exp(-0.967422 * distance())) * pow((width1() + width2()) / 2 , 0.218641)
    }
]
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.034583 * exp(-0.122448 * distance()) + 0.0512539 * exp(-0.967422 * distance())) * pow((width1() + width2()) / 2 , 0.218641) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.034583 * exp(-0.122448 * distance()) + 0.0512539 * exp(-0.967422 * distance())) * pow((width1() + width2()) / 2 , 0.218641)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal6,metal8
//
CAPACITANCE NEARBODY metal6 metal8 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0251294 * exp(-0.182627 * distance()) + 0.0251294 * exp(-0.182627 * distance())) * pow((width1() + width2()) / 2 , 0.228617) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0251294 * exp(-0.182627 * distance()) + 0.0251294 * exp(-0.182627 * distance())) * pow((width1() + width2()) / 2 , 0.228617)
    }
]
CAPACITANCE NEARBODY metal8 metal6 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0251294 * exp(-0.182627 * distance()) + 0.0251294 * exp(-0.182627 * distance())) * pow((width1() + width2()) / 2 , 0.228617) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0251294 * exp(-0.182627 * distance()) + 0.0251294 * exp(-0.182627 * distance())) * pow((width1() + width2()) / 2 , 0.228617)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal1,metal7,metal8
//
CAPACITANCE NEARBODY metal7 metal8 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0208458 * exp(-0.0168677 * distance()) + 0.061011 * exp(-0.540494 * distance())) * pow((width1() + width2()) / 2 , 0.209102) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0208458 * exp(-0.0168677 * distance()) + 0.061011 * exp(-0.540494 * distance())) * pow((width1() + width2()) / 2 , 0.209102)
    }
]
CAPACITANCE NEARBODY metal8 metal7 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0208458 * exp(-0.0168677 * distance()) + 0.061011 * exp(-0.540494 * distance())) * pow((width1() + width2()) / 2 , 0.209102) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0208458 * exp(-0.0168677 * distance()) + 0.061011 * exp(-0.540494 * distance())) * pow((width1() + width2()) / 2 , 0.209102)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal4
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0334505 * exp(-0.716568 * distance()) + 0.0334505 * exp(-0.71657 * distance())) * pow((width1() + width2()) / 2 , 0.234272) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0334505 * exp(-0.716568 * distance()) + 0.0334505 * exp(-0.71657 * distance())) * pow((width1() + width2()) / 2 , 0.234272)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0334505 * exp(-0.716568 * distance()) + 0.0334505 * exp(-0.71657 * distance())) * pow((width1() + width2()) / 2 , 0.234272) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0334505 * exp(-0.716568 * distance()) + 0.0334505 * exp(-0.71657 * distance())) * pow((width1() + width2()) / 2 , 0.234272)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal5
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.017923 * exp(-0.358272 * distance()) + 0.0179231 * exp(-0.35826 * distance())) * pow((width1() + width2()) / 2 , 0.329052) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.017923 * exp(-0.358272 * distance()) + 0.0179231 * exp(-0.35826 * distance())) * pow((width1() + width2()) / 2 , 0.329052)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.017923 * exp(-0.358272 * distance()) + 0.0179231 * exp(-0.35826 * distance())) * pow((width1() + width2()) / 2 , 0.329052) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.017923 * exp(-0.358272 * distance()) + 0.0179231 * exp(-0.35826 * distance())) * pow((width1() + width2()) / 2 , 0.329052)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal6
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0122271 * exp(-0.232774 * distance()) + 0.0122271 * exp(-0.231514 * distance())) * pow((width1() + width2()) / 2 , 0.362064) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0122271 * exp(-0.232774 * distance()) + 0.0122271 * exp(-0.231514 * distance())) * pow((width1() + width2()) / 2 , 0.362064)
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0122271 * exp(-0.232774 * distance()) + 0.0122271 * exp(-0.231514 * distance())) * pow((width1() + width2()) / 2 , 0.362064) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0122271 * exp(-0.232774 * distance()) + 0.0122271 * exp(-0.231514 * distance())) * pow((width1() + width2()) / 2 , 0.362064)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal7
//
CAPACITANCE NEARBODY metal3 metal7 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0092691 * exp(-0.161055 * distance()) + 0.00926908 * exp(-0.16074 * distance())) * pow((width1() + width2()) / 2 , 0.371805) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0092691 * exp(-0.161055 * distance()) + 0.00926908 * exp(-0.16074 * distance())) * pow((width1() + width2()) / 2 , 0.371805)
    }
]
CAPACITANCE NEARBODY metal7 metal3 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0092691 * exp(-0.161055 * distance()) + 0.00926908 * exp(-0.16074 * distance())) * pow((width1() + width2()) / 2 , 0.371805) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0092691 * exp(-0.161055 * distance()) + 0.00926908 * exp(-0.16074 * distance())) * pow((width1() + width2()) / 2 , 0.371805)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal8
//
CAPACITANCE NEARBODY metal3 metal8 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00749879 * exp(-0.105811 * distance()) + 0.00749881 * exp(-0.106105 * distance())) * pow((width1() + width2()) / 2 , 0.345063) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00749879 * exp(-0.105811 * distance()) + 0.00749881 * exp(-0.106105 * distance())) * pow((width1() + width2()) / 2 , 0.345063)
    }
]
CAPACITANCE NEARBODY metal8 metal3 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00749879 * exp(-0.105811 * distance()) + 0.00749881 * exp(-0.106105 * distance())) * pow((width1() + width2()) / 2 , 0.345063) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00749879 * exp(-0.105811 * distance()) + 0.00749881 * exp(-0.106105 * distance())) * pow((width1() + width2()) / 2 , 0.345063)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal5
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0554356 * exp(-0.942652 * distance()) + 0.0206506 * exp(-0.159097 * distance())) * pow((width1() + width2()) / 2 , 0.223329) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0554356 * exp(-0.942652 * distance()) + 0.0206506 * exp(-0.159097 * distance())) * pow((width1() + width2()) / 2 , 0.223329)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0554356 * exp(-0.942652 * distance()) + 0.0206506 * exp(-0.159097 * distance())) * pow((width1() + width2()) / 2 , 0.223329) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0554356 * exp(-0.942652 * distance()) + 0.0206506 * exp(-0.159097 * distance())) * pow((width1() + width2()) / 2 , 0.223329)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal6
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0422824 * exp(-0.297987 * distance()) + 6.49488e-06 * exp(1.12166 * distance())) * pow((width1() + width2()) / 2 , 0.287819) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0422824 * exp(-0.297987 * distance()) + 6.49488e-06 * exp(1.12166 * distance())) * pow((width1() + width2()) / 2 , 0.287819)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0422824 * exp(-0.297987 * distance()) + 6.49488e-06 * exp(1.12166 * distance())) * pow((width1() + width2()) / 2 , 0.287819) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0422824 * exp(-0.297987 * distance()) + 6.49488e-06 * exp(1.12166 * distance())) * pow((width1() + width2()) / 2 , 0.287819)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal7
//
CAPACITANCE NEARBODY metal4 metal7 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0150436 * exp(-0.187302 * distance()) + 0.0150436 * exp(-0.187302 * distance())) * pow((width1() + width2()) / 2 , 0.30263) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0150436 * exp(-0.187302 * distance()) + 0.0150436 * exp(-0.187302 * distance())) * pow((width1() + width2()) / 2 , 0.30263)
    }
]
CAPACITANCE NEARBODY metal7 metal4 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0150436 * exp(-0.187302 * distance()) + 0.0150436 * exp(-0.187302 * distance())) * pow((width1() + width2()) / 2 , 0.30263) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0150436 * exp(-0.187302 * distance()) + 0.0150436 * exp(-0.187302 * distance())) * pow((width1() + width2()) / 2 , 0.30263)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal8
//
CAPACITANCE NEARBODY metal4 metal8 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0117864 * exp(-0.119373 * distance()) + 0.0117863 * exp(-0.120448 * distance())) * pow((width1() + width2()) / 2 , 0.276473) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0117864 * exp(-0.119373 * distance()) + 0.0117863 * exp(-0.120448 * distance())) * pow((width1() + width2()) / 2 , 0.276473)
    }
]
CAPACITANCE NEARBODY metal8 metal4 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0117864 * exp(-0.119373 * distance()) + 0.0117863 * exp(-0.120448 * distance())) * pow((width1() + width2()) / 2 , 0.276473) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0117864 * exp(-0.119373 * distance()) + 0.0117863 * exp(-0.120448 * distance())) * pow((width1() + width2()) / 2 , 0.276473)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal5,metal6
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.027766 * exp(-0.150045 * distance()) + 0.0526362 * exp(-0.961352 * distance())) * pow((width1() + width2()) / 2 , 0.221362) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.027766 * exp(-0.150045 * distance()) + 0.0526362 * exp(-0.961352 * distance())) * pow((width1() + width2()) / 2 , 0.221362)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.027766 * exp(-0.150045 * distance()) + 0.0526362 * exp(-0.961352 * distance())) * pow((width1() + width2()) / 2 , 0.221362) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.027766 * exp(-0.150045 * distance()) + 0.0526362 * exp(-0.961352 * distance())) * pow((width1() + width2()) / 2 , 0.221362)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal5,metal7
//
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0230584 * exp(-0.245392 * distance()) + 0.0230584 * exp(-0.245362 * distance())) * pow((width1() + width2()) / 2 , 0.270247) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0230584 * exp(-0.245392 * distance()) + 0.0230584 * exp(-0.245362 * distance())) * pow((width1() + width2()) / 2 , 0.270247)
    }
]
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0230584 * exp(-0.245392 * distance()) + 0.0230584 * exp(-0.245362 * distance())) * pow((width1() + width2()) / 2 , 0.270247) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0230584 * exp(-0.245392 * distance()) + 0.0230584 * exp(-0.245362 * distance())) * pow((width1() + width2()) / 2 , 0.270247)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal5,metal8
//
CAPACITANCE NEARBODY metal5 metal8 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0168366 * exp(-0.146162 * distance()) + 0.0168367 * exp(-0.14662 * distance())) * pow((width1() + width2()) / 2 , 0.250559) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0168366 * exp(-0.146162 * distance()) + 0.0168367 * exp(-0.14662 * distance())) * pow((width1() + width2()) / 2 , 0.250559)
    }
]
CAPACITANCE NEARBODY metal8 metal5 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0168366 * exp(-0.146162 * distance()) + 0.0168367 * exp(-0.14662 * distance())) * pow((width1() + width2()) / 2 , 0.250559) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0168366 * exp(-0.146162 * distance()) + 0.0168367 * exp(-0.14662 * distance())) * pow((width1() + width2()) / 2 , 0.250559)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal6,metal7
//
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0510761 * exp(-0.969319 * distance()) + 0.0327489 * exp(-0.139361 * distance())) * pow((width1() + width2()) / 2 , 0.220293) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0510761 * exp(-0.969319 * distance()) + 0.0327489 * exp(-0.139361 * distance())) * pow((width1() + width2()) / 2 , 0.220293)
    }
]
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0510761 * exp(-0.969319 * distance()) + 0.0327489 * exp(-0.139361 * distance())) * pow((width1() + width2()) / 2 , 0.220293) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0510761 * exp(-0.969319 * distance()) + 0.0327489 * exp(-0.139361 * distance())) * pow((width1() + width2()) / 2 , 0.220293)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal6,metal8
//
CAPACITANCE NEARBODY metal6 metal8 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0241327 * exp(-0.19586 * distance()) + 0.0241327 * exp(-0.195872 * distance())) * pow((width1() + width2()) / 2 , 0.236104) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0241327 * exp(-0.19586 * distance()) + 0.0241327 * exp(-0.195872 * distance())) * pow((width1() + width2()) / 2 , 0.236104)
    }
]
CAPACITANCE NEARBODY metal8 metal6 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0241327 * exp(-0.19586 * distance()) + 0.0241327 * exp(-0.195872 * distance())) * pow((width1() + width2()) / 2 , 0.236104) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0241327 * exp(-0.19586 * distance()) + 0.0241327 * exp(-0.195872 * distance())) * pow((width1() + width2()) / 2 , 0.236104)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal2,metal7,metal8
//
CAPACITANCE NEARBODY metal7 metal8 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0191501 * exp(-0.0221026 * distance()) + 0.0610742 * exp(-0.539471 * distance())) * pow((width1() + width2()) / 2 , 0.212081) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0191501 * exp(-0.0221026 * distance()) + 0.0610742 * exp(-0.539471 * distance())) * pow((width1() + width2()) / 2 , 0.212081)
    }
]
CAPACITANCE NEARBODY metal8 metal7 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0191501 * exp(-0.0221026 * distance()) + 0.0610742 * exp(-0.539471 * distance())) * pow((width1() + width2()) / 2 , 0.212081) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0191501 * exp(-0.0221026 * distance()) + 0.0610742 * exp(-0.539471 * distance())) * pow((width1() + width2()) / 2 , 0.212081)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal5
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0334645 * exp(-0.714466 * distance()) + 0.0334645 * exp(-0.714465 * distance())) * pow((width1() + width2()) / 2 , 0.234612) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0334645 * exp(-0.714466 * distance()) + 0.0334645 * exp(-0.714465 * distance())) * pow((width1() + width2()) / 2 , 0.234612)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0334645 * exp(-0.714466 * distance()) + 0.0334645 * exp(-0.714465 * distance())) * pow((width1() + width2()) / 2 , 0.234612) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0334645 * exp(-0.714466 * distance()) + 0.0334645 * exp(-0.714465 * distance())) * pow((width1() + width2()) / 2 , 0.234612)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal6
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (1.59715e-06 * exp(1.33915 * distance()) + 0.0360237 * exp(-0.366646 * distance())) * pow((width1() + width2()) / 2 , 0.330055) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (1.59715e-06 * exp(1.33915 * distance()) + 0.0360237 * exp(-0.366646 * distance())) * pow((width1() + width2()) / 2 , 0.330055)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (1.59715e-06 * exp(1.33915 * distance()) + 0.0360237 * exp(-0.366646 * distance())) * pow((width1() + width2()) / 2 , 0.330055) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (1.59715e-06 * exp(1.33915 * distance()) + 0.0360237 * exp(-0.366646 * distance())) * pow((width1() + width2()) / 2 , 0.330055)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal7
//
CAPACITANCE NEARBODY metal4 metal7 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0123302 * exp(-0.229744 * distance()) + 0.0123302 * exp(-0.229876 * distance())) * pow((width1() + width2()) / 2 , 0.360476) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0123302 * exp(-0.229744 * distance()) + 0.0123302 * exp(-0.229876 * distance())) * pow((width1() + width2()) / 2 , 0.360476)
    }
]
CAPACITANCE NEARBODY metal7 metal4 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0123302 * exp(-0.229744 * distance()) + 0.0123302 * exp(-0.229876 * distance())) * pow((width1() + width2()) / 2 , 0.360476) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0123302 * exp(-0.229744 * distance()) + 0.0123302 * exp(-0.229876 * distance())) * pow((width1() + width2()) / 2 , 0.360476)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal8
//
CAPACITANCE NEARBODY metal4 metal8 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00932579 * exp(-0.145078 * distance()) + 0.00932578 * exp(-0.143652 * distance())) * pow((width1() + width2()) / 2 , 0.342412) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00932579 * exp(-0.145078 * distance()) + 0.00932578 * exp(-0.143652 * distance())) * pow((width1() + width2()) / 2 , 0.342412)
    }
]
CAPACITANCE NEARBODY metal8 metal4 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00932579 * exp(-0.145078 * distance()) + 0.00932578 * exp(-0.143652 * distance())) * pow((width1() + width2()) / 2 , 0.342412) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00932579 * exp(-0.145078 * distance()) + 0.00932578 * exp(-0.143652 * distance())) * pow((width1() + width2()) / 2 , 0.342412)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,metal5,metal6
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0213628 * exp(-0.16419 * distance()) + 0.0548585 * exp(-0.94872 * distance())) * pow((width1() + width2()) / 2 , 0.224471) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0213628 * exp(-0.16419 * distance()) + 0.0548585 * exp(-0.94872 * distance())) * pow((width1() + width2()) / 2 , 0.224471)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0213628 * exp(-0.16419 * distance()) + 0.0548585 * exp(-0.94872 * distance())) * pow((width1() + width2()) / 2 , 0.224471) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0213628 * exp(-0.16419 * distance()) + 0.0548585 * exp(-0.94872 * distance())) * pow((width1() + width2()) / 2 , 0.224471)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,metal5,metal7
//
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0212392 * exp(-0.283118 * distance()) + 0.0212392 * exp(-0.283092 * distance())) * pow((width1() + width2()) / 2 , 0.286325) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0212392 * exp(-0.283118 * distance()) + 0.0212392 * exp(-0.283092 * distance())) * pow((width1() + width2()) / 2 , 0.286325)
    }
]
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0212392 * exp(-0.283118 * distance()) + 0.0212392 * exp(-0.283092 * distance())) * pow((width1() + width2()) / 2 , 0.286325) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0212392 * exp(-0.283118 * distance()) + 0.0212392 * exp(-0.283092 * distance())) * pow((width1() + width2()) / 2 , 0.286325)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,metal5,metal8
//
CAPACITANCE NEARBODY metal5 metal8 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0150616 * exp(-0.167169 * distance()) + 0.0150616 * exp(-0.167169 * distance())) * pow((width1() + width2()) / 2 , 0.274284) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0150616 * exp(-0.167169 * distance()) + 0.0150616 * exp(-0.167169 * distance())) * pow((width1() + width2()) / 2 , 0.274284)
    }
]
CAPACITANCE NEARBODY metal8 metal5 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0150616 * exp(-0.167169 * distance()) + 0.0150616 * exp(-0.167169 * distance())) * pow((width1() + width2()) / 2 , 0.274284) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0150616 * exp(-0.167169 * distance()) + 0.0150616 * exp(-0.167169 * distance())) * pow((width1() + width2()) / 2 , 0.274284)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,metal6,metal7
//
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0297671 * exp(-0.161015 * distance()) + 0.0513002 * exp(-0.968665 * distance())) * pow((width1() + width2()) / 2 , 0.222212) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0297671 * exp(-0.161015 * distance()) + 0.0513002 * exp(-0.968665 * distance())) * pow((width1() + width2()) / 2 , 0.222212)
    }
]
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0297671 * exp(-0.161015 * distance()) + 0.0513002 * exp(-0.968665 * distance())) * pow((width1() + width2()) / 2 , 0.222212) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0297671 * exp(-0.161015 * distance()) + 0.0513002 * exp(-0.968665 * distance())) * pow((width1() + width2()) / 2 , 0.222212)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,metal6,metal8
//
CAPACITANCE NEARBODY metal6 metal8 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.022813 * exp(-0.214899 * distance()) + 0.022813 * exp(-0.214921 * distance())) * pow((width1() + width2()) / 2 , 0.246907) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.022813 * exp(-0.214899 * distance()) + 0.022813 * exp(-0.214921 * distance())) * pow((width1() + width2()) / 2 , 0.246907)
    }
]
CAPACITANCE NEARBODY metal8 metal6 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.022813 * exp(-0.214899 * distance()) + 0.022813 * exp(-0.214921 * distance())) * pow((width1() + width2()) / 2 , 0.246907) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.022813 * exp(-0.214899 * distance()) + 0.022813 * exp(-0.214921 * distance())) * pow((width1() + width2()) / 2 , 0.246907)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal3,metal7,metal8
//
CAPACITANCE NEARBODY metal7 metal8 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0166267 * exp(-0.0255026 * distance()) + 0.0615136 * exp(-0.536771 * distance())) * pow((width1() + width2()) / 2 , 0.215927) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0166267 * exp(-0.0255026 * distance()) + 0.0615136 * exp(-0.536771 * distance())) * pow((width1() + width2()) / 2 , 0.215927)
    }
]
CAPACITANCE NEARBODY metal8 metal7 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0166267 * exp(-0.0255026 * distance()) + 0.0615136 * exp(-0.536771 * distance())) * pow((width1() + width2()) / 2 , 0.215927) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0166267 * exp(-0.0255026 * distance()) + 0.0615136 * exp(-0.536771 * distance())) * pow((width1() + width2()) / 2 , 0.215927)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal4,metal5,metal6
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0334963 * exp(-0.709917 * distance()) + 0.0334963 * exp(-0.709916 * distance())) * pow((width1() + width2()) / 2 , 0.235502) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0334963 * exp(-0.709917 * distance()) + 0.0334963 * exp(-0.709916 * distance())) * pow((width1() + width2()) / 2 , 0.235502)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0334963 * exp(-0.709917 * distance()) + 0.0334963 * exp(-0.709916 * distance())) * pow((width1() + width2()) / 2 , 0.235502) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0334963 * exp(-0.709917 * distance()) + 0.0334963 * exp(-0.709916 * distance())) * pow((width1() + width2()) / 2 , 0.235502)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal4,metal5,metal7
//
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0181086 * exp(-0.351035 * distance()) + 0.0181086 * exp(-0.350854 * distance())) * pow((width1() + width2()) / 2 , 0.329878) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0181086 * exp(-0.351035 * distance()) + 0.0181086 * exp(-0.350854 * distance())) * pow((width1() + width2()) / 2 , 0.329878)
    }
]
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0181086 * exp(-0.351035 * distance()) + 0.0181086 * exp(-0.350854 * distance())) * pow((width1() + width2()) / 2 , 0.329878) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0181086 * exp(-0.351035 * distance()) + 0.0181086 * exp(-0.350854 * distance())) * pow((width1() + width2()) / 2 , 0.329878)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal4,metal5,metal8
//
CAPACITANCE NEARBODY metal5 metal8 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0122653 * exp(-0.201732 * distance()) + 0.0122653 * exp(-0.201658 * distance())) * pow((width1() + width2()) / 2 , 0.334986) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0122653 * exp(-0.201732 * distance()) + 0.0122653 * exp(-0.201658 * distance())) * pow((width1() + width2()) / 2 , 0.334986)
    }
]
CAPACITANCE NEARBODY metal8 metal5 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0122653 * exp(-0.201732 * distance()) + 0.0122653 * exp(-0.201658 * distance())) * pow((width1() + width2()) / 2 , 0.334986) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0122653 * exp(-0.201732 * distance()) + 0.0122653 * exp(-0.201658 * distance())) * pow((width1() + width2()) / 2 , 0.334986)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal4,metal6,metal7
//
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0530914 * exp(-0.959783 * distance()) + 0.0237509 * exp(-0.181002 * distance())) * pow((width1() + width2()) / 2 , 0.225541) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0530914 * exp(-0.959783 * distance()) + 0.0237509 * exp(-0.181002 * distance())) * pow((width1() + width2()) / 2 , 0.225541)
    }
]
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0530914 * exp(-0.959783 * distance()) + 0.0237509 * exp(-0.181002 * distance())) * pow((width1() + width2()) / 2 , 0.225541) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0530914 * exp(-0.959783 * distance()) + 0.0237509 * exp(-0.181002 * distance())) * pow((width1() + width2()) / 2 , 0.225541)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal4,metal6,metal8
//
CAPACITANCE NEARBODY metal6 metal8 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0208996 * exp(-0.244435 * distance()) + 0.0208996 * exp(-0.244421 * distance())) * pow((width1() + width2()) / 2 , 0.265869) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0208996 * exp(-0.244435 * distance()) + 0.0208996 * exp(-0.244421 * distance())) * pow((width1() + width2()) / 2 , 0.265869)
    }
]
CAPACITANCE NEARBODY metal8 metal6 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0208996 * exp(-0.244435 * distance()) + 0.0208996 * exp(-0.244421 * distance())) * pow((width1() + width2()) / 2 , 0.265869) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0208996 * exp(-0.244435 * distance()) + 0.0208996 * exp(-0.244421 * distance())) * pow((width1() + width2()) / 2 , 0.265869)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal4,metal7,metal8
//
CAPACITANCE NEARBODY metal7 metal8 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0624041 * exp(-0.533941 * distance()) + 0.0128875 * exp(-0.0233036 * distance())) * pow((width1() + width2()) / 2 , 0.221338) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0624041 * exp(-0.533941 * distance()) + 0.0128875 * exp(-0.0233036 * distance())) * pow((width1() + width2()) / 2 , 0.221338)
    }
]
CAPACITANCE NEARBODY metal8 metal7 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0624041 * exp(-0.533941 * distance()) + 0.0128875 * exp(-0.0233036 * distance())) * pow((width1() + width2()) / 2 , 0.221338) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0624041 * exp(-0.533941 * distance()) + 0.0128875 * exp(-0.0233036 * distance())) * pow((width1() + width2()) / 2 , 0.221338)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal5,metal6,metal7
//
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0337539 * exp(-0.69259 * distance()) + 0.0337539 * exp(-0.692646 * distance())) * pow((width1() + width2()) / 2 , 0.238486) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0337539 * exp(-0.69259 * distance()) + 0.0337539 * exp(-0.692646 * distance())) * pow((width1() + width2()) / 2 , 0.238486)
    }
]
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0337539 * exp(-0.69259 * distance()) + 0.0337539 * exp(-0.692646 * distance())) * pow((width1() + width2()) / 2 , 0.238486) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0337539 * exp(-0.69259 * distance()) + 0.0337539 * exp(-0.692646 * distance())) * pow((width1() + width2()) / 2 , 0.238486)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal5,metal6,metal8
//
CAPACITANCE NEARBODY metal6 metal8 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0176541 * exp(-0.297422 * distance()) + 0.0176541 * exp(-0.295332 * distance())) * pow((width1() + width2()) / 2 , 0.316359) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0176541 * exp(-0.297422 * distance()) + 0.0176541 * exp(-0.295332 * distance())) * pow((width1() + width2()) / 2 , 0.316359)
    }
]
CAPACITANCE NEARBODY metal8 metal6 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0176541 * exp(-0.297422 * distance()) + 0.0176541 * exp(-0.295332 * distance())) * pow((width1() + width2()) / 2 , 0.316359) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0176541 * exp(-0.297422 * distance()) + 0.0176541 * exp(-0.295332 * distance())) * pow((width1() + width2()) / 2 , 0.316359)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal5,metal7,metal8
//
CAPACITANCE NEARBODY metal7 metal8 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00735448 * exp(0.00645815 * distance()) + 0.0636088 * exp(-0.535794 * distance())) * pow((width1() + width2()) / 2 , 0.231114) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00735448 * exp(0.00645815 * distance()) + 0.0636088 * exp(-0.535794 * distance())) * pow((width1() + width2()) / 2 , 0.231114)
    }
]
CAPACITANCE NEARBODY metal8 metal7 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00735448 * exp(0.00645815 * distance()) + 0.0636088 * exp(-0.535794 * distance())) * pow((width1() + width2()) / 2 , 0.231114) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00735448 * exp(0.00645815 * distance()) + 0.0636088 * exp(-0.535794 * distance())) * pow((width1() + width2()) / 2 , 0.231114)
    }
]
//
//  model: 314  target: nearbody2  profile: field  layers: field_base,metal6,metal7,metal8
//
CAPACITANCE NEARBODY metal7 metal8 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0026429 * exp(0.0834185 * distance()) + 0.0603511 * exp(-0.587318 * distance())) * pow((width1() + width2()) / 2 , 0.259827) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0026429 * exp(0.0834185 * distance()) + 0.0603511 * exp(-0.587318 * distance())) * pow((width1() + width2()) / 2 , 0.259827)
    }
]
CAPACITANCE NEARBODY metal8 metal7 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0026429 * exp(0.0834185 * distance()) + 0.0603511 * exp(-0.587318 * distance())) * pow((width1() + width2()) / 2 , 0.259827) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0026429 * exp(0.0834185 * distance()) + 0.0603511 * exp(-0.587318 * distance())) * pow((width1() + width2()) / 2 , 0.259827)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal2,metal3
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF poly metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0267496 * exp(-1.30643 * distance()) + 0.0267496 * exp(-1.30968 * distance())) * pow((width1() + width2()) / 2 , 0.218367) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0267496 * exp(-1.30643 * distance()) + 0.0267496 * exp(-1.30968 * distance())) * pow((width1() + width2()) / 2 , 0.218367)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF poly metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0267496 * exp(-1.30643 * distance()) + 0.0267496 * exp(-1.30968 * distance())) * pow((width1() + width2()) / 2 , 0.218367) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0267496 * exp(-1.30643 * distance()) + 0.0267496 * exp(-1.30968 * distance())) * pow((width1() + width2()) / 2 , 0.218367)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal2,metal4
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0290186 * exp(-1.09858 * distance()) + 0.0290186 * exp(-1.09856 * distance())) * pow((width1() + width2()) / 2 , 0.231814) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0290186 * exp(-1.09858 * distance()) + 0.0290186 * exp(-1.09856 * distance())) * pow((width1() + width2()) / 2 , 0.231814)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0290186 * exp(-1.09858 * distance()) + 0.0290186 * exp(-1.09856 * distance())) * pow((width1() + width2()) / 2 , 0.231814) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0290186 * exp(-1.09858 * distance()) + 0.0290186 * exp(-1.09856 * distance())) * pow((width1() + width2()) / 2 , 0.231814)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal2,metal5
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0298526 * exp(-1.00238 * distance()) + 0.0298397 * exp(-1.01522 * distance())) * pow((width1() + width2()) / 2 , 0.240507) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0298526 * exp(-1.00238 * distance()) + 0.0298397 * exp(-1.01522 * distance())) * pow((width1() + width2()) / 2 , 0.240507)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0298526 * exp(-1.00238 * distance()) + 0.0298397 * exp(-1.01522 * distance())) * pow((width1() + width2()) / 2 , 0.240507) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0298526 * exp(-1.00238 * distance()) + 0.0298397 * exp(-1.01522 * distance())) * pow((width1() + width2()) / 2 , 0.240507)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal2,metal6
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0599486 * exp(-0.988744 * distance()) + 0.000636614 * exp(0.026702 * distance())) * pow((width1() + width2()) / 2 , 0.245457) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0599486 * exp(-0.988744 * distance()) + 0.000636614 * exp(0.026702 * distance())) * pow((width1() + width2()) / 2 , 0.245457)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0599486 * exp(-0.988744 * distance()) + 0.000636614 * exp(0.026702 * distance())) * pow((width1() + width2()) / 2 , 0.245457) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0599486 * exp(-0.988744 * distance()) + 0.000636614 * exp(0.026702 * distance())) * pow((width1() + width2()) / 2 , 0.245457)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal2,metal7
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0594022 * exp(-0.988963 * distance()) + 0.00169806 * exp(-0.0806323 * distance())) * pow((width1() + width2()) / 2 , 0.248445) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0594022 * exp(-0.988963 * distance()) + 0.00169806 * exp(-0.0806323 * distance())) * pow((width1() + width2()) / 2 , 0.248445)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0594022 * exp(-0.988963 * distance()) + 0.00169806 * exp(-0.0806323 * distance())) * pow((width1() + width2()) / 2 , 0.248445) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0594022 * exp(-0.988963 * distance()) + 0.00169806 * exp(-0.0806323 * distance())) * pow((width1() + width2()) / 2 , 0.248445)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal2,metal8
//
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0588095 * exp(-0.992964 * distance()) + 0.00266125 * exp(-0.111839 * distance())) * pow((width1() + width2()) / 2 , 0.250652) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0588095 * exp(-0.992964 * distance()) + 0.00266125 * exp(-0.111839 * distance())) * pow((width1() + width2()) / 2 , 0.250652)
    }
]
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0588095 * exp(-0.992964 * distance()) + 0.00266125 * exp(-0.111839 * distance())) * pow((width1() + width2()) / 2 , 0.250652) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0588095 * exp(-0.992964 * distance()) + 0.00266125 * exp(-0.111839 * distance())) * pow((width1() + width2()) / 2 , 0.250652)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal3,metal4
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0125121 * exp(-0.727427 * distance()) + 0.0125121 * exp(-0.727513 * distance())) * pow((width1() + width2()) / 2 , 0.365726) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0125121 * exp(-0.727427 * distance()) + 0.0125121 * exp(-0.727513 * distance())) * pow((width1() + width2()) / 2 , 0.365726)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0125121 * exp(-0.727427 * distance()) + 0.0125121 * exp(-0.727513 * distance())) * pow((width1() + width2()) / 2 , 0.365726) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0125121 * exp(-0.727427 * distance()) + 0.0125121 * exp(-0.727513 * distance())) * pow((width1() + width2()) / 2 , 0.365726)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal3,metal5
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0140114 * exp(-0.613741 * distance()) + 0.0140113 * exp(-0.614053 * distance())) * pow((width1() + width2()) / 2 , 0.350287) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0140114 * exp(-0.613741 * distance()) + 0.0140113 * exp(-0.614053 * distance())) * pow((width1() + width2()) / 2 , 0.350287)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0140114 * exp(-0.613741 * distance()) + 0.0140113 * exp(-0.614053 * distance())) * pow((width1() + width2()) / 2 , 0.350287) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0140114 * exp(-0.613741 * distance()) + 0.0140113 * exp(-0.614053 * distance())) * pow((width1() + width2()) / 2 , 0.350287)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal3,metal6
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0146455 * exp(-0.554172 * distance()) + 0.0146456 * exp(-0.55453 * distance())) * pow((width1() + width2()) / 2 , 0.349589) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0146455 * exp(-0.554172 * distance()) + 0.0146456 * exp(-0.55453 * distance())) * pow((width1() + width2()) / 2 , 0.349589)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0146455 * exp(-0.554172 * distance()) + 0.0146456 * exp(-0.55453 * distance())) * pow((width1() + width2()) / 2 , 0.349589) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0146455 * exp(-0.554172 * distance()) + 0.0146456 * exp(-0.55453 * distance())) * pow((width1() + width2()) / 2 , 0.349589)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal3,metal7
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.014967 * exp(-0.518013 * distance()) + 0.014967 * exp(-0.518762 * distance())) * pow((width1() + width2()) / 2 , 0.350354) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.014967 * exp(-0.518013 * distance()) + 0.014967 * exp(-0.518762 * distance())) * pow((width1() + width2()) / 2 , 0.350354)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.014967 * exp(-0.518013 * distance()) + 0.014967 * exp(-0.518762 * distance())) * pow((width1() + width2()) / 2 , 0.350354) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.014967 * exp(-0.518013 * distance()) + 0.014967 * exp(-0.518762 * distance())) * pow((width1() + width2()) / 2 , 0.350354)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal3,metal8
//
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0151768 * exp(-0.491605 * distance()) + 0.0151768 * exp(-0.491294 * distance())) * pow((width1() + width2()) / 2 , 0.351354) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0151768 * exp(-0.491605 * distance()) + 0.0151768 * exp(-0.491294 * distance())) * pow((width1() + width2()) / 2 , 0.351354)
    }
]
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0151768 * exp(-0.491605 * distance()) + 0.0151768 * exp(-0.491294 * distance())) * pow((width1() + width2()) / 2 , 0.351354) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0151768 * exp(-0.491605 * distance()) + 0.0151768 * exp(-0.491294 * distance())) * pow((width1() + width2()) / 2 , 0.351354)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal4,metal5
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00752417 * exp(-0.514789 * distance()) + 0.00752611 * exp(-0.51428 * distance())) * pow((width1() + width2()) / 2 , 0.435983) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00752417 * exp(-0.514789 * distance()) + 0.00752611 * exp(-0.51428 * distance())) * pow((width1() + width2()) / 2 , 0.435983)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00752417 * exp(-0.514789 * distance()) + 0.00752611 * exp(-0.51428 * distance())) * pow((width1() + width2()) / 2 , 0.435983) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00752417 * exp(-0.514789 * distance()) + 0.00752611 * exp(-0.51428 * distance())) * pow((width1() + width2()) / 2 , 0.435983)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal4,metal6
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00865845 * exp(-0.436697 * distance()) + 0.00865775 * exp(-0.436354 * distance())) * pow((width1() + width2()) / 2 , 0.402377) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00865845 * exp(-0.436697 * distance()) + 0.00865775 * exp(-0.436354 * distance())) * pow((width1() + width2()) / 2 , 0.402377)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00865845 * exp(-0.436697 * distance()) + 0.00865775 * exp(-0.436354 * distance())) * pow((width1() + width2()) / 2 , 0.402377) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00865845 * exp(-0.436697 * distance()) + 0.00865775 * exp(-0.436354 * distance())) * pow((width1() + width2()) / 2 , 0.402377)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal4,metal7
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00918747 * exp(-0.3917 * distance()) + 0.00918592 * exp(-0.391124 * distance())) * pow((width1() + width2()) / 2 , 0.394146) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00918747 * exp(-0.3917 * distance()) + 0.00918592 * exp(-0.391124 * distance())) * pow((width1() + width2()) / 2 , 0.394146)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00918747 * exp(-0.3917 * distance()) + 0.00918592 * exp(-0.391124 * distance())) * pow((width1() + width2()) / 2 , 0.394146) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00918747 * exp(-0.3917 * distance()) + 0.00918592 * exp(-0.391124 * distance())) * pow((width1() + width2()) / 2 , 0.394146)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal4,metal8
//
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0095182 * exp(-0.357315 * distance()) + 0.00951754 * exp(-0.359635 * distance())) * pow((width1() + width2()) / 2 , 0.390934) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0095182 * exp(-0.357315 * distance()) + 0.00951754 * exp(-0.359635 * distance())) * pow((width1() + width2()) / 2 , 0.390934)
    }
]
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0095182 * exp(-0.357315 * distance()) + 0.00951754 * exp(-0.359635 * distance())) * pow((width1() + width2()) / 2 , 0.390934) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0095182 * exp(-0.357315 * distance()) + 0.00951754 * exp(-0.359635 * distance())) * pow((width1() + width2()) / 2 , 0.390934)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal5,metal6
//
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00501841 * exp(-0.388057 * distance()) + 0.00501846 * exp(-0.38847 * distance())) * pow((width1() + width2()) / 2 , 0.474534) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00501841 * exp(-0.388057 * distance()) + 0.00501846 * exp(-0.38847 * distance())) * pow((width1() + width2()) / 2 , 0.474534)
    }
]
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00501841 * exp(-0.388057 * distance()) + 0.00501846 * exp(-0.38847 * distance())) * pow((width1() + width2()) / 2 , 0.474534) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00501841 * exp(-0.388057 * distance()) + 0.00501846 * exp(-0.38847 * distance())) * pow((width1() + width2()) / 2 , 0.474534)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal5,metal7
//
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0059169 * exp(-0.330894 * distance()) + 0.00591708 * exp(-0.330001 * distance())) * pow((width1() + width2()) / 2 , 0.430164) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0059169 * exp(-0.330894 * distance()) + 0.00591708 * exp(-0.330001 * distance())) * pow((width1() + width2()) / 2 , 0.430164)
    }
]
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0059169 * exp(-0.330894 * distance()) + 0.00591708 * exp(-0.330001 * distance())) * pow((width1() + width2()) / 2 , 0.430164) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0059169 * exp(-0.330894 * distance()) + 0.00591708 * exp(-0.330001 * distance())) * pow((width1() + width2()) / 2 , 0.430164)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal5,metal8
//
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00643208 * exp(-0.284831 * distance()) + 0.0064318 * exp(-0.295037 * distance())) * pow((width1() + width2()) / 2 , 0.415777) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00643208 * exp(-0.284831 * distance()) + 0.0064318 * exp(-0.295037 * distance())) * pow((width1() + width2()) / 2 , 0.415777)
    }
]
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00643208 * exp(-0.284831 * distance()) + 0.0064318 * exp(-0.295037 * distance())) * pow((width1() + width2()) / 2 , 0.415777) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00643208 * exp(-0.284831 * distance()) + 0.0064318 * exp(-0.295037 * distance())) * pow((width1() + width2()) / 2 , 0.415777)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal6,metal7
//
CAPACITANCE NEARBODY metal1 metal6 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00356996 * exp(-0.300254 * distance()) + 0.00357067 * exp(-0.303364 * distance())) * pow((width1() + width2()) / 2 , 0.497996) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00356996 * exp(-0.300254 * distance()) + 0.00357067 * exp(-0.303364 * distance())) * pow((width1() + width2()) / 2 , 0.497996)
    }
]
CAPACITANCE NEARBODY metal6 metal1 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00356996 * exp(-0.300254 * distance()) + 0.00357067 * exp(-0.303364 * distance())) * pow((width1() + width2()) / 2 , 0.497996) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00356996 * exp(-0.300254 * distance()) + 0.00357067 * exp(-0.303364 * distance())) * pow((width1() + width2()) / 2 , 0.497996)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal6,metal8
//
CAPACITANCE NEARBODY metal1 metal6 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00439634 * exp(-0.251429 * distance()) + 0.00439668 * exp(-0.251028 * distance())) * pow((width1() + width2()) / 2 , 0.443263) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00439634 * exp(-0.251429 * distance()) + 0.00439668 * exp(-0.251028 * distance())) * pow((width1() + width2()) / 2 , 0.443263)
    }
]
CAPACITANCE NEARBODY metal6 metal1 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00439634 * exp(-0.251429 * distance()) + 0.00439668 * exp(-0.251028 * distance())) * pow((width1() + width2()) / 2 , 0.443263) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00439634 * exp(-0.251429 * distance()) + 0.00439668 * exp(-0.251028 * distance())) * pow((width1() + width2()) / 2 , 0.443263)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal1,metal7,metal8
//
CAPACITANCE NEARBODY metal1 metal7 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00282626 * exp(-0.230478 * distance()) + 0.00282629 * exp(-0.230428 * distance())) * pow((width1() + width2()) / 2 , 0.495394) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00282626 * exp(-0.230478 * distance()) + 0.00282629 * exp(-0.230428 * distance())) * pow((width1() + width2()) / 2 , 0.495394)
    }
]
CAPACITANCE NEARBODY metal7 metal1 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00282626 * exp(-0.230478 * distance()) + 0.00282629 * exp(-0.230428 * distance())) * pow((width1() + width2()) / 2 , 0.495394) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00282626 * exp(-0.230478 * distance()) + 0.00282629 * exp(-0.230428 * distance())) * pow((width1() + width2()) / 2 , 0.495394)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal3,metal4
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0303745 * exp(-1.05728 * distance()) + 0.0303747 * exp(-1.05859 * distance())) * pow((width1() + width2()) / 2 , 0.199992) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0303745 * exp(-1.05728 * distance()) + 0.0303747 * exp(-1.05859 * distance())) * pow((width1() + width2()) / 2 , 0.199992)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF poly metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0303745 * exp(-1.05728 * distance()) + 0.0303747 * exp(-1.05859 * distance())) * pow((width1() + width2()) / 2 , 0.199992) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0303745 * exp(-1.05728 * distance()) + 0.0303747 * exp(-1.05859 * distance())) * pow((width1() + width2()) / 2 , 0.199992)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal3,metal5
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0331182 * exp(-0.958192 * distance()) + 0.0330941 * exp(-0.817984 * distance())) * pow((width1() + width2()) / 2 , 0.201203) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0331182 * exp(-0.958192 * distance()) + 0.0330941 * exp(-0.817984 * distance())) * pow((width1() + width2()) / 2 , 0.201203)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0331182 * exp(-0.958192 * distance()) + 0.0330941 * exp(-0.817984 * distance())) * pow((width1() + width2()) / 2 , 0.201203) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0331182 * exp(-0.958192 * distance()) + 0.0330941 * exp(-0.817984 * distance())) * pow((width1() + width2()) / 2 , 0.201203)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal3,metal6
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.061465 * exp(-0.893783 * distance()) + 0.00721349 * exp(-0.344781 * distance())) * pow((width1() + width2()) / 2 , 0.206769) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.061465 * exp(-0.893783 * distance()) + 0.00721349 * exp(-0.344781 * distance())) * pow((width1() + width2()) / 2 , 0.206769)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.061465 * exp(-0.893783 * distance()) + 0.00721349 * exp(-0.344781 * distance())) * pow((width1() + width2()) / 2 , 0.206769) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.061465 * exp(-0.893783 * distance()) + 0.00721349 * exp(-0.344781 * distance())) * pow((width1() + width2()) / 2 , 0.206769)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal3,metal7
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0556777 * exp(-0.931933 * distance()) + 0.0143477 * exp(-0.362634 * distance())) * pow((width1() + width2()) / 2 , 0.210852) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0556777 * exp(-0.931933 * distance()) + 0.0143477 * exp(-0.362634 * distance())) * pow((width1() + width2()) / 2 , 0.210852)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0556777 * exp(-0.931933 * distance()) + 0.0143477 * exp(-0.362634 * distance())) * pow((width1() + width2()) / 2 , 0.210852) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0556777 * exp(-0.931933 * distance()) + 0.0143477 * exp(-0.362634 * distance())) * pow((width1() + width2()) / 2 , 0.210852)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal3,metal8
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.054295 * exp(-0.947662 * distance()) + 0.0166562 * exp(-0.326199 * distance())) * pow((width1() + width2()) / 2 , 0.214095) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.054295 * exp(-0.947662 * distance()) + 0.0166562 * exp(-0.326199 * distance())) * pow((width1() + width2()) / 2 , 0.214095)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.054295 * exp(-0.947662 * distance()) + 0.0166562 * exp(-0.326199 * distance())) * pow((width1() + width2()) / 2 , 0.214095) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.054295 * exp(-0.947662 * distance()) + 0.0166562 * exp(-0.326199 * distance())) * pow((width1() + width2()) / 2 , 0.214095)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal4,metal5
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0151085 * exp(-0.608334 * distance()) + 0.0151085 * exp(-0.607907 * distance())) * pow((width1() + width2()) / 2 , 0.325138) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0151085 * exp(-0.608334 * distance()) + 0.0151085 * exp(-0.607907 * distance())) * pow((width1() + width2()) / 2 , 0.325138)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0151085 * exp(-0.608334 * distance()) + 0.0151085 * exp(-0.607907 * distance())) * pow((width1() + width2()) / 2 , 0.325138) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0151085 * exp(-0.608334 * distance()) + 0.0151085 * exp(-0.607907 * distance())) * pow((width1() + width2()) / 2 , 0.325138)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal4,metal6
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0171314 * exp(-0.513911 * distance()) + 0.0171314 * exp(-0.513457 * distance())) * pow((width1() + width2()) / 2 , 0.299656) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0171314 * exp(-0.513911 * distance()) + 0.0171314 * exp(-0.513457 * distance())) * pow((width1() + width2()) / 2 , 0.299656)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0171314 * exp(-0.513911 * distance()) + 0.0171314 * exp(-0.513457 * distance())) * pow((width1() + width2()) / 2 , 0.299656) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0171314 * exp(-0.513911 * distance()) + 0.0171314 * exp(-0.513457 * distance())) * pow((width1() + width2()) / 2 , 0.299656)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal4,metal7
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.018057 * exp(-0.461677 * distance()) + 0.018057 * exp(-0.46132 * distance())) * pow((width1() + width2()) / 2 , 0.295356) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.018057 * exp(-0.461677 * distance()) + 0.018057 * exp(-0.46132 * distance())) * pow((width1() + width2()) / 2 , 0.295356)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.018057 * exp(-0.461677 * distance()) + 0.018057 * exp(-0.46132 * distance())) * pow((width1() + width2()) / 2 , 0.295356) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.018057 * exp(-0.461677 * distance()) + 0.018057 * exp(-0.46132 * distance())) * pow((width1() + width2()) / 2 , 0.295356)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal4,metal8
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0186299 * exp(-0.424181 * distance()) + 0.0186299 * exp(-0.424273 * distance())) * pow((width1() + width2()) / 2 , 0.294689) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0186299 * exp(-0.424181 * distance()) + 0.0186299 * exp(-0.424273 * distance())) * pow((width1() + width2()) / 2 , 0.294689)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0186299 * exp(-0.424181 * distance()) + 0.0186299 * exp(-0.424273 * distance())) * pow((width1() + width2()) / 2 , 0.294689) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0186299 * exp(-0.424181 * distance()) + 0.0186299 * exp(-0.424273 * distance())) * pow((width1() + width2()) / 2 , 0.294689)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal5,metal6
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00953936 * exp(-0.436549 * distance()) + 0.00953957 * exp(-0.437576 * distance())) * pow((width1() + width2()) / 2 , 0.38736) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00953936 * exp(-0.436549 * distance()) + 0.00953957 * exp(-0.437576 * distance())) * pow((width1() + width2()) / 2 , 0.38736)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00953936 * exp(-0.436549 * distance()) + 0.00953957 * exp(-0.437576 * distance())) * pow((width1() + width2()) / 2 , 0.38736) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00953936 * exp(-0.436549 * distance()) + 0.00953957 * exp(-0.437576 * distance())) * pow((width1() + width2()) / 2 , 0.38736)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal5,metal7
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0111204 * exp(-0.371316 * distance()) + 0.011123 * exp(-0.371495 * distance())) * pow((width1() + width2()) / 2 , 0.345193) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0111204 * exp(-0.371316 * distance()) + 0.011123 * exp(-0.371495 * distance())) * pow((width1() + width2()) / 2 , 0.345193)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0111204 * exp(-0.371316 * distance()) + 0.011123 * exp(-0.371495 * distance())) * pow((width1() + width2()) / 2 , 0.345193) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0111204 * exp(-0.371316 * distance()) + 0.011123 * exp(-0.371495 * distance())) * pow((width1() + width2()) / 2 , 0.345193)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal5,metal8
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.012015 * exp(-0.326692 * distance()) + 0.0120148 * exp(-0.326551 * distance())) * pow((width1() + width2()) / 2 , 0.332561) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.012015 * exp(-0.326692 * distance()) + 0.0120148 * exp(-0.326551 * distance())) * pow((width1() + width2()) / 2 , 0.332561)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.012015 * exp(-0.326692 * distance()) + 0.0120148 * exp(-0.326551 * distance())) * pow((width1() + width2()) / 2 , 0.332561) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.012015 * exp(-0.326692 * distance()) + 0.0120148 * exp(-0.326551 * distance())) * pow((width1() + width2()) / 2 , 0.332561)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal6,metal7
//
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00662911 * exp(-0.332895 * distance()) + 0.00660733 * exp(-0.333461 * distance())) * pow((width1() + width2()) / 2 , 0.423466) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00662911 * exp(-0.332895 * distance()) + 0.00660733 * exp(-0.333461 * distance())) * pow((width1() + width2()) / 2 , 0.423466)
    }
]
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00662911 * exp(-0.332895 * distance()) + 0.00660733 * exp(-0.333461 * distance())) * pow((width1() + width2()) / 2 , 0.423466) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00662911 * exp(-0.332895 * distance()) + 0.00660733 * exp(-0.333461 * distance())) * pow((width1() + width2()) / 2 , 0.423466)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal6,metal8
//
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00805832 * exp(-0.276805 * distance()) + 0.00805856 * exp(-0.277725 * distance())) * pow((width1() + width2()) / 2 , 0.367096) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00805832 * exp(-0.276805 * distance()) + 0.00805856 * exp(-0.277725 * distance())) * pow((width1() + width2()) / 2 , 0.367096)
    }
]
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00805832 * exp(-0.276805 * distance()) + 0.00805856 * exp(-0.277725 * distance())) * pow((width1() + width2()) / 2 , 0.367096) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00805832 * exp(-0.276805 * distance()) + 0.00805856 * exp(-0.277725 * distance())) * pow((width1() + width2()) / 2 , 0.367096)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal2,metal7,metal8
//
CAPACITANCE NEARBODY metal2 metal7 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00515146 * exp(-0.252285 * distance()) + 0.00515146 * exp(-0.251494 * distance())) * pow((width1() + width2()) / 2 , 0.426935) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00515146 * exp(-0.252285 * distance()) + 0.00515146 * exp(-0.251494 * distance())) * pow((width1() + width2()) / 2 , 0.426935)
    }
]
CAPACITANCE NEARBODY metal7 metal2 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00515146 * exp(-0.252285 * distance()) + 0.00515146 * exp(-0.251494 * distance())) * pow((width1() + width2()) / 2 , 0.426935) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00515146 * exp(-0.252285 * distance()) + 0.00515146 * exp(-0.251494 * distance())) * pow((width1() + width2()) / 2 , 0.426935)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal4,metal5
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0193896 * exp(-0.849975 * distance()) + 0.0440239 * exp(-0.993059 * distance())) * pow((width1() + width2()) / 2 , 0.210932) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0193896 * exp(-0.849975 * distance()) + 0.0440239 * exp(-0.993059 * distance())) * pow((width1() + width2()) / 2 , 0.210932)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF poly metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0193896 * exp(-0.849975 * distance()) + 0.0440239 * exp(-0.993059 * distance())) * pow((width1() + width2()) / 2 , 0.210932) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0193896 * exp(-0.849975 * distance()) + 0.0440239 * exp(-0.993059 * distance())) * pow((width1() + width2()) / 2 , 0.210932)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal4,metal6
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.010175 * exp(-0.388569 * distance()) + 0.0593788 * exp(-0.89485 * distance())) * pow((width1() + width2()) / 2 , 0.205464) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.010175 * exp(-0.388569 * distance()) + 0.0593788 * exp(-0.89485 * distance())) * pow((width1() + width2()) / 2 , 0.205464)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.010175 * exp(-0.388569 * distance()) + 0.0593788 * exp(-0.89485 * distance())) * pow((width1() + width2()) / 2 , 0.205464) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.010175 * exp(-0.388569 * distance()) + 0.0593788 * exp(-0.89485 * distance())) * pow((width1() + width2()) / 2 , 0.205464)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal4,metal7
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0475206 * exp(-0.971171 * distance()) + 0.0248519 * exp(-0.413995 * distance())) * pow((width1() + width2()) / 2 , 0.208022) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0475206 * exp(-0.971171 * distance()) + 0.0248519 * exp(-0.413995 * distance())) * pow((width1() + width2()) / 2 , 0.208022)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0475206 * exp(-0.971171 * distance()) + 0.0248519 * exp(-0.413995 * distance())) * pow((width1() + width2()) / 2 , 0.208022) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0475206 * exp(-0.971171 * distance()) + 0.0248519 * exp(-0.413995 * distance())) * pow((width1() + width2()) / 2 , 0.208022)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal4,metal8
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0462439 * exp(-0.996448 * distance()) + 0.0279089 * exp(-0.360293 * distance())) * pow((width1() + width2()) / 2 , 0.210995) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0462439 * exp(-0.996448 * distance()) + 0.0279089 * exp(-0.360293 * distance())) * pow((width1() + width2()) / 2 , 0.210995)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0462439 * exp(-0.996448 * distance()) + 0.0279089 * exp(-0.360293 * distance())) * pow((width1() + width2()) / 2 , 0.210995) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0462439 * exp(-0.996448 * distance()) + 0.0279089 * exp(-0.360293 * distance())) * pow((width1() + width2()) / 2 , 0.210995)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal5,metal6
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.016118 * exp(-0.53856 * distance()) + 0.0161179 * exp(-0.537596 * distance())) * pow((width1() + width2()) / 2 , 0.32238) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.016118 * exp(-0.53856 * distance()) + 0.0161179 * exp(-0.537596 * distance())) * pow((width1() + width2()) / 2 , 0.32238)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.016118 * exp(-0.53856 * distance()) + 0.0161179 * exp(-0.537596 * distance())) * pow((width1() + width2()) / 2 , 0.32238) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.016118 * exp(-0.53856 * distance()) + 0.0161179 * exp(-0.537596 * distance())) * pow((width1() + width2()) / 2 , 0.32238)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal5,metal7
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0183921 * exp(-0.453654 * distance()) + 0.018392 * exp(-0.453308 * distance())) * pow((width1() + width2()) / 2 , 0.290567) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0183921 * exp(-0.453654 * distance()) + 0.018392 * exp(-0.453308 * distance())) * pow((width1() + width2()) / 2 , 0.290567)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0183921 * exp(-0.453654 * distance()) + 0.018392 * exp(-0.453308 * distance())) * pow((width1() + width2()) / 2 , 0.290567) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0183921 * exp(-0.453654 * distance()) + 0.018392 * exp(-0.453308 * distance())) * pow((width1() + width2()) / 2 , 0.290567)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal5,metal8
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0196156 * exp(-0.399313 * distance()) + 0.0196156 * exp(-0.399902 * distance())) * pow((width1() + width2()) / 2 , 0.282493) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0196156 * exp(-0.399313 * distance()) + 0.0196156 * exp(-0.399902 * distance())) * pow((width1() + width2()) / 2 , 0.282493)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0196156 * exp(-0.399313 * distance()) + 0.0196156 * exp(-0.399902 * distance())) * pow((width1() + width2()) / 2 , 0.282493) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0196156 * exp(-0.399313 * distance()) + 0.0196156 * exp(-0.399902 * distance())) * pow((width1() + width2()) / 2 , 0.282493)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal6,metal7
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0103604 * exp(-0.38549 * distance()) + 0.0103604 * exp(-0.385391 * distance())) * pow((width1() + width2()) / 2 , 0.374081) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0103604 * exp(-0.38549 * distance()) + 0.0103604 * exp(-0.385391 * distance())) * pow((width1() + width2()) / 2 , 0.374081)
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0103604 * exp(-0.38549 * distance()) + 0.0103604 * exp(-0.385391 * distance())) * pow((width1() + width2()) / 2 , 0.374081) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0103604 * exp(-0.38549 * distance()) + 0.0103604 * exp(-0.385391 * distance())) * pow((width1() + width2()) / 2 , 0.374081)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal6,metal8
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0124 * exp(-0.319456 * distance()) + 0.0124001 * exp(-0.318952 * distance())) * pow((width1() + width2()) / 2 , 0.323565) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0124 * exp(-0.319456 * distance()) + 0.0124001 * exp(-0.318952 * distance())) * pow((width1() + width2()) / 2 , 0.323565)
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0124 * exp(-0.319456 * distance()) + 0.0124001 * exp(-0.318952 * distance())) * pow((width1() + width2()) / 2 , 0.323565) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0124 * exp(-0.319456 * distance()) + 0.0124001 * exp(-0.318952 * distance())) * pow((width1() + width2()) / 2 , 0.323565)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal3,metal7,metal8
//
CAPACITANCE NEARBODY metal3 metal7 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00771849 * exp(-0.282178 * distance()) + 0.00771846 * exp(-0.282803 * distance())) * pow((width1() + width2()) / 2 , 0.384334) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00771849 * exp(-0.282178 * distance()) + 0.00771846 * exp(-0.282803 * distance())) * pow((width1() + width2()) / 2 , 0.384334)
    }
]
CAPACITANCE NEARBODY metal7 metal3 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00771849 * exp(-0.282178 * distance()) + 0.00771846 * exp(-0.282803 * distance())) * pow((width1() + width2()) / 2 , 0.384334) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00771849 * exp(-0.282178 * distance()) + 0.00771846 * exp(-0.282803 * distance())) * pow((width1() + width2()) / 2 , 0.384334)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal5,metal6
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0325006 * exp(-1.16383 * distance()) + 0.0323737 * exp(-0.689302 * distance())) * pow((width1() + width2()) / 2 , 0.217852) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0325006 * exp(-1.16383 * distance()) + 0.0323737 * exp(-0.689302 * distance())) * pow((width1() + width2()) / 2 , 0.217852)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF poly metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0325006 * exp(-1.16383 * distance()) + 0.0323737 * exp(-0.689302 * distance())) * pow((width1() + width2()) / 2 , 0.217852) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0325006 * exp(-1.16383 * distance()) + 0.0323737 * exp(-0.689302 * distance())) * pow((width1() + width2()) / 2 , 0.217852)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal5,metal7
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0519661 * exp(-0.945156 * distance()) + 0.0193396 * exp(-0.391648 * distance())) * pow((width1() + width2()) / 2 , 0.209451) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0519661 * exp(-0.945156 * distance()) + 0.0193396 * exp(-0.391648 * distance())) * pow((width1() + width2()) / 2 , 0.209451)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0519661 * exp(-0.945156 * distance()) + 0.0193396 * exp(-0.391648 * distance())) * pow((width1() + width2()) / 2 , 0.209451) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0519661 * exp(-0.945156 * distance()) + 0.0193396 * exp(-0.391648 * distance())) * pow((width1() + width2()) / 2 , 0.209451)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal5,metal8
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0448732 * exp(-1.00833 * distance()) + 0.029862 * exp(-0.361435 * distance())) * pow((width1() + width2()) / 2 , 0.210814) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0448732 * exp(-1.00833 * distance()) + 0.029862 * exp(-0.361435 * distance())) * pow((width1() + width2()) / 2 , 0.210814)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0448732 * exp(-1.00833 * distance()) + 0.029862 * exp(-0.361435 * distance())) * pow((width1() + width2()) / 2 , 0.210814) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0448732 * exp(-1.00833 * distance()) + 0.029862 * exp(-0.361435 * distance())) * pow((width1() + width2()) / 2 , 0.210814)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal6,metal7
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0166255 * exp(-0.496359 * distance()) + 0.0166256 * exp(-0.496415 * distance())) * pow((width1() + width2()) / 2 , 0.323526) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0166255 * exp(-0.496359 * distance()) + 0.0166256 * exp(-0.496415 * distance())) * pow((width1() + width2()) / 2 , 0.323526)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0166255 * exp(-0.496359 * distance()) + 0.0166256 * exp(-0.496415 * distance())) * pow((width1() + width2()) / 2 , 0.323526) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0166255 * exp(-0.496359 * distance()) + 0.0166256 * exp(-0.496415 * distance())) * pow((width1() + width2()) / 2 , 0.323526)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal6,metal8
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0193461 * exp(-0.406813 * distance()) + 0.0193462 * exp(-0.405902 * distance())) * pow((width1() + width2()) / 2 , 0.286052) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0193461 * exp(-0.406813 * distance()) + 0.0193462 * exp(-0.405902 * distance())) * pow((width1() + width2()) / 2 , 0.286052)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0193461 * exp(-0.406813 * distance()) + 0.0193462 * exp(-0.405902 * distance())) * pow((width1() + width2()) / 2 , 0.286052) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0193461 * exp(-0.406813 * distance()) + 0.0193462 * exp(-0.405902 * distance())) * pow((width1() + width2()) / 2 , 0.286052)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal4,metal7,metal8
//
CAPACITANCE NEARBODY metal4 metal7 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0113633 * exp(-0.339336 * distance()) + 0.0113633 * exp(-0.338605 * distance())) * pow((width1() + width2()) / 2 , 0.353036) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0113633 * exp(-0.339336 * distance()) + 0.0113633 * exp(-0.338605 * distance())) * pow((width1() + width2()) / 2 , 0.353036)
    }
]
CAPACITANCE NEARBODY metal7 metal4 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0113633 * exp(-0.339336 * distance()) + 0.0113633 * exp(-0.338605 * distance())) * pow((width1() + width2()) / 2 , 0.353036) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0113633 * exp(-0.339336 * distance()) + 0.0113633 * exp(-0.338605 * distance())) * pow((width1() + width2()) / 2 , 0.353036)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal5,metal6,metal7
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0326163 * exp(-0.836596 * distance()) + 0.0326163 * exp(-0.867583 * distance())) * pow((width1() + width2()) / 2 , 0.221148) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0326163 * exp(-0.836596 * distance()) + 0.0326163 * exp(-0.867583 * distance())) * pow((width1() + width2()) / 2 , 0.221148)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF poly metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0326163 * exp(-0.836596 * distance()) + 0.0326163 * exp(-0.867583 * distance())) * pow((width1() + width2()) / 2 , 0.221148) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0326163 * exp(-0.836596 * distance()) + 0.0326163 * exp(-0.867583 * distance())) * pow((width1() + width2()) / 2 , 0.221148)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal5,metal6,metal8
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0490366 * exp(-0.975182 * distance()) + 0.0240828 * exp(-0.353466 * distance())) * pow((width1() + width2()) / 2 , 0.212303) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0490366 * exp(-0.975182 * distance()) + 0.0240828 * exp(-0.353466 * distance())) * pow((width1() + width2()) / 2 , 0.212303)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0490366 * exp(-0.975182 * distance()) + 0.0240828 * exp(-0.353466 * distance())) * pow((width1() + width2()) / 2 , 0.212303) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0490366 * exp(-0.975182 * distance()) + 0.0240828 * exp(-0.353466 * distance())) * pow((width1() + width2()) / 2 , 0.212303)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal5,metal7,metal8
//
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0176685 * exp(-0.448488 * distance()) + 0.0176685 * exp(-0.448305 * distance())) * pow((width1() + width2()) / 2 , 0.311974) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0176685 * exp(-0.448488 * distance()) + 0.0176685 * exp(-0.448305 * distance())) * pow((width1() + width2()) / 2 , 0.311974)
    }
]
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0176685 * exp(-0.448488 * distance()) + 0.0176685 * exp(-0.448305 * distance())) * pow((width1() + width2()) / 2 , 0.311974) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0176685 * exp(-0.448488 * distance()) + 0.0176685 * exp(-0.448305 * distance())) * pow((width1() + width2()) / 2 , 0.311974)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,poly,metal6,metal7,metal8
//
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0588978 * exp(-0.937341 * distance()) + 0.00940446 * exp(-0.268735 * distance())) * pow((width1() + width2()) / 2 , 0.221201) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0588978 * exp(-0.937341 * distance()) + 0.00940446 * exp(-0.268735 * distance())) * pow((width1() + width2()) / 2 , 0.221201)
    }
]
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF poly metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0588978 * exp(-0.937341 * distance()) + 0.00940446 * exp(-0.268735 * distance())) * pow((width1() + width2()) / 2 , 0.221201) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0588978 * exp(-0.937341 * distance()) + 0.00940446 * exp(-0.268735 * distance())) * pow((width1() + width2()) / 2 , 0.221201)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal3,metal4
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal1 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0284925 * exp(-1.203 * distance()) + 0.028493 * exp(-1.19568 * distance())) * pow((width1() + width2()) / 2 , 0.192167) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0284925 * exp(-1.203 * distance()) + 0.028493 * exp(-1.19568 * distance())) * pow((width1() + width2()) / 2 , 0.192167)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal1 metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0284925 * exp(-1.203 * distance()) + 0.028493 * exp(-1.19568 * distance())) * pow((width1() + width2()) / 2 , 0.192167) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0284925 * exp(-1.203 * distance()) + 0.028493 * exp(-1.19568 * distance())) * pow((width1() + width2()) / 2 , 0.192167)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal3,metal5
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0310202 * exp(-1.00551 * distance()) + 0.0310181 * exp(-1.0062 * distance())) * pow((width1() + width2()) / 2 , 0.202765) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0310202 * exp(-1.00551 * distance()) + 0.0310181 * exp(-1.0062 * distance())) * pow((width1() + width2()) / 2 , 0.202765)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0310202 * exp(-1.00551 * distance()) + 0.0310181 * exp(-1.0062 * distance())) * pow((width1() + width2()) / 2 , 0.202765) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0310202 * exp(-1.00551 * distance()) + 0.0310181 * exp(-1.0062 * distance())) * pow((width1() + width2()) / 2 , 0.202765)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal3,metal6
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.000486717 * exp(0.0183266 * distance()) + 0.0636083 * exp(-0.93604 * distance())) * pow((width1() + width2()) / 2 , 0.211901) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.000486717 * exp(0.0183266 * distance()) + 0.0636083 * exp(-0.93604 * distance())) * pow((width1() + width2()) / 2 , 0.211901)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.000486717 * exp(0.0183266 * distance()) + 0.0636083 * exp(-0.93604 * distance())) * pow((width1() + width2()) / 2 , 0.211901) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.000486717 * exp(0.0183266 * distance()) + 0.0636083 * exp(-0.93604 * distance())) * pow((width1() + width2()) / 2 , 0.211901)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal3,metal7
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0616093 * exp(-0.942963 * distance()) + 0.00362356 * exp(-0.225332 * distance())) * pow((width1() + width2()) / 2 , 0.217636) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0616093 * exp(-0.942963 * distance()) + 0.00362356 * exp(-0.225332 * distance())) * pow((width1() + width2()) / 2 , 0.217636)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0616093 * exp(-0.942963 * distance()) + 0.00362356 * exp(-0.225332 * distance())) * pow((width1() + width2()) / 2 , 0.217636) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0616093 * exp(-0.942963 * distance()) + 0.00362356 * exp(-0.225332 * distance())) * pow((width1() + width2()) / 2 , 0.217636)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal3,metal8
//
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00645531 * exp(-0.249093 * distance()) + 0.0595467 * exp(-0.957813 * distance())) * pow((width1() + width2()) / 2 , 0.221848) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00645531 * exp(-0.249093 * distance()) + 0.0595467 * exp(-0.957813 * distance())) * pow((width1() + width2()) / 2 , 0.221848)
    }
]
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00645531 * exp(-0.249093 * distance()) + 0.0595467 * exp(-0.957813 * distance())) * pow((width1() + width2()) / 2 , 0.221848) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00645531 * exp(-0.249093 * distance()) + 0.0595467 * exp(-0.957813 * distance())) * pow((width1() + width2()) / 2 , 0.221848)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal4,metal5
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0138215 * exp(-0.683509 * distance()) + 0.0138213 * exp(-0.684262 * distance())) * pow((width1() + width2()) / 2 , 0.338532) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0138215 * exp(-0.683509 * distance()) + 0.0138213 * exp(-0.684262 * distance())) * pow((width1() + width2()) / 2 , 0.338532)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0138215 * exp(-0.683509 * distance()) + 0.0138213 * exp(-0.684262 * distance())) * pow((width1() + width2()) / 2 , 0.338532) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0138215 * exp(-0.683509 * distance()) + 0.0138213 * exp(-0.684262 * distance())) * pow((width1() + width2()) / 2 , 0.338532)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal4,metal6
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0155911 * exp(-0.576976 * distance()) + 0.0155912 * exp(-0.57726 * distance())) * pow((width1() + width2()) / 2 , 0.320917) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0155911 * exp(-0.576976 * distance()) + 0.0155912 * exp(-0.57726 * distance())) * pow((width1() + width2()) / 2 , 0.320917)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0155911 * exp(-0.576976 * distance()) + 0.0155912 * exp(-0.57726 * distance())) * pow((width1() + width2()) / 2 , 0.320917) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0155911 * exp(-0.576976 * distance()) + 0.0155912 * exp(-0.57726 * distance())) * pow((width1() + width2()) / 2 , 0.320917)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal4,metal7
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0163716 * exp(-0.519011 * distance()) + 0.0163717 * exp(-0.519975 * distance())) * pow((width1() + width2()) / 2 , 0.320079) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0163716 * exp(-0.519011 * distance()) + 0.0163717 * exp(-0.519975 * distance())) * pow((width1() + width2()) / 2 , 0.320079)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0163716 * exp(-0.519011 * distance()) + 0.0163717 * exp(-0.519975 * distance())) * pow((width1() + width2()) / 2 , 0.320079) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0163716 * exp(-0.519011 * distance()) + 0.0163717 * exp(-0.519975 * distance())) * pow((width1() + width2()) / 2 , 0.320079)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal4,metal8
//
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0168385 * exp(-0.479682 * distance()) + 0.0168385 * exp(-0.478146 * distance())) * pow((width1() + width2()) / 2 , 0.32151) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0168385 * exp(-0.479682 * distance()) + 0.0168385 * exp(-0.478146 * distance())) * pow((width1() + width2()) / 2 , 0.32151)
    }
]
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0168385 * exp(-0.479682 * distance()) + 0.0168385 * exp(-0.478146 * distance())) * pow((width1() + width2()) / 2 , 0.32151) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0168385 * exp(-0.479682 * distance()) + 0.0168385 * exp(-0.478146 * distance())) * pow((width1() + width2()) / 2 , 0.32151)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal5,metal6
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00857142 * exp(-0.488735 * distance()) + 0.0085713 * exp(-0.488698 * distance())) * pow((width1() + width2()) / 2 , 0.414948) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00857142 * exp(-0.488735 * distance()) + 0.0085713 * exp(-0.488698 * distance())) * pow((width1() + width2()) / 2 , 0.414948)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00857142 * exp(-0.488735 * distance()) + 0.0085713 * exp(-0.488698 * distance())) * pow((width1() + width2()) / 2 , 0.414948) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00857142 * exp(-0.488735 * distance()) + 0.0085713 * exp(-0.488698 * distance())) * pow((width1() + width2()) / 2 , 0.414948)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal5,metal7
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00992256 * exp(-0.414927 * distance()) + 0.00992257 * exp(-0.414632 * distance())) * pow((width1() + width2()) / 2 , 0.377981) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00992256 * exp(-0.414927 * distance()) + 0.00992257 * exp(-0.414632 * distance())) * pow((width1() + width2()) / 2 , 0.377981)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00992256 * exp(-0.414927 * distance()) + 0.00992257 * exp(-0.414632 * distance())) * pow((width1() + width2()) / 2 , 0.377981) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00992256 * exp(-0.414927 * distance()) + 0.00992257 * exp(-0.414632 * distance())) * pow((width1() + width2()) / 2 , 0.377981)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal5,metal8
//
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0106608 * exp(-0.365427 * distance()) + 0.0106608 * exp(-0.364818 * distance())) * pow((width1() + width2()) / 2 , 0.368345) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0106608 * exp(-0.365427 * distance()) + 0.0106608 * exp(-0.364818 * distance())) * pow((width1() + width2()) / 2 , 0.368345)
    }
]
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0106608 * exp(-0.365427 * distance()) + 0.0106608 * exp(-0.364818 * distance())) * pow((width1() + width2()) / 2 , 0.368345) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0106608 * exp(-0.365427 * distance()) + 0.0106608 * exp(-0.364818 * distance())) * pow((width1() + width2()) / 2 , 0.368345)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal6,metal7
//
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00585298 * exp(-0.371396 * distance()) + 0.00585293 * exp(-0.370908 * distance())) * pow((width1() + width2()) / 2 , 0.459394) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00585298 * exp(-0.371396 * distance()) + 0.00585293 * exp(-0.370908 * distance())) * pow((width1() + width2()) / 2 , 0.459394)
    }
]
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00585298 * exp(-0.371396 * distance()) + 0.00585293 * exp(-0.370908 * distance())) * pow((width1() + width2()) / 2 , 0.459394) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00585298 * exp(-0.371396 * distance()) + 0.00585293 * exp(-0.370908 * distance())) * pow((width1() + width2()) / 2 , 0.459394)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal6,metal8
//
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00706609 * exp(-0.308055 * distance()) + 0.00706617 * exp(-0.308836 * distance())) * pow((width1() + width2()) / 2 , 0.406995) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00706609 * exp(-0.308055 * distance()) + 0.00706617 * exp(-0.308836 * distance())) * pow((width1() + width2()) / 2 , 0.406995)
    }
]
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00706609 * exp(-0.308055 * distance()) + 0.00706617 * exp(-0.308836 * distance())) * pow((width1() + width2()) / 2 , 0.406995) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00706609 * exp(-0.308055 * distance()) + 0.00706617 * exp(-0.308836 * distance())) * pow((width1() + width2()) / 2 , 0.406995)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal2,metal7,metal8
//
CAPACITANCE NEARBODY metal2 metal7 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0044854 * exp(-0.279946 * distance()) + 0.00448534 * exp(-0.279267 * distance())) * pow((width1() + width2()) / 2 , 0.468568) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0044854 * exp(-0.279946 * distance()) + 0.00448534 * exp(-0.279267 * distance())) * pow((width1() + width2()) / 2 , 0.468568)
    }
]
CAPACITANCE NEARBODY metal7 metal2 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0044854 * exp(-0.279946 * distance()) + 0.00448534 * exp(-0.279267 * distance())) * pow((width1() + width2()) / 2 , 0.468568) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0044854 * exp(-0.279946 * distance()) + 0.00448534 * exp(-0.279267 * distance())) * pow((width1() + width2()) / 2 , 0.468568)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal4,metal5
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0309955 * exp(-1.0067 * distance()) + 0.0309954 * exp(-1.0085 * distance())) * pow((width1() + width2()) / 2 , 0.204381) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0309955 * exp(-1.0067 * distance()) + 0.0309954 * exp(-1.0085 * distance())) * pow((width1() + width2()) / 2 , 0.204381)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal1 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0309955 * exp(-1.0067 * distance()) + 0.0309954 * exp(-1.0085 * distance())) * pow((width1() + width2()) / 2 , 0.204381) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0309955 * exp(-1.0067 * distance()) + 0.0309954 * exp(-1.0085 * distance())) * pow((width1() + width2()) / 2 , 0.204381)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal4,metal6
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0337665 * exp(-0.699407 * distance()) + 0.0339796 * exp(-1.02084 * distance())) * pow((width1() + width2()) / 2 , 0.202396) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0337665 * exp(-0.699407 * distance()) + 0.0339796 * exp(-1.02084 * distance())) * pow((width1() + width2()) / 2 , 0.202396)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0337665 * exp(-0.699407 * distance()) + 0.0339796 * exp(-1.02084 * distance())) * pow((width1() + width2()) / 2 , 0.202396) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0337665 * exp(-0.699407 * distance()) + 0.0339796 * exp(-1.02084 * distance())) * pow((width1() + width2()) / 2 , 0.202396)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal4,metal7
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0551206 * exp(-0.919691 * distance()) + 0.0152151 * exp(-0.407294 * distance())) * pow((width1() + width2()) / 2 , 0.20666) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0551206 * exp(-0.919691 * distance()) + 0.0152151 * exp(-0.407294 * distance())) * pow((width1() + width2()) / 2 , 0.20666)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0551206 * exp(-0.919691 * distance()) + 0.0152151 * exp(-0.407294 * distance())) * pow((width1() + width2()) / 2 , 0.20666) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0551206 * exp(-0.919691 * distance()) + 0.0152151 * exp(-0.407294 * distance())) * pow((width1() + width2()) / 2 , 0.20666)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal4,metal8
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0212816 * exp(-0.37227 * distance()) + 0.0506869 * exp(-0.959904 * distance())) * pow((width1() + width2()) / 2 , 0.210593) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0212816 * exp(-0.37227 * distance()) + 0.0506869 * exp(-0.959904 * distance())) * pow((width1() + width2()) / 2 , 0.210593)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0212816 * exp(-0.37227 * distance()) + 0.0506869 * exp(-0.959904 * distance())) * pow((width1() + width2()) / 2 , 0.210593) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0212816 * exp(-0.37227 * distance()) + 0.0506869 * exp(-0.959904 * distance())) * pow((width1() + width2()) / 2 , 0.210593)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal5,metal6
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0155689 * exp(-0.577923 * distance()) + 0.0155689 * exp(-0.577821 * distance())) * pow((width1() + width2()) / 2 , 0.322964) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0155689 * exp(-0.577923 * distance()) + 0.0155689 * exp(-0.577821 * distance())) * pow((width1() + width2()) / 2 , 0.322964)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0155689 * exp(-0.577923 * distance()) + 0.0155689 * exp(-0.577821 * distance())) * pow((width1() + width2()) / 2 , 0.322964) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0155689 * exp(-0.577923 * distance()) + 0.0155689 * exp(-0.577821 * distance())) * pow((width1() + width2()) / 2 , 0.322964)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal5,metal7
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0176985 * exp(-0.487449 * distance()) + 0.0176985 * exp(-0.488078 * distance())) * pow((width1() + width2()) / 2 , 0.294637) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0176985 * exp(-0.487449 * distance()) + 0.0176985 * exp(-0.488078 * distance())) * pow((width1() + width2()) / 2 , 0.294637)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0176985 * exp(-0.487449 * distance()) + 0.0176985 * exp(-0.488078 * distance())) * pow((width1() + width2()) / 2 , 0.294637) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0176985 * exp(-0.487449 * distance()) + 0.0176985 * exp(-0.488078 * distance())) * pow((width1() + width2()) / 2 , 0.294637)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal5,metal8
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0188159 * exp(-0.43081 * distance()) + 0.0188159 * exp(-0.430813 * distance())) * pow((width1() + width2()) / 2 , 0.288502) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0188159 * exp(-0.43081 * distance()) + 0.0188159 * exp(-0.430813 * distance())) * pow((width1() + width2()) / 2 , 0.288502)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0188159 * exp(-0.43081 * distance()) + 0.0188159 * exp(-0.430813 * distance())) * pow((width1() + width2()) / 2 , 0.288502) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0188159 * exp(-0.43081 * distance()) + 0.0188159 * exp(-0.430813 * distance())) * pow((width1() + width2()) / 2 , 0.288502)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal6,metal7
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00990597 * exp(-0.415591 * distance()) + 0.00990601 * exp(-0.4148 * distance())) * pow((width1() + width2()) / 2 , 0.380345) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00990597 * exp(-0.415591 * distance()) + 0.00990601 * exp(-0.4148 * distance())) * pow((width1() + width2()) / 2 , 0.380345)
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00990597 * exp(-0.415591 * distance()) + 0.00990601 * exp(-0.4148 * distance())) * pow((width1() + width2()) / 2 , 0.380345) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00990597 * exp(-0.415591 * distance()) + 0.00990601 * exp(-0.4148 * distance())) * pow((width1() + width2()) / 2 , 0.380345)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal6,metal8
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0117908 * exp(-0.344108 * distance()) + 0.011791 * exp(-0.344128 * distance())) * pow((width1() + width2()) / 2 , 0.332755) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0117908 * exp(-0.344108 * distance()) + 0.011791 * exp(-0.344128 * distance())) * pow((width1() + width2()) / 2 , 0.332755)
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0117908 * exp(-0.344108 * distance()) + 0.011791 * exp(-0.344128 * distance())) * pow((width1() + width2()) / 2 , 0.332755) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0117908 * exp(-0.344108 * distance()) + 0.011791 * exp(-0.344128 * distance())) * pow((width1() + width2()) / 2 , 0.332755)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal3,metal7,metal8
//
CAPACITANCE NEARBODY metal3 metal7 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00730176 * exp(-0.304849 * distance()) + 0.0073017 * exp(-0.30462 * distance())) * pow((width1() + width2()) / 2 , 0.39492) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00730176 * exp(-0.304849 * distance()) + 0.0073017 * exp(-0.30462 * distance())) * pow((width1() + width2()) / 2 , 0.39492)
    }
]
CAPACITANCE NEARBODY metal7 metal3 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00730176 * exp(-0.304849 * distance()) + 0.0073017 * exp(-0.30462 * distance())) * pow((width1() + width2()) / 2 , 0.39492) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00730176 * exp(-0.304849 * distance()) + 0.0073017 * exp(-0.30462 * distance())) * pow((width1() + width2()) / 2 , 0.39492)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal5,metal6
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.000459236 * exp(0.0191462 * distance()) + 0.0635686 * exp(-0.937959 * distance())) * pow((width1() + width2()) / 2 , 0.213892) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.000459236 * exp(0.0191462 * distance()) + 0.0635686 * exp(-0.937959 * distance())) * pow((width1() + width2()) / 2 , 0.213892)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal1 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.000459236 * exp(0.0191462 * distance()) + 0.0635686 * exp(-0.937959 * distance())) * pow((width1() + width2()) / 2 , 0.213892) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.000459236 * exp(0.0191462 * distance()) + 0.0635686 * exp(-0.937959 * distance())) * pow((width1() + width2()) / 2 , 0.213892)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal5,metal7
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0553643 * exp(-0.918246 * distance()) + 0.0149517 * exp(-0.405065 * distance())) * pow((width1() + width2()) / 2 , 0.207049) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0553643 * exp(-0.918246 * distance()) + 0.0149517 * exp(-0.405065 * distance())) * pow((width1() + width2()) / 2 , 0.207049)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0553643 * exp(-0.918246 * distance()) + 0.0149517 * exp(-0.405065 * distance())) * pow((width1() + width2()) / 2 , 0.207049) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0553643 * exp(-0.918246 * distance()) + 0.0149517 * exp(-0.405065 * distance())) * pow((width1() + width2()) / 2 , 0.207049)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal5,metal8
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0281508 * exp(-0.390682 * distance()) + 0.0454484 * exp(-0.99521 * distance())) * pow((width1() + width2()) / 2 , 0.209395) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0281508 * exp(-0.390682 * distance()) + 0.0454484 * exp(-0.99521 * distance())) * pow((width1() + width2()) / 2 , 0.209395)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0281508 * exp(-0.390682 * distance()) + 0.0454484 * exp(-0.99521 * distance())) * pow((width1() + width2()) / 2 , 0.209395) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0281508 * exp(-0.390682 * distance()) + 0.0454484 * exp(-0.99521 * distance())) * pow((width1() + width2()) / 2 , 0.209395)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal6,metal7
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0163411 * exp(-0.52059 * distance()) + 0.0163411 * exp(-0.520467 * distance())) * pow((width1() + width2()) / 2 , 0.322652) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0163411 * exp(-0.52059 * distance()) + 0.0163411 * exp(-0.520467 * distance())) * pow((width1() + width2()) / 2 , 0.322652)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0163411 * exp(-0.52059 * distance()) + 0.0163411 * exp(-0.520467 * distance())) * pow((width1() + width2()) / 2 , 0.322652) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0163411 * exp(-0.52059 * distance()) + 0.0163411 * exp(-0.520467 * distance())) * pow((width1() + width2()) / 2 , 0.322652)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal6,metal8
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189557 * exp(-0.427339 * distance()) + 0.0189557 * exp(-0.427148 * distance())) * pow((width1() + width2()) / 2 , 0.28724) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189557 * exp(-0.427339 * distance()) + 0.0189557 * exp(-0.427148 * distance())) * pow((width1() + width2()) / 2 , 0.28724)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189557 * exp(-0.427339 * distance()) + 0.0189557 * exp(-0.427148 * distance())) * pow((width1() + width2()) / 2 , 0.28724) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189557 * exp(-0.427339 * distance()) + 0.0189557 * exp(-0.427148 * distance())) * pow((width1() + width2()) / 2 , 0.28724)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal4,metal7,metal8
//
CAPACITANCE NEARBODY metal4 metal7 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0110891 * exp(-0.356835 * distance()) + 0.011089 * exp(-0.357615 * distance())) * pow((width1() + width2()) / 2 , 0.35543) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0110891 * exp(-0.356835 * distance()) + 0.011089 * exp(-0.357615 * distance())) * pow((width1() + width2()) / 2 , 0.35543)
    }
]
CAPACITANCE NEARBODY metal7 metal4 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0110891 * exp(-0.356835 * distance()) + 0.011089 * exp(-0.357615 * distance())) * pow((width1() + width2()) / 2 , 0.35543) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0110891 * exp(-0.356835 * distance()) + 0.011089 * exp(-0.357615 * distance())) * pow((width1() + width2()) / 2 , 0.35543)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal5,metal6,metal7
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0325551 * exp(-0.65772 * distance()) + 0.0326691 * exp(-1.1846 * distance())) * pow((width1() + width2()) / 2 , 0.219688) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0325551 * exp(-0.65772 * distance()) + 0.0326691 * exp(-1.1846 * distance())) * pow((width1() + width2()) / 2 , 0.219688)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal1 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0325551 * exp(-0.65772 * distance()) + 0.0326691 * exp(-1.1846 * distance())) * pow((width1() + width2()) / 2 , 0.219688) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0325551 * exp(-0.65772 * distance()) + 0.0326691 * exp(-1.1846 * distance())) * pow((width1() + width2()) / 2 , 0.219688)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal5,metal6,metal8
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0231071 * exp(-0.376148 * distance()) + 0.0493969 * exp(-0.966261 * distance())) * pow((width1() + width2()) / 2 , 0.21075) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0231071 * exp(-0.376148 * distance()) + 0.0493969 * exp(-0.966261 * distance())) * pow((width1() + width2()) / 2 , 0.21075)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0231071 * exp(-0.376148 * distance()) + 0.0493969 * exp(-0.966261 * distance())) * pow((width1() + width2()) / 2 , 0.21075) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0231071 * exp(-0.376148 * distance()) + 0.0493969 * exp(-0.966261 * distance())) * pow((width1() + width2()) / 2 , 0.21075)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal5,metal7,metal8
//
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0174879 * exp(-0.463819 * distance()) + 0.0174879 * exp(-0.463587 * distance())) * pow((width1() + width2()) / 2 , 0.311357) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0174879 * exp(-0.463819 * distance()) + 0.0174879 * exp(-0.463587 * distance())) * pow((width1() + width2()) / 2 , 0.311357)
    }
]
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0174879 * exp(-0.463819 * distance()) + 0.0174879 * exp(-0.463587 * distance())) * pow((width1() + width2()) / 2 , 0.311357) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0174879 * exp(-0.463819 * distance()) + 0.0174879 * exp(-0.463587 * distance())) * pow((width1() + width2()) / 2 , 0.311357)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal1,metal6,metal7,metal8
//
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0337291 * exp(-0.790954 * distance()) + 0.0337291 * exp(-0.790929 * distance())) * pow((width1() + width2()) / 2 , 0.219466) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0337291 * exp(-0.790954 * distance()) + 0.0337291 * exp(-0.790929 * distance())) * pow((width1() + width2()) / 2 , 0.219466)
    }
]
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF metal1 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0337291 * exp(-0.790954 * distance()) + 0.0337291 * exp(-0.790929 * distance())) * pow((width1() + width2()) / 2 , 0.219466) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0337291 * exp(-0.790954 * distance()) + 0.0337291 * exp(-0.790929 * distance())) * pow((width1() + width2()) / 2 , 0.219466)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal4,metal5
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal2 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0284925 * exp(-1.203 * distance()) + 0.028493 * exp(-1.19568 * distance())) * pow((width1() + width2()) / 2 , 0.192167) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0284925 * exp(-1.203 * distance()) + 0.028493 * exp(-1.19568 * distance())) * pow((width1() + width2()) / 2 , 0.192167)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal2 metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0284925 * exp(-1.203 * distance()) + 0.028493 * exp(-1.19568 * distance())) * pow((width1() + width2()) / 2 , 0.192167) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0284925 * exp(-1.203 * distance()) + 0.028493 * exp(-1.19568 * distance())) * pow((width1() + width2()) / 2 , 0.192167)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal4,metal6
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0310192 * exp(-1.00409 * distance()) + 0.0310191 * exp(-1.00761 * distance())) * pow((width1() + width2()) / 2 , 0.202766) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0310192 * exp(-1.00409 * distance()) + 0.0310191 * exp(-1.00761 * distance())) * pow((width1() + width2()) / 2 , 0.202766)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0310192 * exp(-1.00409 * distance()) + 0.0310191 * exp(-1.00761 * distance())) * pow((width1() + width2()) / 2 , 0.202766) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0310192 * exp(-1.00409 * distance()) + 0.0310191 * exp(-1.00761 * distance())) * pow((width1() + width2()) / 2 , 0.202766)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal4,metal7
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.000486717 * exp(0.0183266 * distance()) + 0.0636083 * exp(-0.93604 * distance())) * pow((width1() + width2()) / 2 , 0.211901) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.000486717 * exp(0.0183266 * distance()) + 0.0636083 * exp(-0.93604 * distance())) * pow((width1() + width2()) / 2 , 0.211901)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.000486717 * exp(0.0183266 * distance()) + 0.0636083 * exp(-0.93604 * distance())) * pow((width1() + width2()) / 2 , 0.211901) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.000486717 * exp(0.0183266 * distance()) + 0.0636083 * exp(-0.93604 * distance())) * pow((width1() + width2()) / 2 , 0.211901)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal4,metal8
//
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0612089 * exp(-0.945416 * distance()) + 0.00417638 * exp(-0.233585 * distance())) * pow((width1() + width2()) / 2 , 0.218459) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0612089 * exp(-0.945416 * distance()) + 0.00417638 * exp(-0.233585 * distance())) * pow((width1() + width2()) / 2 , 0.218459)
    }
]
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0612089 * exp(-0.945416 * distance()) + 0.00417638 * exp(-0.233585 * distance())) * pow((width1() + width2()) / 2 , 0.218459) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0612089 * exp(-0.945416 * distance()) + 0.00417638 * exp(-0.233585 * distance())) * pow((width1() + width2()) / 2 , 0.218459)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal5,metal6
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0138215 * exp(-0.68392 * distance()) + 0.0138213 * exp(-0.683853 * distance())) * pow((width1() + width2()) / 2 , 0.338532) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0138215 * exp(-0.68392 * distance()) + 0.0138213 * exp(-0.683853 * distance())) * pow((width1() + width2()) / 2 , 0.338532)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0138215 * exp(-0.68392 * distance()) + 0.0138213 * exp(-0.683853 * distance())) * pow((width1() + width2()) / 2 , 0.338532) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0138215 * exp(-0.68392 * distance()) + 0.0138213 * exp(-0.683853 * distance())) * pow((width1() + width2()) / 2 , 0.338532)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal5,metal7
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0155913 * exp(-0.577163 * distance()) + 0.0155913 * exp(-0.57707 * distance())) * pow((width1() + width2()) / 2 , 0.320923) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0155913 * exp(-0.577163 * distance()) + 0.0155913 * exp(-0.57707 * distance())) * pow((width1() + width2()) / 2 , 0.320923)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0155913 * exp(-0.577163 * distance()) + 0.0155913 * exp(-0.57707 * distance())) * pow((width1() + width2()) / 2 , 0.320923) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0155913 * exp(-0.577163 * distance()) + 0.0155913 * exp(-0.57707 * distance())) * pow((width1() + width2()) / 2 , 0.320923)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal5,metal8
//
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0164685 * exp(-0.511674 * distance()) + 0.0164685 * exp(-0.511237 * distance())) * pow((width1() + width2()) / 2 , 0.320291) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0164685 * exp(-0.511674 * distance()) + 0.0164685 * exp(-0.511237 * distance())) * pow((width1() + width2()) / 2 , 0.320291)
    }
]
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0164685 * exp(-0.511674 * distance()) + 0.0164685 * exp(-0.511237 * distance())) * pow((width1() + width2()) / 2 , 0.320291) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0164685 * exp(-0.511674 * distance()) + 0.0164685 * exp(-0.511237 * distance())) * pow((width1() + width2()) / 2 , 0.320291)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal6,metal7
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0085713 * exp(-0.488655 * distance()) + 0.00857141 * exp(-0.488778 * distance())) * pow((width1() + width2()) / 2 , 0.414948) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0085713 * exp(-0.488655 * distance()) + 0.00857141 * exp(-0.488778 * distance())) * pow((width1() + width2()) / 2 , 0.414948)
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0085713 * exp(-0.488655 * distance()) + 0.00857141 * exp(-0.488778 * distance())) * pow((width1() + width2()) / 2 , 0.414948) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0085713 * exp(-0.488655 * distance()) + 0.00857141 * exp(-0.488778 * distance())) * pow((width1() + width2()) / 2 , 0.414948)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal6,metal8
//
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0100819 * exp(-0.404936 * distance()) + 0.0100818 * exp(-0.404257 * distance())) * pow((width1() + width2()) / 2 , 0.375548) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0100819 * exp(-0.404936 * distance()) + 0.0100818 * exp(-0.404257 * distance())) * pow((width1() + width2()) / 2 , 0.375548)
    }
]
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0100819 * exp(-0.404936 * distance()) + 0.0100818 * exp(-0.404257 * distance())) * pow((width1() + width2()) / 2 , 0.375548) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0100819 * exp(-0.404936 * distance()) + 0.0100818 * exp(-0.404257 * distance())) * pow((width1() + width2()) / 2 , 0.375548)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal3,metal7,metal8
//
CAPACITANCE NEARBODY metal3 metal7 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00616015 * exp(-0.357142 * distance()) + 0.00616022 * exp(-0.356935 * distance())) * pow((width1() + width2()) / 2 , 0.442368) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00616015 * exp(-0.357142 * distance()) + 0.00616022 * exp(-0.356935 * distance())) * pow((width1() + width2()) / 2 , 0.442368)
    }
]
CAPACITANCE NEARBODY metal7 metal3 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00616015 * exp(-0.357142 * distance()) + 0.00616022 * exp(-0.356935 * distance())) * pow((width1() + width2()) / 2 , 0.442368) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00616015 * exp(-0.357142 * distance()) + 0.00616022 * exp(-0.356935 * distance())) * pow((width1() + width2()) / 2 , 0.442368)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal5,metal6
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0309953 * exp(-1.00866 * distance()) + 0.0309956 * exp(-1.00653 * distance())) * pow((width1() + width2()) / 2 , 0.204381) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0309953 * exp(-1.00866 * distance()) + 0.0309956 * exp(-1.00653 * distance())) * pow((width1() + width2()) / 2 , 0.204381)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal2 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0309953 * exp(-1.00866 * distance()) + 0.0309956 * exp(-1.00653 * distance())) * pow((width1() + width2()) / 2 , 0.204381) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0309953 * exp(-1.00866 * distance()) + 0.0309956 * exp(-1.00653 * distance())) * pow((width1() + width2()) / 2 , 0.204381)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal5,metal7
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00144387 * exp(-0.190981 * distance()) + 0.0662719 * exp(-0.868288 * distance())) * pow((width1() + width2()) / 2 , 0.202453) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00144387 * exp(-0.190981 * distance()) + 0.0662719 * exp(-0.868288 * distance())) * pow((width1() + width2()) / 2 , 0.202453)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00144387 * exp(-0.190981 * distance()) + 0.0662719 * exp(-0.868288 * distance())) * pow((width1() + width2()) / 2 , 0.202453) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00144387 * exp(-0.190981 * distance()) + 0.0662719 * exp(-0.868288 * distance())) * pow((width1() + width2()) / 2 , 0.202453)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal5,metal8
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0169235 * exp(-0.405197 * distance()) + 0.0537471 * exp(-0.929656 * distance())) * pow((width1() + width2()) / 2 , 0.207416) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0169235 * exp(-0.405197 * distance()) + 0.0537471 * exp(-0.929656 * distance())) * pow((width1() + width2()) / 2 , 0.207416)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0169235 * exp(-0.405197 * distance()) + 0.0537471 * exp(-0.929656 * distance())) * pow((width1() + width2()) / 2 , 0.207416) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0169235 * exp(-0.405197 * distance()) + 0.0537471 * exp(-0.929656 * distance())) * pow((width1() + width2()) / 2 , 0.207416)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal6,metal7
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0155689 * exp(-0.577923 * distance()) + 0.0155689 * exp(-0.577821 * distance())) * pow((width1() + width2()) / 2 , 0.322964) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0155689 * exp(-0.577923 * distance()) + 0.0155689 * exp(-0.577821 * distance())) * pow((width1() + width2()) / 2 , 0.322964)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0155689 * exp(-0.577923 * distance()) + 0.0155689 * exp(-0.577821 * distance())) * pow((width1() + width2()) / 2 , 0.322964) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0155689 * exp(-0.577923 * distance()) + 0.0155689 * exp(-0.577821 * distance())) * pow((width1() + width2()) / 2 , 0.322964)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal6,metal8
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0179429 * exp(-0.475579 * distance()) + 0.0179429 * exp(-0.476199 * distance())) * pow((width1() + width2()) / 2 , 0.293037) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0179429 * exp(-0.475579 * distance()) + 0.0179429 * exp(-0.476199 * distance())) * pow((width1() + width2()) / 2 , 0.293037)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0179429 * exp(-0.475579 * distance()) + 0.0179429 * exp(-0.476199 * distance())) * pow((width1() + width2()) / 2 , 0.293037) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0179429 * exp(-0.475579 * distance()) + 0.0179429 * exp(-0.476199 * distance())) * pow((width1() + width2()) / 2 , 0.293037)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal4,metal7,metal8
//
CAPACITANCE NEARBODY metal4 metal7 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.010393 * exp(-0.398892 * distance()) + 0.010393 * exp(-0.398654 * distance())) * pow((width1() + width2()) / 2 , 0.364713) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.010393 * exp(-0.398892 * distance()) + 0.010393 * exp(-0.398654 * distance())) * pow((width1() + width2()) / 2 , 0.364713)
    }
]
CAPACITANCE NEARBODY metal7 metal4 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.010393 * exp(-0.398892 * distance()) + 0.010393 * exp(-0.398654 * distance())) * pow((width1() + width2()) / 2 , 0.364713) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.010393 * exp(-0.398892 * distance()) + 0.010393 * exp(-0.398654 * distance())) * pow((width1() + width2()) / 2 , 0.364713)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal5,metal6,metal7
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0635765 * exp(-0.937871 * distance()) + 0.000451019 * exp(0.020483 * distance())) * pow((width1() + width2()) / 2 , 0.21389) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0635765 * exp(-0.937871 * distance()) + 0.000451019 * exp(0.020483 * distance())) * pow((width1() + width2()) / 2 , 0.21389)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal2 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0635765 * exp(-0.937871 * distance()) + 0.000451019 * exp(0.020483 * distance())) * pow((width1() + width2()) / 2 , 0.21389) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0635765 * exp(-0.937871 * distance()) + 0.000451019 * exp(0.020483 * distance())) * pow((width1() + width2()) / 2 , 0.21389)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal5,metal6,metal8
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189256 * exp(-0.416697 * distance()) + 0.0521119 * exp(-0.937108 * distance())) * pow((width1() + width2()) / 2 , 0.207461) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189256 * exp(-0.416697 * distance()) + 0.0521119 * exp(-0.937108 * distance())) * pow((width1() + width2()) / 2 , 0.207461)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189256 * exp(-0.416697 * distance()) + 0.0521119 * exp(-0.937108 * distance())) * pow((width1() + width2()) / 2 , 0.207461) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189256 * exp(-0.416697 * distance()) + 0.0521119 * exp(-0.937108 * distance())) * pow((width1() + width2()) / 2 , 0.207461)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal5,metal7,metal8
//
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0170459 * exp(-0.497871 * distance()) + 0.017046 * exp(-0.497658 * distance())) * pow((width1() + width2()) / 2 , 0.310656) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0170459 * exp(-0.497871 * distance()) + 0.017046 * exp(-0.497658 * distance())) * pow((width1() + width2()) / 2 , 0.310656)
    }
]
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0170459 * exp(-0.497871 * distance()) + 0.017046 * exp(-0.497658 * distance())) * pow((width1() + width2()) / 2 , 0.310656) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0170459 * exp(-0.497871 * distance()) + 0.017046 * exp(-0.497658 * distance())) * pow((width1() + width2()) / 2 , 0.310656)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal2,metal6,metal7,metal8
//
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.033457 * exp(-0.821775 * distance()) + 0.033457 * exp(-0.821398 * distance())) * pow((width1() + width2()) / 2 , 0.216128) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.033457 * exp(-0.821775 * distance()) + 0.033457 * exp(-0.821398 * distance())) * pow((width1() + width2()) / 2 , 0.216128)
    }
]
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF metal2 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.033457 * exp(-0.821775 * distance()) + 0.033457 * exp(-0.821398 * distance())) * pow((width1() + width2()) / 2 , 0.216128) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.033457 * exp(-0.821775 * distance()) + 0.033457 * exp(-0.821398 * distance())) * pow((width1() + width2()) / 2 , 0.216128)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal5,metal6
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0284929 * exp(-1.2027 * distance()) + 0.0284933 * exp(-1.19611 * distance())) * pow((width1() + width2()) / 2 , 0.192169) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0284929 * exp(-1.2027 * distance()) + 0.0284933 * exp(-1.19611 * distance())) * pow((width1() + width2()) / 2 , 0.192169)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal3 metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0284929 * exp(-1.2027 * distance()) + 0.0284933 * exp(-1.19611 * distance())) * pow((width1() + width2()) / 2 , 0.192169) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0284929 * exp(-1.2027 * distance()) + 0.0284933 * exp(-1.19611 * distance())) * pow((width1() + width2()) / 2 , 0.192169)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal5,metal7
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal3 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0310188 * exp(-0.998245 * distance()) + 0.0310186 * exp(-1.01345 * distance())) * pow((width1() + width2()) / 2 , 0.202754) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0310188 * exp(-0.998245 * distance()) + 0.0310186 * exp(-1.01345 * distance())) * pow((width1() + width2()) / 2 , 0.202754)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal3 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0310188 * exp(-0.998245 * distance()) + 0.0310186 * exp(-1.01345 * distance())) * pow((width1() + width2()) / 2 , 0.202754) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0310188 * exp(-0.998245 * distance()) + 0.0310186 * exp(-1.01345 * distance())) * pow((width1() + width2()) / 2 , 0.202754)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal5,metal8
//
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.000938781 * exp(-0.0769913 * distance()) + 0.0634229 * exp(-0.93466 * distance())) * pow((width1() + width2()) / 2 , 0.213235) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.000938781 * exp(-0.0769913 * distance()) + 0.0634229 * exp(-0.93466 * distance())) * pow((width1() + width2()) / 2 , 0.213235)
    }
]
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.000938781 * exp(-0.0769913 * distance()) + 0.0634229 * exp(-0.93466 * distance())) * pow((width1() + width2()) / 2 , 0.213235) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.000938781 * exp(-0.0769913 * distance()) + 0.0634229 * exp(-0.93466 * distance())) * pow((width1() + width2()) / 2 , 0.213235)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal6,metal7
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal3 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0138215 * exp(-0.68392 * distance()) + 0.0138213 * exp(-0.683853 * distance())) * pow((width1() + width2()) / 2 , 0.338532) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0138215 * exp(-0.68392 * distance()) + 0.0138213 * exp(-0.683853 * distance())) * pow((width1() + width2()) / 2 , 0.338532)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal3 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0138215 * exp(-0.68392 * distance()) + 0.0138213 * exp(-0.683853 * distance())) * pow((width1() + width2()) / 2 , 0.338532) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0138215 * exp(-0.68392 * distance()) + 0.0138213 * exp(-0.683853 * distance())) * pow((width1() + width2()) / 2 , 0.338532)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal6,metal8
//
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0157868 * exp(-0.562995 * distance()) + 0.0157868 * exp(-0.563574 * distance())) * pow((width1() + width2()) / 2 , 0.320528) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0157868 * exp(-0.562995 * distance()) + 0.0157868 * exp(-0.563574 * distance())) * pow((width1() + width2()) / 2 , 0.320528)
    }
]
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0157868 * exp(-0.562995 * distance()) + 0.0157868 * exp(-0.563574 * distance())) * pow((width1() + width2()) / 2 , 0.320528) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0157868 * exp(-0.562995 * distance()) + 0.0157868 * exp(-0.563574 * distance())) * pow((width1() + width2()) / 2 , 0.320528)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal3,metal4,metal7,metal8
//
CAPACITANCE NEARBODY metal4 metal7 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00897037 * exp(-0.46907 * distance()) + 0.00897024 * exp(-0.468767 * distance())) * pow((width1() + width2()) / 2 , 0.401396) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00897037 * exp(-0.46907 * distance()) + 0.00897024 * exp(-0.468767 * distance())) * pow((width1() + width2()) / 2 , 0.401396)
    }
]
CAPACITANCE NEARBODY metal7 metal4 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00897037 * exp(-0.46907 * distance()) + 0.00897024 * exp(-0.468767 * distance())) * pow((width1() + width2()) / 2 , 0.401396) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00897037 * exp(-0.46907 * distance()) + 0.00897024 * exp(-0.468767 * distance())) * pow((width1() + width2()) / 2 , 0.401396)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal3,metal5,metal6,metal7
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal3 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0309955 * exp(-1.00667 * distance()) + 0.0309953 * exp(-1.00852 * distance())) * pow((width1() + width2()) / 2 , 0.204381) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0309955 * exp(-1.00667 * distance()) + 0.0309953 * exp(-1.00852 * distance())) * pow((width1() + width2()) / 2 , 0.204381)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal3 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0309955 * exp(-1.00667 * distance()) + 0.0309953 * exp(-1.00852 * distance())) * pow((width1() + width2()) / 2 , 0.204381) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0309955 * exp(-1.00667 * distance()) + 0.0309953 * exp(-1.00852 * distance())) * pow((width1() + width2()) / 2 , 0.204381)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal3,metal5,metal6,metal8
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0590418 * exp(-0.905918 * distance()) + 0.00934609 * exp(-0.468332 * distance())) * pow((width1() + width2()) / 2 , 0.203453) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0590418 * exp(-0.905918 * distance()) + 0.00934609 * exp(-0.468332 * distance())) * pow((width1() + width2()) / 2 , 0.203453)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0590418 * exp(-0.905918 * distance()) + 0.00934609 * exp(-0.468332 * distance())) * pow((width1() + width2()) / 2 , 0.203453) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0590418 * exp(-0.905918 * distance()) + 0.00934609 * exp(-0.468332 * distance())) * pow((width1() + width2()) / 2 , 0.203453)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal3,metal5,metal7,metal8
//
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0162185 * exp(-0.552837 * distance()) + 0.0162184 * exp(-0.552643 * distance())) * pow((width1() + width2()) / 2 , 0.312338) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0162185 * exp(-0.552837 * distance()) + 0.0162184 * exp(-0.552643 * distance())) * pow((width1() + width2()) / 2 , 0.312338)
    }
]
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0162185 * exp(-0.552837 * distance()) + 0.0162184 * exp(-0.552643 * distance())) * pow((width1() + width2()) / 2 , 0.312338) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0162185 * exp(-0.552837 * distance()) + 0.0162184 * exp(-0.552643 * distance())) * pow((width1() + width2()) / 2 , 0.312338)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal3,metal6,metal7,metal8
//
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0329613 * exp(-0.870007 * distance()) + 0.0329613 * exp(-0.870007 * distance())) * pow((width1() + width2()) / 2 , 0.210958) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0329613 * exp(-0.870007 * distance()) + 0.0329613 * exp(-0.870007 * distance())) * pow((width1() + width2()) / 2 , 0.210958)
    }
]
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF metal3 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0329613 * exp(-0.870007 * distance()) + 0.0329613 * exp(-0.870007 * distance())) * pow((width1() + width2()) / 2 , 0.210958) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0329613 * exp(-0.870007 * distance()) + 0.0329613 * exp(-0.870007 * distance())) * pow((width1() + width2()) / 2 , 0.210958)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal4,metal5,metal6,metal7
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal4 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0284943 * exp(-1.19964 * distance()) + 0.0284944 * exp(-1.1996 * distance())) * pow((width1() + width2()) / 2 , 0.192176) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0284943 * exp(-1.19964 * distance()) + 0.0284944 * exp(-1.1996 * distance())) * pow((width1() + width2()) / 2 , 0.192176)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal4 metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0284943 * exp(-1.19964 * distance()) + 0.0284944 * exp(-1.1996 * distance())) * pow((width1() + width2()) / 2 , 0.192176) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0284943 * exp(-1.19964 * distance()) + 0.0284944 * exp(-1.1996 * distance())) * pow((width1() + width2()) / 2 , 0.192176)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal4,metal5,metal6,metal8
//
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF metal4 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0312745 * exp(-0.98375 * distance()) + 0.0312745 * exp(-0.983762 * distance())) * pow((width1() + width2()) / 2 , 0.205014) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0312745 * exp(-0.98375 * distance()) + 0.0312745 * exp(-0.983762 * distance())) * pow((width1() + width2()) / 2 , 0.205014)
    }
]
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF metal4 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0312745 * exp(-0.98375 * distance()) + 0.0312745 * exp(-0.983762 * distance())) * pow((width1() + width2()) / 2 , 0.205014) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0312745 * exp(-0.98375 * distance()) + 0.0312745 * exp(-0.983762 * distance())) * pow((width1() + width2()) / 2 , 0.205014)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal4,metal5,metal7,metal8
//
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF metal4 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.014372 * exp(-0.653315 * distance()) + 0.0143716 * exp(-0.653304 * distance())) * pow((width1() + width2()) / 2 , 0.331131) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.014372 * exp(-0.653315 * distance()) + 0.0143716 * exp(-0.653304 * distance())) * pow((width1() + width2()) / 2 , 0.331131)
    }
]
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF metal4 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.014372 * exp(-0.653315 * distance()) + 0.0143716 * exp(-0.653304 * distance())) * pow((width1() + width2()) / 2 , 0.331131) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.014372 * exp(-0.653315 * distance()) + 0.0143716 * exp(-0.653304 * distance())) * pow((width1() + width2()) / 2 , 0.331131)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal4,metal6,metal7,metal8
//
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF metal4 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.031945 * exp(-0.951611 * distance()) + 0.0319443 * exp(-0.957857 * distance())) * pow((width1() + width2()) / 2 , 0.203014) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.031945 * exp(-0.951611 * distance()) + 0.0319443 * exp(-0.957857 * distance())) * pow((width1() + width2()) / 2 , 0.203014)
    }
]
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF metal4 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.031945 * exp(-0.951611 * distance()) + 0.0319443 * exp(-0.957857 * distance())) * pow((width1() + width2()) / 2 , 0.203014) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.031945 * exp(-0.951611 * distance()) + 0.0319443 * exp(-0.957857 * distance())) * pow((width1() + width2()) / 2 , 0.203014)
    }
]
//
//  model: 414  target: nearbody2  profile: field  layers: field_base,metal5,metal6,metal7,metal8
//
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF metal5 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0293612 * exp(-1.13647 * distance()) + 0.0293661 * exp(-1.13652 * distance())) * pow((width1() + width2()) / 2 , 0.194913) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0293612 * exp(-1.13647 * distance()) + 0.0293661 * exp(-1.13652 * distance())) * pow((width1() + width2()) / 2 , 0.194913)
    }
]
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF metal5 metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0293612 * exp(-1.13647 * distance()) + 0.0293661 * exp(-1.13652 * distance())) * pow((width1() + width2()) / 2 , 0.194913) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0293612 * exp(-1.13647 * distance()) + 0.0293661 * exp(-1.13652 * distance())) * pow((width1() + width2()) / 2 , 0.194913)
    }
]
//
//  model: 2013  target: viacap  profile: field  layers: field_base,polyCont,poly,metal1
//
CAPACITANCE CONNECTION poly metal1 MASK
[
    PROPERTY C
    C = 0.009453
]
//
//  model: 2013  target: viacap  profile: field  layers: field_base,via1,metal1,metal2
//
CAPACITANCE CONNECTION metal1 metal2 MASK
[
    PROPERTY C
    C = 0.0160137
]
//
//  model: 2013  target: viacap  profile: field  layers: field_base,via2,metal2,metal3
//
CAPACITANCE CONNECTION metal2 metal3 MASK
[
    PROPERTY C
    C = 0.0148582
]
//
//  model: 2013  target: viacap  profile: field  layers: field_base,via3,metal3,metal4
//
CAPACITANCE CONNECTION metal3 metal4 MASK
[
    PROPERTY C
    C = 0.0148401
]
//
//  model: 2013  target: viacap  profile: field  layers: field_base,via4,metal4,metal5
//
CAPACITANCE CONNECTION metal4 metal5 MASK
[
    PROPERTY C
    C = 0.0148255
]
//
//  model: 2013  target: viacap  profile: field  layers: field_base,via5,metal5,metal6
//
CAPACITANCE CONNECTION metal5 metal6 MASK
[
    PROPERTY C
    C = 0.0148176
]
//
//  model: 2013  target: viacap  profile: field  layers: field_base,via6,metal6,metal7
//
CAPACITANCE CONNECTION metal6 metal7 MASK
[
    PROPERTY C
    C = 0.0148118
]
//
//  model: 2013  target: viacap  profile: field  layers: field_base,via7,metal7,metal8
//
CAPACITANCE CONNECTION metal7 metal8 MASK
[
    PROPERTY C
    C = 0.0252446
]
//
//  model: 514  target: nearbody514  profile: field  layers: field_base,poly,metal1,metal2,metal3,metal4,metal5,metal6,metal7,metal8
//
VARIABLE m514k1 0.903495
VARIABLE m514k2 0.918214
//
//  model: 43  target: nearbody43  profile: field  layers: field_base,poly,metal1,metal2,metal3,metal4,metal5,metal6,metal7,metal8
//
VARIABLE m43nbk1 0.0982944
VARIABLE m43nbk2 0.271266
VARIABLE m43nbk3 0.497083
VARIABLE m43nbk4 0.0596403
//
//  model: 43  target: intrinsic43  profile: field  layers: field_base,poly,metal1,metal2,metal3,metal4,metal5,metal6,metal7,metal8
//
VARIABLE m43ink1 0.407183
VARIABLE m43ink2 0.233743
VARIABLE m43ink3 0.20086
VARIABLE m43ink4 0.53512
#endif // XCALIBRATE_EXCLUDE_PROFILE_field
#ifndef XCALIBRATE_EXCLUDE_PROFILE_active
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal1,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal1 tndiff MASK
[
    PROPERTY C
    C = 0.0805008 * area()
]
CAPACITANCE CROSSOVER PLATE metal1 tpdiff MASK
[
    PROPERTY C
    C = 0.0805008 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal2,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal2 tndiff MASK
[
    PROPERTY C
    C = 0.0306927 * area()
]
CAPACITANCE CROSSOVER PLATE metal2 tpdiff MASK
[
    PROPERTY C
    C = 0.0306927 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal3 tndiff MASK
[
    PROPERTY C
    C = 0.0178128 * area()
]
CAPACITANCE CROSSOVER PLATE metal3 tpdiff MASK
[
    PROPERTY C
    C = 0.0178128 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal4 tndiff MASK
[
    PROPERTY C
    C = 0.0125474 * area()
]
CAPACITANCE CROSSOVER PLATE metal4 tpdiff MASK
[
    PROPERTY C
    C = 0.0125474 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal5 tndiff MASK
[
    PROPERTY C
    C = 0.00968462 * area()
]
CAPACITANCE CROSSOVER PLATE metal5 tpdiff MASK
[
    PROPERTY C
    C = 0.00968462 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal6 tndiff MASK
[
    PROPERTY C
    C = 0.0078855 * area()
]
CAPACITANCE CROSSOVER PLATE metal6 tpdiff MASK
[
    PROPERTY C
    C = 0.0078855 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal7,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal7 tndiff MASK
[
    PROPERTY C
    C = 0.0066501 * area()
]
CAPACITANCE CROSSOVER PLATE metal7 tpdiff MASK
[
    PROPERTY C
    C = 0.0066501 * area()
]
//
//  model: 2  target: intrinsicp  profile: active  layers: active_base,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER PLATE metal8 tndiff MASK
[
    PROPERTY C
    C = 0.00560644 * area()
]
CAPACITANCE CROSSOVER PLATE metal8 tpdiff MASK
[
    PROPERTY C
    C = 0.00560644 * area()
]
//
//  model: 34  target: intrinsic  profile: active  layers: active_base,metal1,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.212726 * (1 - exp(-1.01058 * (distance() + 0.094385))) * pow(width() , 0.00216927 * distance() + 0.027073) * (0.0719998 * thickness() + 0.22002) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0483933 * (1 - exp(-1.0021 * (distance() + 0.046282))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.186506 * pow(width() , 0.0674169) * (0.148913 * thickness() + 0.249396) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.212726 * (1 - exp(-1.01058 * (distance() + 0.094385))) * pow(width() , 0.00216927 * distance() + 0.027073) * (0.0719998 * thickness() + 0.22002) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0483933 * (1 - exp(-1.0021 * (distance() + 0.046282))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.186506 * pow(width() , 0.0674169) * (0.148913 * thickness() + 0.249396) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
//
//  model: 34  target: nearbody  profile: active  layers: active_base,metal1,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.08052 - 1.50086 * distance()) + 0.108376 / pow(distance() , 1.06631)) * 3.50408 * pow((width1() + width2()) / 2 , 0.0977623) * (0.067084 * (thickness1() + thickness2()) / 2 + 0.018033) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.08052 - 1.50086 * distance()) + 0.108376 / pow(distance() , 1.06631)) * 3.50408 * pow((width1() + width2()) / 2 , 0.0977623) * (0.067084 * (thickness1() + thickness2()) / 2 + 0.018033) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
//
//  model: 34  target: intrinsic  profile: active  layers: active_base,metal2,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.187199 * (1 - exp(-0.475671 * (distance() + 0.283475))) * pow(width() , 0.00617311 * distance() + 0.00798002) * (0.0369789 * thickness() + 0.203943) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0385439 * (1 - exp(-0.497324 * (distance() + 0.121776))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.163931 * pow(width() , 0.0782794) * (0.082134 * thickness() + 0.231739) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.18)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.187199 * (1 - exp(-0.475671 * (distance() + 0.283475))) * pow(width() , 0.00617311 * distance() + 0.00798002) * (0.0369789 * thickness() + 0.203943) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0385439 * (1 - exp(-0.497324 * (distance() + 0.121776))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.163931 * pow(width() , 0.0782794) * (0.082134 * thickness() + 0.231739) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.18)))
    }
]
//
//  model: 34  target: nearbody  profile: active  layers: active_base,metal2,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.57895 - 0.748601 * distance()) + 0.1504 / pow(distance() , 0.969384)) * 5.03759 * pow((width1() + width2()) / 2 , 0.1098) * (0.0352656 * (thickness1() + thickness2()) / 2 + 0.0163057) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.18)))
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.57895 - 0.748601 * distance()) + 0.1504 / pow(distance() , 0.969384)) * 5.03759 * pow((width1() + width2()) / 2 , 0.1098) * (0.0352656 * (thickness1() + thickness2()) / 2 + 0.0163057) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.18)))
]
//
//  model: 34  target: intrinsic  profile: active  layers: active_base,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.168317 * (1 - exp(-0.313262 * (distance() + 0.504424))) * pow(width() , 0.00956812 * distance() + -0.0195504) * (0.0283651 * thickness() + 0.198811) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0329384 * (1 - exp(-0.342577 * (distance() + 0.193654))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.153445 * pow(width() , 0.0810344) * (0.0681131 * thickness() + 0.216699) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.168317 * (1 - exp(-0.313262 * (distance() + 0.504424))) * pow(width() , 0.00956812 * distance() + -0.0195504) * (0.0283651 * thickness() + 0.198811) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0329384 * (1 - exp(-0.342577 * (distance() + 0.193654))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.153445 * pow(width() , 0.0810344) * (0.0681131 * thickness() + 0.216699) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2)))
    }
]
//
//  model: 34  target: nearbody  profile: active  layers: active_base,metal3,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.49425 - 0.486911 * distance()) + 0.242602 / pow(distance() , 0.903223)) * 5.60792 * pow((width1() + width2()) / 2 , 0.124162) * (0.0223946 * (thickness1() + thickness2()) / 2 + 0.0113851) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2)))
]
CAPACITANCE NEARBODY metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.49425 - 0.486911 * distance()) + 0.242602 / pow(distance() , 0.903223)) * 5.60792 * pow((width1() + width2()) / 2 , 0.124162) * (0.0223946 * (thickness1() + thickness2()) / 2 + 0.0113851) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2)))
]
//
//  model: 34  target: intrinsic  profile: active  layers: active_base,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.158481 * (1 - exp(-0.236263 * (distance() + 0.732724))) * pow(width() , 0.010978 * distance() + -0.0363309) * (0.0218889 * thickness() + 0.194066) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0297944 * (1 - exp(-0.263455 * (distance() + 0.264625))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.146973 * pow(width() , 0.0808469) * (0.0594856 * thickness() + 0.206773) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.82)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.158481 * (1 - exp(-0.236263 * (distance() + 0.732724))) * pow(width() , 0.010978 * distance() + -0.0363309) * (0.0218889 * thickness() + 0.194066) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0297944 * (1 - exp(-0.263455 * (distance() + 0.264625))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.146973 * pow(width() , 0.0808469) * (0.0594856 * thickness() + 0.206773) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.82)))
    }
]
//
//  model: 34  target: nearbody  profile: active  layers: active_base,metal4,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.0595 - 0.348754 * distance()) + 0.467676 / pow(distance() , 0.86866)) * 5.55694 * pow((width1() + width2()) / 2 , 0.132168) * (0.0125321 * (thickness1() + thickness2()) / 2 + 0.00668301) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.82)))
]
CAPACITANCE NEARBODY metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.0595 - 0.348754 * distance()) + 0.467676 / pow(distance() , 0.86866)) * 5.55694 * pow((width1() + width2()) / 2 , 0.132168) * (0.0125321 * (thickness1() + thickness2()) / 2 + 0.00668301) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.82)))
]
//
//  model: 34  target: intrinsic  profile: active  layers: active_base,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal5 tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.150767 * (1 - exp(-0.187436 * (distance() + 0.970812))) * pow(width() , 0.011723 * distance() + -0.0488262) * (0.018145 * thickness() + 0.19189) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.027741 * (1 - exp(-0.213292 * (distance() + 0.328512))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.143264 * pow(width() , 0.0791172) * (0.0523133 * thickness() + 0.19741) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.64)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal5 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.150767 * (1 - exp(-0.187436 * (distance() + 0.970812))) * pow(width() , 0.011723 * distance() + -0.0488262) * (0.018145 * thickness() + 0.19189) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.027741 * (1 - exp(-0.213292 * (distance() + 0.328512))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.143264 * pow(width() , 0.0791172) * (0.0523133 * thickness() + 0.19741) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.64)))
    }
]
//
//  model: 34  target: nearbody  profile: active  layers: active_base,metal5,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.34423 - 0.26716 * distance()) + 1.03157 / pow(distance() , 0.855338)) * 5.79932 * pow((width1() + width2()) / 2 , 0.136968) * (0.00555182 * (thickness1() + thickness2()) / 2 + 0.00305236) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.64)))
]
CAPACITANCE NEARBODY metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-0.34423 - 0.26716 * distance()) + 1.03157 / pow(distance() , 0.855338)) * 5.79932 * pow((width1() + width2()) / 2 , 0.136968) * (0.00555182 * (thickness1() + thickness2()) / 2 + 0.00305236) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.64)))
]
//
//  model: 34  target: intrinsic  profile: active  layers: active_base,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal6 tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.148758 * (1 - exp(-0.152392 * (distance() + 1.21441))) * pow(width() , 0.0121881 * distance() + -0.0596583) * (0.0121228 * thickness() + 0.187278) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.46)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0264175 * (1 - exp(-0.176804 * (distance() + 0.381723))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.46)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.139477 * pow(width() , 0.0764389) * (0.0465846 * thickness() + 0.190434) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.46)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal6 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.148758 * (1 - exp(-0.152392 * (distance() + 1.21441))) * pow(width() , 0.0121881 * distance() + -0.0596583) * (0.0121228 * thickness() + 0.187278) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.46)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0264175 * (1 - exp(-0.176804 * (distance() + 0.381723))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.46)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.139477 * pow(width() , 0.0764389) * (0.0465846 * thickness() + 0.190434) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.46)))
    }
]
//
//  model: 34  target: nearbody  profile: active  layers: active_base,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(0.631766 - 0.219738 * distance()) + 2.61758 / pow(distance() , 0.857577)) * 6.11991 * pow((width1() + width2()) / 2 , 0.140633) * (0.00204178 * (thickness1() + thickness2()) / 2 + 0.00114655) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.46)))
]
CAPACITANCE NEARBODY metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(0.631766 - 0.219738 * distance()) + 2.61758 / pow(distance() , 0.857577)) * 6.11991 * pow((width1() + width2()) / 2 , 0.140633) * (0.00204178 * (thickness1() + thickness2()) / 2 + 0.00114655) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.46)))
]
//
//  model: 34  target: intrinsic  profile: active  layers: active_base,metal7,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal7 tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.157757 * (1 - exp(-0.112393 * (distance() + 1.49347))) * pow(width() , 0.0140705 * distance() + -0.0820668) * (0.00745502 * thickness() + 0.183814) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.28)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0260251 * (1 - exp(-0.146136 * (distance() + 0.403534))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.28)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.136773 * pow(width() , 0.0708816) * (0.0403966 * thickness() + 0.184698) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 5.28)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal7 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.157757 * (1 - exp(-0.112393 * (distance() + 1.49347))) * pow(width() , 0.0140705 * distance() + -0.0820668) * (0.00745502 * thickness() + 0.183814) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.28)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0260251 * (1 - exp(-0.146136 * (distance() + 0.403534))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.28)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.136773 * pow(width() , 0.0708816) * (0.0403966 * thickness() + 0.184698) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 5.28)))
    }
]
//
//  model: 34  target: nearbody  profile: active  layers: active_base,metal7,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal7 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(1.21648 - 0.2039 * distance()) + 4.29288 / pow(distance() , 0.853698)) * 6.18198 * pow((width1() + width2()) / 2 , 0.145764) * (0.0012097 * (thickness1() + thickness2()) / 2 + 0.00072218) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 5.28)))
]
CAPACITANCE NEARBODY metal7 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(1.21648 - 0.2039 * distance()) + 4.29288 / pow(distance() , 0.853698)) * 6.18198 * pow((width1() + width2()) / 2 , 0.145764) * (0.0012097 * (thickness1() + thickness2()) / 2 + 0.00072218) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 5.28)))
]
//
//  model: 34  target: intrinsic  profile: active  layers: active_base,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal8 tndiff MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.191287 * (1 - exp(-0.0690385 * (distance() + 1.17923))) * pow(width() , 0.0293862 * distance() + -0.207662) * (0.0002799 * thickness() + 0.196613) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 6.32)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0323063 * (1 - exp(-0.0933457 * (distance() + 0.206449))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 6.32)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.117172 * pow(width() , 0.0398831) * (0.0293332 * thickness() + 0.204579) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 6.32)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal8 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.191287 * (1 - exp(-0.0690385 * (distance() + 1.17923))) * pow(width() , 0.0293862 * distance() + -0.207662) * (0.0002799 * thickness() + 0.196613) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 6.32)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0323063 * (1 - exp(-0.0933457 * (distance() + 0.206449))) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 6.32)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.117172 * pow(width() , 0.0398831) * (0.0293332 * thickness() + 0.204579) * (1 - m34RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.38 + m43ink4 * 6.32)))
    }
]
//
//  model: 34  target: nearbody  profile: active  layers: active_base,metal8,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    C = length() * (exp(-1.09825 - 0.248149 * distance()) + 0.635936 / pow(distance() , 0.876879)) * 4.35229 * pow((width1() + width2()) / 2 , 0.108561) * (0.010733 * (thickness1() + thickness2()) / 2 + 0.0104756) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 6.32)))
]
CAPACITANCE NEARBODY metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 2.64 // For reference
    max_caldistance = 8.38 // For reference
    max_width = MAXWIDTH_metal8
    max_radius = MAXRADIUS_metal8
    max_distance = MAXDISTANCE_metal8
    C = length() * (exp(-1.09825 - 0.248149 * distance()) + 0.635936 / pow(distance() , 0.876879)) * 4.35229 * pow((width1() + width2()) / 2 , 0.108561) * (0.010733 * (thickness1() + thickness2()) / 2 + 0.0104756) * (1 - m34RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 6.32)))
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal2,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-0.936146 - 2.69882 * distance()) + 0.00851365 / pow(distance() , 1.70079)) * 3.9685 * pow((width1() + width2()) / 2 , 0.0212111) * (0.13698 * (thickness1() + thickness2()) / 2 + 0.025578) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-0.936146 - 2.69882 * distance()) + 0.00851365 / pow(distance() , 1.70079)) * 3.9685 * pow((width1() + width2()) / 2 , 0.0212111) * (0.13698 * (thickness1() + thickness2()) / 2 + 0.025578) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal2,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal2 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.147881 * (1 - exp(-1.81742 * (distance() + -0.0397494))) * pow(width() , 0.00266029 * distance() + -0.0176704) * (0.127279 * thickness() + 0.154965)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0279925 * (1 - exp(-1.90813 * (distance() + -0.0464048)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.136896 * pow(width() , 0) * (0.184427 * thickness() + 0.153614)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00155298 * distance() + 0.27276) * (0.208217 - exp(-(-0.559304 * distance() + 0.460716) * (enclose() + 2.3188 * distance() + 2.31398) - (0.0029097 * distance() + 0.183303) * (enclose() + 2.38391 * distance() + 1.34011) * (enclose() + 2.38391 * distance() + 1.34011)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.147881 * (1 - exp(-1.81742 * (distance() + -0.0397494))) * pow(width() , 0.00266029 * distance() + -0.0176704) * (0.127279 * thickness() + 0.154965)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.13004 * (0.208217 - exp(4.106 * (enclose() + 21.247) - 0.207061 * (enclose() + 20.8047) * (enclose() + 20.8047)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal2 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.147881 * (1 - exp(-1.81742 * (distance() + -0.0397494))) * pow(width() , 0.00266029 * distance() + -0.0176704) * (0.127279 * thickness() + 0.154965)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0279925 * (1 - exp(-1.90813 * (distance() + -0.0464048)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.136896 * pow(width() , 0) * (0.184427 * thickness() + 0.153614)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00155298 * distance() + 0.27276) * (0.208217 - exp(-(-0.559304 * distance() + 0.460716) * (enclose() + 2.3188 * distance() + 2.31398) - (0.0029097 * distance() + 0.183303) * (enclose() + 2.38391 * distance() + 1.34011) * (enclose() + 2.38391 * distance() + 1.34011)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.147881 * (1 - exp(-1.81742 * (distance() + -0.0397494))) * pow(width() , 0.00266029 * distance() + -0.0176704) * (0.127279 * thickness() + 0.154965)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.13004 * (0.208217 - exp(4.106 * (enclose() + 21.247) - 0.207061 * (enclose() + 20.8047) * (enclose() + 20.8047)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal2,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.126516 * (1 - exp(-1.80919 * (distance() + -0.0378973))) * pow(width() , 0.00267868 * distance() + -0.0164242) * (0.115934 * thickness() + 0.191389) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.02808 * (1 - exp(-1.90363 * (distance() + -0.0466862))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.139519 * pow(width() , -0.00315507) * (0.154574 * thickness() + 0.159142) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.126516 * (1 - exp(-1.80919 * (distance() + -0.0378973))) * pow(width() , 0.00267868 * distance() + -0.0164242) * (0.115934 * thickness() + 0.191389) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.02808 * (1 - exp(-1.90363 * (distance() + -0.0466862))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.139519 * pow(width() , -0.00315507) * (0.154574 * thickness() + 0.159142) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal3,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-0.95711 - 1.94486 * distance()) + 0.0209432 / pow(distance() , 1.46743)) * 3.94665 * pow((width1() + width2()) / 2 , 0.057799) * (0.10321 * (thickness1() + thickness2()) / 2 + 0.0248222) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-0.95711 - 1.94486 * distance()) + 0.0209432 / pow(distance() , 1.46743)) * 3.94665 * pow((width1() + width2()) / 2 , 0.057799) * (0.10321 * (thickness1() + thickness2()) / 2 + 0.0248222) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0743879 * (1 - exp(-1.14346 * (distance() + 0.00212611))) * pow(width() , 0.00633951 * distance() + -0.0189494) * (0.165061 * thickness() + 0.15673)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.014551 * (1 - exp(-1.26204 * (distance() + -0.0344863)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.104224 * pow(width() , 0.0222908) * (0.150272 * thickness() + 0.103069)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00065765 * distance() + 0.0518421) * (0.584703 - exp(-(-0.12946 * distance() + 0.235156) * (enclose() + 2.49926 * distance() + 1.42583) - (0.00389356 * distance() + 0.115845) * (enclose() + 1.3347 * distance() + 0.480789) * (enclose() + 1.3347 * distance() + 0.480789)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0743879 * (1 - exp(-1.14346 * (distance() + 0.00212611))) * pow(width() , 0.00633951 * distance() + -0.0189494) * (0.165061 * thickness() + 0.15673)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0232362 * (0.584703 - exp(0.821885 * (enclose() + 21.8323) - 0.147636 * (enclose() + 11.3786) * (enclose() + 11.3786)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0743879 * (1 - exp(-1.14346 * (distance() + 0.00212611))) * pow(width() , 0.00633951 * distance() + -0.0189494) * (0.165061 * thickness() + 0.15673)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.014551 * (1 - exp(-1.26204 * (distance() + -0.0344863)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.104224 * pow(width() , 0.0222908) * (0.150272 * thickness() + 0.103069)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00065765 * distance() + 0.0518421) * (0.584703 - exp(-(-0.12946 * distance() + 0.235156) * (enclose() + 2.49926 * distance() + 1.42583) - (0.00389356 * distance() + 0.115845) * (enclose() + 1.3347 * distance() + 0.480789) * (enclose() + 1.3347 * distance() + 0.480789)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0743879 * (1 - exp(-1.14346 * (distance() + 0.00212611))) * pow(width() , 0.00633951 * distance() + -0.0189494) * (0.165061 * thickness() + 0.15673)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0232362 * (0.584703 - exp(0.821885 * (enclose() + 21.8323) - 0.147636 * (enclose() + 11.3786) * (enclose() + 11.3786)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.156515 * (1 - exp(-1.23946 * (distance() + 0.00244296))) * pow(width() , 0.00487441 * distance() + -0.0212807) * (0.0812271 * thickness() + 0.208918) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0357503 * (1 - exp(-1.3351 * (distance() + -0.0254057))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.160454 * pow(width() , 0.0115485) * (0.121262 * thickness() + 0.192997) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.156515 * (1 - exp(-1.23946 * (distance() + 0.00244296))) * pow(width() , 0.00487441 * distance() + -0.0212807) * (0.0812271 * thickness() + 0.208918) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0357503 * (1 - exp(-1.3351 * (distance() + -0.0254057))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.160454 * pow(width() , 0.0115485) * (0.121262 * thickness() + 0.192997) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal4,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.01501 - 1.70539 * distance()) + 0.0331522 / pow(distance() , 1.34075)) * 3.92159 * pow((width1() + width2()) / 2 , 0.0757151) * (0.0928399 * (thickness1() + thickness2()) / 2 + 0.0236193) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.01501 - 1.70539 * distance()) + 0.0331522 / pow(distance() , 1.34075)) * 3.92159 * pow((width1() + width2()) / 2 , 0.0757151) * (0.0928399 * (thickness1() + thickness2()) / 2 + 0.0236193) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0526493 * (1 - exp(-0.977522 * (distance() + 0.0385833))) * pow(width() , 0.00782466 * distance() + -0.0180208) * (0.176246 * thickness() + 0.152107)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0100846 * (1 - exp(-1.09291 * (distance() + -0.0179402)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0903841 * pow(width() , 0.0376803) * (0.130653 * thickness() + 0.0818493)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (6.69899e-05 * distance() + 0.0309987) * (0.608353 - exp(-(0.0217938 * distance() + 0.190911) * (enclose() + 0.166314 * distance() + 1.61104) - (0.0562991 * distance() + 0.00584622) * (enclose() + -0.255795 * distance() + 2.3853) * (enclose() + -0.255795 * distance() + 2.3853)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0526493 * (1 - exp(-0.977522 * (distance() + 0.0385833))) * pow(width() , 0.00782466 * distance() + -0.0180208) * (0.176246 * thickness() + 0.152107)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0157728 * (0.608353 - exp(-0.368857 * (enclose() + 2.96899) - 0.465528 * (enclose() + 0.296734) * (enclose() + 0.296734)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0526493 * (1 - exp(-0.977522 * (distance() + 0.0385833))) * pow(width() , 0.00782466 * distance() + -0.0180208) * (0.176246 * thickness() + 0.152107)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0100846 * (1 - exp(-1.09291 * (distance() + -0.0179402)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0903841 * pow(width() , 0.0376803) * (0.130653 * thickness() + 0.0818493)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (6.69899e-05 * distance() + 0.0309987) * (0.608353 - exp(-(0.0217938 * distance() + 0.190911) * (enclose() + 0.166314 * distance() + 1.61104) - (0.0562991 * distance() + 0.00584622) * (enclose() + -0.255795 * distance() + 2.3853) * (enclose() + -0.255795 * distance() + 2.3853)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0526493 * (1 - exp(-0.977522 * (distance() + 0.0385833))) * pow(width() , 0.00782466 * distance() + -0.0180208) * (0.176246 * thickness() + 0.152107)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0157728 * (0.608353 - exp(-0.368857 * (enclose() + 2.96899) - 0.465528 * (enclose() + 0.296734) * (enclose() + 0.296734)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.174958 * (1 - exp(-1.08966 * (distance() + 0.0318632))) * pow(width() , 0.005565 * distance() + -0.0192862) * (0.071224 * thickness() + 0.209367) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0392943 * (1 - exp(-1.17326 * (distance() + -0.00882349))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.169111 * pow(width() , 0.0236346) * (0.118287 * thickness() + 0.20732) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.174958 * (1 - exp(-1.08966 * (distance() + 0.0318632))) * pow(width() , 0.005565 * distance() + -0.0192862) * (0.071224 * thickness() + 0.209367) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0392943 * (1 - exp(-1.17326 * (distance() + -0.00882349))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.169111 * pow(width() , 0.0236346) * (0.118287 * thickness() + 0.20732) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal5,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.07922 - 1.59941 * distance()) + 0.0426065 / pow(distance() , 1.26783)) * 3.99358 * pow((width1() + width2()) / 2 , 0.0838034) * (0.0868169 * (thickness1() + thickness2()) / 2 + 0.02254) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.07922 - 1.59941 * distance()) + 0.0426065 / pow(distance() , 1.26783)) * 3.99358 * pow((width1() + width2()) / 2 , 0.0838034) * (0.0868169 * (thickness1() + thickness2()) / 2 + 0.02254) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0454106 * (1 - exp(-0.918204 * (distance() + 0.0619862))) * pow(width() , 0.00789859 * distance() + -0.0125194) * (0.164692 * thickness() + 0.134737)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0077447 * (1 - exp(-1.02136 * (distance() + -0.0046946)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0812215 * pow(width() , 0.0475976) * (0.11747 * thickness() + 0.0698808)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (1.85948e-05 * distance() + 0.0225734) * (0.639583 - exp(-(0.0175897 * distance() + 0.074106) * (enclose() + 0.25481 * distance() + 1.88256) - (0.0294251 * distance() + 0.0186891) * (enclose() + -0.264622 * distance() + 2.99334) * (enclose() + -0.264622 * distance() + 2.99334)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0454106 * (1 - exp(-0.918204 * (distance() + 0.0619862))) * pow(width() , 0.00789859 * distance() + -0.0125194) * (0.164692 * thickness() + 0.134737)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0113626 * (0.639583 - exp(-0.217726 * (enclose() + 3.96308) - 0.258945 * (enclose() + 0.832701) * (enclose() + 0.832701)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0454106 * (1 - exp(-0.918204 * (distance() + 0.0619862))) * pow(width() , 0.00789859 * distance() + -0.0125194) * (0.164692 * thickness() + 0.134737)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0077447 * (1 - exp(-1.02136 * (distance() + -0.0046946)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0812215 * pow(width() , 0.0475976) * (0.11747 * thickness() + 0.0698808)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (1.85948e-05 * distance() + 0.0225734) * (0.639583 - exp(-(0.0175897 * distance() + 0.074106) * (enclose() + 0.25481 * distance() + 1.88256) - (0.0294251 * distance() + 0.0186891) * (enclose() + -0.264622 * distance() + 2.99334) * (enclose() + -0.264622 * distance() + 2.99334)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0454106 * (1 - exp(-0.918204 * (distance() + 0.0619862))) * pow(width() , 0.00789859 * distance() + -0.0125194) * (0.164692 * thickness() + 0.134737)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0113626 * (0.639583 - exp(-0.217726 * (enclose() + 3.96308) - 0.258945 * (enclose() + 0.832701) * (enclose() + 0.832701)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.183938 * (1 - exp(-1.03597 * (distance() + 0.0496178))) * pow(width() , 0.00536406 * distance() + -0.0141271) * (0.0681171 * thickness() + 0.211264) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0413084 * (1 - exp(-1.10328 * (distance() + 0.00310615))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.173722 * pow(width() , 0.0318524) * (0.119643 * thickness() + 0.215687) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.183938 * (1 - exp(-1.03597 * (distance() + 0.0496178))) * pow(width() , 0.00536406 * distance() + -0.0141271) * (0.0681171 * thickness() + 0.211264) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0413084 * (1 - exp(-1.10328 * (distance() + 0.00310615))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.173722 * pow(width() , 0.0318524) * (0.119643 * thickness() + 0.215687) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.13575 - 1.54669 * distance()) + 0.0494307 / pow(distance() , 1.22203)) * 4.03686 * pow((width1() + width2()) / 2 , 0.0879822) * (0.0840368 * (thickness1() + thickness2()) / 2 + 0.0220223) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.13575 - 1.54669 * distance()) + 0.0494307 / pow(distance() , 1.22203)) * 4.03686 * pow((width1() + width2()) / 2 , 0.0879822) * (0.0840368 * (thickness1() + thickness2()) / 2 + 0.0220223) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0372594 * (1 - exp(-0.894801 * (distance() + 0.0765238))) * pow(width() , 0.00752992 * distance() + -0.00646778) * (0.167923 * thickness() + 0.132913)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00629035 * (1 - exp(-0.98501 * (distance() + 0.00522695)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0745672 * pow(width() , 0.054406) * (0.107763 * thickness() + 0.0618162)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-3.92566e-05 * distance() + 0.0184653) * (0.642357 - exp(-(0.0278897 * distance() + 0.216956) * (enclose() + 0.0542865 * distance() + 1.73345) - (0.0188635 * distance() + -0.00735327) * (enclose() + -0.287459 * distance() + 2.91851) * (enclose() + -0.287459 * distance() + 2.91851)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0372594 * (1 - exp(-0.894801 * (distance() + 0.0765238))) * pow(width() , 0.00752992 * distance() + -0.00646778) * (0.167923 * thickness() + 0.132913)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00907238 * (0.642357 - exp(-0.444675 * (enclose() + 2.1767) - 0.146667 * (enclose() + 0.571407) * (enclose() + 0.571407)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0372594 * (1 - exp(-0.894801 * (distance() + 0.0765238))) * pow(width() , 0.00752992 * distance() + -0.00646778) * (0.167923 * thickness() + 0.132913)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00629035 * (1 - exp(-0.98501 * (distance() + 0.00522695)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0745672 * pow(width() , 0.054406) * (0.107763 * thickness() + 0.0618162)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-3.92566e-05 * distance() + 0.0184653) * (0.642357 - exp(-(0.0278897 * distance() + 0.216956) * (enclose() + 0.0542865 * distance() + 1.73345) - (0.0188635 * distance() + -0.00735327) * (enclose() + -0.287459 * distance() + 2.91851) * (enclose() + -0.287459 * distance() + 2.91851)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0372594 * (1 - exp(-0.894801 * (distance() + 0.0765238))) * pow(width() , 0.00752992 * distance() + -0.00646778) * (0.167923 * thickness() + 0.132913)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00907238 * (0.642357 - exp(-0.444675 * (enclose() + 2.1767) - 0.146667 * (enclose() + 0.571407) * (enclose() + 0.571407)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.189674 * (1 - exp(-1.01537 * (distance() + 0.0602293))) * pow(width() , 0.00492086 * distance() + -0.00867043) * (0.0671543 * thickness() + 0.212239) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0425908 * (1 - exp(-1.06761 * (distance() + 0.0114671))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.176566 * pow(width() , 0.03766) * (0.121818 * thickness() + 0.221208) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.189674 * (1 - exp(-1.01537 * (distance() + 0.0602293))) * pow(width() , 0.00492086 * distance() + -0.00867043) * (0.0671543 * thickness() + 0.212239) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0425908 * (1 - exp(-1.06761 * (distance() + 0.0114671))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.176566 * pow(width() , 0.03766) * (0.121818 * thickness() + 0.221208) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal7,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.15724 - 1.52053 * distance()) + 0.055745 / pow(distance() , 1.19055)) * 4.05193 * pow((width1() + width2()) / 2 , 0.0903742) * (0.0809382 * (thickness1() + thickness2()) / 2 + 0.0212983) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.15724 - 1.52053 * distance()) + 0.055745 / pow(distance() , 1.19055)) * 4.05193 * pow((width1() + width2()) / 2 , 0.0903742) * (0.0809382 * (thickness1() + thickness2()) / 2 + 0.0212983) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal7,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal7 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0280881 * (1 - exp(-0.885482 * (distance() + 0.0858343))) * pow(width() , 0.00710088 * distance() + -0.00120311) * (0.191667 * thickness() + 0.147996)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00529646 * (1 - exp(-0.964861 * (distance() + 0.0125507)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0667263 * pow(width() , 0.059344) * (0.104559 * thickness() + 0.0580786)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-7.05738e-05 * distance() + 0.0157776) * (0.640952 - exp(-(0.0350065 * distance() + 0.194637) * (enclose() + 0.0107676 * distance() + 1.94086) - (0.00981185 * distance() + -0.00445309) * (enclose() + -0.352094 * distance() + 3.471) * (enclose() + -0.352094 * distance() + 3.471)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0280881 * (1 - exp(-0.885482 * (distance() + 0.0858343))) * pow(width() , 0.00710088 * distance() + -0.00120311) * (0.191667 * thickness() + 0.147996)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00760068 * (0.640952 - exp(-0.480465 * (enclose() + 2.02878) - 0.0756607 * (enclose() + 0.596152) * (enclose() + 0.596152)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal7 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0280881 * (1 - exp(-0.885482 * (distance() + 0.0858343))) * pow(width() , 0.00710088 * distance() + -0.00120311) * (0.191667 * thickness() + 0.147996)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00529646 * (1 - exp(-0.964861 * (distance() + 0.0125507)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0667263 * pow(width() , 0.059344) * (0.104559 * thickness() + 0.0580786)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-7.05738e-05 * distance() + 0.0157776) * (0.640952 - exp(-(0.0350065 * distance() + 0.194637) * (enclose() + 0.0107676 * distance() + 1.94086) - (0.00981185 * distance() + -0.00445309) * (enclose() + -0.352094 * distance() + 3.471) * (enclose() + -0.352094 * distance() + 3.471)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0280881 * (1 - exp(-0.885482 * (distance() + 0.0858343))) * pow(width() , 0.00710088 * distance() + -0.00120311) * (0.191667 * thickness() + 0.147996)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00760068 * (0.640952 - exp(-0.480465 * (enclose() + 2.02878) - 0.0756607 * (enclose() + 0.596152) * (enclose() + 0.596152)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal7,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.193886 * (1 - exp(-1.00629 * (distance() + 0.0671298))) * pow(width() , 0.00454038 * distance() + -0.00429154) * (0.0664055 * thickness() + 0.212684) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.043473 * (1 - exp(-1.04752 * (distance() + 0.0175132))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.178497 * pow(width() , 0.041915) * (0.123949 * thickness() + 0.225125) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.193886 * (1 - exp(-1.00629 * (distance() + 0.0671298))) * pow(width() , 0.00454038 * distance() + -0.00429154) * (0.0664055 * thickness() + 0.212684) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.043473 * (1 - exp(-1.04752 * (distance() + 0.0175132))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.178497 * pow(width() , 0.041915) * (0.123949 * thickness() + 0.225125) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal1,metal8,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal1 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.15602 - 1.50374 * distance()) + 0.0627775 / pow(distance() , 1.16521)) * 4.1345 * pow((width1() + width2()) / 2 , 0.0921016) * (0.0754859 * (thickness1() + thickness2()) / 2 + 0.0199394) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
CAPACITANCE NEARBODY metal1 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = length() * (exp(-1.15602 - 1.50374 * distance()) + 0.0627775 / pow(distance() , 1.16521)) * 4.1345 * pow((width1() + width2()) / 2 , 0.0921016) * (0.0754859 * (thickness1() + thickness2()) / 2 + 0.0199394) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 0.47)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal1,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0237059 * (1 - exp(-0.881009 * (distance() + 0.0924543))) * pow(width() , 0.00672604 * distance() + 0.00257597) * (0.19468 * thickness() + 0.146949)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00445619 * (1 - exp(-0.951174 * (distance() + 0.0181462)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0606757 * pow(width() , 0.0628903) * (0.0991723 * thickness() + 0.053679)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00014628 * distance() + 0.0135092) * (0.658674 - exp(-(0.0344533 * distance() + 0.11151) * (enclose() + 0.0248047 * distance() + 2.26257) - (0.00430263 * distance() + 0.00266235) * (enclose() + -0.376149 * distance() + 4.41856) * (enclose() + -0.376149 * distance() + 4.41856)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0237059 * (1 - exp(-0.881009 * (distance() + 0.0924543))) * pow(width() , 0.00672604 * distance() + 0.00257597) * (0.19468 * thickness() + 0.146949)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00615741 * (0.658674 - exp(-0.392821 * (enclose() + 2.4651) - 0.0377933 * (enclose() + 1.3473) * (enclose() + 1.3473)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_calenclose = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = MAXDISTANCE_metal1
    max_enclose = MAXENCLOSE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0237059 * (1 - exp(-0.881009 * (distance() + 0.0924543))) * pow(width() , 0.00672604 * distance() + 0.00257597) * (0.19468 * thickness() + 0.146949)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00445619 * (1 - exp(-0.951174 * (distance() + 0.0181462)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0606757 * pow(width() , 0.0628903) * (0.0991723 * thickness() + 0.053679)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00014628 * distance() + 0.0135092) * (0.658674 - exp(-(0.0344533 * distance() + 0.11151) * (enclose() + 0.0248047 * distance() + 2.26257) - (0.00430263 * distance() + 0.00266235) * (enclose() + -0.376149 * distance() + 4.41856) * (enclose() + -0.376149 * distance() + 4.41856)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0237059 * (1 - exp(-0.881009 * (distance() + 0.0924543))) * pow(width() , 0.00672604 * distance() + 0.00257597) * (0.19468 * thickness() + 0.146949)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.00615741 * (0.658674 - exp(-0.392821 * (enclose() + 2.4651) - 0.0377933 * (enclose() + 1.3473) * (enclose() + 1.3473)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal1,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal1 tndiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.196421 * (1 - exp(-0.999481 * (distance() + 0.0732272))) * pow(width() , 0.00427821 * distance() + -0.00058797) * (0.0660607 * thickness() + 0.214345) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0442175 * (1 - exp(-1.03395 * (distance() + 0.022546))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.172335 * pow(width() , 0.0457109) * (0.131525 * thickness() + 0.238951) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal1 tpdiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    max_width = MAXWIDTH_metal1
    max_radius = MAXRADIUS_metal1
    max_distance = MAXDISTANCE_metal1
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.196421 * (1 - exp(-0.999481 * (distance() + 0.0732272))) * pow(width() , 0.00427821 * distance() + -0.00058797) * (0.0660607 * thickness() + 0.214345) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0442175 * (1 - exp(-1.03395 * (distance() + 0.022546))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 0.47)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.172335 * pow(width() , 0.0457109) * (0.131525 * thickness() + 0.238951) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 8.165 + m43ink4 * 0.47)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal2,metal3,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.00715 - 1.88058 * distance()) + 0.0199748 / pow(distance() , 1.54387)) * 3.92398 * pow((width1() + width2()) / 2 , 0.0419174) * (0.0851339 * (thickness1() + thickness2()) / 2 + 0.0265835) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.18)))
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.00715 - 1.88058 * distance()) + 0.0199748 / pow(distance() , 1.54387)) * 3.92398 * pow((width1() + width2()) / 2 , 0.0419174) * (0.0851339 * (thickness1() + thickness2()) / 2 + 0.0265835) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.18)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal2,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.17009 * (1 - exp(-1.26227 * (distance() + -0.0166225))) * pow(width() , 0.00289241 * distance() + -0.0145244) * (0.0759536 * thickness() + 0.187398)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0365251 * (1 - exp(-1.32141 * (distance() + -0.0342939)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.147883 * pow(width() , 0.0066324) * (0.117067 * thickness() + 0.203519)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-6.44908e-06 * distance() + 0.0977931) * (0.732487 - exp(-(-8.80454e-05 * distance() + 2.45077) * (enclose() + -0.0470503 * distance() + 0.699492) - (0.0841384 * distance() + -0.452168) * (enclose() + -0.30237 * distance() + 1.83865) * (enclose() + -0.30237 * distance() + 1.83865)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.17009 * (1 - exp(-1.26227 * (distance() + -0.0166225))) * pow(width() , 0.00289241 * distance() + -0.0145244) * (0.0759536 * thickness() + 0.187398)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0488643 * (0.732487 - exp(-2.44989 * (enclose() + 0.228989) - 0.389216 * (enclose() + -1.18505) * (enclose() + -1.18505)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.17009 * (1 - exp(-1.26227 * (distance() + -0.0166225))) * pow(width() , 0.00289241 * distance() + -0.0145244) * (0.0759536 * thickness() + 0.187398)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0365251 * (1 - exp(-1.32141 * (distance() + -0.0342939)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.147883 * pow(width() , 0.0066324) * (0.117067 * thickness() + 0.203519)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-6.44908e-06 * distance() + 0.0977931) * (0.732487 - exp(-(-8.80454e-05 * distance() + 2.45077) * (enclose() + -0.0470503 * distance() + 0.699492) - (0.0841384 * distance() + -0.452168) * (enclose() + -0.30237 * distance() + 1.83865) * (enclose() + -0.30237 * distance() + 1.83865)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.17009 * (1 - exp(-1.26227 * (distance() + -0.0166225))) * pow(width() , 0.00289241 * distance() + -0.0145244) * (0.0759536 * thickness() + 0.187398)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0488643 * (0.732487 - exp(-2.44989 * (enclose() + 0.228989) - 0.389216 * (enclose() + -1.18505) * (enclose() + -1.18505)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal2,metal3,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0861227 * (1 - exp(-1.13286 * (distance() + -0.0158605))) * pow(width() , 0.00432692 * distance() + -0.0117252) * (0.103693 * thickness() + 0.16215) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0169875 * (1 - exp(-1.22099 * (distance() + -0.045531))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.109588 * pow(width() , 0.0195417) * (0.107621 * thickness() + 0.116714) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.18)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0861227 * (1 - exp(-1.13286 * (distance() + -0.0158605))) * pow(width() , 0.00432692 * distance() + -0.0117252) * (0.103693 * thickness() + 0.16215) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0169875 * (1 - exp(-1.22099 * (distance() + -0.045531))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.109588 * pow(width() , 0.0195417) * (0.107621 * thickness() + 0.116714) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.18)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal2,metal4,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.08406 - 1.33449 * distance()) + 0.0337717 / pow(distance() , 1.38342)) * 3.92175 * pow((width1() + width2()) / 2 , 0.0663093) * (0.070862 * (thickness1() + thickness2()) / 2 + 0.0284942) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.18)))
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.08406 - 1.33449 * distance()) + 0.0337717 / pow(distance() , 1.38342)) * 3.92175 * pow((width1() + width2()) / 2 , 0.0663093) * (0.070862 * (thickness1() + thickness2()) / 2 + 0.0284942) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.18)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal2,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.107556 * (1 - exp(-0.733589 * (distance() + 0.0234778))) * pow(width() , 0.00758356 * distance() + -0.0305221) * (0.0867143 * thickness() + 0.170257)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0212736 * (1 - exp(-0.823743 * (distance() + -0.0367817)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.120962 * pow(width() , 0.0236961) * (0.0949056 * thickness() + 0.142337)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0007932 * distance() + 0.10398) * (0.417291 - exp(-(-0.167597 * distance() + 0.155028) * (enclose() + 3.93575 * distance() + 4.03762) - (0.00021911 * distance() + 0.0375044) * (enclose() + 3.95948 * distance() + 1.64602) * (enclose() + 3.95948 * distance() + 1.64602)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.107556 * (1 - exp(-0.733589 * (distance() + 0.0234778))) * pow(width() , 0.00758356 * distance() + -0.0305221) * (0.0867143 * thickness() + 0.170257)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.048024 * (0.417291 - exp(1.52094 * (enclose() + 43.3951) - 0.0396955 * (enclose() + 41.2408) * (enclose() + 41.2408)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.107556 * (1 - exp(-0.733589 * (distance() + 0.0234778))) * pow(width() , 0.00758356 * distance() + -0.0305221) * (0.0867143 * thickness() + 0.170257)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0212736 * (1 - exp(-0.823743 * (distance() + -0.0367817)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.120962 * pow(width() , 0.0236961) * (0.0949056 * thickness() + 0.142337)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.0007932 * distance() + 0.10398) * (0.417291 - exp(-(-0.167597 * distance() + 0.155028) * (enclose() + 3.93575 * distance() + 4.03762) - (0.00021911 * distance() + 0.0375044) * (enclose() + 3.95948 * distance() + 1.64602) * (enclose() + 3.95948 * distance() + 1.64602)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.107556 * (1 - exp(-0.733589 * (distance() + 0.0234778))) * pow(width() , 0.00758356 * distance() + -0.0305221) * (0.0867143 * thickness() + 0.170257)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.048024 * (0.417291 - exp(1.52094 * (enclose() + 43.3951) - 0.0396955 * (enclose() + 41.2408) * (enclose() + 41.2408)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal2,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.113424 * (1 - exp(-0.736333 * (distance() + 0.0284956))) * pow(width() , 0.00751264 * distance() + -0.0292217) * (0.0642112 * thickness() + 0.184826) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0231089 * (1 - exp(-0.827455 * (distance() + -0.0327825))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.126742 * pow(width() , 0.024582) * (0.0765784 * thickness() + 0.155836) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.18)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.113424 * (1 - exp(-0.736333 * (distance() + 0.0284956))) * pow(width() , 0.00751264 * distance() + -0.0292217) * (0.0642112 * thickness() + 0.184826) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0231089 * (1 - exp(-0.827455 * (distance() + -0.0327825))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.126742 * pow(width() , 0.024582) * (0.0765784 * thickness() + 0.155836) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.18)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal2,metal5,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.13497 - 1.11769 * distance()) + 0.0483095 / pow(distance() , 1.28537)) * 3.99155 * pow((width1() + width2()) / 2 , 0.0831027) * (0.0620977 * (thickness1() + thickness2()) / 2 + 0.0266959) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.18)))
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.13497 - 1.11769 * distance()) + 0.0483095 / pow(distance() , 1.28537)) * 3.99155 * pow((width1() + width2()) / 2 , 0.0831027) * (0.0620977 * (thickness1() + thickness2()) / 2 + 0.0266959) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.18)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal2,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0809461 * (1 - exp(-0.582834 * (distance() + 0.0782746))) * pow(width() , 0.0103227 * distance() + -0.0413079) * (0.0920794 * thickness() + 0.166273)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0156406 * (1 - exp(-0.679095 * (distance() + -0.0221423)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.107764 * pow(width() , 0.0357646) * (0.0842218 * thickness() + 0.116674)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00048764 * distance() + 0.0548987) * (0.579345 - exp(-(-0.118562 * distance() + 0.143529) * (enclose() + 3.48558 * distance() + 2.94512) - (6.95185e-05 * distance() + 0.0314997) * (enclose() + 3.49789 * distance() + 0.49166) * (enclose() + 3.49789 * distance() + 0.49166)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0809461 * (1 - exp(-0.582834 * (distance() + 0.0782746))) * pow(width() , 0.0103227 * distance() + -0.0413079) * (0.0920794 * thickness() + 0.166273)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0250111 * (0.579345 - exp(1.04209 * (enclose() + 37.8009) - 0.0321949 * (enclose() + 35.4706) * (enclose() + 35.4706)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0809461 * (1 - exp(-0.582834 * (distance() + 0.0782746))) * pow(width() , 0.0103227 * distance() + -0.0413079) * (0.0920794 * thickness() + 0.166273)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0156406 * (1 - exp(-0.679095 * (distance() + -0.0221423)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.107764 * pow(width() , 0.0357646) * (0.0842218 * thickness() + 0.116674)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00048764 * distance() + 0.0548987) * (0.579345 - exp(-(-0.118562 * distance() + 0.143529) * (enclose() + 3.48558 * distance() + 2.94512) - (6.95185e-05 * distance() + 0.0314997) * (enclose() + 3.49789 * distance() + 0.49166) * (enclose() + 3.49789 * distance() + 0.49166)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0809461 * (1 - exp(-0.582834 * (distance() + 0.0782746))) * pow(width() , 0.0103227 * distance() + -0.0413079) * (0.0920794 * thickness() + 0.166273)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0250111 * (0.579345 - exp(1.04209 * (enclose() + 37.8009) - 0.0321949 * (enclose() + 35.4706) * (enclose() + 35.4706)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal2,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.130581 * (1 - exp(-0.603565 * (distance() + 0.0779043))) * pow(width() , 0.00947793 * distance() + -0.0380968) * (0.0511898 * thickness() + 0.191429) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0265935 * (1 - exp(-0.695367 * (distance() + -0.0151091))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.135294 * pow(width() , 0.0328698) * (0.0682456 * thickness() + 0.17478) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.18)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.130581 * (1 - exp(-0.603565 * (distance() + 0.0779043))) * pow(width() , 0.00947793 * distance() + -0.0380968) * (0.0511898 * thickness() + 0.191429) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0265935 * (1 - exp(-0.695367 * (distance() + -0.0151091))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.135294 * pow(width() , 0.0328698) * (0.0682456 * thickness() + 0.17478) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.18)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal2,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.17552 - 1.00176 * distance()) + 0.0616278 / pow(distance() , 1.22173)) * 4.04066 * pow((width1() + width2()) / 2 , 0.0917647) * (0.0566058 * (thickness1() + thickness2()) / 2 + 0.0250083) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.18)))
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.17552 - 1.00176 * distance()) + 0.0616278 / pow(distance() , 1.22173)) * 4.04066 * pow((width1() + width2()) / 2 , 0.0917647) * (0.0566058 * (thickness1() + thickness2()) / 2 + 0.0250083) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.18)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal2,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0653637 * (1 - exp(-0.516917 * (distance() + 0.125247))) * pow(width() , 0.0113533 * distance() + -0.0431551) * (0.0954657 * thickness() + 0.164343)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.012476 * (1 - exp(-0.609815 * (distance() + -0.0040117)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0988061 * pow(width() , 0.0444737) * (0.0776892 * thickness() + 0.101019)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-6.84391e-05 * distance() + 0.0352911) * (0.668549 - exp(-(0.0252929 * distance() + 0.215265) * (enclose() + -0.00779161 * distance() + 1.93675) - (0.0134492 * distance() + -0.0114171) * (enclose() + -0.216912 * distance() + 3.24251) * (enclose() + -0.216912 * distance() + 3.24251)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0653637 * (1 - exp(-0.516917 * (distance() + 0.125247))) * pow(width() , 0.0113533 * distance() + -0.0431551) * (0.0954657 * thickness() + 0.164343)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0173034 * (0.668549 - exp(-0.468194 * (enclose() + 1.85883) - 0.123075 * (enclose() + 1.07339) * (enclose() + 1.07339)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0653637 * (1 - exp(-0.516917 * (distance() + 0.125247))) * pow(width() , 0.0113533 * distance() + -0.0431551) * (0.0954657 * thickness() + 0.164343)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.012476 * (1 - exp(-0.609815 * (distance() + -0.0040117)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0988061 * pow(width() , 0.0444737) * (0.0776892 * thickness() + 0.101019)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-6.84391e-05 * distance() + 0.0352911) * (0.668549 - exp(-(0.0252929 * distance() + 0.215265) * (enclose() + -0.00779161 * distance() + 1.93675) - (0.0134492 * distance() + -0.0114171) * (enclose() + -0.216912 * distance() + 3.24251) * (enclose() + -0.216912 * distance() + 3.24251)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0653637 * (1 - exp(-0.516917 * (distance() + 0.125247))) * pow(width() , 0.0113533 * distance() + -0.0431551) * (0.0954657 * thickness() + 0.164343)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0173034 * (0.668549 - exp(-0.468194 * (enclose() + 1.85883) - 0.123075 * (enclose() + 1.07339) * (enclose() + 1.07339)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal2,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.142062 * (1 - exp(-0.542083 * (distance() + 0.11954))) * pow(width() , 0.0102527 * distance() + -0.0398446) * (0.045132 * thickness() + 0.194445) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0288545 * (1 - exp(-0.629338 * (distance() + 0.00310981))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.140225 * pow(width() , 0.0397006) * (0.0664739 * thickness() + 0.186477) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.18)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.142062 * (1 - exp(-0.542083 * (distance() + 0.11954))) * pow(width() , 0.0102527 * distance() + -0.0398446) * (0.045132 * thickness() + 0.194445) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0288545 * (1 - exp(-0.629338 * (distance() + 0.00310981))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.140225 * pow(width() , 0.0397006) * (0.0664739 * thickness() + 0.186477) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.18)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal2,metal7,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.21208 - 0.931852 * distance()) + 0.0731009 / pow(distance() , 1.17717)) * 4.07355 * pow((width1() + width2()) / 2 , 0.0965914) * (0.053105 * (thickness1() + thickness2()) / 2 + 0.0237715) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.18)))
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.21208 - 0.931852 * distance()) + 0.0731009 / pow(distance() , 1.17717)) * 4.07355 * pow((width1() + width2()) / 2 , 0.0965914) * (0.053105 * (thickness1() + thickness2()) / 2 + 0.0237715) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.18)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal2,metal7,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 metal7 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0548213 * (1 - exp(-0.483292 * (distance() + 0.161794))) * pow(width() , 0.0115739 * distance() + -0.0410684) * (0.0979882 * thickness() + 0.16329)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0104015 * (1 - exp(-0.569958 * (distance() + 0.0135441)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.092534 * pow(width() , 0.0509293) * (0.0716252 * thickness() + 0.0900016)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00024098 * distance() + 0.0296034) * (0.697823 - exp(-(0.0470435 * distance() + 0.184594) * (enclose() + -0.0635872 * distance() + 2.03723) - (0.00494148 * distance() + -0.00936054) * (enclose() + -0.225992 * distance() + 3.37048) * (enclose() + -0.225992 * distance() + 3.37048)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0548213 * (1 - exp(-0.483292 * (distance() + 0.161794))) * pow(width() , 0.0115739 * distance() + -0.0410684) * (0.0979882 * thickness() + 0.16329)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0135968 * (0.697823 - exp(-0.655029 * (enclose() + 1.40136) - 0.0400543 * (enclose() + 1.11056) * (enclose() + 1.11056)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 metal7 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0548213 * (1 - exp(-0.483292 * (distance() + 0.161794))) * pow(width() , 0.0115739 * distance() + -0.0410684) * (0.0979882 * thickness() + 0.16329)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0104015 * (1 - exp(-0.569958 * (distance() + 0.0135441)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.092534 * pow(width() , 0.0509293) * (0.0716252 * thickness() + 0.0900016)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00024098 * distance() + 0.0296034) * (0.697823 - exp(-(0.0470435 * distance() + 0.184594) * (enclose() + -0.0635872 * distance() + 2.03723) - (0.00494148 * distance() + -0.00936054) * (enclose() + -0.225992 * distance() + 3.37048) * (enclose() + -0.225992 * distance() + 3.37048)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0548213 * (1 - exp(-0.483292 * (distance() + 0.161794))) * pow(width() , 0.0115739 * distance() + -0.0410684) * (0.0979882 * thickness() + 0.16329)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0135968 * (0.697823 - exp(-0.655029 * (enclose() + 1.40136) - 0.0400543 * (enclose() + 1.11056) * (enclose() + 1.11056)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal2,metal7,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.150039 * (1 - exp(-0.509821 * (distance() + 0.151674))) * pow(width() , 0.0104223 * distance() + -0.0383407) * (0.0418872 * thickness() + 0.196136) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0304212 * (1 - exp(-0.591106 * (distance() + 0.0195896))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.14354 * pow(width() , 0.0450466) * (0.0662556 * thickness() + 0.194586) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.18)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.150039 * (1 - exp(-0.509821 * (distance() + 0.151674))) * pow(width() , 0.0104223 * distance() + -0.0383407) * (0.0418872 * thickness() + 0.196136) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0304212 * (1 - exp(-0.591106 * (distance() + 0.0195896))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.14354 * pow(width() , 0.0450466) * (0.0662556 * thickness() + 0.194586) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.18)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal2,metal8,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal2 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.23699 - 0.880197 * distance()) + 0.0855656 / pow(distance() , 1.13919)) * 4.1142 * pow((width1() + width2()) / 2 , 0.0999401) * (0.0494971 * (thickness1() + thickness2()) / 2 + 0.0223745) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.18)))
]
CAPACITANCE NEARBODY metal2 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = length() * (exp(-1.23699 - 0.880197 * distance()) + 0.0855656 / pow(distance() , 1.13919)) * 4.1142 * pow((width1() + width2()) / 2 , 0.0999401) * (0.0494971 * (thickness1() + thickness2()) / 2 + 0.0223745) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 1.18)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal2,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0461003 * (1 - exp(-0.462185 * (distance() + 0.1931))) * pow(width() , 0.0114707 * distance() + -0.0378346) * (0.0999019 * thickness() + 0.161903)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00867729 * (1 - exp(-0.542675 * (distance() + 0.0270145)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0873399 * pow(width() , 0.0568577) * (0.0617511 * thickness() + 0.0814807)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00019103 * distance() + 0.0218035) * (0.828936 - exp(-(0.0924781 * distance() + 0.111396) * (enclose() + 0.18172 * distance() + 1.31749) - (-0.00214721 * distance() + -0.00242789) * (enclose() + 0.627115 * distance() + 3.8911) * (enclose() + 0.627115 * distance() + 3.8911)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0461003 * (1 - exp(-0.462185 * (distance() + 0.1931))) * pow(width() , 0.0114707 * distance() + -0.0378346) * (0.0999019 * thickness() + 0.161903)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0099466 * (0.828936 - exp(-1.03618 * (enclose() + 3.13469) - -0.0239 * (enclose() + 10.1623) * (enclose() + 10.1623)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = MAXDISTANCE_metal2
    max_enclose = MAXENCLOSE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0461003 * (1 - exp(-0.462185 * (distance() + 0.1931))) * pow(width() , 0.0114707 * distance() + -0.0378346) * (0.0999019 * thickness() + 0.161903)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.00867729 * (1 - exp(-0.542675 * (distance() + 0.0270145)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.0873399 * pow(width() , 0.0568577) * (0.0617511 * thickness() + 0.0814807)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00019103 * distance() + 0.0218035) * (0.828936 - exp(-(0.0924781 * distance() + 0.111396) * (enclose() + 0.18172 * distance() + 1.31749) - (-0.00214721 * distance() + -0.00242789) * (enclose() + 0.627115 * distance() + 3.8911) * (enclose() + 0.627115 * distance() + 3.8911)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0461003 * (1 - exp(-0.462185 * (distance() + 0.1931))) * pow(width() , 0.0114707 * distance() + -0.0378346) * (0.0999019 * thickness() + 0.161903)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0099466 * (0.828936 - exp(-1.03618 * (enclose() + 3.13469) - -0.0239 * (enclose() + 10.1623) * (enclose() + 10.1623)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal2,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal2 tndiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.156717 * (1 - exp(-0.489659 * (distance() + 0.179453))) * pow(width() , 0.010275 * distance() + -0.0349953) * (0.0396909 * thickness() + 0.197306) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0317419 * (1 - exp(-0.564117 * (distance() + 0.035155))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.146483 * pow(width() , 0.0499918) * (0.0666704 * thickness() + 0.201287) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.18)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal2 tpdiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal2
    max_radius = MAXRADIUS_metal2
    max_distance = MAXDISTANCE_metal2
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.156717 * (1 - exp(-0.489659 * (distance() + 0.179453))) * pow(width() , 0.010275 * distance() + -0.0349953) * (0.0396909 * thickness() + 0.197306) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0317419 * (1 - exp(-0.564117 * (distance() + 0.035155))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 1.18)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.146483 * pow(width() , 0.0499918) * (0.0666704 * thickness() + 0.201287) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 1.18)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal3,metal4,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal3 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.07685 - 1.60426 * distance()) + 0.0320524 / pow(distance() , 1.40635)) * 4.021 * pow((width1() + width2()) / 2 , 0.0638229) * (0.0733235 * (thickness1() + thickness2()) / 2 + 0.0249422) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2)))
]
CAPACITANCE NEARBODY metal3 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.07685 - 1.60426 * distance()) + 0.0320524 / pow(distance() , 1.40635)) * 4.021 * pow((width1() + width2()) / 2 , 0.0638229) * (0.0733235 * (thickness1() + thickness2()) / 2 + 0.0249422) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal3,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.189316 * (1 - exp(-1.08 * (distance() + 0.0158108))) * pow(width() , 0.00365908 * distance() + -0.0141489) * (0.0643985 * thickness() + 0.191684)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0404389 * (1 - exp(-1.13289 * (distance() + -0.0153208)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.155562 * pow(width() , 0.018013) * (0.107236 * thickness() + 0.223493)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (1.65203e-05 * distance() + 0.112) * (0.703675 - exp(-(-0.0298168 * distance() + 2.29967) * (enclose() + -0.0304333 * distance() + 0.677592) - (0.0598031 * distance() + -0.463472) * (enclose() + -0.33723 * distance() + 1.68794) * (enclose() + -0.33723 * distance() + 1.68794)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.189316 * (1 - exp(-1.08 * (distance() + 0.0158108))) * pow(width() , 0.00365908 * distance() + -0.0141489) * (0.0643985 * thickness() + 0.191684)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0560826 * (0.703675 - exp(-2.0015 * (enclose() + 0.373259) - 0.134559 * (enclose() + -1.68436) * (enclose() + -1.68436)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.189316 * (1 - exp(-1.08 * (distance() + 0.0158108))) * pow(width() , 0.00365908 * distance() + -0.0141489) * (0.0643985 * thickness() + 0.191684)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0404389 * (1 - exp(-1.13289 * (distance() + -0.0153208)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.155562 * pow(width() , 0.018013) * (0.107236 * thickness() + 0.223493)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (1.65203e-05 * distance() + 0.112) * (0.703675 - exp(-(-0.0298168 * distance() + 2.29967) * (enclose() + -0.0304333 * distance() + 0.677592) - (0.0598031 * distance() + -0.463472) * (enclose() + -0.33723 * distance() + 1.68794) * (enclose() + -0.33723 * distance() + 1.68794)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.189316 * (1 - exp(-1.08 * (distance() + 0.0158108))) * pow(width() , 0.00365908 * distance() + -0.0141489) * (0.0643985 * thickness() + 0.191684)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0560826 * (0.703675 - exp(-2.0015 * (enclose() + 0.373259) - 0.134559 * (enclose() + -1.68436) * (enclose() + -1.68436)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal3,metal4,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 tndiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0612636 * (1 - exp(-0.924611 * (distance() + 0.0257573))) * pow(width() , 0.00625995 * distance() + -0.0140412) * (0.12156 * thickness() + 0.151043) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0116812 * (1 - exp(-1.01894 * (distance() + -0.0292541))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0946463 * pow(width() , 0.0361893) * (0.0995744 * thickness() + 0.0898434) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 tpdiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0612636 * (1 - exp(-0.924611 * (distance() + 0.0257573))) * pow(width() , 0.00625995 * distance() + -0.0140412) * (0.12156 * thickness() + 0.151043) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0116812 * (1 - exp(-1.01894 * (distance() + -0.0292541))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0946463 * pow(width() , 0.0361893) * (0.0995744 * thickness() + 0.0898434) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal3,metal5,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal3 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.13421 - 1.10119 * distance()) + 0.0485176 / pow(distance() , 1.28377)) * 3.98694 * pow((width1() + width2()) / 2 , 0.0838671) * (0.0617556 * (thickness1() + thickness2()) / 2 + 0.0269478) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2)))
]
CAPACITANCE NEARBODY metal3 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.13421 - 1.10119 * distance()) + 0.0485176 / pow(distance() , 1.28377)) * 3.98694 * pow((width1() + width2()) / 2 , 0.0838671) * (0.0617556 * (thickness1() + thickness2()) / 2 + 0.0269478) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal3,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.125282 * (1 - exp(-0.587025 * (distance() + 0.0742451))) * pow(width() , 0.00987761 * distance() + -0.0416282) * (0.0687084 * thickness() + 0.180153)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0249292 * (1 - exp(-0.679642 * (distance() + -0.0203738)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.130629 * pow(width() , 0.0318695) * (0.0827255 * thickness() + 0.163209)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00085083 * distance() + 0.161135) * (0.306802 - exp(-(-0.0594082 * distance() + 0.0244951) * (enclose() + 7.67512 * distance() + 14.7443) - (0.00011711 * distance() + 0.0184665) * (enclose() + 4.6268 * distance() + 6.13422) * (enclose() + 4.6268 * distance() + 6.13422)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.125282 * (1 - exp(-0.587025 * (distance() + 0.0742451))) * pow(width() , 0.00987761 * distance() + -0.0416282) * (0.0687084 * thickness() + 0.180153)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0763134 * (0.306802 - exp(0.569587 * (enclose() + 91.4955) - 0.0196376 * (enclose() + 52.4022) * (enclose() + 52.4022)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.125282 * (1 - exp(-0.587025 * (distance() + 0.0742451))) * pow(width() , 0.00987761 * distance() + -0.0416282) * (0.0687084 * thickness() + 0.180153)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0249292 * (1 - exp(-0.679642 * (distance() + -0.0203738)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.130629 * pow(width() , 0.0318695) * (0.0827255 * thickness() + 0.163209)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00085083 * distance() + 0.161135) * (0.306802 - exp(-(-0.0594082 * distance() + 0.0244951) * (enclose() + 7.67512 * distance() + 14.7443) - (0.00011711 * distance() + 0.0184665) * (enclose() + 4.6268 * distance() + 6.13422) * (enclose() + 4.6268 * distance() + 6.13422)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.125282 * (1 - exp(-0.587025 * (distance() + 0.0742451))) * pow(width() , 0.00987761 * distance() + -0.0416282) * (0.0687084 * thickness() + 0.180153)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0763134 * (0.306802 - exp(0.569587 * (enclose() + 91.4955) - 0.0196376 * (enclose() + 52.4022) * (enclose() + 52.4022)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal3,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0852213 * (1 - exp(-0.567378 * (distance() + 0.0848072))) * pow(width() , 0.0107429 * distance() + -0.0431284) * (0.0742789 * thickness() + 0.177139) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.016782 * (1 - exp(-0.666546 * (distance() + -0.0206764))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.112108 * pow(width() , 0.0367115) * (0.0709542 * thickness() + 0.126256) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0852213 * (1 - exp(-0.567378 * (distance() + 0.0848072))) * pow(width() , 0.0107429 * distance() + -0.0431284) * (0.0742789 * thickness() + 0.177139) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.016782 * (1 - exp(-0.666546 * (distance() + -0.0206764))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.112108 * pow(width() , 0.0367115) * (0.0709542 * thickness() + 0.126256) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal3,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal3 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.1589 - 0.895412 * distance()) + 0.0661722 / pow(distance() , 1.20873)) * 4.02322 * pow((width1() + width2()) / 2 , 0.0988715) * (0.0537587 * (thickness1() + thickness2()) / 2 + 0.0251283) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2)))
]
CAPACITANCE NEARBODY metal3 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.1589 - 0.895412 * distance()) + 0.0661722 / pow(distance() , 1.20873)) * 4.02322 * pow((width1() + width2()) / 2 , 0.0988715) * (0.0537587 * (thickness1() + thickness2()) / 2 + 0.0251283) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal3,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0985844 * (1 - exp(-0.446284 * (distance() + 0.146305))) * pow(width() , 0.0133495 * distance() + -0.0599283) * (0.0695111 * thickness() + 0.176555)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0189541 * (1 - exp(-0.543989 * (distance() + -0.00526558)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.118079 * pow(width() , 0.0414091) * (0.0717354 * thickness() + 0.137174)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (3.45653e-05 * distance() + 0.0595337) * (0.588827 - exp(-(0.0234563 * distance() + 0.474064) * (enclose() + -0.0126374 * distance() + 1.74917) - (0.0188582 * distance() + -0.057805) * (enclose() + -0.232601 * distance() + 2.6369) * (enclose() + -0.232601 * distance() + 2.6369)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0985844 * (1 - exp(-0.446284 * (distance() + 0.146305))) * pow(width() , 0.0133495 * distance() + -0.0599283) * (0.0695111 * thickness() + 0.176555)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0299397 * (0.588827 - exp(-0.708627 * (enclose() + 1.6228) - 0.130777 * (enclose() + 0.31089) * (enclose() + 0.31089)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0985844 * (1 - exp(-0.446284 * (distance() + 0.146305))) * pow(width() , 0.0133495 * distance() + -0.0599283) * (0.0695111 * thickness() + 0.176555)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0189541 * (1 - exp(-0.543989 * (distance() + -0.00526558)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.118079 * pow(width() , 0.0414091) * (0.0717354 * thickness() + 0.137174)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (3.45653e-05 * distance() + 0.0595337) * (0.588827 - exp(-(0.0234563 * distance() + 0.474064) * (enclose() + -0.0126374 * distance() + 1.74917) - (0.0188582 * distance() + -0.057805) * (enclose() + -0.232601 * distance() + 2.6369) * (enclose() + -0.232601 * distance() + 2.6369)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0985844 * (1 - exp(-0.446284 * (distance() + 0.146305))) * pow(width() , 0.0133495 * distance() + -0.0599283) * (0.0695111 * thickness() + 0.176555)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0299397 * (0.588827 - exp(-0.708627 * (enclose() + 1.6228) - 0.130777 * (enclose() + 0.31089) * (enclose() + 0.31089)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal3,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.102644 * (1 - exp(-0.44516 * (distance() + 0.151399))) * pow(width() , 0.0134397 * distance() + -0.0595425) * (0.0565415 * thickness() + 0.184049) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0199697 * (1 - exp(-0.544485 * (distance() + -0.0027787))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.121582 * pow(width() , 0.0424301) * (0.062154 * thickness() + 0.144705) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.102644 * (1 - exp(-0.44516 * (distance() + 0.151399))) * pow(width() , 0.0134397 * distance() + -0.0595425) * (0.0565415 * thickness() + 0.184049) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0199697 * (1 - exp(-0.544485 * (distance() + -0.0027787))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.121582 * pow(width() , 0.0424301) * (0.062154 * thickness() + 0.144705) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal3,metal7,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal3 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.17492 - 0.784008 * distance()) + 0.083081 / pow(distance() , 1.15678)) * 4.05965 * pow((width1() + width2()) / 2 , 0.106746) * (0.0483006 * (thickness1() + thickness2()) / 2 + 0.023241) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2)))
]
CAPACITANCE NEARBODY metal3 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.17492 - 0.784008 * distance()) + 0.083081 / pow(distance() , 1.15678)) * 4.05965 * pow((width1() + width2()) / 2 , 0.106746) * (0.0483006 * (thickness1() + thickness2()) / 2 + 0.023241) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal3,metal7,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 metal7 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0819431 * (1 - exp(-0.383061 * (distance() + 0.211144))) * pow(width() , 0.0147187 * distance() + -0.0661204) * (0.0702214 * thickness() + 0.175184)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0154717 * (1 - exp(-0.477439 * (distance() + 0.0155555)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.108982 * pow(width() , 0.0484327) * (0.0657037 * thickness() + 0.121273)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00045109 * distance() + 0.044646) * (0.699011 - exp(-(0.0642883 * distance() + 0.239101) * (enclose() + -0.0835915 * distance() + 1.93979) - (0.00184904 * distance() + -0.0178053) * (enclose() + -0.250145 * distance() + 3.20247) * (enclose() + -0.250145 * distance() + 3.20247)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0819431 * (1 - exp(-0.383061 * (distance() + 0.211144))) * pow(width() , 0.0147187 * distance() + -0.0661204) * (0.0702214 * thickness() + 0.175184)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0200675 * (0.699011 - exp(-0.881984 * (enclose() + 1.10387) - 0.0006851 * (enclose() + 0.70102) * (enclose() + 0.70102)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 metal7 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0819431 * (1 - exp(-0.383061 * (distance() + 0.211144))) * pow(width() , 0.0147187 * distance() + -0.0661204) * (0.0702214 * thickness() + 0.175184)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0154717 * (1 - exp(-0.477439 * (distance() + 0.0155555)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.108982 * pow(width() , 0.0484327) * (0.0657037 * thickness() + 0.121273)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00045109 * distance() + 0.044646) * (0.699011 - exp(-(0.0642883 * distance() + 0.239101) * (enclose() + -0.0835915 * distance() + 1.93979) - (0.00184904 * distance() + -0.0178053) * (enclose() + -0.250145 * distance() + 3.20247) * (enclose() + -0.250145 * distance() + 3.20247)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0819431 * (1 - exp(-0.383061 * (distance() + 0.211144))) * pow(width() , 0.0147187 * distance() + -0.0661204) * (0.0702214 * thickness() + 0.175184)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0200675 * (0.699011 - exp(-0.881984 * (enclose() + 1.10387) - 0.0006851 * (enclose() + 0.70102) * (enclose() + 0.70102)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal3,metal7,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 tndiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.114999 * (1 - exp(-0.386664 * (distance() + 0.211394))) * pow(width() , 0.0145975 * distance() + -0.0657824) * (0.0478734 * thickness() + 0.187554) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0221763 * (1 - exp(-0.481584 * (distance() + 0.0185178))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.126611 * pow(width() , 0.0476691) * (0.0596718 * thickness() + 0.15757) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 tpdiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.114999 * (1 - exp(-0.386664 * (distance() + 0.211394))) * pow(width() , 0.0145975 * distance() + -0.0657824) * (0.0478734 * thickness() + 0.187554) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0221763 * (1 - exp(-0.481584 * (distance() + 0.0185178))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.126611 * pow(width() , 0.0476691) * (0.0596718 * thickness() + 0.15757) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal3,metal8,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal3 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.19263 - 0.705654 * distance()) + 0.101446 / pow(distance() , 1.11167)) * 4.10147 * pow((width1() + width2()) / 2 , 0.111809) * (0.0438167 * (thickness1() + thickness2()) / 2 + 0.0214489) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2)))
]
CAPACITANCE NEARBODY metal3 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = length() * (exp(-1.19263 - 0.705654 * distance()) + 0.101446 / pow(distance() , 1.11167)) * 4.10147 * pow((width1() + width2()) / 2 , 0.111809) * (0.0438167 * (thickness1() + thickness2()) / 2 + 0.0214489) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal3,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0682004 * (1 - exp(-0.34484 * (distance() + 0.272715))) * pow(width() , 0.0151946 * distance() + -0.0673396) * (0.0711446 * thickness() + 0.174594)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0127517 * (1 - exp(-0.433107 * (distance() + 0.0393903)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.101271 * pow(width() , 0.0542939) * (0.0602042 * thickness() + 0.107792)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00020824 * distance() + 0.0302037) * (0.851399 - exp(-(0.102735 * distance() + 0.159037) * (enclose() + 0.217822 * distance() + 1.19952) - (-0.00165715 * distance() + -0.0111882) * (enclose() + 0.787747 * distance() + 2.90628) * (enclose() + 0.787747 * distance() + 2.90628)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0682004 * (1 - exp(-0.34484 * (distance() + 0.272715))) * pow(width() , 0.0151946 * distance() + -0.0673396) * (0.0711446 * thickness() + 0.174594)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0140607 * (0.851399 - exp(-1.18639 * (enclose() + 3.37774) - -0.0277597 * (enclose() + 10.7837) * (enclose() + 10.7837)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = MAXDISTANCE_metal3
    max_enclose = MAXENCLOSE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0682004 * (1 - exp(-0.34484 * (distance() + 0.272715))) * pow(width() , 0.0151946 * distance() + -0.0673396) * (0.0711446 * thickness() + 0.174594)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0127517 * (1 - exp(-0.433107 * (distance() + 0.0393903)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.101271 * pow(width() , 0.0542939) * (0.0602042 * thickness() + 0.107792)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00020824 * distance() + 0.0302037) * (0.851399 - exp(-(0.102735 * distance() + 0.159037) * (enclose() + 0.217822 * distance() + 1.19952) - (-0.00165715 * distance() + -0.0111882) * (enclose() + 0.787747 * distance() + 2.90628) * (enclose() + 0.787747 * distance() + 2.90628)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0682004 * (1 - exp(-0.34484 * (distance() + 0.272715))) * pow(width() , 0.0151946 * distance() + -0.0673396) * (0.0711446 * thickness() + 0.174594)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0140607 * (0.851399 - exp(-1.18639 * (enclose() + 3.37774) - -0.0277597 * (enclose() + 10.7837) * (enclose() + 10.7837)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal3,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal3 tndiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.124943 * (1 - exp(-0.350622 * (distance() + 0.268881))) * pow(width() , 0.0149753 * distance() + -0.0668109) * (0.042354 * thickness() + 0.190603) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0240325 * (1 - exp(-0.439099 * (distance() + 0.0424027))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.131054 * pow(width() , 0.0527692) * (0.0579883 * thickness() + 0.16799) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal3 tpdiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal3
    max_radius = MAXRADIUS_metal3
    max_distance = MAXDISTANCE_metal3
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.124943 * (1 - exp(-0.350622 * (distance() + 0.268881))) * pow(width() , 0.0149753 * distance() + -0.0668109) * (0.042354 * thickness() + 0.190603) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0240325 * (1 - exp(-0.439099 * (distance() + 0.0424027))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.131054 * pow(width() , 0.0527692) * (0.0579883 * thickness() + 0.16799) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal4,metal5,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal4 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.14268 - 1.47518 * distance()) + 0.0424456 / pow(distance() , 1.32346)) * 3.99767 * pow((width1() + width2()) / 2 , 0.0750035) * (0.0690095 * (thickness1() + thickness2()) / 2 + 0.0242322) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.82)))
]
CAPACITANCE NEARBODY metal4 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.14268 - 1.47518 * distance()) + 0.0424456 / pow(distance() , 1.32346)) * 3.99767 * pow((width1() + width2()) / 2 , 0.0750035) * (0.0690095 * (thickness1() + thickness2()) / 2 + 0.0242322) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.82)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal4,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.200328 * (1 - exp(-1.00615 * (distance() + 0.038132))) * pow(width() , 0.00376211 * distance() + -0.0105455) * (0.0599886 * thickness() + 0.193903)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0427302 * (1 - exp(-1.04837 * (distance() + -0.00083168)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.160712 * pow(width() , 0.0266522) * (0.105053 * thickness() + 0.233251)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0007376 * distance() + 0.105455) * (0.744138 - exp(-(0.112739 * distance() + 0.806429) * (enclose() + -0.0328883 * distance() + 0.769845) - (0.0183737 * distance() + -0.0174017) * (enclose() + -0.603623 * distance() + 4.9955) * (enclose() + -0.603623 * distance() + 4.9955)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.200328 * (1 - exp(-1.00615 * (distance() + 0.038132))) * pow(width() , 0.00376211 * distance() + -0.0105455) * (0.0599886 * thickness() + 0.193903)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0564155 * (0.744138 - exp(-1.93382 * (enclose() + 0.440962) - 0.166335 * (enclose() + -1.04073) * (enclose() + -1.04073)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.200328 * (1 - exp(-1.00615 * (distance() + 0.038132))) * pow(width() , 0.00376211 * distance() + -0.0105455) * (0.0599886 * thickness() + 0.193903)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0427302 * (1 - exp(-1.04837 * (distance() + -0.00083168)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.160712 * pow(width() , 0.0266522) * (0.105053 * thickness() + 0.233251)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.0007376 * distance() + 0.105455) * (0.744138 - exp(-(0.112739 * distance() + 0.806429) * (enclose() + -0.0328883 * distance() + 0.769845) - (0.0183737 * distance() + -0.0174017) * (enclose() + -0.603623 * distance() + 4.9955) * (enclose() + -0.603623 * distance() + 4.9955)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.200328 * (1 - exp(-1.00615 * (distance() + 0.038132))) * pow(width() , 0.00376211 * distance() + -0.0105455) * (0.0599886 * thickness() + 0.193903)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0564155 * (0.744138 - exp(-1.93382 * (enclose() + 0.440962) - 0.166335 * (enclose() + -1.04073) * (enclose() + -1.04073)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal4,metal5,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0477131 * (1 - exp(-0.842441 * (distance() + 0.0575271))) * pow(width() , 0.00690051 * distance() + -0.0109725) * (0.131422 * thickness() + 0.146015) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00896401 * (1 - exp(-0.931644 * (distance() + -0.0133755))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0856671 * pow(width() , 0.0477963) * (0.0918141 * thickness() + 0.0748309) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.82)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0477131 * (1 - exp(-0.842441 * (distance() + 0.0575271))) * pow(width() , 0.00690051 * distance() + -0.0109725) * (0.131422 * thickness() + 0.146015) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00896401 * (1 - exp(-0.931644 * (distance() + -0.0133755))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0856671 * pow(width() , 0.0477963) * (0.0918141 * thickness() + 0.0748309) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.82)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal4,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal4 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.17034 - 0.976774 * distance()) + 0.0625677 / pow(distance() , 1.21858)) * 4.03784 * pow((width1() + width2()) / 2 , 0.0932471) * (0.0557766 * (thickness1() + thickness2()) / 2 + 0.0251452) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.82)))
]
CAPACITANCE NEARBODY metal4 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.17034 - 0.976774 * distance()) + 0.0625677 / pow(distance() , 1.21858)) * 4.03784 * pow((width1() + width2()) / 2 , 0.0932471) * (0.0557766 * (thickness1() + thickness2()) / 2 + 0.0251452) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.82)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal4,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.137359 * (1 - exp(-0.519547 * (distance() + 0.119589))) * pow(width() , 0.0108867 * distance() + -0.0449111) * (0.0601421 * thickness() + 0.184673)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0273141 * (1 - exp(-0.608068 * (distance() + -0.0015583)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.13664 * pow(width() , 0.0388924) * (0.0786574 * thickness() + 0.175444)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00041663 * distance() + 0.171335) * (0.30606 - exp(-(-0.0278392 * distance() + 0.00457191) * (enclose() + 11.8693 * distance() + 26.6369) - (0.00011365 * distance() + 0.0148029) * (enclose() + 4.30899 * distance() + 7.99351) * (enclose() + 4.30899 * distance() + 7.99351)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.137359 * (1 - exp(-0.519547 * (distance() + 0.119589))) * pow(width() , 0.0108867 * distance() + -0.0449111) * (0.0601421 * thickness() + 0.184673)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0835843 * (0.30606 - exp(0.27382 * (enclose() + 145.33) - 0.0159394 * (enclose() + 51.0834) * (enclose() + 51.0834)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.137359 * (1 - exp(-0.519547 * (distance() + 0.119589))) * pow(width() , 0.0108867 * distance() + -0.0449111) * (0.0601421 * thickness() + 0.184673)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0273141 * (1 - exp(-0.608068 * (distance() + -0.0015583)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.13664 * pow(width() , 0.0388924) * (0.0786574 * thickness() + 0.175444)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00041663 * distance() + 0.171335) * (0.30606 - exp(-(-0.0278392 * distance() + 0.00457191) * (enclose() + 11.8693 * distance() + 26.6369) - (0.00011365 * distance() + 0.0148029) * (enclose() + 4.30899 * distance() + 7.99351) * (enclose() + 4.30899 * distance() + 7.99351)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.137359 * (1 - exp(-0.519547 * (distance() + 0.119589))) * pow(width() , 0.0108867 * distance() + -0.0449111) * (0.0601421 * thickness() + 0.184673)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0835843 * (0.30606 - exp(0.27382 * (enclose() + 145.33) - 0.0159394 * (enclose() + 51.0834) * (enclose() + 51.0834)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal4,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0689572 * (1 - exp(-0.49483 * (distance() + 0.13618))) * pow(width() , 0.0120591 * distance() + -0.0468507) * (0.0793085 * thickness() + 0.173593) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0133294 * (1 - exp(-0.590908 * (distance() + -0.0012261))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.102351 * pow(width() , 0.0456562) * (0.0670098 * thickness() + 0.10903) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.82)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0689572 * (1 - exp(-0.49483 * (distance() + 0.13618))) * pow(width() , 0.0120591 * distance() + -0.0468507) * (0.0793085 * thickness() + 0.173593) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0133294 * (1 - exp(-0.590908 * (distance() + -0.0012261))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.102351 * pow(width() , 0.0456562) * (0.0670098 * thickness() + 0.10903) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.82)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal4,metal7,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal4 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.17178 - 0.776355 * distance()) + 0.0836538 / pow(distance() , 1.15584)) * 4.05825 * pow((width1() + width2()) / 2 , 0.107446) * (0.0480037 * (thickness1() + thickness2()) / 2 + 0.0232227) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.82)))
]
CAPACITANCE NEARBODY metal4 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.17178 - 0.776355 * distance()) + 0.0836538 / pow(distance() , 1.15584)) * 4.05825 * pow((width1() + width2()) / 2 , 0.107446) * (0.0480037 * (thickness1() + thickness2()) / 2 + 0.0232227) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.82)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal4,metal7,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 metal7 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.111567 * (1 - exp(-0.38185 * (distance() + 0.210013))) * pow(width() , 0.0147463 * distance() + -0.0678013) * (0.0583439 * thickness() + 0.181145)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0212507 * (1 - exp(-0.476076 * (distance() + 0.0161481)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.123776 * pow(width() , 0.0468179) * (0.0674702 * thickness() + 0.150864)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (8.11399e-05 * distance() + 0.0590942) * (0.654151 - exp(-(0.060149 * distance() + 0.19743) * (enclose() + -0.0932972 * distance() + 2.16674) - (0.00443559 * distance() + -0.00481339) * (enclose() + -0.319904 * distance() + 4.39846) * (enclose() + -0.319904 * distance() + 4.39846)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.111567 * (1 - exp(-0.38185 * (distance() + 0.210013))) * pow(width() , 0.0147463 * distance() + -0.0678013) * (0.0583439 * thickness() + 0.181145)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0299528 * (0.654151 - exp(-0.79892 * (enclose() + 1.23377) - 0.0395425 * (enclose() + 1.19942) * (enclose() + 1.19942)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 metal7 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.111567 * (1 - exp(-0.38185 * (distance() + 0.210013))) * pow(width() , 0.0147463 * distance() + -0.0678013) * (0.0583439 * thickness() + 0.181145)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0212507 * (1 - exp(-0.476076 * (distance() + 0.0161481)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.123776 * pow(width() , 0.0468179) * (0.0674702 * thickness() + 0.150864)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (8.11399e-05 * distance() + 0.0590942) * (0.654151 - exp(-(0.060149 * distance() + 0.19743) * (enclose() + -0.0932972 * distance() + 2.16674) - (0.00443559 * distance() + -0.00481339) * (enclose() + -0.319904 * distance() + 4.39846) * (enclose() + -0.319904 * distance() + 4.39846)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.111567 * (1 - exp(-0.38185 * (distance() + 0.210013))) * pow(width() , 0.0147463 * distance() + -0.0678013) * (0.0583439 * thickness() + 0.181145)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0299528 * (0.654151 - exp(-0.79892 * (enclose() + 1.23377) - 0.0395425 * (enclose() + 1.19942) * (enclose() + 1.19942)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal4,metal7,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 tndiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0853252 * (1 - exp(-0.375804 * (distance() + 0.22049))) * pow(width() , 0.0150779 * distance() + -0.0676294) * (0.0591341 * thickness() + 0.181405) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0162324 * (1 - exp(-0.472127 * (distance() + 0.0179429))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.11182 * pow(width() , 0.049598) * (0.0579742 * thickness() + 0.127474) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.82)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 tpdiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0853252 * (1 - exp(-0.375804 * (distance() + 0.22049))) * pow(width() , 0.0150779 * distance() + -0.0676294) * (0.0591341 * thickness() + 0.181405) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0162324 * (1 - exp(-0.472127 * (distance() + 0.0179429))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.11182 * pow(width() , 0.049598) * (0.0579742 * thickness() + 0.127474) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.82)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal4,metal8,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal4 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.15967 - 0.654583 * distance()) + 0.10932 / pow(distance() , 1.10275)) * 4.09752 * pow((width1() + width2()) / 2 , 0.115932) * (0.0412924 * (thickness1() + thickness2()) / 2 + 0.0207226) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.82)))
]
CAPACITANCE NEARBODY metal4 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = length() * (exp(-1.15967 - 0.654583 * distance()) + 0.10932 / pow(distance() , 1.10275)) * 4.09752 * pow((width1() + width2()) / 2 , 0.115932) * (0.0412924 * (thickness1() + thickness2()) / 2 + 0.0207226) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 2.82)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal4,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0922736 * (1 - exp(-0.311018 * (distance() + 0.306103))) * pow(width() , 0.0165075 * distance() + -0.0785292) * (0.0574324 * thickness() + 0.17979)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0171057 * (1 - exp(-0.401418 * (distance() + 0.0442357)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.114236 * pow(width() , 0.0532963) * (0.0595251 * thickness() + 0.131795)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00012446 * distance() + 0.0391843) * (0.841461 - exp(-(0.120769 * distance() + 0.20862) * (enclose() + 0.203971 * distance() + 1.36287) - (-0.00209145 * distance() + -0.014846) * (enclose() + 0.714221 * distance() + 3.44845) * (enclose() + 0.714221 * distance() + 3.44845)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0922736 * (1 - exp(-0.311018 * (distance() + 0.306103))) * pow(width() , 0.0165075 * distance() + -0.0785292) * (0.0574324 * thickness() + 0.17979)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0189698 * (0.841461 - exp(-1.41631 * (enclose() + 3.40258) - -0.0357605 * (enclose() + 10.5907) * (enclose() + 10.5907)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = MAXDISTANCE_metal4
    max_enclose = MAXENCLOSE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.0922736 * (1 - exp(-0.311018 * (distance() + 0.306103))) * pow(width() , 0.0165075 * distance() + -0.0785292) * (0.0574324 * thickness() + 0.17979)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0171057 * (1 - exp(-0.401418 * (distance() + 0.0442357)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.114236 * pow(width() , 0.0532963) * (0.0595251 * thickness() + 0.131795)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00012446 * distance() + 0.0391843) * (0.841461 - exp(-(0.120769 * distance() + 0.20862) * (enclose() + 0.203971 * distance() + 1.36287) - (-0.00209145 * distance() + -0.014846) * (enclose() + 0.714221 * distance() + 3.44845) * (enclose() + 0.714221 * distance() + 3.44845)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.0922736 * (1 - exp(-0.311018 * (distance() + 0.306103))) * pow(width() , 0.0165075 * distance() + -0.0785292) * (0.0574324 * thickness() + 0.17979)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0189698 * (0.841461 - exp(-1.41631 * (enclose() + 3.40258) - -0.0357605 * (enclose() + 10.5907) * (enclose() + 10.5907)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal4,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal4 tndiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0998076 * (1 - exp(-0.310564 * (distance() + 0.31151))) * pow(width() , 0.0165291 * distance() + -0.0775149) * (0.0474732 * thickness() + 0.185543) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0186653 * (1 - exp(-0.401768 * (distance() + 0.0471059))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.119072 * pow(width() , 0.0543856) * (0.0538533 * thickness() + 0.141273) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.82)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal4 tpdiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal4
    max_radius = MAXRADIUS_metal4
    max_distance = MAXDISTANCE_metal4
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0998076 * (1 - exp(-0.310564 * (distance() + 0.31151))) * pow(width() , 0.0165291 * distance() + -0.0775149) * (0.0474732 * thickness() + 0.185543) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0186653 * (1 - exp(-0.401768 * (distance() + 0.0471059))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 2.82)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.119072 * pow(width() , 0.0543856) * (0.0538533 * thickness() + 0.141273) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 2.82)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal5,metal6,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal5 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.16182 - 1.40678 * distance()) + 0.052568 / pow(distance() , 1.27028)) * 4.08436 * pow((width1() + width2()) / 2 , 0.0809939) * (0.0626877 * (thickness1() + thickness2()) / 2 + 0.0222986) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.64)))
]
CAPACITANCE NEARBODY metal5 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.16182 - 1.40678 * distance()) + 0.052568 / pow(distance() , 1.27028)) * 4.08436 * pow((width1() + width2()) / 2 , 0.0809939) * (0.0626877 * (thickness1() + thickness2()) / 2 + 0.0222986) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.64)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal5,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.211506 * (1 - exp(-0.971376 * (distance() + 0.0529859))) * pow(width() , 0.00360461 * distance() + -0.00625108) * (0.056644 * thickness() + 0.191477)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0442128 * (1 - exp(-1.00345 * (distance() + 0.00996786)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.164063 * pow(width() , 0.0330153) * (0.105166 * thickness() + 0.239352)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00022931 * distance() + 0.118136) * (0.714439 - exp(-(-0.0417182 * distance() + 1.99618) * (enclose() + 0.0023166 * distance() + 0.608117) - (0.0456275 * distance() + -0.433181) * (enclose() + -0.366957 * distance() + 1.50514) * (enclose() + -0.366957 * distance() + 1.50514)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.211506 * (1 - exp(-0.971376 * (distance() + 0.0529859))) * pow(width() , 0.00360461 * distance() + -0.00625108) * (0.056644 * thickness() + 0.191477)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0602146 * (0.714439 - exp(-1.579 * (enclose() + 0.631283) - 0.023094 * (enclose() + -2.16443) * (enclose() + -2.16443)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal5 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.211506 * (1 - exp(-0.971376 * (distance() + 0.0529859))) * pow(width() , 0.00360461 * distance() + -0.00625108) * (0.056644 * thickness() + 0.191477)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0442128 * (1 - exp(-1.00345 * (distance() + 0.00996786)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.164063 * pow(width() , 0.0330153) * (0.105166 * thickness() + 0.239352)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00022931 * distance() + 0.118136) * (0.714439 - exp(-(-0.0417182 * distance() + 1.99618) * (enclose() + 0.0023166 * distance() + 0.608117) - (0.0456275 * distance() + -0.433181) * (enclose() + -0.366957 * distance() + 1.50514) * (enclose() + -0.366957 * distance() + 1.50514)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.211506 * (1 - exp(-0.971376 * (distance() + 0.0529859))) * pow(width() , 0.00360461 * distance() + -0.00625108) * (0.056644 * thickness() + 0.191477)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0602146 * (0.714439 - exp(-1.579 * (enclose() + 0.631283) - 0.023094 * (enclose() + -2.16443) * (enclose() + -2.16443)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal5,metal6,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal5 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0390666 * (1 - exp(-0.804612 * (distance() + 0.079531))) * pow(width() , 0.00692989 * distance() + -0.00606715) * (0.137845 * thickness() + 0.143136) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00728375 * (1 - exp(-0.885921 * (distance() + -0.00038481))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0790334 * pow(width() , 0.0559725) * (0.0857778 * thickness() + 0.0650641) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.64)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal5 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0390666 * (1 - exp(-0.804612 * (distance() + 0.079531))) * pow(width() , 0.00692989 * distance() + -0.00606715) * (0.137845 * thickness() + 0.143136) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00728375 * (1 - exp(-0.885921 * (distance() + -0.00038481))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0790334 * pow(width() , 0.0559725) * (0.0857778 * thickness() + 0.0650641) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.64)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal5,metal7,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal5 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.21213 - 0.901931 * distance()) + 0.0742481 / pow(distance() , 1.17286)) * 4.06885 * pow((width1() + width2()) / 2 , 0.0984388) * (0.0524521 * (thickness1() + thickness2()) / 2 + 0.0240086) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.64)))
]
CAPACITANCE NEARBODY metal5 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.21213 - 0.901931 * distance()) + 0.0742481 / pow(distance() , 1.17286)) * 4.06885 * pow((width1() + width2()) / 2 , 0.0984388) * (0.0524521 * (thickness1() + thickness2()) / 2 + 0.0240086) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.64)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal5,metal7,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal5 metal7 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.14602 * (1 - exp(-0.484162 * (distance() + 0.155549))) * pow(width() , 0.0111791 * distance() + -0.0442023) * (0.0553159 * thickness() + 0.186953)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0289684 * (1 - exp(-0.567243 * (distance() + 0.0155663)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.13997 * pow(width() , 0.044435) * (0.0770451 * thickness() + 0.184925)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (8.08858e-05 * distance() + 0.177346) * (0.304087 - exp(-(-0.0278983 * distance() + 0.00568989) * (enclose() + 10.8238 * distance() + 25.4157) - (0.00010789 * distance() + 0.0144763) * (enclose() + 4.16149 * distance() + 8.03503) * (enclose() + 4.16149 * distance() + 8.03503)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.14602 * (1 - exp(-0.484162 * (distance() + 0.155549))) * pow(width() , 0.0111791 * distance() + -0.0442023) * (0.0553159 * thickness() + 0.186953)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0890774 * (0.304087 - exp(0.273293 * (enclose() + 133.654) - 0.0155552 * (enclose() + 49.6499) * (enclose() + 49.6499)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal5 metal7 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.14602 * (1 - exp(-0.484162 * (distance() + 0.155549))) * pow(width() , 0.0111791 * distance() + -0.0442023) * (0.0553159 * thickness() + 0.186953)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0289684 * (1 - exp(-0.567243 * (distance() + 0.0155663)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.13997 * pow(width() , 0.044435) * (0.0770451 * thickness() + 0.184925)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (8.08858e-05 * distance() + 0.177346) * (0.304087 - exp(-(-0.0278983 * distance() + 0.00568989) * (enclose() + 10.8238 * distance() + 25.4157) - (0.00010789 * distance() + 0.0144763) * (enclose() + 4.16149 * distance() + 8.03503) * (enclose() + 4.16149 * distance() + 8.03503)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.14602 * (1 - exp(-0.484162 * (distance() + 0.155549))) * pow(width() , 0.0111791 * distance() + -0.0442023) * (0.0553159 * thickness() + 0.186953)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0890774 * (0.304087 - exp(0.273293 * (enclose() + 133.654) - 0.0155552 * (enclose() + 49.6499) * (enclose() + 49.6499)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal5,metal7,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal5 tndiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0579179 * (1 - exp(-0.458028 * (distance() + 0.177147))) * pow(width() , 0.0124121 * distance() + -0.0456509) * (0.0826036 * thickness() + 0.171725) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0110919 * (1 - exp(-0.548953 * (distance() + 0.0154221))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0955532 * pow(width() , 0.0522718) * (0.0625708 * thickness() + 0.0971058) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.64)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal5 tpdiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0579179 * (1 - exp(-0.458028 * (distance() + 0.177147))) * pow(width() , 0.0124121 * distance() + -0.0456509) * (0.0826036 * thickness() + 0.171725) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0110919 * (1 - exp(-0.548953 * (distance() + 0.0154221))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0955532 * pow(width() , 0.0522718) * (0.0625708 * thickness() + 0.0971058) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.64)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal5,metal8,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal5 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.17604 - 0.679183 * distance()) + 0.105248 / pow(distance() , 1.10731)) * 4.09881 * pow((width1() + width2()) / 2 , 0.114034) * (0.0425345 * (thickness1() + thickness2()) / 2 + 0.0211109) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.64)))
]
CAPACITANCE NEARBODY metal5 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = length() * (exp(-1.17604 - 0.679183 * distance()) + 0.105248 / pow(distance() , 1.10731)) * 4.09881 * pow((width1() + width2()) / 2 , 0.114034) * (0.0425345 * (thickness1() + thickness2()) / 2 + 0.0211109) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 3.64)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal5,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal5 metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.117378 * (1 - exp(-0.331314 * (distance() + 0.282833))) * pow(width() , 0.0157524 * distance() + -0.0736506) * (0.0516632 * thickness() + 0.18374)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0221125 * (1 - exp(-0.420516 * (distance() + 0.0418306)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.126266 * pow(width() , 0.0520855) * (0.0638289 * thickness() + 0.156683)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00017928 * distance() + 0.0532962) * (0.776559 - exp(-(0.0547803 * distance() + 0.176579) * (enclose() + 0.15926 * distance() + 1.38895) - (-0.00018233 * distance() + -0.0047878) * (enclose() + -1.73136 * distance() + 3.8177) * (enclose() + -1.73136 * distance() + 3.8177)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.117378 * (1 - exp(-0.331314 * (distance() + 0.282833))) * pow(width() , 0.0157524 * distance() + -0.0736506) * (0.0516632 * thickness() + 0.18374)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0257517 * (0.776559 - exp(-0.724382 * (enclose() + 2.98155) - -0.0066111 * (enclose() + -13.4959) * (enclose() + -13.4959)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal5 metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = MAXDISTANCE_metal5
    max_enclose = MAXENCLOSE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.117378 * (1 - exp(-0.331314 * (distance() + 0.282833))) * pow(width() , 0.0157524 * distance() + -0.0736506) * (0.0516632 * thickness() + 0.18374)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0221125 * (1 - exp(-0.420516 * (distance() + 0.0418306)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.126266 * pow(width() , 0.0520855) * (0.0638289 * thickness() + 0.156683)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (-0.00017928 * distance() + 0.0532962) * (0.776559 - exp(-(0.0547803 * distance() + 0.176579) * (enclose() + 0.15926 * distance() + 1.38895) - (-0.00018233 * distance() + -0.0047878) * (enclose() + -1.73136 * distance() + 3.8177) * (enclose() + -1.73136 * distance() + 3.8177)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.117378 * (1 - exp(-0.331314 * (distance() + 0.282833))) * pow(width() , 0.0157524 * distance() + -0.0736506) * (0.0516632 * thickness() + 0.18374)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0257517 * (0.776559 - exp(-0.724382 * (enclose() + 2.98155) - -0.0066111 * (enclose() + -13.4959) * (enclose() + -13.4959)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal5,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal5 tndiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0757891 * (1 - exp(-0.325501 * (distance() + 0.297432))) * pow(width() , 0.0159518 * distance() + -0.0717938) * (0.0579347 * thickness() + 0.1806) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0141526 * (1 - exp(-0.415937 * (distance() + 0.0449003))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.106226 * pow(width() , 0.0557066) * (0.0535805 * thickness() + 0.117622) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.64)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal5 tpdiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal5
    max_radius = MAXRADIUS_metal5
    max_distance = MAXDISTANCE_metal5
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0757891 * (1 - exp(-0.325501 * (distance() + 0.297432))) * pow(width() , 0.0159518 * distance() + -0.0717938) * (0.0579347 * thickness() + 0.1806) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0141526 * (1 - exp(-0.415937 * (distance() + 0.0449003))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 3.64)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.106226 * pow(width() , 0.0557066) * (0.0535805 * thickness() + 0.117622) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 3.64)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal6,metal7,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal6 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.19933 - 1.36912 * distance()) + 0.0596706 / pow(distance() , 1.23359)) * 4.13093 * pow((width1() + width2()) / 2 , 0.0844958) * (0.0600514 * (thickness1() + thickness2()) / 2 + 0.0215208) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.46)))
]
CAPACITANCE NEARBODY metal6 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.19933 - 1.36912 * distance()) + 0.0596706 / pow(distance() , 1.23359)) * 4.13093 * pow((width1() + width2()) / 2 , 0.0844958) * (0.0600514 * (thickness1() + thickness2()) / 2 + 0.0215208) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.46)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal6,metal7,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal6 metal7 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.215376 * (1 - exp(-0.954386 * (distance() + 0.0627533))) * pow(width() , 0.00335907 * distance() + -0.00214619) * (0.0558554 * thickness() + 0.193297)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0452424 * (1 - exp(-0.977063 * (distance() + 0.0180269)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.166773 * pow(width() , 0.0378135) * (0.105715 * thickness() + 0.243026)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00038001 * distance() + 0.126937) * (0.672892 - exp(-(-0.0437759 * distance() + 1.94625) * (enclose() + 0.0108813 * distance() + 0.627174) - (0.0442727 * distance() + -0.429148) * (enclose() + -0.368194 * distance() + 1.46902) * (enclose() + -0.368194 * distance() + 1.46902)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.215376 * (1 - exp(-0.954386 * (distance() + 0.0627533))) * pow(width() , 0.00335907 * distance() + -0.00214619) * (0.0558554 * thickness() + 0.193297)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0653685 * (0.672892 - exp(-1.50849 * (enclose() + 0.735987) - 0.013579 * (enclose() + -2.21292) * (enclose() + -2.21292)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal6 metal7 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.215376 * (1 - exp(-0.954386 * (distance() + 0.0627533))) * pow(width() , 0.00335907 * distance() + -0.00214619) * (0.0558554 * thickness() + 0.193297)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0452424 * (1 - exp(-0.977063 * (distance() + 0.0180269)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.166773 * pow(width() , 0.0378135) * (0.105715 * thickness() + 0.243026)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00038001 * distance() + 0.126937) * (0.672892 - exp(-(-0.0437759 * distance() + 1.94625) * (enclose() + 0.0108813 * distance() + 0.627174) - (0.0442727 * distance() + -0.429148) * (enclose() + -0.368194 * distance() + 1.46902) * (enclose() + -0.368194 * distance() + 1.46902)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.215376 * (1 - exp(-0.954386 * (distance() + 0.0627533))) * pow(width() , 0.00335907 * distance() + -0.00214619) * (0.0558554 * thickness() + 0.193297)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.0653685 * (0.672892 - exp(-1.50849 * (enclose() + 0.735987) - 0.013579 * (enclose() + -2.21292) * (enclose() + -2.21292)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal6,metal7,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal6 tndiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0345452 * (1 - exp(-0.78604 * (distance() + 0.0945783))) * pow(width() , 0.00673208 * distance() + -0.0010703) * (0.136296 * thickness() + 0.135161) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.46)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00613601 * (1 - exp(-0.859413 * (distance() + 0.0098026))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.46)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0742582 * pow(width() , 0.0619866) * (0.0801612 * thickness() + 0.0578471) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.46)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal6 tpdiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0345452 * (1 - exp(-0.78604 * (distance() + 0.0945783))) * pow(width() , 0.00673208 * distance() + -0.0010703) * (0.136296 * thickness() + 0.135161) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.46)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00613601 * (1 - exp(-0.859413 * (distance() + 0.0098026))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.46)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0742582 * pow(width() , 0.0619866) * (0.0801612 * thickness() + 0.0578471) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.46)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal6,metal8,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal6 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.21736 - 0.800938 * distance()) + 0.0921155 / pow(distance() , 1.12699)) * 4.11037 * pow((width1() + width2()) / 2 , 0.105265) * (0.0467716 * (thickness1() + thickness2()) / 2 + 0.0220782) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.46)))
]
CAPACITANCE NEARBODY metal6 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = length() * (exp(-1.21736 - 0.800938 * distance()) + 0.0921155 / pow(distance() , 1.12699)) * 4.11037 * pow((width1() + width2()) / 2 , 0.105265) * (0.0467716 * (thickness1() + thickness2()) / 2 + 0.0220782) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 4.46)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal6,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal6 metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.146345 * (1 - exp(-0.42617 * (distance() + 0.208001))) * pow(width() , 0.0123388 * distance() + -0.0498388) * (0.0514742 * thickness() + 0.188068)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.02875 * (1 - exp(-0.507175 * (distance() + 0.0343475)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.140959 * pow(width() , 0.0500343) * (0.0729675 * thickness() + 0.184769)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00062933 * distance() + 0.197421) * (0.262512 - exp(-(-0.0822954 * distance() + 0.085467) * (enclose() + 6.22983 * distance() + 11.515) - (1.88261e-05 * distance() + 0.0140378) * (enclose() + 5.88083 * distance() + 4.2129) * (enclose() + 5.88083 * distance() + 4.2129)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.146345 * (1 - exp(-0.42617 * (distance() + 0.208001))) * pow(width() , 0.0123388 * distance() + -0.0498388) * (0.0514742 * thickness() + 0.188068)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.101857 * (0.262512 - exp(0.737487 * (enclose() + 73.8133) - 0.0142261 * (enclose() + 63.0212) * (enclose() + 63.0212)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal6 metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = MAXDISTANCE_metal6
    max_enclose = MAXENCLOSE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.146345 * (1 - exp(-0.42617 * (distance() + 0.208001))) * pow(width() , 0.0123388 * distance() + -0.0498388) * (0.0514742 * thickness() + 0.188068)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.02875 * (1 - exp(-0.507175 * (distance() + 0.0343475)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.140959 * pow(width() , 0.0500343) * (0.0729675 * thickness() + 0.184769)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00062933 * distance() + 0.197421) * (0.262512 - exp(-(-0.0822954 * distance() + 0.085467) * (enclose() + 6.22983 * distance() + 11.515) - (1.88261e-05 * distance() + 0.0140378) * (enclose() + 5.88083 * distance() + 4.2129) * (enclose() + 5.88083 * distance() + 4.2129)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.146345 * (1 - exp(-0.42617 * (distance() + 0.208001))) * pow(width() , 0.0123388 * distance() + -0.0498388) * (0.0514742 * thickness() + 0.188068)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.101857 * (0.262512 - exp(0.737487 * (enclose() + 73.8133) - 0.0142261 * (enclose() + 63.0212) * (enclose() + 63.0212)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal6,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal6 tndiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.052751 * (1 - exp(-0.406487 * (distance() + 0.23218))) * pow(width() , 0.0131796 * distance() + -0.048731) * (0.0791552 * thickness() + 0.173012) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.46)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0100237 * (1 - exp(-0.492293 * (distance() + 0.0366332))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.46)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.092478 * pow(width() , 0.0577741) * (0.0570714 * thickness() + 0.0919145) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.46)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal6 tpdiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal6
    max_radius = MAXRADIUS_metal6
    max_distance = MAXDISTANCE_metal6
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.052751 * (1 - exp(-0.406487 * (distance() + 0.23218))) * pow(width() , 0.0131796 * distance() + -0.048731) * (0.0791552 * thickness() + 0.173012) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.46)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.0100237 * (1 - exp(-0.492293 * (distance() + 0.0366332))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 4.46)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.092478 * pow(width() , 0.0577741) * (0.0570714 * thickness() + 0.0919145) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 4.46)))
    }
]
//
//  model: 36  target: nearbody  profile: active  layers: active_base,metal7,metal8,tndiff,tpdiff
//
CAPACITANCE NEARBODY metal7 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(-1.24535 - 1.17705 * distance()) + 0.068322 / pow(distance() , 1.1795)) * 4.16356 * pow((width1() + width2()) / 2 , 0.0902686) * (0.0567239 * (thickness1() + thickness2()) / 2 + 0.0233387) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 5.28)))
]
CAPACITANCE NEARBODY metal7 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = length() * (exp(-1.24535 - 1.17705 * distance()) + 0.068322 / pow(distance() , 1.1795)) * 4.16356 * pow((width1() + width2()) / 2 , 0.0902686) * (0.0567239 * (thickness1() + thickness2()) / 2 + 0.0233387) * (1 - m36RS * exp((-m43nbk1 * radius_down() - m43nbk2 * radius_up()) / (m43nbk3 * distance() + m43nbk4 * 5.28)))
]
//
//  model: 36  target: crossovere  profile: active  layers: active_base,metal7,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal7 metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.20109 * (1 - exp(-0.755551 * (distance() + 0.0941876))) * pow(width() , 0.00522522 * distance() + -0.00888925) * (0.0544143 * thickness() + 0.19433)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0420383 * (1 - exp(-0.79712 * (distance() + 0.0231218)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.162787 * pow(width() , 0.043225) * (0.096302 * thickness() + 0.234247)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00122886 * distance() + 0.2105) * (0.361967 - exp(-(-0.108137 * distance() + 0.176739) * (enclose() + 2.20734 * distance() + 2.82089) - (0.00193214 * distance() + 0.0549313) * (enclose() + 1.5468 * distance() + 2.63631) * (enclose() + 1.5468 * distance() + 2.63631)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.20109 * (1 - exp(-0.755551 * (distance() + 0.0941876))) * pow(width() , 0.00522522 * distance() + -0.00888925) * (0.0544143 * thickness() + 0.19433)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.111394 * (0.361967 - exp(0.904631 * (enclose() + 24.8943) - 0.0742527 * (enclose() + 18.1043) * (enclose() + 18.1043)))
    }
    if (C < 0.0) {
        C = 0
    }
]
CAPACITANCE CROSSOVER FRINGE metal7 metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_calenclose = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = MAXDISTANCE_metal7
    max_enclose = MAXENCLOSE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0) && (enclose() >= max_enclose)) {
        C = length() * 0.20109 * (1 - exp(-0.755551 * (distance() + 0.0941876))) * pow(width() , 0.00522522 * distance() + -0.00888925) * (0.0544143 * thickness() + 0.19433)
    }
    if ((distance() > 0.0) && (same_net() == 1) && (enclose() >= max_enclose)) {
        C = length() * 0.0420383 * (1 - exp(-0.79712 * (distance() + 0.0231218)))
    }
    if ((distance() <= 0.0) && (enclose() >= max_enclose)) {
        C = length() * 0.162787 * pow(width() , 0.043225) * (0.096302 * thickness() + 0.234247)
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() < distance())) {
        C = length() * 0.5 * (0.00122886 * distance() + 0.2105) * (0.361967 - exp(-(-0.108137 * distance() + 0.176739) * (enclose() + 2.20734 * distance() + 2.82089) - (0.00193214 * distance() + 0.0549313) * (enclose() + 1.5468 * distance() + 2.63631) * (enclose() + 1.5468 * distance() + 2.63631)))
    }
    if ((distance() > 0.0) && (enclose() < max_enclose) && (enclose() >= distance())) {
        C = length() * 0.20109 * (1 - exp(-0.755551 * (distance() + 0.0941876))) * pow(width() , 0.00522522 * distance() + -0.00888925) * (0.0544143 * thickness() + 0.19433)
    }
    if ((distance() <= 0.0) && (enclose() < max_enclose)) {
        C = length() * 0.111394 * (0.361967 - exp(0.904631 * (enclose() + 24.8943) - 0.0742527 * (enclose() + 18.1043) * (enclose() + 18.1043)))
    }
    if (C < 0.0) {
        C = 0
    }
]
//
//  model: 36  target: intrinsic  profile: active  layers: active_base,metal7,metal8,tndiff,tpdiff
//
CAPACITANCE CROSSOVER FRINGE metal7 tndiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0324334 * (1 - exp(-0.653567 * (distance() + 0.131932))) * pow(width() , 0.00791396 * distance() + -0.00698592) * (0.124919 * thickness() + 0.149083) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.28)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00603635 * (1 - exp(-0.726289 * (distance() + 0.022759))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.28)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0747956 * pow(width() , 0.0638498) * (0.0681712 * thickness() + 0.0603892) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 5.28)))
    }
]
CAPACITANCE CROSSOVER FRINGE metal7 tpdiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    max_width = MAXWIDTH_metal7
    max_radius = MAXRADIUS_metal7
    max_distance = MAXDISTANCE_metal7
    C = 0.0
    if ((distance() > 0.0) && (same_net() == 0)) {
        C = length() * 0.0324334 * (1 - exp(-0.653567 * (distance() + 0.131932))) * pow(width() , 0.00791396 * distance() + -0.00698592) * (0.124919 * thickness() + 0.149083) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.28)))
    }
    if ((distance() > 0.0) && (same_net() == 1)) {
        C = length() * 0.00603635 * (1 - exp(-0.726289 * (distance() + 0.022759))) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * distance() + m43ink4 * 5.28)))
    }
    if (distance() <= 0.0) {
        C = length() * 0.0747956 * pow(width() , 0.0638498) * (0.0681712 * thickness() + 0.0603892) * (1 - m36RS * exp((-m43ink1 * radius_down() - m43ink2 * radius_up()) / (m43ink3 * 10 + m43ink4 * 5.28)))
    }
]
#endif // XCALIBRATE_EXCLUDE_PROFILE_active
#ifndef XCALIBRATE_EXCLUDE_PROFILE_sdcont
//
//  model: 2013  target: viacap  profile: sdcont  layers: sdcont_base,odCont,diff,metal1,tndiff,tpdiff
//
CAPACITANCE CONNECTION tndiff metal1 MASK
[
    PROPERTY C
    C = 0.0269267
]
CAPACITANCE CONNECTION tpdiff metal1 MASK
[
    PROPERTY C
    C = 0.0269267
]
//
//  model: 2016  target: nearbody2  profile: sdcont  layers: sdcont_base,poly,odCont,diff,metal1,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_MODEL_2016 // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_MODEL_2016
CAPACITANCE NEARBODY COND2VIA poly tndiff metal1 MASK
[
    PROPERTY C
    max_caldistance = 1.24 // For reference
    max_distance = 1.24
    C = length() * (0.0117169 * exp(-1.46742 * distance())) / pow(distance() , 0.918407)
]
#endif // $XCALIBRATE_INCLUDE_MODEL_2016
// #DEFINE $XCALIBRATE_INCLUDE_MODEL_2016 // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_MODEL_2016
CAPACITANCE NEARBODY COND2VIA poly tpdiff metal1 MASK
[
    PROPERTY C
    max_caldistance = 1.24 // For reference
    max_distance = 1.24
    C = length() * (0.0117169 * exp(-1.46742 * distance())) / pow(distance() , 0.918407)
]
#endif // $XCALIBRATE_INCLUDE_MODEL_2016
#endif // XCALIBRATE_EXCLUDE_PROFILE_sdcont
#DEFINE XCALIBRATE_EXCLUDE_PROFILE_diffcap
#ifndef XCALIBRATE_EXCLUDE_PROFILE_diffcap
//
//  model: 614  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff poly MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.148808 * exp(-9.24459 * distance()) + 0.0746044 * exp(-0.244561 * distance())) * pow((width1() + width2()) / 2 , 0.107466) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.148808 * exp(-9.24459 * distance()) + 0.0746044 * exp(-0.244561 * distance())) * pow((width1() + width2()) / 2 , 0.107466)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff poly MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.148808 * exp(-9.24459 * distance()) + 0.0746044 * exp(-0.244561 * distance())) * pow((width1() + width2()) / 2 , 0.107466) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.148808 * exp(-9.24459 * distance()) + 0.0746044 * exp(-0.244561 * distance())) * pow((width1() + width2()) / 2 , 0.107466)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.148808 * exp(-9.24459 * distance()) + 0.0746044 * exp(-0.244561 * distance())) * pow((width1() + width2()) / 2 , 0.107466) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.148808 * exp(-9.24459 * distance()) + 0.0746044 * exp(-0.244561 * distance())) * pow((width1() + width2()) / 2 , 0.107466)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.148808 * exp(-9.24459 * distance()) + 0.0746044 * exp(-0.244561 * distance())) * pow((width1() + width2()) / 2 , 0.107466) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.148808 * exp(-9.24459 * distance()) + 0.0746044 * exp(-0.244561 * distance())) * pow((width1() + width2()) / 2 , 0.107466)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 614  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0346044 + 0.0537638 * exp(-0.811635 * distance())) * pow((width1() + width2()) / 2 , 0.175985) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0346044 + 0.0537638 * exp(-0.811635 * distance())) * pow((width1() + width2()) / 2 , 0.175985)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0346044 + 0.0537638 * exp(-0.811635 * distance())) * pow((width1() + width2()) / 2 , 0.175985) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0346044 + 0.0537638 * exp(-0.811635 * distance())) * pow((width1() + width2()) / 2 , 0.175985)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0346044 + 0.0537638 * exp(-0.811635 * distance())) * pow((width1() + width2()) / 2 , 0.175985) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0346044 + 0.0537638 * exp(-0.811635 * distance())) * pow((width1() + width2()) / 2 , 0.175985)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0346044 + 0.0537638 * exp(-0.811635 * distance())) * pow((width1() + width2()) / 2 , 0.175985) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0346044 + 0.0537638 * exp(-0.811635 * distance())) * pow((width1() + width2()) / 2 , 0.175985)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 614  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0296538 * exp(-0.117926 * distance()) + 0.0296538 * exp(-0.117931 * distance())) * pow((width1() + width2()) / 2 , 0.164289) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0296538 * exp(-0.117926 * distance()) + 0.0296538 * exp(-0.117931 * distance())) * pow((width1() + width2()) / 2 , 0.164289)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0296538 * exp(-0.117926 * distance()) + 0.0296538 * exp(-0.117931 * distance())) * pow((width1() + width2()) / 2 , 0.164289) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0296538 * exp(-0.117926 * distance()) + 0.0296538 * exp(-0.117931 * distance())) * pow((width1() + width2()) / 2 , 0.164289)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0296538 * exp(-0.117926 * distance()) + 0.0296538 * exp(-0.117931 * distance())) * pow((width1() + width2()) / 2 , 0.164289) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0296538 * exp(-0.117926 * distance()) + 0.0296538 * exp(-0.117931 * distance())) * pow((width1() + width2()) / 2 , 0.164289)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0296538 * exp(-0.117926 * distance()) + 0.0296538 * exp(-0.117931 * distance())) * pow((width1() + width2()) / 2 , 0.164289) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0296538 * exp(-0.117926 * distance()) + 0.0296538 * exp(-0.117931 * distance())) * pow((width1() + width2()) / 2 , 0.164289)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 614  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0239468 * exp(-0.0659974 * distance()) + 0.0239464 * exp(-0.0667538 * distance())) * pow((width1() + width2()) / 2 , 0.150305) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0239468 * exp(-0.0659974 * distance()) + 0.0239464 * exp(-0.0667538 * distance())) * pow((width1() + width2()) / 2 , 0.150305)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0239468 * exp(-0.0659974 * distance()) + 0.0239464 * exp(-0.0667538 * distance())) * pow((width1() + width2()) / 2 , 0.150305) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0239468 * exp(-0.0659974 * distance()) + 0.0239464 * exp(-0.0667538 * distance())) * pow((width1() + width2()) / 2 , 0.150305)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0239468 * exp(-0.0659974 * distance()) + 0.0239464 * exp(-0.0667538 * distance())) * pow((width1() + width2()) / 2 , 0.150305) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0239468 * exp(-0.0659974 * distance()) + 0.0239464 * exp(-0.0667538 * distance())) * pow((width1() + width2()) / 2 , 0.150305)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0239468 * exp(-0.0659974 * distance()) + 0.0239464 * exp(-0.0667538 * distance())) * pow((width1() + width2()) / 2 , 0.150305) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0239468 * exp(-0.0659974 * distance()) + 0.0239464 * exp(-0.0667538 * distance())) * pow((width1() + width2()) / 2 , 0.150305)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 614  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0209265 * exp(-0.0883365 * distance()) + 0.0208953) * pow((width1() + width2()) / 2 , 0.139885) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0209265 * exp(-0.0883365 * distance()) + 0.0208953) * pow((width1() + width2()) / 2 , 0.139885)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0209265 * exp(-0.0883365 * distance()) + 0.0208953) * pow((width1() + width2()) / 2 , 0.139885) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0209265 * exp(-0.0883365 * distance()) + 0.0208953) * pow((width1() + width2()) / 2 , 0.139885)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0209265 * exp(-0.0883365 * distance()) + 0.0208953) * pow((width1() + width2()) / 2 , 0.139885) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0209265 * exp(-0.0883365 * distance()) + 0.0208953) * pow((width1() + width2()) / 2 , 0.139885)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0209265 * exp(-0.0883365 * distance()) + 0.0208953) * pow((width1() + width2()) / 2 , 0.139885) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0209265 * exp(-0.0883365 * distance()) + 0.0208953) * pow((width1() + width2()) / 2 , 0.139885)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 614  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189081 * exp(-0.0238696 * distance()) + 0.0189081 * exp(-0.0238696 * distance())) * pow((width1() + width2()) / 2 , 0.132347) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189081 * exp(-0.0238696 * distance()) + 0.0189081 * exp(-0.0238696 * distance())) * pow((width1() + width2()) / 2 , 0.132347)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189081 * exp(-0.0238696 * distance()) + 0.0189081 * exp(-0.0238696 * distance())) * pow((width1() + width2()) / 2 , 0.132347) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189081 * exp(-0.0238696 * distance()) + 0.0189081 * exp(-0.0238696 * distance())) * pow((width1() + width2()) / 2 , 0.132347)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189081 * exp(-0.0238696 * distance()) + 0.0189081 * exp(-0.0238696 * distance())) * pow((width1() + width2()) / 2 , 0.132347) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189081 * exp(-0.0238696 * distance()) + 0.0189081 * exp(-0.0238696 * distance())) * pow((width1() + width2()) / 2 , 0.132347)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189081 * exp(-0.0238696 * distance()) + 0.0189081 * exp(-0.0238696 * distance())) * pow((width1() + width2()) / 2 , 0.132347) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189081 * exp(-0.0238696 * distance()) + 0.0189081 * exp(-0.0238696 * distance())) * pow((width1() + width2()) / 2 , 0.132347)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 614  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0175014 * exp(-0.0133694 * distance()) + 0.0175014 * exp(-0.0140679 * distance())) * pow((width1() + width2()) / 2 , 0.126196) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0175014 * exp(-0.0133694 * distance()) + 0.0175014 * exp(-0.0140679 * distance())) * pow((width1() + width2()) / 2 , 0.126196)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0175014 * exp(-0.0133694 * distance()) + 0.0175014 * exp(-0.0140679 * distance())) * pow((width1() + width2()) / 2 , 0.126196) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0175014 * exp(-0.0133694 * distance()) + 0.0175014 * exp(-0.0140679 * distance())) * pow((width1() + width2()) / 2 , 0.126196)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0175014 * exp(-0.0133694 * distance()) + 0.0175014 * exp(-0.0140679 * distance())) * pow((width1() + width2()) / 2 , 0.126196) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0175014 * exp(-0.0133694 * distance()) + 0.0175014 * exp(-0.0140679 * distance())) * pow((width1() + width2()) / 2 , 0.126196)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0175014 * exp(-0.0133694 * distance()) + 0.0175014 * exp(-0.0140679 * distance())) * pow((width1() + width2()) / 2 , 0.126196) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0175014 * exp(-0.0133694 * distance()) + 0.0175014 * exp(-0.0140679 * distance())) * pow((width1() + width2()) / 2 , 0.126196)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 614  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.016474 * exp(-0.0143202 * distance()) + 0.016474) * pow((width1() + width2()) / 2 , 0.12032) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.016474 * exp(-0.0143202 * distance()) + 0.016474) * pow((width1() + width2()) / 2 , 0.12032)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.016474 * exp(-0.0143202 * distance()) + 0.016474) * pow((width1() + width2()) / 2 , 0.12032) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.016474 * exp(-0.0143202 * distance()) + 0.016474) * pow((width1() + width2()) / 2 , 0.12032)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.016474 * exp(-0.0143202 * distance()) + 0.016474) * pow((width1() + width2()) / 2 , 0.12032) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.016474 * exp(-0.0143202 * distance()) + 0.016474) * pow((width1() + width2()) / 2 , 0.12032)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.016474 * exp(-0.0143202 * distance()) + 0.016474) * pow((width1() + width2()) / 2 , 0.12032) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.016474 * exp(-0.0143202 * distance()) + 0.016474) * pow((width1() + width2()) / 2 , 0.12032)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 614  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0159026 * exp(-0.00238209 * distance()) + 0.0159026 * exp(-0.00238209 * distance())) * pow((width1() + width2()) / 2 , 0.102111) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0159026 * exp(-0.00238209 * distance()) + 0.0159026 * exp(-0.00238209 * distance())) * pow((width1() + width2()) / 2 , 0.102111)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0159026 * exp(-0.00238209 * distance()) + 0.0159026 * exp(-0.00238209 * distance())) * pow((width1() + width2()) / 2 , 0.102111) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0159026 * exp(-0.00238209 * distance()) + 0.0159026 * exp(-0.00238209 * distance())) * pow((width1() + width2()) / 2 , 0.102111)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0159026 * exp(-0.00238209 * distance()) + 0.0159026 * exp(-0.00238209 * distance())) * pow((width1() + width2()) / 2 , 0.102111) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0159026 * exp(-0.00238209 * distance()) + 0.0159026 * exp(-0.00238209 * distance())) * pow((width1() + width2()) / 2 , 0.102111)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0159026 * exp(-0.00238209 * distance()) + 0.0159026 * exp(-0.00238209 * distance())) * pow((width1() + width2()) / 2 , 0.102111) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0159026 * exp(-0.00238209 * distance()) + 0.0159026 * exp(-0.00238209 * distance())) * pow((width1() + width2()) / 2 , 0.102111)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 114  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00550015 * exp(0.00876981 * distance())) / pow(distance() , 0.191566) * pow((width1() + width2()) / 2 , -0.131183) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00550015 * exp(0.00876981 * distance())) / pow(distance() , 0.191566) * pow((width1() + width2()) / 2 , -0.131183)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tndiff INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00550015 * exp(0.00876981 * distance())) / pow(distance() , 0.191566) * pow((width1() + width2()) / 2 , -0.131183) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00550015 * exp(0.00876981 * distance())) / pow(distance() , 0.191566) * pow((width1() + width2()) / 2 , -0.131183)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff poly INSIDE OF poly MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00550015 * exp(0.00876981 * distance())) / pow(distance() , 0.191566) * pow((width1() + width2()) / 2 , -0.131183) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00550015 * exp(0.00876981 * distance())) / pow(distance() , 0.191566) * pow((width1() + width2()) / 2 , -0.131183)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tpdiff INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00550015 * exp(0.00876981 * distance())) / pow(distance() , 0.191566) * pow((width1() + width2()) / 2 , -0.131183) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00550015 * exp(0.00876981 * distance())) / pow(distance() , 0.191566) * pow((width1() + width2()) / 2 , -0.131183)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 114  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0073671 * exp(0.00114044 * distance())) / pow(distance() , 0.270122) * pow((width1() + width2()) / 2 , -0.0572827) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0073671 * exp(0.00114044 * distance())) / pow(distance() , 0.270122) * pow((width1() + width2()) / 2 , -0.0572827)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tndiff INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0073671 * exp(0.00114044 * distance())) / pow(distance() , 0.270122) * pow((width1() + width2()) / 2 , -0.0572827) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0073671 * exp(0.00114044 * distance())) / pow(distance() , 0.270122) * pow((width1() + width2()) / 2 , -0.0572827)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal1 INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0073671 * exp(0.00114044 * distance())) / pow(distance() , 0.270122) * pow((width1() + width2()) / 2 , -0.0572827) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0073671 * exp(0.00114044 * distance())) / pow(distance() , 0.270122) * pow((width1() + width2()) / 2 , -0.0572827)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tpdiff INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0073671 * exp(0.00114044 * distance())) / pow(distance() , 0.270122) * pow((width1() + width2()) / 2 , -0.0572827) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0073671 * exp(0.00114044 * distance())) / pow(distance() , 0.270122) * pow((width1() + width2()) / 2 , -0.0572827)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 114  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0105928 * exp(-0.102552 * distance())) / pow(distance() , 0.08909) * pow((width1() + width2()) / 2 , 0.0221663) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0105928 * exp(-0.102552 * distance())) / pow(distance() , 0.08909) * pow((width1() + width2()) / 2 , 0.0221663)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tndiff INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0105928 * exp(-0.102552 * distance())) / pow(distance() , 0.08909) * pow((width1() + width2()) / 2 , 0.0221663) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0105928 * exp(-0.102552 * distance())) / pow(distance() , 0.08909) * pow((width1() + width2()) / 2 , 0.0221663)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal2 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0105928 * exp(-0.102552 * distance())) / pow(distance() , 0.08909) * pow((width1() + width2()) / 2 , 0.0221663) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0105928 * exp(-0.102552 * distance())) / pow(distance() , 0.08909) * pow((width1() + width2()) / 2 , 0.0221663)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tpdiff INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0105928 * exp(-0.102552 * distance())) / pow(distance() , 0.08909) * pow((width1() + width2()) / 2 , 0.0221663) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0105928 * exp(-0.102552 * distance())) / pow(distance() , 0.08909) * pow((width1() + width2()) / 2 , 0.0221663)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 114  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.011155 * exp(-0.102817 * distance())) / pow(distance() , 0.0154493) * pow((width1() + width2()) / 2 , 0.0620448) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.011155 * exp(-0.102817 * distance())) / pow(distance() , 0.0154493) * pow((width1() + width2()) / 2 , 0.0620448)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 tndiff INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.011155 * exp(-0.102817 * distance())) / pow(distance() , 0.0154493) * pow((width1() + width2()) / 2 , 0.0620448) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.011155 * exp(-0.102817 * distance())) / pow(distance() , 0.0154493) * pow((width1() + width2()) / 2 , 0.0620448)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal3 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.011155 * exp(-0.102817 * distance())) / pow(distance() , 0.0154493) * pow((width1() + width2()) / 2 , 0.0620448) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.011155 * exp(-0.102817 * distance())) / pow(distance() , 0.0154493) * pow((width1() + width2()) / 2 , 0.0620448)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 tpdiff INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.011155 * exp(-0.102817 * distance())) / pow(distance() , 0.0154493) * pow((width1() + width2()) / 2 , 0.0620448) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.011155 * exp(-0.102817 * distance())) / pow(distance() , 0.0154493) * pow((width1() + width2()) / 2 , 0.0620448)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 114  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0106656 * exp(-0.0773431 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.08037) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0106656 * exp(-0.0773431 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.08037)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 tndiff INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0106656 * exp(-0.0773431 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.08037) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0106656 * exp(-0.0773431 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.08037)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal4 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0106656 * exp(-0.0773431 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.08037) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0106656 * exp(-0.0773431 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.08037)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 tpdiff INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0106656 * exp(-0.0773431 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.08037) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0106656 * exp(-0.0773431 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.08037)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 114  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0099647 * exp(-0.0511662 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0880779) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0099647 * exp(-0.0511662 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0880779)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 tndiff INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0099647 * exp(-0.0511662 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0880779) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0099647 * exp(-0.0511662 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0880779)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal5 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0099647 * exp(-0.0511662 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0880779) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0099647 * exp(-0.0511662 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0880779)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 tpdiff INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0099647 * exp(-0.0511662 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0880779) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0099647 * exp(-0.0511662 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0880779)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 114  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00935976 * exp(-0.0341923 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0904824) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00935976 * exp(-0.0341923 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0904824)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 tndiff INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00935976 * exp(-0.0341923 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0904824) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00935976 * exp(-0.0341923 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0904824)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal6 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00935976 * exp(-0.0341923 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0904824) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00935976 * exp(-0.0341923 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0904824)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 tpdiff INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00935976 * exp(-0.0341923 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0904824) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00935976 * exp(-0.0341923 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0904824)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 114  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal7 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0088221 * exp(-0.0235915 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0881442) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0088221 * exp(-0.0235915 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0881442)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 tndiff INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0088221 * exp(-0.0235915 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0881442) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0088221 * exp(-0.0235915 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0881442)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal7 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0088221 * exp(-0.0235915 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0881442) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0088221 * exp(-0.0235915 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0881442)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 tpdiff INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0088221 * exp(-0.0235915 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0881442) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0088221 * exp(-0.0235915 * distance())) / pow(distance() , 0) * pow((width1() + width2()) / 2 , 0.0881442)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 114  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal8 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00805342 * exp(-0.015696 * distance())) / pow(distance() , 0.00504765) * pow((width1() + width2()) / 2 , 0.0642444) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00805342 * exp(-0.015696 * distance())) / pow(distance() , 0.00504765) * pow((width1() + width2()) / 2 , 0.0642444)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 tndiff INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00805342 * exp(-0.015696 * distance())) / pow(distance() , 0.00504765) * pow((width1() + width2()) / 2 , 0.0642444) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00805342 * exp(-0.015696 * distance())) / pow(distance() , 0.00504765) * pow((width1() + width2()) / 2 , 0.0642444)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal8 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00805342 * exp(-0.015696 * distance())) / pow(distance() , 0.00504765) * pow((width1() + width2()) / 2 , 0.0642444) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00805342 * exp(-0.015696 * distance())) / pow(distance() , 0.00504765) * pow((width1() + width2()) / 2 , 0.0642444)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 tpdiff INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.38 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00805342 * exp(-0.015696 * distance())) / pow(distance() , 0.00504765) * pow((width1() + width2()) / 2 , 0.0642444) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00805342 * exp(-0.015696 * distance())) / pow(distance() , 0.00504765) * pow((width1() + width2()) / 2 , 0.0642444)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal1,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff poly INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.102272 * exp(-14.9123 * distance()) + 0.0836627 * exp(-1.90023 * distance())) * pow((width1() + width2()) / 2 , 0.0857978) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.102272 * exp(-14.9123 * distance()) + 0.0836627 * exp(-1.90023 * distance())) * pow((width1() + width2()) / 2 , 0.0857978)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tndiff INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.102272 * exp(-14.9123 * distance()) + 0.0836627 * exp(-1.90023 * distance())) * pow((width1() + width2()) / 2 , 0.0857978) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.102272 * exp(-14.9123 * distance()) + 0.0836627 * exp(-1.90023 * distance())) * pow((width1() + width2()) / 2 , 0.0857978)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff poly INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.102272 * exp(-14.9123 * distance()) + 0.0836627 * exp(-1.90023 * distance())) * pow((width1() + width2()) / 2 , 0.0857978) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.102272 * exp(-14.9123 * distance()) + 0.0836627 * exp(-1.90023 * distance())) * pow((width1() + width2()) / 2 , 0.0857978)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tpdiff INSIDE OF metal1 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.102272 * exp(-14.9123 * distance()) + 0.0836627 * exp(-1.90023 * distance())) * pow((width1() + width2()) / 2 , 0.0857978) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.102272 * exp(-14.9123 * distance()) + 0.0836627 * exp(-1.90023 * distance())) * pow((width1() + width2()) / 2 , 0.0857978)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal2,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff poly INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0824882 * exp(-1.18701 * distance()) + 0.116998 * exp(-13.0811 * distance())) * pow((width1() + width2()) / 2 , 0.0946513) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0824882 * exp(-1.18701 * distance()) + 0.116998 * exp(-13.0811 * distance())) * pow((width1() + width2()) / 2 , 0.0946513)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tndiff INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0824882 * exp(-1.18701 * distance()) + 0.116998 * exp(-13.0811 * distance())) * pow((width1() + width2()) / 2 , 0.0946513) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0824882 * exp(-1.18701 * distance()) + 0.116998 * exp(-13.0811 * distance())) * pow((width1() + width2()) / 2 , 0.0946513)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff poly INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0824882 * exp(-1.18701 * distance()) + 0.116998 * exp(-13.0811 * distance())) * pow((width1() + width2()) / 2 , 0.0946513) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0824882 * exp(-1.18701 * distance()) + 0.116998 * exp(-13.0811 * distance())) * pow((width1() + width2()) / 2 , 0.0946513)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tpdiff INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0824882 * exp(-1.18701 * distance()) + 0.116998 * exp(-13.0811 * distance())) * pow((width1() + width2()) / 2 , 0.0946513) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0824882 * exp(-1.18701 * distance()) + 0.116998 * exp(-13.0811 * distance())) * pow((width1() + width2()) / 2 , 0.0946513)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal3,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0787983 * exp(-0.879401 * distance()) + 0.126164 * exp(-11.9949 * distance())) * pow((width1() + width2()) / 2 , 0.0999029) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0787983 * exp(-0.879401 * distance()) + 0.126164 * exp(-11.9949 * distance())) * pow((width1() + width2()) / 2 , 0.0999029)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tndiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0787983 * exp(-0.879401 * distance()) + 0.126164 * exp(-11.9949 * distance())) * pow((width1() + width2()) / 2 , 0.0999029) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0787983 * exp(-0.879401 * distance()) + 0.126164 * exp(-11.9949 * distance())) * pow((width1() + width2()) / 2 , 0.0999029)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff poly INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0787983 * exp(-0.879401 * distance()) + 0.126164 * exp(-11.9949 * distance())) * pow((width1() + width2()) / 2 , 0.0999029) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0787983 * exp(-0.879401 * distance()) + 0.126164 * exp(-11.9949 * distance())) * pow((width1() + width2()) / 2 , 0.0999029)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tpdiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0787983 * exp(-0.879401 * distance()) + 0.126164 * exp(-11.9949 * distance())) * pow((width1() + width2()) / 2 , 0.0999029) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0787983 * exp(-0.879401 * distance()) + 0.126164 * exp(-11.9949 * distance())) * pow((width1() + width2()) / 2 , 0.0999029)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0764207 * exp(-0.728031 * distance()) + 0.131282 * exp(-11.4196 * distance())) * pow((width1() + width2()) / 2 , 0.102582) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0764207 * exp(-0.728031 * distance()) + 0.131282 * exp(-11.4196 * distance())) * pow((width1() + width2()) / 2 , 0.102582)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tndiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0764207 * exp(-0.728031 * distance()) + 0.131282 * exp(-11.4196 * distance())) * pow((width1() + width2()) / 2 , 0.102582) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0764207 * exp(-0.728031 * distance()) + 0.131282 * exp(-11.4196 * distance())) * pow((width1() + width2()) / 2 , 0.102582)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff poly INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0764207 * exp(-0.728031 * distance()) + 0.131282 * exp(-11.4196 * distance())) * pow((width1() + width2()) / 2 , 0.102582) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0764207 * exp(-0.728031 * distance()) + 0.131282 * exp(-11.4196 * distance())) * pow((width1() + width2()) / 2 , 0.102582)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tpdiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0764207 * exp(-0.728031 * distance()) + 0.131282 * exp(-11.4196 * distance())) * pow((width1() + width2()) / 2 , 0.102582) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0764207 * exp(-0.728031 * distance()) + 0.131282 * exp(-11.4196 * distance())) * pow((width1() + width2()) / 2 , 0.102582)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.134422 * exp(-11.0594 * distance()) + 0.0749639 * exp(-0.640247 * distance())) * pow((width1() + width2()) / 2 , 0.104191) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.134422 * exp(-11.0594 * distance()) + 0.0749639 * exp(-0.640247 * distance())) * pow((width1() + width2()) / 2 , 0.104191)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.134422 * exp(-11.0594 * distance()) + 0.0749639 * exp(-0.640247 * distance())) * pow((width1() + width2()) / 2 , 0.104191) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.134422 * exp(-11.0594 * distance()) + 0.0749639 * exp(-0.640247 * distance())) * pow((width1() + width2()) / 2 , 0.104191)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff poly INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.134422 * exp(-11.0594 * distance()) + 0.0749639 * exp(-0.640247 * distance())) * pow((width1() + width2()) / 2 , 0.104191) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.134422 * exp(-11.0594 * distance()) + 0.0749639 * exp(-0.640247 * distance())) * pow((width1() + width2()) / 2 , 0.104191)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.134422 * exp(-11.0594 * distance()) + 0.0749639 * exp(-0.640247 * distance())) * pow((width1() + width2()) / 2 , 0.104191) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.134422 * exp(-11.0594 * distance()) + 0.0749639 * exp(-0.640247 * distance())) * pow((width1() + width2()) / 2 , 0.104191)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0740204 * exp(-0.582186 * distance()) + 0.136566 * exp(-10.8205 * distance())) * pow((width1() + width2()) / 2 , 0.105251) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0740204 * exp(-0.582186 * distance()) + 0.136566 * exp(-10.8205 * distance())) * pow((width1() + width2()) / 2 , 0.105251)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0740204 * exp(-0.582186 * distance()) + 0.136566 * exp(-10.8205 * distance())) * pow((width1() + width2()) / 2 , 0.105251) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0740204 * exp(-0.582186 * distance()) + 0.136566 * exp(-10.8205 * distance())) * pow((width1() + width2()) / 2 , 0.105251)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff poly INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0740204 * exp(-0.582186 * distance()) + 0.136566 * exp(-10.8205 * distance())) * pow((width1() + width2()) / 2 , 0.105251) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0740204 * exp(-0.582186 * distance()) + 0.136566 * exp(-10.8205 * distance())) * pow((width1() + width2()) / 2 , 0.105251)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0740204 * exp(-0.582186 * distance()) + 0.136566 * exp(-10.8205 * distance())) * pow((width1() + width2()) / 2 , 0.105251) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0740204 * exp(-0.582186 * distance()) + 0.136566 * exp(-10.8205 * distance())) * pow((width1() + width2()) / 2 , 0.105251)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0735648 * exp(-0.544919 * distance()) + 0.137877 * exp(-10.6489 * distance())) * pow((width1() + width2()) / 2 , 0.105869) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0735648 * exp(-0.544919 * distance()) + 0.137877 * exp(-10.6489 * distance())) * pow((width1() + width2()) / 2 , 0.105869)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tndiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0735648 * exp(-0.544919 * distance()) + 0.137877 * exp(-10.6489 * distance())) * pow((width1() + width2()) / 2 , 0.105869) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0735648 * exp(-0.544919 * distance()) + 0.137877 * exp(-10.6489 * distance())) * pow((width1() + width2()) / 2 , 0.105869)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff poly INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0735648 * exp(-0.544919 * distance()) + 0.137877 * exp(-10.6489 * distance())) * pow((width1() + width2()) / 2 , 0.105869) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0735648 * exp(-0.544919 * distance()) + 0.137877 * exp(-10.6489 * distance())) * pow((width1() + width2()) / 2 , 0.105869)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tpdiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0735648 * exp(-0.544919 * distance()) + 0.137877 * exp(-10.6489 * distance())) * pow((width1() + width2()) / 2 , 0.105869) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0735648 * exp(-0.544919 * distance()) + 0.137877 * exp(-10.6489 * distance())) * pow((width1() + width2()) / 2 , 0.105869)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0728837 * exp(-0.504629 * distance()) + 0.139495 * exp(-10.4843 * distance())) * pow((width1() + width2()) / 2 , 0.106639) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0728837 * exp(-0.504629 * distance()) + 0.139495 * exp(-10.4843 * distance())) * pow((width1() + width2()) / 2 , 0.106639)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tndiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0728837 * exp(-0.504629 * distance()) + 0.139495 * exp(-10.4843 * distance())) * pow((width1() + width2()) / 2 , 0.106639) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0728837 * exp(-0.504629 * distance()) + 0.139495 * exp(-10.4843 * distance())) * pow((width1() + width2()) / 2 , 0.106639)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff poly INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0728837 * exp(-0.504629 * distance()) + 0.139495 * exp(-10.4843 * distance())) * pow((width1() + width2()) / 2 , 0.106639) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0728837 * exp(-0.504629 * distance()) + 0.139495 * exp(-10.4843 * distance())) * pow((width1() + width2()) / 2 , 0.106639)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly tpdiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_poly) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0728837 * exp(-0.504629 * distance()) + 0.139495 * exp(-10.4843 * distance())) * pow((width1() + width2()) / 2 , 0.106639) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0728837 * exp(-0.504629 * distance()) + 0.139495 * exp(-10.4843 * distance())) * pow((width1() + width2()) / 2 , 0.106639)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal2,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal1 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0389872 * exp(-0.91456 * distance()) + 0.0227316 * exp(-0.749564 * distance())) * pow((width1() + width2()) / 2 , 0.245232) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0389872 * exp(-0.91456 * distance()) + 0.0227316 * exp(-0.749564 * distance())) * pow((width1() + width2()) / 2 , 0.245232)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tndiff INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0389872 * exp(-0.91456 * distance()) + 0.0227316 * exp(-0.749564 * distance())) * pow((width1() + width2()) / 2 , 0.245232) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0389872 * exp(-0.91456 * distance()) + 0.0227316 * exp(-0.749564 * distance())) * pow((width1() + width2()) / 2 , 0.245232)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal1 INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0389872 * exp(-0.91456 * distance()) + 0.0227316 * exp(-0.749564 * distance())) * pow((width1() + width2()) / 2 , 0.245232) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0389872 * exp(-0.91456 * distance()) + 0.0227316 * exp(-0.749564 * distance())) * pow((width1() + width2()) / 2 , 0.245232)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tpdiff INSIDE OF metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0389872 * exp(-0.91456 * distance()) + 0.0227316 * exp(-0.749564 * distance())) * pow((width1() + width2()) / 2 , 0.245232) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0389872 * exp(-0.91456 * distance()) + 0.0227316 * exp(-0.749564 * distance())) * pow((width1() + width2()) / 2 , 0.245232)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal3,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal1 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0647738 * exp(-0.748289 * distance()) + 0.00376276 * exp(-0.0423793 * distance())) * pow((width1() + width2()) / 2 , 0.223725) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0647738 * exp(-0.748289 * distance()) + 0.00376276 * exp(-0.0423793 * distance())) * pow((width1() + width2()) / 2 , 0.223725)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tndiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0647738 * exp(-0.748289 * distance()) + 0.00376276 * exp(-0.0423793 * distance())) * pow((width1() + width2()) / 2 , 0.223725) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0647738 * exp(-0.748289 * distance()) + 0.00376276 * exp(-0.0423793 * distance())) * pow((width1() + width2()) / 2 , 0.223725)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal1 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0647738 * exp(-0.748289 * distance()) + 0.00376276 * exp(-0.0423793 * distance())) * pow((width1() + width2()) / 2 , 0.223725) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0647738 * exp(-0.748289 * distance()) + 0.00376276 * exp(-0.0423793 * distance())) * pow((width1() + width2()) / 2 , 0.223725)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tpdiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0647738 * exp(-0.748289 * distance()) + 0.00376276 * exp(-0.0423793 * distance())) * pow((width1() + width2()) / 2 , 0.223725) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0647738 * exp(-0.748289 * distance()) + 0.00376276 * exp(-0.0423793 * distance())) * pow((width1() + width2()) / 2 , 0.223725)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0120067 * exp(-0.13347 * distance()) + 0.0597735 * exp(-0.770018 * distance())) * pow((width1() + width2()) / 2 , 0.217487) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0120067 * exp(-0.13347 * distance()) + 0.0597735 * exp(-0.770018 * distance())) * pow((width1() + width2()) / 2 , 0.217487)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tndiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0120067 * exp(-0.13347 * distance()) + 0.0597735 * exp(-0.770018 * distance())) * pow((width1() + width2()) / 2 , 0.217487) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0120067 * exp(-0.13347 * distance()) + 0.0597735 * exp(-0.770018 * distance())) * pow((width1() + width2()) / 2 , 0.217487)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal1 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0120067 * exp(-0.13347 * distance()) + 0.0597735 * exp(-0.770018 * distance())) * pow((width1() + width2()) / 2 , 0.217487) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0120067 * exp(-0.13347 * distance()) + 0.0597735 * exp(-0.770018 * distance())) * pow((width1() + width2()) / 2 , 0.217487)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tpdiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0120067 * exp(-0.13347 * distance()) + 0.0597735 * exp(-0.770018 * distance())) * pow((width1() + width2()) / 2 , 0.217487) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0120067 * exp(-0.13347 * distance()) + 0.0597735 * exp(-0.770018 * distance())) * pow((width1() + width2()) / 2 , 0.217487)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0164502 * exp(-0.12608 * distance()) + 0.0572969 * exp(-0.787095 * distance())) * pow((width1() + width2()) / 2 , 0.214127) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0164502 * exp(-0.12608 * distance()) + 0.0572969 * exp(-0.787095 * distance())) * pow((width1() + width2()) / 2 , 0.214127)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0164502 * exp(-0.12608 * distance()) + 0.0572969 * exp(-0.787095 * distance())) * pow((width1() + width2()) / 2 , 0.214127) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0164502 * exp(-0.12608 * distance()) + 0.0572969 * exp(-0.787095 * distance())) * pow((width1() + width2()) / 2 , 0.214127)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal1 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0164502 * exp(-0.12608 * distance()) + 0.0572969 * exp(-0.787095 * distance())) * pow((width1() + width2()) / 2 , 0.214127) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0164502 * exp(-0.12608 * distance()) + 0.0572969 * exp(-0.787095 * distance())) * pow((width1() + width2()) / 2 , 0.214127)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0164502 * exp(-0.12608 * distance()) + 0.0572969 * exp(-0.787095 * distance())) * pow((width1() + width2()) / 2 , 0.214127) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0164502 * exp(-0.12608 * distance()) + 0.0572969 * exp(-0.787095 * distance())) * pow((width1() + width2()) / 2 , 0.214127)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0187841 * exp(-0.109797 * distance()) + 0.056323 * exp(-0.796009 * distance())) * pow((width1() + width2()) / 2 , 0.211663) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0187841 * exp(-0.109797 * distance()) + 0.056323 * exp(-0.796009 * distance())) * pow((width1() + width2()) / 2 , 0.211663)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0187841 * exp(-0.109797 * distance()) + 0.056323 * exp(-0.796009 * distance())) * pow((width1() + width2()) / 2 , 0.211663) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0187841 * exp(-0.109797 * distance()) + 0.056323 * exp(-0.796009 * distance())) * pow((width1() + width2()) / 2 , 0.211663)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal1 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0187841 * exp(-0.109797 * distance()) + 0.056323 * exp(-0.796009 * distance())) * pow((width1() + width2()) / 2 , 0.211663) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0187841 * exp(-0.109797 * distance()) + 0.056323 * exp(-0.796009 * distance())) * pow((width1() + width2()) / 2 , 0.211663)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0187841 * exp(-0.109797 * distance()) + 0.056323 * exp(-0.796009 * distance())) * pow((width1() + width2()) / 2 , 0.211663) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0187841 * exp(-0.109797 * distance()) + 0.056323 * exp(-0.796009 * distance())) * pow((width1() + width2()) / 2 , 0.211663)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal1 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0201527 * exp(-0.0947035 * distance()) + 0.0559827 * exp(-0.799647 * distance())) * pow((width1() + width2()) / 2 , 0.209636) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0201527 * exp(-0.0947035 * distance()) + 0.0559827 * exp(-0.799647 * distance())) * pow((width1() + width2()) / 2 , 0.209636)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tndiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0201527 * exp(-0.0947035 * distance()) + 0.0559827 * exp(-0.799647 * distance())) * pow((width1() + width2()) / 2 , 0.209636) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0201527 * exp(-0.0947035 * distance()) + 0.0559827 * exp(-0.799647 * distance())) * pow((width1() + width2()) / 2 , 0.209636)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal1 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0201527 * exp(-0.0947035 * distance()) + 0.0559827 * exp(-0.799647 * distance())) * pow((width1() + width2()) / 2 , 0.209636) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0201527 * exp(-0.0947035 * distance()) + 0.0559827 * exp(-0.799647 * distance())) * pow((width1() + width2()) / 2 , 0.209636)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tpdiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0201527 * exp(-0.0947035 * distance()) + 0.0559827 * exp(-0.799647 * distance())) * pow((width1() + width2()) / 2 , 0.209636) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0201527 * exp(-0.0947035 * distance()) + 0.0559827 * exp(-0.799647 * distance())) * pow((width1() + width2()) / 2 , 0.209636)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0558654 * exp(-0.801168 * distance()) + 0.0212432 * exp(-0.0802033 * distance())) * pow((width1() + width2()) / 2 , 0.20764) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0558654 * exp(-0.801168 * distance()) + 0.0212432 * exp(-0.0802033 * distance())) * pow((width1() + width2()) / 2 , 0.20764)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tndiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0558654 * exp(-0.801168 * distance()) + 0.0212432 * exp(-0.0802033 * distance())) * pow((width1() + width2()) / 2 , 0.20764) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0558654 * exp(-0.801168 * distance()) + 0.0212432 * exp(-0.0802033 * distance())) * pow((width1() + width2()) / 2 , 0.20764)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal1 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0558654 * exp(-0.801168 * distance()) + 0.0212432 * exp(-0.0802033 * distance())) * pow((width1() + width2()) / 2 , 0.20764) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0558654 * exp(-0.801168 * distance()) + 0.0212432 * exp(-0.0802033 * distance())) * pow((width1() + width2()) / 2 , 0.20764)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 tpdiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0558654 * exp(-0.801168 * distance()) + 0.0212432 * exp(-0.0802033 * distance())) * pow((width1() + width2()) / 2 , 0.20764) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0558654 * exp(-0.801168 * distance()) + 0.0212432 * exp(-0.0802033 * distance())) * pow((width1() + width2()) / 2 , 0.20764)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal3,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal2 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0173947 * exp(-0.441739 * distance()) + 0.0173946 * exp(-0.441736 * distance())) * pow((width1() + width2()) / 2 , 0.28766) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0173947 * exp(-0.441739 * distance()) + 0.0173946 * exp(-0.441736 * distance())) * pow((width1() + width2()) / 2 , 0.28766)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tndiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0173947 * exp(-0.441739 * distance()) + 0.0173946 * exp(-0.441736 * distance())) * pow((width1() + width2()) / 2 , 0.28766) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0173947 * exp(-0.441739 * distance()) + 0.0173946 * exp(-0.441736 * distance())) * pow((width1() + width2()) / 2 , 0.28766)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal2 INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0173947 * exp(-0.441739 * distance()) + 0.0173946 * exp(-0.441736 * distance())) * pow((width1() + width2()) / 2 , 0.28766) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0173947 * exp(-0.441739 * distance()) + 0.0173946 * exp(-0.441736 * distance())) * pow((width1() + width2()) / 2 , 0.28766)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tpdiff INSIDE OF metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0173947 * exp(-0.441739 * distance()) + 0.0173946 * exp(-0.441736 * distance())) * pow((width1() + width2()) / 2 , 0.28766) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0173947 * exp(-0.441739 * distance()) + 0.0173946 * exp(-0.441736 * distance())) * pow((width1() + width2()) / 2 , 0.28766)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0200801 * exp(-0.354213 * distance()) + 0.0200802 * exp(-0.355697 * distance())) * pow((width1() + width2()) / 2 , 0.248994) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0200801 * exp(-0.354213 * distance()) + 0.0200802 * exp(-0.355697 * distance())) * pow((width1() + width2()) / 2 , 0.248994)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tndiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0200801 * exp(-0.354213 * distance()) + 0.0200802 * exp(-0.355697 * distance())) * pow((width1() + width2()) / 2 , 0.248994) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0200801 * exp(-0.354213 * distance()) + 0.0200802 * exp(-0.355697 * distance())) * pow((width1() + width2()) / 2 , 0.248994)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal2 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0200801 * exp(-0.354213 * distance()) + 0.0200802 * exp(-0.355697 * distance())) * pow((width1() + width2()) / 2 , 0.248994) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0200801 * exp(-0.354213 * distance()) + 0.0200802 * exp(-0.355697 * distance())) * pow((width1() + width2()) / 2 , 0.248994)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tpdiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0200801 * exp(-0.354213 * distance()) + 0.0200802 * exp(-0.355697 * distance())) * pow((width1() + width2()) / 2 , 0.248994) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0200801 * exp(-0.354213 * distance()) + 0.0200802 * exp(-0.355697 * distance())) * pow((width1() + width2()) / 2 , 0.248994)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0214852 * exp(-0.307571 * distance()) + 0.0214852 * exp(-0.307525 * distance())) * pow((width1() + width2()) / 2 , 0.23496) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0214852 * exp(-0.307571 * distance()) + 0.0214852 * exp(-0.307525 * distance())) * pow((width1() + width2()) / 2 , 0.23496)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0214852 * exp(-0.307571 * distance()) + 0.0214852 * exp(-0.307525 * distance())) * pow((width1() + width2()) / 2 , 0.23496) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0214852 * exp(-0.307571 * distance()) + 0.0214852 * exp(-0.307525 * distance())) * pow((width1() + width2()) / 2 , 0.23496)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal2 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0214852 * exp(-0.307571 * distance()) + 0.0214852 * exp(-0.307525 * distance())) * pow((width1() + width2()) / 2 , 0.23496) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0214852 * exp(-0.307571 * distance()) + 0.0214852 * exp(-0.307525 * distance())) * pow((width1() + width2()) / 2 , 0.23496)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0214852 * exp(-0.307571 * distance()) + 0.0214852 * exp(-0.307525 * distance())) * pow((width1() + width2()) / 2 , 0.23496) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0214852 * exp(-0.307571 * distance()) + 0.0214852 * exp(-0.307525 * distance())) * pow((width1() + width2()) / 2 , 0.23496)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0223838 * exp(-0.277612 * distance()) + 0.0223839 * exp(-0.277611 * distance())) * pow((width1() + width2()) / 2 , 0.226905) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0223838 * exp(-0.277612 * distance()) + 0.0223839 * exp(-0.277611 * distance())) * pow((width1() + width2()) / 2 , 0.226905)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0223838 * exp(-0.277612 * distance()) + 0.0223839 * exp(-0.277611 * distance())) * pow((width1() + width2()) / 2 , 0.226905) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0223838 * exp(-0.277612 * distance()) + 0.0223839 * exp(-0.277611 * distance())) * pow((width1() + width2()) / 2 , 0.226905)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal2 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0223838 * exp(-0.277612 * distance()) + 0.0223839 * exp(-0.277611 * distance())) * pow((width1() + width2()) / 2 , 0.226905) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0223838 * exp(-0.277612 * distance()) + 0.0223839 * exp(-0.277611 * distance())) * pow((width1() + width2()) / 2 , 0.226905)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0223838 * exp(-0.277612 * distance()) + 0.0223839 * exp(-0.277611 * distance())) * pow((width1() + width2()) / 2 , 0.226905) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0223838 * exp(-0.277612 * distance()) + 0.0223839 * exp(-0.277611 * distance())) * pow((width1() + width2()) / 2 , 0.226905)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal2 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0230345 * exp(-0.256798 * distance()) + 0.0230345 * exp(-0.257003 * distance())) * pow((width1() + width2()) / 2 , 0.221257) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0230345 * exp(-0.256798 * distance()) + 0.0230345 * exp(-0.257003 * distance())) * pow((width1() + width2()) / 2 , 0.221257)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tndiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0230345 * exp(-0.256798 * distance()) + 0.0230345 * exp(-0.257003 * distance())) * pow((width1() + width2()) / 2 , 0.221257) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0230345 * exp(-0.256798 * distance()) + 0.0230345 * exp(-0.257003 * distance())) * pow((width1() + width2()) / 2 , 0.221257)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal2 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0230345 * exp(-0.256798 * distance()) + 0.0230345 * exp(-0.257003 * distance())) * pow((width1() + width2()) / 2 , 0.221257) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0230345 * exp(-0.256798 * distance()) + 0.0230345 * exp(-0.257003 * distance())) * pow((width1() + width2()) / 2 , 0.221257)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tpdiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0230345 * exp(-0.256798 * distance()) + 0.0230345 * exp(-0.257003 * distance())) * pow((width1() + width2()) / 2 , 0.221257) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0230345 * exp(-0.256798 * distance()) + 0.0230345 * exp(-0.257003 * distance())) * pow((width1() + width2()) / 2 , 0.221257)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0236294 * exp(-0.238808 * distance()) + 0.0236294 * exp(-0.239408 * distance())) * pow((width1() + width2()) / 2 , 0.216155) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0236294 * exp(-0.238808 * distance()) + 0.0236294 * exp(-0.239408 * distance())) * pow((width1() + width2()) / 2 , 0.216155)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tndiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0236294 * exp(-0.238808 * distance()) + 0.0236294 * exp(-0.239408 * distance())) * pow((width1() + width2()) / 2 , 0.216155) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0236294 * exp(-0.238808 * distance()) + 0.0236294 * exp(-0.239408 * distance())) * pow((width1() + width2()) / 2 , 0.216155)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal2 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0236294 * exp(-0.238808 * distance()) + 0.0236294 * exp(-0.239408 * distance())) * pow((width1() + width2()) / 2 , 0.216155) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0236294 * exp(-0.238808 * distance()) + 0.0236294 * exp(-0.239408 * distance())) * pow((width1() + width2()) / 2 , 0.216155)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 tpdiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0236294 * exp(-0.238808 * distance()) + 0.0236294 * exp(-0.239408 * distance())) * pow((width1() + width2()) / 2 , 0.216155) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0236294 * exp(-0.238808 * distance()) + 0.0236294 * exp(-0.239408 * distance())) * pow((width1() + width2()) / 2 , 0.216155)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal3 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.01119 * exp(-0.286199 * distance()) + 0.01119 * exp(-0.286136 * distance())) * pow((width1() + width2()) / 2 , 0.300535) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.01119 * exp(-0.286199 * distance()) + 0.01119 * exp(-0.286136 * distance())) * pow((width1() + width2()) / 2 , 0.300535)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 tndiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.01119 * exp(-0.286199 * distance()) + 0.01119 * exp(-0.286136 * distance())) * pow((width1() + width2()) / 2 , 0.300535) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.01119 * exp(-0.286199 * distance()) + 0.01119 * exp(-0.286136 * distance())) * pow((width1() + width2()) / 2 , 0.300535)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal3 INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.01119 * exp(-0.286199 * distance()) + 0.01119 * exp(-0.286136 * distance())) * pow((width1() + width2()) / 2 , 0.300535) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.01119 * exp(-0.286199 * distance()) + 0.01119 * exp(-0.286136 * distance())) * pow((width1() + width2()) / 2 , 0.300535)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 tpdiff INSIDE OF metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.01119 * exp(-0.286199 * distance()) + 0.01119 * exp(-0.286136 * distance())) * pow((width1() + width2()) / 2 , 0.300535) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.01119 * exp(-0.286199 * distance()) + 0.01119 * exp(-0.286136 * distance())) * pow((width1() + width2()) / 2 , 0.300535)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0135471 * exp(-0.233117 * distance()) + 0.0135471 * exp(-0.232179 * distance())) * pow((width1() + width2()) / 2 , 0.252618) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0135471 * exp(-0.233117 * distance()) + 0.0135471 * exp(-0.232179 * distance())) * pow((width1() + width2()) / 2 , 0.252618)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0135471 * exp(-0.233117 * distance()) + 0.0135471 * exp(-0.232179 * distance())) * pow((width1() + width2()) / 2 , 0.252618) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0135471 * exp(-0.233117 * distance()) + 0.0135471 * exp(-0.232179 * distance())) * pow((width1() + width2()) / 2 , 0.252618)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal3 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0135471 * exp(-0.233117 * distance()) + 0.0135471 * exp(-0.232179 * distance())) * pow((width1() + width2()) / 2 , 0.252618) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0135471 * exp(-0.233117 * distance()) + 0.0135471 * exp(-0.232179 * distance())) * pow((width1() + width2()) / 2 , 0.252618)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0135471 * exp(-0.233117 * distance()) + 0.0135471 * exp(-0.232179 * distance())) * pow((width1() + width2()) / 2 , 0.252618) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0135471 * exp(-0.233117 * distance()) + 0.0135471 * exp(-0.232179 * distance())) * pow((width1() + width2()) / 2 , 0.252618)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0149253 * exp(-0.201004 * distance()) + 0.0149253 * exp(-0.201849 * distance())) * pow((width1() + width2()) / 2 , 0.234354) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0149253 * exp(-0.201004 * distance()) + 0.0149253 * exp(-0.201849 * distance())) * pow((width1() + width2()) / 2 , 0.234354)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0149253 * exp(-0.201004 * distance()) + 0.0149253 * exp(-0.201849 * distance())) * pow((width1() + width2()) / 2 , 0.234354) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0149253 * exp(-0.201004 * distance()) + 0.0149253 * exp(-0.201849 * distance())) * pow((width1() + width2()) / 2 , 0.234354)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal3 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0149253 * exp(-0.201004 * distance()) + 0.0149253 * exp(-0.201849 * distance())) * pow((width1() + width2()) / 2 , 0.234354) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0149253 * exp(-0.201004 * distance()) + 0.0149253 * exp(-0.201849 * distance())) * pow((width1() + width2()) / 2 , 0.234354)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0149253 * exp(-0.201004 * distance()) + 0.0149253 * exp(-0.201849 * distance())) * pow((width1() + width2()) / 2 , 0.234354) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0149253 * exp(-0.201004 * distance()) + 0.0149253 * exp(-0.201849 * distance())) * pow((width1() + width2()) / 2 , 0.234354)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal3 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0158721 * exp(-0.181045 * distance()) + 0.0158721 * exp(-0.181045 * distance())) * pow((width1() + width2()) / 2 , 0.223799) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0158721 * exp(-0.181045 * distance()) + 0.0158721 * exp(-0.181045 * distance())) * pow((width1() + width2()) / 2 , 0.223799)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 tndiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0158721 * exp(-0.181045 * distance()) + 0.0158721 * exp(-0.181045 * distance())) * pow((width1() + width2()) / 2 , 0.223799) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0158721 * exp(-0.181045 * distance()) + 0.0158721 * exp(-0.181045 * distance())) * pow((width1() + width2()) / 2 , 0.223799)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal3 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0158721 * exp(-0.181045 * distance()) + 0.0158721 * exp(-0.181045 * distance())) * pow((width1() + width2()) / 2 , 0.223799) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0158721 * exp(-0.181045 * distance()) + 0.0158721 * exp(-0.181045 * distance())) * pow((width1() + width2()) / 2 , 0.223799)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 tpdiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0158721 * exp(-0.181045 * distance()) + 0.0158721 * exp(-0.181045 * distance())) * pow((width1() + width2()) / 2 , 0.223799) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0158721 * exp(-0.181045 * distance()) + 0.0158721 * exp(-0.181045 * distance())) * pow((width1() + width2()) / 2 , 0.223799)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal3 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0167051 * exp(-0.164332 * distance()) + 0.0167051 * exp(-0.164332 * distance())) * pow((width1() + width2()) / 2 , 0.215253) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0167051 * exp(-0.164332 * distance()) + 0.0167051 * exp(-0.164332 * distance())) * pow((width1() + width2()) / 2 , 0.215253)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 tndiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0167051 * exp(-0.164332 * distance()) + 0.0167051 * exp(-0.164332 * distance())) * pow((width1() + width2()) / 2 , 0.215253) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0167051 * exp(-0.164332 * distance()) + 0.0167051 * exp(-0.164332 * distance())) * pow((width1() + width2()) / 2 , 0.215253)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal3 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0167051 * exp(-0.164332 * distance()) + 0.0167051 * exp(-0.164332 * distance())) * pow((width1() + width2()) / 2 , 0.215253) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0167051 * exp(-0.164332 * distance()) + 0.0167051 * exp(-0.164332 * distance())) * pow((width1() + width2()) / 2 , 0.215253)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 tpdiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0167051 * exp(-0.164332 * distance()) + 0.0167051 * exp(-0.164332 * distance())) * pow((width1() + width2()) / 2 , 0.215253) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0167051 * exp(-0.164332 * distance()) + 0.0167051 * exp(-0.164332 * distance())) * pow((width1() + width2()) / 2 , 0.215253)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00810425 * exp(-0.200519 * distance()) + 0.00810418 * exp(-0.200671 * distance())) * pow((width1() + width2()) / 2 , 0.303399) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00810425 * exp(-0.200519 * distance()) + 0.00810418 * exp(-0.200671 * distance())) * pow((width1() + width2()) / 2 , 0.303399)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 tndiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00810425 * exp(-0.200519 * distance()) + 0.00810418 * exp(-0.200671 * distance())) * pow((width1() + width2()) / 2 , 0.303399) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00810425 * exp(-0.200519 * distance()) + 0.00810418 * exp(-0.200671 * distance())) * pow((width1() + width2()) / 2 , 0.303399)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal4 INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00810425 * exp(-0.200519 * distance()) + 0.00810418 * exp(-0.200671 * distance())) * pow((width1() + width2()) / 2 , 0.303399) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00810425 * exp(-0.200519 * distance()) + 0.00810418 * exp(-0.200671 * distance())) * pow((width1() + width2()) / 2 , 0.303399)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 tpdiff INSIDE OF metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00810425 * exp(-0.200519 * distance()) + 0.00810418 * exp(-0.200671 * distance())) * pow((width1() + width2()) / 2 , 0.303399) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00810425 * exp(-0.200519 * distance()) + 0.00810418 * exp(-0.200671 * distance())) * pow((width1() + width2()) / 2 , 0.303399)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0101789 * exp(-0.16426 * distance()) + 0.010179 * exp(-0.164804 * distance())) * pow((width1() + width2()) / 2 , 0.251291) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0101789 * exp(-0.16426 * distance()) + 0.010179 * exp(-0.164804 * distance())) * pow((width1() + width2()) / 2 , 0.251291)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0101789 * exp(-0.16426 * distance()) + 0.010179 * exp(-0.164804 * distance())) * pow((width1() + width2()) / 2 , 0.251291) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0101789 * exp(-0.16426 * distance()) + 0.010179 * exp(-0.164804 * distance())) * pow((width1() + width2()) / 2 , 0.251291)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal4 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0101789 * exp(-0.16426 * distance()) + 0.010179 * exp(-0.164804 * distance())) * pow((width1() + width2()) / 2 , 0.251291) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0101789 * exp(-0.16426 * distance()) + 0.010179 * exp(-0.164804 * distance())) * pow((width1() + width2()) / 2 , 0.251291)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0101789 * exp(-0.16426 * distance()) + 0.010179 * exp(-0.164804 * distance())) * pow((width1() + width2()) / 2 , 0.251291) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0101789 * exp(-0.16426 * distance()) + 0.010179 * exp(-0.164804 * distance())) * pow((width1() + width2()) / 2 , 0.251291)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal4 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114876 * exp(-0.142656 * distance()) + 0.011487 * exp(-0.142551 * distance())) * pow((width1() + width2()) / 2 , 0.23061) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114876 * exp(-0.142656 * distance()) + 0.011487 * exp(-0.142551 * distance())) * pow((width1() + width2()) / 2 , 0.23061)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 tndiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114876 * exp(-0.142656 * distance()) + 0.011487 * exp(-0.142551 * distance())) * pow((width1() + width2()) / 2 , 0.23061) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114876 * exp(-0.142656 * distance()) + 0.011487 * exp(-0.142551 * distance())) * pow((width1() + width2()) / 2 , 0.23061)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal4 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114876 * exp(-0.142656 * distance()) + 0.011487 * exp(-0.142551 * distance())) * pow((width1() + width2()) / 2 , 0.23061) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114876 * exp(-0.142656 * distance()) + 0.011487 * exp(-0.142551 * distance())) * pow((width1() + width2()) / 2 , 0.23061)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 tpdiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114876 * exp(-0.142656 * distance()) + 0.011487 * exp(-0.142551 * distance())) * pow((width1() + width2()) / 2 , 0.23061) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114876 * exp(-0.142656 * distance()) + 0.011487 * exp(-0.142551 * distance())) * pow((width1() + width2()) / 2 , 0.23061)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal4 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0125911 * exp(-0.125619 * distance()) + 0.0125911 * exp(-0.12562 * distance())) * pow((width1() + width2()) / 2 , 0.217179) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0125911 * exp(-0.125619 * distance()) + 0.0125911 * exp(-0.12562 * distance())) * pow((width1() + width2()) / 2 , 0.217179)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 tndiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0125911 * exp(-0.125619 * distance()) + 0.0125911 * exp(-0.12562 * distance())) * pow((width1() + width2()) / 2 , 0.217179) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0125911 * exp(-0.125619 * distance()) + 0.0125911 * exp(-0.12562 * distance())) * pow((width1() + width2()) / 2 , 0.217179)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal4 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0125911 * exp(-0.125619 * distance()) + 0.0125911 * exp(-0.12562 * distance())) * pow((width1() + width2()) / 2 , 0.217179) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0125911 * exp(-0.125619 * distance()) + 0.0125911 * exp(-0.12562 * distance())) * pow((width1() + width2()) / 2 , 0.217179)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 tpdiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0125911 * exp(-0.125619 * distance()) + 0.0125911 * exp(-0.12562 * distance())) * pow((width1() + width2()) / 2 , 0.217179) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0125911 * exp(-0.125619 * distance()) + 0.0125911 * exp(-0.12562 * distance())) * pow((width1() + width2()) / 2 , 0.217179)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal5,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00629454 * exp(-0.146374 * distance()) + 0.0062945 * exp(-0.146419 * distance())) * pow((width1() + width2()) / 2 , 0.303053) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00629454 * exp(-0.146374 * distance()) + 0.0062945 * exp(-0.146419 * distance())) * pow((width1() + width2()) / 2 , 0.303053)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 tndiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00629454 * exp(-0.146374 * distance()) + 0.0062945 * exp(-0.146419 * distance())) * pow((width1() + width2()) / 2 , 0.303053) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00629454 * exp(-0.146374 * distance()) + 0.0062945 * exp(-0.146419 * distance())) * pow((width1() + width2()) / 2 , 0.303053)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal5 INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00629454 * exp(-0.146374 * distance()) + 0.0062945 * exp(-0.146419 * distance())) * pow((width1() + width2()) / 2 , 0.303053) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00629454 * exp(-0.146374 * distance()) + 0.0062945 * exp(-0.146419 * distance())) * pow((width1() + width2()) / 2 , 0.303053)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 tpdiff INSIDE OF metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00629454 * exp(-0.146374 * distance()) + 0.0062945 * exp(-0.146419 * distance())) * pow((width1() + width2()) / 2 , 0.303053) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00629454 * exp(-0.146374 * distance()) + 0.0062945 * exp(-0.146419 * distance())) * pow((width1() + width2()) / 2 , 0.303053)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal5,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal5 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00814293 * exp(-0.12083 * distance()) + 0.00814288 * exp(-0.121319 * distance())) * pow((width1() + width2()) / 2 , 0.248594) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00814293 * exp(-0.12083 * distance()) + 0.00814288 * exp(-0.121319 * distance())) * pow((width1() + width2()) / 2 , 0.248594)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 tndiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00814293 * exp(-0.12083 * distance()) + 0.00814288 * exp(-0.121319 * distance())) * pow((width1() + width2()) / 2 , 0.248594) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00814293 * exp(-0.12083 * distance()) + 0.00814288 * exp(-0.121319 * distance())) * pow((width1() + width2()) / 2 , 0.248594)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal5 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00814293 * exp(-0.12083 * distance()) + 0.00814288 * exp(-0.121319 * distance())) * pow((width1() + width2()) / 2 , 0.248594) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00814293 * exp(-0.12083 * distance()) + 0.00814288 * exp(-0.121319 * distance())) * pow((width1() + width2()) / 2 , 0.248594)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 tpdiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00814293 * exp(-0.12083 * distance()) + 0.00814288 * exp(-0.121319 * distance())) * pow((width1() + width2()) / 2 , 0.248594) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00814293 * exp(-0.12083 * distance()) + 0.00814288 * exp(-0.121319 * distance())) * pow((width1() + width2()) / 2 , 0.248594)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal5,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal5 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00957623 * exp(-0.102754 * distance()) + 0.00957632 * exp(-0.102746 * distance())) * pow((width1() + width2()) / 2 , 0.224032) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00957623 * exp(-0.102754 * distance()) + 0.00957632 * exp(-0.102746 * distance())) * pow((width1() + width2()) / 2 , 0.224032)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 tndiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00957623 * exp(-0.102754 * distance()) + 0.00957632 * exp(-0.102746 * distance())) * pow((width1() + width2()) / 2 , 0.224032) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00957623 * exp(-0.102754 * distance()) + 0.00957632 * exp(-0.102746 * distance())) * pow((width1() + width2()) / 2 , 0.224032)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal5 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00957623 * exp(-0.102754 * distance()) + 0.00957632 * exp(-0.102746 * distance())) * pow((width1() + width2()) / 2 , 0.224032) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00957623 * exp(-0.102754 * distance()) + 0.00957632 * exp(-0.102746 * distance())) * pow((width1() + width2()) / 2 , 0.224032)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 tpdiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00957623 * exp(-0.102754 * distance()) + 0.00957632 * exp(-0.102746 * distance())) * pow((width1() + width2()) / 2 , 0.224032) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00957623 * exp(-0.102754 * distance()) + 0.00957632 * exp(-0.102746 * distance())) * pow((width1() + width2()) / 2 , 0.224032)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal6,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal6 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00513077 * exp(-0.110363 * distance()) + 0.00513041 * exp(-0.109275 * distance())) * pow((width1() + width2()) / 2 , 0.301246) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00513077 * exp(-0.110363 * distance()) + 0.00513041 * exp(-0.109275 * distance())) * pow((width1() + width2()) / 2 , 0.301246)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 tndiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00513077 * exp(-0.110363 * distance()) + 0.00513041 * exp(-0.109275 * distance())) * pow((width1() + width2()) / 2 , 0.301246) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00513077 * exp(-0.110363 * distance()) + 0.00513041 * exp(-0.109275 * distance())) * pow((width1() + width2()) / 2 , 0.301246)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal6 INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00513077 * exp(-0.110363 * distance()) + 0.00513041 * exp(-0.109275 * distance())) * pow((width1() + width2()) / 2 , 0.301246) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00513077 * exp(-0.110363 * distance()) + 0.00513041 * exp(-0.109275 * distance())) * pow((width1() + width2()) / 2 , 0.301246)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 tpdiff INSIDE OF metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00513077 * exp(-0.110363 * distance()) + 0.00513041 * exp(-0.109275 * distance())) * pow((width1() + width2()) / 2 , 0.301246) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00513077 * exp(-0.110363 * distance()) + 0.00513041 * exp(-0.109275 * distance())) * pow((width1() + width2()) / 2 , 0.301246)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal6,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal6 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00705299 * exp(-0.0891977 * distance()) + 0.00705295 * exp(-0.0889054 * distance())) * pow((width1() + width2()) / 2 , 0.24002) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00705299 * exp(-0.0891977 * distance()) + 0.00705295 * exp(-0.0889054 * distance())) * pow((width1() + width2()) / 2 , 0.24002)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 tndiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00705299 * exp(-0.0891977 * distance()) + 0.00705295 * exp(-0.0889054 * distance())) * pow((width1() + width2()) / 2 , 0.24002) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00705299 * exp(-0.0891977 * distance()) + 0.00705295 * exp(-0.0889054 * distance())) * pow((width1() + width2()) / 2 , 0.24002)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal6 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00705299 * exp(-0.0891977 * distance()) + 0.00705295 * exp(-0.0889054 * distance())) * pow((width1() + width2()) / 2 , 0.24002) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00705299 * exp(-0.0891977 * distance()) + 0.00705295 * exp(-0.0889054 * distance())) * pow((width1() + width2()) / 2 , 0.24002)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 tpdiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00705299 * exp(-0.0891977 * distance()) + 0.00705295 * exp(-0.0889054 * distance())) * pow((width1() + width2()) / 2 , 0.24002) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00705299 * exp(-0.0891977 * distance()) + 0.00705295 * exp(-0.0889054 * distance())) * pow((width1() + width2()) / 2 , 0.24002)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 214  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal7,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff metal7 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00470854 * exp(-0.0815914 * distance()) + 0.00470851 * exp(-0.0809402 * distance())) * pow((width1() + width2()) / 2 , 0.280732) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00470854 * exp(-0.0815914 * distance()) + 0.00470851 * exp(-0.0809402 * distance())) * pow((width1() + width2()) / 2 , 0.280732)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 tndiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00470854 * exp(-0.0815914 * distance()) + 0.00470851 * exp(-0.0809402 * distance())) * pow((width1() + width2()) / 2 , 0.280732) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00470854 * exp(-0.0815914 * distance()) + 0.00470851 * exp(-0.0809402 * distance())) * pow((width1() + width2()) / 2 , 0.280732)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff metal7 INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00470854 * exp(-0.0815914 * distance()) + 0.00470851 * exp(-0.0809402 * distance())) * pow((width1() + width2()) / 2 , 0.280732) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00470854 * exp(-0.0815914 * distance()) + 0.00470851 * exp(-0.0809402 * distance())) * pow((width1() + width2()) / 2 , 0.280732)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 tpdiff INSIDE OF metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.15 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = (MAXDISTANCE_diff2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00470854 * exp(-0.0815914 * distance()) + 0.00470851 * exp(-0.0809402 * distance())) * pow((width1() + width2()) / 2 , 0.280732) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00470854 * exp(-0.0815914 * distance()) + 0.00470851 * exp(-0.0809402 * distance())) * pow((width1() + width2()) / 2 , 0.280732)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal1,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00466603 * exp(-0.246473 * distance()) + 0.0634701 * exp(-1.7376 * distance())) * pow((width1() + width2()) / 2 , 0.311716) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00466603 * exp(-0.246473 * distance()) + 0.0634701 * exp(-1.7376 * distance())) * pow((width1() + width2()) / 2 , 0.311716)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00466603 * exp(-0.246473 * distance()) + 0.0634701 * exp(-1.7376 * distance())) * pow((width1() + width2()) / 2 , 0.311716) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00466603 * exp(-0.246473 * distance()) + 0.0634701 * exp(-1.7376 * distance())) * pow((width1() + width2()) / 2 , 0.311716)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00466603 * exp(-0.246473 * distance()) + 0.0634701 * exp(-1.7376 * distance())) * pow((width1() + width2()) / 2 , 0.311716) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00466603 * exp(-0.246473 * distance()) + 0.0634701 * exp(-1.7376 * distance())) * pow((width1() + width2()) / 2 , 0.311716)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00466603 * exp(-0.246473 * distance()) + 0.0634701 * exp(-1.7376 * distance())) * pow((width1() + width2()) / 2 , 0.311716) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00466603 * exp(-0.246473 * distance()) + 0.0634701 * exp(-1.7376 * distance())) * pow((width1() + width2()) / 2 , 0.311716)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal2,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal2 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.013892 * exp(-0.559088 * distance()) + 0.0138922 * exp(-0.566255 * distance())) * pow((width1() + width2()) / 2 , 0.428169) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.013892 * exp(-0.559088 * distance()) + 0.0138922 * exp(-0.566255 * distance())) * pow((width1() + width2()) / 2 , 0.428169)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 poly INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.013892 * exp(-0.559088 * distance()) + 0.0138922 * exp(-0.566255 * distance())) * pow((width1() + width2()) / 2 , 0.428169) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.013892 * exp(-0.559088 * distance()) + 0.0138922 * exp(-0.566255 * distance())) * pow((width1() + width2()) / 2 , 0.428169)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal2 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.013892 * exp(-0.559088 * distance()) + 0.0138922 * exp(-0.566255 * distance())) * pow((width1() + width2()) / 2 , 0.428169) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.013892 * exp(-0.559088 * distance()) + 0.0138922 * exp(-0.566255 * distance())) * pow((width1() + width2()) / 2 , 0.428169)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 poly INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.013892 * exp(-0.559088 * distance()) + 0.0138922 * exp(-0.566255 * distance())) * pow((width1() + width2()) / 2 , 0.428169) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.013892 * exp(-0.559088 * distance()) + 0.0138922 * exp(-0.566255 * distance())) * pow((width1() + width2()) / 2 , 0.428169)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal3,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00780687 * exp(-0.324898 * distance()) + 0.0078065 * exp(-0.326824 * distance())) * pow((width1() + width2()) / 2 , 0.452714) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00780687 * exp(-0.324898 * distance()) + 0.0078065 * exp(-0.326824 * distance())) * pow((width1() + width2()) / 2 , 0.452714)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 poly INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00780687 * exp(-0.324898 * distance()) + 0.0078065 * exp(-0.326824 * distance())) * pow((width1() + width2()) / 2 , 0.452714) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00780687 * exp(-0.324898 * distance()) + 0.0078065 * exp(-0.326824 * distance())) * pow((width1() + width2()) / 2 , 0.452714)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00780687 * exp(-0.324898 * distance()) + 0.0078065 * exp(-0.326824 * distance())) * pow((width1() + width2()) / 2 , 0.452714) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00780687 * exp(-0.324898 * distance()) + 0.0078065 * exp(-0.326824 * distance())) * pow((width1() + width2()) / 2 , 0.452714)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 poly INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00780687 * exp(-0.324898 * distance()) + 0.0078065 * exp(-0.326824 * distance())) * pow((width1() + width2()) / 2 , 0.452714) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00780687 * exp(-0.324898 * distance()) + 0.0078065 * exp(-0.326824 * distance())) * pow((width1() + width2()) / 2 , 0.452714)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00527187 * exp(-0.212978 * distance()) + 0.00527174 * exp(-0.215232 * distance())) * pow((width1() + width2()) / 2 , 0.457344) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00527187 * exp(-0.212978 * distance()) + 0.00527174 * exp(-0.215232 * distance())) * pow((width1() + width2()) / 2 , 0.457344)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 poly INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00527187 * exp(-0.212978 * distance()) + 0.00527174 * exp(-0.215232 * distance())) * pow((width1() + width2()) / 2 , 0.457344) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00527187 * exp(-0.212978 * distance()) + 0.00527174 * exp(-0.215232 * distance())) * pow((width1() + width2()) / 2 , 0.457344)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00527187 * exp(-0.212978 * distance()) + 0.00527174 * exp(-0.215232 * distance())) * pow((width1() + width2()) / 2 , 0.457344) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00527187 * exp(-0.212978 * distance()) + 0.00527174 * exp(-0.215232 * distance())) * pow((width1() + width2()) / 2 , 0.457344)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 poly INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00527187 * exp(-0.212978 * distance()) + 0.00527174 * exp(-0.215232 * distance())) * pow((width1() + width2()) / 2 , 0.457344) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00527187 * exp(-0.212978 * distance()) + 0.00527174 * exp(-0.215232 * distance())) * pow((width1() + width2()) / 2 , 0.457344)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00393767 * exp(-0.148178 * distance()) + 0.00393961 * exp(-0.147772 * distance())) * pow((width1() + width2()) / 2 , 0.457006) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00393767 * exp(-0.148178 * distance()) + 0.00393961 * exp(-0.147772 * distance())) * pow((width1() + width2()) / 2 , 0.457006)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 poly INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00393767 * exp(-0.148178 * distance()) + 0.00393961 * exp(-0.147772 * distance())) * pow((width1() + width2()) / 2 , 0.457006) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00393767 * exp(-0.148178 * distance()) + 0.00393961 * exp(-0.147772 * distance())) * pow((width1() + width2()) / 2 , 0.457006)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00393767 * exp(-0.148178 * distance()) + 0.00393961 * exp(-0.147772 * distance())) * pow((width1() + width2()) / 2 , 0.457006) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00393767 * exp(-0.148178 * distance()) + 0.00393961 * exp(-0.147772 * distance())) * pow((width1() + width2()) / 2 , 0.457006)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 poly INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00393767 * exp(-0.148178 * distance()) + 0.00393961 * exp(-0.147772 * distance())) * pow((width1() + width2()) / 2 , 0.457006) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00393767 * exp(-0.148178 * distance()) + 0.00393961 * exp(-0.147772 * distance())) * pow((width1() + width2()) / 2 , 0.457006)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00314081 * exp(-0.105237 * distance()) + 0.00313886 * exp(-0.10535 * distance())) * pow((width1() + width2()) / 2 , 0.454832) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00314081 * exp(-0.105237 * distance()) + 0.00313886 * exp(-0.10535 * distance())) * pow((width1() + width2()) / 2 , 0.454832)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 poly INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00314081 * exp(-0.105237 * distance()) + 0.00313886 * exp(-0.10535 * distance())) * pow((width1() + width2()) / 2 , 0.454832) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00314081 * exp(-0.105237 * distance()) + 0.00313886 * exp(-0.10535 * distance())) * pow((width1() + width2()) / 2 , 0.454832)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00314081 * exp(-0.105237 * distance()) + 0.00313886 * exp(-0.10535 * distance())) * pow((width1() + width2()) / 2 , 0.454832) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00314081 * exp(-0.105237 * distance()) + 0.00313886 * exp(-0.10535 * distance())) * pow((width1() + width2()) / 2 , 0.454832)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 poly INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00314081 * exp(-0.105237 * distance()) + 0.00313886 * exp(-0.10535 * distance())) * pow((width1() + width2()) / 2 , 0.454832) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00314081 * exp(-0.105237 * distance()) + 0.00313886 * exp(-0.10535 * distance())) * pow((width1() + width2()) / 2 , 0.454832)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal7 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00263509 * exp(-0.0764272 * distance()) + 0.00263263 * exp(-0.0765808 * distance())) * pow((width1() + width2()) / 2 , 0.451065) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00263509 * exp(-0.0764272 * distance()) + 0.00263263 * exp(-0.0765808 * distance())) * pow((width1() + width2()) / 2 , 0.451065)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 poly INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00263509 * exp(-0.0764272 * distance()) + 0.00263263 * exp(-0.0765808 * distance())) * pow((width1() + width2()) / 2 , 0.451065) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00263509 * exp(-0.0764272 * distance()) + 0.00263263 * exp(-0.0765808 * distance())) * pow((width1() + width2()) / 2 , 0.451065)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal7 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00263509 * exp(-0.0764272 * distance()) + 0.00263263 * exp(-0.0765808 * distance())) * pow((width1() + width2()) / 2 , 0.451065) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00263509 * exp(-0.0764272 * distance()) + 0.00263263 * exp(-0.0765808 * distance())) * pow((width1() + width2()) / 2 , 0.451065)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 poly INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00263509 * exp(-0.0764272 * distance()) + 0.00263263 * exp(-0.0765808 * distance())) * pow((width1() + width2()) / 2 , 0.451065) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00263509 * exp(-0.0764272 * distance()) + 0.00263263 * exp(-0.0765808 * distance())) * pow((width1() + width2()) / 2 , 0.451065)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00231877 * exp(-0.0519844 * distance()) + 0.00232088 * exp(-0.0517582 * distance())) * pow((width1() + width2()) / 2 , 0.424227) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00231877 * exp(-0.0519844 * distance()) + 0.00232088 * exp(-0.0517582 * distance())) * pow((width1() + width2()) / 2 , 0.424227)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 poly INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00231877 * exp(-0.0519844 * distance()) + 0.00232088 * exp(-0.0517582 * distance())) * pow((width1() + width2()) / 2 , 0.424227) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00231877 * exp(-0.0519844 * distance()) + 0.00232088 * exp(-0.0517582 * distance())) * pow((width1() + width2()) / 2 , 0.424227)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00231877 * exp(-0.0519844 * distance()) + 0.00232088 * exp(-0.0517582 * distance())) * pow((width1() + width2()) / 2 , 0.424227) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00231877 * exp(-0.0519844 * distance()) + 0.00232088 * exp(-0.0517582 * distance())) * pow((width1() + width2()) / 2 , 0.424227)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 poly INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00231877 * exp(-0.0519844 * distance()) + 0.00232088 * exp(-0.0517582 * distance())) * pow((width1() + width2()) / 2 , 0.424227) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00231877 * exp(-0.0519844 * distance()) + 0.00232088 * exp(-0.0517582 * distance())) * pow((width1() + width2()) / 2 , 0.424227)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal2,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0595714 * exp(-0.940215 * distance()) + 0.0066461 * exp(-0.0859236 * distance())) * pow((width1() + width2()) / 2 , 0.246147) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0595714 * exp(-0.940215 * distance()) + 0.0066461 * exp(-0.0859236 * distance())) * pow((width1() + width2()) / 2 , 0.246147)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0595714 * exp(-0.940215 * distance()) + 0.0066461 * exp(-0.0859236 * distance())) * pow((width1() + width2()) / 2 , 0.246147) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0595714 * exp(-0.940215 * distance()) + 0.0066461 * exp(-0.0859236 * distance())) * pow((width1() + width2()) / 2 , 0.246147)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0595714 * exp(-0.940215 * distance()) + 0.0066461 * exp(-0.0859236 * distance())) * pow((width1() + width2()) / 2 , 0.246147) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0595714 * exp(-0.940215 * distance()) + 0.0066461 * exp(-0.0859236 * distance())) * pow((width1() + width2()) / 2 , 0.246147)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0595714 * exp(-0.940215 * distance()) + 0.0066461 * exp(-0.0859236 * distance())) * pow((width1() + width2()) / 2 , 0.246147) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0595714 * exp(-0.940215 * distance()) + 0.0066461 * exp(-0.0859236 * distance())) * pow((width1() + width2()) / 2 , 0.246147)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal3,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0170089 * exp(-0.368617 * distance()) + 0.0170089 * exp(-0.368446 * distance())) * pow((width1() + width2()) / 2 , 0.327722) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0170089 * exp(-0.368617 * distance()) + 0.0170089 * exp(-0.368446 * distance())) * pow((width1() + width2()) / 2 , 0.327722)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0170089 * exp(-0.368617 * distance()) + 0.0170089 * exp(-0.368446 * distance())) * pow((width1() + width2()) / 2 , 0.327722) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0170089 * exp(-0.368617 * distance()) + 0.0170089 * exp(-0.368446 * distance())) * pow((width1() + width2()) / 2 , 0.327722)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0170089 * exp(-0.368617 * distance()) + 0.0170089 * exp(-0.368446 * distance())) * pow((width1() + width2()) / 2 , 0.327722) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0170089 * exp(-0.368617 * distance()) + 0.0170089 * exp(-0.368446 * distance())) * pow((width1() + width2()) / 2 , 0.327722)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0170089 * exp(-0.368617 * distance()) + 0.0170089 * exp(-0.368446 * distance())) * pow((width1() + width2()) / 2 , 0.327722) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0170089 * exp(-0.368617 * distance()) + 0.0170089 * exp(-0.368446 * distance())) * pow((width1() + width2()) / 2 , 0.327722)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0113823 * exp(-0.238658 * distance()) + 0.0113825 * exp(-0.235162 * distance())) * pow((width1() + width2()) / 2 , 0.352181) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0113823 * exp(-0.238658 * distance()) + 0.0113825 * exp(-0.235162 * distance())) * pow((width1() + width2()) / 2 , 0.352181)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0113823 * exp(-0.238658 * distance()) + 0.0113825 * exp(-0.235162 * distance())) * pow((width1() + width2()) / 2 , 0.352181) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0113823 * exp(-0.238658 * distance()) + 0.0113825 * exp(-0.235162 * distance())) * pow((width1() + width2()) / 2 , 0.352181)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0113823 * exp(-0.238658 * distance()) + 0.0113825 * exp(-0.235162 * distance())) * pow((width1() + width2()) / 2 , 0.352181) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0113823 * exp(-0.238658 * distance()) + 0.0113825 * exp(-0.235162 * distance())) * pow((width1() + width2()) / 2 , 0.352181)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0113823 * exp(-0.238658 * distance()) + 0.0113825 * exp(-0.235162 * distance())) * pow((width1() + width2()) / 2 , 0.352181) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0113823 * exp(-0.238658 * distance()) + 0.0113825 * exp(-0.235162 * distance())) * pow((width1() + width2()) / 2 , 0.352181)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00847343 * exp(-0.163325 * distance()) + 0.00847327 * exp(-0.163281 * distance())) * pow((width1() + width2()) / 2 , 0.359879) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00847343 * exp(-0.163325 * distance()) + 0.00847327 * exp(-0.163281 * distance())) * pow((width1() + width2()) / 2 , 0.359879)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00847343 * exp(-0.163325 * distance()) + 0.00847327 * exp(-0.163281 * distance())) * pow((width1() + width2()) / 2 , 0.359879) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00847343 * exp(-0.163325 * distance()) + 0.00847327 * exp(-0.163281 * distance())) * pow((width1() + width2()) / 2 , 0.359879)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00847343 * exp(-0.163325 * distance()) + 0.00847327 * exp(-0.163281 * distance())) * pow((width1() + width2()) / 2 , 0.359879) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00847343 * exp(-0.163325 * distance()) + 0.00847327 * exp(-0.163281 * distance())) * pow((width1() + width2()) / 2 , 0.359879)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00847343 * exp(-0.163325 * distance()) + 0.00847327 * exp(-0.163281 * distance())) * pow((width1() + width2()) / 2 , 0.359879) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00847343 * exp(-0.163325 * distance()) + 0.00847327 * exp(-0.163281 * distance())) * pow((width1() + width2()) / 2 , 0.359879)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00673439 * exp(-0.117493 * distance()) + 0.0067344 * exp(-0.115913 * distance())) * pow((width1() + width2()) / 2 , 0.360845) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00673439 * exp(-0.117493 * distance()) + 0.0067344 * exp(-0.115913 * distance())) * pow((width1() + width2()) / 2 , 0.360845)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal1 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00673439 * exp(-0.117493 * distance()) + 0.0067344 * exp(-0.115913 * distance())) * pow((width1() + width2()) / 2 , 0.360845) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00673439 * exp(-0.117493 * distance()) + 0.0067344 * exp(-0.115913 * distance())) * pow((width1() + width2()) / 2 , 0.360845)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00673439 * exp(-0.117493 * distance()) + 0.0067344 * exp(-0.115913 * distance())) * pow((width1() + width2()) / 2 , 0.360845) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00673439 * exp(-0.117493 * distance()) + 0.0067344 * exp(-0.115913 * distance())) * pow((width1() + width2()) / 2 , 0.360845)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal1 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00673439 * exp(-0.117493 * distance()) + 0.0067344 * exp(-0.115913 * distance())) * pow((width1() + width2()) / 2 , 0.360845) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00673439 * exp(-0.117493 * distance()) + 0.0067344 * exp(-0.115913 * distance())) * pow((width1() + width2()) / 2 , 0.360845)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal7 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00562846 * exp(-0.0851725 * distance()) + 0.00562814 * exp(-0.0851883 * distance())) * pow((width1() + width2()) / 2 , 0.357669) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00562846 * exp(-0.0851725 * distance()) + 0.00562814 * exp(-0.0851883 * distance())) * pow((width1() + width2()) / 2 , 0.357669)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal1 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00562846 * exp(-0.0851725 * distance()) + 0.00562814 * exp(-0.0851883 * distance())) * pow((width1() + width2()) / 2 , 0.357669) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00562846 * exp(-0.0851725 * distance()) + 0.00562814 * exp(-0.0851883 * distance())) * pow((width1() + width2()) / 2 , 0.357669)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal7 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00562846 * exp(-0.0851725 * distance()) + 0.00562814 * exp(-0.0851883 * distance())) * pow((width1() + width2()) / 2 , 0.357669) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00562846 * exp(-0.0851725 * distance()) + 0.00562814 * exp(-0.0851883 * distance())) * pow((width1() + width2()) / 2 , 0.357669)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal1 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00562846 * exp(-0.0851725 * distance()) + 0.00562814 * exp(-0.0851883 * distance())) * pow((width1() + width2()) / 2 , 0.357669) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00562846 * exp(-0.0851725 * distance()) + 0.00562814 * exp(-0.0851883 * distance())) * pow((width1() + width2()) / 2 , 0.357669)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00491475 * exp(-0.0599289 * distance()) + 0.00491473 * exp(-0.0572194 * distance())) * pow((width1() + width2()) / 2 , 0.326731) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00491475 * exp(-0.0599289 * distance()) + 0.00491473 * exp(-0.0572194 * distance())) * pow((width1() + width2()) / 2 , 0.326731)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 metal1 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00491475 * exp(-0.0599289 * distance()) + 0.00491473 * exp(-0.0572194 * distance())) * pow((width1() + width2()) / 2 , 0.326731) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00491475 * exp(-0.0599289 * distance()) + 0.00491473 * exp(-0.0572194 * distance())) * pow((width1() + width2()) / 2 , 0.326731)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00491475 * exp(-0.0599289 * distance()) + 0.00491473 * exp(-0.0572194 * distance())) * pow((width1() + width2()) / 2 , 0.326731) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00491475 * exp(-0.0599289 * distance()) + 0.00491473 * exp(-0.0572194 * distance())) * pow((width1() + width2()) / 2 , 0.326731)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 metal1 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00491475 * exp(-0.0599289 * distance()) + 0.00491473 * exp(-0.0572194 * distance())) * pow((width1() + width2()) / 2 , 0.326731) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00491475 * exp(-0.0599289 * distance()) + 0.00491473 * exp(-0.0572194 * distance())) * pow((width1() + width2()) / 2 , 0.326731)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal3,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189627 * exp(-0.155084 * distance()) + 0.0563231 * exp(-0.936648 * distance())) * pow((width1() + width2()) / 2 , 0.223995) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189627 * exp(-0.155084 * distance()) + 0.0563231 * exp(-0.936648 * distance())) * pow((width1() + width2()) / 2 , 0.223995)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189627 * exp(-0.155084 * distance()) + 0.0563231 * exp(-0.936648 * distance())) * pow((width1() + width2()) / 2 , 0.223995) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189627 * exp(-0.155084 * distance()) + 0.0563231 * exp(-0.936648 * distance())) * pow((width1() + width2()) / 2 , 0.223995)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189627 * exp(-0.155084 * distance()) + 0.0563231 * exp(-0.936648 * distance())) * pow((width1() + width2()) / 2 , 0.223995) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189627 * exp(-0.155084 * distance()) + 0.0563231 * exp(-0.936648 * distance())) * pow((width1() + width2()) / 2 , 0.223995)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189627 * exp(-0.155084 * distance()) + 0.0563231 * exp(-0.936648 * distance())) * pow((width1() + width2()) / 2 , 0.223995) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189627 * exp(-0.155084 * distance()) + 0.0563231 * exp(-0.936648 * distance())) * pow((width1() + width2()) / 2 , 0.223995)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0415677 * exp(-0.305998 * distance()) + 4.12069e-06 * exp(1.21173 * distance())) * pow((width1() + width2()) / 2 , 0.2911) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0415677 * exp(-0.305998 * distance()) + 4.12069e-06 * exp(1.21173 * distance())) * pow((width1() + width2()) / 2 , 0.2911)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0415677 * exp(-0.305998 * distance()) + 4.12069e-06 * exp(1.21173 * distance())) * pow((width1() + width2()) / 2 , 0.2911) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0415677 * exp(-0.305998 * distance()) + 4.12069e-06 * exp(1.21173 * distance())) * pow((width1() + width2()) / 2 , 0.2911)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0415677 * exp(-0.305998 * distance()) + 4.12069e-06 * exp(1.21173 * distance())) * pow((width1() + width2()) / 2 , 0.2911) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0415677 * exp(-0.305998 * distance()) + 4.12069e-06 * exp(1.21173 * distance())) * pow((width1() + width2()) / 2 , 0.2911)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0415677 * exp(-0.305998 * distance()) + 4.12069e-06 * exp(1.21173 * distance())) * pow((width1() + width2()) / 2 , 0.2911) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0415677 * exp(-0.305998 * distance()) + 4.12069e-06 * exp(1.21173 * distance())) * pow((width1() + width2()) / 2 , 0.2911)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0146185 * exp(-0.192873 * distance()) + 0.0146185 * exp(-0.19287 * distance())) * pow((width1() + width2()) / 2 , 0.309934) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0146185 * exp(-0.192873 * distance()) + 0.0146185 * exp(-0.19287 * distance())) * pow((width1() + width2()) / 2 , 0.309934)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0146185 * exp(-0.192873 * distance()) + 0.0146185 * exp(-0.19287 * distance())) * pow((width1() + width2()) / 2 , 0.309934) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0146185 * exp(-0.192873 * distance()) + 0.0146185 * exp(-0.19287 * distance())) * pow((width1() + width2()) / 2 , 0.309934)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0146185 * exp(-0.192873 * distance()) + 0.0146185 * exp(-0.19287 * distance())) * pow((width1() + width2()) / 2 , 0.309934) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0146185 * exp(-0.192873 * distance()) + 0.0146185 * exp(-0.19287 * distance())) * pow((width1() + width2()) / 2 , 0.309934)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0146185 * exp(-0.192873 * distance()) + 0.0146185 * exp(-0.19287 * distance())) * pow((width1() + width2()) / 2 , 0.309934) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0146185 * exp(-0.192873 * distance()) + 0.0146185 * exp(-0.19287 * distance())) * pow((width1() + width2()) / 2 , 0.309934)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0113226 * exp(-0.136003 * distance()) + 0.0113226 * exp(-0.134057 * distance())) * pow((width1() + width2()) / 2 , 0.315095) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0113226 * exp(-0.136003 * distance()) + 0.0113226 * exp(-0.134057 * distance())) * pow((width1() + width2()) / 2 , 0.315095)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0113226 * exp(-0.136003 * distance()) + 0.0113226 * exp(-0.134057 * distance())) * pow((width1() + width2()) / 2 , 0.315095) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0113226 * exp(-0.136003 * distance()) + 0.0113226 * exp(-0.134057 * distance())) * pow((width1() + width2()) / 2 , 0.315095)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0113226 * exp(-0.136003 * distance()) + 0.0113226 * exp(-0.134057 * distance())) * pow((width1() + width2()) / 2 , 0.315095) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0113226 * exp(-0.136003 * distance()) + 0.0113226 * exp(-0.134057 * distance())) * pow((width1() + width2()) / 2 , 0.315095)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0113226 * exp(-0.136003 * distance()) + 0.0113226 * exp(-0.134057 * distance())) * pow((width1() + width2()) / 2 , 0.315095) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0113226 * exp(-0.136003 * distance()) + 0.0113226 * exp(-0.134057 * distance())) * pow((width1() + width2()) / 2 , 0.315095)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal7 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00931223 * exp(-0.0971666 * distance()) + 0.00931222 * exp(-0.0980246 * distance())) * pow((width1() + width2()) / 2 , 0.312396) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00931223 * exp(-0.0971666 * distance()) + 0.00931222 * exp(-0.0980246 * distance())) * pow((width1() + width2()) / 2 , 0.312396)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal2 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00931223 * exp(-0.0971666 * distance()) + 0.00931222 * exp(-0.0980246 * distance())) * pow((width1() + width2()) / 2 , 0.312396) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00931223 * exp(-0.0971666 * distance()) + 0.00931222 * exp(-0.0980246 * distance())) * pow((width1() + width2()) / 2 , 0.312396)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal7 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00931223 * exp(-0.0971666 * distance()) + 0.00931222 * exp(-0.0980246 * distance())) * pow((width1() + width2()) / 2 , 0.312396) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00931223 * exp(-0.0971666 * distance()) + 0.00931222 * exp(-0.0980246 * distance())) * pow((width1() + width2()) / 2 , 0.312396)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal2 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00931223 * exp(-0.0971666 * distance()) + 0.00931222 * exp(-0.0980246 * distance())) * pow((width1() + width2()) / 2 , 0.312396) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00931223 * exp(-0.0971666 * distance()) + 0.00931222 * exp(-0.0980246 * distance())) * pow((width1() + width2()) / 2 , 0.312396)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00804331 * exp(-0.0670747 * distance()) + 0.00804342 * exp(-0.0671058 * distance())) * pow((width1() + width2()) / 2 , 0.280402) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00804331 * exp(-0.0670747 * distance()) + 0.00804342 * exp(-0.0671058 * distance())) * pow((width1() + width2()) / 2 , 0.280402)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 metal2 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00804331 * exp(-0.0670747 * distance()) + 0.00804342 * exp(-0.0671058 * distance())) * pow((width1() + width2()) / 2 , 0.280402) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00804331 * exp(-0.0670747 * distance()) + 0.00804342 * exp(-0.0671058 * distance())) * pow((width1() + width2()) / 2 , 0.280402)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00804331 * exp(-0.0670747 * distance()) + 0.00804342 * exp(-0.0671058 * distance())) * pow((width1() + width2()) / 2 , 0.280402) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00804331 * exp(-0.0670747 * distance()) + 0.00804342 * exp(-0.0671058 * distance())) * pow((width1() + width2()) / 2 , 0.280402)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 metal2 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00804331 * exp(-0.0670747 * distance()) + 0.00804342 * exp(-0.0671058 * distance())) * pow((width1() + width2()) / 2 , 0.280402) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00804331 * exp(-0.0670747 * distance()) + 0.00804342 * exp(-0.0671058 * distance())) * pow((width1() + width2()) / 2 , 0.280402)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0260016 * exp(-0.144954 * distance()) + 0.0537875 * exp(-0.948968 * distance())) * pow((width1() + width2()) / 2 , 0.22114) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0260016 * exp(-0.144954 * distance()) + 0.0537875 * exp(-0.948968 * distance())) * pow((width1() + width2()) / 2 , 0.22114)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0260016 * exp(-0.144954 * distance()) + 0.0537875 * exp(-0.948968 * distance())) * pow((width1() + width2()) / 2 , 0.22114) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0260016 * exp(-0.144954 * distance()) + 0.0537875 * exp(-0.948968 * distance())) * pow((width1() + width2()) / 2 , 0.22114)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0260016 * exp(-0.144954 * distance()) + 0.0537875 * exp(-0.948968 * distance())) * pow((width1() + width2()) / 2 , 0.22114) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0260016 * exp(-0.144954 * distance()) + 0.0537875 * exp(-0.948968 * distance())) * pow((width1() + width2()) / 2 , 0.22114)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0260016 * exp(-0.144954 * distance()) + 0.0537875 * exp(-0.948968 * distance())) * pow((width1() + width2()) / 2 , 0.22114) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0260016 * exp(-0.144954 * distance()) + 0.0537875 * exp(-0.948968 * distance())) * pow((width1() + width2()) / 2 , 0.22114)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0226242 * exp(-0.251006 * distance()) + 0.0226242 * exp(-0.252038 * distance())) * pow((width1() + width2()) / 2 , 0.273344) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0226242 * exp(-0.251006 * distance()) + 0.0226242 * exp(-0.252038 * distance())) * pow((width1() + width2()) / 2 , 0.273344)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0226242 * exp(-0.251006 * distance()) + 0.0226242 * exp(-0.252038 * distance())) * pow((width1() + width2()) / 2 , 0.273344) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0226242 * exp(-0.251006 * distance()) + 0.0226242 * exp(-0.252038 * distance())) * pow((width1() + width2()) / 2 , 0.273344)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0226242 * exp(-0.251006 * distance()) + 0.0226242 * exp(-0.252038 * distance())) * pow((width1() + width2()) / 2 , 0.273344) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0226242 * exp(-0.251006 * distance()) + 0.0226242 * exp(-0.252038 * distance())) * pow((width1() + width2()) / 2 , 0.273344)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0226242 * exp(-0.251006 * distance()) + 0.0226242 * exp(-0.252038 * distance())) * pow((width1() + width2()) / 2 , 0.273344) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0226242 * exp(-0.251006 * distance()) + 0.0226242 * exp(-0.252038 * distance())) * pow((width1() + width2()) / 2 , 0.273344)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0164236 * exp(-0.165161 * distance()) + 0.0164236 * exp(-0.165179 * distance())) * pow((width1() + width2()) / 2 , 0.284477) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0164236 * exp(-0.165161 * distance()) + 0.0164236 * exp(-0.165179 * distance())) * pow((width1() + width2()) / 2 , 0.284477)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0164236 * exp(-0.165161 * distance()) + 0.0164236 * exp(-0.165179 * distance())) * pow((width1() + width2()) / 2 , 0.284477) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0164236 * exp(-0.165161 * distance()) + 0.0164236 * exp(-0.165179 * distance())) * pow((width1() + width2()) / 2 , 0.284477)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0164236 * exp(-0.165161 * distance()) + 0.0164236 * exp(-0.165179 * distance())) * pow((width1() + width2()) / 2 , 0.284477) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0164236 * exp(-0.165161 * distance()) + 0.0164236 * exp(-0.165179 * distance())) * pow((width1() + width2()) / 2 , 0.284477)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0164236 * exp(-0.165161 * distance()) + 0.0164236 * exp(-0.165179 * distance())) * pow((width1() + width2()) / 2 , 0.284477) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0164236 * exp(-0.165161 * distance()) + 0.0164236 * exp(-0.165179 * distance())) * pow((width1() + width2()) / 2 , 0.284477)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal7 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0130722 * exp(-0.115918 * distance()) + 0.0130722 * exp(-0.115933 * distance())) * pow((width1() + width2()) / 2 , 0.282987) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0130722 * exp(-0.115918 * distance()) + 0.0130722 * exp(-0.115933 * distance())) * pow((width1() + width2()) / 2 , 0.282987)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0130722 * exp(-0.115918 * distance()) + 0.0130722 * exp(-0.115933 * distance())) * pow((width1() + width2()) / 2 , 0.282987) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0130722 * exp(-0.115918 * distance()) + 0.0130722 * exp(-0.115933 * distance())) * pow((width1() + width2()) / 2 , 0.282987)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal7 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0130722 * exp(-0.115918 * distance()) + 0.0130722 * exp(-0.115933 * distance())) * pow((width1() + width2()) / 2 , 0.282987) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0130722 * exp(-0.115918 * distance()) + 0.0130722 * exp(-0.115933 * distance())) * pow((width1() + width2()) / 2 , 0.282987)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0130722 * exp(-0.115918 * distance()) + 0.0130722 * exp(-0.115933 * distance())) * pow((width1() + width2()) / 2 , 0.282987) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0130722 * exp(-0.115918 * distance()) + 0.0130722 * exp(-0.115933 * distance())) * pow((width1() + width2()) / 2 , 0.282987)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0110527 * exp(-0.07776 * distance()) + 0.0110527 * exp(-0.078648 * distance())) * pow((width1() + width2()) / 2 , 0.250484) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0110527 * exp(-0.07776 * distance()) + 0.0110527 * exp(-0.078648 * distance())) * pow((width1() + width2()) / 2 , 0.250484)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 metal3 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0110527 * exp(-0.07776 * distance()) + 0.0110527 * exp(-0.078648 * distance())) * pow((width1() + width2()) / 2 , 0.250484) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0110527 * exp(-0.07776 * distance()) + 0.0110527 * exp(-0.078648 * distance())) * pow((width1() + width2()) / 2 , 0.250484)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0110527 * exp(-0.07776 * distance()) + 0.0110527 * exp(-0.078648 * distance())) * pow((width1() + width2()) / 2 , 0.250484) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0110527 * exp(-0.07776 * distance()) + 0.0110527 * exp(-0.078648 * distance())) * pow((width1() + width2()) / 2 , 0.250484)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 metal3 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0110527 * exp(-0.07776 * distance()) + 0.0110527 * exp(-0.078648 * distance())) * pow((width1() + width2()) / 2 , 0.250484) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0110527 * exp(-0.07776 * distance()) + 0.0110527 * exp(-0.078648 * distance())) * pow((width1() + width2()) / 2 , 0.250484)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0299999 * exp(-0.12851 * distance()) + 0.05274 * exp(-0.958268 * distance())) * pow((width1() + width2()) / 2 , 0.21985) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0299999 * exp(-0.12851 * distance()) + 0.05274 * exp(-0.958268 * distance())) * pow((width1() + width2()) / 2 , 0.21985)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0299999 * exp(-0.12851 * distance()) + 0.05274 * exp(-0.958268 * distance())) * pow((width1() + width2()) / 2 , 0.21985) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0299999 * exp(-0.12851 * distance()) + 0.05274 * exp(-0.958268 * distance())) * pow((width1() + width2()) / 2 , 0.21985)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0299999 * exp(-0.12851 * distance()) + 0.05274 * exp(-0.958268 * distance())) * pow((width1() + width2()) / 2 , 0.21985) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0299999 * exp(-0.12851 * distance()) + 0.05274 * exp(-0.958268 * distance())) * pow((width1() + width2()) / 2 , 0.21985)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0299999 * exp(-0.12851 * distance()) + 0.05274 * exp(-0.958268 * distance())) * pow((width1() + width2()) / 2 , 0.21985) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0299999 * exp(-0.12851 * distance()) + 0.05274 * exp(-0.958268 * distance())) * pow((width1() + width2()) / 2 , 0.21985)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0239626 * exp(-0.225699 * distance()) + 0.0239626 * exp(-0.224253 * distance())) * pow((width1() + width2()) / 2 , 0.263712) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0239626 * exp(-0.225699 * distance()) + 0.0239626 * exp(-0.224253 * distance())) * pow((width1() + width2()) / 2 , 0.263712)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0239626 * exp(-0.225699 * distance()) + 0.0239626 * exp(-0.224253 * distance())) * pow((width1() + width2()) / 2 , 0.263712) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0239626 * exp(-0.225699 * distance()) + 0.0239626 * exp(-0.224253 * distance())) * pow((width1() + width2()) / 2 , 0.263712)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0239626 * exp(-0.225699 * distance()) + 0.0239626 * exp(-0.224253 * distance())) * pow((width1() + width2()) / 2 , 0.263712) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0239626 * exp(-0.225699 * distance()) + 0.0239626 * exp(-0.224253 * distance())) * pow((width1() + width2()) / 2 , 0.263712)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0239626 * exp(-0.225699 * distance()) + 0.0239626 * exp(-0.224253 * distance())) * pow((width1() + width2()) / 2 , 0.263712) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0239626 * exp(-0.225699 * distance()) + 0.0239626 * exp(-0.224253 * distance())) * pow((width1() + width2()) / 2 , 0.263712)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal7 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0178389 * exp(-0.143555 * distance()) + 0.0178389 * exp(-0.151607 * distance())) * pow((width1() + width2()) / 2 , 0.268197) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0178389 * exp(-0.143555 * distance()) + 0.0178389 * exp(-0.151607 * distance())) * pow((width1() + width2()) / 2 , 0.268197)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0178389 * exp(-0.143555 * distance()) + 0.0178389 * exp(-0.151607 * distance())) * pow((width1() + width2()) / 2 , 0.268197) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0178389 * exp(-0.143555 * distance()) + 0.0178389 * exp(-0.151607 * distance())) * pow((width1() + width2()) / 2 , 0.268197)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal7 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0178389 * exp(-0.143555 * distance()) + 0.0178389 * exp(-0.151607 * distance())) * pow((width1() + width2()) / 2 , 0.268197) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0178389 * exp(-0.143555 * distance()) + 0.0178389 * exp(-0.151607 * distance())) * pow((width1() + width2()) / 2 , 0.268197)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0178389 * exp(-0.143555 * distance()) + 0.0178389 * exp(-0.151607 * distance())) * pow((width1() + width2()) / 2 , 0.268197) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0178389 * exp(-0.143555 * distance()) + 0.0178389 * exp(-0.151607 * distance())) * pow((width1() + width2()) / 2 , 0.268197)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0145143 * exp(-0.0956269 * distance()) + 0.0145145 * exp(-0.096135 * distance())) * pow((width1() + width2()) / 2 , 0.235985) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0145143 * exp(-0.0956269 * distance()) + 0.0145145 * exp(-0.096135 * distance())) * pow((width1() + width2()) / 2 , 0.235985)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 metal4 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0145143 * exp(-0.0956269 * distance()) + 0.0145145 * exp(-0.096135 * distance())) * pow((width1() + width2()) / 2 , 0.235985) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0145143 * exp(-0.0956269 * distance()) + 0.0145145 * exp(-0.096135 * distance())) * pow((width1() + width2()) / 2 , 0.235985)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0145143 * exp(-0.0956269 * distance()) + 0.0145145 * exp(-0.096135 * distance())) * pow((width1() + width2()) / 2 , 0.235985) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0145143 * exp(-0.0956269 * distance()) + 0.0145145 * exp(-0.096135 * distance())) * pow((width1() + width2()) / 2 , 0.235985)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 metal4 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0145143 * exp(-0.0956269 * distance()) + 0.0145145 * exp(-0.096135 * distance())) * pow((width1() + width2()) / 2 , 0.235985) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0145143 * exp(-0.0956269 * distance()) + 0.0145145 * exp(-0.096135 * distance())) * pow((width1() + width2()) / 2 , 0.235985)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal5,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0327961 * exp(-0.116322 * distance()) + 0.0521427 * exp(-0.965152 * distance())) * pow((width1() + width2()) / 2 , 0.218841) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0327961 * exp(-0.116322 * distance()) + 0.0521427 * exp(-0.965152 * distance())) * pow((width1() + width2()) / 2 , 0.218841)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0327961 * exp(-0.116322 * distance()) + 0.0521427 * exp(-0.965152 * distance())) * pow((width1() + width2()) / 2 , 0.218841) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0327961 * exp(-0.116322 * distance()) + 0.0521427 * exp(-0.965152 * distance())) * pow((width1() + width2()) / 2 , 0.218841)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0327961 * exp(-0.116322 * distance()) + 0.0521427 * exp(-0.965152 * distance())) * pow((width1() + width2()) / 2 , 0.218841) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0327961 * exp(-0.116322 * distance()) + 0.0521427 * exp(-0.965152 * distance())) * pow((width1() + width2()) / 2 , 0.218841)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0327961 * exp(-0.116322 * distance()) + 0.0521427 * exp(-0.965152 * distance())) * pow((width1() + width2()) / 2 , 0.218841) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0327961 * exp(-0.116322 * distance()) + 0.0521427 * exp(-0.965152 * distance())) * pow((width1() + width2()) / 2 , 0.218841)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal5,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0251409 * exp(-0.206847 * distance()) + 0.0251409 * exp(-0.206862 * distance())) * pow((width1() + width2()) / 2 , 0.255131) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0251409 * exp(-0.206847 * distance()) + 0.0251409 * exp(-0.206862 * distance())) * pow((width1() + width2()) / 2 , 0.255131)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0251409 * exp(-0.206847 * distance()) + 0.0251409 * exp(-0.206862 * distance())) * pow((width1() + width2()) / 2 , 0.255131) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0251409 * exp(-0.206847 * distance()) + 0.0251409 * exp(-0.206862 * distance())) * pow((width1() + width2()) / 2 , 0.255131)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0251409 * exp(-0.206847 * distance()) + 0.0251409 * exp(-0.206862 * distance())) * pow((width1() + width2()) / 2 , 0.255131) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0251409 * exp(-0.206847 * distance()) + 0.0251409 * exp(-0.206862 * distance())) * pow((width1() + width2()) / 2 , 0.255131)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0251409 * exp(-0.206847 * distance()) + 0.0251409 * exp(-0.206862 * distance())) * pow((width1() + width2()) / 2 , 0.255131) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0251409 * exp(-0.206847 * distance()) + 0.0251409 * exp(-0.206862 * distance())) * pow((width1() + width2()) / 2 , 0.255131)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal5,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189918 * exp(-0.124749 * distance()) + 0.018992 * exp(-0.123916 * distance())) * pow((width1() + width2()) / 2 , 0.228484) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189918 * exp(-0.124749 * distance()) + 0.018992 * exp(-0.123916 * distance())) * pow((width1() + width2()) / 2 , 0.228484)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 metal5 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189918 * exp(-0.124749 * distance()) + 0.018992 * exp(-0.123916 * distance())) * pow((width1() + width2()) / 2 , 0.228484) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189918 * exp(-0.124749 * distance()) + 0.018992 * exp(-0.123916 * distance())) * pow((width1() + width2()) / 2 , 0.228484)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189918 * exp(-0.124749 * distance()) + 0.018992 * exp(-0.123916 * distance())) * pow((width1() + width2()) / 2 , 0.228484) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189918 * exp(-0.124749 * distance()) + 0.018992 * exp(-0.123916 * distance())) * pow((width1() + width2()) / 2 , 0.228484)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 metal5 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0189918 * exp(-0.124749 * distance()) + 0.018992 * exp(-0.123916 * distance())) * pow((width1() + width2()) / 2 , 0.228484) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0189918 * exp(-0.124749 * distance()) + 0.018992 * exp(-0.123916 * distance())) * pow((width1() + width2()) / 2 , 0.228484)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal6,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0356821 * exp(-0.110806 * distance()) + 0.051532 * exp(-0.964376 * distance())) * pow((width1() + width2()) / 2 , 0.217352) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0356821 * exp(-0.110806 * distance()) + 0.051532 * exp(-0.964376 * distance())) * pow((width1() + width2()) / 2 , 0.217352)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0356821 * exp(-0.110806 * distance()) + 0.051532 * exp(-0.964376 * distance())) * pow((width1() + width2()) / 2 , 0.217352) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0356821 * exp(-0.110806 * distance()) + 0.051532 * exp(-0.964376 * distance())) * pow((width1() + width2()) / 2 , 0.217352)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0356821 * exp(-0.110806 * distance()) + 0.051532 * exp(-0.964376 * distance())) * pow((width1() + width2()) / 2 , 0.217352) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0356821 * exp(-0.110806 * distance()) + 0.051532 * exp(-0.964376 * distance())) * pow((width1() + width2()) / 2 , 0.217352)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0356821 * exp(-0.110806 * distance()) + 0.051532 * exp(-0.964376 * distance())) * pow((width1() + width2()) / 2 , 0.217352) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0356821 * exp(-0.110806 * distance()) + 0.051532 * exp(-0.964376 * distance())) * pow((width1() + width2()) / 2 , 0.217352)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal6,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0258268 * exp(-0.174058 * distance()) + 0.0258268 * exp(-0.174058 * distance())) * pow((width1() + width2()) / 2 , 0.22369) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0258268 * exp(-0.174058 * distance()) + 0.0258268 * exp(-0.174058 * distance())) * pow((width1() + width2()) / 2 , 0.22369)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 metal6 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0258268 * exp(-0.174058 * distance()) + 0.0258268 * exp(-0.174058 * distance())) * pow((width1() + width2()) / 2 , 0.22369) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0258268 * exp(-0.174058 * distance()) + 0.0258268 * exp(-0.174058 * distance())) * pow((width1() + width2()) / 2 , 0.22369)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0258268 * exp(-0.174058 * distance()) + 0.0258268 * exp(-0.174058 * distance())) * pow((width1() + width2()) / 2 , 0.22369) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0258268 * exp(-0.174058 * distance()) + 0.0258268 * exp(-0.174058 * distance())) * pow((width1() + width2()) / 2 , 0.22369)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 metal6 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0258268 * exp(-0.174058 * distance()) + 0.0258268 * exp(-0.174058 * distance())) * pow((width1() + width2()) / 2 , 0.22369) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0258268 * exp(-0.174058 * distance()) + 0.0258268 * exp(-0.174058 * distance())) * pow((width1() + width2()) / 2 , 0.22369)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 314  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal7,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal8 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0219257 * exp(-0.0123878 * distance()) + 0.0610942 * exp(-0.540626 * distance())) * pow((width1() + width2()) / 2 , 0.206901) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0219257 * exp(-0.0123878 * distance()) + 0.0610942 * exp(-0.540626 * distance())) * pow((width1() + width2()) / 2 , 0.206901)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 metal7 INSIDE OF tndiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0219257 * exp(-0.0123878 * distance()) + 0.0610942 * exp(-0.540626 * distance())) * pow((width1() + width2()) / 2 , 0.206901) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0219257 * exp(-0.0123878 * distance()) + 0.0610942 * exp(-0.540626 * distance())) * pow((width1() + width2()) / 2 , 0.206901)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal8 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0219257 * exp(-0.0123878 * distance()) + 0.0610942 * exp(-0.540626 * distance())) * pow((width1() + width2()) / 2 , 0.206901) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0219257 * exp(-0.0123878 * distance()) + 0.0610942 * exp(-0.540626 * distance())) * pow((width1() + width2()) / 2 , 0.206901)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal8 metal7 INSIDE OF tpdiff MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal7
    max_distance = (MAXDISTANCE_metal7 + MAXDISTANCE_metal8) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0219257 * exp(-0.0123878 * distance()) + 0.0610942 * exp(-0.540626 * distance())) * pow((width1() + width2()) / 2 , 0.206901) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0219257 * exp(-0.0123878 * distance()) + 0.0610942 * exp(-0.540626 * distance())) * pow((width1() + width2()) / 2 , 0.206901)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal1,metal2,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tndiff metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.03064 * exp(-2.07071 * distance()) + 0.03064 * exp(-2.07073 * distance())) * pow((width1() + width2()) / 2 , 0.280681) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.03064 * exp(-2.07071 * distance()) + 0.03064 * exp(-2.07073 * distance())) * pow((width1() + width2()) / 2 , 0.280681)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tndiff metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.03064 * exp(-2.07071 * distance()) + 0.03064 * exp(-2.07073 * distance())) * pow((width1() + width2()) / 2 , 0.280681) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.03064 * exp(-2.07071 * distance()) + 0.03064 * exp(-2.07073 * distance())) * pow((width1() + width2()) / 2 , 0.280681)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tpdiff metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.03064 * exp(-2.07071 * distance()) + 0.03064 * exp(-2.07073 * distance())) * pow((width1() + width2()) / 2 , 0.280681) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.03064 * exp(-2.07071 * distance()) + 0.03064 * exp(-2.07073 * distance())) * pow((width1() + width2()) / 2 , 0.280681)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tpdiff metal2 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.03064 * exp(-2.07071 * distance()) + 0.03064 * exp(-2.07073 * distance())) * pow((width1() + width2()) / 2 , 0.280681) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.03064 * exp(-2.07071 * distance()) + 0.03064 * exp(-2.07073 * distance())) * pow((width1() + width2()) / 2 , 0.280681)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal1,metal3,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tndiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0327548 * exp(-1.75985 * distance()) + 0.0325836 * exp(-1.75942 * distance())) * pow((width1() + width2()) / 2 , 0.297938) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0327548 * exp(-1.75985 * distance()) + 0.0325836 * exp(-1.75942 * distance())) * pow((width1() + width2()) / 2 , 0.297938)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tndiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0327548 * exp(-1.75985 * distance()) + 0.0325836 * exp(-1.75942 * distance())) * pow((width1() + width2()) / 2 , 0.297938) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0327548 * exp(-1.75985 * distance()) + 0.0325836 * exp(-1.75942 * distance())) * pow((width1() + width2()) / 2 , 0.297938)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tpdiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0327548 * exp(-1.75985 * distance()) + 0.0325836 * exp(-1.75942 * distance())) * pow((width1() + width2()) / 2 , 0.297938) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0327548 * exp(-1.75985 * distance()) + 0.0325836 * exp(-1.75942 * distance())) * pow((width1() + width2()) / 2 , 0.297938)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tpdiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0327548 * exp(-1.75985 * distance()) + 0.0325836 * exp(-1.75942 * distance())) * pow((width1() + width2()) / 2 , 0.297938) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0327548 * exp(-1.75985 * distance()) + 0.0325836 * exp(-1.75942 * distance())) * pow((width1() + width2()) / 2 , 0.297938)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal1,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00122709 * exp(-0.287823 * distance()) + 0.0653319 * exp(-1.71372 * distance())) * pow((width1() + width2()) / 2 , 0.304032) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00122709 * exp(-0.287823 * distance()) + 0.0653319 * exp(-1.71372 * distance())) * pow((width1() + width2()) / 2 , 0.304032)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00122709 * exp(-0.287823 * distance()) + 0.0653319 * exp(-1.71372 * distance())) * pow((width1() + width2()) / 2 , 0.304032) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00122709 * exp(-0.287823 * distance()) + 0.0653319 * exp(-1.71372 * distance())) * pow((width1() + width2()) / 2 , 0.304032)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00122709 * exp(-0.287823 * distance()) + 0.0653319 * exp(-1.71372 * distance())) * pow((width1() + width2()) / 2 , 0.304032) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00122709 * exp(-0.287823 * distance()) + 0.0653319 * exp(-1.71372 * distance())) * pow((width1() + width2()) / 2 , 0.304032)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00122709 * exp(-0.287823 * distance()) + 0.0653319 * exp(-1.71372 * distance())) * pow((width1() + width2()) / 2 , 0.304032) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00122709 * exp(-0.287823 * distance()) + 0.0653319 * exp(-1.71372 * distance())) * pow((width1() + width2()) / 2 , 0.304032)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal1,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.064299 * exp(-1.72475 * distance()) + 0.00281014 * exp(-0.361676 * distance())) * pow((width1() + width2()) / 2 , 0.306727) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.064299 * exp(-1.72475 * distance()) + 0.00281014 * exp(-0.361676 * distance())) * pow((width1() + width2()) / 2 , 0.306727)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.064299 * exp(-1.72475 * distance()) + 0.00281014 * exp(-0.361676 * distance())) * pow((width1() + width2()) / 2 , 0.306727) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.064299 * exp(-1.72475 * distance()) + 0.00281014 * exp(-0.361676 * distance())) * pow((width1() + width2()) / 2 , 0.306727)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.064299 * exp(-1.72475 * distance()) + 0.00281014 * exp(-0.361676 * distance())) * pow((width1() + width2()) / 2 , 0.306727) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.064299 * exp(-1.72475 * distance()) + 0.00281014 * exp(-0.361676 * distance())) * pow((width1() + width2()) / 2 , 0.306727)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.064299 * exp(-1.72475 * distance()) + 0.00281014 * exp(-0.361676 * distance())) * pow((width1() + width2()) / 2 , 0.306727) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.064299 * exp(-1.72475 * distance()) + 0.00281014 * exp(-0.361676 * distance())) * pow((width1() + width2()) / 2 , 0.306727)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal1,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00359193 * exp(-0.354867 * distance()) + 0.0638079 * exp(-1.73212 * distance())) * pow((width1() + width2()) / 2 , 0.308196) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00359193 * exp(-0.354867 * distance()) + 0.0638079 * exp(-1.73212 * distance())) * pow((width1() + width2()) / 2 , 0.308196)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00359193 * exp(-0.354867 * distance()) + 0.0638079 * exp(-1.73212 * distance())) * pow((width1() + width2()) / 2 , 0.308196) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00359193 * exp(-0.354867 * distance()) + 0.0638079 * exp(-1.73212 * distance())) * pow((width1() + width2()) / 2 , 0.308196)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00359193 * exp(-0.354867 * distance()) + 0.0638079 * exp(-1.73212 * distance())) * pow((width1() + width2()) / 2 , 0.308196) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00359193 * exp(-0.354867 * distance()) + 0.0638079 * exp(-1.73212 * distance())) * pow((width1() + width2()) / 2 , 0.308196)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00359193 * exp(-0.354867 * distance()) + 0.0638079 * exp(-1.73212 * distance())) * pow((width1() + width2()) / 2 , 0.308196) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00359193 * exp(-0.354867 * distance()) + 0.0638079 * exp(-1.73212 * distance())) * pow((width1() + width2()) / 2 , 0.308196)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal1,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0635348 * exp(-1.73749 * distance()) + 0.00402597 * exp(-0.343851 * distance())) * pow((width1() + width2()) / 2 , 0.308957) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0635348 * exp(-1.73749 * distance()) + 0.00402597 * exp(-0.343851 * distance())) * pow((width1() + width2()) / 2 , 0.308957)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0635348 * exp(-1.73749 * distance()) + 0.00402597 * exp(-0.343851 * distance())) * pow((width1() + width2()) / 2 , 0.308957) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0635348 * exp(-1.73749 * distance()) + 0.00402597 * exp(-0.343851 * distance())) * pow((width1() + width2()) / 2 , 0.308957)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0635348 * exp(-1.73749 * distance()) + 0.00402597 * exp(-0.343851 * distance())) * pow((width1() + width2()) / 2 , 0.308957) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0635348 * exp(-1.73749 * distance()) + 0.00402597 * exp(-0.343851 * distance())) * pow((width1() + width2()) / 2 , 0.308957)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0635348 * exp(-1.73749 * distance()) + 0.00402597 * exp(-0.343851 * distance())) * pow((width1() + width2()) / 2 , 0.308957) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0635348 * exp(-1.73749 * distance()) + 0.00402597 * exp(-0.343851 * distance())) * pow((width1() + width2()) / 2 , 0.308957)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal1,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0635631 * exp(-1.73613 * distance()) + 0.00411796 * exp(-0.319058 * distance())) * pow((width1() + width2()) / 2 , 0.309614) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0635631 * exp(-1.73613 * distance()) + 0.00411796 * exp(-0.319058 * distance())) * pow((width1() + width2()) / 2 , 0.309614)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0635631 * exp(-1.73613 * distance()) + 0.00411796 * exp(-0.319058 * distance())) * pow((width1() + width2()) / 2 , 0.309614) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0635631 * exp(-1.73613 * distance()) + 0.00411796 * exp(-0.319058 * distance())) * pow((width1() + width2()) / 2 , 0.309614)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal1 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0635631 * exp(-1.73613 * distance()) + 0.00411796 * exp(-0.319058 * distance())) * pow((width1() + width2()) / 2 , 0.309614) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0635631 * exp(-1.73613 * distance()) + 0.00411796 * exp(-0.319058 * distance())) * pow((width1() + width2()) / 2 , 0.309614)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 poly INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal1) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0635631 * exp(-1.73613 * distance()) + 0.00411796 * exp(-0.319058 * distance())) * pow((width1() + width2()) / 2 , 0.309614) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0635631 * exp(-1.73613 * distance()) + 0.00411796 * exp(-0.319058 * distance())) * pow((width1() + width2()) / 2 , 0.309614)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal2,metal3,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal2 INSIDE OF tndiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0120335 * exp(-0.903548 * distance()) + 0.012031 * exp(-0.911378 * distance())) * pow((width1() + width2()) / 2 , 0.438307) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0120335 * exp(-0.903548 * distance()) + 0.012031 * exp(-0.911378 * distance())) * pow((width1() + width2()) / 2 , 0.438307)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 poly INSIDE OF tndiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0120335 * exp(-0.903548 * distance()) + 0.012031 * exp(-0.911378 * distance())) * pow((width1() + width2()) / 2 , 0.438307) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0120335 * exp(-0.903548 * distance()) + 0.012031 * exp(-0.911378 * distance())) * pow((width1() + width2()) / 2 , 0.438307)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal2 INSIDE OF tpdiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0120335 * exp(-0.903548 * distance()) + 0.012031 * exp(-0.911378 * distance())) * pow((width1() + width2()) / 2 , 0.438307) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0120335 * exp(-0.903548 * distance()) + 0.012031 * exp(-0.911378 * distance())) * pow((width1() + width2()) / 2 , 0.438307)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 poly INSIDE OF tpdiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0120335 * exp(-0.903548 * distance()) + 0.012031 * exp(-0.911378 * distance())) * pow((width1() + width2()) / 2 , 0.438307) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0120335 * exp(-0.903548 * distance()) + 0.012031 * exp(-0.911378 * distance())) * pow((width1() + width2()) / 2 , 0.438307)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal2,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal2 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0130505 * exp(-0.768456 * distance()) + 0.0130776 * exp(-0.768648 * distance())) * pow((width1() + width2()) / 2 , 0.428669) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0130505 * exp(-0.768456 * distance()) + 0.0130776 * exp(-0.768648 * distance())) * pow((width1() + width2()) / 2 , 0.428669)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 poly INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0130505 * exp(-0.768456 * distance()) + 0.0130776 * exp(-0.768648 * distance())) * pow((width1() + width2()) / 2 , 0.428669) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0130505 * exp(-0.768456 * distance()) + 0.0130776 * exp(-0.768648 * distance())) * pow((width1() + width2()) / 2 , 0.428669)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal2 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0130505 * exp(-0.768456 * distance()) + 0.0130776 * exp(-0.768648 * distance())) * pow((width1() + width2()) / 2 , 0.428669) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0130505 * exp(-0.768456 * distance()) + 0.0130776 * exp(-0.768648 * distance())) * pow((width1() + width2()) / 2 , 0.428669)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 poly INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0130505 * exp(-0.768456 * distance()) + 0.0130776 * exp(-0.768648 * distance())) * pow((width1() + width2()) / 2 , 0.428669) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0130505 * exp(-0.768456 * distance()) + 0.0130776 * exp(-0.768648 * distance())) * pow((width1() + width2()) / 2 , 0.428669)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal2,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal2 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0134195 * exp(-0.704653 * distance()) + 0.0134195 * exp(-0.703772 * distance())) * pow((width1() + width2()) / 2 , 0.42741) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0134195 * exp(-0.704653 * distance()) + 0.0134195 * exp(-0.703772 * distance())) * pow((width1() + width2()) / 2 , 0.42741)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 poly INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0134195 * exp(-0.704653 * distance()) + 0.0134195 * exp(-0.703772 * distance())) * pow((width1() + width2()) / 2 , 0.42741) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0134195 * exp(-0.704653 * distance()) + 0.0134195 * exp(-0.703772 * distance())) * pow((width1() + width2()) / 2 , 0.42741)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal2 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0134195 * exp(-0.704653 * distance()) + 0.0134195 * exp(-0.703772 * distance())) * pow((width1() + width2()) / 2 , 0.42741) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0134195 * exp(-0.704653 * distance()) + 0.0134195 * exp(-0.703772 * distance())) * pow((width1() + width2()) / 2 , 0.42741)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 poly INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0134195 * exp(-0.704653 * distance()) + 0.0134195 * exp(-0.703772 * distance())) * pow((width1() + width2()) / 2 , 0.42741) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0134195 * exp(-0.704653 * distance()) + 0.0134195 * exp(-0.703772 * distance())) * pow((width1() + width2()) / 2 , 0.42741)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal2,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal2 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0135917 * exp(-0.668145 * distance()) + 0.0135877 * exp(-0.66794 * distance())) * pow((width1() + width2()) / 2 , 0.42774) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0135917 * exp(-0.668145 * distance()) + 0.0135877 * exp(-0.66794 * distance())) * pow((width1() + width2()) / 2 , 0.42774)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 poly INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0135917 * exp(-0.668145 * distance()) + 0.0135877 * exp(-0.66794 * distance())) * pow((width1() + width2()) / 2 , 0.42774) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0135917 * exp(-0.668145 * distance()) + 0.0135877 * exp(-0.66794 * distance())) * pow((width1() + width2()) / 2 , 0.42774)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal2 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0135917 * exp(-0.668145 * distance()) + 0.0135877 * exp(-0.66794 * distance())) * pow((width1() + width2()) / 2 , 0.42774) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0135917 * exp(-0.668145 * distance()) + 0.0135877 * exp(-0.66794 * distance())) * pow((width1() + width2()) / 2 , 0.42774)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 poly INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0135917 * exp(-0.668145 * distance()) + 0.0135877 * exp(-0.66794 * distance())) * pow((width1() + width2()) / 2 , 0.42774) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0135917 * exp(-0.668145 * distance()) + 0.0135877 * exp(-0.66794 * distance())) * pow((width1() + width2()) / 2 , 0.42774)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal2,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal2 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0136843 * exp(-0.645327 * distance()) + 0.0136577 * exp(-0.645368 * distance())) * pow((width1() + width2()) / 2 , 0.42774) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0136843 * exp(-0.645327 * distance()) + 0.0136577 * exp(-0.645368 * distance())) * pow((width1() + width2()) / 2 , 0.42774)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 poly INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0136843 * exp(-0.645327 * distance()) + 0.0136577 * exp(-0.645368 * distance())) * pow((width1() + width2()) / 2 , 0.42774) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0136843 * exp(-0.645327 * distance()) + 0.0136577 * exp(-0.645368 * distance())) * pow((width1() + width2()) / 2 , 0.42774)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal2 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0136843 * exp(-0.645327 * distance()) + 0.0136577 * exp(-0.645368 * distance())) * pow((width1() + width2()) / 2 , 0.42774) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0136843 * exp(-0.645327 * distance()) + 0.0136577 * exp(-0.645368 * distance())) * pow((width1() + width2()) / 2 , 0.42774)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 poly INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0136843 * exp(-0.645327 * distance()) + 0.0136577 * exp(-0.645368 * distance())) * pow((width1() + width2()) / 2 , 0.42774) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0136843 * exp(-0.645327 * distance()) + 0.0136577 * exp(-0.645368 * distance())) * pow((width1() + width2()) / 2 , 0.42774)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal2,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal2 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0137286 * exp(-0.629064 * distance()) + 0.0137259 * exp(-0.627187 * distance())) * pow((width1() + width2()) / 2 , 0.427913) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0137286 * exp(-0.629064 * distance()) + 0.0137259 * exp(-0.627187 * distance())) * pow((width1() + width2()) / 2 , 0.427913)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 poly INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0137286 * exp(-0.629064 * distance()) + 0.0137259 * exp(-0.627187 * distance())) * pow((width1() + width2()) / 2 , 0.427913) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0137286 * exp(-0.629064 * distance()) + 0.0137259 * exp(-0.627187 * distance())) * pow((width1() + width2()) / 2 , 0.427913)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal2 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0137286 * exp(-0.629064 * distance()) + 0.0137259 * exp(-0.627187 * distance())) * pow((width1() + width2()) / 2 , 0.427913) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0137286 * exp(-0.629064 * distance()) + 0.0137259 * exp(-0.627187 * distance())) * pow((width1() + width2()) / 2 , 0.427913)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 poly INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0137286 * exp(-0.629064 * distance()) + 0.0137259 * exp(-0.627187 * distance())) * pow((width1() + width2()) / 2 , 0.427913) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0137286 * exp(-0.629064 * distance()) + 0.0137259 * exp(-0.627187 * distance())) * pow((width1() + width2()) / 2 , 0.427913)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal3,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal3 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00609248 * exp(-0.594967 * distance()) + 0.00608982 * exp(-0.600568 * distance())) * pow((width1() + width2()) / 2 , 0.490365) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00609248 * exp(-0.594967 * distance()) + 0.00608982 * exp(-0.600568 * distance())) * pow((width1() + width2()) / 2 , 0.490365)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 poly INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00609248 * exp(-0.594967 * distance()) + 0.00608982 * exp(-0.600568 * distance())) * pow((width1() + width2()) / 2 , 0.490365) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00609248 * exp(-0.594967 * distance()) + 0.00608982 * exp(-0.600568 * distance())) * pow((width1() + width2()) / 2 , 0.490365)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal3 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00609248 * exp(-0.594967 * distance()) + 0.00608982 * exp(-0.600568 * distance())) * pow((width1() + width2()) / 2 , 0.490365) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00609248 * exp(-0.594967 * distance()) + 0.00608982 * exp(-0.600568 * distance())) * pow((width1() + width2()) / 2 , 0.490365)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 poly INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00609248 * exp(-0.594967 * distance()) + 0.00608982 * exp(-0.600568 * distance())) * pow((width1() + width2()) / 2 , 0.490365) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00609248 * exp(-0.594967 * distance()) + 0.00608982 * exp(-0.600568 * distance())) * pow((width1() + width2()) / 2 , 0.490365)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal3,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal3 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00688483 * exp(-0.508624 * distance()) + 0.0068855 * exp(-0.507694 * distance())) * pow((width1() + width2()) / 2 , 0.466698) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00688483 * exp(-0.508624 * distance()) + 0.0068855 * exp(-0.507694 * distance())) * pow((width1() + width2()) / 2 , 0.466698)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 poly INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00688483 * exp(-0.508624 * distance()) + 0.0068855 * exp(-0.507694 * distance())) * pow((width1() + width2()) / 2 , 0.466698) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00688483 * exp(-0.508624 * distance()) + 0.0068855 * exp(-0.507694 * distance())) * pow((width1() + width2()) / 2 , 0.466698)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal3 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00688483 * exp(-0.508624 * distance()) + 0.0068855 * exp(-0.507694 * distance())) * pow((width1() + width2()) / 2 , 0.466698) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00688483 * exp(-0.508624 * distance()) + 0.0068855 * exp(-0.507694 * distance())) * pow((width1() + width2()) / 2 , 0.466698)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 poly INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00688483 * exp(-0.508624 * distance()) + 0.0068855 * exp(-0.507694 * distance())) * pow((width1() + width2()) / 2 , 0.466698) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00688483 * exp(-0.508624 * distance()) + 0.0068855 * exp(-0.507694 * distance())) * pow((width1() + width2()) / 2 , 0.466698)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal3,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal3 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00721477 * exp(-0.459917 * distance()) + 0.00721345 * exp(-0.458457 * distance())) * pow((width1() + width2()) / 2 , 0.460763) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00721477 * exp(-0.459917 * distance()) + 0.00721345 * exp(-0.458457 * distance())) * pow((width1() + width2()) / 2 , 0.460763)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 poly INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00721477 * exp(-0.459917 * distance()) + 0.00721345 * exp(-0.458457 * distance())) * pow((width1() + width2()) / 2 , 0.460763) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00721477 * exp(-0.459917 * distance()) + 0.00721345 * exp(-0.458457 * distance())) * pow((width1() + width2()) / 2 , 0.460763)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal3 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00721477 * exp(-0.459917 * distance()) + 0.00721345 * exp(-0.458457 * distance())) * pow((width1() + width2()) / 2 , 0.460763) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00721477 * exp(-0.459917 * distance()) + 0.00721345 * exp(-0.458457 * distance())) * pow((width1() + width2()) / 2 , 0.460763)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 poly INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00721477 * exp(-0.459917 * distance()) + 0.00721345 * exp(-0.458457 * distance())) * pow((width1() + width2()) / 2 , 0.460763) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00721477 * exp(-0.459917 * distance()) + 0.00721345 * exp(-0.458457 * distance())) * pow((width1() + width2()) / 2 , 0.460763)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal3,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal3 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00736912 * exp(-0.43005 * distance()) + 0.00737066 * exp(-0.42823 * distance())) * pow((width1() + width2()) / 2 , 0.457523) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00736912 * exp(-0.43005 * distance()) + 0.00737066 * exp(-0.42823 * distance())) * pow((width1() + width2()) / 2 , 0.457523)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 poly INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00736912 * exp(-0.43005 * distance()) + 0.00737066 * exp(-0.42823 * distance())) * pow((width1() + width2()) / 2 , 0.457523) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00736912 * exp(-0.43005 * distance()) + 0.00737066 * exp(-0.42823 * distance())) * pow((width1() + width2()) / 2 , 0.457523)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal3 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00736912 * exp(-0.43005 * distance()) + 0.00737066 * exp(-0.42823 * distance())) * pow((width1() + width2()) / 2 , 0.457523) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00736912 * exp(-0.43005 * distance()) + 0.00737066 * exp(-0.42823 * distance())) * pow((width1() + width2()) / 2 , 0.457523)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 poly INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00736912 * exp(-0.43005 * distance()) + 0.00737066 * exp(-0.42823 * distance())) * pow((width1() + width2()) / 2 , 0.457523) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00736912 * exp(-0.43005 * distance()) + 0.00737066 * exp(-0.42823 * distance())) * pow((width1() + width2()) / 2 , 0.457523)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal3,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal3 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00748665 * exp(-0.406462 * distance()) + 0.00748804 * exp(-0.406626 * distance())) * pow((width1() + width2()) / 2 , 0.456887) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00748665 * exp(-0.406462 * distance()) + 0.00748804 * exp(-0.406626 * distance())) * pow((width1() + width2()) / 2 , 0.456887)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 poly INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00748665 * exp(-0.406462 * distance()) + 0.00748804 * exp(-0.406626 * distance())) * pow((width1() + width2()) / 2 , 0.456887) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00748665 * exp(-0.406462 * distance()) + 0.00748804 * exp(-0.406626 * distance())) * pow((width1() + width2()) / 2 , 0.456887)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal3 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00748665 * exp(-0.406462 * distance()) + 0.00748804 * exp(-0.406626 * distance())) * pow((width1() + width2()) / 2 , 0.456887) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00748665 * exp(-0.406462 * distance()) + 0.00748804 * exp(-0.406626 * distance())) * pow((width1() + width2()) / 2 , 0.456887)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 poly INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00748665 * exp(-0.406462 * distance()) + 0.00748804 * exp(-0.406626 * distance())) * pow((width1() + width2()) / 2 , 0.456887) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00748665 * exp(-0.406462 * distance()) + 0.00748804 * exp(-0.406626 * distance())) * pow((width1() + width2()) / 2 , 0.456887)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal4,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal4 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00368424 * exp(-0.438836 * distance()) + 0.00368278 * exp(-0.439191 * distance())) * pow((width1() + width2()) / 2 , 0.514802) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00368424 * exp(-0.438836 * distance()) + 0.00368278 * exp(-0.439191 * distance())) * pow((width1() + width2()) / 2 , 0.514802)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 poly INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00368424 * exp(-0.438836 * distance()) + 0.00368278 * exp(-0.439191 * distance())) * pow((width1() + width2()) / 2 , 0.514802) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00368424 * exp(-0.438836 * distance()) + 0.00368278 * exp(-0.439191 * distance())) * pow((width1() + width2()) / 2 , 0.514802)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal4 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00368424 * exp(-0.438836 * distance()) + 0.00368278 * exp(-0.439191 * distance())) * pow((width1() + width2()) / 2 , 0.514802) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00368424 * exp(-0.438836 * distance()) + 0.00368278 * exp(-0.439191 * distance())) * pow((width1() + width2()) / 2 , 0.514802)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 poly INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00368424 * exp(-0.438836 * distance()) + 0.00368278 * exp(-0.439191 * distance())) * pow((width1() + width2()) / 2 , 0.514802) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00368424 * exp(-0.438836 * distance()) + 0.00368278 * exp(-0.439191 * distance())) * pow((width1() + width2()) / 2 , 0.514802)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal4,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal4 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00431013 * exp(-0.369049 * distance()) + 0.00430427 * exp(-0.377813 * distance())) * pow((width1() + width2()) / 2 , 0.482988) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00431013 * exp(-0.369049 * distance()) + 0.00430427 * exp(-0.377813 * distance())) * pow((width1() + width2()) / 2 , 0.482988)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 poly INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00431013 * exp(-0.369049 * distance()) + 0.00430427 * exp(-0.377813 * distance())) * pow((width1() + width2()) / 2 , 0.482988) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00431013 * exp(-0.369049 * distance()) + 0.00430427 * exp(-0.377813 * distance())) * pow((width1() + width2()) / 2 , 0.482988)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal4 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00431013 * exp(-0.369049 * distance()) + 0.00430427 * exp(-0.377813 * distance())) * pow((width1() + width2()) / 2 , 0.482988) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00431013 * exp(-0.369049 * distance()) + 0.00430427 * exp(-0.377813 * distance())) * pow((width1() + width2()) / 2 , 0.482988)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 poly INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00431013 * exp(-0.369049 * distance()) + 0.00430427 * exp(-0.377813 * distance())) * pow((width1() + width2()) / 2 , 0.482988) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00431013 * exp(-0.369049 * distance()) + 0.00430427 * exp(-0.377813 * distance())) * pow((width1() + width2()) / 2 , 0.482988)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal4,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal4 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00458973 * exp(-0.334757 * distance()) + 0.00459988 * exp(-0.334801 * distance())) * pow((width1() + width2()) / 2 , 0.473402) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00458973 * exp(-0.334757 * distance()) + 0.00459988 * exp(-0.334801 * distance())) * pow((width1() + width2()) / 2 , 0.473402)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 poly INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00458973 * exp(-0.334757 * distance()) + 0.00459988 * exp(-0.334801 * distance())) * pow((width1() + width2()) / 2 , 0.473402) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00458973 * exp(-0.334757 * distance()) + 0.00459988 * exp(-0.334801 * distance())) * pow((width1() + width2()) / 2 , 0.473402)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal4 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00458973 * exp(-0.334757 * distance()) + 0.00459988 * exp(-0.334801 * distance())) * pow((width1() + width2()) / 2 , 0.473402) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00458973 * exp(-0.334757 * distance()) + 0.00459988 * exp(-0.334801 * distance())) * pow((width1() + width2()) / 2 , 0.473402)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 poly INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00458973 * exp(-0.334757 * distance()) + 0.00459988 * exp(-0.334801 * distance())) * pow((width1() + width2()) / 2 , 0.473402) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00458973 * exp(-0.334757 * distance()) + 0.00459988 * exp(-0.334801 * distance())) * pow((width1() + width2()) / 2 , 0.473402)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal4,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal4 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00477378 * exp(-0.306301 * distance()) + 0.0047805 * exp(-0.306491 * distance())) * pow((width1() + width2()) / 2 , 0.468902) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00477378 * exp(-0.306301 * distance()) + 0.0047805 * exp(-0.306491 * distance())) * pow((width1() + width2()) / 2 , 0.468902)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 poly INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00477378 * exp(-0.306301 * distance()) + 0.0047805 * exp(-0.306491 * distance())) * pow((width1() + width2()) / 2 , 0.468902) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00477378 * exp(-0.306301 * distance()) + 0.0047805 * exp(-0.306491 * distance())) * pow((width1() + width2()) / 2 , 0.468902)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal4 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00477378 * exp(-0.306301 * distance()) + 0.0047805 * exp(-0.306491 * distance())) * pow((width1() + width2()) / 2 , 0.468902) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00477378 * exp(-0.306301 * distance()) + 0.0047805 * exp(-0.306491 * distance())) * pow((width1() + width2()) / 2 , 0.468902)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 poly INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00477378 * exp(-0.306301 * distance()) + 0.0047805 * exp(-0.306491 * distance())) * pow((width1() + width2()) / 2 , 0.468902) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00477378 * exp(-0.306301 * distance()) + 0.0047805 * exp(-0.306491 * distance())) * pow((width1() + width2()) / 2 , 0.468902)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal5,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal5 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00245161 * exp(-0.334603 * distance()) + 0.00245067 * exp(-0.336025 * distance())) * pow((width1() + width2()) / 2 , 0.526754) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00245161 * exp(-0.334603 * distance()) + 0.00245067 * exp(-0.336025 * distance())) * pow((width1() + width2()) / 2 , 0.526754)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 poly INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00245161 * exp(-0.334603 * distance()) + 0.00245067 * exp(-0.336025 * distance())) * pow((width1() + width2()) / 2 , 0.526754) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00245161 * exp(-0.334603 * distance()) + 0.00245067 * exp(-0.336025 * distance())) * pow((width1() + width2()) / 2 , 0.526754)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal5 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00245161 * exp(-0.334603 * distance()) + 0.00245067 * exp(-0.336025 * distance())) * pow((width1() + width2()) / 2 , 0.526754) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00245161 * exp(-0.334603 * distance()) + 0.00245067 * exp(-0.336025 * distance())) * pow((width1() + width2()) / 2 , 0.526754)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 poly INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00245161 * exp(-0.334603 * distance()) + 0.00245067 * exp(-0.336025 * distance())) * pow((width1() + width2()) / 2 , 0.526754) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00245161 * exp(-0.334603 * distance()) + 0.00245067 * exp(-0.336025 * distance())) * pow((width1() + width2()) / 2 , 0.526754)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal5,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal5 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0029539 * exp(-0.285463 * distance()) + 0.00295264 * exp(-0.285634 * distance())) * pow((width1() + width2()) / 2 , 0.49105) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0029539 * exp(-0.285463 * distance()) + 0.00295264 * exp(-0.285634 * distance())) * pow((width1() + width2()) / 2 , 0.49105)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 poly INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0029539 * exp(-0.285463 * distance()) + 0.00295264 * exp(-0.285634 * distance())) * pow((width1() + width2()) / 2 , 0.49105) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0029539 * exp(-0.285463 * distance()) + 0.00295264 * exp(-0.285634 * distance())) * pow((width1() + width2()) / 2 , 0.49105)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal5 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0029539 * exp(-0.285463 * distance()) + 0.00295264 * exp(-0.285634 * distance())) * pow((width1() + width2()) / 2 , 0.49105) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0029539 * exp(-0.285463 * distance()) + 0.00295264 * exp(-0.285634 * distance())) * pow((width1() + width2()) / 2 , 0.49105)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 poly INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0029539 * exp(-0.285463 * distance()) + 0.00295264 * exp(-0.285634 * distance())) * pow((width1() + width2()) / 2 , 0.49105) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0029539 * exp(-0.285463 * distance()) + 0.00295264 * exp(-0.285634 * distance())) * pow((width1() + width2()) / 2 , 0.49105)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal5,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal5 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00323962 * exp(-0.25036 * distance()) + 0.0032405 * exp(-0.250252 * distance())) * pow((width1() + width2()) / 2 , 0.478528) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00323962 * exp(-0.25036 * distance()) + 0.0032405 * exp(-0.250252 * distance())) * pow((width1() + width2()) / 2 , 0.478528)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 poly INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00323962 * exp(-0.25036 * distance()) + 0.0032405 * exp(-0.250252 * distance())) * pow((width1() + width2()) / 2 , 0.478528) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00323962 * exp(-0.25036 * distance()) + 0.0032405 * exp(-0.250252 * distance())) * pow((width1() + width2()) / 2 , 0.478528)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal5 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00323962 * exp(-0.25036 * distance()) + 0.0032405 * exp(-0.250252 * distance())) * pow((width1() + width2()) / 2 , 0.478528) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00323962 * exp(-0.25036 * distance()) + 0.0032405 * exp(-0.250252 * distance())) * pow((width1() + width2()) / 2 , 0.478528)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 poly INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00323962 * exp(-0.25036 * distance()) + 0.0032405 * exp(-0.250252 * distance())) * pow((width1() + width2()) / 2 , 0.478528) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00323962 * exp(-0.25036 * distance()) + 0.0032405 * exp(-0.250252 * distance())) * pow((width1() + width2()) / 2 , 0.478528)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal6,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal6 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00174298 * exp(-0.265273 * distance()) + 0.00174314 * exp(-0.258054 * distance())) * pow((width1() + width2()) / 2 , 0.534529) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00174298 * exp(-0.265273 * distance()) + 0.00174314 * exp(-0.258054 * distance())) * pow((width1() + width2()) / 2 , 0.534529)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 poly INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00174298 * exp(-0.265273 * distance()) + 0.00174314 * exp(-0.258054 * distance())) * pow((width1() + width2()) / 2 , 0.534529) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00174298 * exp(-0.265273 * distance()) + 0.00174314 * exp(-0.258054 * distance())) * pow((width1() + width2()) / 2 , 0.534529)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal6 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00174298 * exp(-0.265273 * distance()) + 0.00174314 * exp(-0.258054 * distance())) * pow((width1() + width2()) / 2 , 0.534529) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00174298 * exp(-0.265273 * distance()) + 0.00174314 * exp(-0.258054 * distance())) * pow((width1() + width2()) / 2 , 0.534529)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 poly INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00174298 * exp(-0.265273 * distance()) + 0.00174314 * exp(-0.258054 * distance())) * pow((width1() + width2()) / 2 , 0.534529) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00174298 * exp(-0.265273 * distance()) + 0.00174314 * exp(-0.258054 * distance())) * pow((width1() + width2()) / 2 , 0.534529)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal6,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal6 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00220396 * exp(-0.217993 * distance()) + 0.00220393 * exp(-0.217247 * distance())) * pow((width1() + width2()) / 2 , 0.493022) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00220396 * exp(-0.217993 * distance()) + 0.00220393 * exp(-0.217247 * distance())) * pow((width1() + width2()) / 2 , 0.493022)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 poly INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00220396 * exp(-0.217993 * distance()) + 0.00220393 * exp(-0.217247 * distance())) * pow((width1() + width2()) / 2 , 0.493022) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00220396 * exp(-0.217993 * distance()) + 0.00220393 * exp(-0.217247 * distance())) * pow((width1() + width2()) / 2 , 0.493022)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal6 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00220396 * exp(-0.217993 * distance()) + 0.00220393 * exp(-0.217247 * distance())) * pow((width1() + width2()) / 2 , 0.493022) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00220396 * exp(-0.217993 * distance()) + 0.00220393 * exp(-0.217247 * distance())) * pow((width1() + width2()) / 2 , 0.493022)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 poly INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00220396 * exp(-0.217993 * distance()) + 0.00220393 * exp(-0.217247 * distance())) * pow((width1() + width2()) / 2 , 0.493022) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00220396 * exp(-0.217993 * distance()) + 0.00220393 * exp(-0.217247 * distance())) * pow((width1() + width2()) / 2 , 0.493022)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,poly,metal7,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal7 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00139273 * exp(-0.200054 * distance()) + 0.00139273 * exp(-0.199703 * distance())) * pow((width1() + width2()) / 2 , 0.527146) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00139273 * exp(-0.200054 * distance()) + 0.00139273 * exp(-0.199703 * distance())) * pow((width1() + width2()) / 2 , 0.527146)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 poly INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00139273 * exp(-0.200054 * distance()) + 0.00139273 * exp(-0.199703 * distance())) * pow((width1() + width2()) / 2 , 0.527146) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00139273 * exp(-0.200054 * distance()) + 0.00139273 * exp(-0.199703 * distance())) * pow((width1() + width2()) / 2 , 0.527146)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY poly metal7 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00139273 * exp(-0.200054 * distance()) + 0.00139273 * exp(-0.199703 * distance())) * pow((width1() + width2()) / 2 , 0.527146) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00139273 * exp(-0.200054 * distance()) + 0.00139273 * exp(-0.199703 * distance())) * pow((width1() + width2()) / 2 , 0.527146)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 poly INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.44 // For reference
    max_caldistance = 5 // For reference
    min_calwidth = 0.11 // For reference
    max_width = MAXWIDTH_poly
    max_distance = (MAXDISTANCE_poly + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00139273 * exp(-0.200054 * distance()) + 0.00139273 * exp(-0.199703 * distance())) * pow((width1() + width2()) / 2 , 0.527146) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00139273 * exp(-0.200054 * distance()) + 0.00139273 * exp(-0.199703 * distance())) * pow((width1() + width2()) / 2 , 0.527146)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal2,metal3,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF tndiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.028106 * exp(-1.21 * distance()) + 0.028106 * exp(-1.20995 * distance())) * pow((width1() + width2()) / 2 , 0.215603) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.028106 * exp(-1.21 * distance()) + 0.028106 * exp(-1.20995 * distance())) * pow((width1() + width2()) / 2 , 0.215603)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF tndiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.028106 * exp(-1.21 * distance()) + 0.028106 * exp(-1.20995 * distance())) * pow((width1() + width2()) / 2 , 0.215603) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.028106 * exp(-1.21 * distance()) + 0.028106 * exp(-1.20995 * distance())) * pow((width1() + width2()) / 2 , 0.215603)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF tpdiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.028106 * exp(-1.21 * distance()) + 0.028106 * exp(-1.20995 * distance())) * pow((width1() + width2()) / 2 , 0.215603) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.028106 * exp(-1.21 * distance()) + 0.028106 * exp(-1.20995 * distance())) * pow((width1() + width2()) / 2 , 0.215603)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF tpdiff metal3 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.028106 * exp(-1.21 * distance()) + 0.028106 * exp(-1.20995 * distance())) * pow((width1() + width2()) / 2 , 0.215603) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.028106 * exp(-1.21 * distance()) + 0.028106 * exp(-1.20995 * distance())) * pow((width1() + width2()) / 2 , 0.215603)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal2,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0304869 * exp(-1.01569 * distance()) + 0.0304869 * exp(-1.01652 * distance())) * pow((width1() + width2()) / 2 , 0.223471) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0304869 * exp(-1.01569 * distance()) + 0.0304869 * exp(-1.01652 * distance())) * pow((width1() + width2()) / 2 , 0.223471)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0304869 * exp(-1.01569 * distance()) + 0.0304869 * exp(-1.01652 * distance())) * pow((width1() + width2()) / 2 , 0.223471) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0304869 * exp(-1.01569 * distance()) + 0.0304869 * exp(-1.01652 * distance())) * pow((width1() + width2()) / 2 , 0.223471)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0304869 * exp(-1.01569 * distance()) + 0.0304869 * exp(-1.01652 * distance())) * pow((width1() + width2()) / 2 , 0.223471) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0304869 * exp(-1.01569 * distance()) + 0.0304869 * exp(-1.01652 * distance())) * pow((width1() + width2()) / 2 , 0.223471)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0304869 * exp(-1.01569 * distance()) + 0.0304869 * exp(-1.01652 * distance())) * pow((width1() + width2()) / 2 , 0.223471) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0304869 * exp(-1.01569 * distance()) + 0.0304869 * exp(-1.01652 * distance())) * pow((width1() + width2()) / 2 , 0.223471)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal2,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0314447 * exp(-0.975178 * distance()) + 0.031314 * exp(-0.888151 * distance())) * pow((width1() + width2()) / 2 , 0.230273) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0314447 * exp(-0.975178 * distance()) + 0.031314 * exp(-0.888151 * distance())) * pow((width1() + width2()) / 2 , 0.230273)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0314447 * exp(-0.975178 * distance()) + 0.031314 * exp(-0.888151 * distance())) * pow((width1() + width2()) / 2 , 0.230273) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0314447 * exp(-0.975178 * distance()) + 0.031314 * exp(-0.888151 * distance())) * pow((width1() + width2()) / 2 , 0.230273)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0314447 * exp(-0.975178 * distance()) + 0.031314 * exp(-0.888151 * distance())) * pow((width1() + width2()) / 2 , 0.230273) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0314447 * exp(-0.975178 * distance()) + 0.031314 * exp(-0.888151 * distance())) * pow((width1() + width2()) / 2 , 0.230273)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0314447 * exp(-0.975178 * distance()) + 0.031314 * exp(-0.888151 * distance())) * pow((width1() + width2()) / 2 , 0.230273) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0314447 * exp(-0.975178 * distance()) + 0.031314 * exp(-0.888151 * distance())) * pow((width1() + width2()) / 2 , 0.230273)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal2,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00102639 * exp(-0.0272902 * distance()) + 0.0627305 * exp(-0.918428 * distance())) * pow((width1() + width2()) / 2 , 0.23447) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00102639 * exp(-0.0272902 * distance()) + 0.0627305 * exp(-0.918428 * distance())) * pow((width1() + width2()) / 2 , 0.23447)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00102639 * exp(-0.0272902 * distance()) + 0.0627305 * exp(-0.918428 * distance())) * pow((width1() + width2()) / 2 , 0.23447) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00102639 * exp(-0.0272902 * distance()) + 0.0627305 * exp(-0.918428 * distance())) * pow((width1() + width2()) / 2 , 0.23447)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00102639 * exp(-0.0272902 * distance()) + 0.0627305 * exp(-0.918428 * distance())) * pow((width1() + width2()) / 2 , 0.23447) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00102639 * exp(-0.0272902 * distance()) + 0.0627305 * exp(-0.918428 * distance())) * pow((width1() + width2()) / 2 , 0.23447)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00102639 * exp(-0.0272902 * distance()) + 0.0627305 * exp(-0.918428 * distance())) * pow((width1() + width2()) / 2 , 0.23447) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00102639 * exp(-0.0272902 * distance()) + 0.0627305 * exp(-0.918428 * distance())) * pow((width1() + width2()) / 2 , 0.23447)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal2,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0618075 * exp(-0.922339 * distance()) + 0.00252818 * exp(-0.117234 * distance())) * pow((width1() + width2()) / 2 , 0.237095) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0618075 * exp(-0.922339 * distance()) + 0.00252818 * exp(-0.117234 * distance())) * pow((width1() + width2()) / 2 , 0.237095)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0618075 * exp(-0.922339 * distance()) + 0.00252818 * exp(-0.117234 * distance())) * pow((width1() + width2()) / 2 , 0.237095) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0618075 * exp(-0.922339 * distance()) + 0.00252818 * exp(-0.117234 * distance())) * pow((width1() + width2()) / 2 , 0.237095)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0618075 * exp(-0.922339 * distance()) + 0.00252818 * exp(-0.117234 * distance())) * pow((width1() + width2()) / 2 , 0.237095) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0618075 * exp(-0.922339 * distance()) + 0.00252818 * exp(-0.117234 * distance())) * pow((width1() + width2()) / 2 , 0.237095)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0618075 * exp(-0.922339 * distance()) + 0.00252818 * exp(-0.117234 * distance())) * pow((width1() + width2()) / 2 , 0.237095) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0618075 * exp(-0.922339 * distance()) + 0.00252818 * exp(-0.117234 * distance())) * pow((width1() + width2()) / 2 , 0.237095)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal2,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00376943 * exp(-0.138531 * distance()) + 0.0609862 * exp(-0.928054 * distance())) * pow((width1() + width2()) / 2 , 0.239083) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00376943 * exp(-0.138531 * distance()) + 0.0609862 * exp(-0.928054 * distance())) * pow((width1() + width2()) / 2 , 0.239083)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00376943 * exp(-0.138531 * distance()) + 0.0609862 * exp(-0.928054 * distance())) * pow((width1() + width2()) / 2 , 0.239083) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00376943 * exp(-0.138531 * distance()) + 0.0609862 * exp(-0.928054 * distance())) * pow((width1() + width2()) / 2 , 0.239083)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal2 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00376943 * exp(-0.138531 * distance()) + 0.0609862 * exp(-0.928054 * distance())) * pow((width1() + width2()) / 2 , 0.239083) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00376943 * exp(-0.138531 * distance()) + 0.0609862 * exp(-0.928054 * distance())) * pow((width1() + width2()) / 2 , 0.239083)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal1 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal2) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00376943 * exp(-0.138531 * distance()) + 0.0609862 * exp(-0.928054 * distance())) * pow((width1() + width2()) / 2 , 0.239083) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00376943 * exp(-0.138531 * distance()) + 0.0609862 * exp(-0.928054 * distance())) * pow((width1() + width2()) / 2 , 0.239083)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal3,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0133392 * exp(-0.685797 * distance()) + 0.0133392 * exp(-0.682435 * distance())) * pow((width1() + width2()) / 2 , 0.346324) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0133392 * exp(-0.685797 * distance()) + 0.0133392 * exp(-0.682435 * distance())) * pow((width1() + width2()) / 2 , 0.346324)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0133392 * exp(-0.685797 * distance()) + 0.0133392 * exp(-0.682435 * distance())) * pow((width1() + width2()) / 2 , 0.346324) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0133392 * exp(-0.685797 * distance()) + 0.0133392 * exp(-0.682435 * distance())) * pow((width1() + width2()) / 2 , 0.346324)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0133392 * exp(-0.685797 * distance()) + 0.0133392 * exp(-0.682435 * distance())) * pow((width1() + width2()) / 2 , 0.346324) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0133392 * exp(-0.685797 * distance()) + 0.0133392 * exp(-0.682435 * distance())) * pow((width1() + width2()) / 2 , 0.346324)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0133392 * exp(-0.685797 * distance()) + 0.0133392 * exp(-0.682435 * distance())) * pow((width1() + width2()) / 2 , 0.346324) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0133392 * exp(-0.685797 * distance()) + 0.0133392 * exp(-0.682435 * distance())) * pow((width1() + width2()) / 2 , 0.346324)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal3,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.014983 * exp(-0.578701 * distance()) + 0.0149829 * exp(-0.577964 * distance())) * pow((width1() + width2()) / 2 , 0.327064) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.014983 * exp(-0.578701 * distance()) + 0.0149829 * exp(-0.577964 * distance())) * pow((width1() + width2()) / 2 , 0.327064)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.014983 * exp(-0.578701 * distance()) + 0.0149829 * exp(-0.577964 * distance())) * pow((width1() + width2()) / 2 , 0.327064) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.014983 * exp(-0.578701 * distance()) + 0.0149829 * exp(-0.577964 * distance())) * pow((width1() + width2()) / 2 , 0.327064)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.014983 * exp(-0.578701 * distance()) + 0.0149829 * exp(-0.577964 * distance())) * pow((width1() + width2()) / 2 , 0.327064) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.014983 * exp(-0.578701 * distance()) + 0.0149829 * exp(-0.577964 * distance())) * pow((width1() + width2()) / 2 , 0.327064)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.014983 * exp(-0.578701 * distance()) + 0.0149829 * exp(-0.577964 * distance())) * pow((width1() + width2()) / 2 , 0.327064) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.014983 * exp(-0.578701 * distance()) + 0.0149829 * exp(-0.577964 * distance())) * pow((width1() + width2()) / 2 , 0.327064)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal3,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0156894 * exp(-0.521756 * distance()) + 0.0156893 * exp(-0.521734 * distance())) * pow((width1() + width2()) / 2 , 0.324652) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0156894 * exp(-0.521756 * distance()) + 0.0156893 * exp(-0.521734 * distance())) * pow((width1() + width2()) / 2 , 0.324652)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0156894 * exp(-0.521756 * distance()) + 0.0156893 * exp(-0.521734 * distance())) * pow((width1() + width2()) / 2 , 0.324652) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0156894 * exp(-0.521756 * distance()) + 0.0156893 * exp(-0.521734 * distance())) * pow((width1() + width2()) / 2 , 0.324652)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0156894 * exp(-0.521756 * distance()) + 0.0156893 * exp(-0.521734 * distance())) * pow((width1() + width2()) / 2 , 0.324652) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0156894 * exp(-0.521756 * distance()) + 0.0156893 * exp(-0.521734 * distance())) * pow((width1() + width2()) / 2 , 0.324652)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0156894 * exp(-0.521756 * distance()) + 0.0156893 * exp(-0.521734 * distance())) * pow((width1() + width2()) / 2 , 0.324652) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0156894 * exp(-0.521756 * distance()) + 0.0156893 * exp(-0.521734 * distance())) * pow((width1() + width2()) / 2 , 0.324652)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal3,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0160545 * exp(-0.487545 * distance()) + 0.0160545 * exp(-0.486914 * distance())) * pow((width1() + width2()) / 2 , 0.324625) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0160545 * exp(-0.487545 * distance()) + 0.0160545 * exp(-0.486914 * distance())) * pow((width1() + width2()) / 2 , 0.324625)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0160545 * exp(-0.487545 * distance()) + 0.0160545 * exp(-0.486914 * distance())) * pow((width1() + width2()) / 2 , 0.324625) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0160545 * exp(-0.487545 * distance()) + 0.0160545 * exp(-0.486914 * distance())) * pow((width1() + width2()) / 2 , 0.324625)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0160545 * exp(-0.487545 * distance()) + 0.0160545 * exp(-0.486914 * distance())) * pow((width1() + width2()) / 2 , 0.324625) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0160545 * exp(-0.487545 * distance()) + 0.0160545 * exp(-0.486914 * distance())) * pow((width1() + width2()) / 2 , 0.324625)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0160545 * exp(-0.487545 * distance()) + 0.0160545 * exp(-0.486914 * distance())) * pow((width1() + width2()) / 2 , 0.324625) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0160545 * exp(-0.487545 * distance()) + 0.0160545 * exp(-0.486914 * distance())) * pow((width1() + width2()) / 2 , 0.324625)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal3,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0162961 * exp(-0.461253 * distance()) + 0.0162961 * exp(-0.461084 * distance())) * pow((width1() + width2()) / 2 , 0.325096) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0162961 * exp(-0.461253 * distance()) + 0.0162961 * exp(-0.461084 * distance())) * pow((width1() + width2()) / 2 , 0.325096)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0162961 * exp(-0.461253 * distance()) + 0.0162961 * exp(-0.461084 * distance())) * pow((width1() + width2()) / 2 , 0.325096) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0162961 * exp(-0.461253 * distance()) + 0.0162961 * exp(-0.461084 * distance())) * pow((width1() + width2()) / 2 , 0.325096)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal3 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0162961 * exp(-0.461253 * distance()) + 0.0162961 * exp(-0.461084 * distance())) * pow((width1() + width2()) / 2 , 0.325096) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0162961 * exp(-0.461253 * distance()) + 0.0162961 * exp(-0.461084 * distance())) * pow((width1() + width2()) / 2 , 0.325096)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal1 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0162961 * exp(-0.461253 * distance()) + 0.0162961 * exp(-0.461084 * distance())) * pow((width1() + width2()) / 2 , 0.325096) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0162961 * exp(-0.461253 * distance()) + 0.0162961 * exp(-0.461084 * distance())) * pow((width1() + width2()) / 2 , 0.325096)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal4,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00811106 * exp(-0.480984 * distance()) + 0.00811183 * exp(-0.493699 * distance())) * pow((width1() + width2()) / 2 , 0.408699) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00811106 * exp(-0.480984 * distance()) + 0.00811183 * exp(-0.493699 * distance())) * pow((width1() + width2()) / 2 , 0.408699)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00811106 * exp(-0.480984 * distance()) + 0.00811183 * exp(-0.493699 * distance())) * pow((width1() + width2()) / 2 , 0.408699) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00811106 * exp(-0.480984 * distance()) + 0.00811183 * exp(-0.493699 * distance())) * pow((width1() + width2()) / 2 , 0.408699)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00811106 * exp(-0.480984 * distance()) + 0.00811183 * exp(-0.493699 * distance())) * pow((width1() + width2()) / 2 , 0.408699) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00811106 * exp(-0.480984 * distance()) + 0.00811183 * exp(-0.493699 * distance())) * pow((width1() + width2()) / 2 , 0.408699)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00811106 * exp(-0.480984 * distance()) + 0.00811183 * exp(-0.493699 * distance())) * pow((width1() + width2()) / 2 , 0.408699) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00811106 * exp(-0.480984 * distance()) + 0.00811183 * exp(-0.493699 * distance())) * pow((width1() + width2()) / 2 , 0.408699)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal4,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00936451 * exp(-0.413779 * distance()) + 0.00936463 * exp(-0.414476 * distance())) * pow((width1() + width2()) / 2 , 0.372296) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00936451 * exp(-0.413779 * distance()) + 0.00936463 * exp(-0.414476 * distance())) * pow((width1() + width2()) / 2 , 0.372296)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00936451 * exp(-0.413779 * distance()) + 0.00936463 * exp(-0.414476 * distance())) * pow((width1() + width2()) / 2 , 0.372296) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00936451 * exp(-0.413779 * distance()) + 0.00936463 * exp(-0.414476 * distance())) * pow((width1() + width2()) / 2 , 0.372296)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00936451 * exp(-0.413779 * distance()) + 0.00936463 * exp(-0.414476 * distance())) * pow((width1() + width2()) / 2 , 0.372296) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00936451 * exp(-0.413779 * distance()) + 0.00936463 * exp(-0.414476 * distance())) * pow((width1() + width2()) / 2 , 0.372296)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00936451 * exp(-0.413779 * distance()) + 0.00936463 * exp(-0.414476 * distance())) * pow((width1() + width2()) / 2 , 0.372296) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00936451 * exp(-0.413779 * distance()) + 0.00936463 * exp(-0.414476 * distance())) * pow((width1() + width2()) / 2 , 0.372296)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal4,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00996065 * exp(-0.371117 * distance()) + 0.00996058 * exp(-0.371222 * distance())) * pow((width1() + width2()) / 2 , 0.363012) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00996065 * exp(-0.371117 * distance()) + 0.00996058 * exp(-0.371222 * distance())) * pow((width1() + width2()) / 2 , 0.363012)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00996065 * exp(-0.371117 * distance()) + 0.00996058 * exp(-0.371222 * distance())) * pow((width1() + width2()) / 2 , 0.363012) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00996065 * exp(-0.371117 * distance()) + 0.00996058 * exp(-0.371222 * distance())) * pow((width1() + width2()) / 2 , 0.363012)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00996065 * exp(-0.371117 * distance()) + 0.00996058 * exp(-0.371222 * distance())) * pow((width1() + width2()) / 2 , 0.363012) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00996065 * exp(-0.371117 * distance()) + 0.00996058 * exp(-0.371222 * distance())) * pow((width1() + width2()) / 2 , 0.363012)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00996065 * exp(-0.371117 * distance()) + 0.00996058 * exp(-0.371222 * distance())) * pow((width1() + width2()) / 2 , 0.363012) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00996065 * exp(-0.371117 * distance()) + 0.00996058 * exp(-0.371222 * distance())) * pow((width1() + width2()) / 2 , 0.363012)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal4,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0103346 * exp(-0.341381 * distance()) + 0.0103349 * exp(-0.337804 * distance())) * pow((width1() + width2()) / 2 , 0.358712) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0103346 * exp(-0.341381 * distance()) + 0.0103349 * exp(-0.337804 * distance())) * pow((width1() + width2()) / 2 , 0.358712)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0103346 * exp(-0.341381 * distance()) + 0.0103349 * exp(-0.337804 * distance())) * pow((width1() + width2()) / 2 , 0.358712) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0103346 * exp(-0.341381 * distance()) + 0.0103349 * exp(-0.337804 * distance())) * pow((width1() + width2()) / 2 , 0.358712)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal4 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0103346 * exp(-0.341381 * distance()) + 0.0103349 * exp(-0.337804 * distance())) * pow((width1() + width2()) / 2 , 0.358712) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0103346 * exp(-0.341381 * distance()) + 0.0103349 * exp(-0.337804 * distance())) * pow((width1() + width2()) / 2 , 0.358712)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal1 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0103346 * exp(-0.341381 * distance()) + 0.0103349 * exp(-0.337804 * distance())) * pow((width1() + width2()) / 2 , 0.358712) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0103346 * exp(-0.341381 * distance()) + 0.0103349 * exp(-0.337804 * distance())) * pow((width1() + width2()) / 2 , 0.358712)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal5,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00545271 * exp(-0.371193 * distance()) + 0.00545296 * exp(-0.367434 * distance())) * pow((width1() + width2()) / 2 , 0.442094) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00545271 * exp(-0.371193 * distance()) + 0.00545296 * exp(-0.367434 * distance())) * pow((width1() + width2()) / 2 , 0.442094)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00545271 * exp(-0.371193 * distance()) + 0.00545296 * exp(-0.367434 * distance())) * pow((width1() + width2()) / 2 , 0.442094) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00545271 * exp(-0.371193 * distance()) + 0.00545296 * exp(-0.367434 * distance())) * pow((width1() + width2()) / 2 , 0.442094)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00545271 * exp(-0.371193 * distance()) + 0.00545296 * exp(-0.367434 * distance())) * pow((width1() + width2()) / 2 , 0.442094) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00545271 * exp(-0.371193 * distance()) + 0.00545296 * exp(-0.367434 * distance())) * pow((width1() + width2()) / 2 , 0.442094)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00545271 * exp(-0.371193 * distance()) + 0.00545296 * exp(-0.367434 * distance())) * pow((width1() + width2()) / 2 , 0.442094) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00545271 * exp(-0.371193 * distance()) + 0.00545296 * exp(-0.367434 * distance())) * pow((width1() + width2()) / 2 , 0.442094)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal5,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00645849 * exp(-0.314951 * distance()) + 0.0064585 * exp(-0.314416 * distance())) * pow((width1() + width2()) / 2 , 0.396929) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00645849 * exp(-0.314951 * distance()) + 0.0064585 * exp(-0.314416 * distance())) * pow((width1() + width2()) / 2 , 0.396929)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00645849 * exp(-0.314951 * distance()) + 0.0064585 * exp(-0.314416 * distance())) * pow((width1() + width2()) / 2 , 0.396929) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00645849 * exp(-0.314951 * distance()) + 0.0064585 * exp(-0.314416 * distance())) * pow((width1() + width2()) / 2 , 0.396929)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00645849 * exp(-0.314951 * distance()) + 0.0064585 * exp(-0.314416 * distance())) * pow((width1() + width2()) / 2 , 0.396929) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00645849 * exp(-0.314951 * distance()) + 0.0064585 * exp(-0.314416 * distance())) * pow((width1() + width2()) / 2 , 0.396929)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00645849 * exp(-0.314951 * distance()) + 0.0064585 * exp(-0.314416 * distance())) * pow((width1() + width2()) / 2 , 0.396929) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00645849 * exp(-0.314951 * distance()) + 0.0064585 * exp(-0.314416 * distance())) * pow((width1() + width2()) / 2 , 0.396929)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal5,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00704109 * exp(-0.275965 * distance()) + 0.007041 * exp(-0.276131 * distance())) * pow((width1() + width2()) / 2 , 0.381712) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00704109 * exp(-0.275965 * distance()) + 0.007041 * exp(-0.276131 * distance())) * pow((width1() + width2()) / 2 , 0.381712)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00704109 * exp(-0.275965 * distance()) + 0.007041 * exp(-0.276131 * distance())) * pow((width1() + width2()) / 2 , 0.381712) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00704109 * exp(-0.275965 * distance()) + 0.007041 * exp(-0.276131 * distance())) * pow((width1() + width2()) / 2 , 0.381712)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal5 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00704109 * exp(-0.275965 * distance()) + 0.007041 * exp(-0.276131 * distance())) * pow((width1() + width2()) / 2 , 0.381712) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00704109 * exp(-0.275965 * distance()) + 0.007041 * exp(-0.276131 * distance())) * pow((width1() + width2()) / 2 , 0.381712)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal1 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00704109 * exp(-0.275965 * distance()) + 0.007041 * exp(-0.276131 * distance())) * pow((width1() + width2()) / 2 , 0.381712) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00704109 * exp(-0.275965 * distance()) + 0.007041 * exp(-0.276131 * distance())) * pow((width1() + width2()) / 2 , 0.381712)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal6,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal6 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00391142 * exp(-0.287805 * distance()) + 0.00391135 * exp(-0.288051 * distance())) * pow((width1() + width2()) / 2 , 0.463978) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00391142 * exp(-0.287805 * distance()) + 0.00391135 * exp(-0.288051 * distance())) * pow((width1() + width2()) / 2 , 0.463978)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal1 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00391142 * exp(-0.287805 * distance()) + 0.00391135 * exp(-0.288051 * distance())) * pow((width1() + width2()) / 2 , 0.463978) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00391142 * exp(-0.287805 * distance()) + 0.00391135 * exp(-0.288051 * distance())) * pow((width1() + width2()) / 2 , 0.463978)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal6 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00391142 * exp(-0.287805 * distance()) + 0.00391135 * exp(-0.288051 * distance())) * pow((width1() + width2()) / 2 , 0.463978) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00391142 * exp(-0.287805 * distance()) + 0.00391135 * exp(-0.288051 * distance())) * pow((width1() + width2()) / 2 , 0.463978)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal1 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00391142 * exp(-0.287805 * distance()) + 0.00391135 * exp(-0.288051 * distance())) * pow((width1() + width2()) / 2 , 0.463978) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00391142 * exp(-0.287805 * distance()) + 0.00391135 * exp(-0.288051 * distance())) * pow((width1() + width2()) / 2 , 0.463978)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal6,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal6 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00483568 * exp(-0.239866 * distance()) + 0.0048358 * exp(-0.239827 * distance())) * pow((width1() + width2()) / 2 , 0.408051) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00483568 * exp(-0.239866 * distance()) + 0.0048358 * exp(-0.239827 * distance())) * pow((width1() + width2()) / 2 , 0.408051)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal1 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00483568 * exp(-0.239866 * distance()) + 0.0048358 * exp(-0.239827 * distance())) * pow((width1() + width2()) / 2 , 0.408051) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00483568 * exp(-0.239866 * distance()) + 0.0048358 * exp(-0.239827 * distance())) * pow((width1() + width2()) / 2 , 0.408051)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal6 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00483568 * exp(-0.239866 * distance()) + 0.0048358 * exp(-0.239827 * distance())) * pow((width1() + width2()) / 2 , 0.408051) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00483568 * exp(-0.239866 * distance()) + 0.0048358 * exp(-0.239827 * distance())) * pow((width1() + width2()) / 2 , 0.408051)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal1 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00483568 * exp(-0.239866 * distance()) + 0.0048358 * exp(-0.239827 * distance())) * pow((width1() + width2()) / 2 , 0.408051) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00483568 * exp(-0.239866 * distance()) + 0.0048358 * exp(-0.239827 * distance())) * pow((width1() + width2()) / 2 , 0.408051)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal1,metal7,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal7 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.003118 * exp(-0.220424 * distance()) + 0.00311707 * exp(-0.220225 * distance())) * pow((width1() + width2()) / 2 , 0.459724) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.003118 * exp(-0.220424 * distance()) + 0.00311707 * exp(-0.220225 * distance())) * pow((width1() + width2()) / 2 , 0.459724)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal1 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.003118 * exp(-0.220424 * distance()) + 0.00311707 * exp(-0.220225 * distance())) * pow((width1() + width2()) / 2 , 0.459724) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.003118 * exp(-0.220424 * distance()) + 0.00311707 * exp(-0.220225 * distance())) * pow((width1() + width2()) / 2 , 0.459724)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal1 metal7 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.003118 * exp(-0.220424 * distance()) + 0.00311707 * exp(-0.220225 * distance())) * pow((width1() + width2()) / 2 , 0.459724) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.003118 * exp(-0.220424 * distance()) + 0.00311707 * exp(-0.220225 * distance())) * pow((width1() + width2()) / 2 , 0.459724)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal1 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 0.96 // For reference
    max_caldistance = 8.165 // For reference
    min_calwidth = 0.175 // For reference
    max_width = MAXWIDTH_metal1
    max_distance = (MAXDISTANCE_metal1 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.003118 * exp(-0.220424 * distance()) + 0.00311707 * exp(-0.220225 * distance())) * pow((width1() + width2()) / 2 , 0.459724) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.003118 * exp(-0.220424 * distance()) + 0.00311707 * exp(-0.220225 * distance())) * pow((width1() + width2()) / 2 , 0.459724)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal3,metal4,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0307967 * exp(-1.0235 * distance()) + 0.0307968 * exp(-1.02389 * distance())) * pow((width1() + width2()) / 2 , 0.203131) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0307967 * exp(-1.0235 * distance()) + 0.0307968 * exp(-1.02389 * distance())) * pow((width1() + width2()) / 2 , 0.203131)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF tndiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0307967 * exp(-1.0235 * distance()) + 0.0307968 * exp(-1.02389 * distance())) * pow((width1() + width2()) / 2 , 0.203131) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0307967 * exp(-1.0235 * distance()) + 0.0307968 * exp(-1.02389 * distance())) * pow((width1() + width2()) / 2 , 0.203131)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0307967 * exp(-1.0235 * distance()) + 0.0307968 * exp(-1.02389 * distance())) * pow((width1() + width2()) / 2 , 0.203131) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0307967 * exp(-1.0235 * distance()) + 0.0307968 * exp(-1.02389 * distance())) * pow((width1() + width2()) / 2 , 0.203131)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF tpdiff metal4 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0307967 * exp(-1.0235 * distance()) + 0.0307968 * exp(-1.02389 * distance())) * pow((width1() + width2()) / 2 , 0.203131) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0307967 * exp(-1.0235 * distance()) + 0.0307968 * exp(-1.02389 * distance())) * pow((width1() + width2()) / 2 , 0.203131)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal3,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.033669 * exp(-1.00646 * distance()) + 0.0335806 * exp(-0.731215 * distance())) * pow((width1() + width2()) / 2 , 0.202208) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.033669 * exp(-1.00646 * distance()) + 0.0335806 * exp(-0.731215 * distance())) * pow((width1() + width2()) / 2 , 0.202208)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.033669 * exp(-1.00646 * distance()) + 0.0335806 * exp(-0.731215 * distance())) * pow((width1() + width2()) / 2 , 0.202208) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.033669 * exp(-1.00646 * distance()) + 0.0335806 * exp(-0.731215 * distance())) * pow((width1() + width2()) / 2 , 0.202208)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.033669 * exp(-1.00646 * distance()) + 0.0335806 * exp(-0.731215 * distance())) * pow((width1() + width2()) / 2 , 0.202208) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.033669 * exp(-1.00646 * distance()) + 0.0335806 * exp(-0.731215 * distance())) * pow((width1() + width2()) / 2 , 0.202208)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.033669 * exp(-1.00646 * distance()) + 0.0335806 * exp(-0.731215 * distance())) * pow((width1() + width2()) / 2 , 0.202208) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.033669 * exp(-1.00646 * distance()) + 0.0335806 * exp(-0.731215 * distance())) * pow((width1() + width2()) / 2 , 0.202208)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal3,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.057298 * exp(-0.909728 * distance()) + 0.0125006 * exp(-0.394695 * distance())) * pow((width1() + width2()) / 2 , 0.206896) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.057298 * exp(-0.909728 * distance()) + 0.0125006 * exp(-0.394695 * distance())) * pow((width1() + width2()) / 2 , 0.206896)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.057298 * exp(-0.909728 * distance()) + 0.0125006 * exp(-0.394695 * distance())) * pow((width1() + width2()) / 2 , 0.206896) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.057298 * exp(-0.909728 * distance()) + 0.0125006 * exp(-0.394695 * distance())) * pow((width1() + width2()) / 2 , 0.206896)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.057298 * exp(-0.909728 * distance()) + 0.0125006 * exp(-0.394695 * distance())) * pow((width1() + width2()) / 2 , 0.206896) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.057298 * exp(-0.909728 * distance()) + 0.0125006 * exp(-0.394695 * distance())) * pow((width1() + width2()) / 2 , 0.206896)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.057298 * exp(-0.909728 * distance()) + 0.0125006 * exp(-0.394695 * distance())) * pow((width1() + width2()) / 2 , 0.206896) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.057298 * exp(-0.909728 * distance()) + 0.0125006 * exp(-0.394695 * distance())) * pow((width1() + width2()) / 2 , 0.206896)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal3,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.052373 * exp(-0.947981 * distance()) + 0.0188309 * exp(-0.377355 * distance())) * pow((width1() + width2()) / 2 , 0.210518) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.052373 * exp(-0.947981 * distance()) + 0.0188309 * exp(-0.377355 * distance())) * pow((width1() + width2()) / 2 , 0.210518)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.052373 * exp(-0.947981 * distance()) + 0.0188309 * exp(-0.377355 * distance())) * pow((width1() + width2()) / 2 , 0.210518) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.052373 * exp(-0.947981 * distance()) + 0.0188309 * exp(-0.377355 * distance())) * pow((width1() + width2()) / 2 , 0.210518)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.052373 * exp(-0.947981 * distance()) + 0.0188309 * exp(-0.377355 * distance())) * pow((width1() + width2()) / 2 , 0.210518) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.052373 * exp(-0.947981 * distance()) + 0.0188309 * exp(-0.377355 * distance())) * pow((width1() + width2()) / 2 , 0.210518)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.052373 * exp(-0.947981 * distance()) + 0.0188309 * exp(-0.377355 * distance())) * pow((width1() + width2()) / 2 , 0.210518) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.052373 * exp(-0.947981 * distance()) + 0.0188309 * exp(-0.377355 * distance())) * pow((width1() + width2()) / 2 , 0.210518)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal3,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0517771 * exp(-0.960584 * distance()) + 0.0204009 * exp(-0.334732 * distance())) * pow((width1() + width2()) / 2 , 0.213485) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0517771 * exp(-0.960584 * distance()) + 0.0204009 * exp(-0.334732 * distance())) * pow((width1() + width2()) / 2 , 0.213485)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0517771 * exp(-0.960584 * distance()) + 0.0204009 * exp(-0.334732 * distance())) * pow((width1() + width2()) / 2 , 0.213485) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0517771 * exp(-0.960584 * distance()) + 0.0204009 * exp(-0.334732 * distance())) * pow((width1() + width2()) / 2 , 0.213485)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal3 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0517771 * exp(-0.960584 * distance()) + 0.0204009 * exp(-0.334732 * distance())) * pow((width1() + width2()) / 2 , 0.213485) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0517771 * exp(-0.960584 * distance()) + 0.0204009 * exp(-0.334732 * distance())) * pow((width1() + width2()) / 2 , 0.213485)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal2 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal3) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0517771 * exp(-0.960584 * distance()) + 0.0204009 * exp(-0.334732 * distance())) * pow((width1() + width2()) / 2 , 0.213485) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0517771 * exp(-0.960584 * distance()) + 0.0204009 * exp(-0.334732 * distance())) * pow((width1() + width2()) / 2 , 0.213485)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal4,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0154176 * exp(-0.587256 * distance()) + 0.0154176 * exp(-0.58831 * distance())) * pow((width1() + width2()) / 2 , 0.323799) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0154176 * exp(-0.587256 * distance()) + 0.0154176 * exp(-0.58831 * distance())) * pow((width1() + width2()) / 2 , 0.323799)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0154176 * exp(-0.587256 * distance()) + 0.0154176 * exp(-0.58831 * distance())) * pow((width1() + width2()) / 2 , 0.323799) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0154176 * exp(-0.587256 * distance()) + 0.0154176 * exp(-0.58831 * distance())) * pow((width1() + width2()) / 2 , 0.323799)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0154176 * exp(-0.587256 * distance()) + 0.0154176 * exp(-0.58831 * distance())) * pow((width1() + width2()) / 2 , 0.323799) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0154176 * exp(-0.587256 * distance()) + 0.0154176 * exp(-0.58831 * distance())) * pow((width1() + width2()) / 2 , 0.323799)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0154176 * exp(-0.587256 * distance()) + 0.0154176 * exp(-0.58831 * distance())) * pow((width1() + width2()) / 2 , 0.323799) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0154176 * exp(-0.587256 * distance()) + 0.0154176 * exp(-0.58831 * distance())) * pow((width1() + width2()) / 2 , 0.323799)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal4,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0175113 * exp(-0.496895 * distance()) + 0.0175113 * exp(-0.495645 * distance())) * pow((width1() + width2()) / 2 , 0.296437) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0175113 * exp(-0.496895 * distance()) + 0.0175113 * exp(-0.495645 * distance())) * pow((width1() + width2()) / 2 , 0.296437)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0175113 * exp(-0.496895 * distance()) + 0.0175113 * exp(-0.495645 * distance())) * pow((width1() + width2()) / 2 , 0.296437) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0175113 * exp(-0.496895 * distance()) + 0.0175113 * exp(-0.495645 * distance())) * pow((width1() + width2()) / 2 , 0.296437)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0175113 * exp(-0.496895 * distance()) + 0.0175113 * exp(-0.495645 * distance())) * pow((width1() + width2()) / 2 , 0.296437) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0175113 * exp(-0.496895 * distance()) + 0.0175113 * exp(-0.495645 * distance())) * pow((width1() + width2()) / 2 , 0.296437)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0175113 * exp(-0.496895 * distance()) + 0.0175113 * exp(-0.495645 * distance())) * pow((width1() + width2()) / 2 , 0.296437) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0175113 * exp(-0.496895 * distance()) + 0.0175113 * exp(-0.495645 * distance())) * pow((width1() + width2()) / 2 , 0.296437)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal4,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0184798 * exp(-0.445747 * distance()) + 0.0184797 * exp(-0.445679 * distance())) * pow((width1() + width2()) / 2 , 0.291147) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0184798 * exp(-0.445747 * distance()) + 0.0184797 * exp(-0.445679 * distance())) * pow((width1() + width2()) / 2 , 0.291147)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0184798 * exp(-0.445747 * distance()) + 0.0184797 * exp(-0.445679 * distance())) * pow((width1() + width2()) / 2 , 0.291147) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0184798 * exp(-0.445747 * distance()) + 0.0184797 * exp(-0.445679 * distance())) * pow((width1() + width2()) / 2 , 0.291147)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0184798 * exp(-0.445747 * distance()) + 0.0184797 * exp(-0.445679 * distance())) * pow((width1() + width2()) / 2 , 0.291147) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0184798 * exp(-0.445747 * distance()) + 0.0184797 * exp(-0.445679 * distance())) * pow((width1() + width2()) / 2 , 0.291147)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0184798 * exp(-0.445747 * distance()) + 0.0184797 * exp(-0.445679 * distance())) * pow((width1() + width2()) / 2 , 0.291147) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0184798 * exp(-0.445747 * distance()) + 0.0184797 * exp(-0.445679 * distance())) * pow((width1() + width2()) / 2 , 0.291147)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal4,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0190854 * exp(-0.408975 * distance()) + 0.0190854 * exp(-0.409686 * distance())) * pow((width1() + width2()) / 2 , 0.289919) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0190854 * exp(-0.408975 * distance()) + 0.0190854 * exp(-0.409686 * distance())) * pow((width1() + width2()) / 2 , 0.289919)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0190854 * exp(-0.408975 * distance()) + 0.0190854 * exp(-0.409686 * distance())) * pow((width1() + width2()) / 2 , 0.289919) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0190854 * exp(-0.408975 * distance()) + 0.0190854 * exp(-0.409686 * distance())) * pow((width1() + width2()) / 2 , 0.289919)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal4 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0190854 * exp(-0.408975 * distance()) + 0.0190854 * exp(-0.409686 * distance())) * pow((width1() + width2()) / 2 , 0.289919) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0190854 * exp(-0.408975 * distance()) + 0.0190854 * exp(-0.409686 * distance())) * pow((width1() + width2()) / 2 , 0.289919)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal2 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0190854 * exp(-0.408975 * distance()) + 0.0190854 * exp(-0.409686 * distance())) * pow((width1() + width2()) / 2 , 0.289919) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0190854 * exp(-0.408975 * distance()) + 0.0190854 * exp(-0.409686 * distance())) * pow((width1() + width2()) / 2 , 0.289919)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal5,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00978412 * exp(-0.422426 * distance()) + 0.00978407 * exp(-0.422548 * distance())) * pow((width1() + width2()) / 2 , 0.382766) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00978412 * exp(-0.422426 * distance()) + 0.00978407 * exp(-0.422548 * distance())) * pow((width1() + width2()) / 2 , 0.382766)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00978412 * exp(-0.422426 * distance()) + 0.00978407 * exp(-0.422548 * distance())) * pow((width1() + width2()) / 2 , 0.382766) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00978412 * exp(-0.422426 * distance()) + 0.00978407 * exp(-0.422548 * distance())) * pow((width1() + width2()) / 2 , 0.382766)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00978412 * exp(-0.422426 * distance()) + 0.00978407 * exp(-0.422548 * distance())) * pow((width1() + width2()) / 2 , 0.382766) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00978412 * exp(-0.422426 * distance()) + 0.00978407 * exp(-0.422548 * distance())) * pow((width1() + width2()) / 2 , 0.382766)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00978412 * exp(-0.422426 * distance()) + 0.00978407 * exp(-0.422548 * distance())) * pow((width1() + width2()) / 2 , 0.382766) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00978412 * exp(-0.422426 * distance()) + 0.00978407 * exp(-0.422548 * distance())) * pow((width1() + width2()) / 2 , 0.382766)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal5,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114309 * exp(-0.358896 * distance()) + 0.0114307 * exp(-0.359169 * distance())) * pow((width1() + width2()) / 2 , 0.339177) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114309 * exp(-0.358896 * distance()) + 0.0114307 * exp(-0.359169 * distance())) * pow((width1() + width2()) / 2 , 0.339177)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114309 * exp(-0.358896 * distance()) + 0.0114307 * exp(-0.359169 * distance())) * pow((width1() + width2()) / 2 , 0.339177) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114309 * exp(-0.358896 * distance()) + 0.0114307 * exp(-0.359169 * distance())) * pow((width1() + width2()) / 2 , 0.339177)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114309 * exp(-0.358896 * distance()) + 0.0114307 * exp(-0.359169 * distance())) * pow((width1() + width2()) / 2 , 0.339177) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114309 * exp(-0.358896 * distance()) + 0.0114307 * exp(-0.359169 * distance())) * pow((width1() + width2()) / 2 , 0.339177)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114309 * exp(-0.358896 * distance()) + 0.0114307 * exp(-0.359169 * distance())) * pow((width1() + width2()) / 2 , 0.339177) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114309 * exp(-0.358896 * distance()) + 0.0114307 * exp(-0.359169 * distance())) * pow((width1() + width2()) / 2 , 0.339177)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal5,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0123706 * exp(-0.315512 * distance()) + 0.0123706 * exp(-0.315742 * distance())) * pow((width1() + width2()) / 2 , 0.325742) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0123706 * exp(-0.315512 * distance()) + 0.0123706 * exp(-0.315742 * distance())) * pow((width1() + width2()) / 2 , 0.325742)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0123706 * exp(-0.315512 * distance()) + 0.0123706 * exp(-0.315742 * distance())) * pow((width1() + width2()) / 2 , 0.325742) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0123706 * exp(-0.315512 * distance()) + 0.0123706 * exp(-0.315742 * distance())) * pow((width1() + width2()) / 2 , 0.325742)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal5 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0123706 * exp(-0.315512 * distance()) + 0.0123706 * exp(-0.315742 * distance())) * pow((width1() + width2()) / 2 , 0.325742) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0123706 * exp(-0.315512 * distance()) + 0.0123706 * exp(-0.315742 * distance())) * pow((width1() + width2()) / 2 , 0.325742)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal2 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0123706 * exp(-0.315512 * distance()) + 0.0123706 * exp(-0.315742 * distance())) * pow((width1() + width2()) / 2 , 0.325742) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0123706 * exp(-0.315512 * distance()) + 0.0123706 * exp(-0.315742 * distance())) * pow((width1() + width2()) / 2 , 0.325742)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal6,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00681824 * exp(-0.322301 * distance()) + 0.00681836 * exp(-0.321962 * distance())) * pow((width1() + width2()) / 2 , 0.416728) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00681824 * exp(-0.322301 * distance()) + 0.00681836 * exp(-0.321962 * distance())) * pow((width1() + width2()) / 2 , 0.416728)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00681824 * exp(-0.322301 * distance()) + 0.00681836 * exp(-0.321962 * distance())) * pow((width1() + width2()) / 2 , 0.416728) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00681824 * exp(-0.322301 * distance()) + 0.00681836 * exp(-0.321962 * distance())) * pow((width1() + width2()) / 2 , 0.416728)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00681824 * exp(-0.322301 * distance()) + 0.00681836 * exp(-0.321962 * distance())) * pow((width1() + width2()) / 2 , 0.416728) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00681824 * exp(-0.322301 * distance()) + 0.00681836 * exp(-0.321962 * distance())) * pow((width1() + width2()) / 2 , 0.416728)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00681824 * exp(-0.322301 * distance()) + 0.00681836 * exp(-0.321962 * distance())) * pow((width1() + width2()) / 2 , 0.416728) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00681824 * exp(-0.322301 * distance()) + 0.00681836 * exp(-0.321962 * distance())) * pow((width1() + width2()) / 2 , 0.416728)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal6,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00832403 * exp(-0.26783 * distance()) + 0.00832418 * exp(-0.26844 * distance())) * pow((width1() + width2()) / 2 , 0.359284) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00832403 * exp(-0.26783 * distance()) + 0.00832418 * exp(-0.26844 * distance())) * pow((width1() + width2()) / 2 , 0.359284)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00832403 * exp(-0.26783 * distance()) + 0.00832418 * exp(-0.26844 * distance())) * pow((width1() + width2()) / 2 , 0.359284) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00832403 * exp(-0.26783 * distance()) + 0.00832418 * exp(-0.26844 * distance())) * pow((width1() + width2()) / 2 , 0.359284)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal6 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00832403 * exp(-0.26783 * distance()) + 0.00832418 * exp(-0.26844 * distance())) * pow((width1() + width2()) / 2 , 0.359284) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00832403 * exp(-0.26783 * distance()) + 0.00832418 * exp(-0.26844 * distance())) * pow((width1() + width2()) / 2 , 0.359284)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal2 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00832403 * exp(-0.26783 * distance()) + 0.00832418 * exp(-0.26844 * distance())) * pow((width1() + width2()) / 2 , 0.359284) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00832403 * exp(-0.26783 * distance()) + 0.00832418 * exp(-0.26844 * distance())) * pow((width1() + width2()) / 2 , 0.359284)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal2,metal7,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal7 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00533163 * exp(-0.24371 * distance()) + 0.00533173 * exp(-0.243604 * distance())) * pow((width1() + width2()) / 2 , 0.418683) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00533163 * exp(-0.24371 * distance()) + 0.00533173 * exp(-0.243604 * distance())) * pow((width1() + width2()) / 2 , 0.418683)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal2 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00533163 * exp(-0.24371 * distance()) + 0.00533173 * exp(-0.243604 * distance())) * pow((width1() + width2()) / 2 , 0.418683) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00533163 * exp(-0.24371 * distance()) + 0.00533173 * exp(-0.243604 * distance())) * pow((width1() + width2()) / 2 , 0.418683)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal2 metal7 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00533163 * exp(-0.24371 * distance()) + 0.00533173 * exp(-0.243604 * distance())) * pow((width1() + width2()) / 2 , 0.418683) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00533163 * exp(-0.24371 * distance()) + 0.00533173 * exp(-0.243604 * distance())) * pow((width1() + width2()) / 2 , 0.418683)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal2 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal2
    max_distance = (MAXDISTANCE_metal2 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00533163 * exp(-0.24371 * distance()) + 0.00533173 * exp(-0.243604 * distance())) * pow((width1() + width2()) / 2 , 0.418683) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00533163 * exp(-0.24371 * distance()) + 0.00533173 * exp(-0.243604 * distance())) * pow((width1() + width2()) / 2 , 0.418683)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal4,metal5,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.000259151 * exp(0.0997672 * distance()) + 0.063559 * exp(-0.941361 * distance())) * pow((width1() + width2()) / 2 , 0.212888) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.000259151 * exp(0.0997672 * distance()) + 0.063559 * exp(-0.941361 * distance())) * pow((width1() + width2()) / 2 , 0.212888)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF tndiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.000259151 * exp(0.0997672 * distance()) + 0.063559 * exp(-0.941361 * distance())) * pow((width1() + width2()) / 2 , 0.212888) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.000259151 * exp(0.0997672 * distance()) + 0.063559 * exp(-0.941361 * distance())) * pow((width1() + width2()) / 2 , 0.212888)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.000259151 * exp(0.0997672 * distance()) + 0.063559 * exp(-0.941361 * distance())) * pow((width1() + width2()) / 2 , 0.212888) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.000259151 * exp(0.0997672 * distance()) + 0.063559 * exp(-0.941361 * distance())) * pow((width1() + width2()) / 2 , 0.212888)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF tpdiff metal5 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.000259151 * exp(0.0997672 * distance()) + 0.063559 * exp(-0.941361 * distance())) * pow((width1() + width2()) / 2 , 0.212888) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.000259151 * exp(0.0997672 * distance()) + 0.063559 * exp(-0.941361 * distance())) * pow((width1() + width2()) / 2 , 0.212888)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal4,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0138709 * exp(-0.408301 * distance()) + 0.0561826 * exp(-0.913528 * distance())) * pow((width1() + width2()) / 2 , 0.20652) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0138709 * exp(-0.408301 * distance()) + 0.0561826 * exp(-0.913528 * distance())) * pow((width1() + width2()) / 2 , 0.20652)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0138709 * exp(-0.408301 * distance()) + 0.0561826 * exp(-0.913528 * distance())) * pow((width1() + width2()) / 2 , 0.20652) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0138709 * exp(-0.408301 * distance()) + 0.0561826 * exp(-0.913528 * distance())) * pow((width1() + width2()) / 2 , 0.20652)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0138709 * exp(-0.408301 * distance()) + 0.0561826 * exp(-0.913528 * distance())) * pow((width1() + width2()) / 2 , 0.20652) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0138709 * exp(-0.408301 * distance()) + 0.0561826 * exp(-0.913528 * distance())) * pow((width1() + width2()) / 2 , 0.20652)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0138709 * exp(-0.408301 * distance()) + 0.0561826 * exp(-0.913528 * distance())) * pow((width1() + width2()) / 2 , 0.20652) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0138709 * exp(-0.408301 * distance()) + 0.0561826 * exp(-0.913528 * distance())) * pow((width1() + width2()) / 2 , 0.20652)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal4,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0463327 * exp(-0.983415 * distance()) + 0.0265946 * exp(-0.405278 * distance())) * pow((width1() + width2()) / 2 , 0.208634) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0463327 * exp(-0.983415 * distance()) + 0.0265946 * exp(-0.405278 * distance())) * pow((width1() + width2()) / 2 , 0.208634)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0463327 * exp(-0.983415 * distance()) + 0.0265946 * exp(-0.405278 * distance())) * pow((width1() + width2()) / 2 , 0.208634) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0463327 * exp(-0.983415 * distance()) + 0.0265946 * exp(-0.405278 * distance())) * pow((width1() + width2()) / 2 , 0.208634)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0463327 * exp(-0.983415 * distance()) + 0.0265946 * exp(-0.405278 * distance())) * pow((width1() + width2()) / 2 , 0.208634) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0463327 * exp(-0.983415 * distance()) + 0.0265946 * exp(-0.405278 * distance())) * pow((width1() + width2()) / 2 , 0.208634)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0463327 * exp(-0.983415 * distance()) + 0.0265946 * exp(-0.405278 * distance())) * pow((width1() + width2()) / 2 , 0.208634) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0463327 * exp(-0.983415 * distance()) + 0.0265946 * exp(-0.405278 * distance())) * pow((width1() + width2()) / 2 , 0.208634)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal4,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0290812 * exp(-0.350764 * distance()) + 0.0456712 * exp(-1.0035 * distance())) * pow((width1() + width2()) / 2 , 0.21133) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0290812 * exp(-0.350764 * distance()) + 0.0456712 * exp(-1.0035 * distance())) * pow((width1() + width2()) / 2 , 0.21133)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0290812 * exp(-0.350764 * distance()) + 0.0456712 * exp(-1.0035 * distance())) * pow((width1() + width2()) / 2 , 0.21133) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0290812 * exp(-0.350764 * distance()) + 0.0456712 * exp(-1.0035 * distance())) * pow((width1() + width2()) / 2 , 0.21133)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal4 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0290812 * exp(-0.350764 * distance()) + 0.0456712 * exp(-1.0035 * distance())) * pow((width1() + width2()) / 2 , 0.21133) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0290812 * exp(-0.350764 * distance()) + 0.0456712 * exp(-1.0035 * distance())) * pow((width1() + width2()) / 2 , 0.21133)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal3 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal4) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0290812 * exp(-0.350764 * distance()) + 0.0456712 * exp(-1.0035 * distance())) * pow((width1() + width2()) / 2 , 0.21133) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0290812 * exp(-0.350764 * distance()) + 0.0456712 * exp(-1.0035 * distance())) * pow((width1() + width2()) / 2 , 0.21133)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal5,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0162635 * exp(-0.526783 * distance()) + 0.0162634 * exp(-0.526482 * distance())) * pow((width1() + width2()) / 2 , 0.322571) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0162635 * exp(-0.526783 * distance()) + 0.0162634 * exp(-0.526482 * distance())) * pow((width1() + width2()) / 2 , 0.322571)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0162635 * exp(-0.526783 * distance()) + 0.0162634 * exp(-0.526482 * distance())) * pow((width1() + width2()) / 2 , 0.322571) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0162635 * exp(-0.526783 * distance()) + 0.0162634 * exp(-0.526482 * distance())) * pow((width1() + width2()) / 2 , 0.322571)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0162635 * exp(-0.526783 * distance()) + 0.0162634 * exp(-0.526482 * distance())) * pow((width1() + width2()) / 2 , 0.322571) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0162635 * exp(-0.526783 * distance()) + 0.0162634 * exp(-0.526482 * distance())) * pow((width1() + width2()) / 2 , 0.322571)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0162635 * exp(-0.526783 * distance()) + 0.0162634 * exp(-0.526482 * distance())) * pow((width1() + width2()) / 2 , 0.322571) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0162635 * exp(-0.526783 * distance()) + 0.0162634 * exp(-0.526482 * distance())) * pow((width1() + width2()) / 2 , 0.322571)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal5,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0185799 * exp(-0.443422 * distance()) + 0.01858 * exp(-0.443566 * distance())) * pow((width1() + width2()) / 2 , 0.289846) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0185799 * exp(-0.443422 * distance()) + 0.01858 * exp(-0.443566 * distance())) * pow((width1() + width2()) / 2 , 0.289846)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0185799 * exp(-0.443422 * distance()) + 0.01858 * exp(-0.443566 * distance())) * pow((width1() + width2()) / 2 , 0.289846) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0185799 * exp(-0.443422 * distance()) + 0.01858 * exp(-0.443566 * distance())) * pow((width1() + width2()) / 2 , 0.289846)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0185799 * exp(-0.443422 * distance()) + 0.01858 * exp(-0.443566 * distance())) * pow((width1() + width2()) / 2 , 0.289846) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0185799 * exp(-0.443422 * distance()) + 0.01858 * exp(-0.443566 * distance())) * pow((width1() + width2()) / 2 , 0.289846)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0185799 * exp(-0.443422 * distance()) + 0.01858 * exp(-0.443566 * distance())) * pow((width1() + width2()) / 2 , 0.289846) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0185799 * exp(-0.443422 * distance()) + 0.01858 * exp(-0.443566 * distance())) * pow((width1() + width2()) / 2 , 0.289846)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal5,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0198361 * exp(-0.390774 * distance()) + 0.0198361 * exp(-0.390261 * distance())) * pow((width1() + width2()) / 2 , 0.281212) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0198361 * exp(-0.390774 * distance()) + 0.0198361 * exp(-0.390261 * distance())) * pow((width1() + width2()) / 2 , 0.281212)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0198361 * exp(-0.390774 * distance()) + 0.0198361 * exp(-0.390261 * distance())) * pow((width1() + width2()) / 2 , 0.281212) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0198361 * exp(-0.390774 * distance()) + 0.0198361 * exp(-0.390261 * distance())) * pow((width1() + width2()) / 2 , 0.281212)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal5 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0198361 * exp(-0.390774 * distance()) + 0.0198361 * exp(-0.390261 * distance())) * pow((width1() + width2()) / 2 , 0.281212) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0198361 * exp(-0.390774 * distance()) + 0.0198361 * exp(-0.390261 * distance())) * pow((width1() + width2()) / 2 , 0.281212)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal3 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0198361 * exp(-0.390774 * distance()) + 0.0198361 * exp(-0.390261 * distance())) * pow((width1() + width2()) / 2 , 0.281212) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0198361 * exp(-0.390774 * distance()) + 0.0198361 * exp(-0.390261 * distance())) * pow((width1() + width2()) / 2 , 0.281212)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal6,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.010485 * exp(-0.377063 * distance()) + 0.010485 * exp(-0.376257 * distance())) * pow((width1() + width2()) / 2 , 0.372795) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.010485 * exp(-0.377063 * distance()) + 0.010485 * exp(-0.376257 * distance())) * pow((width1() + width2()) / 2 , 0.372795)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.010485 * exp(-0.377063 * distance()) + 0.010485 * exp(-0.376257 * distance())) * pow((width1() + width2()) / 2 , 0.372795) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.010485 * exp(-0.377063 * distance()) + 0.010485 * exp(-0.376257 * distance())) * pow((width1() + width2()) / 2 , 0.372795)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.010485 * exp(-0.377063 * distance()) + 0.010485 * exp(-0.376257 * distance())) * pow((width1() + width2()) / 2 , 0.372795) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.010485 * exp(-0.377063 * distance()) + 0.010485 * exp(-0.376257 * distance())) * pow((width1() + width2()) / 2 , 0.372795)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.010485 * exp(-0.377063 * distance()) + 0.010485 * exp(-0.376257 * distance())) * pow((width1() + width2()) / 2 , 0.372795) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.010485 * exp(-0.377063 * distance()) + 0.010485 * exp(-0.376257 * distance())) * pow((width1() + width2()) / 2 , 0.372795)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal6,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0125707 * exp(-0.31186 * distance()) + 0.0125708 * exp(-0.311741 * distance())) * pow((width1() + width2()) / 2 , 0.321453) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0125707 * exp(-0.31186 * distance()) + 0.0125708 * exp(-0.311741 * distance())) * pow((width1() + width2()) / 2 , 0.321453)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0125707 * exp(-0.31186 * distance()) + 0.0125708 * exp(-0.311741 * distance())) * pow((width1() + width2()) / 2 , 0.321453) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0125707 * exp(-0.31186 * distance()) + 0.0125708 * exp(-0.311741 * distance())) * pow((width1() + width2()) / 2 , 0.321453)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal6 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0125707 * exp(-0.31186 * distance()) + 0.0125708 * exp(-0.311741 * distance())) * pow((width1() + width2()) / 2 , 0.321453) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0125707 * exp(-0.31186 * distance()) + 0.0125708 * exp(-0.311741 * distance())) * pow((width1() + width2()) / 2 , 0.321453)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal3 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0125707 * exp(-0.31186 * distance()) + 0.0125708 * exp(-0.311741 * distance())) * pow((width1() + width2()) / 2 , 0.321453) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0125707 * exp(-0.31186 * distance()) + 0.0125708 * exp(-0.311741 * distance())) * pow((width1() + width2()) / 2 , 0.321453)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal3,metal7,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal7 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0078364 * exp(-0.275482 * distance()) + 0.00783637 * exp(-0.276148 * distance())) * pow((width1() + width2()) / 2 , 0.381858) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0078364 * exp(-0.275482 * distance()) + 0.00783637 * exp(-0.276148 * distance())) * pow((width1() + width2()) / 2 , 0.381858)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal3 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0078364 * exp(-0.275482 * distance()) + 0.00783637 * exp(-0.276148 * distance())) * pow((width1() + width2()) / 2 , 0.381858) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0078364 * exp(-0.275482 * distance()) + 0.00783637 * exp(-0.276148 * distance())) * pow((width1() + width2()) / 2 , 0.381858)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal3 metal7 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0078364 * exp(-0.275482 * distance()) + 0.00783637 * exp(-0.276148 * distance())) * pow((width1() + width2()) / 2 , 0.381858) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0078364 * exp(-0.275482 * distance()) + 0.00783637 * exp(-0.276148 * distance())) * pow((width1() + width2()) / 2 , 0.381858)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal3 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal3
    max_distance = (MAXDISTANCE_metal3 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0078364 * exp(-0.275482 * distance()) + 0.00783637 * exp(-0.276148 * distance())) * pow((width1() + width2()) / 2 , 0.381858) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0078364 * exp(-0.275482 * distance()) + 0.00783637 * exp(-0.276148 * distance())) * pow((width1() + width2()) / 2 , 0.381858)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,metal5,metal6,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0621745 * exp(-0.940128 * distance()) + 0.00285761 * exp(-0.199 * distance())) * pow((width1() + width2()) / 2 , 0.219141) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0621745 * exp(-0.940128 * distance()) + 0.00285761 * exp(-0.199 * distance())) * pow((width1() + width2()) / 2 , 0.219141)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF tndiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0621745 * exp(-0.940128 * distance()) + 0.00285761 * exp(-0.199 * distance())) * pow((width1() + width2()) / 2 , 0.219141) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0621745 * exp(-0.940128 * distance()) + 0.00285761 * exp(-0.199 * distance())) * pow((width1() + width2()) / 2 , 0.219141)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0621745 * exp(-0.940128 * distance()) + 0.00285761 * exp(-0.199 * distance())) * pow((width1() + width2()) / 2 , 0.219141) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0621745 * exp(-0.940128 * distance()) + 0.00285761 * exp(-0.199 * distance())) * pow((width1() + width2()) / 2 , 0.219141)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF tpdiff metal6 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0621745 * exp(-0.940128 * distance()) + 0.00285761 * exp(-0.199 * distance())) * pow((width1() + width2()) / 2 , 0.219141) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0621745 * exp(-0.940128 * distance()) + 0.00285761 * exp(-0.199 * distance())) * pow((width1() + width2()) / 2 , 0.219141)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,metal5,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0514427 * exp(-0.951056 * distance()) + 0.0201445 * exp(-0.38282 * distance())) * pow((width1() + width2()) / 2 , 0.210202) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0514427 * exp(-0.951056 * distance()) + 0.0201445 * exp(-0.38282 * distance())) * pow((width1() + width2()) / 2 , 0.210202)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0514427 * exp(-0.951056 * distance()) + 0.0201445 * exp(-0.38282 * distance())) * pow((width1() + width2()) / 2 , 0.210202) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0514427 * exp(-0.951056 * distance()) + 0.0201445 * exp(-0.38282 * distance())) * pow((width1() + width2()) / 2 , 0.210202)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0514427 * exp(-0.951056 * distance()) + 0.0201445 * exp(-0.38282 * distance())) * pow((width1() + width2()) / 2 , 0.210202) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0514427 * exp(-0.951056 * distance()) + 0.0201445 * exp(-0.38282 * distance())) * pow((width1() + width2()) / 2 , 0.210202)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0514427 * exp(-0.951056 * distance()) + 0.0201445 * exp(-0.38282 * distance())) * pow((width1() + width2()) / 2 , 0.210202) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0514427 * exp(-0.951056 * distance()) + 0.0201445 * exp(-0.38282 * distance())) * pow((width1() + width2()) / 2 , 0.210202)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,metal5,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0301692 * exp(-0.351633 * distance()) + 0.0448955 * exp(-1.0108 * distance())) * pow((width1() + width2()) / 2 , 0.211327) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0301692 * exp(-0.351633 * distance()) + 0.0448955 * exp(-1.0108 * distance())) * pow((width1() + width2()) / 2 , 0.211327)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0301692 * exp(-0.351633 * distance()) + 0.0448955 * exp(-1.0108 * distance())) * pow((width1() + width2()) / 2 , 0.211327) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0301692 * exp(-0.351633 * distance()) + 0.0448955 * exp(-1.0108 * distance())) * pow((width1() + width2()) / 2 , 0.211327)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal5 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0301692 * exp(-0.351633 * distance()) + 0.0448955 * exp(-1.0108 * distance())) * pow((width1() + width2()) / 2 , 0.211327) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0301692 * exp(-0.351633 * distance()) + 0.0448955 * exp(-1.0108 * distance())) * pow((width1() + width2()) / 2 , 0.211327)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal4 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal5) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0301692 * exp(-0.351633 * distance()) + 0.0448955 * exp(-1.0108 * distance())) * pow((width1() + width2()) / 2 , 0.211327) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0301692 * exp(-0.351633 * distance()) + 0.0448955 * exp(-1.0108 * distance())) * pow((width1() + width2()) / 2 , 0.211327)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,metal6,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0167052 * exp(-0.489267 * distance()) + 0.0167052 * exp(-0.48912 * distance())) * pow((width1() + width2()) / 2 , 0.323875) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0167052 * exp(-0.489267 * distance()) + 0.0167052 * exp(-0.48912 * distance())) * pow((width1() + width2()) / 2 , 0.323875)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0167052 * exp(-0.489267 * distance()) + 0.0167052 * exp(-0.48912 * distance())) * pow((width1() + width2()) / 2 , 0.323875) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0167052 * exp(-0.489267 * distance()) + 0.0167052 * exp(-0.48912 * distance())) * pow((width1() + width2()) / 2 , 0.323875)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0167052 * exp(-0.489267 * distance()) + 0.0167052 * exp(-0.48912 * distance())) * pow((width1() + width2()) / 2 , 0.323875) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0167052 * exp(-0.489267 * distance()) + 0.0167052 * exp(-0.48912 * distance())) * pow((width1() + width2()) / 2 , 0.323875)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0167052 * exp(-0.489267 * distance()) + 0.0167052 * exp(-0.48912 * distance())) * pow((width1() + width2()) / 2 , 0.323875) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0167052 * exp(-0.489267 * distance()) + 0.0167052 * exp(-0.48912 * distance())) * pow((width1() + width2()) / 2 , 0.323875)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,metal6,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0194581 * exp(-0.400246 * distance()) + 0.0194581 * exp(-0.399912 * distance())) * pow((width1() + width2()) / 2 , 0.285816) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0194581 * exp(-0.400246 * distance()) + 0.0194581 * exp(-0.399912 * distance())) * pow((width1() + width2()) / 2 , 0.285816)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0194581 * exp(-0.400246 * distance()) + 0.0194581 * exp(-0.399912 * distance())) * pow((width1() + width2()) / 2 , 0.285816) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0194581 * exp(-0.400246 * distance()) + 0.0194581 * exp(-0.399912 * distance())) * pow((width1() + width2()) / 2 , 0.285816)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal6 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0194581 * exp(-0.400246 * distance()) + 0.0194581 * exp(-0.399912 * distance())) * pow((width1() + width2()) / 2 , 0.285816) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0194581 * exp(-0.400246 * distance()) + 0.0194581 * exp(-0.399912 * distance())) * pow((width1() + width2()) / 2 , 0.285816)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal4 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0194581 * exp(-0.400246 * distance()) + 0.0194581 * exp(-0.399912 * distance())) * pow((width1() + width2()) / 2 , 0.285816) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0194581 * exp(-0.400246 * distance()) + 0.0194581 * exp(-0.399912 * distance())) * pow((width1() + width2()) / 2 , 0.285816)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal4,metal7,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal7 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114429 * exp(-0.333316 * distance()) + 0.0114429 * exp(-0.333563 * distance())) * pow((width1() + width2()) / 2 , 0.35249) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114429 * exp(-0.333316 * distance()) + 0.0114429 * exp(-0.333563 * distance())) * pow((width1() + width2()) / 2 , 0.35249)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal4 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114429 * exp(-0.333316 * distance()) + 0.0114429 * exp(-0.333563 * distance())) * pow((width1() + width2()) / 2 , 0.35249) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114429 * exp(-0.333316 * distance()) + 0.0114429 * exp(-0.333563 * distance())) * pow((width1() + width2()) / 2 , 0.35249)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal4 metal7 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114429 * exp(-0.333316 * distance()) + 0.0114429 * exp(-0.333563 * distance())) * pow((width1() + width2()) / 2 , 0.35249) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114429 * exp(-0.333316 * distance()) + 0.0114429 * exp(-0.333563 * distance())) * pow((width1() + width2()) / 2 , 0.35249)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal4 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal4
    max_distance = (MAXDISTANCE_metal4 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0114429 * exp(-0.333316 * distance()) + 0.0114429 * exp(-0.333563 * distance())) * pow((width1() + width2()) / 2 , 0.35249) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0114429 * exp(-0.333316 * distance()) + 0.0114429 * exp(-0.333563 * distance())) * pow((width1() + width2()) / 2 , 0.35249)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal5,metal6,metal7,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0601578 * exp(-0.95402 * distance()) + 0.00559021 * exp(-0.242363 * distance())) * pow((width1() + width2()) / 2 , 0.223104) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0601578 * exp(-0.95402 * distance()) + 0.00559021 * exp(-0.242363 * distance())) * pow((width1() + width2()) / 2 , 0.223104)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF tndiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0601578 * exp(-0.95402 * distance()) + 0.00559021 * exp(-0.242363 * distance())) * pow((width1() + width2()) / 2 , 0.223104) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0601578 * exp(-0.95402 * distance()) + 0.00559021 * exp(-0.242363 * distance())) * pow((width1() + width2()) / 2 , 0.223104)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0601578 * exp(-0.95402 * distance()) + 0.00559021 * exp(-0.242363 * distance())) * pow((width1() + width2()) / 2 , 0.223104) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0601578 * exp(-0.95402 * distance()) + 0.00559021 * exp(-0.242363 * distance())) * pow((width1() + width2()) / 2 , 0.223104)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF tpdiff metal7 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0601578 * exp(-0.95402 * distance()) + 0.00559021 * exp(-0.242363 * distance())) * pow((width1() + width2()) / 2 , 0.223104) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0601578 * exp(-0.95402 * distance()) + 0.00559021 * exp(-0.242363 * distance())) * pow((width1() + width2()) / 2 , 0.223104)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal5,metal6,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0241591 * exp(-0.344945 * distance()) + 0.0491425 * exp(-0.976056 * distance())) * pow((width1() + width2()) / 2 , 0.2128) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0241591 * exp(-0.344945 * distance()) + 0.0491425 * exp(-0.976056 * distance())) * pow((width1() + width2()) / 2 , 0.2128)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0241591 * exp(-0.344945 * distance()) + 0.0491425 * exp(-0.976056 * distance())) * pow((width1() + width2()) / 2 , 0.2128) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0241591 * exp(-0.344945 * distance()) + 0.0491425 * exp(-0.976056 * distance())) * pow((width1() + width2()) / 2 , 0.2128)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal6 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0241591 * exp(-0.344945 * distance()) + 0.0491425 * exp(-0.976056 * distance())) * pow((width1() + width2()) / 2 , 0.2128) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0241591 * exp(-0.344945 * distance()) + 0.0491425 * exp(-0.976056 * distance())) * pow((width1() + width2()) / 2 , 0.2128)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal5 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal6) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0241591 * exp(-0.344945 * distance()) + 0.0491425 * exp(-0.976056 * distance())) * pow((width1() + width2()) / 2 , 0.2128) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0241591 * exp(-0.344945 * distance()) + 0.0491425 * exp(-0.976056 * distance())) * pow((width1() + width2()) / 2 , 0.2128)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal5,metal7,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0177214 * exp(-0.443646 * distance()) + 0.0177214 * exp(-0.443787 * distance())) * pow((width1() + width2()) / 2 , 0.312193) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0177214 * exp(-0.443646 * distance()) + 0.0177214 * exp(-0.443787 * distance())) * pow((width1() + width2()) / 2 , 0.312193)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0177214 * exp(-0.443646 * distance()) + 0.0177214 * exp(-0.443787 * distance())) * pow((width1() + width2()) / 2 , 0.312193) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0177214 * exp(-0.443646 * distance()) + 0.0177214 * exp(-0.443787 * distance())) * pow((width1() + width2()) / 2 , 0.312193)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal5 metal7 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0177214 * exp(-0.443646 * distance()) + 0.0177214 * exp(-0.443787 * distance())) * pow((width1() + width2()) / 2 , 0.312193) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0177214 * exp(-0.443646 * distance()) + 0.0177214 * exp(-0.443787 * distance())) * pow((width1() + width2()) / 2 , 0.312193)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal5 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal5
    max_distance = (MAXDISTANCE_metal5 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.0177214 * exp(-0.443646 * distance()) + 0.0177214 * exp(-0.443787 * distance())) * pow((width1() + width2()) / 2 , 0.312193) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.0177214 * exp(-0.443646 * distance()) + 0.0177214 * exp(-0.443787 * distance())) * pow((width1() + width2()) / 2 , 0.312193)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 414  target: nearbody2  profile: diffcap  layers: diffcap_base,diff2,metal6,metal7,metal8,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00968663 * exp(-0.265864 * distance()) + 0.0587101 * exp(-0.939237 * distance())) * pow((width1() + width2()) / 2 , 0.221666) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00968663 * exp(-0.265864 * distance()) + 0.0587101 * exp(-0.939237 * distance())) * pow((width1() + width2()) / 2 , 0.221666)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF tndiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00968663 * exp(-0.265864 * distance()) + 0.0587101 * exp(-0.939237 * distance())) * pow((width1() + width2()) / 2 , 0.221666) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00968663 * exp(-0.265864 * distance()) + 0.0587101 * exp(-0.939237 * distance())) * pow((width1() + width2()) / 2 , 0.221666)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal6 metal7 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00968663 * exp(-0.265864 * distance()) + 0.0587101 * exp(-0.939237 * distance())) * pow((width1() + width2()) / 2 , 0.221666) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00968663 * exp(-0.265864 * distance()) + 0.0587101 * exp(-0.939237 * distance())) * pow((width1() + width2()) / 2 , 0.221666)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY metal7 metal6 INSIDE OF tpdiff metal8 MASK
[
    PROPERTY C
    max_calwidth = 1.2 // For reference
    max_caldistance = 10 // For reference
    min_calwidth = 0.22 // For reference
    max_width = MAXWIDTH_metal6
    max_distance = (MAXDISTANCE_metal6 + MAXDISTANCE_metal7) / 2
    C = 0.0
    if (distance2() > 0.0) {
        C = length() * (0.00968663 * exp(-0.265864 * distance()) + 0.0587101 * exp(-0.939237 * distance())) * pow((width1() + width2()) / 2 , 0.221666) * (1 - exp((-m514k1 * distance2()) / (distance() + m514k2)))
    }
    if (distance2() <= 0.0) {
        C = length() * (0.00968663 * exp(-0.265864 * distance()) + 0.0587101 * exp(-0.939237 * distance())) * pow((width1() + width2()) / 2 , 0.221666)
    }
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
//
//  model: 1034  target: nearbody  profile: diffcap  layers: diffcap_base,diff2,tndiff,tpdiff
//
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff WITH SHIELD tndiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 4.95 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = MAXDISTANCE_diff2
    C = length() * (exp(-4.68322 - 0.0232051 * distance()) + 0.0170951 / pow(distance() , 0.783748)) * 1.52905 * pow((width1() + width2()) / 2 , 0.124594)
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tpdiff WITH SHIELD tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 4.95 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = MAXDISTANCE_diff2
    C = length() * (exp(-4.68322 - 0.0232051 * distance()) + 0.0170951 / pow(distance() , 0.783748)) * 1.52905 * pow((width1() + width2()) / 2 , 0.124594)
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
// #DEFINE $XCALIBRATE_INCLUDE_PROFILE_diffcap // uncomment out to compile following section
#IFDEF $XCALIBRATE_INCLUDE_PROFILE_diffcap
CAPACITANCE NEARBODY tndiff tpdiff MASK
[
    PROPERTY C
    max_calwidth = 0.6 // For reference
    max_caldistance = 4.95 // For reference
    max_width = MAXWIDTH_diff2
    max_distance = MAXDISTANCE_diff2
    C = length() * (exp(-4.68322 - 0.0232051 * distance()) + 0.0170951 / pow(distance() , 0.783748)) * 1.52905 * pow((width1() + width2()) / 2 , 0.124594)
]
#endif // $XCALIBRATE_INCLUDE_PROFILE_diffcap
#endif // XCALIBRATE_EXCLUDE_PROFILE_diffcap
//
// Completed on Wed Apr 19 22:52:30 2006 by xCalibrate v2006.1_19.19 Tue Mar  7 12:29:27 PST 2006
//
//attach the following commands to support both xCalibre and XRC tools.
CAPACITANCE CROSSOVER PLATE gate poly
[
    PROPERTY C
      C = 0
]
CAPACITANCE CROSSOVER FRINGE poly tndiff
[
    PROPERTY C
      C = 0
]
CAPACITANCE CROSSOVER FRINGE poly tpdiff
[
    PROPERTY C
      C = 0
]


//End ./rules 

#ELSE

//#############################################
//# ERC CHECK                                 #
//#############################################
// Reports regions on a psub_term layer that connect to more than one node.
LVS SOFTCHK psub_term CONTACT
// Reports regions on a n_psub layer that connect to more than one node.
LVS SOFTCHK n_psub CONTACT
// Reports regions on a psub layer that connect to more than one node.
LVS SOFTCHK psub CONTACT
// Reports regions on a coll1 layer that connect to more than one node.
LVS SOFTCHK coll1 CONTACT
// Reports regions on a poly_term layer that connect to more than one node.
LVS SOFTCHK poly_term CONTACT
// Reports regions on a nxwell layer that connect to more than one node.
LVS SOFTCHK nxwell CONTACT
// Reports regions on a poly_term layer that connect to more than one node.
LVS SOFTCHK poly_term CONTACT
// Reports regions on a DNW layer that connect to more than one node.
LVS SOFTCHK DNW CONTACT

 // MOS P S/D connect to POWER&GROUND --mppg*/
N1tpdiff = NET tpdiff POWER_NAME
T1tpdiff =  pgate TOUCH N1tpdiff
N2tpdiff = NET tpdiff GROUND_NAME
T2tpdiff =  pgate TOUCH N2tpdiff
mppg { @ MOS connected to both power and ground
 T1tpdiff AND T2tpdiff
}

 // MOS N S/D connect to POWER&GROUND --mnpg*/
N1tndiff = NET tndiff POWER_NAME
T1tndiff = ngate  TOUCH N1tndiff
N2tndiff = NET tndiff GROUND_NAME
T2tndiff = ngate  TOUCH N2tndiff
mnpg { @ MOS connected to both power and ground
 T1tndiff AND T2tndiff
}

 // ptap connect to power 
ppvdd49 { @ ptap connected to POWER
	NET ptap POWER_NAME
}
 gate1_not_IO1 = gate1 NOT OD2
 CONNECT gate1_not_IO1 poly
 gate1_PWR = NET gate1_not_IO1 POWER_NAME

 // gate1 connect to power 
pppow50 { @ gate1 connected to POWER
 gate1_PWR1 = gate1_PWR INTERACT N1tpdiff == 2
 gate1_PWR NOT gate1_PWR1
}

// Floating gate no any CONT interact with the POLYG.
floating { @ Please check the floating gate
  POLYG_no_CONT = POLYG NOT INTERACT CONT
  gate_no_co = POLYG_no_CONT AND diff
  gate_no_co1 = gate_no_co NOT RFDMY
  POLYG_no_CONT INTERACT gate_no_co1
}

// Nodes with a path to power but not ground 
ERC PATHCHK GROUND && !POWER

// Nodes with a path to ground but not power 
ERC PATHCHK POWER && !GROUND

// Nodes without a path to power and ground 
ERC PATHCHK !POWER && !GROUND NOFLOAT
ERC PATHCHK !LABELED NOFLOAT

 // ntap connect to ground 
npvss49 { @ ntap connected to GROUND
 ntap_not_var = ntap NOT VARDMY
	NET ntap_not_var GROUND_NAME
}
 gate1_GND = NET gate1_not_IO1 GROUND_NAME

 // gate1 connect to ground 
npgnd50 { @ gate1 connected to GROUND
 gate1_GND1 = gate1_GND INTERACT N2tndiff == 2
 gate1_GND NOT gate1_GND1
}

#ENDIF


//#########################################################################
//		END
//#########################################################################
